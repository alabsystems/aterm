\* Fixed variant: the cap reads the CONTENT-only clock (content_pending). The efficiency
\* theorem ContentIdleEchoImmediate holds under exhaustive BFS.
CONSTANT Buggy = 0
CONSTANT FrameInterval = 2
CONSTANT MaxTime = 6
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT ContentIdleEchoImmediate
CHECK_DEADLOCK FALSE
