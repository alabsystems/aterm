// Copyright 2026 Andrew Yates
// SPDX-License-Identifier: Apache-2.0

//! `atpkg` — the aterm toolchain package manager CLI.
//!
//! The local read/maintenance verbs (`which`/`list`/`uninstall`) and `doctor` are wired
//! here over the tested library (`crate::ops`, `crate::store`); the network-driven verbs
//! (`install`/`update`/`rollback`, plus the `install --default-set` bootstrap)
//! compose the same tested primitives with the GitHub/dir fetch. Every network verb reads
//! the `[packages]` table of the SAME `aterm.toml` the GUI owns ([`crate::config`]) —
//! account/channel/include/exclude/links — with env always winning over config. The
//! verification anchor is the COMMITTED paper-master keyset [`crate::PKG_TRUST_ANCHORS`]
//! (`aterm_update_core::pins::PAPER_MASTER_PUBKEYS`) — the SAME root the app updater uses
//! — and nothing else: no env var or build-time
//! variable can supply or swap it (see [`effective_anchor`]); a tree whose master keyset
//! is empty builds a manager that is INERT and says so. THIS tree is ARMED
//! (2026-08-15), so every verb below is live — as this module's own test
//! `no_environment_variable_can_supply_the_verification_anchor` asserts.

use std::process::ExitCode;

use crate::flow::now_unix;

/// Every verb the dispatch below accepts, in the order the unknown-verb hint
/// prints them.
///
/// Kept as DATA, and checked against the `match` by
/// [`tests::verb_hint_matches_dispatch`], because the hint has drifted from
/// reality twice: `sync` (deleted in `a43193f4`) and the source-build-era
/// `seed` (deleted in `ba832933`; the verb returned 2026-08-17 as the signed
/// bundled-seed bootstrap only) were both still advertised here long after
/// they stopped dispatching — so a user who followed the suggestion got the
/// very error that printed it. A hand-maintained help string cannot be
/// trusted to track a match arm; a test can.
/// The dispatch roster, for anything that DOCUMENTS these verbs.
///
/// Public so a prose gate in a sibling crate can pin what the manual says
/// against what actually runs. The manual claimed that property already — "the
/// roster `aterm pkg --help` prints is test-pinned to the dispatch table — it
/// never advertises a verb that does not run" — and it was true of the ROSTER
/// and false of the page asserting it: that page prescribed `aterm pkg
/// shellenv`, `aterm pkg seam` and `aterm pkg doctor --fix`, none of which
/// exist, and an unknown verb exits 2 with EMPTY stdout, so
/// `eval "$(aterm pkg shellenv)"` succeeded and did nothing.
#[must_use]
pub fn dispatch_roster() -> &'static [&'static str] {
    VERBS
}

const VERBS: &[&str] = &[
    "doctor",
    "status",
    "which",
    "list",
    "repair",
    "uninstall",
    "tree-root",
    "verify-index",
    "verify-pkg",
    "install",
    "seed",
    "update",
    "rollback",
    "pin",
    "unpin",
    "gc",
    "verify",
    "link",
    "unlink",
    "refresh",
    "run",
    "relocate",
    "noindex",
    "machine",
];

/// One usage line per verb — what `atpkg <verb> --help` prints.
///
/// These strings already existed, one `eprintln!` at a time, on each verb's
/// arity/positional failure path: the binary always knew every verb's grammar and
/// would only say it if you got the call wrong. `--help` answered with the top-level
/// banner instead, so the one command the flag error itself recommends
/// (``aterm pkg <verb> --help``) was circular, and 35 of 55 audited verbs across the
/// CLI could not answer "what do you accept"
/// (docs/AUDIT-cli-per-verb-help-2026-08-31.md).
///
/// That is how an audit of this project concluded atpkg "structurally requires sudo":
/// `install --help` gave the banner, every flag probe answered `is not a program name`,
/// and `--elevate` — a real flag, documented only in `aterm help pkg` — was invisible.
///
/// Every entry in [`VERBS`] must appear here; `tests::every_verb_has_a_usage_line`
/// fails the build otherwise, so a new verb cannot land helpless.
const VERB_USAGE: &[(&str, &str)] = &[
    (
        "doctor",
        "atpkg doctor            (alias: status) — full health report",
    ),
    (
        "status",
        "atpkg status            (alias of doctor) — full health report",
    ),
    ("which", "atpkg which <tool>"),
    ("list", "atpkg list [--porcelain]"),
    (
        "uninstall",
        "atpkg uninstall <program> | atpkg uninstall --all",
    ),
    ("tree-root", "atpkg tree-root <dir>"),
    (
        "verify-index",
        "atpkg verify-index <master-pubkey-b64> <index.toml> <index.toml.sig> \
         <aterm-machines.toml> <aterm-machines.toml.sig>",
    ),
    (
        "verify-pkg",
        "atpkg verify-pkg <master-pubkey-b64> <pkg-*.toml> <pkg.sig> \
         <aterm-machines.toml> <aterm-machines.toml.sig>",
    ),
    (
        "install",
        "atpkg install <program> [--elevate=sudo|osascript|never]\n\
         atpkg install --default-set    — the whole ALab toolset",
    ),
    ("seed", "atpkg seed"),
    ("update", "atpkg update [program]"),
    ("rollback", "atpkg rollback <program>"),
    ("pin", "atpkg pin <program>"),
    ("unpin", "atpkg unpin <program>"),
    ("gc", "atpkg gc                — reclaim superseded builds"),
    ("verify", "atpkg verify [program]"),
    ("link", "atpkg link <program> <checkout> [rel-bin…]"),
    ("unlink", "atpkg unlink <program>"),
    (
        "refresh",
        "atpkg refresh [program…]  — bare: every dev-linked program",
    ),
    ("run", "atpkg run <tool> [args…]"),
    (
        "relocate",
        "atpkg relocate <stage-root> [--sign <identity>] [--advisory]",
    ),
    (
        "repair",
        "atpkg repair            — re-lay shims and shell integration after a broken install",
    ),
    (
        "noindex",
        "atpkg noindex [scan] [<root>] [--depth <n>]  — cargo target dirs Spotlight indexes\n\
         atpkg noindex verify <dir>                   — MEASURE whether <dir> is excluded\n\
         atpkg noindex migrate <dir> [--dry-run]      — rename <dir> to the excluded form\n\
         atpkg noindex apply (--all | <dir>...) [--dry-run] — migrate AND keep cargo pointed: \
         a `target` symlink in a git checkout, [build] target-dir elsewhere (--all: every \
         repo target dir under $HOME, as doctor scans)",
    ),
    (
        "machine",
        "atpkg machine                 — the [machine] settings, as the doctor reads them\n\
         atpkg machine apply           — apply them now: Universal Control off for this host,\n\
         \u{20}                               cargo build output hidden from Spotlight",
    ),
];

/// The usage line for `verb`, if it has one.
fn usage_of(verb: &str) -> Option<&'static str> {
    VERB_USAGE
        .iter()
        .find(|(name, _)| *name == verb)
        .map(|(_, usage)| *usage)
}

/// Whether a `-h`/`--help` in `rest` (the argv after `verb`) is a question for atpkg
/// rather than an argument travelling to a tool. A literal `--` ends the scan for every
/// verb; for `run`, only the FIRST operand counts — everything after `<tool>` belongs to
/// the tool, so `atpkg run rg --help` reaches rg, and so does `aterm rg --help`, which
/// arrives here as `run rg -- --help`. Both used to print atpkg's own usage instead.
fn help_flag_addresses_atpkg(verb: &str, rest: &[String]) -> bool {
    let scanned = if verb == "run" {
        &rest[..rest.len().min(1)]
    } else {
        rest
    };
    scanned
        .iter()
        .take_while(|a| *a != "--")
        .any(|a| a == "-h" || a == "--help")
}

/// Answer `<verb> --help` from [`VERB_USAGE`]: the verb's own grammar, then the one
/// pointer to the long-form manual. Exit 0 — asking a question is not an error.
fn cmd_verb_help(verb: &str) -> ExitCode {
    match usage_of(verb) {
        Some(usage) => {
            println!("usage: {usage}");
            println!("atpkg: full manual for this verb: aterm help pkg");
            ExitCode::SUCCESS
        }
        // NOT a verb we know. This used to fall back to `cmd_help()` — the
        // roster, at exit 0 — which is a FALSE SUCCESS: `atpkg help install`,
        // `atpkg help gc` and `atpkg help zzz-total-nonsense` returned three
        // byte-identical pages at rc 0, and a page of text after a `help <x>`
        // reads as "this is the help for x" (audit D-C). `aterm help` was
        // already the honest counter-example, so this copies its shape: name
        // the topic, suggest the near miss, list the real ones, exit 2.
        None => {
            eprintln!("atpkg help: unknown topic '{verb}'");
            if let Some(near) = did_you_mean(verb) {
                eprintln!("atpkg:   did you mean `atpkg help {near}`?");
            }
            eprintln!();
            eprintln!("available topics (atpkg help <verb>):");
            print_verb_tiers(&mut std::io::stderr());
            eprintln!("full manual: aterm help pkg");
            ExitCode::from(2)
        }
    }
}

/// The advertised roster, grouped the way a person meets it. PRESENTATION ONLY:
/// dispatch order stays [`VERBS`] (pinned by [`tests::verb_hint_matches_dispatch`]);
/// these tiers are pinned as an EXACT partition of it by
/// [`tests::verb_tiers_partition_the_roster`], so a verb added to dispatch without
/// a tier fails loudly instead of silently vanishing from every human surface —
/// the drift that killed `sync` and `seed`.
///
/// Three tiers, because that is the question a first hour actually has: what do I
/// type today (daily), what do I type when something needs undoing or holding
/// (occasional), and what exists but is not for me yet (plumbing). A flat 21-name
/// comma list buried `install` between `verify-pkg` and `seed`, which is how a new
/// user ends up reading the whole roster to find the one verb the docs call "the
/// usual first command".
const VERB_TIERS: &[(&str, &[&str])] = &[
    (
        "daily",
        &[
            "install", "list", "update", "doctor", "status", "which", "run",
        ],
    ),
    (
        "occasional",
        &[
            "uninstall",
            "rollback",
            "pin",
            "unpin",
            "verify",
            "gc",
            "repair",
            "noindex",
            "machine",
        ],
    ),
    (
        "plumbing",
        &[
            "seed",
            "link",
            "unlink",
            "refresh",
            "tree-root",
            "verify-index",
            "verify-pkg",
            "relocate",
        ],
    ),
];

/// The tier block, rendered — ONE function so bare `atpkg`, `--help`, and the
/// unknown-verb error can never fork the roster the way the old two hand-written
/// copies (bare vs `--help`) already had.
///
/// `status` is real in [`VERB_TIERS`] (the partition test needs every dispatched
/// verb placed) but renders attached to `doctor` as `doctor (or: status)`: the two
/// are one report, and listing them as siblings would imply a difference for the
/// reader to go hunting for.
fn verb_tier_lines() -> Vec<String> {
    VERB_TIERS
        .iter()
        .map(|(tier, verbs)| {
            let shown: Vec<&str> = verbs
                .iter()
                .filter(|v| **v != "status")
                .map(|v| {
                    if *v == "doctor" {
                        "doctor (or: status)"
                    } else {
                        *v
                    }
                })
                .collect();
            format!("atpkg:   {tier:<12} {}", shown.join(", "))
        })
        .collect()
}

/// Print [`verb_tier_lines`] to `out` — the one shared renderer (stdout for the
/// posture/help surfaces, stderr beside the unknown-verb error).
fn print_verb_tiers(out: &mut impl std::io::Write) {
    for line in verb_tier_lines() {
        let _ = writeln!(out, "{line}");
    }
}

/// The advertised verb closest to `input` (Levenshtein distance ≤ 2), or `None` when
/// nothing is close enough to suggest — a far-fetched guess ("frobnicate → refresh"?)
/// erodes trust in the near ones. Ties resolve to the first in [`VERBS`] order.
fn did_you_mean(input: &str) -> Option<&'static str> {
    let mut best: Option<(usize, &'static str)> = None;
    for verb in VERBS {
        let d = levenshtein(input, verb);
        if best.is_none_or(|(b, _)| d < b) {
            best = Some((d, verb));
        }
    }
    best.filter(|(d, _)| *d <= 2).map(|(_, v)| v)
}

/// Plain O(len_a * len_b) edit distance over chars — the roster is 21 short names,
/// so clarity beats cleverness here.
fn levenshtein(a: &str, b: &str) -> usize {
    let b_chars: Vec<char> = b.chars().collect();
    let mut prev: Vec<usize> = (0..=b_chars.len()).collect();
    for (i, ca) in a.chars().enumerate() {
        let mut row = vec![i + 1];
        for (j, cb) in b_chars.iter().enumerate() {
            let cost = usize::from(ca != *cb);
            row.push((prev[j] + cost).min(prev[j + 1] + 1).min(row[j] + 1));
        }
        prev = row;
    }
    prev[b_chars.len()]
}

/// The whole package-manager CLI as a callable: `argv[1..]` in, exit code
/// out. Served in-process by the ONE `aterm` binary (`aterm pkg …` / the
/// `atpkg` argv0 alias) and by the thin standalone bin. Everything below is
/// unchanged from the binary era.
pub fn main_entry(argv: Vec<std::ffi::OsString>) -> ExitCode {
    // Before any wait: the clock a waited launch pass compares the store's record
    // against ([`pass_finished_while_we_waited`]).
    let _ = PROCESS_START_UNIX.set(unix_now());
    // THE HIDDEN HELPER VERBS FIRST, ON THE RAW ARGV. Their one argument is a spec
    // PATH — a file under the store prefix — and a prefix under a non-UTF-8 name must
    // reach the helper byte for byte: the lossy conversion below turned such a path
    // into one the helper could not open, and the lane counted that as its own failure
    // (audit 2026-09-14). Unlisted, dispatched before the store lock (their parent
    // HOLDS it), served by name through the `aterm` front door too.
    if let Some(verb) = argv.first().and_then(|v| v.to_str()) {
        if verb == crate::stage_helper::HIDDEN_VERB {
            return crate::stage_helper::run_helper(&argv[1..]);
        }
        if verb == crate::lay::HIDDEN_VERB {
            return crate::lay::run_helper(&argv[1..]);
        }
        if verb == crate::seam::HIDDEN_VERB {
            return crate::seam::run_helper(&argv[1..]);
        }
    }
    let mut args: Vec<String> = argv
        .into_iter()
        .map(|a| a.to_string_lossy().into_owned())
        .collect();
    // `--progress-file <path>` — the GUI-only machine-progress opt-in (R5), stripped
    // BEFORE dispatch so no verb ever sees it as an operand. Recognized only on the
    // provisioning verbs; a plain terminal `aterm pkg update` never passes it, so the
    // terminal lanes' stdout is byte-unchanged. The stripped path feeds
    // [`progress_path`], which the provisioning passes consult.
    if matches!(
        args.first().map(String::as_str),
        Some("seed" | "update" | "install")
    ) {
        strip_progress_file_flag(&mut args);
        // `--wait-lock <secs>` — the machine lanes' opt-in to QUEUE behind a sibling's
        // pass at the store lock instead of refusing (2026-09-10), stripped here for
        // the same reason and BEFORE everything that would otherwise refuse it:
        // `cmd_seed`'s operand refusal, the flag gate below, and the lock itself,
        // which is what reads it ([`mutator_store_lock`]).
        if let Some(bound) = take_wait_lock_flag(&mut args) {
            // The spawner's pid is captured HERE, at the edge, because "my parent
            // is gone" is a CHANGE of parent (see `parent_is_gone`), and this is
            // the last moment the parent is known to be the spawner.
            let _ = WAIT_LOCK.set(WaitLock {
                bound,
                spawner: spawner_pid(),
            });
            // THE ORPHAN WATCH, armed HERE for the whole process — not inside a
            // progress pass, which the channel-apply lane (the multi-gigabyte rustc
            // group update, exactly the incident's) never opens: a window that exits
            // under this child (a self-update's successor, a quit-and-reopen) leaves
            // its pipes unread, and the next print would kill the pass with EPIPE.
            // The watch notices the parent CHANGE and re-points stdout/stderr at a
            // log beside the progress file ([`crate::progress::watch_for_orphaning`]).
            let log = crate::store::resolve_configured()
                .map(|l| l.prefix.join(crate::progress::ORPHAN_LOG_NAME))
                .unwrap_or_else(|| std::env::temp_dir().join(crate::progress::ORPHAN_LOG_NAME));
            crate::progress::watch_for_orphaning(log);
        }
    }
    let verb = args.first().map(String::as_str);
    // The HIDDEN pending-program verb (R6): what a laid stub execs. Deliberately
    // unlisted in help/VERBS (it is machinery, not vocabulary), handled before the
    // dispatch match so the roster-coherence scraper sees only real verbs — and
    // BEFORE the store lock: it only reads progress/status and appends one line to
    // the reorder-only bump file, and it must answer instantly while the installer
    // HOLDS the store lock.
    if verb == Some("__pending") {
        return cmd_pending(args.get(1), args.get(2..).unwrap_or(&[]));
    }
    // The HIDDEN self-update verb ([`crate::selfupdate`], 2026-09-19): what an agent
    // program's `agents/` twin execs when its FIRST argument is one of the vendor's own
    // self-update verbs (`claude update|upgrade|install`, `codex update`). Same
    // discipline as `__pending` — unlisted, dispatched before the match so the roster
    // scraper sees only real verbs, and BEFORE the help edge and the store lock: it never
    // mutates the store itself — the CHILD it spawns does, `update <program>` through
    // this very dispatch edge, lock and all — and it must answer while a pass HOLDS the
    // lock, when its answer is "that pass is doing this work".
    if verb == Some(crate::selfupdate::HIDDEN_VERB) {
        return cmd_selfupdate(&args[1..]);
    }
    // The HIDDEN landing verb ([`crate::landing`], 2026-09-16): what an agent program's
    // `agents/` twin execs while a NEWER build of that program is landing. Same
    // discipline as `__pending` — unlisted, dispatched before the match, and BEFORE the
    // store lock, which the pass that is landing the build HOLDS for the whole wait.
    if verb == Some(crate::landing::HIDDEN_VERB) {
        return cmd_landing(&args[1..]);
    }
    // The HIDDEN reroute verb (philosophy §4, `crate::reroute`): what a reroute stub
    // execs for `cargo`/`rustfmt`/… typed inside a session. Same discipline as
    // `__pending` — unlisted, dispatched before the match so the roster scraper sees
    // only real verbs, and BEFORE the store lock: it must answer while the installer
    // HOLDS the lock, and it never mutates the store.
    if verb == Some(crate::reroute::HIDDEN_VERB) {
        return cmd_reroute(&args[1..]);
    }
    // (The three HIDDEN lane verbs — `crate::stage_helper`, `crate::lay`, `crate::seam`
    // — were dispatched here on the lossy args until 2026-09-15; they are now answered
    // at the top of this function on the raw argv, for the reason given there.)
    // THE single-writer-per-store gate ([`crate::lock`]): every store-MUTATING verb
    // TRY-acquires the store-wide `store.lock` here — at the ONE dispatch edge, so
    // internal verb re-routing (`update <p>` → install) can
    // never double-acquire — and holds it for the whole verb. Contention is a loud
    // exit-75 refusal naming the lock path for a typed verb (the GUI Packages page
    // surfaces the child stderr); the window's launch lanes pass `--wait-lock` and
    // QUEUE behind the holder instead (see `mutator_store_lock`), and their loop
    // retries a timed-out wait on a short backoff — never "simply next pass"
    // (incident 2026-09-10). Read-only verbs skip this entirely and never need the
    // lock.
    // The conventional help spellings land on the help surface with exit 0, not the
    // unknown-verb error path — `atpkg` rides PATH as an argv0 alias, so `--help` is
    // the first thing a shell (or an AI) tries. Handled BEFORE the verb match so the
    // dispatch-coherence test's arm extraction sees only real verbs.
    if matches!(verb, Some("help" | "-h" | "--help")) {
        // `atpkg help <verb>` answers for THAT verb, matching `aterm help <topic>`;
        // bare `help` keeps the roster. An UNKNOWN topic is a usage error, not a
        // roster at exit 0 — see `cmd_verb_help`.
        if let Some(topic) = args.get(1) {
            return cmd_verb_help(topic);
        }
        return cmd_help();
    }
    // `<verb> --help` anywhere before a literal `--` answers for that verb and does
    // nothing else — for `run`, only as the first operand, since everything after
    // `<tool>` belongs to the tool ([`help_flag_addresses_atpkg`]).
    //
    // Placed before the store lock and before every verb body on purpose. Asking a
    // mutating verb how it works must never mutate: `atpkg gc --help` used to reclaim
    // the rollback window, and a third of the audited CLI surface DID something when
    // handed `--help` (docs/AUDIT-cli-per-verb-help-2026-08-31.md). Intercepting at the
    // one dispatch edge closes that class for every verb at once, including ones nobody
    // thought to probe.
    if let Some(v) = verb
        && usage_of(v).is_some()
        && help_flag_addresses_atpkg(v, &args[1..])
    {
        return cmd_verb_help(v);
    }
    // A FLAG IS NEVER A PROGRAM NAME. The verbs below read `args[1]` as a program and hand
    // it to the signed index, so `atpkg install --help` used to RESOLVE `"--help"`, fail,
    // and persist `[programs.--help] state = "error: --help is not named in the signed
    // index"` into `status.toml` — where nothing ever removed it, so `atpkg doctor` called
    // the toolset incomplete for the life of the machine while all ten real programs sat
    // there `active`. `atpkg uninstall --version` was worse than noisy: it matched nothing,
    // took the clean-success path, cleared the program's adoption, printed "uninstalled
    // --version" and exited 0. Gated at the ONE dispatch edge, and BEFORE the store lock, so
    // a typo never contends for `store.lock` (2026-08-20 round-10 audit).
    if let Some(v) = verb
        && let Some(operand) = args.get(1)
        && operand.starts_with('-')
        && let Some((_, allowed)) = NAME_TAKING_VERBS.iter().find(|(name, _)| *name == v)
        && !allowed.contains(&operand.as_str())
    {
        // (`-h`/`--help` never reaches here — the dispatch edge above answers it with
        // this verb's own usage.)
        //
        // Name the verb's grammar HERE rather than pointing at another command. The
        // old pair said `"--prefix" is not a program name — it is a flag` and then
        // `aterm pkg install --help shows what install accepts`, which printed the
        // top-level banner — so the reader learned nothing and could reasonably
        // conclude the flag did not exist. One of this project's own audits concluded
        // exactly that and wrote it down.
        eprintln!("atpkg {v}: unknown option {operand:?} for `atpkg {v}`");
        if let Some(usage) = usage_of(v) {
            eprintln!("usage: {usage}");
        }
        if !allowed.is_empty() {
            eprintln!("atpkg:   {v} accepts: {}", allowed.join(", "));
        }
        return ExitCode::from(2);
    }
    // BEFORE THE LOCK, BECAUSE THIS WORK NEEDS NO LOCK. A contended store used to hold
    // the host settings hostage for the whole `--wait-lock` bound, and a refused pass
    // dropped them entirely; they are neither the store's state nor racing anything in
    // it. See `apply_machine_settings_once`.
    if verb.is_some_and(verb_applies_machine_settings) {
        let _ = apply_machine_settings_once();
    }
    let _store_lock = if verb.is_some_and(verb_mutates_store) {
        match mutator_store_lock() {
            Ok(guard) => guard,
            Err(code) => {
                // The three passes apply the `[machine]` settings first thing — but the
                // lock is taken HERE, before the pass body, so a contended or unwritable
                // store refuses the whole pass and the host settings with it. They need
                // no lock at all; say so, on stderr (stdout stays the marker channel the
                // waiter tests pin), and only while someone is there to read it.
                if verb.is_some_and(verb_applies_machine_settings) && !parent_is_gone() {
                    use std::io::Write as _;
                    let _ = writeln!(
                        std::io::stderr(),
                        "atpkg: the [machine] settings took no store lock and were applied \
                         above this refusal — `aterm pkg machine apply` re-applies them"
                    );
                }
                return code;
            }
        }
    } else {
        None
    };
    match verb {
        // `status` is the name people reach for first — it is what every other package
        // manager calls this — and it answered "unknown verb" while `doctor` sat two lines
        // away. An alias costs nothing and removes a guess from the first minute of use.
        Some("doctor") => {
            return zero_arity(&args[1..], "doctor").unwrap_or_else(|| doctor("doctor"));
        }
        // Same report, and the report SIGNS ITSELF with the verb the user typed: every
        // line answered "doctor:" regardless, so a `status` user could not tell which
        // verb they had run and a script keying on the prefix keyed on the wrong verb.
        Some("status") => {
            return zero_arity(&args[1..], "status").unwrap_or_else(|| doctor("status"));
        }
        Some("which") => return cmd_which(args.get(1)),
        Some("list") => return cmd_list(args.get(1..).unwrap_or(&[])),
        // A STORE MUTATOR that took any argument and ignored it. `atpkg repair
        // --dry-run` rewrote `~/.aterm/shell.d` and the rc wiring, re-laid the reroute
        // stubs, re-asserted the rustup seam and re-laid every installed program's
        // shims, printed "repair: shell integration rewritten" and exited 0 — without
        // ever naming the flag, so the reader concluded the preview had been honoured
        // and nothing had changed. That is audit D-12's swallowed-argument class (see
        // the note on `zero_arity`) on the one verb of these four that WRITES, so it
        // refuses here like the three read-only ones: `repair` takes no arguments.
        Some("repair") => {
            return zero_arity(&args[1..], "repair").unwrap_or_else(|| run_repair(layout()));
        }
        Some("uninstall") => return cmd_uninstall(args.get(1)),
        Some("tree-root") => return cmd_tree_root(args.get(1)),
        Some("verify-index") => return cmd_verify_index(args.get(1..).unwrap_or(&[])),
        Some("verify-pkg") => return cmd_verify_pkg(args.get(1..).unwrap_or(&[])),
        Some("install") => return cmd_install_argv(&args[1..]),
        Some("seed") => return cmd_seed(&args[1..]),
        Some("update") => return cmd_update(args.get(1)),
        Some("rollback") => return cmd_rollback(args.get(1)),
        Some("pin") => return cmd_pin(args.get(1), true),
        Some("unpin") => return cmd_pin(args.get(1), false),
        Some("gc") => return zero_arity(&args[1..], "gc").unwrap_or_else(cmd_gc),
        Some("verify") => return cmd_verify(args.get(1)),
        Some("link") => return cmd_link(&args[1..]),
        Some("unlink") => return cmd_unlink(args.get(1)),
        Some("refresh") => return cmd_refresh(&args[1..]),
        Some("run") => return cmd_run(&args[1..]),
        Some("relocate") => return cmd_relocate(&args[1..]),
        // Storage hygiene, and deliberately NOT a store mutator: `noindex` writes nothing
        // under `<prefix>` — its probe file is removed by a `Drop` guard and its one
        // `rename(2)` lives inside the tree the user named — so it stays out of
        // `verb_mutates_store` and a scan of a 2 TB home can never queue behind, or
        // block, an install. `args[1]` here is a SUBCOMMAND, not a program name, which is
        // why `noindex` is absent from `NAME_TAKING_VERBS` (as `relocate` is): its own
        // parser owns every `-`-leading token.
        Some("noindex") => return cmd_noindex(&args[1..]),
        // The [machine] settings' own door. Every pass applies them first and
        // unconditionally; this is for the person the doctor just told about them, so
        // the fix is one verb away and never "wait for the next pass". Writes a per-host
        // preference and, at most, renames build output inside trees the user owns —
        // nothing under `<prefix>`, so no store lock, like `noindex`.
        Some("machine") => return cmd_machine(&args[1..]),
        Some(other) => {
            // The head `atpkg: unknown verb '<x>'` is byte-stable for scripts; the
            // suggestion rides after an em dash so a typo's fix is the FIRST thing on
            // screen, and the full tiered roster still prints below — arrangement, not
            // deletion: hiding plumbing verbs from the error would strand the operator
            // who misspells one.
            match did_you_mean(other) {
                Some(verb) => {
                    eprintln!("atpkg: unknown verb '{other}' — did you mean `{verb}`?");
                }
                None => eprintln!("atpkg: unknown verb '{other}'"),
            }
            print_verb_tiers(&mut std::io::stderr());
            eprintln!("atpkg: full manual: aterm help pkg");
            return ExitCode::from(2);
        }
        None => cmd_bare(),
    }
    ExitCode::SUCCESS
}

/// `atpkg help` / `-h` / `--help` — the conventional help surface: usage plus the ONE
/// advertised verb roster (tiered — the same [`print_verb_tiers`] block every other
/// surface prints, so they can never fork), exit 0. The full manual is `aterm help pkg`;
/// this stays a summary so the two never fork. Static — no store reads — so it answers
/// even with HOME unset.
fn cmd_help() -> ExitCode {
    println!("atpkg — the aterm toolchain package manager");
    println!("{}", aterm_types::identity::ORIGIN_LINE);
    // The usage line keeps its `atpkg` spelling (that IS this program's name, and the
    // argv0 alias makes it runnable), but a first-hour user arrived via `aterm pkg` —
    // name the spelling they can actually paste.
    println!("usage: atpkg <verb> [args…]   (you type: aterm pkg <verb>)");
    print_verb_tiers(&mut std::io::stdout());
    println!("atpkg: new machine: aterm pkg install --default-set installs the whole ALab toolset");
    println!("full manual: aterm help pkg");
    ExitCode::SUCCESS
}

/// The one-line answer for a program/tool the store does not hold — fact first, then
/// the fix, because a bare "ty is not installed" is a dead end: the user's very next
/// question is ALWAYS "then how do I get it", and the first hour should never need a
/// second guess. `fix:` is the house word for the attached remedy (one spelling
/// everywhere, greppable).
fn not_installed_fix(name: &str) -> String {
    format!("atpkg: {name} is not installed (fix: aterm pkg install {name})")
}

/// The `--progress-file` path this invocation carries, if any — set once at the
/// dispatch edge by [`strip_progress_file_flag`], read by the provisioning passes.
/// Process-global because the pass that consumes it (`install_default_set`) sits
/// several call layers below verbs whose signatures are shared with paths that
/// must never know about it.
static PROGRESS_FILE: std::sync::OnceLock<std::path::PathBuf> = std::sync::OnceLock::new();

/// Remove every `--progress-file <path>` pair from `args`, recording the FIRST path
/// given. Value-missing is tolerated as "no opt-in" rather than an error: the flag
/// is machinery for the GUI spawn, not user vocabulary, and a broken spawn must
/// degrade to a progress-less install, never a refused one.
fn strip_progress_file_flag(args: &mut Vec<String>) {
    while let Some(i) = args.iter().position(|a| a == "--progress-file") {
        args.remove(i);
        if i < args.len() {
            let path = args.remove(i);
            let _ = PROGRESS_FILE.set(std::path::PathBuf::from(path));
        }
    }
}

/// Where this invocation's live progress should land, if the GUI opted in.
fn progress_path() -> Option<&'static std::path::Path> {
    PROGRESS_FILE.get().map(std::path::PathBuf::as_path)
}

/// What the dispatch edge recorded from `--wait-lock <secs>`: the bound, and the pid
/// of the parent that spawned this process at that moment — the window — so a
/// waiter can tell "my parent is gone" as a CHANGE of parent rather than as
/// "re-parented to pid 1", which is true on macOS (orphans go to launchd) and false
/// on a Linux box with a subreaper (`systemd --user`, most container and sandbox
/// runtimes), where an orphan re-parents to the subreaper's pid instead.
#[derive(Clone, Copy, Debug)]
struct WaitLock {
    bound: std::time::Duration,
    spawner: Option<u32>,
}

/// The [`WaitLock`] this invocation carries, if any — set once at the dispatch edge
/// by [`main_entry`] from the pure [`take_wait_lock_flag`]'s return value, read
/// through [`wait_lock`] by [`mutator_store_lock`] and by [`parent_is_gone`].
/// Process-global for the same reason as [`PROGRESS_FILE`]: the lock edge has no
/// argv of its own.
static WAIT_LOCK: std::sync::OnceLock<WaitLock> = std::sync::OnceLock::new();

/// Whether this process found the store lock CONTENDED at least once before it
/// took it ([`mutator_store_lock`]'s poll): set on every contended poll of a
/// `--wait-lock` wait, read by [`pass_finished_while_we_waited`].
static WAITED_FOR_LOCK: std::sync::atomic::AtomicBool = std::sync::atomic::AtomicBool::new(false);

/// When this process started, unix seconds — captured at the top of
/// [`main_entry`], before any wait.
static PROCESS_START_UNIX: std::sync::OnceLock<i64> = std::sync::OnceLock::new();

/// The unix second of this process's FIRST contended poll at the store lock: the
/// instant it is known that someone else holds the store. A success stamp
/// STRICTLY later than this second was written by a pass that ran while this
/// process waited ([`pass_finished_while_we_waited`]); a same-second stamp cannot
/// qualify, which fails safe (the verb runs) against one-second stamp
/// granularity.
static FIRST_CONTENDED_UNIX: std::sync::OnceLock<i64> = std::sync::OnceLock::new();

fn unix_now() -> i64 {
    std::time::SystemTime::now()
        .duration_since(std::time::SystemTime::UNIX_EPOCH)
        .map_or(0, |d| i64::try_from(d.as_secs()).unwrap_or(i64::MAX))
}

/// THE HOLDER DID THIS PASS (2026-09-18). A launch UPDATE child (the `--wait-lock`
/// lane; the launch seed always runs — its under-lock work is the new build's own
/// stub/seam/exec-root reconcile, about a second) that queued behind another atpkg
/// pass and then took the lock re-did that pass's whole work on the store the
/// holder had just checked —
/// a self-update's successor behind its own predecessor's launch pass, measured on
/// the owner's Mac: 13 s queued, then ~14 s more under the lock for "up to date".
/// Pure over the three facts: whether this process WAITED (a lock taken at the
/// first try proves nothing about what ran before), the store's last SUCCESSFUL
/// update-class pass (`status.toml` `last_success_at` — stamped only by a pass
/// that resolved the index and ran to its end; never by a failed pass, a seed's
/// own install or a typed `install <p>`, all of which move `updated_at`), and the
/// second this process first found the lock held. A success stamped STRICTLY
/// after that second was written by a pass that ran while this one waited — that
/// pass is the one this launch owed, so the verb stands down; `Some(age)` says
/// how long ago it ended. Anything less — no success, an older success, a holder
/// that failed or installed one program — runs the verb: standing down is only
/// ever "up to date" on proof.
fn pass_finished_since_start(
    waited: bool,
    last_success_unix: Option<i64>,
    first_contended_unix: i64,
    now_unix: i64,
) -> Option<u64> {
    if !waited {
        return None;
    }
    let stamp = last_success_unix?;
    (stamp > first_contended_unix)
        .then(|| u64::try_from(now_unix.saturating_sub(stamp)).unwrap_or(0))
}

/// [`pass_finished_since_start`] over this process's record: `Some(age)` when an
/// update-class pass on `layout`'s store SUCCEEDED while this process waited for
/// the lock. Never under the once-pass knob (`ATPKG_UPDATE_INTERVAL_SECS=0`, the
/// test lane): its contract is one pass that RAN per launch, and a stood-down
/// child would end it with no update having run.
fn pass_finished_while_we_waited(layout: &crate::store::Layout) -> Option<u64> {
    let waited = WAITED_FOR_LOCK.load(std::sync::atomic::Ordering::Relaxed);
    if !waited || aterm_update_core::pkg_check::update_interval_secs() == 0 {
        return None;
    }
    let first_contended = *FIRST_CONTENDED_UNIX.get_or_init(unix_now);
    let text = std::fs::read_to_string(layout.status()).ok()?;
    let last_success = aterm_update_core::pkg_check::last_success_at(&text)
        .and_then(|stamp| aterm_update_core::pkg_check::rfc3339_to_unix(&stamp));
    pass_finished_since_start(waited, last_success, first_contended, unix_now())
}

/// The line a stood-down launch pass prints — plain, no marker: to the window it
/// is a quiet pass (exit 0), which raises nothing.
fn stood_down_after_wait_line(verb: &str, ago: u64) -> String {
    format!(
        "atpkg: {verb}: up to date \u{2014} a pass on this store succeeded {ago} s ago, while \
         this one waited for it; nothing to redo"
    )
}

#[cfg(test)]
mod stand_down_after_wait_tests {
    use super::pass_finished_since_start;

    /// A launch child that waited and finds a SUCCESS stamped strictly after the
    /// second it first found the lock held stands down; one that did not wait,
    /// whose record predates that second or lands in the same second, or whose
    /// store carries no success, runs.
    #[test]
    fn only_a_waited_child_behind_a_pass_that_succeeded_since_it_queued_stands_down() {
        let first = 1_700_000_000;
        assert_eq!(
            pass_finished_since_start(true, Some(first + 12), first, first + 20),
            Some(8),
            "the holder finished 8 s ago: stand down"
        );
        assert_eq!(
            pass_finished_since_start(true, Some(first), first, first + 1),
            None,
            "a same-second stamp is not proof (one-second granularity): run"
        );
        assert_eq!(
            pass_finished_since_start(true, Some(first - 1), first, first + 20),
            None,
            "a success older than this wait is the previous pass: run"
        );
        assert_eq!(
            pass_finished_since_start(false, Some(first + 12), first, first + 20),
            None,
            "a lock taken at the first try waited for nobody: run"
        );
        assert_eq!(
            pass_finished_since_start(true, None, first, first + 20),
            None,
            "no success on record (a failed holder, a seed's install): run"
        );
        assert_eq!(
            pass_finished_since_start(true, Some(first + 30), first, first + 20),
            Some(0),
            "a stamp ahead of the clock reads as just now"
        );
    }
}

/// The environment variable a spawner sets beside `--wait-lock` to say WHO it is —
/// its own pid — so the waiter's "my window is gone" is a comparison against a
/// fact, not against a guess. The guess (`getppid` at the dispatch edge) races a
/// parent that dies inside the child's few milliseconds of startup: by the time
/// the edge looks, the child is already re-parented, and the "spawner" it records
/// is init or the subreaper — the very pid it would later be compared against
/// (measured 2026-09-10: a `sh -c '… &'` parent exiting at once left the waiter
/// polling out its whole bound under ppid 1). Hidden machinery like
/// `--wait-lock` itself; `aterm-gui` imports the name so a rename is a compile
/// error on both sides.
///
/// Unnamed, the edge records the parent it sees, and the waiter stands down when
/// its parent CHANGES from that ([`is_orphaned`]) — whoever that parent was. A
/// `nohup … &` job is no exception: its parent at the edge is the shell that
/// launched it, so when that shell exits the job is re-parented and its next
/// contended poll stands down, silently, with 75 (measured 2026-09-12: gone 4.1 s
/// into a 60 s bound, the lock still held, once the launching `sh` exited).
/// `nohup` guards against SIGHUP, not against this rule: a caller that wants its
/// waiter to outlive it must keep the job's parent alive, or the waiter stands
/// down. Only a parent that is ALREADY init or launchd at the edge — a launchd
/// agent's job — never changes, and that waiter waits out its bound like a typed
/// one. A cron job run in the foreground waits too, though not for that reason:
/// its parent is cron's own child or the `sh` it runs, not init, and that parent
/// outlives the job. The same `ppid 1` that means "gone" for a window's child
/// means "spawned by launchd" for an agent's job, which is why the rule is a
/// change and never the pid alone. A caller that wants its waiter to stand down
/// when IT exits names itself here — which also covers the one shape the fallback
/// misses: an unnamed parent that dies inside the waiter's startup, before the
/// edge looks, so the edge records init or the subreaper and the waiter polls out
/// its bound (the documented degradation; no window is unnamed).
pub const SPAWNER_PID_ENV: &str = "ATPKG_SPAWNER_PID";

/// This process's parent pid right now; `None` where the platform has no notion
/// of one that std exposes.
fn current_parent_id() -> Option<u32> {
    #[cfg(unix)]
    {
        Some(std::os::unix::process::parent_id())
    }
    #[cfg(not(unix))]
    {
        None
    }
}

/// The pid of the process that spawned this one, as recorded at the dispatch edge:
/// what the spawner SAID ([`SPAWNER_PID_ENV`]) when it said anything, else the
/// parent pid as seen right now (a typed `--wait-lock` from a shell).
fn spawner_pid() -> Option<u32> {
    std::env::var(SPAWNER_PID_ENV)
        .ok()
        .and_then(|v| v.trim().parse::<u32>().ok())
        .or_else(current_parent_id)
}

/// Whether a waiter spawned by `spawner` has been orphaned, given its parent pid
/// `now`: its parent is no longer the spawner — init on macOS and a subreaper-less
/// Linux, the subreaper's pid where there is one — and both count because the rule
/// is a CHANGE, never "pid 1" on its own. A parent nobody can read (`now == None`:
/// std has no notion of one on Windows) is never judged gone, and neither is a
/// spawner nobody could name; both poll out the bound instead — still a TRY at
/// the end, still store-safe. Init as the parent is NOT gone by itself: an
/// unnamed waiter whose parent was already init at the edge (a launchd agent's
/// job) recorded init as its spawner, and that spawner never changes. Any other
/// recorded parent can: a `nohup … &` job recorded the shell that launched it, and
/// is orphaned once that shell exits (see [`SPAWNER_PID_ENV`]). The rule used to
/// be `now == Some(1) || …`, which made an unnamed `--wait-lock` a silent, instant
/// exit 75 from every pid-1-parented caller, and
/// `spawner.is_some() && now != spawner` alone made the window's own waiter —
/// named, on a platform with no readable parent — an orphan at its first contended
/// poll (both 2026-09-10). Pure for the test.
fn is_orphaned(spawner: Option<u32>, now: Option<u32>) -> bool {
    matches!((spawner, now), (Some(spawner), Some(now)) if now != spawner)
}

/// Remove every `--wait-lock <secs>` pair from `args`, returning the LAST parsable
/// value. Pure (no static) so the edge grammar is unit-testable. An unparsable VALUE
/// degrades to "no wait" — the `--progress-file` doctrine: the flag is machinery for
/// the window's spawn, never user vocabulary, and a broken spawn must degrade to the
/// fail-fast try, never to a refused verb. The token after the flag is consumed
/// either way, so it can never be read as a program name — which means a VALUELESS
/// flag eats whatever follows it: `atpkg install --wait-lock --default-set` loses
/// `--default-set` and is refused as a bare `install` (the usage, exit 2). Only a
/// valueless flag at the END of argv degrades to "no wait"; no spawner produces the
/// valueless shape (the window always passes a number).
fn take_wait_lock_flag(args: &mut Vec<String>) -> Option<std::time::Duration> {
    let mut wait = None;
    while let Some(i) = args.iter().position(|a| a == "--wait-lock") {
        args.remove(i);
        if i < args.len() {
            let value = args.remove(i);
            if let Ok(secs) = value.parse::<u64>() {
                wait = Some(std::time::Duration::from_secs(secs));
            }
        }
    }
    wait
}

/// The wait the edge recorded, if the caller opted in.
fn wait_lock() -> Option<WaitLock> {
    WAIT_LOCK.get().copied()
}

/// Mark `program` terminal on the live progress pass, if one is running. The ONE
/// funnel for `done`/`failed`/`skipped` so the overall counter can never double-count
/// a program (flow marks only non-terminal phases).
fn note_finished(program: &str, phase: crate::progress::Phase, error: Option<String>) {
    if let Some(sink) = crate::progress::active() {
        sink.finished(program, phase, error);
    }
}

/// `atpkg __pending <tool>` — the hidden verb a pending-program stub execs (R6).
///
/// Reads `progress.json` + `status.toml` under the UNTRUSTED-reader rules, appends
/// the tool to the reorder-only bump file, prints ONE honest state, and exits 127
/// (the stub's contract: the tool did not run). It never claims progress it cannot
/// read, and never claims a bump the installer would silently drop. An EXTRA's stub
/// that is not opted in asks first — see [`pending_extra_consent`].
fn cmd_pending(tool: Option<&String>, passthrough: &[String]) -> ExitCode {
    let Some(tool) = tool else {
        eprintln!("usage: atpkg __pending <tool> [args…]");
        return ExitCode::from(2);
    };
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    // R3: the stub is the first thing a fresh machine's user runs; if no pass has
    // ever completed, stderr says so before the state machine speaks.
    warn_if_never_checked(&layout);
    run_pending(&layout, tool, passthrough)
}

/// The copy of an AGENT program's stub that runs MEANWHILE: a foreign `claude`/`codex`
/// on `path_var` outside the managed prefix (a brew cask, Anthropic's native installer),
/// `None` for every other program and when no such copy exists. The agent stubs are laid
/// in `agents/` — first on every PATH, so that the state is printed at all (a stub last
/// on PATH behind a cask never answered, audit 2026-09-14) — and a stub that ran
/// INSTEAD of the user's working copy for the minutes of an install would be the
/// manager fighting its owner; so the stub says its one line on stderr and execs the
/// copy the user had, with the arguments verbatim. The managed twin replaces the stub
/// the moment the program installs, and from then on aterm's copy is what runs (owner
/// decision 2026-09-10).
#[must_use]
pub fn pending_passthrough(
    layout: &crate::store::Layout,
    tool: &str,
    path_var: Option<&std::ffi::OsStr>,
) -> Option<std::path::PathBuf> {
    if !crate::stub::is_agent_program(tool) {
        return None;
    }
    crate::vendor::system_binary_on_path(&layout.prefix, tool, path_var)
}

/// `atpkg __landing <program> [<prefix>] -- [args…]` — the hidden verb an agent program's
/// `agents/` twin execs while `<prefix>/landing/<program>` stands ([`crate::landing`]): wait
/// for the newer build, then `exec` the current `bin/<program>` shim, re-resolved after the
/// wait so the new build is what runs. On Windows the `.cmd` shim runs as a child instead.
///
/// Every ending of the wait runs the tool — landed, timed out, failed, stale marker, not
/// landing, or Ctrl-C. The one exception is Windows with an unarmed console control handler
/// ([`crate::platform::add_ctrl_handler`]), where Ctrl-C ends this process instead.
fn cmd_landing(rest: &[String]) -> ExitCode {
    // The operands, parsed by the pure half ([`crate::landing::HandOver::parse`]): the
    // program; the PREFIX operand the twin's prelude passes (absolute, laid from the
    // layout that owns the twin) so the store is found with no `HOME` — `env -i`,
    // launchd, a container entrypoint — where `layout()` would have printed "HOME is
    // unset" and exited 1 with the tool never run (review, 2026-09-16), a twin from
    // before the operand passing none; and the user's arguments with the ONE `--` the
    // twin inserted stripped, so a user's own leading `--` survives.
    let Some(hand_over) = crate::landing::HandOver::parse(rest) else {
        eprintln!(
            "usage: atpkg {} <program> [<prefix>] -- [args…]",
            crate::landing::HIDDEN_VERB
        );
        return ExitCode::from(2);
    };
    let program = hand_over.program;
    let Some(tool) = crate::store::ToolName::new(program) else {
        eprintln!("atpkg: {program:?} is not a tool name");
        return ExitCode::from(2);
    };
    let layout = match hand_over.prefix {
        Some(prefix) => crate::store::Layout { prefix },
        None => match layout() {
            Some(l) => l,
            None => return ExitCode::from(1),
        },
    };
    landing_wait_in_process(&layout, tool.as_str(), crate::landing::WAIT_SECS);
    // The CURRENT `bin/<program>` shim — never the `agents/` twin, whose prelude would
    // hand over here again. Re-resolved now: after a landing it is the new build's.
    // `platform::exec_or_run` is the `exec(2)` on Unix; on Windows (where the shim is
    // the `.cmd` wrapper and the `.cmd` twin has carried the prelude since 2026-09-17)
    // it is a child with inherited stdio whose REAL exit code this process exits with
    // — no `exec` exists there. Either way it returns only the error that stopped it.
    let mut command = crate::landing::shim_command(&layout, &tool, hand_over.args);
    let err = crate::platform::exec_or_run(&mut command);
    eprintln!(
        "atpkg: could not run {}: {err}",
        command.get_program().to_string_lossy()
    );
    ExitCode::from(126)
}

/// `atpkg __selfupdate <program> [<prefix>] -- [args…]` — the hidden verb an agent
/// program's `agents/` twin execs when the user's FIRST argument is one of the vendor's
/// own self-update verbs ([`crate::selfupdate`], 2026-09-19; owner: *"aterm reports that
/// these packages are automatically managed by atpkg to keep to the latest version (and
/// then does a check to make sure that they are updated and then actually updates) via
/// the standard pkg manager"*). The operands are [`crate::landing::HandOver`]'s, with one
/// addition: a program operand that starts with `-` (`atpkg __selfupdate --help`) is the
/// usage line, exit 2 — non-mutating by design, not by the accident of a refused name.
///
/// In order, each ending pinned in-process by `cmd_selfupdate_refuses_forwards_and_never_takes_the_lock`
/// and at the process edge by `tests/selfupdate_intercept.rs`:
/// 1. usage exit 2; not a tool name exit 2; the layout from the prefix operand (so no
///    `HOME` is needed — `env -i`, launchd) else [`layout`] (exit 1 with its line);
/// 2. no roster row ⇒ `exec` the `bin/<program>` shim verbatim, nothing printed — a twin
///    laid by a NEWER client against this older roster must still run the tool;
/// 3. THE PREFIX CROSS-CHECK: when [`crate::store::resolve_configured`] resolves here
///    (it falls back to the passwd home when `HOME` is unset) and, canonicalized, names a
///    DIFFERENT store than the operand, refuse with exit 2 naming both — a child `update`
///    would move THAT store, never the one this twin runs; when it cannot resolve at
///    all, the operand layout stands;
/// 4. [`crate::selfupdate::classify`]: help ⇒ one stderr note, then the shim verbatim
///    (the vendor prints its own help; never mutates); pass-through ⇒ the shim verbatim,
///    silent; otherwise on to 5 with the verdict in hand;
/// 5. a declined shape ⇒ its line, exit 2, nothing run — never handed to the vendor's
///    own updater, whose copy this name never runs, and with no bypass named, because
///    there is none (owner, 2026-09-19: no environment knobs; the first cut's escape
///    hatch is gone); a check ⇒ on;
/// 6. the manager disabled (`ATPKG_DISABLE`, or no root key) ⇒ the disabled line, exit 1,
///    nothing run — NOT handed to the vendor either, for the same reason; `aterm pkg
///    doctor` says why the manager is off;
/// 7. the announce line, flushed, then THE CHILD: the embedded co-located `atpkg` (else
///    this executable) as `update <program> --wait-lock 1800`
///    ([`crate::selfupdate::WAIT_LOCK_SECS`], the window's own bound — a store another
///    pass holds is waited for, audibly: the child's `lock-waiting:` after 2 s and
///    `lock-acquired:` when it gets the store) with `ATPKG_SPAWNER_PID` set to this pid
///    and stdio INHERITED — the shape the window's pass uses minus its `--progress-file`
///    — waited for as its parent for the whole wait (Ctrl-C reaches both through the
///    foreground group and ends it; no handler, no epilogue for a signal death).
///    The child's status, relayed with LITERAL arms so the exit-code registry
///    (`lock::contention_exit_code_is_temp_fail_and_unshared`) reads them: 0 ⇒ nothing
///    more, the child's own stdout line was the verdict; 2 ⇒ 2; 75 ⇒ the contended line
///    and 75; anything else (1, a signal) ⇒ the incomplete line and 1; a spawn that
///    fails ⇒ `could not run`, 126.
///
/// WHY A CHILD and not `cmd_update_one` in-process: hidden verbs are dispatched before
/// the lock and outside [`verb_mutates_store`], so an in-process update would be an
/// unaudited lock-taker; the `--wait-lock`/orphan statics are edge-owned; a panic would
/// die holding the lock inside the twin's process; and the child gets the real dispatch
/// edge and, for its own lines, bytes identical to a typed `aterm pkg update claude` —
/// plus the `--wait-lock` lane's stdout markers a typed verb never prints (measured
/// 2026-09-19 under a held lock: `lock-waiting:` after the 2 s grace, `lock-acquired:`
/// when the holder lets go, `seed-busy:` at the 75 ending; [`SEED_BUSY_MARKER`] records
/// the exception, `tests/selfupdate_intercept.rs` pins the two the contended ending
/// prints). WHY NOT `exec` the child: the 75 and 1 epilogues are what turn "refusing to
/// mutate the store concurrently" into "the window's pass is doing this work".
fn cmd_selfupdate(rest: &[String]) -> ExitCode {
    let hand_over = crate::landing::HandOver::parse(rest).filter(|h| !h.program.starts_with('-'));
    let Some(hand_over) = hand_over else {
        eprintln!(
            "usage: atpkg {} <program> [<prefix>] -- [args…]",
            crate::selfupdate::HIDDEN_VERB
        );
        return ExitCode::from(2);
    };
    let program = hand_over.program;
    let Some(tool) = crate::store::ToolName::new(program) else {
        eprintln!("atpkg: {program:?} is not a tool name");
        return ExitCode::from(2);
    };
    let layout = match hand_over.prefix {
        Some(prefix) => crate::store::Layout { prefix },
        None => match layout() {
            Some(l) => l,
            None => return ExitCode::from(1),
        },
    };
    let resolved = crate::store::resolve_configured();
    match selfupdate_plan(
        &layout,
        &tool,
        hand_over.args,
        resolved.as_ref(),
        manager_enabled(),
    ) {
        SelfUpdatePlan::Refuse(line) => {
            eprintln!("{line}");
            ExitCode::from(2)
        }
        SelfUpdatePlan::Disabled(line) => {
            eprintln!("{line}");
            ExitCode::from(1)
        }
        SelfUpdatePlan::Forward { mut command, note } => {
            if let Some(note) = note {
                eprintln!("{note}");
            }
            // The `bin/<program>` shim — never the `agents/` twin, whose block would hand
            // over here again — with the arguments verbatim: `exec(2)` on Unix, a child
            // with the real exit code on Windows ([`crate::platform::exec_or_run`]).
            let err = crate::platform::exec_or_run(&mut command);
            eprintln!(
                "atpkg: could not run {}: {err}",
                command.get_program().to_string_lossy()
            );
            ExitCode::from(126)
        }
        SelfUpdatePlan::Check { row, words } => selfupdate_check(
            &layout,
            row,
            &words,
            &selfupdate_child_binary(),
            crate::selfupdate::WAIT_LOCK_SECS,
        ),
    }
}

/// What [`cmd_selfupdate`] decided, before anything runs or prints — the pure half of
/// the verb, so every ending that is not the child is pinned in-process (an `exec`
/// would replace the test process; the tests run the planned command as a child instead
/// and read its argv off a log).
#[derive(Debug)]
enum SelfUpdatePlan {
    /// One stderr line, exit 2, nothing run: the prefix cross-check, or a declined shape.
    Refuse(String),
    /// One stderr line, exit 1, nothing run: the manager is disabled here.
    Disabled(String),
    /// `exec` the `bin/<program>` shim with the arguments verbatim — after `note` when
    /// there is one (the help note); nothing printed for a pass-through or a program
    /// with no row.
    Forward {
        command: std::process::Command,
        note: Option<String>,
    },
    /// Announce, then the standard update as a child ([`selfupdate_check`]).
    Check {
        row: &'static crate::selfupdate::Row,
        words: String,
    },
}

/// Steps 2–6 of [`cmd_selfupdate`], pure: the row, the prefix cross-check against what
/// this environment `resolved` (`None` ⇒ nothing to check against), the
/// classification, the manager's enablement. No environment is read here: `enabled` is
/// the one fact the process supplies (`manager_enabled()`), and there is no other.
fn selfupdate_plan(
    layout: &crate::store::Layout,
    tool: &crate::store::ToolName,
    args: &[String],
    resolved: Option<&crate::store::Layout>,
    enabled: bool,
) -> SelfUpdatePlan {
    use crate::selfupdate::{self, Verdict};
    let forward = |note: Option<String>| SelfUpdatePlan::Forward {
        command: crate::landing::shim_command(layout, tool, args),
        note,
    };
    let Some(row) = selfupdate::row_for(tool.as_str()) else {
        return forward(None);
    };
    if let Some(resolved) = resolved {
        let canon =
            |p: &std::path::Path| std::fs::canonicalize(p).unwrap_or_else(|_| p.to_path_buf());
        if canon(&resolved.prefix) != canon(&layout.prefix) {
            return SelfUpdatePlan::Refuse(selfupdate::prefix_mismatch_line(
                row,
                &layout.prefix,
                &resolved.prefix,
            ));
        }
    }
    // A declined shape is simply declined (exit 2, nothing run): the vendor's own
    // updater is never run through the managed name — the copy it installs is one this
    // name never runs — and nobody is told about a bypass, because there is none (the
    // first cut's environment escape hatch was removed the same day at the owner's
    // direction). Help and a pass-through forward, with their own note or none.
    let words = match selfupdate::classify(row, args) {
        Verdict::Help => return forward(Some(selfupdate::help_line(row))),
        Verdict::PassThrough => return forward(None),
        Verdict::Declined(d) => {
            return SelfUpdatePlan::Refuse(selfupdate::declined_line(row, &d));
        }
        Verdict::Check { words, .. } => words,
    };
    if !enabled {
        let build = crate::ops::active_builds(layout).get(row.program).copied();
        return SelfUpdatePlan::Disabled(selfupdate::disabled_line(row, build));
    }
    SelfUpdatePlan::Check { row, words }
}

/// Step 7 of [`cmd_selfupdate`]: the announce line, then the standard update as a child
/// this process stays the parent of — `atpkg` run as `update <program> --wait-lock
/// <bound>` ([`crate::selfupdate::child_argv`]) — and the child's status relayed with
/// literal arms. `atpkg` is [`selfupdate_child_binary`] and `bound` is
/// [`crate::selfupdate::WAIT_LOCK_SECS`] in production; both are PARAMETERS so the
/// in-process test can drive the real child, against a store lock it holds itself,
/// through a wrapper that confines it to a temp HOME, with a one-second bound — the 75
/// ending, which no environment knob exists to shorten (and none may: owner,
/// 2026-09-19). The active build the epilogues name is read AFTER the child, offline
/// ([`crate::ops::active_builds`]): it is the build that STAYS.
fn selfupdate_check(
    layout: &crate::store::Layout,
    row: &crate::selfupdate::Row,
    words: &str,
    atpkg: &std::path::Path,
    bound: u64,
) -> ExitCode {
    use std::io::Write as _;
    eprintln!("{}", crate::selfupdate::announce_line(row, words));
    let _ = std::io::stderr().flush();
    let status = std::process::Command::new(atpkg)
        .args(crate::selfupdate::child_argv(atpkg, row.program, bound))
        .env(
            SPAWNER_PID_ENV,
            crate::dec_u64(u64::from(std::process::id())),
        )
        .spawn()
        .and_then(|mut child| child.wait());
    let status = match status {
        Ok(status) => status,
        Err(e) => {
            eprintln!(
                "{}",
                crate::selfupdate::could_not_run_line(atpkg, row.program, &e)
            );
            return ExitCode::from(126);
        }
    };
    let build = || crate::ops::active_builds(layout).get(row.program).copied();
    match status.code() {
        Some(0) => ExitCode::SUCCESS,
        Some(2) => ExitCode::from(2),
        Some(c) if c == i32::from(crate::lock::CONTENDED_EXIT) => {
            eprintln!("{}", crate::selfupdate::contended_line(row, build(), bound));
            ExitCode::from(crate::lock::CONTENDED_EXIT)
        }
        _ => {
            eprintln!("{}", crate::selfupdate::incomplete_line(row, build()));
            ExitCode::from(1)
        }
    }
}

/// The binary [`selfupdate_check`] runs as the child: the embedded co-located `atpkg`
/// the twin itself execs ([`crate::stub::embedded_atpkg_path`] — the binary this process
/// IS when it came through the twin) when that is an executable regular file, else this
/// process's own executable. The same code either way — but NOT the same dispatch: the
/// one binary routes on its name, and under any name but `atpkg` a bare `update` is
/// aterm's app-update lane, so [`crate::selfupdate::child_argv`] puts `pkg` ahead of the
/// argv for the fallback's `aterm` (review, 2026-09-19: a sibling-less `aterm` spawned
/// `aterm update claude`, got `unknown update sub-command`, exit 2, and relayed it as
/// the check's). The fallback is reached only off the twin's path — the twin's own
/// `[ -x "$__atpkg" ]` guarantees the embedded path from a hand-over; it covers a
/// hand-typed hidden verb from a dev `aterm` copied out of `target/` or a bundle
/// missing the alias symlink.
fn selfupdate_child_binary() -> std::path::PathBuf {
    let embedded = crate::stub::embedded_atpkg_path();
    if is_executable_regular_file(&embedded) {
        return embedded;
    }
    std::env::current_exe().unwrap_or(embedded)
}

/// A regular file with an execute bit (any execute bit, on Unix; regular is enough
/// elsewhere) — the `[ -x ]` the twin's block asks before its own hand-over.
fn is_executable_regular_file(path: &std::path::Path) -> bool {
    let Ok(md) = std::fs::metadata(path) else {
        return false;
    };
    if !md.is_file() {
        return false;
    }
    #[cfg(unix)]
    {
        use std::os::unix::fs::PermissionsExt as _;
        md.permissions().mode() & 0o111 != 0
    }
    #[cfg(not(unix))]
    {
        true
    }
}

/// Set by the handler [`landing_wait_in_process`] installs for the wait's duration —
/// `landing_sigint` on Unix, [`landing_ctrl`] on Windows: the wait stops, the current
/// build runs.
static LANDING_INTERRUPTED: std::sync::atomic::AtomicBool =
    std::sync::atomic::AtomicBool::new(false);

/// The handler: one atomic store, nothing else (async-signal-safe).
#[cfg(unix)]
extern "C" fn landing_sigint(_signal: libc::c_int) {
    LANDING_INTERRUPTED.store(true, std::sync::atomic::Ordering::SeqCst);
}

/// The Windows twin of [`landing_sigint`]: the console control handler the wait installs
/// ([`crate::platform::add_ctrl_handler`]). Ctrl-C and Ctrl-Break are the one store and
/// `TRUE` (handled: the process lives, the wait's next slice sees the flag); a console
/// close, logoff or shutdown is passed on (`FALSE`) to the default, which ends the
/// process as it should. Without this the default handler ended `atpkg.exe` on the first
/// Ctrl-C and the "— Ctrl-C runs <current> now" the waiting line promises was false on
/// the platform the `.cmd` twin had just made it reachable on (review finding,
/// 2026-09-17; no Windows box has run it).
#[cfg(windows)]
unsafe extern "system" fn landing_ctrl(ctrl_type: u32) -> i32 {
    if matches!(
        ctrl_type,
        crate::platform::CTRL_C_EVENT | crate::platform::CTRL_BREAK_EVENT
    ) {
        LANDING_INTERRUPTED.store(true, std::sync::atomic::Ordering::SeqCst);
        1
    } else {
        0
    }
}

/// The process half of the landing wait: the Ctrl-C handler for its duration (`SIGINT`
/// on Unix; a console control handler on Windows, [`landing_ctrl`]), stderr as the seam
/// (a terminal gets the waiting line refreshed in place with `\r`; a pipe gets one line
/// per change), and a sleep sliced fine enough that Ctrl-C answers within 100 ms. The
/// state machine itself is [`crate::landing::wait_for_landing`].
fn landing_wait_in_process(layout: &crate::store::Layout, program: &str, max_wait: u64) {
    use std::io::{IsTerminal as _, Write as _};
    let tty = std::io::stderr().is_terminal();
    LANDING_INTERRUPTED.store(false, std::sync::atomic::Ordering::SeqCst);
    // Best effort: when the install fails the wait still runs and every ending still
    // runs the tool — only the promise that Ctrl-C ends the wait early is then the
    // default disposition's (the process ends), which is what it was before 2026-09-17.
    #[cfg(windows)]
    let armed = crate::platform::add_ctrl_handler(landing_ctrl);
    #[cfg(unix)]
    let previous = {
        // SAFETY: `signal(2)` installs a handler that does one atomic store; the
        // previous disposition is kept and restored below, and no memory of ours is
        // touched from the handler.
        unsafe {
            libc::signal(
                libc::SIGINT,
                landing_sigint as *const () as libc::sighandler_t,
            )
        }
    };
    let mut pending_refresh = false;
    let mut last: Option<String> = None;
    let mut say = |line: &str, refresh: bool| {
        let mut err = std::io::stderr().lock();
        if tty {
            if refresh {
                let _ = write!(err, "\r{line}\x1b[K");
                pending_refresh = true;
            } else {
                if pending_refresh {
                    let _ = writeln!(err);
                    pending_refresh = false;
                }
                let _ = writeln!(err, "{line}");
            }
        } else if !refresh || last.as_deref() != Some(line) {
            let _ = writeln!(err, "{line}");
        }
        last = Some(line.to_string());
        let _ = err.flush();
    };
    let mut sleep = |d: std::time::Duration| {
        let slice = std::time::Duration::from_millis(100);
        let deadline = std::time::Instant::now() + d;
        while std::time::Instant::now() < deadline {
            if LANDING_INTERRUPTED.load(std::sync::atomic::Ordering::SeqCst) {
                return false;
            }
            std::thread::sleep(
                slice.min(deadline.saturating_duration_since(std::time::Instant::now())),
            );
        }
        !LANDING_INTERRUPTED.load(std::sync::atomic::Ordering::SeqCst)
    };
    let mut io = crate::landing::LandingIo {
        say: &mut say,
        sleep: &mut sleep,
    };
    let _ = crate::landing::wait_for_landing(layout, program, max_wait, &mut io);
    #[cfg(unix)]
    {
        // SAFETY: restores the disposition captured above before the exec.
        unsafe {
            libc::signal(libc::SIGINT, previous);
        }
    }
    #[cfg(windows)]
    {
        // The hand-over (`platform::exec_or_run`) arms its own handler for the child's
        // lifetime, so the wrapper never dies under the agent's Ctrl-C.
        if armed {
            let _ = crate::platform::remove_ctrl_handler(landing_ctrl);
        }
    }
}

/// `atpkg __reroute <upstream> [args…]` — the hidden verb a reroute stub execs
/// (philosophy §4, [`crate::reroute::run`]). Unlisted for the reason `__pending` is:
/// it is machinery a stub reaches, not vocabulary a user types, so it is dispatched
/// before the verb match and the roster scraper never sees it. Dispatched BEFORE the
/// store lock too: a `cargo build` typed while the installer HOLDS `store.lock` must
/// still get its signpost instantly, and the verb never mutates the store — it
/// announces, refuses, or execs a copy that is already there. The arguments after
/// the upstream name are the caller's, VERBATIM: the stub inserts no separator of
/// its own, so a leading `--` here is the user's and is kept (`rustc -- x.rs`).
fn cmd_reroute(rest: &[String]) -> ExitCode {
    let Some((upstream, args)) = rest.split_first() else {
        eprintln!(
            "usage: atpkg {} <upstream> [args…]",
            crate::reroute::HIDDEN_VERB
        );
        return ExitCode::from(2);
    };
    let Some(layout) = layout() else {
        // No resolvable store (no `HOME`, an `env -i` wrapper): the same
        // fail-closed answer the stub gives when atpkg is unreachable — the
        // escape named, exit 2 — never an unexplained 1.
        eprintln!("{}", crate::reroute::unreachable_message(upstream));
        return ExitCode::from(crate::reroute::REFUSAL_EXIT);
    };
    crate::reroute::run(&layout, upstream, args)
}

/// Re-assert the rustup SEAM: the managed `trust` toolchain linked into rustup as
/// channel `trust`, so `cargo +trust`, `targo` resolved through rustup, and every
/// repo pinning `channel = "trust"` all work on THIS machine.
///
/// `seam.rs` has carried attach/detach/reassert since the seam was designed, and
/// its header CLAIMS re-assertion "after every successful activation of `trust`,
/// after a rollback of `trust`, and at the end of the unattended `update` pass".
/// MEASURED 2026-09-08: nothing called it. The managed toolchain was invisible
/// to rustup on every fresh machine, and the first thing a Rust project pinning
/// the channel saw was `'rustc' is not installed for the custom toolchain
/// 'trust'` — the message that reads as a blocked machine. Owner, the same day:
/// the Trust toolchain is to be "very strongly encouraged by the aterm system
/// itself"; a store rustup cannot see is the opposite of that.
///
/// Fail-soft by construction: no rustup home ⇒ nothing to do; `reassert` only
/// ever creates a name nothing else owns, adopts a link already pointing into
/// the store, and REFUSES (with the fix printed) anything foreign under
/// `~/.rustup` — it never follows or replaces an entry aterm did not lay.
fn reassert_rustup_seam(layout: &crate::store::Layout) {
    let Some(home) = crate::seam::rustup_home() else {
        return;
    };
    for line in crate::seam::reassert(layout, &home) {
        println!("atpkg: rustup seam: {line}");
    }
}

/// Bring the trust EXEC ROOTS and every trust shim in line ([`crate::compat::reconcile`]),
/// print `atpkg: exec root: …` only for what changed or failed, and answer whether nothing
/// failed.
///
/// Why a pass-level call and not only the lay in `activate::install_tools_env`: an
/// up-to-date program is short-circuited before any shim is laid, and the alias and agents
/// reconciles write only their own names, never a primary. A machine already on trust 8595
/// — whose `bin/rustc` is a separate copy of `trustc` that its tippy refuses — would
/// otherwise keep plain primaries until something re-laid them: a `repair` nobody knew to
/// run, or the next trust build. Called from the update pass (after its gc), the default-set
/// lane (after its alias reconcile), `repair` (at `Deep`, where a failure is exit 1),
/// `seed` (beside the rustup seam) and a trust `rollback`. It prints nothing when nothing
/// changed and nothing failed, so it adds no line to the six-hourly tick.
///
/// Changes go to stdout, failures to stderr: the window that runs a pass reads only the
/// marker lines off its stdout and keeps its whole stderr as what the pass said
/// (aterm-gui's `read_seed_markers` and `read_pass_stderr`), and a root that could not be
/// laid is the line someone has to see.
fn reconcile_exec_roots(layout: &crate::store::Layout, depth: crate::seam::Depth) -> bool {
    let report = crate::compat::reconcile(layout, depth);
    for n in &report.built {
        println!("{}", crate::compat::laid_line(layout, *n));
    }
    if !report.routed.is_empty() {
        let names: Vec<String> = report
            .routed
            .iter()
            .filter_map(|p| p.file_name().map(|n| n.to_string_lossy().into_owned()))
            .collect();
        println!(
            "atpkg: exec root: re-laid {} trust shim(s) to match the exec roots: {}",
            names.len(),
            names.join(", ")
        );
    }
    for path in &report.swept {
        println!(
            "atpkg: exec root: removed {} — not a root any build here needs (its build is \
             gone or needs none, or it is debris a killed run left)",
            path.display()
        );
    }
    for e in &report.errors {
        eprintln!("atpkg: exec root: warn — {e}");
    }
    report.errors.is_empty()
}

/// The whole-set removal's companion ([`crate::seam::detach_recorded`]): detach every
/// rustup seam atpkg recorded, never `--force` — an entry atpkg did not lay is refused
/// and named, not removed. Nothing recorded, or no rustup home: silent.
fn detach_rustup_seams(layout: &crate::store::Layout) {
    let Some(home) = crate::seam::rustup_home() else {
        return;
    };
    for line in crate::seam::detach_recorded(layout, &home) {
        println!("atpkg: rustup seam: {line}");
    }
}

/// Lay (or refresh) the reroute stubs beside the pending-stub reconcile — at seed,
/// after each install pass, and on `repair` — and never let a failure fail the pass:
/// the stubs are a session convenience the spawn seam re-lays anyway, while the pass
/// that called this just installed a toolchain the user is waiting for. One stderr
/// line names what did not happen.
fn lay_reroute_stubs(layout: &crate::store::Layout) {
    if let Err(err) = crate::reroute::lay(layout) {
        eprintln!(
            "atpkg: warn — reroute stubs not laid under {}: {err} (an aterm session lays \
             them at spawn; `aterm pkg repair` retries)",
            crate::reroute::dir(layout).display()
        );
    }
}

/// The `managed` state `which` prints for `program` at `build`: the row's own pin when
/// the row is a managed one; otherwise — the row a fault (`aborted: stage`, `error: …`)
/// or absent — the CACHED index's answer, so the line says what the index pins and that
/// the pass did not get there. It used to synthesize `pinned by index <floor>` from the
/// durable floor over any row (audit 2026-09-14: `trust` read "managed 6808 — pinned by
/// index 30" while index 30 pinned 8595 and the row said `aborted: stage`).
fn managed_state_of(
    layout: &crate::store::Layout,
    program: &str,
    build: u64,
    row: Option<&crate::ProgramStatus>,
) -> String {
    if let Some((_, index)) = row.and_then(|r| crate::state::managed_pin(&r.state)) {
        return crate::state::managed(build, index);
    }
    let Some(index) = cached_index(layout) else {
        return match row {
            Some(r) => format!("managed {build} — last pass: {}", r.state),
            None => crate::state::managed(build, build_floor(layout).index_build),
        };
    };
    let pinned = index
        .channel_for(crate::config::cached().channel(), current_triple())
        .and_then(|ch| ch.pin.get(program).copied());
    match (pinned, row) {
        (Some(pin), _) if pin == build => crate::state::managed(build, index.index_build),
        (Some(pin), Some(r)) => format!(
            "managed {build} — index {} pins {pin} (last pass: {})",
            index.index_build, r.state
        ),
        (Some(pin), None) => format!(
            "managed {build} — index {} pins {pin} (not yet applied)",
            index.index_build
        ),
        (None, Some(r)) => format!("managed {build} — last pass: {}", r.state),
        (None, None) => crate::state::managed(build, index.index_build),
    }
}

/// The pending verb's body, shared by `__pending` and `atpkg run`'s pending arm so
/// the three dead ends (the shell stub, `aterm <tool>`, `atpkg run <tool>`) converge
/// on ONE state machine — consent question included. Interactive iff stdin AND
/// stdout are terminals (the question is printed to one and answered on the other);
/// a pipe, a script or an agent harness is never prompted. Whether a running aterm
/// will act on a bump is read from `ATERM_CHILD`, the marker the GUI stamps on every
/// child it spawns.
///
/// Exits 127 — whatever was printed, or installed, the command the user typed did not
/// run — with ONE exception (2026-09-15): an AGENT program with a foreign copy to run
/// meanwhile ([`pending_passthrough`]) has that copy `exec`ed in this process's place,
/// so the user's command really does run and this never returns; 126 if that `exec`
/// itself fails, and off Unix, where there is no `exec`, the 127 stands.
fn run_pending(layout: &crate::store::Layout, tool: &str, passthrough: &[String]) -> ExitCode {
    use std::io::IsTerminal as _;
    let interactive = std::io::stdin().is_terminal() && std::io::stdout().is_terminal();
    // An agent program with a foreign copy to run meanwhile says its lines on STDERR:
    // the copy's stdout is the user's (a pipe, a script), and the state is a note.
    let onward = pending_passthrough(layout, tool, std::env::var_os("PATH").as_deref());
    let to_stderr = onward.is_some();
    let mut say = |line: String| {
        if to_stderr {
            eprintln!("{line}");
        } else {
            println!("{line}");
        }
    };
    let mut ask = tty_consent;
    let mut io = PendingIo {
        say: &mut say,
        ask: if interactive { Some(&mut ask) } else { None },
        gui_present: inside_aterm(),
    };
    if pending_state(layout, tool, &mut io) == PendingNext::InstallInline {
        // THE CONSENTED INSTALL, headless, in this process: re-entered through the
        // dispatch edge so the store lock is taken exactly as `aterm pkg install
        // <tool>` takes it (this verb itself runs lock-free, by design — it must
        // answer while an installer HOLDS the lock). The install verb prints its own
        // outcome; the one line added is the honest post-check — does the name
        // resolve to a real shim now? — never the verb's exit code re-narrated.
        let _ = main_entry(vec![
            std::ffi::OsString::from("install"),
            std::ffi::OsString::from(tool),
        ]);
        if crate::which(layout, tool).is_some() {
            println!("atpkg: {tool} is installed — run it again.");
        } else {
            println!(
                "atpkg: {tool} did not install — the lines above say why; fix: aterm pkg \
                 install {tool}"
            );
        }
    }
    if let Some(exe) = onward {
        eprintln!(
            "atpkg: running {} meanwhile — the managed {tool} takes over once it is installed",
            exe.display()
        );
        #[cfg(unix)]
        {
            use std::os::unix::process::CommandExt as _;
            let err = std::process::Command::new(&exe).args(passthrough).exec();
            eprintln!("atpkg: could not run {}: {err}", exe.display());
            return ExitCode::from(126);
        }
        #[cfg(not(unix))]
        {
            // No `exec(2)` to hand the process over with: the stub contract's 127
            // stands, and the line above told the user which copy to run.
            let _ = passthrough;
            return ExitCode::from(127);
        }
    }
    ExitCode::from(127)
}

/// Whether this process runs inside an aterm session — `ATERM_CHILD=1`, stamped on
/// every child the GUI spawns — and so a running aterm's bump watch (design §4) will
/// turn a fresh bump into an install pass on its own, rate floor permitting.
fn inside_aterm() -> bool {
    std::env::var_os("ATERM_CHILD").is_some_and(|v| v == "1")
}

/// The TTY consent question: `prompt` to stdout (flushed, no newline), ONE line from
/// stdin; `y`/`yes` in any case is consent, anything else — a bare Enter, `n`, EOF,
/// a read error — is not. Default NO, so an accidental keystroke never moves 200 MB.
fn tty_consent(prompt: &str) -> bool {
    use std::io::Write as _;
    print!("{prompt}");
    let _ = std::io::stdout().flush();
    let mut line = String::new();
    if std::io::stdin().read_line(&mut line).is_err() {
        return false;
    }
    matches!(line.trim().to_ascii_lowercase().as_str(), "y" | "yes")
}

/// How long an echoed error line may get before visible elision — generous enough
/// for every honest FlowError, finite against a hostile record.
const PENDING_ERROR_CAP: usize = 300;

/// The `__pending` state machine's I/O seam, so every state — the consent question
/// included — is exercisable without a process, a TTY or an environment variable:
/// `say` receives each honest line in order (production prints it; tests collect
/// it); `ask` is the consent question, `None` when no terminal can answer — and then
/// an extra is NEVER prompted, opted in or bumped; `gui_present` is whether a running
/// aterm's bump watch will act on a bump ([`inside_aterm`]).
struct PendingIo<'a> {
    say: &'a mut dyn FnMut(String),
    ask: Option<&'a mut dyn FnMut(&str) -> bool>,
    gui_present: bool,
}

/// What the caller of [`pending_state`] does after the lines: nothing more, or run
/// the consented install inline (headless, no installer running — the ONE case where
/// the stub's own process is the only one that can move the bytes).
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum PendingNext {
    Done,
    InstallInline,
}

/// The five honest states (+ the honesty edge, + the extras' consent state), ONE
/// function for the three dead ends. SIDE-EFFECTFUL on purpose: the bump append
/// (and, for an extra, the opt-in marker) is part of the state machine — a claimed
/// bump must really have been written — so the tests exercise message and channel
/// together.
fn pending_state(layout: &crate::store::Layout, tool: &str, io: &mut PendingIo<'_>) -> PendingNext {
    // The name gate first: only an admitted ToolName is ever echoed back to the TTY
    // (an unadmitted one could never have a stub OR a shim, so nothing pends).
    let Some(tn) = crate::store::ToolName::new(tool) else {
        (io.say)("atpkg: that is not an installable program name".to_string());
        return PendingNext::Done;
    };
    let name = tn.as_str();
    // THE EXTRAS GATE, before any bump (owner decision 2026-08-26): an extra this
    // machine has not opted in to is not coming — no pass plans it — so every
    // default-set state below would be a lie for it. The stub on disk says what the
    // program IS (the reconcile writes that from the signed index's `extra` flag);
    // the compiled roster answers for a name typed with no stub laid. The opt-in
    // marker says what the user ANSWERED: once it exists the extra is in every
    // pass's wanted set and the ordinary states tell its truth.
    // An AGENT PROGRAM (`claude`, `codex`) is default-set on this client whatever a
    // stub laid by an older pass says (owner decision 2026-09-10): the pass is
    // installing it, so the default-set states below are its truth.
    let extra = !crate::stub::is_agent_program(name)
        && crate::stub::pending_stub_kind(layout, name).map_or_else(
            || crate::stub::compiled_extra(name),
            |kind| kind == crate::stub::StubKind::Extra,
        );
    if extra && !layout.optin_exists(name) {
        return pending_extra_consent(layout, &tn, io);
    }
    let name = tn.as_str();
    match crate::stub::describe(name) {
        Some(what) => (io.say)(format!("atpkg: {name}: {what} — not installed yet.")),
        None => (io.say)(format!(
            "atpkg: {name}: part of the ALab toolset — not installed yet."
        )),
    }
    let snapshot = crate::progress::read_progress(layout);
    let now = crate::flow::now_unix().max(0).unsigned_abs();
    let running = snapshot
        .as_ref()
        .is_some_and(|f| crate::progress::snapshot_running(f, now));
    if let Some(file) = snapshot.as_ref().filter(|_| running) {
        if file.v != crate::progress::PROGRESS_VERSION {
            // A NEWER writer's file: render the generic line, never a guess at
            // fields whose meaning may have changed. The bump file's contract is
            // versionless (names only), so the bump is still honest.
            (io.say)(format!(
                "atpkg: aterm is installing the toolset now — re-run {name} shortly."
            ));
            let _ = crate::progress::append_bump(layout, &tn);
            return PendingNext::Done;
        }
        let overall = &file.overall;
        (io.say)(format!(
            "atpkg: aterm is installing your toolchain now ({} of {} done).",
            overall.programs_done, overall.programs_total
        ));
        let Some(row) = file.programs.get(name) else {
            // THE HONESTY EDGE: a resolved-pass snapshot is readable and does not
            // plan this program — a compiled-roster stub the signed index no longer
            // lists. Claiming "bumped" would be a lie the reorder-only intersection
            // silently swallows; the reconcile removes this stub at pass end.
            (io.say)(format!(
                "atpkg: {name} is no longer part of the default set — nothing is \
                 scheduled to install it. `aterm pkg install {name}` tries it \
                 individually."
            ));
            return PendingNext::Done;
        };
        use crate::progress::Phase;
        match row.phase {
            Phase::Download => {
                if row.bytes_total > 0 {
                    (io.say)(format!(
                        "atpkg: {name} is downloading NOW — {:.1} of {:.1} MB ({}%). \
                         Re-run {name} when it lands.",
                        row.bytes_done as f64 / 1e6,
                        row.bytes_total as f64 / 1e6,
                        row.bytes_done.saturating_mul(100) / row.bytes_total.max(1)
                    ));
                } else {
                    (io.say)(format!(
                        "atpkg: {name} is downloading NOW — re-run it when it lands."
                    ));
                }
            }
            Phase::Verify | Phase::Extract | Phase::Link => {
                (io.say)(format!(
                    "atpkg: {name} is installing NOW ({}) — re-run it in a moment.",
                    match row.phase {
                        Phase::Verify => "verifying",
                        Phase::Extract => "extracting",
                        _ => "activating",
                    }
                ));
            }
            Phase::Done => {
                (io.say)(format!(
                    "atpkg: {name} just finished installing — re-run it now."
                ));
            }
            Phase::Failed => {
                let why = row.error.as_deref().unwrap_or("no recorded reason");
                (io.say)(format!(
                    "atpkg: {name} FAILED to install this pass: {} — fix: aterm pkg \
                     update retries it; Settings ▸ Packages shows details.",
                    crate::progress::sanitize_for_tty(why, PENDING_ERROR_CAP)
                ));
            }
            Phase::Queued => {
                let position = file.queue.iter().position(|q| q == name);
                let current = file.queue.first().filter(|c| c.as_str() != name);
                let bumped = crate::progress::append_bump(layout, &tn).is_ok();
                match (position, current, bumped) {
                    (Some(p), Some(cur), true) => {
                        // `cur` came off disk: it is echoed only after its own
                        // ToolName round-trip (queue names are untrusted).
                        let cur = crate::store::ToolName::new(cur).map_or_else(
                            || "the current program".to_string(),
                            |t| t.as_str().to_string(),
                        );
                        (io.say)(format!(
                            "atpkg: {name} was queued {} of {}; it is now BUMPED to \
                             install next, after {cur} finishes. Re-run it in a minute.",
                            p + 1,
                            file.queue.len()
                        ));
                    }
                    (_, _, true) => {
                        (io.say)(format!(
                            "atpkg: {name} is queued and now BUMPED to install next — \
                             re-run it in a minute."
                        ));
                    }
                    _ => {
                        (io.say)(format!(
                            "atpkg: {name} is queued — re-run it once the pass reaches it."
                        ));
                    }
                }
            }
            Phase::Skipped => {
                // SKIPPED IS TERMINAL, NOT A WAIT (2026-09-16 audit).
                // [`crate::progress::Sink::finished`] retires a skipped program FROM the
                // queue and advances `programs_done`, so nothing later in THIS pass
                // installs it — and the bump file only REORDERS what is still queued (a
                // clean pass end then deletes it), so no bump could put it back. Folded
                // into the `Queued` arm above, this phase claimed "queued and now BUMPED
                // to install next" for a member the pass had just held — blocked by an
                // unmet requirement, dev-linked, narrowed out of a locked tuple, held by
                // a local pin, tombstoned, or deferred `needs admin` — and buried the
                // reason the very same call recorded. Say that reason instead, and claim
                // no bump: the honesty edge above, one phase further on.
                match row.error.as_deref() {
                    Some(why) => (io.say)(format!(
                        "atpkg: {name} was SKIPPED by this pass: {} — nothing later in \
                         this pass installs it; `aterm pkg install {name}` tries it \
                         individually, and Settings ▸ Packages shows details.",
                        crate::progress::sanitize_for_tty(why, PENDING_ERROR_CAP)
                    )),
                    None => (io.say)(format!(
                        "atpkg: {name} was SKIPPED by this pass — it is already current, \
                         or nothing in this pass installs it. Re-run it; if it still does \
                         not run: aterm pkg install {name}"
                    )),
                }
            }
        }
        return PendingNext::Done;
    }
    // NOT RUNNING (no file, stale heartbeat, dead pid, or a terminal snapshot):
    // only not-running states may render, whatever the snapshot claims. A recorded
    // failure outranks the generic line — every failure names its next act.
    if let Some(status) = crate::status::read(layout) {
        if let Some(row) = status.programs.get(name)
            && row.state.starts_with("error")
        {
            (io.say)(format!(
                "atpkg: the last install attempt FAILED: {} — fix: aterm pkg update \
                 retries it; Settings ▸ Packages shows details.",
                crate::progress::sanitize_for_tty(&row.state, PENDING_ERROR_CAP)
            ));
            let _ = crate::progress::append_bump(layout, &tn);
            return PendingNext::Done;
        }
        // A member the pinned build does not serve on THIS target — the canonical
        // `unavailable on <target>: <hint>` row the pass records per program — is not
        // coming on its own either: say the row's own words (the hint names why), and
        // do not bump: nothing can move.
        if let Some(row) = status.programs.get(name)
            && row.state.starts_with(crate::state::UNAVAILABLE_PREFIX)
        {
            (io.say)(format!(
                "atpkg: {name} is not coming on its own: {} — Settings ▸ Packages \
                 shows details.",
                crate::progress::sanitize_for_tty(&row.state, PENDING_ERROR_CAP)
            ));
            return PendingNext::Done;
        }
        // A member BLOCKED BY A REQUIREMENT (`blocked by <dep>: <dep state>`, §17.10)
        // is coming — after the dependency. Say whose act that is (the row quotes the
        // dependency's own state), name the door that does both in order, and bump:
        // the dependency's state may have moved since the row was written.
        if let Some(row) = status.programs.get(name)
            && let Some((dep, dep_state)) = crate::state::blocked_by(&row.state)
        {
            (io.say)(format!(
                "atpkg: {name} is waiting on {dep} ({}) — `aterm pkg install {name}` \
                 installs both, in order; Settings ▸ Packages shows details.",
                crate::progress::sanitize_for_tty(dep_state, PENDING_ERROR_CAP)
            ));
            let _ = crate::progress::append_bump(layout, &tn);
            return PendingNext::Done;
        }
        // A BLOCKED toolset outranks the generic promise: "open aterm, it
        // provisions" is a lie when the recorded `*toolset*` row says the
        // registry publishes no build for this machine (or the disk cannot
        // hold one) — the reconcile pass writes that row precisely so this
        // stub, which may outlive the pass by months, can tell the truth.
        if let Some(row) = status.programs.get("*toolset*")
            && let Some(why) = row.state.strip_prefix("blocked: ")
        {
            (io.say)(format!(
                "atpkg: {name} is not coming on its own: {} — Settings ▸ Packages \
                 shows details.",
                crate::progress::sanitize_for_tty(why, PENDING_ERROR_CAP)
            ));
            // Still worth a bump: an adopted machine that just freed space gets
            // an immediate pass from the GUI's bump watch; an unserved-triple
            // pass re-skips quietly. The seed's own disk refusal leaves the store
            // UNADOPTED ([`refuse_seed_for_disk`]), so that pass is an empty no-op
            // and the sealed install is retried by the next launch's seed.
            let _ = crate::progress::append_bump(layout, &tn);
            return PendingNext::Done;
        }
    }
    // BUMP FIRST, THEN SAY WHAT THAT MEANS — the order the `Queued` arm above
    // already uses, and for the same reason. Advising "run: aterm pkg update"
    // and THEN recording the wish told the user to do by hand the very thing
    // the wish had just arranged: an open aterm window stats this file every
    // 5 s and turns a fresh bump into an immediate pass, so the honest sentence
    // depends on whether the bump was actually recorded.
    let bumped = crate::progress::append_bump(layout, &tn).is_ok();
    (io.say)(if bumped {
        format!(
            "atpkg: nothing is installing right now — {name} is recorded, and an open \
             aterm window starts a pass within seconds (at most one every 5 minutes). \
             No window open? Open aterm, or run: aterm pkg update"
        )
    } else {
        "atpkg: nothing is installing right now — fix: open aterm (it provisions the \
         toolset on launch), or run: aterm pkg update"
            .to_string()
    });
    PendingNext::Done
}

/// The "extra, not opted in" state (owner decision 2026-08-26; design §5 extended):
/// name the vendor, the license, the size and the host — the authored roster line —
/// and ASK. No byte moves before the answer. `y` records the opt-in marker FIRST
/// (that is what adds the program to every later pass's wanted set — the bump file
/// stays reorder-only), then bumps, then hands the install to whoever can run it: a
/// pass already running finishes and the bump front-loads the next; a running
/// aterm's bump watch turns the fresh mtime into a pass (rate-floored — within a few
/// minutes); headless with nothing running, the caller runs `install <tool>` inline.
/// `N`, a bare Enter, or no terminal at all: say how to opt in later and move nothing
/// — not even a bump, which for an unplanned name could only trigger a pass that
/// installs nothing.
fn pending_extra_consent(
    layout: &crate::store::Layout,
    tn: &crate::store::ToolName,
    io: &mut PendingIo<'_>,
) -> PendingNext {
    let name = tn.as_str();
    let what = crate::stub::describe(name).unwrap_or("an extra the signed index lists");
    (io.say)(format!(
        "atpkg: {name}: {what} — an EXTRA: not part of the default set; nothing downloads \
         until you say so."
    ));
    // What it REQUIRES (§17.10), read from the stub the reconcile wrote from the signed
    // index — offline, like everything else here. The question names them: consenting
    // to the program is consenting to what it needs. An extra among them is NOT opted
    // in by this answer — it installs with this program at the explicit door, and the
    // pass keeps only what the user opted in to by name.
    let needs = crate::stub::pending_stub_requires(layout, name);
    let Some(ask) = io.ask.as_mut() else {
        if !needs.is_empty() {
            (io.say)(format!(
                "atpkg: {name} also needs: {} — installed first when you opt in.",
                needs.join(", ")
            ));
        }
        (io.say)(format!("atpkg: to install it: aterm pkg install {name}"));
        return PendingNext::Done;
    };
    let prompt = if needs.is_empty() {
        format!("Install {name}? [y/N] ")
    } else {
        format!("Install {name}? It also needs: {} [y/N] ", needs.join(", "))
    };
    if !ask(&prompt) {
        (io.say)(format!(
            "atpkg: not installed — to opt in later: aterm pkg install {name}"
        ));
        return PendingNext::Done;
    }
    if let Err(e) = layout.record_optin(name) {
        (io.say)(format!(
            "atpkg: could not record the opt-in at {}: {e} — installing now where possible, \
             but later passes will not keep {name} current until the marker exists",
            layout.optin_marker(name).display()
        ));
    }
    let bumped = crate::progress::append_bump(layout, tn).is_ok();
    let now = crate::flow::now_unix().max(0).unsigned_abs();
    let running = crate::progress::read_progress(layout)
        .is_some_and(|f| crate::progress::snapshot_running(&f, now));
    if running {
        (io.say)(format!(
            "atpkg: opted in — a pass is installing now; {name} follows it{}. Re-run {name} \
             in a few minutes, or: aterm pkg install {name}",
            if bumped {
                " (bumped to the front of the next pass)"
            } else {
                ""
            }
        ));
        return PendingNext::Done;
    }
    if io.gui_present {
        (io.say)(if bumped {
            format!(
                "atpkg: opted in — aterm picks {name} up within a few minutes (Settings ▸ \
                 Packages shows progress). Re-run {name} when it lands, or now: aterm pkg \
                 install {name}"
            )
        } else {
            format!(
                "atpkg: opted in — aterm installs {name} on its next pass. Now: aterm pkg \
                 install {name}"
            )
        });
        return PendingNext::Done;
    }
    (io.say)(format!("atpkg: opted in — installing {name} now."));
    PendingNext::InstallInline
}

/// After an [`crate::FlowError::Unreachable`] failure line, name the ONE act that
/// resumes the flow — because the Display used to end "…the toolchain retries
/// automatically", which is true only inside the windowed app's 6-hour loop and a
/// plain lie at this CLI edge (nothing retries a foreground verb). The re-run is
/// per-verb, so the edge that knows the verb appends it; and when the reason is the
/// anonymous GitHub budget, waiting is not the only fix, so that one case names the
/// token that lifts it.
fn print_unreachable_followup(e: &crate::FlowError, rerun: &str) {
    let crate::FlowError::Unreachable(why) = e else {
        return;
    };
    eprintln!("atpkg:   when the connection is back, re-run: {rerun}");
    if why.contains("403") || why.to_ascii_lowercase().contains("rate limit") {
        eprintln!(
            "atpkg:   the anonymous GitHub limit resets within the hour; \
             `ATPKG_TOKEN=$(gh auth token)` in the environment lifts it"
        );
    }
}

/// The chain-validated `[packages].prefix` override, or `None` for the default.
/// Threaded through `store::resolve` so the override is REACHABLE: both call sites
/// previously passed a hardcoded `None`, which made `vet_prefix`'s whole
/// configured-prefix branch dead code and pinned every install to `$HOME`.
///
/// (That was a real bug, but NOT for the reason once recorded here: `$HOME` does not
/// mean "the unverified lane". Trust's default `CallerOwned` authority mode admits a
/// toolchain owned by the invoking identity, so the default prefix proves fine —
/// see `docs/GOLDEN-INSTALL-PATH.md` §2. The override matters for a shared
/// multi-user store.)
fn configured_prefix() -> Option<std::path::PathBuf> {
    crate::config::load().prefix_path(aterm_types::dirs::home_dir().as_deref())
}

/// Resolve the install layout, or print why we can't and return `None`.
fn layout() -> Option<crate::store::Layout> {
    match crate::store::resolve(configured_prefix().as_deref()) {
        Some(l) => Some(l),
        None => {
            eprintln!("atpkg: HOME is unset — cannot locate the install prefix");
            None
        }
    }
}

/// Whether `verb` MUTATES the store and must therefore hold the store-wide
/// single-writer lock ([`crate::lock`]) for its whole run. The mutators are every
/// verb that stages/activates/discards builds or rewrites shims (`install` — incl.
/// `--default-set` —, `seed`, `update`, `rollback`, `uninstall`, `gc`), every
/// link-mutating verb (`link`, `unlink`, `refresh` — which also covers the
/// `[packages.links]` reconciliation the network verbs run), and `pin`/`unpin`:
/// pins are LOCAL state files, but they gate the coherence-group transaction and
/// their read-modify-write of the pin set is itself check-then-act, so they take
/// the same lock. Everything else (`doctor`, `which`, `list`, `run`, `verify`,
/// `tree-root`, `verify-index`, `relocate`, bare status) reads only — lock-free,
/// and `run` in particular may exec a long-lived tool that must never hold it.
/// The verbs whose FIRST positional is a program name, each paired with the flag-shaped
/// literals it legitimately accepts in that slot.
///
/// Exactly two such literals exist across the whole surface — `install --default-set` and
/// `uninstall --all` — and both are real operands, not flags: they name a SET where a
/// program name would go. Everything else beginning with `-` is a mistyped flag.
///
/// `run` is deliberately absent. `aterm <tool>` synthesizes `["run", <tool>, "--", …]`, so
/// `cmd_run` owns its own `--` handling, and its failure mode is already non-durable (exit
/// 127, no `status.toml` write) — there is nothing here to protect and a dispatcher to
/// avoid disturbing.
/// Refuse any argument to a verb that takes none — `Some(usage error)` when one
/// was given, `None` to proceed. `doctor`, `status` and `gc` ran to completion
/// at exit 0 on any argument (audit D-12: `atpkg doctor --json` printed the
/// human report and discarded the flag, so a script author read the exit code
/// as "flag accepted" and went hunting for the JSON bug downstream). The
/// `-h`/`--help` spellings never reach here — the dispatch edge above answers
/// them with the verb's own usage.
fn zero_arity(rest: &[String], verb: &str) -> Option<ExitCode> {
    let stray = rest.first()?;
    eprintln!("atpkg {verb}: unknown argument {stray:?} — this verb takes none");
    if let Some(usage) = usage_of(verb) {
        eprintln!("usage: {usage}");
    }
    Some(ExitCode::from(2))
}

const NAME_TAKING_VERBS: &[(&str, &[&str])] = &[
    (
        "install",
        &[
            "--default-set",
            "--elevate=sudo",
            "--elevate=osascript",
            "--elevate=never",
        ],
    ),
    ("uninstall", &["--all"]),
    ("update", &[]),
    ("rollback", &[]),
    ("which", &[]),
    ("tree-root", &[]),
    ("verify", &[]),
    ("pin", &[]),
    ("unpin", &[]),
    ("link", &[]),
    ("unlink", &[]),
    ("refresh", &[]),
];

fn verb_mutates_store(verb: &str) -> bool {
    matches!(
        verb,
        "install"
            | "seed"
            | "update"
            | "rollback"
            | "uninstall"
            | "gc"
            | "link"
            | "unlink"
            | "refresh"
            | "pin"
            | "unpin"
            | "repair"
    )
}

/// Acquire the store-wide writer lock for a mutating verb at the dispatch edge: a
/// TRY by default, or — when the edge recorded `--wait-lock <secs>` — a BOUNDED WAIT
/// ([`crate::lock::lock_store_waiting`]) that queues behind a sibling's pass and
/// announces itself ONCE on stdout as the [`LOCK_WAITING_MARKER`] line (this is that
/// marker's one production emission site). `Ok(None)` when no prefix resolves (HOME
/// unset) — nothing to lock, and the verb itself refuses with its own message
/// moments later. Every refusal stays fail-closed and prints the loud one-line
/// sentence: contention — fail-fast or timed out — exits
/// [`crate::lock::CONTENDED_EXIT`] (75) so a caller classifies it by code; an
/// unusable lock file (`Io`) exits 1. A waiter whose parent has died (the window
/// quit — its pipes are gone) stands down silently with the same code at its next
/// CONTENDED poll: the wait consults [`parent_is_gone`] only when a try finds the
/// lock still held (before the announcement, before the deadline), so an orphan
/// whose holder lets go within that poll interval (≤ 500 ms) takes the lock and runs
/// its verb — the race with the successor's own waiter is narrowed, not closed. And
/// every line a waiter prints — the `lock-waiting:` announcement, the refusal
/// sentence on stderr and the `seed-busy:` terminal ([`emit_marker_line_lossy`]) —
/// is a write whose failure is ignored, so a window that quits between the check and
/// the print costs an `EPIPE` nobody sees rather than a panic (Rust leaves `SIGPIPE`
/// ignored and `println!` would panic on the dead pipe).
fn mutator_store_lock() -> Result<Option<crate::lock::StoreLock>, ExitCode> {
    let Some(layout) = crate::store::resolve(configured_prefix().as_deref()) else {
        return Ok(None);
    };
    // Whether the wait ANNOUNCED itself: only an announced wait is answered below
    // (`lock-acquired:`) — a wait that ended inside the grace put nothing on the
    // glass, so there is nothing to retire.
    let announced = std::cell::Cell::new(false);
    let acquired = match wait_lock() {
        Some(WaitLock { bound, .. }) => crate::lock::lock_store_waiting(
            &layout,
            bound,
            |path| {
                use std::io::Write as _;
                announced.set(true);
                let _ = writeln!(
                    std::io::stdout(),
                    "atpkg: {LOCK_WAITING_MARKER}another atpkg process holds the store lock \
                     at {} \u{2014} waiting up to {} s for it to finish",
                    path.display(),
                    bound.as_secs()
                );
            },
            || {
                // Asked only on a CONTENDED poll: the lock was held by someone
                // when this process wanted it ([`pass_finished_while_we_waited`]),
                // and the first such second is the wait's start.
                WAITED_FOR_LOCK.store(true, std::sync::atomic::Ordering::Relaxed);
                let _ = FIRST_CONTENDED_UNIX.set(unix_now());
                !parent_is_gone()
            },
        ),
        None => crate::lock::try_lock_store(&layout),
    };
    match acquired {
        Ok(guard) => {
            // THE WAIT IS OVER, said once and only after it was announced: the verb
            // that follows may be quiet for the whole length of its own work under
            // the lock, and the GUI's waiting row has nothing else to retire it
            // before this child exits (2026-09-14). A lossy write, like the
            // announcement: the window may have quit meanwhile.
            if announced.get() {
                use std::io::Write as _;
                let _ = writeln!(
                    std::io::stdout(),
                    "atpkg: {LOCK_ACQUIRED_MARKER}the other process finished \u{2014} \
                     running now"
                );
            }
            Ok(Some(guard))
        }
        Err(e) => {
            // An orphaned waiter has nobody to tell — its stderr and stdout are the
            // dead window's pipes — and the exit code says it all. That guard is a
            // snapshot, and a window can quit between it and the prints below, so a
            // WAITER's two lines are writes whose failure is ignored — an `EPIPE`
            // nobody sees — never the `eprintln!`/`println!` pair, which panics on a
            // dead pipe and would trade the promised silent 75 for a 101. A typed
            // verb keeps the macros: its stderr is a person's terminal.
            if !parent_is_gone() {
                let waiter = wait_lock().is_some();
                if waiter {
                    use std::io::Write as _;
                    let _ = writeln!(std::io::stderr(), "atpkg: {e}");
                } else {
                    eprintln!("atpkg: {e}");
                }
                // CONTENTION GETS A MARKER; every other refusal does not. An unwritable
                // prefix or a broken lock file is a real outage and must keep reaching
                // the GUI's refusal card. A lock held by another `atpkg` is the
                // opposite: that process is doing this work right now, and the only
                // honest thing to say about THIS pass is that it stood aside (see
                // [`SEED_BUSY_MARKER`]). Said to the `--wait-lock` lane only — the
                // GUI's machine lanes, and since 2026-09-19 the self-update intercept's
                // child, which opts in on behalf of a person who typed `claude update`
                // — like the `lock-waiting:` line above: a typed `aterm pkg` verb is told
                // on stderr and keeps its stdout silent (the two-process proof pins
                // both), and the exit code below says the same thing to every caller.
                if matches!(e, crate::lock::StoreLockError::Contended(_)) && waiter {
                    emit_marker_line_lossy(&format!(
                        "atpkg: {SEED_BUSY_MARKER}another atpkg process holds the store lock \
                         — that pass is doing this work and this one stood aside"
                    ));
                }
            }
            Err(ExitCode::from(match e {
                crate::lock::StoreLockError::Contended(_) => crate::lock::CONTENDED_EXIT,
                crate::lock::StoreLockError::Io(..) => 1,
            }))
        }
    }
}

/// Whether this process has been orphaned: its parent — the aterm window that
/// spawned it, which named itself in [`SPAWNER_PID_ENV`] or was read at the edge
/// with the `--wait-lock` flag — is gone, which shows as a different parent pid
/// now: init on macOS, a subreaper on Linux ([`is_orphaned`]). Only a `--wait-lock`
/// waiter asks (no recorded wait ⇒ always false). A waiter holds nothing and prints
/// at most one line while it polls — the `lock-waiting:` announcement, once the
/// grace has passed — so it is the one atpkg child that would otherwise outlive its
/// window for the whole bound and then race the successor's own waiter for the freed
/// lock. It is asked only on a CONTENDED poll, so that race is narrowed to one poll
/// interval (≤ 500 ms: an orphan whose holder lets go inside it still takes the
/// lock), not closed. An INSTALLING child is a different case — it KEEPS RUNNING:
/// the orphan watch ([`crate::progress::watch_for_orphaning`], 2026-09-14) re-points
/// its stdio at `orphan-pass.log` when its window goes, so it finishes its pass and
/// holds the lock for the whole of it (it used to die at its next print, and the
/// store is still crash-consistent under a death, `crate::lock`).
pub(crate) fn parent_is_gone() -> bool {
    wait_lock().is_some_and(|w| is_orphaned(w.spawner, current_parent_id()))
}

/// `atpkg` (no verb) — the inert/enabled posture, observable from the shell, answering
/// a first hour's four questions in order: what is this (the head), what do I have (the
/// counts), what can I type (the tiers), and the ONE next command. Read-only and
/// lock-free — `list_installed`/`active_builds` read the store without the writer lock,
/// so the bare invocation's classification in `store_lock_verb_roster_is_exact` holds.
///
/// The stable head `atpkg: enabled (root key pinned)` and the byte-exact disabled line
/// are the script-facing contract; the counts ride after an em dash on the same line so
/// nothing keying on the head moves. (The old `. Verbs: …` tail is gone — the roster
/// now prints tiered below, on lines a script was never keying on.)
fn cmd_bare() {
    if !manager_enabled() {
        println!("atpkg: disabled (no root key pinned) -- inert");
        return;
    }
    // HOME unset: the head still answers (posture is a fact about the BINARY), with no
    // counts to claim and no next act to name — a next command would fail in the same
    // broken environment that emptied the tail.
    let counts = crate::store::resolve(configured_prefix().as_deref()).map(|layout| {
        let programs: std::collections::BTreeSet<String> = crate::list_installed(&layout)
            .into_iter()
            .map(|(program, _)| program)
            .collect();
        (programs.len(), crate::active_builds(&layout).len())
    });
    for line in bare_lines(counts) {
        println!("{line}");
    }
}

/// The bare posture's lines for an ENABLED manager — pure, so the head's stability and
/// the one-next-command rule are testable without a process.
fn bare_lines(counts: Option<(usize, usize)>) -> Vec<String> {
    let mut lines = vec![match counts {
        Some((0, _)) => "atpkg: enabled (root key pinned) — nothing installed yet".to_string(),
        Some((programs, live)) => format!(
            "atpkg: enabled (root key pinned) — {programs} program(s) installed, {live} live"
        ),
        None => "atpkg: enabled (root key pinned)".to_string(),
    }];
    lines.extend(verb_tier_lines());
    match counts {
        // Empty store: the one next act is the install, and it says what it does.
        Some((0, _)) => lines.push(
            "atpkg: next: aterm pkg install --default-set — installs the whole ALab \
             toolset · manual: aterm help pkg"
                .to_string(),
        ),
        Some(_) => lines.push(
            "atpkg: next: aterm pkg list — what you have · full manual: aterm help pkg".to_string(),
        ),
        None => {}
    }
    lines
}

/// `atpkg doctor` — the full health surface (§15): trust root, PATH wiring, broken-symlink
/// scan, active-build store integrity, shell.d hooks + fish-safety, disk headroom, index
/// freshness, and rustup state. No network, no mutation. Structural breakage exits
/// nonzero; advisory warnings stay exit-0.
fn doctor(prefix: &str) -> ExitCode {
    let Some(layout) = layout() else {
        return ExitCode::from(1); // HOME unset is itself structural
    };
    warn_if_never_checked(&layout);
    if crate::doctor::run(&layout, prefix) {
        ExitCode::SUCCESS
    } else {
        ExitCode::from(1)
    }
}

/// `atpkg which <tool>` — ONE line: the copy that runs and why, in the canonical state
/// spellings ([`crate::state`]). See [`which_line`].
fn cmd_which(tool: Option<&String>) -> ExitCode {
    let Some(tool) = tool else {
        eprintln!("usage: atpkg which <tool>");
        return ExitCode::from(2);
    };
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    warn_if_never_checked(&layout);
    match which_line(&layout, tool, std::env::var_os("PATH").as_deref()) {
        Ok(line) => {
            println!("{line}");
            ExitCode::SUCCESS
        }
        Err(why) => {
            eprintln!("{why}");
            ExitCode::from(1)
        }
    }
}

/// The one-line answer to "which copy of `tool` runs, and why?" — design S6. Offline: it
/// reads the managed shim, the process `PATH` (`path_var`), the status record and the
/// retirement marker; it never fetches. In `PATH` order the first copy wins, and the
/// managed `bin/` is appended LAST inside an aterm session, so:
///
/// * a managed shim with a foreign copy ahead of it on `PATH` ⇒
///   `<tool> → <foreign> — managed <build> — SHADOWED by <foreign>`;
/// * a managed shim otherwise ⇒
///   `<tool> → <prefix>/bin/<tool> → <store path> — managed <build> — pinned by index <N>`
///   (the index from the recorded row, else the durable floor);
/// * no shim but a system copy on `PATH` ⇒ `<tool> → <path> — system: <path> — not managed
///   by aterm`, with ` (managed copy retired <date>)` when the retirement marker (or the
///   recorded row) says so;
/// * a consent stub for an extra that is not opted in ⇒ `… (consent stub) — extra — not
///   installed (opt in: aterm pkg install <name>)`;
/// * a pending default-set stub ⇒ named as such;
/// * a recorded `installed via` / `needs admin` / `unavailable on` / `blocked by` row ⇒
///   that row's words;
/// * a REROUTED upstream name (`cargo`, `rustfmt`, … — [`crate::reroute::TABLE`]) ⇒ the
///   reroute stub, its state, and the row's policy in the stub's own words
///   ([`reroute_which_line`]), answered before every other arm;
/// * nothing at all ⇒ `Err` with the one-line fix.
///
/// # Errors
/// The tool is not an admissible name, or nothing on this machine answers to it.
fn which_line(
    layout: &crate::store::Layout,
    tool: &str,
    path_var: Option<&std::ffi::OsStr>,
) -> Result<String, String> {
    // A REROUTED upstream name (philosophy §4) is answered by the table, ahead of the
    // deny-list gate and the store: inside an aterm session the reroute dir is FIRST on
    // PATH, so the stub is the copy that runs whatever else PATH holds (measured
    // 2026-09-07, before it was: `~/.cargo/bin` at position 17 ran upstream cargo
    // silently), and the managed bin/ never carries these names — `cargo`/`rustc` are
    // deny-listed, and no index program exposes the other six. Without this arm `which
    // cargo` refused the name and `which rustfmt` promised an install; neither said
    // what runs.
    if let Some(row) = crate::reroute::row_for(tool) {
        return Ok(reroute_which_line(layout, row));
    }
    let Some(tn) = crate::store::ToolName::new(tool) else {
        return Err(format!("atpkg: {tool:?} is not a tool name"));
    };
    let status = crate::status::read(layout);
    let row_of = |program: &str| {
        status
            .as_ref()
            .and_then(|s| s.programs.get(program).cloned())
    };
    let shim = layout.shim(&tn);
    // 1. A managed shim (a real one — a pending stub does not resolve).
    if let Some(target) = crate::which(layout, tool) {
        let Some((program, build)) = crate::ops::program_build_of_target(&target) else {
            return Ok(format!(
                "{tool} → {} → {} — dev-linked (managed from that checkout, not the index)",
                shim.display(),
                target.display()
            ));
        };
        if let Some(shadow) =
            crate::vendor::shadowing_binary_on_path(&layout.prefix, tool, path_var)
        {
            // An AGENT PROGRAM whose twin is laid (2026-09-16): the foreign copy runs in
            // THIS shell only, and the remedy is in place — never a new tab, never
            // "remove that copy". The foreign copies line follows so the picture is
            // whole — labelled for what is true HERE (`on this shell's PATH`), not
            // `out-ranked`: in this shell the copy just named as running is not
            // out-ranked by anything (review, 2026-09-16).
            if let Some(row) = agent_shell_shadow(layout, &program, build, &shadow, path_var) {
                let mut line = format!("{tool} → {} — {row}", shadow.display());
                let foreign = crate::vendor::foreign_copies_on_path(&layout.prefix, tool, path_var);
                if !foreign.is_empty() {
                    line.push('\n');
                    line.push_str(&foreign_copies_line_with(FOREIGN_ON_THIS_SHELL, &foreign));
                }
                return Ok(line);
            }
            // The canonical state, then the alias fix-line as a separate sentence when
            // `alab-<tool>` is laid: the copy that runs, and the name that runs ours.
            let state = crate::state::shadowed(build, &shadow);
            return Ok(match alias_fix(layout, &tn, &program, path_var) {
                Some(fix) => format!("{tool} → {} — {state} — {fix}", shadow.display()),
                None => format!("{tool} → {} — {state}", shadow.display()),
            });
        }
        // The canonical state, then — as a separate trailing sentence, never inside it —
        // what the managed copy runs with when its shim exports an environment (design
        // S7: `self-update off (DISABLE_AUTOUPDATER=1)`). Read off the shim that runs.
        let state = managed_state_of(layout, &program, build, row_of(&program).as_ref());
        let mut line = match shim_env_fix(&shim) {
            Some(fix) => format!(
                "{tool} → {} → {} — {state} — {fix}",
                shim.display(),
                target.display()
            ),
            None => format!(
                "{tool} → {} → {} — {state}",
                shim.display(),
                target.display()
            ),
        };
        // …and, for a ROSTERED agent program (2026-09-19, [`crate::selfupdate`]), one
        // more trailing sentence: what the vendor's own self-update verb does on this
        // name — `` `claude update` here runs `aterm pkg update claude` `` — for every
        // rostered member whether or not its shim exports an env (codex exports none, so
        // the fix-line alone could not carry it). Never for a shadowed row: the twin is
        // not what runs in that shell, and the sentence would be false there. And ONLY
        // where "here" is true (review, 2026-09-19): the laid twin must be current and
        // carry the hand-over ([`agent_twin_intercepts`] — the `.cmd` twin never does, a
        // twin from before the block does not until a pass re-lays it), and `agents/`
        // must be on THIS shell's `PATH` — the same two predicates the `managed-current:`
        // line and the shadow row already answer with. A shell whose `PATH` holds `bin/`
        // and not `agents/` (opened before the install that introduced `agents/`; the
        // manual names it) reaches this arm with no foreign copy ahead, runs
        // `bin/claude`, which carries no block, and `claude update` there IS the
        // vendor's updater — the first cut printed the sentence in exactly that shell
        // (measured on the real binary with `PATH=<prefix>/bin:/usr/bin:/bin`).
        if let Some(row) = crate::selfupdate::row_for(&program)
            && agent_twin_intercepts(layout, &program, build)
            && crate::vendor::agents_dir_on_path(&layout.prefix, path_var)
        {
            line.push_str(" — ");
            line.push_str(&crate::selfupdate::which_sentence(row));
        }
        // An AGENT PROGRAM's second line (owner decision 2026-09-10): the managed copy
        // runs because `agents/` is first on PATH, and the copies it beat — a vendor's
        // native install, a brew cask — are named so nobody has to `which -a` to learn
        // what a `brew upgrade` or Anthropic's own updater is still moving underneath.
        if crate::stub::is_agent_program(&program) {
            let foreign = crate::vendor::foreign_copies_on_path(&layout.prefix, tool, path_var);
            if !foreign.is_empty() {
                line.push('\n');
                line.push_str(&foreign_copies_line(&foreign));
            }
        }
        return Ok(line);
    }
    // 1b. An index PROGRAM whose installed build exposes no tool of its own name
    //     (`trust` exposes trustc/targo/…, `trust-mc` exposes trust-mc-driver): the
    //     managed bundle answers, with the names it does put on PATH. Without this arm
    //     `which trust` named Homebrew's p11-kit `trust` as "the system copy" and `which
    //     trust-mc` said "not installed" over a live store/trust-mc/20065 (2026-09-10
    //     audit) — the foreign-PATH arm below is for a program's OWN name, and a same-named
    //     unrelated binary is named as exactly that, after the truth.
    if let Some(build) = crate::active_builds(layout).get(tool).copied() {
        let state = managed_state_of(layout, tool, build, row_of(tool).as_ref());
        let mut exposes = crate::installed_exposes(layout, tool).unwrap_or_default();
        exposes.sort();
        let mut line = match exposes.first() {
            Some(first) => format!(
                "{tool} → {} (bundle; exposes {} — run `aterm pkg which {first}`) — {state}",
                layout.build_dir(tool, build).display(),
                exposes.join(", ")
            ),
            None => format!(
                "{tool} → {} (bundle; exposes no command of its own) — {state}",
                layout.build_dir(tool, build).display()
            ),
        };
        if let Some(path) = crate::vendor::system_binary_on_path(&layout.prefix, tool, path_var) {
            line.push('\n');
            line.push_str(&format!(
                "{} on PATH is an unrelated binary, not this program",
                path.display()
            ));
        }
        return Ok(line);
    }
    // 1b'. A program DEV-LINKED from a checkout whose own name is not a tool (`trust`
    //      exposes trustc/targo/…, never a `trust` binary): the link is what runs, and
    //      `which trust` must say so — before the not-installed arm below, which was
    //      answering "not installed (fix: aterm pkg install trust)" on a box whose
    //      every build ran through `aterm pkg link trust <stage2>` (2026-09-14).
    if crate::linkmode::is_linked(layout, tool) {
        return Ok(dev_linked_program_line(
            tool,
            crate::linkmode::linked_checkout(layout, tool).as_deref(),
            &crate::linkmode::linked_bins(layout, tool),
            crate::vendor::system_binary_on_path(&layout.prefix, tool, path_var).as_deref(),
        ));
    }
    // 1c. An INDEX PROGRAM of ALab's own that nothing has installed here — no shim, no
    //     bundle, no pending stub — while a SAME-NAMED unrelated binary sits on PATH. Arm 2
    //     below would name that binary as "the system copy": on this Linux host `which
    //     trust` answered `/usr/bin/trust — system — not managed by aterm`, and that file
    //     is p11-kit's certificate-store tool, not the Trust compiler. Nothing in the line
    //     said the program was unserved on this target; the wrong binary was presented as
    //     the answer. Arm 1b already handles the INSTALLED case ("an unrelated binary, not
    //     this program"); this is the same sentence for the not-installed case, with the
    //     truth first — the recorded reason when a row carries one, else plain
    //     not-installed with the fix.
    let stub_kind = crate::stub::pending_stub_kind(layout, tool);
    let index = cached_index(layout);
    if stub_kind.is_none()
        && let Some(entry) = index.as_ref().and_then(|i| i.program(tool))
        && entry.system.is_none()
        && !entry.extra
        && !crate::stub::is_agent_program(tool)
        && let Some(path) = crate::vendor::system_binary_on_path(&layout.prefix, tool, path_var)
    {
        let row = row_of(tool).map(|r| r.state);
        return Ok(index_program_shadowed_by_unrelated_line(
            tool,
            row.as_deref(),
            &path,
        ));
    }
    // 2. A system copy on PATH outside the prefix: it runs, and atpkg does not manage it.
    //    With a PENDING STUB in the managed bin/ (an extra's consent stub, a default-set
    //    member's placeholder) the question is which comes FIRST on PATH: a foreign copy
    //    ahead of the managed bin/ runs; one behind it loses to the stub, which asks (or
    //    waits) — the shadow walk answers that, the system walk would skip the stub.
    let foreign = if stub_kind.is_some() {
        crate::vendor::shadowing_binary_on_path(&layout.prefix, tool, path_var)
    } else {
        crate::vendor::system_binary_on_path(&layout.prefix, tool, path_var)
    };
    if let Some(path) = foreign {
        let retired = layout.retired_date(tool).or_else(|| {
            row_of(tool).and_then(|r| crate::state::system_retired(&r.state).map(str::to_string))
        });
        return Ok(format!(
            "{tool} → {} — {}",
            path.display(),
            crate::state::system(&path, retired.as_deref())
        ));
    }
    // 3. A pending stub in the managed bin/: typing the name asks (an extra) or waits —
    //    and a recorded row that says WHY it waits (blocked by a requirement, needing
    //    admin, unserved here) outranks the generic promise.
    if let Some(kind) = stub_kind {
        let waiting = row_of(tool).map(|r| r.state).filter(|st| {
            st.starts_with(crate::state::BLOCKED_PREFIX)
                || st.starts_with(crate::state::NEEDS_ADMIN_PREFIX)
                || st.starts_with(crate::state::UNAVAILABLE_PREFIX)
        });
        return Ok(match (kind, waiting) {
            // An agent program's stub is never a consent stub: `claude`/`codex` are
            // default-set members the pass installs unasked (owner decision 2026-09-10);
            // a stub of the `Extra` kind under one of those names is a stale pre-index
            // stub, and calling it a consent stub sent the user to opt in to something
            // the next pass installs anyway (audit 2026-09-14).
            (crate::stub::StubKind::Extra, _)
                if !layout.optin_exists(tool) && !crate::stub::is_agent_program(tool) =>
            {
                format!(
                    "{tool} → {} (consent stub) — {}",
                    shim.display(),
                    crate::state::extra_not_installed(tool)
                )
            }
            (_, Some(st)) => format!("{tool} → {} (pending stub) — {st}", shim.display()),
            _ => format!(
                "{tool} → {} (pending stub) — not installed yet: the next pass installs it \
                 (now: aterm pkg install {tool})",
                shim.display()
            ),
        });
    }
    // 4. A recorded state that names no shim: another protocol's install, a member
    //    waiting on elevation, or a target the pinned build does not serve.
    if let Some(row) = row_of(tool) {
        let st = row.state.as_str();
        if let Some((_, path)) = crate::state::installed_via_path(st) {
            return Ok(format!("{tool} → {path} — {st}"));
        }
        if st.starts_with(crate::state::NEEDS_ADMIN_PREFIX)
            || st.starts_with(crate::state::UNAVAILABLE_PREFIX)
            || st.starts_with(crate::state::EXTRA_PREFIX)
            || st.starts_with(crate::state::BLOCKED_PREFIX)
        {
            return Ok(format!("{tool} → (nothing runs) — {st}"));
        }
    }
    // 5. An `alab-<x>` that nothing answers to: say honestly WHY there is no alias rather
    //    than promising `aterm pkg install alab-x` (no such program). The alias exists only
    //    for ALab's own tools, so a vendor extra or a system-satisfiable member never has
    //    one — the cached index says which, offline; an ALab tool that is simply not
    //    installed (or whose alias the next pass lays) gets the plain name's fix.
    if let Some(base) = tn.alias_base() {
        return Err(no_alias_line(layout, tool, base.as_str()));
    }
    Err(not_installed_fix(tool))
}

/// The `which` answer for a program dev-linked from a checkout (arm 1b' of
/// [`which_line`]): where it is linked from, what it exposes, and — when a same-named
/// unrelated binary sits on PATH — that binary named for what it is. Pure, unit-tested.
fn dev_linked_program_line(
    tool: &str,
    checkout: Option<&std::path::Path>,
    bins: &[String],
    foreign: Option<&std::path::Path>,
) -> String {
    let mut sorted: Vec<&str> = bins.iter().map(String::as_str).collect();
    sorted.sort_unstable();
    let from = checkout.map_or_else(|| "a checkout".to_string(), |c| c.display().to_string());
    let mut line = if let Some(first) = sorted.first() {
        format!(
            "{tool} → {from} (dev-linked; exposes {} — run `aterm pkg which {first}`) — managed from that checkout, not the index; `aterm pkg unlink {tool}` returns it to the index",
            sorted.join(", ")
        )
    } else {
        format!(
            "{tool} → {from} (dev-linked; exposes no command of its own) — managed from that checkout, not the index; `aterm pkg unlink {tool}` returns it to the index"
        )
    };
    if let Some(path) = foreign {
        line.push('\n');
        line.push_str(&format!(
            "{} on PATH is an unrelated binary, not this program",
            path.display()
        ));
    }
    line
}

/// The `which` answer for an index program of ALab's own that is NOT installed while a
/// same-named unrelated binary sits on PATH (arm 1c of [`which_line`]). Pure, so the
/// wording is unit-testable without a signed index cache on disk.
///
/// The truth leads: a recorded row that says WHY nothing runs (unserved on this target,
/// blocked, needing admin) is quoted as-is; otherwise plain not-installed with the fix.
/// Then the foreign binary, named for what it is — never as "the system copy" of a
/// program it has nothing to do with.
fn index_program_shadowed_by_unrelated_line(
    tool: &str,
    row_state: Option<&str>,
    path: &std::path::Path,
) -> String {
    let truth = match row_state {
        Some(st)
            if st.starts_with(crate::state::UNAVAILABLE_PREFIX)
                || st.starts_with(crate::state::BLOCKED_PREFIX)
                || st.starts_with(crate::state::NEEDS_ADMIN_PREFIX) =>
        {
            format!("(nothing runs) — {st}")
        }
        _ => format!("(nothing runs) — not installed (fix: aterm pkg install {tool})"),
    };
    format!(
        "{tool} → {truth}\n{} on PATH is an unrelated binary, not this program",
        path.display()
    )
}

/// `foreign copies out-ranked: ~/.local/bin/claude (2.1.267), /opt/homebrew/bin/claude
/// (2.1.204)` — each path home-abbreviated, each with the version its real location
/// spells when it does ([`version_from_path`]); no parenthetical otherwise. Nothing is
/// executed to learn a version: `which` is a read-only verb and a foreign binary is
/// not ours to run.
fn foreign_copies_line(foreign: &[std::path::PathBuf]) -> String {
    foreign_copies_line_with("foreign copies out-ranked", foreign)
}

/// The label of the agent programs' second `which` line in a shell where a foreign copy
/// RUNS: `foreign copies on this shell's PATH: …` — every copy, the running one included.
const FOREIGN_ON_THIS_SHELL: &str = "foreign copies on this shell's PATH";

/// [`foreign_copies_line`] under `label`.
fn foreign_copies_line_with(label: &str, foreign: &[std::path::PathBuf]) -> String {
    let home = aterm_types::dirs::home_dir();
    let rendered: Vec<String> = foreign
        .iter()
        .map(|p| {
            let shown = home
                .as_deref()
                .and_then(|h| p.strip_prefix(h).ok())
                .map_or_else(
                    || p.display().to_string(),
                    |rest| format!("~/{}", rest.display()),
                );
            match version_from_path(p) {
                Some(v) => format!("{shown} ({v})"),
                None => shown,
            }
        })
        .collect();
    format!("{label}: {}", rendered.join(", "))
}

/// The version a foreign copy's REAL location spells, read off the path alone: the
/// LAST path component of the canonical path that is dotted digits (`2.1.267` for
/// Anthropic's `~/.local/share/claude/versions/2.1.267`, `2.1.204` for Homebrew's
/// `Caskroom/claude-code/2.1.204/claude`, `0.151.0` for `Caskroom/codex/0.151.0/bin/codex`).
/// `None` when no component qualifies — the line then shows the path alone rather than a
/// guess. Symlinks are followed (`canonicalize`), which is where those spellings live.
fn version_from_path(path: &std::path::Path) -> Option<String> {
    let real = std::fs::canonicalize(path).unwrap_or_else(|_| path.to_path_buf());
    version_from_components(&real)
}

/// The pure half of [`version_from_path`]: last dotted-digits component wins.
fn version_from_components(path: &std::path::Path) -> Option<String> {
    path.components().rev().find_map(|c| {
        let s = c.as_os_str().to_str()?;
        let dotted = s.contains('.')
            && s.split('.')
                .all(|part| !part.is_empty() && part.bytes().all(|b| b.is_ascii_digit()));
        dotted.then(|| s.to_string())
    })
}

/// The `which` answer for a REROUTED upstream name (philosophy §4): the stub that runs
/// inside a session, its state (`laid` / `missing` / `foreign`, [`crate::reroute::states`]),
/// and the row's policy in the stub's own words ([`crate::reroute::policy_summary`]) —
/// `<args>` standing for the caller's arguments, since `which` has none to fill in.
fn reroute_which_line(layout: &crate::store::Layout, row: &crate::reroute::Row) -> String {
    use crate::reroute::StubState;
    let stub = crate::reroute::stub_path(layout, row.upstream);
    let state = crate::reroute::states(layout)
        .into_iter()
        .find(|(name, _)| *name == row.upstream)
        .map_or("missing", |(_, state)| match state {
            StubState::Laid => "laid",
            StubState::Missing => "missing",
            StubState::Foreign => "foreign",
        });
    format!(
        "{} → {} (reroute stub, {state}) — rerouted inside aterm sessions: {}",
        row.upstream,
        stub.display(),
        crate::reroute::policy_summary(row)
    )
}

/// The one-line answer for `alias` (`alab-<base>`) when no such shim is laid — the reverse
/// of [`alias_fix`]. Reads the §14 cached index (offline; absent ⇒ the base is unknown).
fn no_alias_line(layout: &crate::store::Layout, alias: &str, base: &str) -> String {
    let index = cached_index(layout);
    no_alias_line_for(
        layout,
        alias,
        base,
        index.as_ref().and_then(|i| i.program(base)),
    )
}

/// [`no_alias_line`] over an already-looked-up index `entry` for `base` (`None` ⇒ not
/// listed, or nothing cached).
fn no_alias_line_for(
    layout: &crate::store::Layout,
    alias: &str,
    base: &str,
    entry: Option<&crate::manifest::Program>,
) -> String {
    match entry {
        Some(p) if p.system.is_some() => format!(
            "atpkg: no alias {alias} — {base} is not one of ALab's own tools (a system copy \
             may satisfy it: system = \"{}\"); type {base}",
            p.system.as_deref().unwrap_or(base)
        ),
        Some(p) if p.extra || crate::stub::is_agent_program(base) => format!(
            "atpkg: no alias {alias} — {base} is not one of ALab's own tools (a vendor's); \
             type {base}"
        ),
        Some(_) if crate::which(layout, base).is_some() => format!(
            "atpkg: {alias} is not laid yet — the next pass lays it beside {base} (now: aterm \
             pkg update)"
        ),
        Some(_) => format!(
            "atpkg: {alias} names nothing yet — {}",
            not_installed_fix(base)
        ),
        None if crate::which(layout, base).is_some() => format!(
            "atpkg: no alias {alias} — {base} is not one of ALab's own tools, or the next \
             pass lays it (aterm pkg update); type {base}"
        ),
        None => format!("atpkg: no alias {alias} — {}", not_installed_fix(base)),
    }
}

/// `atpkg run <tool> [--] [args…]` — run a pinned, installed tool from the store, replacing
/// this process. Resolves the tool through the store shim (NEVER ambient `$PATH`); appends the
/// managed `bin/` to the child `PATH` (append-not-prepend, so a pinned tool finds its pinned
/// siblings while system `sudo`/`ssh`/… are never shadowed); then `exec`s it. This is the
/// engine behind the `aterm <tool>` dispatcher (docs/ATERM-DISTRIBUTION-WEDGE.md §4).
///
/// An optional literal `--` after the tool name separates it from the tool's own args, so
/// `atpkg run ay -- --version` and `atpkg run ay --version` are equivalent.
fn cmd_run(rest: &[String]) -> ExitCode {
    let Some((tool, args)) = rest.split_first() else {
        eprintln!("usage: atpkg run <tool> [args…]");
        return ExitCode::from(2);
    };
    // Drop a single leading `--` separator if the caller passed one.
    let args: &[String] = match args.split_first() {
        Some((sep, tail)) if sep == "--" => tail,
        _ => args,
    };
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    // `exec_path`, not `which`: this execs the tool without its shim, so it has to take
    // the exec root the shim's guard would (`crate::compat` — `aterm tippy` on a trust
    // build whose tippy refuses the store's own `bin/rustc` copy lints only from there).
    let Some(target) = crate::ops::exec_path(&layout, tool) else {
        // The third dead end converges on the pending message (R6): when the name is
        // a laid pending stub, `atpkg run <tool>` gets the same live state + bump the
        // stub itself prints — not a static "not installed" over a tool that is
        // literally downloading right now. A non-pending name keeps the classic line.
        if crate::stub::pending_stub_exists(&layout, tool) {
            return run_pending(&layout, tool, &[]);
        }
        eprintln!("{}", not_installed_fix(tool));
        return ExitCode::from(127);
    };
    let child_path =
        crate::store::append_bin_to_path(std::env::var_os("PATH").as_deref(), &layout.bin_dir());
    // `platform::exec_or_run` replaces this process on Unix (`execve`, never returns on
    // success) or spawns+waits+exits on Windows (no `execve`); reaching past it means the
    // launch itself failed, and the returned value is that error.
    let mut command = std::process::Command::new(&target);
    command.args(args).env("PATH", child_path);
    let err = crate::platform::exec_or_run(&mut command);
    eprintln!("atpkg: failed to exec {}: {err}", target.display());
    ExitCode::from(127)
}

/// `atpkg relocate <stage-root> [--sign <id>] [--advisory]` — PRODUCER-side
/// pack-time relocation (§10.1). Vendors machine-local shared-library deps into
/// the staged sysroot and rewrites/deletes machine-local load commands so the
/// eventual tarball is self-contained. Run BETWEEN staging and tar so the signed
/// `tree_root` is computed over the RELOCATED payload. Fail-closed on any
/// unresolved machine-local reference unless `--advisory`.
fn cmd_relocate(rest: &[String]) -> ExitCode {
    let mut stage: Option<&str> = None;
    let mut sign_id: Option<&str> = None;
    let mut advisory = false;
    let mut it = rest.iter();
    while let Some(a) = it.next() {
        match a.as_str() {
            "--sign" => sign_id = it.next().map(String::as_str),
            "--advisory" => advisory = true,
            s if !s.starts_with('-') && stage.is_none() => stage = Some(s),
            other => {
                // Name the grammar with the refusal. The bare "unexpected
                // argument" read as "this verb takes no flags", and the manual
                // agreed by omission — the audit's D-5, in the verb where the
                // wrong conclusion means signing with the wrong identity.
                eprintln!("atpkg relocate: unexpected argument {other:?}");
                eprintln!("usage: atpkg relocate <stage-root> [--sign <identity>] [--advisory]");
                return ExitCode::from(2);
            }
        }
    }
    let Some(stage) = stage else {
        eprintln!("usage: atpkg relocate <stage-root> [--sign <identity>] [--advisory]");
        return ExitCode::from(2);
    };
    let stage_path = std::path::Path::new(stage);
    if !stage_path.is_dir() {
        eprintln!("atpkg relocate: {stage} is not a directory");
        return ExitCode::from(1);
    }
    match crate::relocate::relocate_stage(stage_path, sign_id) {
        Ok(report) => {
            if !report.vendored.is_empty() {
                println!(
                    "atpkg relocate: vendored {} lib(s) into {}, rewrote {} object(s): {}",
                    report.vendored.len(),
                    crate::relocate::VENDOR_REL,
                    report.rewritten,
                    report.vendored.join(", ")
                );
            } else {
                println!("atpkg relocate: no machine-local references found (already portable)");
            }
            if !advisory {
                if let Err(e) = report.require_self_contained() {
                    eprintln!("atpkg relocate: {e}");
                    return ExitCode::from(1);
                }
            } else {
                for u in &report.unresolved {
                    eprintln!("atpkg relocate: advisory: {u}");
                }
            }
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg relocate: {e}");
            ExitCode::from(1)
        }
    }
}

/// The one non-macOS answer, shared by all three `noindex` subcommands so they cannot
/// fork. `.noindex` is a behaviour MEASURED on macOS 26.6.2 and nowhere else has a
/// Spotlight index to hide from — so the verb says "not applicable" rather than
/// reporting a clean scan. "0 indexed" on Linux would read as a measurement, and this
/// whole feature exists because a false-clean answer is worse than no answer (the
/// earlier attempt planted 189 inert `.metadata_never_index` files and reported success).
const NOINDEX_NOT_APPLICABLE: &str =
    "atpkg noindex: not applicable — Spotlight is a macOS index; nothing to exclude here";

/// What `atpkg noindex` was asked to do.
///
/// A parsed job rather than a bag of `Option`s threaded through the body, so the whole
/// grammar — including which flag belongs to which subcommand — is decided by a pure
/// function and pinned by `tests::noindex_grammar_is_exact`, with no filesystem and no
/// 20-second probe in the loop.
#[derive(Debug, PartialEq, Eq)]
enum NoindexJob {
    /// `noindex [scan] [<root>] [--depth <n>]`. `root: None` means the cwd — resolved in
    /// the body, not here, so this stays pure.
    Scan {
        /// The tree to walk, as the user spelled it.
        root: Option<String>,
        /// Directory levels below `root` to descend.
        depth: usize,
    },
    /// `noindex verify <dir>` — the empirical probe.
    Verify {
        /// The directory to measure.
        dir: String,
    },
    /// `noindex migrate <dir> [--dry-run]` — the one rename.
    Migrate {
        /// The directory to rename.
        dir: String,
        /// Print the rename that would happen and touch nothing.
        dry_run: bool,
    },
    /// `noindex apply (--all | <dir>...) [--dry-run]` — the doctor's remedy, done:
    /// migrate and re-point cargo per repo ([`crate::noindex::apply_one`]).
    Apply {
        /// `--all`: every exposed repo target dir under `$HOME` at the doctor's depth.
        all: bool,
        /// The directories named, when not `--all`.
        dirs: Vec<String>,
        /// Print what would happen and touch nothing.
        dry_run: bool,
    },
}

/// One usage refusal for `noindex`: the specific complaint, then the grammar, exit 2.
///
/// The grammar comes from [`VERB_USAGE`] rather than a literal here — `relocate` keeps a
/// second hand-written copy of its usage string in its parser, and a second copy is the
/// thing that rots. `unwrap_or_default` keeps this total: a missing entry costs a blank
/// line, never a panic on the error path.
fn noindex_usage_error(problem: &str) -> ExitCode {
    eprintln!("atpkg noindex: {problem}");
    eprintln!("usage: {}", usage_of("noindex").unwrap_or_default());
    ExitCode::from(2)
}

/// Parse `noindex`'s argv. `Err(code)` has ALREADY printed the usage error.
///
/// Pure apart from those two `eprintln!`s, which is the point: the flag grammar is pinned
/// by a test rather than discovered by running a verb whose `verify` form takes 20
/// seconds and whose `migrate` form renames a directory.
///
/// A flag on the WRONG subcommand is a usage error, never a silent ignore. `noindex scan
/// --dry-run` accepted-and-ignored would answer "nothing would change" about a form that
/// changes nothing anyway — a true sentence and a wrong answer, which is exactly the
/// shape of bug (audit D-12: `doctor --json` swallowed the flag and exited 0) that the
/// per-verb arity work closed everywhere else.
fn parse_noindex(rest: &[String]) -> Result<NoindexJob, ExitCode> {
    let mut sub: Option<&str> = None;
    let mut operands: Vec<&str> = Vec::new();
    let mut depth: Option<usize> = None;
    let mut dry_run = false;
    let mut all = false;
    let mut it = rest.iter();
    while let Some(a) = it.next() {
        match a.as_str() {
            "--dry-run" => dry_run = true,
            "--all" => all = true,
            "--depth" => {
                // Bounded at BOTH ends. `--depth 0` walks nothing and would report
                // "0 indexed" over a home full of build output — the false-clean answer
                // again — and a depth past 32 is a request to walk the machine, not a
                // depth. A non-number is refused rather than defaulted, so a typo'd
                // `--depth six` cannot silently become the default 6.
                let Some(n) = it
                    .next()
                    .and_then(|v| v.parse::<usize>().ok())
                    .filter(|n| (1..=32).contains(n))
                else {
                    return Err(noindex_usage_error("--depth wants a whole number 1..=32"));
                };
                depth = Some(n);
            }
            // The subcommand is only a subcommand in the FIRST operand slot, so a
            // directory named `scan` still reaches `noindex migrate ./scan` as the thing
            // to rename.
            "scan" | "verify" | "migrate" | "apply" if sub.is_none() && operands.is_empty() => {
                sub = Some(a.as_str());
            }
            // `apply` takes any number of directories; every other form takes at most one.
            s if !s.starts_with('-') && (operands.is_empty() || sub == Some("apply")) => {
                operands.push(s);
            }
            other => {
                return Err(noindex_usage_error(&format!(
                    "unexpected argument {other:?}"
                )));
            }
        }
    }
    // `scan` is the default subcommand, so bare `atpkg noindex` is a scan of the cwd.
    let sub = sub.unwrap_or("scan");
    let operand = operands.first().copied();
    match sub {
        "apply" if depth.is_some() => Err(noindex_usage_error(
            "--depth belongs to `scan`, not `apply` — `apply --all` scans at doctor's depth",
        )),
        "apply" if all && !operands.is_empty() => Err(noindex_usage_error(
            "`apply` takes --all OR directories, not both",
        )),
        "apply" if !all && operands.is_empty() => Err(noindex_usage_error(
            "`apply` wants --all or the directories to migrate, e.g. `aterm pkg noindex apply \
             --all`",
        )),
        "apply" => Ok(NoindexJob::Apply {
            all,
            dirs: operands.iter().map(|s| (*s).to_owned()).collect(),
            dry_run,
        }),
        _ if all => Err(noindex_usage_error(&format!(
            "--all belongs to `apply`, not `{sub}`"
        ))),
        "verify" | "migrate" if operand.is_none() => Err(noindex_usage_error(&format!(
            "`{sub}` names no directory — say which one, e.g. `aterm pkg noindex {sub} ./target`"
        ))),
        "scan" | "verify" if dry_run => Err(noindex_usage_error(&format!(
            "--dry-run belongs to `migrate`, not `{sub}` — `{sub}` never changes anything, \
             so there is no rename to withhold"
        ))),
        "verify" | "migrate" if depth.is_some() => Err(noindex_usage_error(&format!(
            "--depth belongs to `scan`, not `{sub}` — `{sub}` acts on the one directory you name"
        ))),
        "verify" => Ok(NoindexJob::Verify {
            dir: operand.unwrap_or_default().to_owned(),
        }),
        "migrate" => Ok(NoindexJob::Migrate {
            dir: operand.unwrap_or_default().to_owned(),
            dry_run,
        }),
        _ => Ok(NoindexJob::Scan {
            root: operand.map(ToOwned::to_owned),
            depth: depth.unwrap_or(crate::noindex::VERB_DEPTH),
        }),
    }
}

/// `atpkg noindex` — Spotlight exposure of Rust build output (§9, storage hygiene).
///
/// The companion to [`crate::freespace`] on the incident of 2026-09-01 01:54, where
/// `mds` grinding 2.0 TB of build output across 127 target dirs was one of the two
/// amplifiers that turned a busy build into a killed WindowServer.
///
/// The REPORT is exit 0 always: it is a report, not a gate. `verify` is the only form
/// with a meaningful exit code, and it has THREE because the question has three answers
/// and collapsing "could not measure" into either of the other two is exactly the bug
/// this whole feature exists to prevent. Exit 3 is new to atpkg and deliberate: a script
/// must be able to tell "measured exposed" from "could not measure", and folding
/// [`crate::noindex::Verdict::Unknown`] into 0 would ship the silent-false-success bug in
/// exit-code form.
fn cmd_noindex(rest: &[String]) -> ExitCode {
    let job = match parse_noindex(rest) {
        Ok(job) => job,
        Err(code) => return code,
    };
    match job {
        NoindexJob::Scan { root, depth } => noindex_scan(root.as_deref(), depth),
        NoindexJob::Verify { dir } => noindex_verify(&dir),
        NoindexJob::Migrate { dir, dry_run } => noindex_migrate(&dir, dry_run),
        NoindexJob::Apply { all, dirs, dry_run } => noindex_apply(all, &dirs, dry_run),
    }
}

/// `atpkg noindex apply (--all | <dir>...) [--dry-run]` — the doctor's Spotlight remedy,
/// applied: each exposed cargo target dir is renamed to its `.noindex` form and the
/// repo's `.cargo/config.toml` is pointed at the new name. `--all` is the doctor's own
/// scan of `$HOME` (repos only — a free-standing target dir is named and left); named
/// directories are migrated whether or not a repo sits beside them. Idempotent: an
/// excluded directory is "already excluded", a live build is "retried next pass".
///
/// Exit 1 only when a rename FAILED (an I/O error); a skip with its reason is exit 0 —
/// the verb the pass runs unattended must not read a busy build as a failure.
fn noindex_apply(all: bool, dirs: &[String], dry_run: bool) -> ExitCode {
    if !crate::noindex::SUPPORTED {
        println!("{NOINDEX_NOT_APPLICABLE}");
        return ExitCode::SUCCESS;
    }
    let (outcomes, complete) = if all {
        let Some(home) = aterm_types::dirs::home_dir() else {
            eprintln!("atpkg noindex: no home directory to scan — name the directories instead");
            return ExitCode::from(1);
        };
        println!(
            "atpkg noindex: scanning {} (depth {})",
            home.display(),
            crate::noindex::DOCTOR_DEPTH
        );
        crate::noindex::apply_under(
            &home,
            crate::noindex::DOCTOR_DEPTH,
            &crate::noindex::Budget::VERB,
            dry_run,
        )
    } else {
        (
            dirs.iter()
                .map(|d| crate::noindex::apply_one(std::path::Path::new(d), false, dry_run))
                .collect(),
            true,
        )
    };
    let mut failed = false;
    for line in render_applied_tagged(&outcomes, dry_run) {
        println!("atpkg noindex: {line}");
    }
    // The outcome type says it now, so the verb no longer has to recognise a failure by
    // the words in its reason string.
    for o in &outcomes {
        if o.failure().is_some() {
            failed = true;
        }
    }
    if !complete {
        println!(
            "atpkg noindex: the walk stopped at its budget — this list is a floor, not a census"
        );
    }
    let migrated = outcomes.iter().filter(|o| o.migrated()).count();
    if migrated > 0 {
        // The same marker the pass prints, so a CLI-run apply reads the same to a parser.
        println!(
            "atpkg: {}",
            machine_settings_line(&[spotlight_entry(migrated)])
        );
    }
    if failed {
        ExitCode::from(1)
    } else {
        ExitCode::SUCCESS
    }
}

/// One line per outcome of an apply, plus the summary — pure, so the words are pinned.
fn render_applied(outcomes: &[crate::noindex::Applied]) -> Vec<String> {
    render_applied_tagged(outcomes, false)
}

/// [`render_applied`], with the dry-run tag forced on.
///
/// The tag used to ride `planned > 0`, so a `--dry-run` whose every candidate was
/// skipped or already excluded printed the REAL-RUN sentence and the user could not tell
/// the preview flag had been honoured.
fn render_applied_tagged(outcomes: &[crate::noindex::Applied], dry_run: bool) -> Vec<String> {
    use crate::noindex::{Applied, ConfigNote, ExcludeNote, Pointer};
    let mut lines = Vec::new();
    let (mut migrated, mut excluded, mut skipped, mut planned) = (0usize, 0usize, 0usize, 0usize);
    let mut failed = 0usize;
    for o in outcomes {
        match o {
            Applied::Migrated {
                from,
                to,
                config:
                    ConfigNote::Linked {
                        link,
                        exclude,
                        link_exclude,
                    },
            } => {
                // The git-checkout line (noindex module doc, 2026-09-10): the config is
                // never opened, so the line says what stands in its place.
                migrated += 1;
                lines.push(format!(
                    "migrated {} -> {} (symlink {} left in place; no config edit: git checkout)",
                    from.display(),
                    to.display(),
                    link.display()
                ));
                lines.push(match exclude {
                    ExcludeNote::AlreadyIgnored => format!(
                        "  git: {} is already ignored — the working tree stays clean",
                        to.display()
                    ),
                    ExcludeNote::Added(p) => format!(
                        "  git: {} added to {} — the working tree stays clean",
                        to.display(),
                        p.display()
                    ),
                    ExcludeNote::NotIgnored(why) => format!(
                        "  git: {} is NOT ignored — {why}; git status shows it until it is",
                        to.display()
                    ),
                });
                // The link: silent when the entry that ignored the directory matches it
                // too; its own exclude line when that entry was directory-only
                // (`target/` matches a directory, not a symlink); the honest row when the
                // directory was never ignored either.
                match link_exclude {
                    ExcludeNote::AlreadyIgnored => {}
                    ExcludeNote::Added(p) => lines.push(format!(
                        "  git: {} added to {} — the ignore entry is directory-only and does \
                         not match the link; the working tree stays clean",
                        link.display(),
                        p.display()
                    )),
                    ExcludeNote::NotIgnored(why) => lines.push(format!(
                        "  git: {} is not gitignored here — {why}",
                        link.display()
                    )),
                }
            }
            Applied::Migrated { from, to, config } => {
                migrated += 1;
                lines.push(format!("migrated {} -> {}", from.display(), to.display()));
                lines.push(match config {
                    ConfigNote::Written(p) => {
                        format!("  cargo: wrote [build] target-dir into {}", p.display())
                    }
                    ConfigNote::Rewritten(p) => {
                        format!("  cargo: rewrote target-dir in {}", p.display())
                    }
                    ConfigNote::Linked { .. } => unreachable!("matched by the arm above"),
                    ConfigNote::Untouched(why) => format!("  cargo: NOT re-pointed — {why}"),
                });
                if matches!(config, ConfigNote::Untouched(_)) {
                    for hint in crate::noindex::cargo_hint(to) {
                        lines.push(format!("  {hint}"));
                    }
                }
            }
            Applied::Planned { from, to, pointer } => {
                planned += 1;
                lines.push(format!(
                    "would rename {} -> {}",
                    from.display(),
                    to.display()
                ));
                lines.push(match pointer {
                    Pointer::None => {
                        "  cargo: no Cargo.toml beside it — nothing would be re-pointed".into()
                    }
                    Pointer::Symlink => format!(
                        "  cargo: would leave a symlink {} in place; no config edit: git checkout",
                        from.display()
                    ),
                    Pointer::Config(config) => format!(
                        "  cargo: would write [build] target-dir into {}",
                        config.display()
                    ),
                });
            }
            Applied::AlreadyExcluded(p) => {
                excluded += 1;
                lines.push(format!(
                    "already excluded — {} (nothing to do)",
                    p.display()
                ));
            }
            Applied::Skipped { path, reason } => {
                skipped += 1;
                lines.push(format!("skipped {} — {reason}", path.display()));
            }
            // A FAILURE IS NOT A SKIP, and the summary counts it separately so a pass
            // that could not do its work never renders as a quiet one.
            Applied::Failed { path, reason } => {
                failed += 1;
                lines.push(format!("FAILED {} — {reason}", path.display()));
            }
        }
    }
    let failures = if failed > 0 {
        format!(", {failed} failed")
    } else {
        String::new()
    };
    if planned > 0 || dry_run {
        lines.push(format!(
            "{planned} would migrate, {excluded} already excluded, {skipped} skipped{failures} \
             (dry run)"
        ));
    } else {
        lines.push(format!(
            "{migrated} migrated, {excluded} already excluded, {skipped} skipped{failures}"
        ));
    }
    lines
}

/// `atpkg noindex scan` — which cargo target dirs under `root` Spotlight is free to walk.
///
/// Exit 0 whatever it finds; the one exception is having no root at all (a deleted cwd
/// and no operand), which is a failure to do the thing asked, not a finding.
fn noindex_scan(root: Option<&str>, depth: usize) -> ExitCode {
    if !crate::noindex::SUPPORTED {
        println!("{NOINDEX_NOT_APPLICABLE}");
        return ExitCode::SUCCESS;
    }
    let root = match root {
        Some(r) => std::path::PathBuf::from(r),
        None => match std::env::current_dir() {
            Ok(cwd) => cwd,
            Err(e) => {
                eprintln!("atpkg noindex: no current directory to scan ({e}) — name a root");
                return ExitCode::from(1);
            }
        },
    };
    // A ROOT THAT CANNOT BE READ IS NOT AN EMPTY TREE. `aterm pkg noindex scan ~/srcc` (a
    // one-character typo for a `~/src` holding 40 exposed target dirs) must never print
    // "no cargo target directories under /Users//example/srcc" at exit 0 — that is a
    // confident false clean over a path nothing walked, the same shape as the 189 inert
    // `.metadata_never_index` markers this feature exists to prevent. Exit 1, the same
    // treatment a missing cwd already gets above: it is a failure to do the thing asked.
    match std::fs::metadata(&root) {
        Ok(m) if m.is_dir() => {}
        Ok(_) => {
            eprintln!(
                "atpkg noindex: {} is not a directory — name a directory to scan",
                root.display()
            );
            return ExitCode::from(1);
        }
        Err(e) => {
            eprintln!(
                "atpkg noindex: cannot scan {} ({e}) — check the path",
                root.display()
            );
            return ExitCode::from(1);
        }
    }
    println!("atpkg noindex: scanning {} (depth {depth})", root.display());
    let scan = crate::noindex::scan(&root, depth, &crate::noindex::Budget::VERB);
    let (n_indexed, n_hidden) = scan.counts();
    let exposed: Vec<&crate::noindex::Target> = scan.exposed().collect();

    // Sizes for the EXPOSED targets only, under ONE shared wall clock. Three things are
    // load-bearing here. Sizing a tree we already know is hidden is the expensive half of
    // the walk for no information, so hidden rows carry no size. Giving each of 127
    // target dirs the full ceiling is an hour, not a report, so the elapsed time is
    // subtracted from the budget as the pass proceeds. And the summary is the SUM of the
    // printed rows rather than an independent walk, so the arithmetic on screen adds up.
    let started = std::time::Instant::now();
    let mut sizes: Vec<crate::noindex::Size> = Vec::with_capacity(exposed.len());
    let mut total_bytes = 0u64;
    let mut total_complete = true;
    for target in &exposed {
        let budget = crate::noindex::Budget {
            max_entries: crate::noindex::Budget::VERB_SIZE.max_entries,
            max_wall: crate::noindex::Budget::VERB_SIZE
                .max_wall
                .saturating_sub(started.elapsed()),
        };
        let size = crate::noindex::size_of(&target.path, &budget);
        total_bytes = total_bytes.saturating_add(size.bytes);
        total_complete &= size.complete;
        sizes.push(size);
    }

    // Exposed first, then hidden — both already in path order from the scan. The rows
    // that carry an action go above the rows that do not.
    // An explicit space between the two columns, never just the field widths: a size of
    // "at least 41.2 GiB" (or "unmeasured") overruns `{:>11}` and would otherwise abut the
    // label as `indexedat least 41.2 GiB`.
    for (target, size) in exposed.iter().zip(&sizes) {
        println!(
            "atpkg noindex: {:<7} {:>11}  {}",
            "indexed",
            crate::noindex::human_size(size),
            target.path.display()
        );
    }
    for target in scan.targets.iter().filter(|t| !t.exposed()) {
        println!(
            "atpkg noindex: {:<7} {:>11}  {}",
            "hidden",
            "",
            target.path.display()
        );
    }

    if scan.targets.is_empty() {
        println!(
            "atpkg noindex: no cargo target directories under {} at depth {depth}",
            root.display()
        );
    } else if exposed.is_empty() {
        println!(
            "atpkg noindex: {n_indexed} indexed, {n_hidden} hidden — no build output here is \
             exposed to Spotlight"
        );
    } else {
        let total = crate::noindex::Size {
            bytes: total_bytes,
            complete: total_complete,
        };
        println!(
            "atpkg noindex: {n_indexed} indexed, {n_hidden} hidden — {} of build output is in \
             Spotlight",
            crate::noindex::human_size(&total)
        );
    }
    if !scan.complete {
        // A truncated walk that printed a bare count would be read as a census, and
        // "3 indexed" over a home that holds 127 target dirs is a wrong answer with a
        // confident shape.
        println!(
            "atpkg noindex: the walk stopped at its budget — this list is a floor, not a census"
        );
    }
    // The DISCOVERY walk and the SIZING pass have separate budgets, and the note above
    // covers only the first. On the motivating machine — 127 target dirs, 2.0 TB — the
    // shared 30 s sizing clock is spent on the first few trees and every remaining row
    // gets a zero-length walk. Those rows render `unmeasured` (never `at least 0 B`,
    // which reads as a small tree), and they need their own floor note, or the reader
    // takes a truncated sizing pass for a measured one.
    let sizes_truncated = sizes.iter().any(|s| !s.complete);
    if sizes_truncated {
        println!(
            "atpkg noindex: the sizing pass stopped at its {}s budget — every size above is \
             a floor, and an `unmeasured` row was not walked at all",
            crate::noindex::Budget::VERB_SIZE.max_wall.as_secs()
        );
    }
    // The biggest tree is the biggest win, and `min_by_key` over `Reverse` keeps the
    // FIRST of a tie, so the suggestion is stable across runs rather than dependent on
    // which equal-sized tree the walk happened to reach last.
    if let Some(target) = sizes
        .iter()
        .enumerate()
        .min_by_key(|(_, s)| std::cmp::Reverse(s.bytes))
        .map(|(i, _)| exposed[i])
    {
        println!(
            "atpkg noindex: next — aterm pkg noindex migrate {}",
            target.path.display()
        );
        if sizes_truncated {
            // "The biggest tree is the biggest win" is only true over rows that were
            // actually measured: the pick above can name a 2 GiB tree while an 800 GiB one
            // three rows below reads `unmeasured`.
            println!(
                "atpkg noindex:   (the biggest MEASURED tree — an unmeasured row above may \
                 be larger; name one to `migrate` yourself)"
            );
        }
    }
    if n_hidden > 0 {
        // `hidden` above is [`crate::noindex::exclusion_of`] reading the NAME. The name
        // is not the index: `.metadata_never_index` also looks like an exclusion and is
        // inert. Only `verify` measures, and the row must not be mistaken for it.
        println!(
            "atpkg noindex: `hidden` is what the NAME says; `aterm pkg noindex verify <dir>` \
             measures it"
        );
    }
    ExitCode::SUCCESS
}

/// `atpkg noindex verify <dir>` — MEASURE the exclusion, and say so in the exit code.
///
/// THREE exits, because the question has three answers: 0 excluded, 1 measured-indexed,
/// 3 could-not-measure. The verdict's own `Display` is the whole answer line — a second
/// sentence restating it here is how two spellings of one fact start to drift — so each
/// arm adds only what `Display` cannot know: the path to migrate, or what exit 3 means.
fn noindex_verify(dir: &str) -> ExitCode {
    use crate::noindex::Verdict;
    let path = std::path::Path::new(dir);
    // The real directory behind a symlinked operand, when there is one. Every path this
    // function PRINTS as an act must be that one: `migrate` refuses a symlink (renaming
    // the link would move the link and leave the build output indexed where it lives), so
    // a `next` line naming the link would be a dead end the reader cannot get out of.
    let through_link = std::fs::symlink_metadata(path)
        .is_ok_and(|m| m.file_type().is_symlink())
        .then(|| std::fs::canonicalize(path).ok())
        .flatten();
    let act_on = through_link.as_deref().unwrap_or(path);
    if crate::noindex::SUPPORTED {
        // Announce the wait BEFORE it happens. Twenty seconds of a silent cursor reads
        // as a hang, and the first thing a reader does about a hang is ^C — which kills
        // the measurement mid-probe and leaves them with no answer at all.
        println!(
            "atpkg noindex: probing (up to {}s — the control must index first, and the \
             margin after it scales with how long that took)",
            crate::noindex::Timing::DEFAULT.worst_case().as_secs()
        );
        // The probe measures the REAL directory (`verify` canonicalizes), so when the
        // operand is a link, say which tree the answer is about.
        if let Some(real) = &through_link {
            println!(
                "atpkg noindex: {} is a symlink — the probe measures {}",
                path.display(),
                real.display()
            );
        }
    }
    let verdict = crate::noindex::verify(path, &crate::noindex::Timing::DEFAULT);
    println!("atpkg noindex: {}: {verdict}", path.display());
    match &verdict {
        Verdict::Excluded => ExitCode::SUCCESS,
        Verdict::Indexed => {
            println!(
                "atpkg noindex: next — aterm pkg noindex migrate {}",
                act_on.display()
            );
            ExitCode::from(1)
        }
        Verdict::Unknown(_) => {
            println!(
                "atpkg noindex: could not measure — nothing is claimed either way; this exit \
                 (3) is neither excluded (0) nor INDEXED (1)"
            );
            ExitCode::from(3)
        }
        // The verdict line above already IS the not-applicable sentence; a second one
        // would only say it twice.
        Verdict::NotApplicable => ExitCode::SUCCESS,
    }
}

/// `atpkg noindex migrate <dir> [--dry-run]` — the one `rename(2)`.
///
/// Exit 1 on refusal, never 2: the grammar was fine and the operation the user asked for
/// did not happen, which is a different thing from being asked wrongly. 2 stays reserved
/// for usage across the whole CLI.
fn noindex_migrate(dir: &str, dry_run: bool) -> ExitCode {
    use crate::noindex::Migration;
    let path = std::path::Path::new(dir);
    match crate::noindex::migrate(path, dry_run) {
        Ok(Migration::Migrated { from, to }) => {
            println!(
                "atpkg noindex: migrated {} -> {}",
                from.display(),
                to.display()
            );
            // Say what did NOT happen. A tool that "moves" 41 GiB invites the reader to
            // wonder what it copied and what it left behind; this one moved a directory
            // entry inside a single parent, and if it had failed nothing would have
            // changed.
            println!(
                "atpkg noindex: nothing was copied and nothing was deleted — one rename inside \
                 the parent"
            );
            for line in crate::noindex::cargo_hint(&to) {
                println!("atpkg noindex:   {line}");
            }
            println!(
                "atpkg noindex: next — aterm pkg noindex verify {}",
                to.display()
            );
            ExitCode::SUCCESS
        }
        Ok(Migration::Planned { from, to }) => {
            println!(
                "atpkg noindex: would rename {} -> {}",
                from.display(),
                to.display()
            );
            // The same hint on the dry run: the rename is the easy half, and re-pointing
            // cargo is the half a reader wants to have read before committing to it.
            for line in crate::noindex::cargo_hint(&to) {
                println!("atpkg noindex:   {line}");
            }
            ExitCode::SUCCESS
        }
        // Idempotence is a SUCCESS, not a complaint: re-running migrate over a tree that
        // is already excluded is what a script does, and exit 1 there would fail a loop
        // that has nothing wrong with it.
        Ok(Migration::AlreadyExcluded(p)) => {
            println!(
                "atpkg noindex: already excluded — {} (nothing to do)",
                p.display()
            );
            ExitCode::SUCCESS
        }
        Ok(Migration::NotApplicable) => {
            println!("{NOINDEX_NOT_APPLICABLE}");
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg noindex: {e}");
            ExitCode::from(1)
        }
    }
}

/// `atpkg list` — the installed `(program, build)` pairs.
///
/// TWO AUDIENCES, TWO SHAPES, ONE SET OF FACTS. A terminal gets the human report:
/// verdict first (`atpkg: N program(s) — L live`), then one row per PROGRAM with its
/// live build, because the most common question here — "what am I running, is it
/// healthy?" — used to take twenty rows of raw TSV printed superseded-first. A pipe
/// (or `--porcelain`, for a script that runs on a TTY) gets the original headerless
/// `program\tbuild\tnotes` rows BYTE-IDENTICAL to what every existing parser keys on:
/// ascending builds, superseded before live, `atpkg: no programs installed` when empty.
///
/// The only accepted flag is `--porcelain`; anything else is a usage error (extra args
/// used to be silently ignored, which is a meaning nobody asked for).
fn cmd_list(args: &[String]) -> ExitCode {
    use std::io::IsTerminal as _;
    let mut porcelain = false;
    for a in args {
        match a.as_str() {
            "--porcelain" => porcelain = true,
            _ => {
                eprintln!("usage: atpkg list [--porcelain]");
                return ExitCode::from(2);
            }
        }
    }
    let human = !porcelain && std::io::stdout().is_terminal();
    run_list(layout(), human)
}

/// What `atpkg repair`'s shell-integration step prints: the hooks headline, then one line per
/// rc file the pass edited, failed to edit, or will never edit; an rc that already carried the
/// block gets none. Repair is the one pass that re-lays a block the user deleted
/// ([`crate::hooks::refresh_rewiring_rc`]), so a re-laid opt-out is named with the way back
/// out rather than folded into a wholesale "rc wiring" claim.
fn repair_hook_lines(pass: &crate::hooks::HookPass) -> Vec<String> {
    use crate::hooks::{HookPass, RcOutcome};
    let mut lines = match pass {
        HookPass::Rewritten(_) => vec!["repair: ~/.aterm/shell.d hooks rewritten".to_string()],
        HookPass::HooksNotWritten => {
            return vec![
                "repair: a ~/.aterm/shell.d hook could NOT be written, so no rc was wired — \
                 atpkg's block may only name a hook that is on disk; the next pass retries \
                 both"
                    .to_string(),
            ];
        }
        HookPass::NotHardened => {
            return vec![
                "repair: shell integration NOT rewritten — $HOME is unset, or ~/.aterm or \
                 ~/.aterm/shell.d could not be made your own private (0700) directory (a \
                 symlink, a foreign owner or a group/other-writable mode there is refused); \
                 no rc was read or written"
                    .to_string(),
            ];
        }
    };
    for (rc, outcome) in pass.rc() {
        lines.push(match outcome {
            RcOutcome::AlreadyWired => continue,
            RcOutcome::Appended => format!(
                "repair: appended atpkg's block to ~/{rc} (it sources ~/.aterm/shell.d; \
                 delete the block to opt out)"
            ),
            RcOutcome::RelaidOverOptOut => format!(
                "repair: re-laid atpkg's block in ~/{rc} — you had deleted it; delete it \
                 again to opt out (install and update passes then leave it deleted)"
            ),
            RcOutcome::OptOutKept => {
                format!("repair: ~/{rc} left alone — you deleted atpkg's block")
            }
            RcOutcome::ConsentFenced => format!(
                "repair: ~/{rc} left alone — it resolves under a folder macOS guards with a \
                 consent dialog (Documents, Desktop, Downloads, Pictures, Movies, Music, \
                 iCloud Drive, a sync provider's folder, a mounted volume), which no atpkg \
                 pass opens; source ~/.aterm/shell.d from it yourself, or use the export \
                 line `aterm pkg doctor` prints"
            ),
            RcOutcome::Unreadable => format!(
                "repair: ~/{rc} left alone — atpkg edits only a regular file it can read as \
                 UTF-8 (a dangling link, a directory, a FIFO or device, or other bytes is \
                 never touched)"
            ),
            RcOutcome::WriteFailed => format!(
                "repair: ~/{rc} could NOT be rewritten (the file or its directory refused \
                 the write); atpkg's block is not in it"
            ),
        });
    }
    #[cfg(unix)]
    if pass.rc().is_empty() {
        let names: Vec<String> = crate::hooks::RC_FILES
            .iter()
            .map(|(rc, _)| format!("~/{rc}"))
            .collect();
        lines.push(format!(
            "repair: none of {} exists, so no rc was wired (atpkg never creates one)",
            names.join(", ")
        ));
    }
    lines
}

/// `atpkg repair` — put a half-installed store back into a state the rest of the CLI
/// can describe honestly.
///
/// atpkg could already diagnose this precisely and could not fix it: `doctor` named 22
/// broken shims, and the only advice available was to reinstall by hand
/// (docs/AUDIT-nux-first-open-toolchain-2026-08-31.md, B6). Repair is deliberately made
/// of the same steps an install already runs, so there is one code path for laying
/// shims and one for shell integration:
///
/// 1. rewrite the shell hooks and wire them into the user's rc (PATH in every shell);
/// 2. re-lay each installed program's shims from its live `current` build (the newest
///    complete build only where no `current` link selects one) — never over a revoked
///    build's tombstone, which only an update pass may lift;
/// 3. lay — at `Deep` — the exec root of a trust build whose `bin/rustc` is a separate copy
///    of `trustc`, and route every trust shim through it ([`reconcile_exec_roots`]);
/// 4. name — and do not silently paper over — programs whose build is GONE, which no
///    local operation can fix and only a reinstall can.
///
/// Exits 1 when something remains that repair cannot do locally, so a script can tell
/// "fixed" from "needs the network" — exec roots the reconcile could not put in order
/// included.
fn run_repair(layout: Option<crate::store::Layout>) -> ExitCode {
    let Some(layout) = layout else {
        return ExitCode::from(1);
    };
    // Shell integration first: it is the half that needs no store at all, so a machine
    // with nothing installed still leaves repair better wired than it arrived. It REWIRES
    // an rc whose block the user deleted, which no unattended pass does (that deletion is
    // the opt-out the block documents, honoured through `hooks::RC_LEDGER`): repair is the
    // pass the user asks for. Say what happened, not what was attempted — one line per rc
    // it edited, left alone or skipped (`repair_hook_lines`).
    for line in repair_hook_lines(&crate::hooks::refresh_rewiring_rc(&layout)) {
        println!("{line}");
    }
    // The reroute stubs are the other store-less half: they embed this atpkg's path,
    // so a relocated or self-updated binary is exactly what repair re-lays for.
    lay_reroute_stubs(&layout);
    reassert_rustup_seam(&layout);
    repair_store(&layout)
}

/// The store half of [`run_repair`] — everything after the shell hooks, the reroute stubs
/// and the rustup seam — split out so its exit code is testable against a temp prefix
/// without rewriting the rc wiring of the account running the tests.
fn repair_store(layout: &crate::store::Layout) -> ExitCode {
    let installed = crate::list_installed(layout);
    if installed.is_empty() {
        // Nothing installed can still leave a root behind (a removal that failed
        // part-way): the sweep inside the reconcile takes it, and says so.
        let _ = reconcile_exec_roots(layout, crate::seam::Depth::Deep);
        println!(
            "repair: no programs installed — `aterm pkg install --default-set` installs the toolset"
        );
        return ExitCode::SUCCESS;
    }
    let (relaid, missing, revoked, tracked, failed) = relay_shims(layout);
    println!("repair: re-laid shims for {relaid} program(s)");
    for r in &revoked {
        println!(
            "repair: {r} was revoked (yanked/below floor) — left disabled; run `aterm pkg update`"
        );
    }
    // The exec roots, at `Deep`, over the shims just re-laid: every file of every root proven
    // a clone of its store file, a root rebuilt when it is not, and every trust shim rendered
    // through it. Repair is
    // the fix doctor names for a PATH tippy that cannot run, so a root it could not lay is
    // something left undone: exit 1, like a shim that could not be re-laid.
    let exec_roots_ok = reconcile_exec_roots(layout, crate::seam::Depth::Deep);
    if !exec_roots_ok {
        println!(
            "repair: the trust exec roots are not in order (the `atpkg: exec root: warn` \
             line(s) on stderr name what failed) — a trust shim they cover may still run the \
             store path, where that build's tippy refuses to start"
        );
    }
    if missing.is_empty()
        && revoked.is_empty()
        && tracked.is_empty()
        && failed.is_empty()
        && exec_roots_ok
    {
        println!("repair: done — `aterm pkg doctor` confirms");
        return ExitCode::SUCCESS;
    }
    if !missing.is_empty() {
        println!(
            "repair: {} program(s) have no build on disk and need a reinstall: {}",
            missing.len(),
            missing.join(", ")
        );
        println!("repair: `aterm pkg install --default-set` refetches them");
    }
    if !tracked.is_empty() {
        println!(
            "repair: {} build(s) carry a tracked-install record (<build>.tracked-install), \
             written when a provenance-tracked installer either staged the bundle IN-PROCESS \
             because its untracked launchd lane could not run — every executable then carries \
             com.apple.provenance; that is the default, and {}=1 refuses instead — or KEPT \
             an untracked lane's tree that came back tagged. Each build's own reason follows \
             it. Repair does not scan the bundle: `aterm pkg doctor` is what says which files \
             carry the tag today, and it names a stale record when none does: {}",
            tracked.len(),
            crate::lay::REFUSE_TRACKED_ENV,
            tracked.join("; ")
        );
        println!(
            "repair: nothing local removes the tag (`xattr -d` exits 0 and removes nothing) \
             and the archive is reclaimed after every stage, so a build whose files really do \
             carry it needs a re-seed through a working untracked lane — from any shell, \
             atpkg hands the stage to a launchd job running its own binary in place, or from \
             a clean byte copy of it or of its whole app bundle when the binary is tagged: \
             `aterm pkg uninstall <program> && aterm pkg install <program>`"
        );
    }
    if !failed.is_empty() {
        println!(
            "repair: {} program(s) still have shims that could not be re-laid: {}",
            failed.len(),
            failed.join("; ")
        );
    }
    ExitCode::from(1)
}

/// Step 2 of [`run_repair`], split out so it can be tested without the rc wiring step 1
/// writes: re-lay every installed program's shims. Returns how many programs were re-laid,
/// the `program@build` of each whose build is gone, of each left tombstoned, of each a
/// provenance-tracked installer recorded (`<build>.tracked-install` — staged in-process,
/// or a kept lane tree that came back tagged), and of each
/// whose shims could not be re-laid — the last is what a REFUSED lay from a tracked process
/// looks like, and it must reach the exit code (audit 2026-09-12: repair said "done" over it).
fn relay_shims(
    layout: &crate::store::Layout,
) -> (usize, Vec<String>, Vec<String>, Vec<String>, Vec<String>) {
    let mut builds: std::collections::BTreeMap<String, std::collections::BTreeSet<u64>> =
        std::collections::BTreeMap::new();
    for (program, build) in crate::list_installed(layout) {
        builds.entry(program).or_default().insert(build);
    }
    let recorded = crate::status::read(layout)
        .map(|s| s.programs)
        .unwrap_or_default();
    let (mut relaid, mut missing, mut revoked, mut tracked, mut failed) =
        (0usize, Vec::new(), Vec::new(), Vec::new(), Vec::new());
    for (program, complete) in &builds {
        let Some(&newest) = complete.last() else {
            continue;
        };
        // The build ACTIVATION selects (`store/<program>/current`), not the highest on disk:
        // `rollback` keeps the build it rolled off until gc, and re-laying that one silently
        // undid the rollback and minted a ChannelShimMismatch (audit K2, 2026-09-12). With no
        // usable link — a prefix older than it, or one naming a build that is gone — the
        // newest complete build, as before.
        let build = std::fs::read_link(layout.program_current(program))
            .ok()
            .and_then(|t| crate::ops::store_build_of(&layout.prefix, &t))
            .filter(|(owner, b)| owner == program && complete.contains(b))
            .map_or(newest, |(_, b)| b);
        let build_dir = layout.build_dir(program, build);
        if !build_dir.exists() {
            missing.push(format!("{program}@{build}"));
            continue;
        }
        // A build a provenance-tracked installer RECORDED (`<build>.tracked-install`,
        // `crate::store::record_tracked_install`). Two outcomes write it
        // (`install::decide_tracked_stage`): an in-process stage because the lane could
        // not run, which tags every executable, and `KeepRecorded` — the untracked lane laid the
        // tree and only its witness came back tagged. This reads the record and never
        // scans, so it reports the record, not the tag; `doctor` does the scan and says
        // "a stale record" when nothing in `bin/` carries it. Exit 1 either way: a
        // recorded build needs an operator's eye, like a build that is gone.
        if let Some(why) = crate::store::tracked_install_record(&build_dir) {
            tracked.push(format!("{program}@{build} — {why}"));
        }
        // What this program exposes, recovered from its own shims where they survive
        // (they resolve even when their TARGET is gone, which is the audited shape) and
        // falling back to the program's own name, which is the overwhelmingly common
        // single-tool case.
        let exposes =
            crate::installed_exposes(layout, program).unwrap_or_else(|| vec![program.clone()]);
        // Never over a tombstone (audit K2, 2026-09-12). A revoked build keeps its tree,
        // marker and `current` links; only its shims turn into failing notices, which
        // resolve to nothing — so the fallback above read "nothing recorded" and laid a
        // live shim that made the yanked/below-floor build runnable again. Only an update
        // pass, which holds the index that revoked it, may lift that.
        let tombstoned = recorded
            .get(program)
            .is_some_and(|r| r.state.starts_with("tombstoned:"))
            || exposes.iter().any(|tool| is_tombstone_shim(layout, tool));
        if tombstoned {
            revoked.push(format!("{program}@{build}"));
            continue;
        }
        match crate::activate::install_shims(
            layout,
            &build_dir,
            &exposes,
            crate::activate::Aliases::laid_for(layout, program),
        ) {
            Ok(refused) => {
                relaid = relaid.saturating_add(1);
                for name in refused {
                    println!("repair: {program}: refused shim {name:?} (not an allowed tool name)");
                }
            }
            Err(e) => {
                println!("repair: {program}: could not re-lay shims: {e}");
                failed.push(format!("{program}@{build} — {e}"));
            }
        }
    }
    (relaid, missing, revoked, tracked, failed)
}

/// Whether this machine has DISABLED `program`: its recorded row says `tombstoned:` or
/// `aborted:` (the update lane's rows for a build it revoked, or a group it aborted at),
/// or one of the tools the program exposes is a tombstone shim on disk. The same two
/// measurements `repair` makes before it would re-lay a shim (audit K2) — read, never
/// inferred from the pin — so a lane that repaints rows for members absent on this
/// target leaves a fault row where it found one.
fn program_disabled_here(
    layout: &crate::store::Layout,
    recorded: Option<&crate::Status>,
    program: &str,
) -> bool {
    // A tombstoned build is no longer counted as installed, so its exposes are not
    // readable from the store; the program's own name is the tool that matters then
    // (the same fallback `repair` uses).
    recorded
        .and_then(|s| s.programs.get(program))
        .is_some_and(|r| r.state.starts_with("tombstoned:") || r.state.starts_with("aborted:"))
        || crate::installed_exposes(layout, program)
            .unwrap_or_else(|| vec![program.to_string()])
            .iter()
            .any(|tool| is_tombstone_shim(layout, tool))
}

/// Whether `bin/<tool>` is a tombstone: a shim that forwards nowhere and carries the
/// notice `activate::install_tombstone_shim` writes.
fn is_tombstone_shim(layout: &crate::store::Layout, tool: &str) -> bool {
    let Some(tool) = crate::store::ToolName::new(tool) else {
        return false;
    };
    let shim = layout.shim(&tool);
    crate::platform::resolve_shim(&shim).is_none()
        && crate::metadata_io::read_bounded_regular_utf8(&shim, crate::platform::MAX_SHIM_BYTES)
            .is_ok_and(|body| body.contains(" was yanked/revoked"))
}

/// [`cmd_list`] under an already-resolved layout — split so the exit codes and both
/// renderings are exercisable against a temp store without a process (and without a TTY).
fn run_list(layout: Option<crate::store::Layout>, human: bool) -> ExitCode {
    // Exit 1, like every sibling read verb (`which`, `doctor`, `verify`): `layout()`
    // already printed why. This used to return success with an error on stderr — a
    // silent green in exactly the environments that are already broken.
    let Some(layout) = layout else {
        return ExitCode::from(1);
    };
    warn_if_never_checked(&layout);
    let installed = crate::list_installed(&layout);
    // A program DEV-LINKED FROM THE START — never installed from the index, so it has no
    // store build for `list_installed` to find — is still a program this machine runs:
    // its shims are in the managed bin/ and `which` names them. It was invisible here
    // (measured 2026-09-14: a Linux box with the Trust stage2 dev-linked listed only
    // claude and codex), which made "via atpkg" unverifiable from the one verb that
    // exists to say what atpkg manages. Such programs get a row with no build number.
    let linked_only: Vec<String> = crate::linkmode::linked_programs(&layout)
        .into_iter()
        .filter(|p| !installed.iter().any(|(q, _)| q == p))
        .collect();
    if installed.is_empty() && linked_only.is_empty() {
        if human {
            println!("{EMPTY_LIST_HUMAN}");
        } else {
            // The stable piped form — byte-identical to every earlier release.
            println!("atpkg: no programs installed");
        }
        return ExitCode::SUCCESS;
    }
    let live = crate::active_builds(&layout);
    let mut linked: std::collections::BTreeMap<String, Option<std::path::PathBuf>> =
        std::collections::BTreeMap::new();
    let mut pinned: std::collections::BTreeSet<String> = std::collections::BTreeSet::new();
    for (program, _) in &installed {
        if !linked.contains_key(program) && crate::linkmode::is_linked(&layout, program) {
            linked.insert(
                program.clone(),
                crate::linkmode::linked_checkout(&layout, program),
            );
        }
        if crate::pin::is_pinned(&layout, program) {
            pinned.insert(program.clone());
        }
    }
    for program in &linked_only {
        linked.insert(
            program.clone(),
            crate::linkmode::linked_checkout(&layout, program),
        );
    }
    let lines = if human {
        list_human_lines(&installed, &linked_only, &live, &linked, &pinned)
    } else {
        list_porcelain_lines(&installed, &linked_only, &live, &linked, &pinned)
    };
    for line in lines {
        println!("{line}");
    }
    ExitCode::SUCCESS
}

/// The empty store's human answer — the fact and, on the same line, the one command
/// that changes it (the no-dead-ends rule).
const EMPTY_LIST_HUMAN: &str =
    "atpkg: no programs installed — aterm pkg install --default-set installs the ALab toolset";

/// `update` on an empty, unadopted store — the fact keeps its stable head
/// (`atpkg: nothing installed to update`), the remedy rides after the em dash.
const EMPTY_UPDATE: &str = "atpkg: nothing installed to update — aterm pkg install \
                            --default-set installs the ALab toolset";

/// `verify` over an empty store — nothing was attested because nothing is installed,
/// and saying only that is a dead end; the order of acts matters (install, THEN verify).
const EMPTY_VERIFY: &str = "atpkg: nothing installed to verify — install first (aterm pkg \
                            install --default-set), then verify re-attests every program \
                            against the signed root";

/// The STABLE script form: one row per installed BUILD, `program\tbuild\tnotes`,
/// headerless, ascending builds (superseded rows before the live one) — byte-identical
/// to what `atpkg list` always printed, because pipes elsewhere key on it.
///
/// WHAT AM I ACTUALLY RUNNING. `list` used to print `<program>\t<build>` and nothing
/// else, which cannot answer that — the store legitimately holds MORE THAN ONE build per
/// program (retention is live + 1 rollback). The three facts that change what runs are
/// which build is LIVE, whether a dev link has taken the program over, and whether a pin
/// is holding it back; each comes from the authority that owns it (`active_builds` reads
/// the shims, the link marker, the pin state).
fn list_porcelain_lines(
    installed: &[(String, u64)],
    linked_only: &[String],
    live: &std::collections::BTreeMap<String, u64>,
    linked: &std::collections::BTreeMap<String, Option<std::path::PathBuf>>,
    pinned: &std::collections::BTreeSet<String>,
) -> Vec<String> {
    let mut shown_link = std::collections::BTreeSet::new();
    let mut lines = Vec::new();
    // Dev-linked from the start: no store build, so the build column is `-` — the same
    // spelling the human form uses for a build that is not what runs.
    for program in linked_only {
        let note = match linked.get(program).and_then(|c| c.as_ref()) {
            Some(path) => format!("DEV-LINKED -> {}", path.display()),
            None => "DEV-LINKED".to_string(),
        };
        let mut notes = vec![note];
        if pinned.contains(program) {
            notes.push("pinned".to_string());
        }
        lines.push(format!("{program}\t-\t{}", notes.join("  ")));
    }
    for (program, build) in installed {
        let mut notes: Vec<String> = Vec::new();
        if let Some(checkout) = linked.get(program) {
            // A dev link supersedes EVERY store build, so the note belongs on the program,
            // not on one row; print it once and never call a store build "live" while it is
            // shadowed — that was the misleading part.
            if shown_link.insert(program.clone()) {
                match checkout {
                    Some(path) => notes.push(format!("DEV-LINKED -> {}", path.display())),
                    None => notes.push("DEV-LINKED".to_string()),
                }
            } else {
                notes.push("(shadowed by the dev link)".to_string());
            }
        } else if live.get(program) == Some(build) {
            notes.push("live".to_string());
        } else {
            notes.push("superseded (kept for rollback)".to_string());
        }
        if pinned.contains(program) {
            notes.push("pinned".to_string());
        }
        lines.push(format!("{program}\t{build}\t{}", notes.join("  ")));
    }
    lines
}

/// The human report: a count summary FIRST (verdict before detail — the grid rule),
/// then one aligned row per PROGRAM in its live state, with superseded builds folded
/// into a trailing "(N older build(s) kept for rollback)" — the fact survives, the
/// twenty-row scan does not. No health adjective anywhere: counts are what the shims
/// prove; "healthy" is doctor's word, earned by running checks.
fn list_human_lines(
    installed: &[(String, u64)],
    linked_only: &[String],
    live: &std::collections::BTreeMap<String, u64>,
    linked: &std::collections::BTreeMap<String, Option<std::path::PathBuf>>,
    pinned: &std::collections::BTreeSet<String>,
) -> Vec<String> {
    // Group the per-build rows per program, preserving list order (ascending builds).
    let mut builds: std::collections::BTreeMap<&str, Vec<u64>> = std::collections::BTreeMap::new();
    for (program, build) in installed {
        builds.entry(program).or_default().push(*build);
    }
    // A dev-linked program with no store build still takes a row: an empty build list.
    for program in linked_only {
        builds.entry(program).or_default();
    }
    let name_width = builds.keys().map(|p| p.len()).max().unwrap_or(0);
    let build_width = installed
        .iter()
        .map(|(_, b)| b.to_string().len())
        .max()
        .unwrap_or(0)
        .max(usize::from(!linked_only.is_empty()));
    let mut live_count = 0usize;
    let mut no_live = 0usize;
    let mut rows = Vec::new();
    for (program, program_builds) in &builds {
        let is_pinned = pinned.contains(*program);
        let state = if let Some(checkout) = linked.get(*program) {
            let mut s = match checkout {
                Some(path) => format!("DEV-LINKED -> {}", path.display()),
                None => "DEV-LINKED".to_string(),
            };
            if program_builds.is_empty() {
                s.push_str("  (no store build — linked from the start)");
            } else {
                s.push_str(&format!(
                    "  ({} store build(s) shadowed)",
                    program_builds.len()
                ));
            }
            if is_pinned {
                s.push_str(", pinned (updates held)");
            }
            s
        } else if let Some(live_build) = live.get(*program) {
            live_count += 1;
            let mut s = String::from("live");
            if is_pinned {
                s.push_str(", pinned (updates held)");
            }
            let older = program_builds.iter().filter(|b| *b != live_build).count();
            if older > 0 {
                s.push_str(&format!("   ({older} older build(s) kept for rollback)"));
            }
            s
        } else {
            // Loud and uppercase, like the porcelain DEV-LINKED marker: a program with
            // builds on disk and nothing on PATH is the one row that needs doctor.
            no_live += 1;
            format!("NO LIVE BUILD ({} build(s) on disk)", program_builds.len())
        };
        let shown_build = if linked.contains_key(*program) {
            // Shadowed: the store build number would imply it is what runs.
            "-".to_string()
        } else {
            live.get(*program)
                .or_else(|| program_builds.last())
                .map(u64::to_string)
                .unwrap_or_default()
        };
        rows.push(format!(
            "  {program:<name_width$}  {shown_build:>build_width$}  {state}"
        ));
    }
    // The summary, zero-count categories omitted — "10 program(s) — 10 live" is the
    // whole story on a healthy machine, and every extra clause would dilute the one
    // that matters on an unhealthy one.
    let mut clauses = vec![format!("{live_count} live")];
    if !pinned.is_empty() {
        clauses.push(format!("{} pinned", pinned.len()));
    }
    if !linked.is_empty() {
        clauses.push(format!("{} dev-linked", linked.len()));
    }
    if no_live > 0 {
        clauses.push(format!("{no_live} without a live build"));
    }
    let mut lines = vec![format!(
        "atpkg: {} program(s) — {}",
        builds.len(),
        clauses.join(", ")
    )];
    lines.extend(rows);
    if no_live > 0 {
        // The one next act, only when a row needs explaining — doctor names the why.
        lines.push("atpkg: next — aterm pkg doctor".to_string());
    }
    lines
}

/// `atpkg uninstall <program>` — remove its shims + store builds (fail-closed inside the prefix).
/// Remove `program` from the store and retire everything this machine recorded about it.
///
/// Split out of [`cmd_uninstall`] so the bookkeeping is exercisable without a process — the
/// CLI arm keeps only its printing and its exit code. Both steps are part of "uninstalled",
/// and neither happened before:
///
/// * THE STATUS ROW. `active_builds` reads shims, so `doctor` went quiet after a removal —
///   but Settings ▸ Packages reads the RECORD, and listed a deleted program as `active` at
///   its last build forever. The stale-row reconciler cannot help and correctly declines
///   to: the signed index still names this program, it is this MACHINE that no longer
///   carries it.
/// * THE REMOVED MARKER, which already worked: without it the resumable seed lane
///   reinstalls the program on the next launch — the manager undoing a deliberate act.
///
/// THE INTENT IS WRITTEN BEFORE THE DELETE. `ops::uninstall` takes the shims off first and
/// then spends seconds in `remove_dir_all` (the 3.4 GB trust tree). The marker used to land
/// only after that returned, so a kill inside the delete — Ctrl-C, the window closing, a
/// crash — left the program absent to `active_builds` with no `removed` entry, and the next
/// six-hourly pass pulled the whole tuple member straight back. A half-deleted program is
/// still one the user asked to remove; the name gates are the only failure that deletes
/// nothing, so they run first and the marker is never minted for a refused name.
fn uninstall_and_retire(layout: &crate::store::Layout, program: &str) -> std::io::Result<()> {
    retirement_name_gate(program)?;
    record_removed(layout, program);
    // Removing an EXTRA withdraws its opt-in: the marker is what kept it in the set, and
    // `removed` alone would be lifted by the next explicit install anyway.
    layout.clear_optin(program);
    // A removal on purpose is not a retirement in favour of a system copy: forget the
    // date so a later `system:` row never reports a retirement this act superseded.
    layout.clear_retired(program);
    // The rustup seam goes WITH the toolchain it presents (audit 2026-09-14): the view
    // is a clone of the live trust build, so `uninstall trust` would otherwise leave
    // `~/.rustup/toolchains/trust` running a ghost of the deleted compiler. The single
    // program now does too — before the build trees go, as the whole-set path orders it.
    if program == "trust" {
        detach_rustup_seams(layout);
    }
    crate::uninstall(layout, program)?;
    // The row stays on a failed delete: the machine is in a half-state worth reporting
    // (the same choice `uninstall --all` makes).
    clear_status_row(layout, program);
    Ok(())
}

/// The names [`uninstall_and_retire`] refuses before it records or deletes anything.
///
/// ROW DELETION IS A NEW CAPABILITY, so it gets the name gate the old code never needed.
/// `ops::uninstall`'s shape rule rejects only empty/`.`/`..`/separators/NUL, so without
/// this `atpkg uninstall '*toolset*'` returned Ok and DELETED a bookkeeping row that has
/// its own owner and its own reaper, and `atpkg uninstall --help` appended a flag to the
/// removed-markers file. Neither is a program; neither may be retired through here. The
/// shape rule is asked here too, because the removal intent is recorded before
/// `ops::uninstall` would get to refuse the name.
fn retirement_name_gate(program: &str) -> std::io::Result<()> {
    if (program.starts_with('*') && program.ends_with('*')) || program.starts_with('-') {
        return Err(std::io::Error::new(
            std::io::ErrorKind::InvalidInput,
            format!("{program:?} is not a program name"),
        ));
    }
    crate::ops::uninstall_name_shape(program)
}

fn cmd_uninstall(program: Option<&String>) -> ExitCode {
    let Some(program) = program else {
        eprintln!("usage: atpkg uninstall <program> | --all");
        return ExitCode::from(2);
    };
    if program == "--all" {
        return cmd_uninstall_all();
    }
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    uninstall_one(&layout, program)
}

/// Whether anything on this machine still bears `program`'s name: a store tree (even a
/// partial one an interrupted install left) or a bin shim resolving into it — the same
/// two places `ops::uninstall` cleans. The record row is deliberately NOT consulted:
/// a row is a claim about the store, not the store.
fn program_present(layout: &crate::store::Layout, program: &str) -> bool {
    let prog_store = layout.prefix.join("store").join(program);
    if prog_store.exists() {
        return true;
    }
    if let Ok(entries) = std::fs::read_dir(layout.bin_dir()) {
        for e in entries.flatten() {
            if let Some(target) = crate::platform::resolve_shim(&e.path())
                && target.starts_with(&prog_store)
            {
                return true;
            }
        }
    }
    false
}

/// [`cmd_uninstall`]'s single-program body, split from the layout resolution so the
/// refusal below is exercisable against a temp store.
fn uninstall_one(layout: &crate::store::Layout, program: &str) -> ExitCode {
    // REFUSE TO "UNINSTALL" WHAT WAS NEVER THERE. This used to print "uninstalled ty",
    // exit 0, and mint a durable removed-marker — a fabricated success whose hidden
    // state change silently suppressed future set-completion for a program the user
    // never had. Only program-shaped names take this path: a sentinel/flag name still
    // reaches the retirement gate's own refusal, which owns those words.
    if !((program.starts_with('*') && program.ends_with('*')) || program.starts_with('-'))
        && !program_present(layout, program)
    {
        eprintln!("atpkg: {program} is not installed — nothing to uninstall");
        return ExitCode::from(1);
    }
    // Removing a managed program is an EXPLICIT act, so this machine stops being one
    // that keeps the whole set complete — otherwise the next unattended pass would
    // reinstall what the user just removed, which is the manager fighting its owner.
    // Adoption is re-established by the deliberate whole-set act (Settings ▸ Packages ▸
    // Install ALab toolset), and the way to drop ONE program while staying adopted is
    // `[packages].exclude`. Like the removed marker, this is INTENT, so it lands before
    // the multi-second delete a kill can interrupt — but only for a name the retirement
    // gate accepts, which is the one failure that deletes nothing.
    let opted_out = retirement_name_gate(program).is_ok() && adopted(layout);
    if opted_out {
        clear_adoption(layout);
    }
    let code = match uninstall_and_retire(layout, program) {
        Ok(()) => {
            // A removed program's pending stub goes with it: `removed` suppresses the
            // reinstall, so a stub promising "installing" would be a standing lie.
            crate::stub::remove_stub(layout, program);
            println!("atpkg: uninstalled {program}");
            // Programs that REQUIRE it are told, not stopped (§17.10).
            warn_dependents(layout, program);
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg: uninstall {program} failed: {e}");
            ExitCode::from(1)
        }
    };
    if opted_out {
        println!(
            "atpkg: this machine no longer auto-completes the ALab toolset (removing \
             a program opts out). Re-adopt with `aterm pkg install --default-set`, or \
             keep the set and drop just this one with [packages].exclude in aterm.toml"
        );
    }
    code
}

/// The parenthetical for a status row that names a copy of the program this manager does
/// NOT own: a binary the user already had (`system: <path>`), or one obtained through
/// another protocol (`installed via <protocol>: <path>`). `None` for every other row —
/// those name nothing that is installed on this machine.
///
/// [`cmd_uninstall_all`] uses it to say what it LEFT ALONE, which is the half of §S11 the
/// code never had: *"system copies are never touched **and the output says so**"*
/// (`docs/DESIGN-which-copy-runs-2026-08-27.md`).
fn foreign_copy_note(state: &str) -> Option<String> {
    if let Some(path) = crate::state::system_path(state) {
        return Some(format!("(system copy: {path})"));
    }
    let (protocol, path) = crate::state::installed_via_path(state)?;
    Some(format!("(installed via {protocol}: {path})"))
}

/// What one [`sweep_uninstall_targets`] pass did: the programs it actually removed, the
/// foreign copies it deliberately did not touch, and how many removals failed.
#[derive(Default)]
struct UninstallSweep {
    /// Names whose store tree or shims this pass really took off the machine.
    removed: Vec<String>,
    /// `<name> (system copy: <path>)` / `<name> (installed via <protocol>: <path>)` for a
    /// copy that is still exactly where it was.
    left_alone: Vec<String>,
    /// Removals that errored; each has already printed its own stderr line.
    failures: u32,
}

/// The programs [`cmd_uninstall_all`] sweeps.
///
/// THE SWEEP IS THE UNION OF WHAT IS LIVE AND WHAT THE RECORD STILL CLAIMS. A TOMBSTONED
/// program is invisible to `active_builds` — its shims were replaced by failing stubs —
/// so walking the live set alone stepped straight past it, leaving its build tree on disk
/// and its row in the record after an explicit whole-set removal. That is the exact class
/// this verb exists to clear. Sentinel bookkeeping rows and stray flag rows are excluded:
/// neither is a program, and neither is this verb's to retire.
fn uninstall_all_targets(layout: &crate::store::Layout) -> std::collections::BTreeSet<String> {
    let mut targets: std::collections::BTreeSet<String> =
        crate::active_builds(layout).keys().cloned().collect();
    if let Some(s) = crate::status::read(layout) {
        targets.extend(
            s.programs
                .keys()
                .filter(|k| !(k.starts_with('*') && k.ends_with('*')) && !k.starts_with('-'))
                .cloned(),
        );
    }
    targets
}

/// Remove every target this manager actually installed, and report the rest.
///
/// NOTHING IS "REMOVED" THAT WAS NEVER OURS. The target list above is the union of the
/// live builds and every status row, and a row is written for plenty of names atpkg never
/// installed: `system: /opt/homebrew/bin/gh — not managed by aterm` for a foreign copy
/// that satisfies a member, `installed via pkg: …` for an OS-level install (the Command
/// Line Tools, Homebrew), `unavailable on <target>`, `extra — not installed`, `agent
/// program — installing`. For all of those `ops::uninstall` finds no store tree under the
/// prefix, deletes nothing, and returns `Ok(())` — which this loop read as a removal. So
/// `aterm pkg uninstall --all` printed `atpkg: removed ay, clt, gh, trust` on a machine
/// where `gh` and the Command Line Tools were untouched and still ran, exited 0, and
/// wiped the very rows that recorded where those copies live (the next pass then had to
/// re-discover them). Presence on disk — [`program_present`], the same two places
/// `ops::uninstall` cleans — is the only thing that makes a name this verb's to claim.
fn sweep_uninstall_targets(
    layout: &crate::store::Layout,
    targets: &std::collections::BTreeSet<String>,
) -> UninstallSweep {
    // The rows as they stood BEFORE the sweep: a row is what says whether this manager
    // ever owned a copy, and `clear_status_row` rewrites the record as we go.
    let rows = crate::status::read(layout)
        .map(|s| s.programs)
        .unwrap_or_default();
    let mut sweep = UninstallSweep::default();
    for program in targets {
        let state = rows.get(program).map(|r| r.state.as_str()).unwrap_or("");
        if !program_present(layout, program) {
            match foreign_copy_note(state) {
                // A copy that is STILL THERE and was never ours. Name it, and KEEP its
                // row: the row is the true answer about this machine (`which` and the
                // next pass both read it), not a claim about a store tree this verb
                // emptied.
                Some(note) => sweep.left_alone.push(format!("{program} {note}")),
                // A row with nothing behind it — an extra nobody opted in to, a member
                // with no build for this target, a managed row that outlived its program.
                // It still goes, because a record that outlives its program is a claim
                // about a machine state that no longer exists; but it was not a removal,
                // so it is not reported as one.
                None => clear_status_row(layout, program),
            }
            continue;
        }
        match crate::uninstall(layout, program) {
            Ok(()) => {
                // Same retirement as the single-program verb: a row that outlives its
                // program is a claim about a machine state that no longer exists.
                //
                // Only on success — but note what an `Err` here actually means. `uninstall`
                // removes the shims FIRST and unconditionally; its only fallible step is the
                // final `remove_dir_all`. So a failure leaves the program already OFF PATH
                // with its build tree behind, and keeping the row is the conservative choice
                // precisely because the machine is now in a half-state worth reporting.
                clear_status_row(layout, program);
                sweep.removed.push(program.clone());
            }
            Err(e) => {
                sweep.failures += 1;
                eprintln!("atpkg: uninstall {program} failed: {e}");
            }
        }
    }
    sweep
}

/// Name the copies the sweep did not touch, or print nothing when there were none.
///
/// Silence here is what let those names ride in the `removed` list instead: a user whose
/// `gh` comes from Homebrew was told aterm had removed it.
fn print_left_alone(left_alone: &[String]) {
    if left_alone.is_empty() {
        return;
    }
    println!(
        "atpkg: left alone — not installed by aterm, so nothing was removed: {}",
        left_alone.join(", ")
    );
}

/// `atpkg uninstall --all` — remove the WHOLE managed toolset and reclaim its disk.
///
/// The batteries-included install lays down ~3.2 GB without asking (§9.1 — the bytes
/// shipped inside the app, so installing the app is the consent). A user who decides
/// they do not want it must have a way OUT that is as single-step as the way in;
/// making them run `uninstall` eight times, once per program they never chose
/// individually, is the kind of asymmetry that earns a product a reputation.
///
/// Composed from the tested per-program primitive plus a GC sweep rather than an
/// `rm -rf` of the prefix: `[packages].prefix` is user-configurable and chain-vetted,
/// and a recursive delete of a path from config is exactly the operation that must
/// never exist. Adoption is cleared last, so the 6h pass does not put it all back.
fn cmd_uninstall_all() -> ExitCode {
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let targets = uninstall_all_targets(&layout);
    // The seam goes with the set, and goes FIRST: every recorded rustup entry is
    // detached while it still resolves into the prefix. Before the branch, so a record
    // left on an already-empty store is dropped too.
    detach_rustup_seams(&layout);
    if targets.is_empty() {
        // RECORD THE DECLINE ANYWAY. "Remove the ALab toolset" states an INTENT —
        // "I do not want this on my machine" — and that intent is independent of
        // whether anything happens to be installed at this instant. Returning early
        // made the documented exit door a silent no-op in exactly the cases that
        // need it most: an Intel/lean Mac (where the store is ALWAYS empty, so the
        // button could never do anything), and a launch during which the seed pass
        // has not run yet. The user saw "ALab toolset removed", and the next launch
        // re-adopted the machine and installed the whole set.
        clear_adoption(&layout);
        record_decline(&layout);
        // A decline withdraws every extras opt-in too: the machine wants none of the set.
        layout.clear_all_optins();
        layout.clear_all_retired();
        // The decline extends to the pending stubs: a declined machine keeps NO
        // default-set names on PATH promising an install that will never come.
        crate::stub::remove_all_stubs(&layout);
        crate::reroute::remove_all(&layout);
        remove_exec_roots(&layout);
        println!(
            "atpkg: removed nothing (nothing installed) — noted that this machine declines \
             the ALab toolset: no later pass installs it (not the first-run seed, not the \
             unattended update, not set auto-completion); aterm pkg install --default-set \
             opts back in"
        );
        return ExitCode::SUCCESS;
    }
    let UninstallSweep {
        mut removed,
        left_alone,
        failures,
    } = sweep_uninstall_targets(&layout, &targets);
    // Reclaim the now-unreferenced build trees — this is where the gigabytes
    // actually go back, and reporting it is the point of the verb.
    let report = crate::gc::run(&layout);
    print_gc_sweeps("uninstall --all", &report);
    print_gc_abstentions("uninstall --all", &report);
    crate::hooks::refresh(&layout);
    // Last, and only after the removals: this machine no longer runs the set, so
    // set-completion must not reinstate it on the next unattended pass — and the
    // SEED lane must not either. The seed installs whatever the store lacks (that
    // is what makes an interrupted first run resumable), so without a durable
    // decline the next launch would cheerfully restore all 3.2 GB the user just
    // removed.
    clear_adoption(&layout);
    record_decline(&layout);
    // Same as the empty-store branch: a decline withdraws every extras opt-in.
    layout.clear_all_optins();
    layout.clear_all_retired();
    // Same stub discipline as the empty-store branch: `uninstall --all` removes
    // every pending stub with the toolset it declines.
    crate::stub::remove_all_stubs(&layout);
    crate::reroute::remove_all(&layout);
    remove_exec_roots(&layout);
    if removed.is_empty() {
        // NOTHING OF OURS WAS HERE — a machine whose every row names a system copy or an
        // OS-level install. The decline recorded just above is the act that was asked for
        // and it landed, so this is a success with an honest report, not a failure: the
        // old code reached this exit only when every removal had ERRORED, because a
        // no-op `uninstall` counted as one (`tools/install.sh` reads the exit code and
        // would otherwise tell the user to run the sweep by hand).
        if failures == 0 {
            println!(
                "atpkg: removed nothing (nothing here was installed by aterm) — noted that \
                 this machine declines the ALab toolset: no later pass installs it; aterm \
                 pkg install --default-set opts back in"
            );
            print_left_alone(&left_alone);
            return ExitCode::SUCCESS;
        }
        eprintln!("atpkg: removed nothing");
        return ExitCode::from(1);
    }
    removed.sort();
    println!("atpkg: removed {}", removed.join(", "));
    print_left_alone(&left_alone);
    println!(
        "atpkg: the ALab toolset is no longer managed here. `aterm pkg install --default-set` \
         puts it back; the app itself is untouched."
    );
    if failures == 0 {
        ExitCode::SUCCESS
    } else {
        ExitCode::from(1)
    }
}

/// `uninstall --all`'s exec-root half, on both of its branches: remove `<prefix>/compat`
/// whole ([`crate::compat::remove_all`]). The per-program uninstall already took each
/// program's roots with its tree; this takes what an empty store can still carry — a root a
/// failed removal or an older client left — because a declined machine keeps no clone of
/// builds it no longer has. A failure is one stderr line: the removed
/// programs' shims are already gone, and `aterm pkg gc` sweeps any trust root left in it
/// whose build is gone.
fn remove_exec_roots(layout: &crate::store::Layout) {
    if let Err(e) = crate::compat::remove_all(layout) {
        eprintln!(
            "atpkg: warn — {} was not removed ({e}); `aterm pkg gc` sweeps any trust exec \
             root left in it",
            crate::compat::compat_dir(layout).display()
        );
    }
}

/// Name the programs a GC pass ABSTAINED on, or print nothing when it abstained on none.
///
/// Abstention must not read as "there was nothing to do". A program whose two views disagree
/// is skipped and its superseded builds stay on disk *forever* — a pass that skipped every
/// program otherwise reports the reassuring "nothing to reclaim", with no hint that `doctor`
/// knows exactly why. Naming them is the difference between a silent unbounded-growth bug and
/// a diagnosable one, which is why it is a shared helper rather than a line inside `cmd_gc`:
/// every verb that runs a GC pass owes the same disclosure.
fn print_gc_abstentions(verb: &str, report: &crate::gc::GcReport) {
    if report.diverged.is_empty() {
        return;
    }
    let names: Vec<&str> = report.diverged.iter().map(|d| d.program.as_str()).collect();
    println!(
        "atpkg {verb}: skipped {} program(s) with no proven live build ({}) — \
         run `aterm pkg doctor` for the reason",
        names.len(),
        names.join(", ")
    );
}

/// `atpkg gc` — reclaim superseded store builds (per program: keep the live build plus one
/// rollback target, discard the rest), and sweep what an interrupted install left behind. No
/// network.
///
/// The three outcomes are printed on separate lines because they are different claims about
/// where the disk went: a RECLAIM retired a build that really was installed; a SWEEP removed a
/// tree (or its stage scratch) that never finished installing and that, until it was swept, no
/// verb in the manager could even name. Folding the sweep into "reclaimed" would report a leak
/// as routine housekeeping — and the leak being unreportable is the whole reason it grew.
fn cmd_gc() -> ExitCode {
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let report = crate::gc::run(&layout);
    if report.reclaimed.is_empty()
        && report.swept_partial.is_empty()
        && report.swept_scratch.is_empty()
        && report.swept_exec_roots.is_empty()
    {
        println!("atpkg gc: nothing to reclaim");
    } else {
        for (p, builds) in &report.reclaimed {
            println!(
                "atpkg gc: reclaimed {p} build(s) {}",
                builds
                    .iter()
                    .map(u64::to_string)
                    .collect::<Vec<_>>()
                    .join(", ")
            );
        }
    }
    print_gc_sweeps("gc", &report);
    print_gc_abstentions("gc", &report);
    ExitCode::SUCCESS
}

/// Disclose what a GC pass SWEPT — the trees and stage scratch an interrupted install left
/// behind.
///
/// A shared helper for the same reason [`print_gc_abstentions`] is one: `gc::run` is not
/// only reached through `atpkg gc`. It also runs after an install, after an update, and
/// after a grouped update, and those three passes delete exactly as much as the explicit
/// verb does. A sweep that prints only under `atpkg gc` means the common case — the pass
/// that happens automatically — removes gigabytes and says nothing, which is the same
/// silence that let the leak grow in the first place.
fn print_gc_sweeps(verb: &str, report: &crate::gc::GcReport) {
    for (p, builds) in &report.swept_partial {
        println!(
            "atpkg {verb}: swept interrupted {p} install(s), build(s) {} — never installed, \
             so until now nothing in the manager could name them",
            builds
                .iter()
                .map(u64::to_string)
                .collect::<Vec<_>>()
                .join(", ")
        );
    }
    for (p, names) in &report.swept_scratch {
        println!("atpkg {verb}: swept {p} stage scratch {}", names.join(", "));
    }
    // Say it, because the whole point is that this space used to vanish silently:
    // a killed download stranded a multi-hundred-MB archive in `staging/` that
    // NOTHING swept, while `gc` reported "nothing to reclaim".
    for (p, names) in &report.swept_staging {
        println!(
            "atpkg {verb}: swept {p} interrupted download(s) {} — a killed transfer left the compressed archive behind",
            names.join(", ")
        );
    }
    // An exec root is a clone of its build's files, outside `store/`: one left behind
    // keeps a reclaimed build's blocks allocated, so where they went back is said too.
    for path in &report.swept_exec_roots {
        println!(
            "atpkg {verb}: swept exec root {} — not a root any build here needs (its build is \
             gone or needs none, or it is debris a killed run left)",
            path.display()
        );
    }
    for e in &report.exec_root_errors {
        eprintln!("atpkg {verb}: warn — {e}");
    }
}

/// `atpkg tree-root <dir>` — print the SHA-256 tree_root of an extracted directory, the
/// value the publish pipeline (§4.2/§12) writes into a per-build manifest's `tree_root`.
/// A producer helper exposing the same hashing the client re-verifies with (§8).
fn cmd_tree_root(dir: Option<&String>) -> ExitCode {
    let Some(dir) = dir else {
        eprintln!("usage: atpkg tree-root <dir>");
        return ExitCode::from(2);
    };
    match crate::tree_root(std::path::Path::new(dir)) {
        Ok(root) => {
            println!("{root}");
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg: tree-root {dir} failed: {e}");
            ExitCode::from(1)
        }
    }
}

/// The target triple of this build, for artifact selection (§4.2).
fn current_triple() -> &'static str {
    #[cfg(all(target_arch = "aarch64", target_os = "macos"))]
    {
        "aarch64-apple-darwin"
    }
    #[cfg(all(target_arch = "x86_64", target_os = "macos"))]
    {
        "x86_64-apple-darwin"
    }
    #[cfg(all(target_arch = "x86_64", target_os = "linux"))]
    {
        "x86_64-unknown-linux-gnu"
    }
    #[cfg(all(target_arch = "aarch64", target_os = "linux"))]
    {
        "aarch64-unknown-linux-gnu"
    }
    // Windows arms: without these a Windows build reports "unknown" and can
    // never select ANY artifact — the one-binary Windows packaging lane
    // (apps/aterm-win) ships a client that clean-skips every install.
    #[cfg(all(target_arch = "x86_64", target_os = "windows"))]
    {
        "x86_64-pc-windows-msvc"
    }
    #[cfg(all(target_arch = "aarch64", target_os = "windows"))]
    {
        "aarch64-pc-windows-msvc"
    }
    #[cfg(not(any(
        all(target_arch = "aarch64", target_os = "macos"),
        all(target_arch = "x86_64", target_os = "macos"),
        all(target_arch = "x86_64", target_os = "linux"),
        all(target_arch = "aarch64", target_os = "linux"),
        all(target_arch = "x86_64", target_os = "windows"),
        all(target_arch = "aarch64", target_os = "windows"),
    )))]
    {
        "unknown"
    }
}

/// RFC3339 UTC timestamp for the status record. Pure Rust — NO `/bin/date` shell-out, which
/// could never spawn on Windows (no such binary; the absolute path also bypasses PATHEXT), so
/// `updated_at` was left permanently empty there, silently disabling doctor's index-freshness /
/// "publishing looks frozen" warning. Formats the current epoch second via the shared
/// `aterm_types::rfc3339` civil-calendar math, the exact inverse of `flow::rfc3339_to_unix`,
/// so the two round-trip. Empty string on a pre-epoch (or exactly-epoch) clock.
fn now_rfc3339() -> String {
    let secs = now_unix();
    // Both ends are "the clock is unusable", and both must yield an EMPTY stamp rather than
    // a formatted lie. `now_unix` returns `i64::MAX` when the clock cannot be read at all —
    // the fail-CLOSED sentinel the freshness gates need — and formatting that would stamp
    // `status.toml` with a year in the hundreds of billions, which doctor's
    // "publishing looks frozen" check would then read as a perfectly fresh record.
    if secs <= 0 || secs == i64::MAX {
        return String::new();
    }
    // secs > 0, so the cast is lossless.
    aterm_types::rfc3339::format_rfc3339(secs as u64)
}

/// A caller's ALREADY-VERIFIED index, offered to a second lane of the SAME pass — but
/// only while its signed freshness window is still open at `now` (§8 gate 2).
///
/// `atpkg update` runs several lanes over one store: the channel apply, the attestation
/// repair below, the set completion, the empty-store diagnosis. Each resolved the signed
/// index for itself, and one resolve re-reads and TOML-parses `index-cache.toml`, re-runs
/// the master-signature verify + roster admit + machine-signature verify over every
/// candidate, and re-observes the roster generation — two to four times per six-hourly
/// tick, for an answer that cannot differ inside one pass. Threading the pass's first
/// index through the rest is the shape [`crate::flow::bootstrap_group`] and
/// [`crate::flow::apply_channel_with`] already have.
///
/// What it must never become is a way around a gate. Everything a resolve proves — the
/// anti-rollback floor, the roster admission, the machine signature over the index — is a
/// property of BYTES this pass has already checked, so it still holds. Freshness is the
/// one property that is a function of the CLOCK rather than the bytes, so it is re-checked
/// here: an index whose window lapsed during a long pass is refused re-use, that lane
/// resolves for itself, and it meets the identical `Stale` refusal it always did.
fn reusable_index(index: Option<&crate::TrustedIndex>, now: i64) -> Option<&crate::TrustedIndex> {
    index.filter(|index| crate::flow::index_is_fresh(index, now))
}

/// Re-record the signed `tree_root` for any program that is LIVE but unattested.
///
/// See the call site for the window that produces one. Silent and best-effort in
/// both directions: a program whose root cannot be re-derived is left exactly as it
/// was — fail-closed — and nothing here can fail an update.
///
/// `trusted` is the caller's own verified index when its pass already resolved one (the
/// update lane's), re-used through [`reusable_index`]; `None` (the rollback verb, which
/// holds no index) resolves one here.
fn recover_missing_roots(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    trusted: Option<&crate::TrustedIndex>,
) {
    let live = crate::active_builds(layout);
    if live.is_empty() {
        return;
    }
    let recorded = crate::status::read(layout)
        .map(|s| s.programs)
        .unwrap_or_default();
    let unattested: Vec<(String, u64)> = live
        .iter()
        .filter(|(program, _)| {
            recorded
                .get(program.as_str())
                .is_none_or(|row| row.tree_root.is_empty())
        })
        .map(|(program, build)| (program.clone(), *build))
        .collect();
    // Cheap pre-filter for the SECOND job below: is there any row that could turn out to
    // be stale? Deciding this before the index resolve keeps the common case — a healthy
    // store with nothing to do — exactly as free as it was.
    let maybe_stale = recorded
        .keys()
        .any(|k| !(k.starts_with('*') && k.ends_with('*')) && !live.contains_key(k.as_str()));
    if unattested.is_empty() && !maybe_stale {
        return;
    }
    // The pass's own index when it has one and its window is still open, else one
    // resolved here — never a second verify-select over the same cache in one pass.
    let resolved;
    let index = match reusable_index(trusted, now_unix()) {
        Some(index) => index,
        None => {
            let Ok(index) = crate::resolve_verified_index(
                fetcher,
                layout,
                &effective_anchor(layout),
                build_floor(layout),
                now_unix(),
            ) else {
                return;
            };
            resolved = index;
            &resolved
        }
    };
    // RECONCILE THE MEMO AGAINST ITS AUTHORITY. A row naming something the verified index
    // does not name and the store does not carry is a stray: a mistyped `atpkg install`
    // used to mint one permanently, and doctor then reported a healthy toolset as
    // incomplete for the life of the machine. Announced rather than silent — a manager
    // that quietly edits its own record is the thing this crate exists not to be.
    for stale in prunable_status_rows(&recorded, &live, &|n| index.is_program(n)) {
        println!("atpkg: cleared a stale record for {stale:?} — the signed index does not name it");
        clear_status_row(layout, &stale);
    }
    for (program, build) in unattested {
        let Some(root) = crate::flow::signed_root_for_installed(
            fetcher,
            index,
            &program,
            build,
            current_triple(),
        ) else {
            continue;
        };
        println!("atpkg: {program} was installed without its signed attestation — recorded");
        record_status(
            layout,
            &program,
            crate::ProgramStatus {
                installed_build: Some(build),
                state: crate::state::managed(build, index.index_build),
                tree_root: root,
            },
            format!("recovered the signed attestation for {program}"),
        );
    }
}

/// The durable `state` to record for a failed install — or `None` when the failure is a
/// fact about the REQUEST rather than about this machine's store.
///
/// [`crate::FlowError::NotReachable`] means the signed index resolved, verified, and does
/// not name the token. Nothing was ever installed under that name and nothing ever will
/// be, so there is nothing for `status.toml` to remember: writing a row there INVENTS a
/// program, and every later `atpkg doctor` reads it back as a missing member of the
/// toolset. The attempt still reaches the aggregate outcome sentence and `cmd_install`
/// still prints it and still exits 1 — the failure is not swallowed, it is just not
/// promoted to a permanent resident of the store's record.
///
/// The OFFLINE FAMILY is suppressed for the same reason, and it is the larger leak of the
/// two. `NoIndex`, `Unreachable` and `Stale` are all raised inside `resolve_verified_index`
/// (flow.rs:1327/1330/1430) — strictly BEFORE the program lookup at flow.rs:384 — so the
/// run never reached a question about this program and learned nothing about it. Recording
/// "error: no signature-valid index" against `ay` says the program is broken; what actually
/// happened is that a laptop was on a plane.
///
/// The damage was durable and asymmetric. A default-set member heals on the next update
/// (§11 reinstalls it), but a program the user asked for BY NAME while offline is not
/// swept by anything: it is not live, so `update` skips it, and it IS named by the index,
/// so the stale-row reconciler correctly declines to prune it. Its row sat there reading
/// `error:` forever, on a machine that had been online and healthy for months.
///
/// Nothing is lost by suppressing it. The reason still reaches the aggregate outcome
/// sentence (which Settings ▸ Packages prints and `doctor` now falls back to), `cmd_install`
/// still prints it live and still exits 1, and the environmental condition itself is
/// doctor's own business — it independently reports index age and days-since-update, which
/// is where "this machine cannot reach the channel" belongs. What is removed is only the
/// false claim that a specific program is at fault.
fn failed_install_state(e: &crate::FlowError) -> Option<String> {
    if let Some(state) = canonical_non_error_state(e) {
        return Some(state);
    }
    match e {
        // A dev-linked program is a benign HARD-SKIP (§13), not an error state.
        crate::FlowError::Linked(_) => Some(String::from("dev-linked (skipped)")),
        // Verdicts about the REQUEST or the NETWORK — never about this program.
        crate::FlowError::NotReachable(..)
        | crate::FlowError::NoIndex
        | crate::FlowError::Unreachable(_)
        | crate::FlowError::Stale => None,
        _ => Some(format!("error: {e}")),
    }
}

/// The CANONICAL, non-error state some install refusals really are (design §2): a
/// target the pinned build carries no row for is `unavailable on <target>: <hint>`.
/// `None` for every genuine fault. (Every protocol the schema admits now has its lane;
/// a `system-pm` row whose manager is absent answers through the flow's
/// [`crate::ProtocolOutcome::Unavailable`], not through an error.)
///
/// The hint here is the generic one; the pass's missing-triple prescan, which holds the
/// verified index, records the program's own `unavailable_hint` ahead of this.
fn canonical_non_error_state(e: &crate::FlowError) -> Option<String> {
    match e {
        crate::FlowError::NoArtifact(t) => Some(crate::state::unavailable(t, "")),
        _ => None,
    }
}

/// The recorded rows naming nothing the signed index knows — the ones safe to drop.
///
/// `status.toml` is a DERIVED memo and the signed index is the authority on what programs
/// exist, so reconciling the memo against its authority at the moment that authority
/// speaks is the memo doing its job, not a destructive edit. Four conditions, all
/// required:
///
/// * the caller holds a VERIFIED index — never prune on a network error, because
///   inventing an absence is the same class of mistake as inventing a fault;
/// * the key is not a `*sentinel*` bookkeeping row (`*index*`, `*toolset*`, `*seed*` have
///   their own owners and their own reapers);
/// * the index does not name it;
/// * it is not LIVE. A program dropped upstream while still installed keeps its row:
///   [`crate::verify`] fails closed on an empty recorded root, so deleting a live
///   program's row would erase the attestation `atpkg verify` needs.
fn prunable_status_rows(
    recorded: &std::collections::BTreeMap<String, crate::ProgramStatus>,
    live: &std::collections::BTreeMap<String, u64>,
    known: &dyn Fn(&str) -> bool,
) -> Vec<String> {
    recorded
        .keys()
        .filter(|k| !(k.starts_with('*') && k.ends_with('*')))
        .filter(|k| !live.contains_key(k.as_str()))
        .filter(|k| !known(k))
        .cloned()
        .collect()
}

/// The seed for a writer that REBUILDS `status.toml` — `None` means the record on disk
/// EXISTS but could not be read, i.e. do not write.
///
/// [`crate::status::read`] answers an unreadable record (a symlinked path, a hand edit
/// that broke the TOML, non-UTF-8 bytes, an oversize file, a permission denial) with the
/// same `None` an absent one yields, so the rewriting writers below used to answer it by
/// REPLACING the file with their single row: every other program's state and signed
/// `tree_root`, the `seams` list `uninstall --all` walks to detach the rustup toolchain,
/// and `last_success_at` all went, silently, from one diagnostic write inside a failed
/// pass ([`crate::status::seed_for_rewrite`]). The line is printed at most once per
/// process because a pass calls these writers once per program.
fn status_seed_for_rewrite(layout: &crate::store::Layout) -> Option<crate::Status> {
    match crate::status::seed_for_rewrite(layout) {
        Ok(seed) => Some(seed),
        Err(e) => {
            use std::sync::atomic::{AtomicBool, Ordering};
            static SAID: AtomicBool = AtomicBool::new(false);
            if !SAID.swap(true, Ordering::Relaxed) {
                eprintln!(
                    "atpkg: {} is unreadable ({e}) — NOT rewriting it, so the recorded \
                     rows, seams and attestations survive; move the file aside to let \
                     the next pass write a fresh record",
                    layout.status().display()
                );
            }
            None
        }
    }
}

/// Replace the aggregate `status.outcome` sentence without touching per-program rows.
///
/// The counterpart to [`clear_status_row`] for the line Settings ▸ Packages actually
/// shows: retiring a failure row while leaving its sentence behind just makes the
/// staleness quieter (2026-08-20 round-8 audit).
fn record_outcome(layout: &crate::store::Layout, outcome: String) {
    // CREATE the record if it does not exist yet, rather than returning silently. This
    // used to early-return on a virgin store, which was harmless while every failure also
    // wrote a per-program row — but the environmental failures now route HERE and nowhere
    // else, so a first-run offline `atpkg install` left no witness of any kind. That is
    // precisely the failed-seed machine `tools/install.sh` hands `pkg doctor` to, and it
    // made doctor's own fallback to this sentence dead code in the case that motivated it.
    // Fields are stamped exactly as `record_status` stamps them: `enabled` and
    // `index_source` are CURRENT facts, so recomputing beats carrying a stale pair.
    let Some(existing) = status_seed_for_rewrite(layout) else {
        return;
    };
    let _ = crate::status::write(
        layout,
        &crate::Status {
            schema: 1,
            enabled: manager_enabled(),
            index_source: crate::resolve_account(crate::config::cached().account()).slug(),
            outcome,
            // The TIMESTAMP moves with the sentence. Refreshing one and not the other
            // paired a fresh verdict with the moment of the last failure, which is
            // what Settings and `atpkg doctor` show side by side
            // (2026-08-20 round-9 audit).
            updated_at: now_rfc3339(),
            ..existing
        },
    );
}

/// Remove one bookkeeping row from `status.toml` — the counterpart to
/// [`record_status`] for a failure a later pass has disproved.
///
/// Rows like `*index*` and `*toolset*` are WRITE-ONLY diagnostics: nothing ever
/// deleted one, so a transient failure was indistinguishable from a standing one
/// for the life of the machine. Best-effort and silent, exactly like the writer:
/// observability must never fail an update.
fn clear_status_row(layout: &crate::store::Layout, program: &str) {
    let Some(existing) = crate::status::read(layout) else {
        return;
    };
    let mut programs = existing.programs;
    if programs.remove(program).is_none() {
        return;
    }
    let _ = crate::status::write(
        layout,
        &crate::Status {
            programs,
            ..existing
        },
    );
}

/// The signed `tree_root` to persist for `program`: the freshly-applied one when
/// non-empty, otherwise the ALREADY-RECORDED root. An `already_current` install, or an
/// untouched coherence-group sibling, reports an empty tree_root (nothing was flipped) —
/// persisting that empty value would erase a perfectly good attestation and break
/// `atpkg verify` for an untampered program. So an empty new root means "keep what we
/// had", never "forget it".
fn effective_tree_root(layout: &crate::store::Layout, program: &str, new_root: &str) -> String {
    if !new_root.is_empty() {
        return new_root.to_string();
    }
    crate::status::read(layout)
        .and_then(|s| s.programs.get(program).map(|p| p.tree_root.clone()))
        .unwrap_or_default()
}

/// The row to record when an install FAILS for a program that may already be live.
///
/// A FAILED INSTALL UNINSTALLS NOTHING. An upgrade that dies at download, stage, activate
/// or manifest-fetch leaves the previous build exactly where it was — still activated,
/// still shimmed, still working — and every failure arm nonetheless recorded
/// `installed_build: None, tree_root: ""`, which says the program is gone AND unattested.
/// Both false, and the second is destructive rather than merely wrong: `crate::verify`
/// fails closed on an empty recorded root, so one failed upgrade turned `atpkg verify` into
/// a permanent "cannot verify" for a program whose bytes were never touched, recoverable
/// only by reinstalling gigabytes to re-record a root that had been correct all along.
///
/// The live build comes from `active_builds` — the shims, i.e. ground truth — rather than
/// from the record being overwritten, so a machine whose record disagrees with its store
/// gets the store's answer.
///
/// THE ATTESTATION BELONGS TO THE BUILD IT WAS CAPTURED FOR, and that is why the two fields
/// cannot simply be read from two different places. Taking the build from the store and the
/// root from the record with no agreement check stitches together a row saying "live build
/// 20, attested by a root signed for build 18" — and `crate::verify` is ordered
/// `NotInstalled → NoSignedRoot → BuildMismatch → compare`, so writing the live build into
/// `installed_build` DISARMS the `BuildMismatch` guard that exists to catch exactly this.
/// Execution then reaches the tree comparison, where differing bytes report `Drift` — a
/// tampering accusation against bytes nobody touched — and identical bytes report `Match`,
/// affirming a build no signature ever covered. The second is a fail-OPEN on authenticity,
/// which is the one direction this crate may never fail.
///
/// So the root is kept only when it still means something: when record and store name the
/// SAME build, or when the store names none at all. That second case is not an edge — it is
/// a TOMBSTONED program, whose shims were replaced by failing stubs while its bytes and
/// their attestation sit untouched in the store. Dropping the root there would erase a good
/// attestation on the strength of a yanked pin.
fn failure_row(
    layout: &crate::store::Layout,
    program: &str,
    state: String,
) -> crate::ProgramStatus {
    let live = crate::active_builds(layout).get(program).copied();
    let recorded = crate::status::read(layout).and_then(|s| s.programs.get(program).cloned());
    crate::ProgramStatus {
        installed_build: live,
        state,
        tree_root: match &recorded {
            Some(r) if live.is_none() || r.installed_build == live => r.tree_root.clone(),
            _ => String::new(),
        },
    }
}

/// Record the install outcome to `status.toml` (the silent manager's observability
/// surface, §5/§9). Best-effort — diagnostics, never load-bearing.
fn record_status(
    layout: &crate::store::Layout,
    program: &str,
    state: crate::ProgramStatus,
    outcome: String,
) {
    // Seed the per-program map (and the seam record) from the EXISTING record so
    // updating ONE program does not clobber every OTHER program's last-known state.
    // The silent update loop calls this once per program, and status.toml is the
    // per-program observability surface (§5/§9) — a fresh single-entry map would
    // erase the rest each pass, and dropping `seams` would silently disown the
    // rustup seam ([`crate::seam`]) on every install.
    // `last_success_at` is carried, never stamped: this writer runs on failure rows
    // too, and the field means "a pass COMPLETED" ([`record_success`]).
    // The freshness fields ride the same way ([`crate::status::stamp_index_freshness`]
    // is their one writer). A record that EXISTS but cannot be read seeds nothing: this
    // writer would replace it wholesale, which is how a single unreadable file used to
    // cost the machine every other row, its seams and its `last_success_at`.
    let Some(existing) = status_seed_for_rewrite(layout) else {
        return;
    };
    let mut programs = existing.programs;
    programs.insert(program.to_string(), state);
    let status = crate::Status {
        schema: 1,
        updated_at: now_rfc3339(),
        enabled: manager_enabled(),
        index_source: crate::resolve_account(crate::config::cached().account()).slug(),
        outcome,
        seams: existing.seams,
        last_success_at: existing.last_success_at,
        last_index_reached_at: existing.last_index_reached_at,
        last_index_build: existing.last_index_build,
        index_build_changed_at: existing.index_build_changed_at,
        programs,
    };
    let _ = crate::status::write(layout, &status);
}

/// The ONE writer of `last_success_at` ([`crate::status::stamp_success`]): a pass that
/// resolved the signed index and ran to its end. Called at `update`'s clean exit, at its
/// failed-pass exit when a lane of the pass resolved the index (a member that failed is
/// recorded in its own row), and at the zero-failure exits of `install --default-set` —
/// never from a per-program writer, an unresolved index, the seed offer, or a shadow
/// reconcile, all of which move `updated_at` and none of which is a completed check.
fn record_success(layout: &crate::store::Layout) {
    let _ = crate::status::stamp_success(layout, &now_rfc3339());
}

/// Stamp what this pass's index resolve MEASURED about the network
/// ([`crate::flow::last_resolve`]) into the freshness fields `doctor` reads, and — when
/// the listing was NOT reached — say so in the aggregate outcome, so a pass served from
/// the §14 cache never records a bare green "up to date" (audit 2026-09-14).
/// `index_build` is the build the pass resolved, when it has one in hand.
fn record_index_freshness(layout: &crate::store::Layout, index_build: Option<u64>) {
    let Some(resolve) = crate::flow::last_resolve() else {
        return;
    };
    let _ =
        crate::status::stamp_index_freshness(layout, &now_rfc3339(), resolve.reached, index_build);
    if !resolve.reached {
        let why = resolve
            .why
            .as_deref()
            .map(clip_cause)
            .unwrap_or_else(|| String::from("the listing was not reached"));
        let Some(mut status) = status_seed_for_rewrite(layout) else {
            return;
        };
        if !status.outcome.contains("index from cache") {
            status.outcome = format!("{} (index from cache — {why})", status.outcome);
            let _ = crate::status::write(layout, &status);
        }
    }
}

/// R3: print [`crate::status::NEVER_CHECKED_LINE`] on STDERR when no update pass has
/// ever completed on this machine ([`crate::status::never_checked`]) — from every
/// read-only verb and the pending stub, so a console learns that packages cannot be
/// updated yet without anyone opening a window. Returns whether it printed, for the
/// tests; stdout is untouched, so scripts parsing a verb's answer see no change.
fn warn_if_never_checked(layout: &crate::store::Layout) -> bool {
    if !crate::status::never_checked(layout) {
        return false;
    }
    eprintln!("{}", crate::status::NEVER_CHECKED_LINE);
    true
}

/// Pure precedence core of [`resolve_pkg_token`]: a non-empty `ATPKG_TOKEN` env
/// value wins outright (the dedicated, atpkg-specific override — unchanged
/// behavior); otherwise the shared `aterm-update-core` chain (`chain`, invoked
/// LAZILY so no keychain/`gh` subprocess runs when the env var suffices)
/// supplies both the token and its source label. No token at all ⇒ the empty
/// anonymous token (fine for public repos, rate-limited). The label names the
/// SOURCE only — never the token itself.
fn pick_pkg_token(
    atpkg_env: Option<String>,
    chain: impl FnOnce() -> Option<(String, String)>,
) -> (String, Option<String>) {
    if let Some(t) = atpkg_env.filter(|t| !t.is_empty()) {
        return (t, Some("$ATPKG_TOKEN".to_string()));
    }
    match chain() {
        Some((t, src)) => (t, Some(src)),
        None => (String::new(), None),
    }
}

/// Whether a fetch destination engages the FULL credential chain in
/// `aterm-update-core`'s token walk. Spelled with the SAME constants as
/// `token.rs::needs_ambient_credential` (the crate-private gate inside
/// `resolve_with_source`) so the pick below can never disagree with what the
/// walk will actually do: only the compiled-in public channel slug is anonymous
/// by construction; anything else may be private and runs every rung.
fn engages_credential_chain(owner: &str, repo: &str) -> bool {
    owner != aterm_update_core::DEFAULT_OWNER || repo != aterm_update_core::DEFAULT_REPO
}

/// The ONE `owner/repo` the credential chain is keyed to, chosen over EVERY
/// destination this process's fetcher will actually reach — the resolved index
/// slug plus every `[packages.links]` fetch-override slug — not just the index.
///
/// `GithubFetcher` presents a single token to all of its destinations
/// (`net.rs`: an override is "a possibly-private repo, reached with the same
/// token"), so keying the chain to the index alone starved the links lane: on
/// the compiled default account the index slug IS the public channel, the
/// chain's gate short-circuits to the `$ATERM_UPDATE_TOKEN` rung only, and a
/// `[packages.links] prog = "someorg/private-repo"` fetch went out anonymous —
/// 404 on a machine whose keychain/0600-file/`gh auth` credential worked the
/// round before (adversarial review 2026-08-11). Picking the FIRST
/// chain-engaging destination is equivalent to resolving per fetch: the walk's
/// rung order does not vary by slug once the gate opens, so one chain-engaging
/// slug yields the same token any of them would.
///
/// Order: the index slug when it engages the chain (a repointed account governs
/// every non-overridden fetch), else the first chain-engaging override slug
/// (BTreeMap order — deterministic), else the index slug so the compiled
/// public default stays anonymous by construction.
fn credential_destination(
    index_owner: &str,
    index_repo: &str,
    overrides: &std::collections::BTreeMap<String, String>,
) -> (String, String) {
    if !engages_credential_chain(index_owner, index_repo) {
        for slug in overrides.values() {
            if let Some((o, r)) = slug.split_once('/')
                && engages_credential_chain(o, r)
            {
                return (o.to_string(), r.to_string());
            }
        }
    }
    (index_owner.to_string(), index_repo.to_string())
}

/// Resolve the GitHub token for the network verbs: `$ATPKG_TOKEN` first (the
/// dedicated override), then `aterm-update-core`'s full per-machine chain
/// (`$ATERM_UPDATE_TOKEN` → keychain → 0600 file → `$GITHUB_TOKEN` → `$GH_TOKEN`
/// → `gh auth token`) against the shared support dir (the pkg prefix's parent —
/// the same `…/aterm` dir the app updater resolves against, so ONE provisioned
/// credential serves both). Returns `(token, source_label)`; the label feeds the
/// loud `atpkg doctor` line and NEVER carries the token.
pub(crate) fn resolve_pkg_token(layout: &crate::store::Layout) -> (String, Option<String>) {
    pick_pkg_token(std::env::var("ATPKG_TOKEN").ok(), || {
        let support = layout
            .prefix
            .parent()
            .unwrap_or(&layout.prefix)
            .to_path_buf();
        // The credential chain is keyed to where the fetches actually go — ALL
        // of them, via [`credential_destination`]: the resolved index slug PLUS
        // the `[packages.links]` fetch overrides, because the fetcher presents
        // this one token to every destination. On the compiled default with no
        // overrides the chain's own gate keeps the lookup anonymous by
        // construction (no ambient PAT is gathered for the public channel); a
        // repointed account (`[packages].account`/`ATPKG_ACCOUNT`) OR any
        // links override to a non-default repo consults the full chain, so a
        // private per-program override still reaches the keychain / 0600-file /
        // ambient rungs.
        let cfg = crate::config::cached();
        let index = crate::resolve_account(cfg.account());
        let (owner, repo) =
            credential_destination(&index.owner, &index.repo, &crate::repo_overrides(cfg));
        aterm_update_core::token::resolve_with_source(&support, &owner, &repo)
            .map(|(t, src)| (t, src.to_string()))
    })
}

/// The production GitHub fetcher: the `[packages].account`-aware owner (env
/// `ATPKG_ACCOUNT` still beats config — [`crate::resolve_account`]), the token
/// chain ([`resolve_pkg_token`]), and the `[packages.links]` per-program
/// `owner/repo` fetch overrides ([`crate::repo_overrides`]).
fn github_fetcher(layout: &crate::store::Layout) -> crate::GithubFetcher {
    let cfg = crate::config::cached();
    let owner = crate::resolve_account(cfg.account()).owner;
    // Public program repos need no token; a token is the optional rate-limit / private aid.
    let (token, _source) = resolve_pkg_token(layout);
    crate::GithubFetcher::new(owner, token).with_overrides(crate::repo_overrides(cfg))
}

/// Pick the fetcher for the network verbs: a `dir:<path>` `ATPKG_REGISTRY` selects the
/// offline / publisher-test [`crate::DirFetcher`] (§14); otherwise the production GitHub
/// fetcher. A `dir:` registry's bytes STILL pass the identical verify + floor + freshness
/// gates — the source is not an authenticity input. (Env wins over config: a `dir:`
/// registry serves everything locally, so `[packages.links]` fetch overrides are moot
/// there by construction.)
fn resolve_fetcher(layout: &crate::store::Layout) -> Box<dyn crate::flow::Fetcher> {
    if let Some(spec) = std::env::var_os("ATPKG_REGISTRY") {
        let spec = spec.to_string_lossy();
        if let Some(dir) = spec.strip_prefix("dir:") {
            return Box::new(crate::DirFetcher::new(std::path::PathBuf::from(dir)));
        }
    }
    let github = Box::new(github_fetcher(layout));
    // An app bundle sealing a signed seed registry (§9.1) joins the flow as a
    // FALLBACK leg — but ONLY as a BOOTSTRAP source, never an update source.
    //
    // The seal is a snapshot of the channel at CUT time; a machine that has
    // already trusted a published index holds a durable floor at or above it,
    // and `select_index` admits any signature-valid index `>= floor`. Chaining
    // the seed unconditionally therefore had two teeth (found by adversarial
    // review 2026-07-30, both traced end-to-end):
    //   * DOWNGRADE — the seed and a later publish routinely share an
    //     `index_build` (the refresh script reads the counter that only a
    //     successful UPLOAD bumps) while pinning different builds, so an
    //     unreachable network let the sealed pins re-install OVER newer
    //     installed builds on the GUI's unattended 6h pass (`gate::decide`
    //     force-installs on any `pinned != installed`, including lower).
    //   * CACHE MASKING — a seed-leg success turned a network failure into
    //     `Ok`, so `flow::resolve_candidates`' §14 last-good-index cache
    //     fallback (its `Err` arm) could never fire, and the seed-only set
    //     then OVERWROTE that cache under the chain's source id.
    // Both teeth are closed structurally, which is why the lane could return
    // (2026-08-17, resurrecting the shape deleted in `ba832933`):
    //   * the chain joins ONLY on an EMPTY store — the first run, where there
    //     is no floor to roll back and no installed build for a stale pin to
    //     downgrade; every subsequent pass is network-only, so the seed can
    //     never act as an update source;
    //   * `ChainFetcher` caches under the PRIMARY leg's id and persists only
    //     the primary's own candidates (`cache_source_id`,
    //     `cacheable_candidates` — net.rs), so a seed-leg success can neither
    //     satisfy nor overwrite the last-good NETWORK cache.
    // The seed is the network registry's twin, not a second trust path: the
    // DirFetcher's bytes pass the identical verify + floor + freshness gates.
    let seeded_bootstrap = seed_bootstrap_leg(
        crate::bundled_seed_dir(),
        crate::active_builds(layout).is_empty(),
    )
    .map(|seed| Box::new(crate::DirFetcher::new(seed)) as Box<dyn crate::flow::Fetcher>);
    match seeded_bootstrap {
        Some(seed) => Box::new(crate::ChainFetcher::new(github, seed)),
        None => github,
    }
}

/// THE bootstrap-only rule, as a pure function: the co-located seed joins the
/// fetcher chain **iff** it exists AND the store is empty.
///
/// Split out of [`resolve_fetcher`] because it is the entire safety argument
/// for re-admitting the seed lane (§9.1), and the version of it that lived
/// inline was untestable — it reached for `current_exe` and process env — so
/// the one claim the 2026-07-30 adversarial review turned on had no test at
/// all. Both teeth it closes are restated at the call site; what this function
/// guarantees is the premise they rest on.
///
/// `store_is_empty` is the SHIM view (`active_builds`), not "no build dirs on
/// disk", so a store whose every program has been TOMBSTONED by a yank reads
/// empty here and re-arms the chain. That is safe, but for a reason OUTSIDE
/// this function: the pass that tombstones also advances the durable
/// `index_build` floor to the yanking index, and a yanking index is by
/// construction published above the sealed one — so a seal that would
/// reinstate yanked builds is refused by the floor, not by this predicate.
/// Worth knowing before anyone "tightens" the emptiness test to count
/// directories instead.
fn seed_bootstrap_leg(
    seed: Option<std::path::PathBuf>,
    store_is_empty: bool,
) -> Option<std::path::PathBuf> {
    seed.filter(|_| store_is_empty)
}

/// The trust anchor the network verbs verify under: the committed paper-master keyset
/// ([`crate::PKG_TRUST_ANCHORS`]) plus this store's durable `roster_seq` ratchet, and
/// nothing else.
///
/// There used to be an `ATPKG_ROOTKEY_OVERRIDE` here that swapped WHICH root key anchored
/// verification "without a rebuild". It is gone. However carefully it was scoped, it let
/// ambient process state decide what the package manager trusted — an unpinned build
/// could be handed an anchor by an environment variable. Trusting a mirror or a second
/// owner account is now a committed change to `aterm_update_core::pins`, visible in a
/// diff like any other trust decision.
///
/// The roster floor is read HERE, per call, rather than snapshotted once at process
/// start: a verb that ratchets the floor mid-pass (the default-set bootstrap installs
/// several programs in a row) must not have a later step verify under a stale, lower
/// ratchet than the one it just advanced.
fn effective_anchor(layout: &crate::store::Layout) -> crate::Anchor {
    crate::Anchor::pinned(crate::sig::Floor::new(layout.roster_floor()).current())
}

/// This store's durable `index_build` high-water AND the roster generation that recorded
/// it (§8 gate 3) — one value, because a floor a MACHINE set must not outlive the
/// generation that revoked that machine. See [`crate::sig::BuildFloor`].
fn build_floor(layout: &crate::store::Layout) -> crate::sig::BuildFloor {
    crate::sig::BuildFloor {
        index_build: crate::sig::Floor::new(layout.floor()).current(),
        roster_seq: crate::sig::Floor::new(layout.floor_generation()).current(),
    }
}

/// Advance the durable ratchets after a successful resolve. Best-effort, exactly as the
/// single floor advance always was — a failed write never turns a completed install into
/// an error.
///
/// # Which ratchet turns where, and why they are not the same call
///
/// The `roster_seq` high-water is NOT advanced here as its authoritative write: it turns on
/// OBSERVATION, inside `flow::verify_select_fresh`, the instant a generation is admitted —
/// because a client that merely SAW a revocation must refuse the pre-revocation roster even
/// if it went on to install nothing. Repeating it here is belt-and-braces over an idempotent
/// monotonic write (this seq was admitted in the same pass), not the defence itself.
///
/// The `index_build` floor is the one this function really owns, and it is written
/// GENERATION-AWARE: a strictly newer master-signed generation RE-BASES it (possibly
/// downward) instead of ratcheting it. Without that, one rostered machine publishing
/// `index_build = u64::MAX` would raise a monotonic floor above everything the owner can
/// ever publish — including the index carrying that machine's revocation — and no republish
/// could recover the store. Only the paper master can mint a generation, so only the paper
/// master can pull that lever.
fn advance_floors(layout: &crate::store::Layout, index_build: u64, roster_seq: u64) {
    let generation = crate::sig::Floor::new(layout.floor_generation());
    let build = crate::sig::Floor::new(layout.floor());
    if roster_seq > generation.current() {
        build.rebase(index_build);
    } else {
        let _ = build.check_and_record(index_build);
    }
    let _ = generation.check_and_record(roster_seq);
    crate::flow::observe_roster_generation(layout, roster_seq);
}

/// Whether the manager may act on the network verbs: a committed root anchor to
/// verify under AND the user has not opted out via `ATPKG_DISABLE`.
fn manager_enabled() -> bool {
    crate::manager_enabled()
}

/// Whether this machine has ADOPTED the ALab toolset — see [`crate::store::Layout::adopted`]
/// for what that means and why it is not the same question as `[packages].auto_install`.
fn adopted(layout: &crate::store::Layout) -> bool {
    layout.adopted().is_file()
}

/// Record adoption after a deliberate whole-set install. Idempotent, and best-effort in the
/// same sense the floor writes are: a machine that adopted but could not persist the marker
/// simply asks the consent question again next pass, which is the safe direction.
fn record_adoption(layout: &crate::store::Layout) {
    let path = layout.adopted();
    if path.is_file() {
        return;
    }
    // The payload is documentation for whoever finds the file, never something read back:
    // adoption is the file's EXISTENCE, so a truncated or garbled write cannot be
    // misinterpreted as a different answer.
    match crate::platform::open_create_write(&path, 0o600) {
        Ok(mut f) => {
            use std::io::Write as _;
            let _ = writeln!(
                f,
                "# This machine runs the ALab toolset as a SET.\n\
                 # atpkg therefore keeps that set COMPLETE — a program published to the\n\
                 # signed index later is installed on a later pass, not silently skipped.\n\
                 # Written by the batteries-included seed bootstrap or `install --default-set`.\n\
                 # Removed by `uninstall`. To drop one program while staying adopted, use\n\
                 # [packages].exclude in aterm.toml."
            );
        }
        Err(e) => eprintln!(
            "atpkg: could not record toolset adoption at {}: {e} — the set will not be \
             auto-completed until a later pass records it",
            path.display()
        ),
    }
}

/// Whether an explicit `install --default-set` pass RECORDS adoption: it installed at
/// least one member (`activated > 0`), or it found the set already complete — there were
/// builds before it ran, it failed nothing, skipped nothing and owes nothing. The
/// already-complete arm was missing until 2026-09-14, and the gap was a trap the manager
/// set for itself: `uninstall` CLEARS adoption and tells the user to re-adopt with this
/// very verb, and `aterm pkg doctor`'s own re-seed remedy for a tagged bundle is
/// `uninstall <program> && install <program>` — after which the set is complete again,
/// this verb printed "default set already complete — nothing to install", recorded
/// nothing, and unattended set-completion stayed off for good: the next program
/// published to the index never arrived (observed live on the owner's machine after
/// re-seeding five bundles, 2026-09-14). Pure for the table test.
fn default_set_adopts(
    activated: usize,
    failures: u32,
    had_builds_before: bool,
    skipped: &[String],
    remaining_missing: &[String],
) -> bool {
    activated > 0
        || (failures == 0
            && had_builds_before
            && skipped.is_empty()
            && remaining_missing.is_empty())
}

/// Forget adoption. Called by `uninstall`: removing a managed program is an explicit act,
/// and set-completion must never undo it on the next unattended pass. The record of who
/// adopted goes with it ([`clear_seed_adoption`]), so no record outlives the marker it
/// describes.
fn clear_adoption(layout: &crate::store::Layout) {
    let path = layout.adopted();
    if path.is_file() {
        let _ = std::fs::remove_file(&path);
    }
    clear_seed_adoption(layout);
}

/// Whether the seed pass adopted this machine ([`crate::store::Layout::adopted_by_seed`]):
/// `cmd_seed` created the [`adopted`] marker itself, so installing aterm is the consent behind
/// the set, and `[packages].seed_install = false`, read before it adopts, is the switch that
/// would have stopped it. Both files must exist: a record without the marker vouches for
/// nothing, and a marker without the record claims no more than the uninstall
/// ([`set_completion_consent`]).
fn adopted_by_seed(layout: &crate::store::Layout) -> bool {
    adopted(layout) && layout.adopted_by_seed().is_file()
}

/// `cmd_seed`'s adoption: [`record_adoption`] plus the record of who adopted, written only
/// when this call creates the marker. The seed pass runs on every launch, so if it stamped a
/// marker it found, a machine the explicit verb adopted would become the first launch's on its
/// very next launch and its update passes would again say "installing aterm is the consent".
/// Best-effort like the marker: a missing record claims less, never more.
fn record_seed_adoption(layout: &crate::store::Layout) {
    if adopted(layout) {
        return;
    }
    record_adoption(layout);
    if !adopted(layout) {
        return;
    }
    let path = layout.adopted_by_seed();
    match crate::platform::open_create_write(&path, 0o600) {
        Ok(mut f) => {
            use std::io::Write as _;
            let _ = writeln!(
                f,
                "# `atpkg seed`, the first launch's bootstrap, adopted this machine.\n\
                 # Its EXISTENCE beside `adopted` is the whole fact: an update pass that\n\
                 # completes the set says installing aterm is the consent, and names\n\
                 # [packages].seed_install = false as the switch that would have stopped it.\n\
                 # Removed with `adopted`, and by `install --default-set`, which is its own\n\
                 # consent."
            );
        }
        Err(e) => eprintln!(
            "atpkg: could not record that the seed pass adopted the toolset at {}: {e} — \
             update passes will name only `aterm pkg uninstall --all` as the way out",
            path.display()
        ),
    }
}

/// Forget that the seed pass adopted. This is the explicit toolset verb's call: running it
/// is the consent, and it never reads `seed_install`. From then on neither "installing
/// aterm is the consent" nor that key holds for the passes that keep the set complete.
fn clear_seed_adoption(layout: &crate::store::Layout) {
    let path = layout.adopted_by_seed();
    if path.is_file() {
        let _ = std::fs::remove_file(&path);
    }
}

/// The programs this user removed individually ([`crate::store::Layout::removed`]).
fn removed_programs(layout: &crate::store::Layout) -> std::collections::BTreeSet<String> {
    // ONE reader, on the layout: the unattended update lanes need the same answer,
    // and a second copy here is how they came to disagree.
    layout.removed_programs()
}

/// The programs THIS machine keeps complete: the signed DEFAULT SET narrowed by the
/// user's `[packages].include`/`exclude`, plus every EXTRA ([`crate::manifest::Program::extra`])
/// this machine opted in to by name ([`crate::store::Layout::optins`]). ONE reader for the
/// three lanes that plan a set (the network pass, the seed prescan, the seed offer), so
/// an opt-in recorded at the explicit door is honoured by every unattended pass after it.
pub(crate) fn wanted_programs(
    layout: &crate::store::Layout,
    index: &crate::manifest::Index,
    cfg: &crate::config::PackagesConfig,
) -> std::collections::BTreeSet<String> {
    index.installable_with_optins(cfg.include(), cfg.exclude(), &layout.optins())
}

/// Record an opt-in for `program` iff the VERIFIED index lists it as an extra. `true` iff
/// a marker now exists. A default-set member needs no marker (asking for it by name is
/// already `install <program>`'s job) and an unlisted name gets none (nothing outside the
/// signed set can be opted in to). Best-effort like every marker write: a failure is
/// printed, and the install still proceeds — the user asked for it NOW; only the "keep it
/// on later passes" promise is what the marker carries.
fn note_extra_optin(
    layout: &crate::store::Layout,
    index: &crate::manifest::Index,
    program: &str,
) -> bool {
    if !index.is_extra(program) {
        return false;
    }
    match layout.record_optin(program) {
        Ok(()) => true,
        Err(e) => {
            eprintln!(
                "atpkg: could not record the opt-in for {program} at {}: {e} — installing it \
                 now, but later passes will not keep it current until the marker exists",
                layout.optin_marker(program).display()
            );
            false
        }
    }
}

/// Reconcile SYSTEM SATISFACTION over `wanted` against the given `PATH`. Returns the
/// members satisfied this pass (each also recorded + announced by [`satisfy_by_system`],
/// and dropped from `installed` when a managed copy was retired). The caller removes the
/// returned names from its wanted set.
///
/// The `PATH` is INJECTED, never read here — the env-reading wrapper this used to
/// carry is gone. A pass now reads `PATH` exactly once, at its own entry
/// ([`install_default_set`]), and threads the one value to both consumers, so the
/// answer to "what was on `PATH` during this pass" cannot differ between the
/// system-satisfaction question and the shadow question.
fn reconcile_system_satisfied_with(
    layout: &crate::store::Layout,
    index: &crate::manifest::Index,
    wanted: &std::collections::BTreeSet<String>,
    installed: &mut std::collections::BTreeMap<String, u64>,
    path_var: Option<&std::ffi::OsStr>,
) -> Vec<String> {
    let mut satisfied = Vec::new();
    for name in wanted {
        let Some(program) = index.program(name) else {
            continue;
        };
        let Some(bin) = program.system.as_deref() else {
            continue;
        };
        let Some(path) = crate::vendor::system_binary_on_path(&layout.prefix, bin, path_var) else {
            continue;
        };
        satisfy_by_system(layout, name, &path, installed);
        satisfied.push(name.clone());
    }
    satisfied
}

/// Reconcile the OS-INSTALLED members of `wanted` — those whose pinned row for this
/// triple is a `pkg`, `softwareupdate` or `system-pm` row — through the real flow under
/// this pass's (Deferred) elevation: present at a `provides` path ⇒ `installed via
/// <protocol>: <path>`; absent and elevated ⇒ `needs admin — run: aterm pkg install
/// <name>`; a `system-pm` row whose manager is absent ⇒ `unavailable on <target>:
/// <hint>`; a user-scoped `system-pm` row (brew, winget, scoop, cargo, pipx) INSTALLS
/// here, unattended, through the manager the user already has; a dependency's
/// deferral defers the member. Rows are written only when they change. Returns the
/// names handled (the caller drops them from its plan) and the count of HARD failures
/// (a refused signature cannot happen here — nothing is downloaded — but a broken
/// manifest or an unmet requirement is recorded like any bootstrap error). A member
/// whose manifest cannot be read is left in the plan for the ordinary arm to explain.
#[allow(
    clippy::too_many_arguments,
    reason = "the pass's irreducible inputs, sliced for one member class"
)]
fn reconcile_protocol_members(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    anchor: &crate::Anchor,
    index: &crate::TrustedIndex,
    cfg: &crate::config::PackagesConfig,
    now: i64,
    wanted: &std::collections::BTreeSet<String>,
    installed: &std::collections::BTreeMap<String, u64>,
) -> (Vec<String>, u32) {
    let mut handled = Vec::new();
    let mut failures = 0u32;
    let triple = current_triple();
    let Some(ch) = index.channel_for(cfg.channel(), triple) else {
        return (handled, failures);
    };
    let ch = &ch;
    // DEPENDENCY-FIRST (§17.10): the plan's own order, so a member's requirement — clt
    // under brew — has its row written before the member's requirement gate reads it.
    let ordered: Vec<String> = crate::plan_groups(index, ch)
        .iter()
        .flat_map(|g| g.members.iter().cloned())
        .filter(|n| wanted.contains(n))
        .collect();
    for name in &ordered {
        if installed.contains_key(name) || crate::linkmode::is_linked(layout, name) {
            continue;
        }
        let Some((_, _, pkg)) = crate::flow::verified_pkg(fetcher, index, ch, name) else {
            continue;
        };
        let Some(row) = pkg.artifact_for(triple) else {
            continue;
        };
        if !matches!(
            crate::dispatch::strategy_for(&row.kind, &row.protocol),
            crate::dispatch::ApplyStrategy::Pkg
                | crate::dispatch::ApplyStrategy::SoftwareUpdate
                | crate::dispatch::ApplyStrategy::SystemPm
        ) {
            continue;
        }
        // THE REQUIREMENT GATE: a member never starts — never downloads a byte — while
        // one of its `requires` is not installed, system-satisfied or installed through
        // its protocol. It records `blocked by <dep>: <dep state>` (the dependency's own
        // row quoted, so the line names whose act unblocks it) and waits for the next
        // pass, or for the explicit door, which resolves both in order.
        let requires = index
            .program(name)
            .map(|p| p.requires.clone())
            .unwrap_or_default();
        if let Some((dep, dep_state)) = crate::requires::unmet_requirement(layout, index, &requires)
        {
            record_state_if_changed(layout, name, crate::state::blocked(&dep, &dep_state));
            handled.push(name.clone());
            continue;
        }
        let req = crate::InstallRequest {
            channel: cfg.channel(),
            program: name,
            triple,
            installed: None,
        };
        match crate::flow::install(fetcher, layout, anchor, &req, build_floor(layout), now) {
            Ok(r) => {
                advance_floors(layout, r.index_build, r.roster_seq);
                record_installed_deps(layout, name, r.index_build, &r.dependencies);
                if let Some(outcome) = &r.protocol {
                    record_state_if_changed(layout, name, outcome.state(name));
                }
                handled.push(name.clone());
            }
            Err(e) => {
                eprintln!("atpkg: {name}: {e} (continuing)");
                record_bootstrap_error(layout, name, &e);
                failures += 1;
                handled.push(name.clone());
            }
        }
    }
    (handled, failures)
}

/// Expand the admitted bump list with each bumped program's UNMET requirements still in
/// the plan (§17.10), dependency-first, so a bump never lifts a program above what it
/// needs: `[brew]` becomes `[(clt, Some("brew")), (brew, None)]` — the requirement tagged
/// with the program that pulled it, for the progress row ("bumped with brew"). A
/// requirement outside the plan (installed, system-satisfied, protocol-handled, unserved
/// here) is not in `plannable` and pulls nothing; a name already listed is not listed
/// twice (first mention wins, the user's own ask included). Pure over its inputs, and the
/// output's names are a subset of `plannable`, so the reorder-only property holds by
/// construction.
fn bump_with_requires(
    index: &crate::manifest::Index,
    groups: &[crate::Group],
    bump: &[String],
    plannable: &std::collections::BTreeSet<String>,
) -> Vec<(String, Option<String>)> {
    fn pull(
        index: &crate::manifest::Index,
        groups: &[crate::Group],
        name: &str,
        with: &str,
        plannable: &std::collections::BTreeSet<String>,
        out: &mut Vec<(String, Option<String>)>,
        seen: &mut std::collections::BTreeSet<String>,
    ) {
        if !seen.insert(name.to_string()) {
            return;
        }
        let Some(group) = groups.iter().find(|g| g.members.iter().any(|m| m == name)) else {
            return;
        };
        for dep in crate::apply::group_requires(index, group) {
            if plannable.contains(&dep) {
                pull(index, groups, &dep, with, plannable, out, seen);
            }
        }
        if name != with && !out.iter().any(|(n, _)| n == name) {
            out.push((name.to_string(), Some(with.to_string())));
        }
    }
    let mut out: Vec<(String, Option<String>)> = Vec::new();
    for b in bump {
        let mut seen = std::collections::BTreeSet::new();
        pull(index, groups, b, b, plannable, &mut out, &mut seen);
        if !out.iter().any(|(n, _)| n == b) {
            out.push((b.clone(), None));
        }
    }
    out
}

/// The index's `requires` relation as the stub reconcile wants it: `program → requires`
/// for every program that has any ([`crate::stub::reconcile_with_requires`]).
fn requires_of(index: &crate::manifest::Index) -> std::collections::BTreeMap<String, Vec<String>> {
    index
        .programs
        .iter()
        .filter(|(_, p)| !p.requires.is_empty())
        .map(|(n, p)| (n.clone(), p.requires.clone()))
        .collect()
}

/// Every index program that REQUIRES `program`, in index order.
fn dependents_of(index: &crate::manifest::Index, program: &str) -> Vec<String> {
    index
        .programs
        .iter()
        .filter(|(_, p)| p.requires.iter().any(|r| r == program))
        .map(|(n, _)| n.clone())
        .collect()
}

/// The last index this machine VERIFIED, from the §14 cache — offline, no network, no
/// floor or ratchet written. `None` when nothing is cached (or nothing cached verifies):
/// the advisory surfaces that read it (the uninstall warning) then say nothing.
pub(crate) fn cached_index(layout: &crate::store::Layout) -> Option<crate::TrustedIndex> {
    let slug = crate::resolve_account(crate::config::cached().account()).slug();
    let cache = crate::IndexCache::for_layout(layout);
    let candidates = cache.load(&format!("github:{slug}"))?;
    crate::select_index(
        &effective_anchor(layout),
        candidates,
        build_floor(layout),
        now_unix(),
    )
    .selected
    .map(|s| s.index)
}

/// After removing `program`: name every program that REQUIRES it and that this machine
/// keeps (installed, or in its wanted set) — each reads `blocked by <program>: …` on the
/// next pass until it is back. A WARNING, never a refusal: the user owns the store, and
/// the way back is one explicit install. Offline: the cached index answers, or nothing
/// does.
fn warn_dependents(layout: &crate::store::Layout, program: &str) {
    let Some(index) = cached_index(layout) else {
        return;
    };
    let kept = wanted_programs(layout, &index, crate::config::cached());
    let active = crate::active_builds(layout);
    let dependents: Vec<String> = dependents_of(&index, program)
        .into_iter()
        .filter(|d| kept.contains(d) || active.contains_key(d))
        .collect();
    if dependents.is_empty() {
        return;
    }
    let (verb, pronoun) = if dependents.len() == 1 {
        ("requires", "it")
    } else {
        ("require", "they")
    };
    eprintln!(
        "atpkg: warning: {} {verb} {program} — {pronoun} will read `blocked by {program}: …` \
         until it is back (aterm pkg install {program})",
        dependents.join(", ")
    );
}

/// The index-listed EXTRAS that may carry a consent stub this pass
/// ([`crate::stub::StubKind::Extra`]): `extra = true`, not removed on purpose, not
/// excluded by config, not installed (or dev-linked), and not satisfied by a system
/// install on this process's `PATH`. The caller adds the triple probe. Opted-in
/// extras qualify too — the kind says what the program is, the marker what the user
/// answered.
fn extra_stub_candidates(
    layout: &crate::store::Layout,
    index: &crate::manifest::Index,
    cfg: &crate::config::PackagesConfig,
    installed: &std::collections::BTreeMap<String, u64>,
) -> std::collections::BTreeSet<String> {
    extra_stub_candidates_with(
        layout,
        index,
        cfg,
        installed,
        std::env::var_os("PATH").as_deref(),
    )
}

/// [`extra_stub_candidates`] over an injected `PATH` value, so the rule is testable
/// without mutating the process environment.
fn extra_stub_candidates_with(
    layout: &crate::store::Layout,
    index: &crate::manifest::Index,
    cfg: &crate::config::PackagesConfig,
    installed: &std::collections::BTreeMap<String, u64>,
    path_var: Option<&std::ffi::OsStr>,
) -> std::collections::BTreeSet<String> {
    let removed = layout.removed_programs();
    index
        .programs
        .iter()
        .filter(|(name, p)| {
            index.is_extra(name)
                && !removed.contains(name.as_str())
                && !cfg.exclude().contains(name)
                && !installed.contains_key(name.as_str())
                && !crate::linkmode::is_linked(layout, name)
                && p.system
                    .as_deref()
                    .and_then(|bin| {
                        crate::vendor::system_binary_on_path(&layout.prefix, bin, path_var)
                    })
                    .is_none()
                // A stub is a courtesy for a name that would otherwise be "command not
                // found". A FOREIGN copy of the program's own name anywhere on PATH
                // (the vendor's native installer under ~/.local/bin, a brew codex) already
                // answers — and, laid ahead of it, a consent stub would HIJACK a working
                // install with `Install claude? [y/N]` (2026-09-07 review). `system =`
                // above is the owner's explicit satisfaction rule; this is the weaker
                // "do not shadow what runs" rule, and it needs no flag.
                && crate::vendor::system_binary_on_path(&layout.prefix, name, path_var)
                    .is_none()
        })
        .map(|(name, _)| name.clone())
        .collect()
}

/// `program` is satisfied by the system install at `path`: say so in the canonical
/// words, record the `system: <path> — not managed by aterm` row, and — when atpkg
/// carries its own copy — retire that copy (shims, channel links, store tree, through the
/// fail-closed `ops::uninstall`) so the system binary is the only `<program>` the user
/// can reach, and remember the DAY it happened (`<prefix>/retired/<program>`) so the row,
/// `doctor` and `which` can say `(managed copy retired <date>)`. NOT a removal on
/// purpose: no `removed` marker is written, so the member installs again the moment the
/// system binary goes away. `installed` loses the program when a copy was retired, so the
/// caller's plan reads the store as it now is.
fn satisfy_by_system(
    layout: &crate::store::Layout,
    program: &str,
    path: &std::path::Path,
    installed: &mut std::collections::BTreeMap<String, u64>,
) {
    let mut event = String::new();
    if let Some(build) = installed.remove(program) {
        match crate::uninstall(layout, program) {
            Ok(()) => {
                let today = today_ymd();
                if !today.is_empty()
                    && let Err(e) = layout.record_retired(program, &today)
                {
                    eprintln!(
                        "atpkg: {program}: could not record the retirement date at {}: {e}",
                        layout.retired_marker(program).display()
                    );
                }
                event = format!(" — retired the managed copy (build {build})");
            }
            Err(e) => {
                event = format!(" — could not retire the managed copy (build {build}): {e}");
            }
        }
    }
    let retired = layout.retired_date(program);
    let state = crate::state::system(path, retired.as_deref());
    // The pass log prints the SAME words the row records (design §2), plus the event.
    if event.contains("could not") {
        eprintln!("atpkg: {program}: {state}{event}");
    } else {
        println!("atpkg: {program}: {state}{event}");
    }
    // The ROW carries the fact; the aggregate sentence stays the pass's own verdict
    // (Settings ▸ Packages prints it as its detail line, and "up to date" must not be
    // displaced by a per-member note every six hours). Only a record with no sentence
    // yet takes this one.
    let outcome = crate::status::read(layout)
        .map(|s| s.outcome)
        .filter(|o| !o.is_empty())
        .unwrap_or_else(|| format!("{program} satisfied by system {}", path.display()));
    record_status(
        layout,
        program,
        crate::ProgramStatus {
            installed_build: None,
            state,
            tree_root: String::new(),
        },
        outcome,
    );
}

/// Record `state` as `program`'s row — no installed build, no root — and print it in the
/// pass log, UNLESS the record already says exactly that: a pass that changes nothing
/// writes nothing and says nothing. The aggregate outcome is carried over untouched (the
/// row is a per-program fact, not the pass's verdict). Returns whether a row was written.
fn record_state_if_changed(layout: &crate::store::Layout, program: &str, state: String) -> bool {
    let recorded = crate::status::read(layout);
    let same = recorded
        .as_ref()
        .and_then(|s| s.programs.get(program))
        .is_some_and(|r| r.state == state);
    if same {
        return false;
    }
    println!("atpkg: {program}: {state}");
    record_status(
        layout,
        program,
        crate::ProgramStatus {
            installed_build: None,
            state,
            tree_root: String::new(),
        },
        recorded.map(|s| s.outcome).unwrap_or_default(),
    );
    true
}

/// Whether an UpToDate member's row must be rewritten to `managed <build> — pinned by
/// index <index_build>`: a row that is absent, the legacy `active` spelling (no current
/// writer emits it — `state.rs` lists it among the "other" strings), or a managed row
/// whose build or index differs from this pass's — or a FAULT this very pass has
/// disproved.
///
/// THE FAULT LIFT (measured 2026-09-14). `aborted: <phase>` (the `Aborted` arm of
/// [`report_channel_apply`]) and `error: …` ([`failed_install_state`]) are written by a
/// pass that FAILED, and nothing was ever charged with taking them back: every other
/// writer rewrites a row only when bytes move. So once the program settles at its pin
/// with no bytes left to move — the bad build yanked and the pin rolled back, a sibling
/// upgraded through the explicit door (which rewrites only its own row), or a group-abort
/// charged to an arbitrary member that itself needed no move ([`crate::doctor`]) — the
/// row outlives the failure it describes, and it is still a fault to
/// `doctor::is_problem_state`. The machine then reported PROBLEM forever and Settings ▸
/// Packages showed `trust` and `clean` reading `aborted: stage` beside the healthy builds
/// they were actually running; only an explicit `aterm pkg install <program>` cleared it,
/// which is not a thing an unattended manager may require. Reaching this predicate IS the
/// disproof: the group came back UpToDate and the caller has already found a live,
/// shim-derived build for this program, i.e. it is installed at the pin. Same doctrine as
/// the `blocked by` lift above — the gate that recorded a row lifts it when the fact it
/// stated is gone.
///
/// Every OTHER spelling is still another arm's to change — SHADOWED (the shadow
/// reconcile), `system:` (a retirement), `installed via`, `needs admin` — so it is left
/// alone, and `tombstoned:` never reaches here at all: a tombstoned program has no real
/// shim, so it is absent from `active_builds` and the caller skips it.
fn up_to_date_row_needs_rewrite(prior: Option<&str>, build: u64, index_build: u64) -> bool {
    match prior {
        None | Some("active") => true,
        // The two MACHINE-LOCAL fault prefixes, spelled with the same literals as
        // `doctor::is_problem_state`. The other rows that read as faults are not lifted
        // here: `blocked by …` has its own lift above (which narrates), and an unserved
        // triple's row belongs to a program with no live build to find.
        Some(s) if s.starts_with("aborted:") || s.starts_with("error:") => true,
        Some(s) => {
            crate::state::managed_pin(s).is_some_and(|(b, i)| b != build || i != index_build)
        }
    }
}

/// Write one program's row and NOTHING ELSE: the aggregate `outcome` is carried from the
/// existing record. [`record_status`] takes the sentence as a parameter because its
/// callers ARE the pass's verdict; a row migration is not, and must not swap `up to date
/// (index build N)` for a sentence about itself.
fn record_row_keeping_outcome(
    layout: &crate::store::Layout,
    program: &str,
    row: crate::ProgramStatus,
) {
    let outcome = crate::status::read(layout)
        .map(|s| s.outcome)
        .unwrap_or_default();
    record_status(layout, program, row, outcome);
}

/// Today as `YYYY-MM-DD` (UTC), or empty when the clock is unusable — the retirement
/// marker's payload. Derived from the same RFC3339 stamp `status.toml` carries.
fn today_ymd() -> String {
    let stamp = now_rfc3339();
    stamp.get(..10).map(str::to_string).unwrap_or_default()
}

/// The fix-line for a managed shim that EXPORTS an environment before it execs (design
/// S7, [`crate::shim_env::ShimEnv::fix_line`]): `self-update off (DISABLE_AUTOUPDATER=1)`,
/// or `runs with NAME=VALUE` for an env naming no self-update switch. Read off the shim
/// AS LAID — the thing that actually runs — never off a manifest or a record; `None` for
/// a plain shim. Never part of the recorded state: the surfaces that speak to a person
/// (`which`, `doctor`) append it after the canonical row as a separate sentence.
pub(crate) fn shim_env_fix(shim: &std::path::Path) -> Option<String> {
    crate::platform::shim_env_of(shim).fix_line()
}

/// The fix-line for a SHADOWED `tool` of `program`: `type alab-<tool> for the managed one`
/// ([`crate::state::alias_hint`]) — printed ONLY when typing the alias really runs the
/// managed copy: `alab-<tool>` is laid, resolves into THIS prefix's `store/<program>/`
/// (the anchored containment, never the unanchored parse a hand-made link into a checkout
/// could satisfy), and is not itself shadowed by a foreign `alab-<tool>` earlier on
/// `path_var` than the managed `bin/`. `None` for a vendor tool or a system-satisfiable
/// member (no alias by policy), for a tool that is its own alias, for an install a
/// pre-alias client laid that the pass has not reconciled yet, and for an alias that
/// would itself run someone else's copy. Never part of the recorded state.
pub(crate) fn alias_fix(
    layout: &crate::store::Layout,
    tool: &crate::store::ToolName,
    program: &str,
    path_var: Option<&std::ffi::OsStr>,
) -> Option<String> {
    let alias = tool.alias()?;
    let target = crate::which(layout, alias.as_str())?;
    let (owner, _) = crate::ops::store_build_of(&layout.prefix, &target)?;
    if owner != program {
        return None;
    }
    if crate::vendor::shadowing_binary_on_path(&layout.prefix, alias.as_str(), path_var).is_some() {
        return None;
    }
    Some(crate::state::alias_hint(alias.as_str()))
}

/// ALIAS reconcile (owner decision 2026-08-27): for every installed, non-dev-linked
/// program, bring its `alab-<tool>` aliases in line with the signed index's verdict on
/// whether it is ALab's own ([`crate::activate::Aliases::for_program`]) — laid when
/// missing (an install a pre-alias client made, and which the pipeline short-circuits as
/// up to date forever after), swept when the entry says the program is not ALab's. The
/// primaries are never rewritten, so the silent tick touches nothing once the aliases
/// stand. Best-effort per program: an unwritable `bin/` is the install lane's problem to
/// report, not this pass's to fail on.
fn reconcile_aliases(layout: &crate::store::Layout, index: &crate::manifest::Index) {
    // One listing of `bin/` for the whole pass, and exact for it: this loop lays `alab-`
    // aliases and prunes, neither of which touches the primary shims `active_tools_in`
    // reports. The deleting half is not on the snapshot — `prune_stale_shims` lists the
    // directory itself, after this pass's writes, at the moment it decides.
    let scan = crate::ops::BinScan::capture(layout);
    for (program, build) in crate::ops::active_builds_in(&scan) {
        if crate::linkmode::is_linked(layout, &program) {
            continue;
        }
        let tools = crate::ops::active_tools_in(&scan, &program, build);
        let aliases = crate::activate::Aliases::for_program(&program, index.program(&program));
        let _ = crate::activate::reconcile_aliases(
            layout,
            &layout.build_dir(&program, build),
            &tools,
            aliases,
        );
    }
    // ONCE, AFTER THE LOOP: the front-of-PATH `agents/` twins. Nothing in
    // `activate::reconcile_agents` is a function of the program being reconciled — it
    // checks both agent programs and sweeps `agents/` whatever it is handed — and it used
    // to hang off the end of `activate::reconcile_aliases`, which this loop calls once per
    // ACTIVE PROGRAM. A twelve-program machine therefore did the whole agents reconcile
    // twelve times per pass: on an untracked machine wasted reads, and on a
    // provenance-tracked process whose untracked launchd lane fails, a re-lay of both
    // twins per program — the in-process fallback write is tagged again, and the
    // keep-predicate refuses a tagged twin — so the work converged neither within a pass
    // nor across passes (audit 2026-09-15). Unconditional, outside the loop: an empty
    // store has no program to hang it on and its sweep is exactly what must still run.
    crate::activate::reconcile_agents(layout);
}

/// SHADOW reconcile (design S5): for every MANAGED member — no `system` key, or one whose
/// managed copy is present — and every tool it exposes, a foreign executable of that name
/// EARLIER on `PATH` than the managed `bin/` is what actually runs. Record the canonical
/// `managed <build> — SHADOWED by <path>` row, print the same words, and never touch
/// `PATH` or the foreign copy: it is a warning the user owns, not a fault atpkg fixes. A
/// member whose row read SHADOWED last pass and is no longer is put back to `managed
/// <build> — pinned by index <N>`. Returns the shadowed programs, for the caller's log.
/// The log line carries the alias fix-line after the state when the alias is laid
/// ([`alias_fix`]); the recorded row never does.
///
/// THE AGENT PROGRAMS (2026-09-16): a `claude`/`codex` whose `agents/` twin is laid and
/// current ([`agent_twin_current`]) is what every aterm tab runs, so a foreign copy ahead
/// on THIS pass's `PATH` — the GUI-spawned login shell's, no user's — is no shadow at
/// all: printed as the managed row plus [`pass_path_tail`] (never the shell-local
/// source-the-hook wording, which describes a shell somebody is in — review, 2026-09-16),
/// recorded as managed, and named on no `shadowed:` line; a row an older client recorded
/// as SHADOWED is put back.
fn reconcile_shadowed(
    layout: &crate::store::Layout,
    index_build: u64,
    path_var: Option<&std::ffi::OsStr>,
) -> Vec<String> {
    let mut shadowed = Vec::new();
    let recorded = crate::status::read(layout);
    let outcome = recorded
        .as_ref()
        .map(|s| s.outcome.clone())
        .unwrap_or_default();
    // One listing for this pass too, and an easier case than the alias reconcile's: this
    // lane only reads, prints and records — nothing here writes to `bin/` or deletes from
    // it, so the snapshot cannot go stale under its own feet.
    let scan = crate::ops::BinScan::capture(layout);
    for (program, build) in crate::ops::active_builds_in(&scan) {
        if crate::linkmode::is_linked(layout, &program) {
            continue;
        }
        let shadow = crate::ops::active_tools_in(&scan, &program, build)
            .into_iter()
            .find_map(|tool| {
                crate::vendor::shadowing_binary_on_path(&layout.prefix, tool.as_str(), path_var)
                    .map(|path| (tool, path))
            });
        let was = recorded
            .as_ref()
            .and_then(|s| s.programs.get(&program))
            .map(|r| r.state.clone())
            .unwrap_or_default();
        // An AGENT PROGRAM with its twin laid and current is NOT shadowed on this machine
        // (2026-09-16): every aterm tab puts `agents/` first, so the foreign copy this
        // pass's own PATH reaches is one shell's truth. Say it, with the in-place remedy,
        // and record — and mark — nothing but the managed row.
        let agent_tail = shadow.as_ref().and_then(|(_, path)| {
            agent_shell_shadow(layout, &program, build, path, path_var)
                .map(|_| pass_path_tail(path))
        });
        let state = match (shadow, agent_tail) {
            (Some(_), Some(tail)) => {
                let state = crate::state::managed(build, index_build);
                println!("atpkg: {program}: {state} — {tail}");
                if crate::state::shadowed_by(&was).is_none() {
                    continue;
                }
                state
            }
            (Some((tool, path)), None) => {
                let state = crate::state::shadowed(build, &path);
                // The canonical row, then — as a SEPARATE sentence, never recorded — the
                // way out when the alias is laid: `type alab-<tool> for the managed one`.
                match alias_fix(layout, &tool, &program, path_var) {
                    Some(fix) => println!("atpkg: {program}: {state} — {fix}"),
                    None => println!("atpkg: {program}: {state}"),
                }
                shadowed.push(program.clone());
                state
            }
            (None, _) if crate::state::shadowed_by(&was).is_some() => {
                let state = crate::state::managed(build, index_build);
                println!("atpkg: {program}: {state} (no longer shadowed)");
                state
            }
            (None, _) => continue,
        };
        record_status(
            layout,
            &program,
            crate::ProgramStatus {
                installed_build: Some(build),
                state,
                tree_root: effective_tree_root(layout, &program, ""),
            },
            outcome.clone(),
        );
    }
    if !shadowed.is_empty() {
        println!("atpkg: {SHADOWED_MARKER}{}", shadowed.join(", "));
    }
    shadowed
}

/// THE EXPLICIT DOOR (`atpkg install <program>`) for the two owner rules that need the
/// verified index before the flow runs:
///
/// * an EXTRA records its opt-in FIRST, then installs — so the unattended passes keep it
///   current from now on ([`note_extra_optin`]);
/// * a SYSTEM-SATISFIED member ([`crate::manifest::Program::system`]) is not installed:
///   `Some(path)` names the system binary, the row is recorded and any managed copy
///   retired ([`satisfy_by_system`]), and the caller stops there with success.
///
/// `None` in every other case, INCLUDING an unresolvable index or an unlisted name — the
/// flow that follows raises the real error with its real remedy; this door never invents
/// a second diagnosis. The resolve is memoized in the fetcher, so the flow's own resolve
/// costs no second round-trip.
fn explicit_install_door(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    program: &str,
) -> Option<std::path::PathBuf> {
    let index = crate::resolve_verified_index(
        fetcher,
        layout,
        &effective_anchor(layout),
        build_floor(layout),
        now_unix(),
    )
    .ok()?;
    let prog = index.program(program)?;
    // (`note_extra_optin` asks `Index::is_extra`, so an agent program — default-set
    // whatever its row's flag says — records no opt-in marker.)
    if note_extra_optin(layout, &index, program) {
        println!("atpkg: {program} is an extra — opted in; later passes keep it current");
    }
    // Its `requires` (§17.10) are resolved FIRST by the flow, in this order, on this
    // thread — one terminal, one elevation policy, one sudo session (`aterm pkg install
    // brew` installs the Command Line Tools, then Homebrew). Said before anything moves.
    if !prog.requires.is_empty() {
        println!(
            "atpkg: {program} also needs: {} — resolved first, in that order",
            prog.requires.join(", ")
        );
    }
    let path = crate::vendor::system_satisfied(&layout.prefix, prog)?;
    let mut installed = crate::active_builds(layout);
    satisfy_by_system(layout, program, &path, &mut installed);
    Some(path)
}

/// Record that `program` was removed on purpose, so no unattended pass puts it back.
///
/// A write that did NOT land is printed. This record is the only thing keeping the
/// program out of the unattended lanes ([`seed_serviceable`] and
/// `install_default_set_inner` both filter on it, and `cmd_seed` records adoption again
/// on every launch), while the caller prints `atpkg: uninstalled {program}` and exits 0
/// whatever happened here — so an unwritable prefix used to mean the next launch quietly
/// reinstalled what the user had just removed, with nothing said at the time.
/// Best-effort like every marker write, and like `record_adoption`/`record_decline` the
/// failure is said out loud.
fn record_removed(layout: &crate::store::Layout, program: &str) {
    let mut all = removed_programs(layout);
    if !all.insert(program.to_string()) {
        return;
    }
    if let Err(e) = write_removed(layout, &all) {
        eprintln!(
            "atpkg: could not record the removal at {}: {e} — the next unattended pass may \
             reinstall {program}",
            layout.removed().display()
        );
    }
}

/// Forget removals for `programs` — an explicit install is an unambiguous change of mind.
///
/// A failure is printed for the same reason the other direction's is: the toolset verb
/// clears the markers BEFORE a pass that narrows `wanted` by them, so a clear that did
/// not land makes that pass skip the very member it was asked to restore and then report
/// the set complete.
fn clear_removed(layout: &crate::store::Layout, programs: &[String]) {
    let mut all = removed_programs(layout);
    let mut lifted: Vec<&str> = Vec::new();
    for p in programs {
        if all.remove(p) {
            lifted.push(p.as_str());
        }
    }
    if lifted.is_empty() {
        return;
    }
    if let Err(e) = write_removed(layout, &all) {
        eprintln!(
            "atpkg: could not lift the removal record at {}: {e} — set completion may keep \
             skipping {}",
            layout.removed().display(),
            lifted.join(", ")
        );
    }
}

/// Write the whole record, atomically, and hand back any failure.
///
/// # Errors
/// The open/write/rename error, or the delete's when the last entry is being lifted
/// (an already-absent file is not one). Both callers report it: nothing else in the
/// process will, and this record is load-bearing state, not diagnostics.
fn write_removed(
    layout: &crate::store::Layout,
    all: &std::collections::BTreeSet<String>,
) -> std::io::Result<()> {
    let path = layout.removed();
    if all.is_empty() {
        return match std::fs::remove_file(&path) {
            Err(e) if e.kind() != std::io::ErrorKind::NotFound => Err(e),
            _ => Ok(()),
        };
    }
    let mut body = String::from(
        "# Programs removed on purpose. No unattended pass reinstalls these;\n\
         # `aterm pkg install <program>` brings one back.\n",
    );
    for p in all {
        body.push_str(p);
        body.push('\n');
    }
    // TEMP + RENAME, like `status::write`: truncating the live file in place meant a
    // crash between the truncate and the last line left a shorter list — every program
    // past the cut silently un-removed, and back on the next unattended pass.
    let mut tmp_name = String::from("removed.tmp-");
    tmp_name.push_str(&crate::dec_u64(u64::from(std::process::id())));
    let tmp = path.with_file_name(tmp_name);
    let _ = std::fs::remove_file(&tmp);
    let wrote = crate::platform::open_create_write(&tmp, 0o600).and_then(|mut f| {
        use std::io::Write as _;
        f.write_all(body.as_bytes())?;
        f.sync_all()
    });
    let published = wrote.and_then(|()| std::fs::rename(&tmp, &path));
    if published.is_err() {
        let _ = std::fs::remove_file(&tmp);
    }
    published
}

/// `group-aborted: <label> update aborted at <member> during <phase>: <cause>` — a
/// coherence group's transaction aborted (2026-09-15). The window renders it as a Warn
/// row on the toolchain lane, a terminal row THERE (`toolchain_failed`) though not an
/// answer in the announcement ledger below, which lists it nowhere: until this marker
/// existed the abort reached the log alone, and the 2026-09-14 incident's "rustc update
/// ABORTED at trust during stage" was on no screen while the seed lane's row said
/// "install failed" for a different reason. Printed by [`report_channel_apply`], once
/// per aborted group.
pub const GROUP_ABORTED_MARKER: &str = "group-aborted: ";
/// `tracked-install: <program> build <n> — <why>` — a bundle staged IN-PROCESS by a
/// provenance-tracked installer whose untracked lane could not run (2026-09-15), the
/// default policy's recorded outcome ([`crate::install::stage_for_store_with`]). Not a
/// terminal (the install succeeded); the window shows a Warn row naming the program
/// and `aterm pkg doctor`, so a tagged toolchain is never a silent one. Printed by
/// [`crate::install::verify_and_stage_with`] beside the record it writes.
pub const TRACKED_INSTALL_MARKER: &str = "tracked-install: ";
/// The first line of a cause, bounded for a status row and a marker line: a stage
/// error can carry a multi-line transport message, and the record and the glass want
/// one sentence (the doctor prints the full row on request).
#[must_use]
pub fn clip_cause(why: &str) -> String {
    const CAUSE_CAP: usize = 240;
    let first = why.lines().next().unwrap_or("").trim();
    if first.chars().count() <= CAUSE_CAP {
        return first.to_string();
    }
    let mut clipped: String = first.chars().take(CAUSE_CAP).collect();
    clipped.push('…');
    clipped
}

/// The STABLE stdout markers the GUI parses (`crates/aterm-gui`, `parse_seed_line`).
///
/// Constants rather than inline literals because this is a CROSS-CRATE CONTRACT with a
/// silent failure mode: a reworded prefix does not break a build or a test, it just
/// means the notice never appears again — and this contract has already broken
/// undetected once. `aterm-gui` imports these, so a rename is a compile error on both
/// sides instead of a string that quietly stops matching.
///
/// Every marker is a TERMINAL answer to an announcement except the TWO that OPEN one
/// — `SEED_STARTING` (the local lane) and `NET_STARTING` (its twin for the wire, added
/// after this paragraph first named only the local one) — and the ones that are
/// neither, which the ledger deliberately leaves unclassified: `LOCK_WAITING` (a
/// non-terminal "queued behind a sibling" row; [`LOCK_WAITING_MARKER`] says what
/// retires it), `LOCK_ACQUIRED` (what retires that row when the wait ends in the
/// lock, [`LOCK_ACQUIRED_MARKER`]), `GROUP_ABORTED` and `TRACKED_INSTALL` (both
/// 2026-09-15: rows of their own on the toolchain lane, neither of which answers a
/// pass's announcement) and the informational `SHADOWED`, `MANAGED_CURRENT` and
/// `MACHINE_SETTINGS` rows. [`announcement::MARKERS`] is where that split is recorded
/// and [`emit_marker_line`] is what keeps the ledger, so the classification lives in
/// one table rather than in this sentence. An announcement with no answer leaves
/// "Installing…" on screen forever.
pub const SEED_STARTING_MARKER: &str = "seed-starting: ";
pub const SEED_INSTALLED_MARKER: &str = "seed-installed: ";
pub const SEED_PENDING_MARKER: &str = "seed-pending: ";
pub const SEED_UNUSABLE_MARKER: &str = "seed-unusable: ";
pub const SEED_FAILED_MARKER: &str = "seed-failed: ";
pub const SEED_PARTIAL_MARKER: &str = "seed-partial: ";
/// The NETWORK completion lane's answer — programs that arrived over the wire on an
/// adopted machine, which used to happen with no user-visible trace at all.
pub const NET_INSTALLED_MARKER: &str = "net-installed: ";
/// The network lane's ANNOUNCEMENT, printed BEFORE any bytes move — the
/// seed-starting twin for the wire. The comment above `complete_the_set`
/// already argued for it ("ANNOUNCE BEFORE ACTING, exactly as the local seed
/// lane does") while the code announced nothing: gigabytes could stream with
/// the first user-visible line being the completion.
///
/// Emitted ONLY by the wire lane ([`net_announcement`], gated on
/// [`ProvisionLane`]). The sealed-payload seed once announced itself with this
/// marker — "installing … over the network" during an lsof-verified
/// zero-socket local extraction — and the GUI, which parses these markers
/// byte-for-byte, repeated the fabricated network install on its progress
/// card. The local lane's announcement is [`SEED_STARTING_MARKER`], printed by
/// `cmd_seed` itself.
pub const NET_STARTING_MARKER: &str = "net-starting: ";
/// The network lane's failure TERMINAL: an announced provisioning that then
/// installs nothing must retire its own status bar with the truth — the seed
/// lane shipped exactly this bug once (a held card that sat for its full 20
/// minutes), and the marker contract is what prevents the rerun.
pub const NET_FAILED_MARKER: &str = "net-failed: ";
/// The dispatch edge's "queued behind a sibling" ANNOUNCEMENT (2026-09-10): printed
/// ONCE, before any verb output, only when the caller opted into `--wait-lock`, the
/// store lock is held, and the wait has outlasted [`crate::lock::WAIT_ANNOUNCE_GRACE`].
/// It is neither a pass start nor a terminal answer — the GUI logs it (one INFO
/// line, `Wake::PkgLockWaiting`) and paints NO row (2026-09-18; it used to open a
/// "waiting" row that named another aterm, false after a self-update where the
/// holder is the app's own predecessor's pass): a holder with a plan shows through
/// the sibling's progress file, a no-op holder is nothing to show, and a wait that
/// ran out (exit 75) is the lane's deferred notice. Emitted by exactly one site
/// ([`mutator_store_lock`]).
pub const LOCK_WAITING_MARKER: &str = "lock-waiting: ";
/// The dispatch edge's "the wait is over" line (2026-09-14): printed ONCE, right
/// after a wait that ANNOUNCED itself ([`LOCK_WAITING_MARKER`]) acquires the lock —
/// never after a silent wait inside the grace, never after an uncontended try, and
/// never by a typed verb. Neither a pass start nor a terminal answer, like the line
/// it answers: the GUI logs it (`Wake::PkgLockAcquired`), so the interval the child
/// spent queued is on record — there is no row to retire (2026-09-18). The verb
/// that follows may be QUIET, or stand down at once when the holder succeeded at
/// this store's pass meanwhile ([`pass_finished_while_we_waited`]). Emitted by the
/// same one site.
pub const LOCK_ACQUIRED_MARKER: &str = "lock-acquired: ";
/// The SHADOW reconcile's answer ([`reconcile_shadowed`]): the managed programs a
/// foreign copy out-ranks on this pass's `PATH`, comma-separated — the pass printed
/// one `<program>: managed <build> — SHADOWED by <path>` line per member and no
/// machine-readable summary at all (2026-09-10 audit), so a GUI could not tell a pass
/// that found a shadow from one that found none.
pub const SHADOWED_MARKER: &str = "shadowed: ";
/// The MANAGED-CURRENT answer, printed at the end of every successful update/seed
/// pass: each agent program ([`crate::stub::AGENT_PROGRAMS`]) that is installed AND at
/// the index pin, as `<name> <version> (build <build>)`, `; `-separated — the R6 row
/// "the latest aterm-managed claude/codex are in use". Omitted when none qualifies.
pub const MANAGED_CURRENT_MARKER: &str = "managed-current: ";

/// `machine-settings: spotlight-noindex 73 dir(s) migrated; universal-control disabled`
/// (R5/R6, contract 2026-09-10): what THIS pass changed about the machine per the
/// doctor's remedies — entries `; `-separated, the line omitted when nothing changed.
/// The pull-down row that renders it must carry the revert
/// ([`crate::machine::UNIVERSAL_CONTROL_REVERT`]).
pub const MACHINE_SETTINGS_MARKER: &str = "machine-settings: ";

/// The `machine-state:` marker bare `atpkg machine` prints — the READ counterpart of
/// `machine-settings:` (a change). One byte-stable line, `key=value` pairs joined by
/// `; `, built by [`crate::machine::machine_state_line`] and parsed back by
/// [`crate::machine::parse_machine_state`], so a window reader confirms what the machine
/// measured rather than re-deriving it from prose.
pub const MACHINE_STATE_MARKER: &str = "machine-state: ";

/// The prefix of every `atpkg machine` / `atpkg machine apply` verdict sentence
/// (`applied — …`, `nothing changed — …`, `not applied — …`, `next — …`, …). A
/// window worker keys on it to quote the verdict; spelled once here so a reword is
/// a compile error on both sides rather than a card that silently stops quoting.
pub const MACHINE_VERDICT_PREFIX: &str = "atpkg machine: ";

/// The line [`apply_machine_settings`] prints when it refuses to touch the machine
/// (a home that is not this account's): `atpkg: machine settings not applied — <why>`.
/// The verb still exits 0, so a window worker keys on THIS prefix to report the
/// refusal as the outcome rather than a success.
pub const MACHINE_NOT_APPLIED_PREFIX: &str = "atpkg: machine settings not applied — ";

/// A machine action ran but failed. The shell verb remains best-effort and can
/// exit 0, so the GUI must read this independently of its change markers and
/// final verdict. Successful changes from the same pass remain valid.
pub const MACHINE_APPLY_FAILED_PREFIX: &str = "atpkg: machine settings failed — ";

/// The `machine-settings:` line for `entries` (already non-empty).
#[must_use]
pub fn machine_settings_line(entries: &[String]) -> String {
    format!("{MACHINE_SETTINGS_MARKER}{}", entries.join("; "))
}

/// The Spotlight entry: `spotlight-noindex <n> dir(s) migrated`.
#[must_use]
pub fn spotlight_entry(migrated: usize) -> String {
    format!("spotlight-noindex {migrated} dir(s) migrated")
}

/// Report the actual Universal Control outcome without discarding earlier
/// Spotlight changes or treating a failed preference write as an applied one.
fn record_universal_control_outcome(
    outcome: crate::machine::UniversalControlOutcome,
    entries: &mut Vec<String>,
) -> Option<String> {
    use crate::machine::UniversalControlOutcome;
    match outcome {
        UniversalControlOutcome::Disabled => {
            entries.push(crate::machine::UNIVERSAL_CONTROL_ENTRY.to_string());
            Some(format!(
                "atpkg: Universal Control disabled for this host (revert: {})",
                crate::machine::UNIVERSAL_CONTROL_REVERT
            ))
        }
        UniversalControlOutcome::WriteFailed => Some(format!(
            "{MACHINE_APPLY_FAILED_PREFIX}Universal Control could not be disabled for this host — \
             the preference write did not land (`aterm pkg doctor` reads the keys)"
        )),
        UniversalControlOutcome::AlreadyDisabled | UniversalControlOutcome::NotApplicable => None,
    }
}

/// What one bounded walk of a home says about Spotlight: the counts the
/// `machine-state:` record carries, measured the one way both the read and the pass
/// measure them.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
struct SpotlightCensus {
    /// Cargo target dirs whose name leaves them open to Spotlight.
    exposed: usize,
    /// Already hidden by name.
    hidden: usize,
    /// Of the exposed, how many a pass would migrate — a dry-run apply, which refuses the
    /// free-standing dir, the live build and the occupied destination exactly as the real pass
    /// does, so this count can reach zero where `exposed` never could (the doctor records that
    /// trap). `apply_one(.., true, true)` is pure: `Planned` is returned before any rename.
    would_migrate: usize,
    /// `false` when the walk hit its budget: every surface then says "at least".
    complete: bool,
}

/// One doctor-budget walk of `home` and a dry-run apply over what it found.
fn spotlight_census(home: &std::path::Path) -> SpotlightCensus {
    let scan = crate::noindex::scan(
        home,
        crate::noindex::DOCTOR_DEPTH,
        &crate::noindex::Budget::DOCTOR,
    );
    let (exposed, hidden) = scan.counts();
    let would_migrate = scan
        .exposed()
        .filter(|t| {
            matches!(
                crate::noindex::apply_one(&t.path, true, true),
                crate::noindex::Applied::Planned { .. }
            )
        })
        .count();
    SpotlightCensus {
        exposed,
        hidden,
        would_migrate,
        complete: scan.complete,
    }
}

/// The Spotlight half of the pass's own `machine-state:` record, after its apply: the pass
/// reports from the walk it already paid for. A pass that migrated nothing and failed nothing
/// knows the posture already — every exposed dir it left was a refusal a dry run refuses
/// identically ([`crate::noindex::apply_one`]) — so a pass that moved something, or failed
/// to, walks again, that being the one case where the pre-apply scan no longer fits the
/// disk. The other walk is a pass whose Spotlight half is OFF and whose Universal Control
/// half changed something: it has no scan to report from, and a record without counts
/// would say less than the read it replaces.
fn spotlight_after_apply(
    home: &std::path::Path,
    scan: &crate::noindex::Scan,
    outcomes: &[crate::noindex::Applied],
) -> SpotlightCensus {
    let moved_or_failed = outcomes
        .iter()
        .any(|o| o.migrated() || o.failure().is_some());
    if moved_or_failed {
        return spotlight_census(home);
    }
    let (exposed, hidden) = scan.counts();
    SpotlightCensus {
        exposed,
        hidden,
        would_migrate: 0,
        complete: scan.complete,
    }
}

/// Print the pass's own `machine-state:` record from `census`, the machine re-read for the
/// Universal Control half (the keys themselves, not the apply's outcome). The same line, from
/// the same builder, that bare `atpkg machine` prints — [`MACHINE_STATE_MARKER`] — so the
/// window's one parser serves both.
fn print_machine_state_after_apply(
    cfg: &crate::config::MachineConfig,
    policy: crate::config::UniversalControlPolicy,
    census: SpotlightCensus,
) {
    let state = crate::machine::universal_control_state(&crate::machine::SystemDefaults);
    let (home_posture, _) = machine_home();
    let st = crate::machine::MachineState {
        universal_control: state.posture(),
        policy,
        spotlight_noindex: cfg.spotlight_noindex(),
        exposed: census.exposed,
        hidden: census.hidden,
        would_migrate: census.would_migrate,
        scan_complete: census.complete,
        home: home_posture,
        config_unreadable: cfg.unreadable,
    };
    println!(
        "atpkg: {MACHINE_STATE_MARKER}{}",
        crate::machine::machine_state_line(&st)
    );
}

/// Apply the `[machine]` settings — at the TOP of every pass, and from `atpkg machine
/// apply` — and print what CHANGED, as the `machine-settings:` marker (nothing printed
/// when nothing changed — the pull-down row is for a change, not a state). Returns the
/// same entries so `atpkg machine apply` can say "nothing changed" without re-reading its own stdout. Spotlight: the doctor's scan of `$HOME`, migrated with
/// each repo's cargo re-pointed ([`crate::noindex::apply_under`]; a live build is skipped
/// and retried next pass). Universal Control: [`crate::machine::apply_universal_control`]
/// through the real `defaults`. Best-effort throughout — a machine setting must never
/// fail the toolchain pass that carried it — and every migration is also narrated on
/// its own lines for the log.
fn apply_machine_settings() -> Vec<String> {
    let cfg = crate::config::cached_machine();
    let mut entries: Vec<String> = Vec::new();
    // AN ORPHANED PASS LEAVES THE MACHINE TO THE SUCCESSOR (2026-09-15). A pass whose
    // spawning window is gone ([`crate::progress::watch_for_orphaning`]) finishes its
    // store work, but the `machine-settings:` line it would print reaches no window —
    // and disabling Universal Control or migrating build directories is the loudest
    // thing a pass does to a Mac; it must be done by a pass that can say so. The
    // settings are idempotent per doctor's own findings, so the successor's pass, minutes
    // later at most, applies and reports them.
    // SILENTLY (2026-09-16). The refusal is upstream's and it is right; the SENTENCE that
    // went with it is not, and it only became reachable when the apply moved to the
    // dispatch edge: an orphaned waiter now passes through here before it ever blocks on
    // the lock, so a pass whose window is gone printed a line to a pipe nobody holds —
    // and `an_orphaned_waiter_stands_down_silently_before_its_bound` asserts, correctly,
    // that such a pass says nothing on either stream. There is by construction no reader:
    // that IS the condition being detected. The successor pass applies and reports.
    if parent_is_gone() {
        return entries;
    }
    // Off macOS there is nothing to apply — no Spotlight, no Universal Control — and
    // nothing to say about it either: the home guard below would otherwise print a
    // line on every Linux pass whose HOME is not the passwd home (a CI container, this
    // crate's own integration tests), about settings that do not exist there.
    if !crate::noindex::SUPPORTED {
        return entries;
    }
    // A SYNTHETIC HOME IS A SYNTHETIC MACHINE. `defaults` writes the ACCOUNT's per-host
    // domain and ignores `$HOME` (measured 2026-09-14), so a pass pointed at another
    // home — this crate's own integration tests spawn `seed`/`update`/`install` against a
    // temp HOME, and did write the developer's real Universal Control keys the moment
    // this ran ahead of everything — must not touch the real machine at all. The
    // Spotlight half follows the same rule for symmetry: it walks `$HOME`, and a home
    // that is not this account's is not one to rename things under. One line says why,
    // so a `sudo -H` or a CI job is never a silent no-op.
    if let Some(reason) = machine_refusal(cfg) {
        println!("{MACHINE_NOT_APPLIED_PREFIX}{reason}");
        return entries;
    }
    let home = aterm_types::dirs::home_dir()
        .filter(|h| !h.as_os_str().is_empty())
        .or_else(crate::platform::account_home);
    // The Spotlight half's own posture after its apply, for the record at the end.
    let mut census: Option<SpotlightCensus> = None;
    if cfg.spotlight_noindex()
        && crate::noindex::SUPPORTED
        && let Some(home) = home.as_deref()
    {
        let (outcomes, scan) = crate::noindex::apply_under_scan(
            home,
            crate::noindex::DOCTOR_DEPTH,
            &crate::noindex::Budget::DOCTOR,
            false,
        );
        let complete = scan.complete;
        // A TRUNCATED WALK IS NOT A FINISHED ONE. The scan pops a deterministic stack
        // and clears it at the budget, so the same pass stops in the same place every
        // time: without this line a home too large for the doctor's budget would look,
        // pass after pass, exactly like a home with nothing left to do.
        if !complete {
            println!(
                "atpkg noindex: the walk stopped at its budget — what it reached is done, and \
                 `aterm pkg noindex apply --all` walks further"
            );
        }
        for line in render_applied(&outcomes) {
            if noindex_pass_prints(&line) {
                println!("atpkg noindex: {line}");
            }
        }
        let migrated = outcomes.iter().filter(|o| o.migrated()).count();
        if migrated > 0 {
            entries.push(spotlight_entry(migrated));
        }
        // A SPOTLIGHT HALF THAT FAILED SAYS SO IN THE WINDOW'S OWN WORDS. Until now only
        // the Universal Control half could produce a `machine settings failed —` line,
        // so a rename that failed on every pass reached the card as a success with a
        // smaller count — and the count is the only thing the card could compare.
        let failures: Vec<String> = outcomes
            .iter()
            .filter_map(|o| {
                o.failure()
                    .map(|(path, reason)| format!("{} ({reason})", path.display()))
            })
            .collect();
        if !failures.is_empty() {
            println!(
                "{MACHINE_APPLY_FAILED_PREFIX}{} build directory migration(s) did not land: {}",
                failures.len(),
                failures.join("; ")
            );
        }
        census = Some(spotlight_after_apply(home, &scan, &outcomes));
    }
    // Once: `universal_control()` warns on stderr about a spelling it does not know, and the
    // record below asks for the policy too.
    let policy = cfg.universal_control();
    let outcome = crate::machine::apply_universal_control(policy, &crate::machine::SystemDefaults);
    if let Some(line) = record_universal_control_outcome(outcome, &mut entries) {
        println!("{line}");
    }
    if !entries.is_empty() {
        println!("atpkg: {}", machine_settings_line(&entries));
    }
    // The record follows the change, from this pass's own measurement, so a window that just
    // read the change line has the state a moment later instead of spawning a walk of `$HOME`.
    // Printed whenever the Spotlight half walked, and — with that half off — only when
    // something changed. Off macOS there is no machine to record.
    if crate::noindex::SUPPORTED
        && let Some(home) = home.as_deref()
    {
        let census = census.or_else(|| (!entries.is_empty()).then(|| spotlight_census(home)));
        if let Some(census) = census {
            print_machine_state_after_apply(cfg, policy, census);
        }
    }
    entries
}

/// Why the `[machine]` settings must NOT touch this process's machine, or `None` when
/// they may: `$HOME` (what `home_dir()` answers) is not the account's home, or the
/// account could not be resolved. The decision ([`machine_home_verdict`]) is pure over
/// its inputs so the rule is pinned by a test rather than by prose.
/// Whether this verb runs a pass that owes the machine its `[machine]` settings.
///
/// The three long passes, and `install <name>` with them: it is the other verb that
/// holds the store lock for minutes, and a user who runs it is a user whose machine
/// should come out of it configured. The quick mutators (`pin`, `gc`, `link`, …) are
/// deliberately absent — a scan of `$HOME` is up to [`crate::noindex::Budget::DOCTOR`]
/// seconds of work, and bolting it onto a verb that otherwise returns instantly would
/// be a cost the user did not ask for.
fn verb_applies_machine_settings(verb: &str) -> bool {
    matches!(verb, "install" | "seed" | "update")
}

/// The `[machine]` settings, applied AT MOST ONCE in this process.
///
/// THE LOCK IS NOT THIS WORK'S TO WAIT FOR (2026-09-15). The passes call this first
/// thing in their bodies — but the store lock is taken at the DISPATCH EDGE, above
/// every body, so until now a launch whose seed queued behind another holder waited the
/// whole `--wait-lock` bound (the window passes 30 minutes) before the host settings
/// were touched, and a pass refused outright (a wedged holder with `auto_update =
/// false`, an unwritable or symlinked prefix) never applied them at all. None of this
/// work touches the store: `defaults` writes a per-host preference domain and the
/// Spotlight migration renames directories under `$HOME`. So the edge applies them
/// BEFORE it reaches for the lock, and the bodies still ask — through this wrapper,
/// which answers with what the edge already did rather than walking `$HOME` twice.
///
/// `aterm pkg machine apply` deliberately does NOT come through here: an explicit
/// gesture must re-read and re-write the machine, not replay a cached answer.
fn apply_machine_settings_once() -> Vec<String> {
    static APPLIED: std::sync::OnceLock<Vec<String>> = std::sync::OnceLock::new();
    APPLIED.get_or_init(apply_machine_settings).clone()
}

fn machine_home_mismatch() -> Option<String> {
    machine_home().1
}

/// Why the `[machine]` settings must NOT be applied in this process, or `None`.
///
/// Two refusals, the config one first because it is the cheaper question and the one
/// the owner can fix: a config file that EXISTS and does not parse cannot tell us
/// whether either setting was switched off, and both defaults ACT. Then the
/// synthetic-home rule ([`machine_home_mismatch`]).
fn machine_refusal(cfg: &crate::config::MachineConfig) -> Option<String> {
    if cfg.unreadable {
        return Some(
            "aterm.toml does not parse, so the [machine] opt-outs could not be read — fix the \
             file (`aterm --validate-config`), then `aterm pkg machine apply`"
                .to_string(),
        );
    }
    machine_home_mismatch()
}

/// The synthetic-home rule, resolved against this process: the posture the
/// `machine-state:` line reports, and — when it is not the account's home — the one
/// sentence that says why nothing is applied.
fn machine_home() -> (crate::machine::HomePosture, Option<String>) {
    // AN EMPTY `HOME` IS NOT A HOME. `home_dir()` answers `Some("")` for `HOME=`
    // (set but empty), which renders as a blank in the refusal sentence — "HOME is
    // but this account's home is …" — and reads as a bug in the message rather than
    // the misconfiguration it is reporting.
    let home = aterm_types::dirs::home_dir().filter(|h| !h.as_os_str().is_empty());
    let account = crate::platform::account_home();
    let same = match (&home, &account) {
        (Some(h), Some(a)) => same_directory(h, a),
        _ => false,
    };
    machine_home_verdict(home.as_deref(), account.as_deref(), same)
}

/// The decision behind [`machine_home`], on its inputs. FAIL-CLOSED when `HOME` is
/// set: an account that could not be resolved is not proof that `$HOME` is it, so
/// nothing is applied — the direction the `account_home` doc promises. No `HOME` at all
/// (`home_dir()` is `None`: unset, and `/etc/passwd` — where macOS local accounts do not
/// appear — has no entry) is treated as the account's home, resolved or not: there is no
/// redirect to guard against, and the Spotlight half has no home to walk. `same` is the
/// filesystem's own answer ([`same_directory`]) for the spellings a string compare gets
/// wrong — a symlinked, case-variant or firmlinked home.
fn machine_home_verdict(
    home: Option<&std::path::Path>,
    account: Option<&std::path::Path>,
    same: bool,
) -> (crate::machine::HomePosture, Option<String>) {
    use crate::machine::HomePosture;
    let Some(home) = home else {
        return (HomePosture::Account, None);
    };
    let Some(account) = account else {
        return (
            HomePosture::Unresolved,
            Some(format!(
                "HOME is {} but this account's home could not be resolved (getpwuid_r); \
                 per-host preferences follow the account, and HOME cannot be proven to be it",
                home.display()
            )),
        );
    };
    if same {
        return (HomePosture::Account, None);
    }
    match machine_home_mismatch_between(home, account) {
        None => (HomePosture::Account, None),
        Some(reason) => (HomePosture::Mismatch, Some(reason)),
    }
}

/// Whether two paths name ONE directory — by device and inode, following symlinks —
/// so `/users/x`, a symlinked home, and the `/System/Volumes/Data/Users//x` firmlink
/// spelling (which `canonicalize` does not unify; firmlinks are not symlinks) all read
/// as the account home they are. A missing path is `false`: nothing is proven by it.
#[cfg(unix)]
fn same_directory(a: &std::path::Path, b: &std::path::Path) -> bool {
    use std::os::unix::fs::MetadataExt as _;
    match (std::fs::metadata(a), std::fs::metadata(b)) {
        (Ok(x), Ok(y)) => x.dev() == y.dev() && x.ino() == y.ino(),
        _ => false,
    }
}

#[cfg(not(unix))]
fn same_directory(_a: &std::path::Path, _b: &std::path::Path) -> bool {
    false
}

/// The decision behind [`machine_home_mismatch`], on its inputs.
fn machine_home_mismatch_between(
    home: &std::path::Path,
    account: &std::path::Path,
) -> Option<String> {
    if home == account {
        return None;
    }
    Some(format!(
        "HOME is {} but this account's home is {}; a redirected home is a synthetic \
         machine, and per-host preferences follow the account, not HOME",
        home.display(),
        account.display()
    ))
}

/// `atpkg machine [apply]` — the `[machine]` settings' own door.
///
/// Bare `atpkg machine` READS: the doctor's Universal Control line, a Spotlight line
/// that counts what a pass WOULD hide (not merely what is exposed), the byte-stable
/// [`MACHINE_STATE_MARKER`] record a window reader parses, and one verdict — `next — …`
/// naming what an apply would do, `nothing to apply`, or — on a home that is not this
/// account's — `not applied here — <why>`. A person sees the state
/// without wading through the toolchain rows; a window confirms it from the record. `atpkg machine apply` runs
/// [`apply_machine_settings`] on the spot — the exact pass every launch runs first — and
/// then says what changed, that nothing changed, or that nothing was applied and why,
/// because a verb
/// that applies and prints nothing looks broken. Exit 0 either way: this is a state, not
/// a gate. Anything else is a usage error.
fn cmd_machine(rest: &[String]) -> ExitCode {
    match rest {
        [] => {
            if !crate::noindex::SUPPORTED {
                println!("{MACHINE_VERDICT_PREFIX}not applicable — these are macOS host settings");
                return ExitCode::SUCCESS;
            }
            let cfg = crate::config::cached_machine();
            // ONCE: `universal_control()` warns on stderr about a spelling it does not
            // know, so asking twice printed the same warning twice per read.
            let policy = cfg.universal_control();
            let state = crate::machine::universal_control_state(&crate::machine::SystemDefaults);
            println!("{MACHINE_VERDICT_PREFIX}{}", state.doctor_line(policy));
            // NO HOME IS NOT AN EMPTY HOME. With `$HOME` unset (a launchd job, a `su`
            // without `-`, a sandbox) there is nothing to walk, and reporting
            // `scan=complete; spotlight-exposed=0` said the opposite of what was known:
            // it told the reader the machine had been measured and found clean. An
            // absent home is an INCOMPLETE scan — the same flag a budget-truncated walk
            // sets — so every surface that says "at least" says it here too.
            let (mut exposed, mut hidden, mut would_migrate, mut scan_complete) =
                (0usize, 0usize, 0usize, false);
            // The same rule the guard uses: `HOME=` is `Some("")`, which is not a home.
            // The same rule the guard uses, and the same fallback: `HOME=` is `Some("")`,
            // which is not a home, and with `HOME` unset the ACCOUNT's home is the one
            // this process is actually configuring (`defaults` follows it regardless).
            let walk_root = aterm_types::dirs::home_dir()
                .filter(|h| !h.as_os_str().is_empty())
                .or_else(crate::platform::account_home);
            // TWO REASONS A WALK IS INCOMPLETE, AND THEY READ DIFFERENTLY. `scan`
            // answers `complete: false` both when its root is not a readable directory
            // and when the budget ran out; only the second is "the scan hit its
            // budget", and telling a user with an unreadable home that they have a big
            // one sends them to the wrong fix.
            let root_readable = walk_root
                .as_deref()
                .is_some_and(|h| std::fs::metadata(h).is_ok_and(|m| m.is_dir()));
            if let Some(home) = walk_root.as_deref().filter(|_| root_readable) {
                let census = spotlight_census(home);
                (exposed, hidden, would_migrate, scan_complete) = (
                    census.exposed,
                    census.hidden,
                    census.would_migrate,
                    census.complete,
                );
                // What an APPLY can hide: nothing when the switch is off, and never the
                // dirs a pass refuses — a free-standing one (`aterm pkg noindex apply
                // <dir>` by name migrates it), a live build, a name already in the way.
                let can_hide = if cfg.spotlight_noindex() {
                    would_migrate
                } else {
                    0
                };
                println!(
                    "{MACHINE_VERDICT_PREFIX}{exposed} cargo target dir(s) under {} open to Spotlight \
                     ({can_hide} an apply can hide; the rest are free-standing — `aterm pkg \
                     noindex apply <dir>` by name — or refused for now: a live build, a name \
                     in the way), {hidden} hidden ([machine] spotlight_noindex = {}){}",
                    home.display(),
                    cfg.spotlight_noindex(),
                    if scan_complete {
                        ""
                    } else {
                        " — at least; the scan hit its budget"
                    }
                );
                // WHAT THE COUNT IS NOT A COUNT OF. The walk is bounded in depth and it
                // prunes the folders macOS guards with a consent dialog — an automatic
                // pass may not raise a system modal in aterm's name — so a repository in
                // `~/Documents/GitHub` is outside every number above. Said plainly, and
                // with the verb that reaches it, because the alternative is a clean bill
                // of health over a place nothing looked at.
                println!(
                    "{MACHINE_VERDICT_PREFIX}not walked: {} (macOS asks a human before a \
                     program reads those, and this pass never raises that question) — and \
                     nothing deeper than {} levels; `aterm pkg noindex scan <dir>` reads \
                     any of them by name",
                    crate::noindex::skipped_names(),
                    crate::noindex::DOCTOR_DEPTH
                );
            } else {
                // The `if` above is the whole Spotlight report; without a readable home
                // there is nothing to report and the reader must not be left to assume
                // there was.
                match walk_root.as_deref() {
                    None => println!(
                        "{MACHINE_VERDICT_PREFIX}build output was not looked at: there is no \
                         home directory to walk (HOME is unset or empty, and the account's \
                         home could not be resolved) — `aterm pkg noindex scan <dir>` reads \
                         one by name"
                    ),
                    Some(home) => println!(
                        "{MACHINE_VERDICT_PREFIX}build output was not looked at: {} is not a \
                         readable directory — `aterm pkg noindex scan <dir>` reads one by name",
                        home.display()
                    ),
                }
            }
            let (home_posture, mismatch) = machine_home();
            let st = crate::machine::MachineState {
                universal_control: state.posture(),
                policy,
                spotlight_noindex: cfg.spotlight_noindex(),
                exposed,
                hidden,
                would_migrate,
                scan_complete,
                home: home_posture,
                config_unreadable: cfg.unreadable,
            };
            // THE LINE THE WINDOW READS — after the prose, so a person still sees
            // sentences first, and after the SUPPORTED check, so its absence means "not
            // macOS" and never "nothing to apply".
            println!(
                "atpkg: {MACHINE_STATE_MARKER}{}",
                crate::machine::machine_state_line(&st)
            );
            // ONE verdict for both surfaces: `st.next()` is what the window's Apply button
            // reads too, so the CLI cannot say "nothing to apply" while a pass would rename
            // a directory — which is exactly what the first cut of this read did.
            let refusal = machine_refusal(cfg).or(mismatch);
            match (refusal, st.next()) {
                (Some(reason), _) => {
                    println!("{MACHINE_VERDICT_PREFIX}not applied here — {reason}")
                }
                // "NOTHING TO APPLY" IS A CLAIM ABOUT WHAT WAS SEEN. When the walk did
                // not finish — a budget cut it short, or there was no home to walk at
                // all — the Spotlight half of that sentence is not something this read
                // is entitled to say, so it says what it actually knows.
                // Exposed but unreachable: `[machine] spotlight_noindex = true` wants
                // these hidden and no pass can do it (free-standing, a live build, a
                // name in the way), so "where [machine] wants them" is false. The prose
                // above says which; the verdict must not contradict it.
                (None, None)
                    if scan_complete && exposed > would_migrate && cfg.spotlight_noindex() =>
                {
                    println!(
                        "{MACHINE_VERDICT_PREFIX}nothing an apply can do — {} cargo target \
                         dir(s) stay open to Spotlight (free-standing, or refused for now; \
                         the lines above say which) — `aterm pkg noindex apply <dir>` by name",
                        exposed - would_migrate
                    );
                }
                (None, None) if !scan_complete => println!(
                    "{MACHINE_VERDICT_PREFIX}nothing to apply from what was seen — Universal \
                     Control is where [machine] wants it, and the build-output scan did not \
                     finish, so there may be more"
                ),
                (None, None) => println!(
                    "{MACHINE_VERDICT_PREFIX}nothing to apply — Universal Control and Spotlight are \
                     where [machine] wants them"
                ),
                (None, Some(next)) => {
                    let mut what: Vec<String> = Vec::new();
                    if next.universal_control {
                        what.push("Universal Control off for this host".to_string());
                    }
                    if next.spotlight > 0 {
                        what.push(format!(
                            "{} target dir(s) hidden from Spotlight",
                            next.spotlight
                        ));
                    }
                    println!(
                        "{MACHINE_VERDICT_PREFIX}next — aterm pkg machine apply ({})",
                        what.join("; ")
                    );
                }
            }
            ExitCode::SUCCESS
        }
        [sub] if sub == "apply" => {
            if !crate::noindex::SUPPORTED {
                println!("{MACHINE_VERDICT_PREFIX}not applicable — these are macOS host settings");
                return ExitCode::SUCCESS;
            }
            // The guard is asked here as well as inside the apply: the apply prints the
            // refusal and returns nothing, and a verb that then said "nothing changed"
            // would contradict the line above it.
            if let Some(reason) = machine_refusal(crate::config::cached_machine()) {
                let _ = apply_machine_settings();
                println!("{MACHINE_VERDICT_PREFIX}not applied — {reason}");
                return ExitCode::SUCCESS;
            }
            // THE RAW APPLY, never the once-wrapper: `machine apply` is the explicit
            // gesture, and a person who runs it twice is asking the machine to be read
            // and written twice — not to be told what some earlier call in this process
            // found. (The edge never applies for this verb anyway: it takes no store
            // lock, so it is not in `verb_applies_machine_settings`.)
            let entries = apply_machine_settings();
            if entries.is_empty() {
                // THE CARD SHOWS THIS SENTENCE ALONE. "The lines above say which" is
                // true in a terminal and false in the window, which renders the verdict
                // without the narration — so the verdict now names the three answers
                // and points at the verb that prints the detail.
                println!(
                    "{MACHINE_VERDICT_PREFIX}nothing changed — already applied, switched off in \
                     [machine], or a change that did not land; `aterm pkg machine` lists what \
                     is still open"
                );
            } else {
                println!("{MACHINE_VERDICT_PREFIX}applied — {}", entries.join("; "));
            }
            ExitCode::SUCCESS
        }
        // NAME THE WORD THAT WAS REFUSED, as `noindex` does: a bare usage block leaves
        // the reader to diff their command against it, and `machine help` (a reasonable
        // guess) looks identical to a typo.
        other => {
            match other {
                [word] => eprintln!(
                    "atpkg machine: unknown argument {word:?} — a bare `machine` reads, \
                     `machine apply` applies"
                ),
                [] => {}
                _ => eprintln!(
                    "atpkg machine: too many arguments ({}) — a bare `machine` reads, \
                     `machine apply` applies",
                    other.len()
                ),
            }
            eprintln!("usage: {}", usage_of("machine").unwrap_or_default());
            ExitCode::from(2)
        }
    }
}

/// Whether the automatic pass narrates one [`render_applied`] line: the rows that DID
/// something, plus every skip the user has to act on. The pass log is not the place for
/// 73 routine "skipped" lines every six hours — not beside a Cargo.toml, a live build
/// (retried next pass), not applicable — nor for already-excluded rows. Any OTHER skip
/// recurs on every pass until someone acts: a crate whose cargo config cannot be pointed
/// (nothing renamed), a destination that already exists. Until 2026-09-12 those were
/// silent here forever (review of audit K11). One outcome renders one `skipped` line,
/// so each prints once per pass.
fn noindex_pass_prints(line: &str) -> bool {
    if line.starts_with("migrated ")
        || line.starts_with("FAILED ")
        || line.starts_with("the walk stopped at its budget")
        || line.starts_with("  cargo")
        || line.starts_with("  git")
    {
        return true;
    }
    let Some(skip) = line.strip_prefix("skipped ") else {
        return false;
    };
    let Some((_, reason)) = skip.split_once(" — ") else {
        return true;
    };
    !(reason.starts_with("not beside a Cargo.toml")
        || reason.starts_with("not applicable")
        || (reason.starts_with("a build holds ") && reason.ends_with("retried next pass")))
}

/// The `managed-current:` payload ([`MANAGED_CURRENT_MARKER`]) for `entries` —
/// `(name, version, build)` per agent program that is installed AND at the index pin, in
/// roster order — as `claude 2.1.267 (build 2026091001); codex 0.154.0 (build 2026091001)`.
/// A version the signed manifest could not be fetched for is left out of its entry
/// (`claude (build 2026091001)`) rather than guessed. `None` when no entry qualifies: the
/// line is then omitted, so its presence means exactly "the managed agents are current".
fn managed_current_line(entries: &[(String, Option<String>, u64)]) -> Option<String> {
    if entries.is_empty() {
        return None;
    }
    let parts: Vec<String> = entries
        .iter()
        .map(|(name, version, build)| match version {
            Some(v) => format!("{name} {v} (build {build})"),
            None => format!("{name} (build {build})"),
        })
        .collect();
    Some(format!("{MANAGED_CURRENT_MARKER}{}", parts.join("; ")))
}

/// Whether `agents/<name>` — the copy every aterm tab runs (owner decision 2026-09-10) —
/// is laid and resolves exactly where `bin/<name>` does, into `store/<name>/<build>`.
/// The predicate behind the `managed-current:` line's honesty and the agent programs'
/// shadow wording (2026-09-16).
pub(crate) fn agent_twin_current(layout: &crate::store::Layout, name: &str, build: u64) -> bool {
    let Some(tool) = crate::store::ToolName::new(name) else {
        return false;
    };
    let Some(target) = crate::platform::resolve_shim(&layout.shim(&tool)) else {
        return false;
    };
    if !crate::ops::store_build_of(&layout.prefix, &target)
        .is_some_and(|(p, b)| p == name && b == build)
    {
        return false;
    }
    crate::platform::resolve_shim(&layout.agent_shim(&tool)).is_some_and(|t| t == target)
}

/// [`agent_twin_current`] AND the twin's bytes carry the self-update hand-over
/// ([`twin_carries_hand_over`]) — the predicate behind the `which` sentence `` `claude
/// update` here runs `aterm pkg update claude` `` (2026-09-19). Target equality is not
/// enough: a twin laid by an older client resolves exactly where `bin/` does and still
/// runs the vendor's updater on `claude update` until a pass re-lays it (the dev box's
/// own `agents/claude`, laid by the installed app, carried only the landing block when
/// this was reviewed), and the `.cmd` twin resolves the same and carries no block at
/// all ([`crate::platform::cmd_selfupdate_prelude`] renders the empty string, pinned).
/// One bounded, non-link regular read; anything unreadable answers `false`, never a
/// sentence.
pub(crate) fn agent_twin_intercepts(layout: &crate::store::Layout, name: &str, build: u64) -> bool {
    if !agent_twin_current(layout, name, build) {
        return false;
    }
    let Some(tool) = crate::store::ToolName::new(name) else {
        return false;
    };
    crate::metadata_io::read_bounded_regular(
        &layout.agent_shim(&tool),
        crate::platform::MAX_SHIM_BYTES,
    )
    .is_ok_and(|bytes| twin_carries_hand_over(&bytes))
}

/// Whether a laid twin's bytes carry the hidden verb's hand-over: the word
/// [`crate::selfupdate::HIDDEN_VERB`] appears nowhere in a shim this crate lays EXCEPT
/// the `sh` self-update block (`bin/` carries neither block — pinned by the activate
/// twin test — and the `.cmd` twin renders none). Pure, so the Windows bytes are pinned
/// from the Unix suite like every `.cmd` test.
fn twin_carries_hand_over(bytes: &[u8]) -> bool {
    bytes
        .windows(crate::selfupdate::HIDDEN_VERB.len())
        .any(|w| w == crate::selfupdate::HIDDEN_VERB.as_bytes())
}

/// The pass log's tail for an agent program with a foreign copy ahead on the PASS's own
/// `PATH`: `a foreign copy is ahead on this pass's PATH (<path>); a tab opened on this
/// build puts agents/ first on its own`. A fact about this process, not about any shell a user is in.
fn pass_path_tail(foreign: &std::path::Path) -> String {
    let mut s = String::from("a foreign copy is ahead on this pass's PATH (");
    s.push_str(&foreign.display().to_string());
    s.push_str("); a tab opened on this build puts agents/ first on its own");
    s
}

/// The in-place remedy for a shell whose `PATH` lacks `agents/` (or has it behind a
/// foreign copy): SOURCE THE HOOK WHERE IT STANDS — `. ~/.aterm/shell.d/00-atpkg.zsh`
/// (`. …bash`; `source …fish` for fish; the dot-source `. …ps1` for pwsh) — whenever
/// that hook file exists for the INVOKING shell's family ([`crate::hooks::hook_file`]);
/// the same sentence the window's status row says for a tab from before the update. Only
/// where no hook file exists (a shell atpkg writes no hook for, an install whose hook
/// write failed, no shell/`HOME` known at all) is a `PATH` line printed instead — in the
/// shell's own dialect (`export PATH="<agents>:$PATH"`, fish's `set -gx PATH …`, pwsh's
/// `$env:PATH = …`), so what is printed runs where it is typed.
///
/// THE SHELL IS THE ONE THAT TYPED THIS, not `$SHELL` ([`crate::caller_shell`],
/// 2026-09-16): aterm spawns the configured shell without re-pointing `$SHELL`, so a fish
/// tab on a zsh-login machine was told the zsh line "here picks the managed copy up" —
/// one fish cannot source — while the status row, keyed on the spawn shell, said the fish
/// line in the same window (review finding). The parent process is the shell that typed
/// the command (an `exec`'d shim keeps it); `$SHELL` answers only when the parent is not
/// a shell (`sudo`, a script driver).
///
/// NEVER `exec $SHELL` (2026-09-16). The first cut said `exec $SHELL` wherever an rc
/// sourced the hook, and that was measured wrong on the owner's machine the same day:
/// inside an aterm tab a re-exec'd shell LOSES the tab's shell integration —
/// `aterm-shell-integration`'s zsh wrapper restores or unsets `ZDOTDIR` and consumes
/// `ATERM_ORIGINAL_ZDOTDIR` before sourcing, so the new zsh never loads
/// `aterm_shell_integration.zsh` (bash rides `--rcfile`, same loss): no OSC 133/633
/// marks, no cwd/title tracking, no live `PATH` re-assert. Sourcing the hook in the live
/// shell is the whole remedy and keeps everything the tab has — measured in the frozen
/// shell: `which claude` → `~/.local/bin/claude` before; after `. ~/.aterm/shell.d/
/// 00-atpkg.zsh` → `<prefix>/agents/claude`, `ATPKG_AGENTS` exported, `PATH` head =
/// `agents/`. Outside a session (Terminal.app, ssh) the same source works — it is what
/// the rc block runs — and is cheaper than a re-exec that re-reads every rc, so it is the
/// ONE remedy everywhere, wired rc or not. Owner, 2026-09-16: *"all the latest and best
/// MUST WORK IN THE SAME TAB with live update"* — never a new tab, never a shell that
/// forgets it is an aterm tab.
pub(crate) fn shell_remedy_command(layout: &crate::store::Layout) -> String {
    let shell = crate::caller_shell::invoking_shell();
    let home = aterm_types::dirs::home_dir();
    shell_remedy_for(shell.as_deref(), home.as_deref(), layout)
}

/// [`shell_remedy_command`] over an explicit `shell` (the invoking shell's name —
/// `zsh`, `bash`, `fish`, `pwsh`) and `home`, so a test can lay the hook under a
/// throwaway home and read the exact line.
pub(crate) fn shell_remedy_for(
    shell: Option<&str>,
    home: Option<&std::path::Path>,
    layout: &crate::store::Layout,
) -> String {
    let hook = match (shell, home) {
        (Some(sh), Some(home)) => crate::hooks::hook_file(home, sh).map(|(_, hook)| hook),
        _ => None,
    };
    if let Some(hook) = hook {
        let mut s = String::from(if hook.ends_with(".fish") {
            "source "
        } else {
            ". "
        });
        s.push_str("~/.aterm/shell.d/");
        s.push_str(hook);
        return s;
    }
    let agents = layout.agents_dir().display().to_string();
    let mut s = String::new();
    // (Arms over the bare name, not `Some("…") =>`: `verb_hint_matches_dispatch`
    // scrapes this file for that shape as the verb dispatch table.)
    match shell.unwrap_or_default() {
        "fish" => {
            s.push_str("set -gx PATH \"");
            s.push_str(&agents);
            s.push_str("\" $PATH");
        }
        "pwsh" | "powershell" => {
            s.push_str("$env:PATH = \"");
            s.push_str(&agents);
            s.push_str(if cfg!(windows) { ";" } else { ":" });
            s.push_str("$env:PATH\"");
        }
        _ => {
            s.push_str("export PATH=\"");
            s.push_str(&agents);
            s.push_str(":$PATH\"");
        }
    }
    s
}

/// The AGENT PROGRAMS' shadow row for ONE shell (2026-09-16): `Some` when `program` is an
/// agent program whose `agents/` twin is laid and current — so aterm's copy is what
/// every tab runs, and `foreign` ahead of it on `path_var` is this shell's state, not
/// the machine's ([`crate::state::agent_shadowed_in_shell`]). `None` for every other
/// program: their `managed <build> — SHADOWED by <path>` row is unchanged.
pub(crate) fn agent_shell_shadow(
    layout: &crate::store::Layout,
    program: &str,
    build: u64,
    foreign: &std::path::Path,
    path_var: Option<&std::ffi::OsStr>,
) -> Option<String> {
    if !crate::stub::is_agent_program(program) || !agent_twin_current(layout, program, build) {
        return None;
    }
    Some(crate::state::agent_shadowed_in_shell(
        build,
        foreign,
        &layout.agents_dir(),
        crate::vendor::agents_dir_on_path(&layout.prefix, path_var),
        &shell_remedy_command(layout),
    ))
}

/// The entries behind [`managed_current_line`]: every [`crate::stub::AGENT_PROGRAMS`]
/// member whose active build equals the channel's pin, with the version its SIGNED
/// pkg manifest states (fetched through the pass's memoized fetcher and verified under
/// the index, as the install did — never a locally recorded string).
fn managed_current_entries(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    index: &crate::TrustedIndex,
    channel: &str,
) -> Vec<(String, Option<String>, u64)> {
    let Some(ch) = index.channel_for(channel, current_triple()) else {
        return Vec::new();
    };
    let ch = &ch;
    let installed = crate::active_builds(layout);
    crate::stub::AGENT_PROGRAMS
        .iter()
        .filter_map(|name| {
            let build = *installed.get(*name)?;
            if ch.pin.get(*name).copied() != Some(build) {
                return None;
            }
            // HONEST (2026-09-16): the row this line becomes says the managed copies are
            // what every tab runs — true only when the front-of-PATH twin really IS this
            // build. A pass whose twin was not laid (agents/ unmakeable, a lane that
            // failed) or is still on the previous build says nothing here.
            if !agent_twin_current(layout, name, build) {
                return None;
            }
            let version = crate::flow::verified_pkg(fetcher, index, ch, name)
                .filter(|(pinned, _, pkg)| *pinned == build && pkg.is_for(name))
                .map(|(_, _, pkg)| pkg.version);
            Some(((*name).to_string(), version, build))
        })
        .collect()
}

/// The pass-end PROVENANCE line (2026-09-15): every active build whose `bin/` or `lib/`
/// carries `com.apple.provenance` is named on one `tracked-install:`-shaped marker, so a
/// bundle tagged AFTER it was installed — the 0.86.0 view re-tagging `trust/8595` on
/// every pass, a stale twin — reaches the window as a row, not only `doctor`. macOS
/// only, active builds only, one line per pass; silent when nothing carries the tag.
fn print_provenance_carriers(layout: &crate::store::Layout) {
    if !cfg!(target_os = "macos") {
        return;
    }
    let mut carriers: Vec<String> = Vec::new();
    for (program, build) in crate::active_builds(layout) {
        let build_dir = layout.build_dir(&program, build);
        let bin = crate::provenance::tagged_files_in(
            &build_dir.join("bin"),
            crate::provenance::PROVENANCE_XATTR,
        );
        let lib = crate::provenance::tagged_files_under(
            &build_dir.join("lib"),
            crate::provenance::PROVENANCE_XATTR,
        );
        let tagged = bin.carriers.len() + lib.carriers.len();
        if tagged > 0 {
            carriers.push(format!(
                "{program} build {build} ({tagged} of {} file(s))",
                bin.total + lib.total
            ));
        }
    }
    if carriers.is_empty() {
        return;
    }
    println!(
        "atpkg: {}{} — carries com.apple.provenance; `aterm pkg doctor` names the cause \
         and the re-seed",
        TRACKED_INSTALL_MARKER,
        carriers.join(", ")
    );
}

/// Print the `managed-current:` marker at the end of a SUCCESSFUL pass (R6: the
/// pull-down row "the latest aterm-managed claude/codex are in use"). The index resolve
/// is memoized in the fetcher, so this costs no second round-trip; a resolve that
/// nevertheless fails prints nothing rather than a guess.
fn print_managed_current(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    cfg: &crate::config::PackagesConfig,
) {
    let Ok(index) = crate::resolve_verified_index(
        fetcher,
        layout,
        &effective_anchor(layout),
        build_floor(layout),
        now_unix(),
    ) else {
        return;
    };
    if let Some(line) = managed_current_line(&managed_current_entries(
        layout,
        fetcher,
        &index,
        cfg.channel(),
    )) {
        println!("atpkg: {line}");
    }
}

/// The POSITIVE terminal: an announced pass that ran to its end and has no richer
/// marker of its own to print.
///
/// Until this existed the contract had only FAILURE terminals (`seed-failed:`,
/// `seed-partial:`, `seed-unusable:`, `net-failed:`) plus the two install rosters,
/// so a pass that announced itself and then ended well through a path with no
/// roster to name was SILENT — and silence is the one thing the reader of these
/// markers cannot safely interpret. The GUI had to infer success from the ABSENCE
/// of a terminal line, which is exactly the reading the 2026-08-20 round-9 audit
/// proved unsafe (a child that DIES after announcing is also silent), so it read
/// the absence as a failure and put "⚠ ALab toolchain install failed" over a clean
/// run. An absence now means one thing only — nobody answered — and every ending
/// has a line of its own.
pub const SEED_DONE_MARKER: &str = "seed-done: ";

/// ANOTHER `atpkg` IS ALREADY DOING THIS WORK. Printed when the store lock is
/// CONTENDED at the dispatch edge — never for any other refusal — and only to a
/// caller that opted into `--wait-lock` and whose parent is still there (the GUI's
/// machine lanes, exactly like the `lock-waiting:` line): a typed `aterm pkg` verb is
/// told on stderr and keeps its stdout silent, and the contention exit code
/// ([`crate::lock::CONTENDED_EXIT`], 75) says the same thing to every caller. That
/// code, not this line, is what the lanes key their "deferred, never failed" verdict
/// on; the line is the pass's own words for their log. ONE caller that is not a
/// machine lane opts in too (2026-09-19): the self-update intercept's child
/// ([`cmd_selfupdate`], `update <program> --wait-lock 1800` with stdio inherited), so a
/// person who typed `claude update` while a pass holds the store sees the
/// `lock-waiting:` line on their terminal's stdout, `lock-acquired:` when the holder
/// lets go — and, only when the whole 30 minutes run out, this line, then the
/// intercept's own contended line on stderr — measured, and pinned in-process by
/// `cmd_selfupdate_refuses_forwards_and_never_takes_the_lock` (the 75 lane, a 1 s
/// bound) and at the edge by `tests/selfupdate_intercept.rs` (the wait that ends in the
/// lock).
///
/// This is the line the owner's 2026-09-11 banner needed and did not have. Two aterm
/// processes launched sixteen seconds apart; the second one's `seed` and `update`
/// children both lost the lock to the first one's, exited non-zero and printed no
/// marker, and the GUI's refusal branch — correct for an unwritable prefix, correct
/// for a bundle whose atpkg cannot exec — put "⚠ ALab toolchain install failed" on a
/// machine where the install was at that moment RUNNING in the other process.
///
/// Contention is a fact about this process's timing, not about the toolchain: it is
/// transient, self-healing, and the work is being done. Saying so costs one line and
/// removes a warning nobody can act on.
pub const SEED_BUSY_MARKER: &str = "seed-busy: ";

/// THE ANNOUNCEMENT LEDGER — the marker contract's other half, kept by the process
/// instead of by each verb's exits.
///
/// `seed-starting:` / `net-starting:` OPEN a held "Installing the ALab toolchain…"
/// card in the GUI; every other marker CLOSES it. Nothing enforced that a verb which
/// opened one ever closed it: `cmd_install_default_set` announces over the network
/// and then ends through four separate `return`s, none of which prints a marker, so
/// the card's only possible answer was the reader's guess (2026-09-11).
///
/// The bookkeeping lives where a marker reaches stdout ([`emit_marker_line`], and
/// its one lossy twin [`emit_marker_line_lossy`] for the waiter's `seed-busy:` line)
/// rather than at each print site, so a lane that grows a new early return cannot
/// forget to answer — answering is not its job.
pub(crate) mod announcement {
    use std::sync::atomic::{AtomicBool, Ordering};

    static OPENED: AtomicBool = AtomicBool::new(false);
    static ANSWERED: AtomicBool = AtomicBool::new(false);

    /// Every marker in the stdout contract, and whether it OPENS an announcement
    /// rather than answering one. The GUI's `parse_seed_line` matches the same set;
    /// this table is what keeps the two halves from drifting.
    ///
    /// `seed-pending:` is an OFFER, printed only on the lane that announces nothing,
    /// so classifying it as an answer costs nothing and keeps the table total.
    ///
    /// `lock-waiting:` ([`super::LOCK_WAITING_MARKER`]) is deliberately ABSENT: it is
    /// printed at the dispatch edge, before any verb, and is neither half of the
    /// contract — not an announcement (nothing to retire) and not an answer (were it
    /// one, a child that waited and then announced would look answered before it
    /// began). [`note`] ignores an unlisted prefix, which is the right reading of it.
    /// So is `lock-acquired:` ([`super::LOCK_ACQUIRED_MARKER`]), printed at the same
    /// edge when that wait ends in the lock: it answers the wait, not an announcement.
    pub(crate) const MARKERS: &[(&str, bool)] = &[
        (super::SEED_STARTING_MARKER, true),
        (super::NET_STARTING_MARKER, true),
        (super::SEED_INSTALLED_MARKER, false),
        (super::SEED_PENDING_MARKER, false),
        (super::SEED_UNUSABLE_MARKER, false),
        (super::SEED_FAILED_MARKER, false),
        (super::SEED_PARTIAL_MARKER, false),
        (super::SEED_DONE_MARKER, false),
        (super::SEED_BUSY_MARKER, false),
        (super::NET_INSTALLED_MARKER, false),
        (super::NET_FAILED_MARKER, false),
    ];

    /// Record what a marker line just said. `line` is the whole stdout line,
    /// `atpkg: ` prefix and all — the same bytes the GUI parses.
    pub(crate) fn note(line: &str) {
        let Some(rest) = line.strip_prefix("atpkg: ") else {
            return;
        };
        for (marker, opens) in MARKERS {
            if rest.starts_with(marker) {
                if *opens {
                    OPENED.store(true, Ordering::Release);
                } else {
                    ANSWERED.store(true, Ordering::Release);
                }
                return;
            }
        }
    }

    /// Whether this process announced a pass and has not yet answered it.
    pub(crate) fn is_open() -> bool {
        OPENED.load(Ordering::Acquire) && !ANSWERED.load(Ordering::Acquire)
    }

    /// Test seam: a process-global ledger would otherwise carry one test's
    /// announcement into the next.
    #[cfg(test)]
    pub(crate) fn reset() {
        OPENED.store(false, Ordering::Release);
        ANSWERED.store(false, Ordering::Release);
    }
}

/// THE way a marker reaches stdout: print it, and record it against the
/// announcement ledger. The bytes are unchanged — `line` is already the whole
/// `atpkg: <marker><body>` line the GUI parses — so this adds bookkeeping and
/// nothing else. `println!` on purpose: an INSTALLING child whose window has quit
/// is meant to die at its next line of output (aterm-gui's final-exit path in its
/// `main_entry`, 2026-09-10), and a marker is one of those lines. A WAITER's
/// `seed-busy:` goes through [`emit_marker_line_lossy`] — the same printer with the
/// panic taken out — and its `lock-waiting:` announcement is a bare write with the
/// error ignored, in [`mutator_store_lock`]'s wait (a marker the ledger does not list).
fn emit_marker_line(line: &str) {
    println!("{line}");
    announcement::note(line);
}

/// [`emit_marker_line`] for the `--wait-lock` waiter's `seed-busy:` line: the same
/// bytes and the same ledger entry, but a write whose failure is ignored. A waiter's
/// contract is a SILENT stand-down with exit 75 once its window has gone, and the
/// [`parent_is_gone`] guard at the lock edge is a snapshot — a window that quits
/// between it and this print leaves a dead pipe for a stdout, where `println!`
/// would panic (exit 101) and an `EPIPE` nobody sees is the promised outcome.
fn emit_marker_line_lossy(line: &str) {
    use std::io::Write as _;
    let _ = writeln!(std::io::stdout(), "{line}");
    announcement::note(line);
}

/// ANSWER AN ANNOUNCEMENT THIS VERB LEFT OPEN. Called at the tail of every verb that
/// can announce; a no-op for the lanes that already printed a terminal of their own,
/// which is why their stdout is byte-unchanged.
///
/// `ok` is the verb's OWN verdict, not a guess from what was printed. That matters:
/// the reader needs a failed pass to say so, and an absence cannot distinguish
/// "finished quietly" from "died". A verb that announced and then failed without a
/// marker gets the failure terminal; one that announced and succeeded gets the
/// positive one, carrying the STORE's own count — a fact about what is installed
/// rather than a claim about what this pass did.
fn answer_announcement(ok: bool) {
    if !announcement::is_open() {
        return;
    }
    if ok {
        let held = layout().map_or(0, |l| crate::active_builds(&l).len());
        if held == 0 {
            // EXIT 0 OVER AN EMPTY STORE IS NOT A SUCCESS TO REPORT. The pass
            // announced an install and the disk shows none — a positive fact, and a
            // better witness than the exit code, which says only that no step
            // errored. Reporting the tick here is how a green "toolchain installed"
            // lands over an empty prefix.
            emit_marker_line(&format!(
                "atpkg: {SEED_FAILED_MARKER}the pass finished without installing \
                 anything — see the lines above for each program's reason"
            ));
            return;
        }
        emit_marker_line(&format!(
            "atpkg: {SEED_DONE_MARKER}the pass finished; {held} ALab program(s) are installed"
        ));
    } else {
        emit_marker_line(&format!(
            "atpkg: {SEED_FAILED_MARKER}the pass ended without installing what it announced — \
             see the lines above for the reason"
        ));
    }
}

/// The human line printed on its own row AFTER an install marker (`seed-installed:` /
/// `net-installed:`) — never appended to the marker itself, which the GUI parses
/// byte-for-byte. Answers the first question a fresh install raises: how do I run one?
const SEED_FOLLOW_ON: &str = "atpkg: these tools are on PATH in every aterm shell — \
                              aterm pkg list shows them; aterm <tool> runs one anywhere";

/// WHETHER THE 6-HOUR PASS MAY COMPLETE THE SET — the whole consent policy, as one
/// pure function so it can be tested.
///
/// Four inputs, and the precedence between them is the part that keeps going wrong:
///
/// * `declined` — the user removed the toolset on purpose. It outranks everything,
///   including `auto_install`, because it is the later and more specific act. Checked
///   HERE and not only in the seed lane: the network pass is what the loop runs, so a
///   decline honoured locally while this pass reinstalled the set worked for exactly
///   one launch.
/// * `adopted` — this machine runs the toolset (installing aterm is wanting it), so
///   newly published members should arrive without asking again.
/// * `auto_install` — explicit consent to pull the set over the network onto a machine
///   that has none.
/// * `config_unreadable` — the `[packages]` table did not parse
///   ([`crate::config::PackagesConfig::unreadable_table`]). It outranks `adopted` and
///   `auto_install` for one reason: this lane is narrowed by `[packages].exclude`, and
///   an unreadable table has no exclusions to narrow it WITH. Completing the set on
///   those terms reinstalls, over the network, precisely the program the owner excluded
///   — the reinstall `exclude` was wired into this pass to prevent. The pass still
///   applies updates to what is installed; only the lane that installs something NEW
///   waits for the file to be fixed.
fn should_complete_set(
    auto_install: bool,
    adopted: bool,
    declined: bool,
    config_unreadable: bool,
) -> bool {
    !declined && !config_unreadable && (auto_install || adopted)
}

/// Whether this machine has DECLINED the bundled toolset
/// ([`crate::store::Layout::declined`]).
fn declined(layout: &crate::store::Layout) -> bool {
    layout.declined().is_file()
}

/// Record a decline, so the seed lane stops re-installing what the user removed.
fn record_decline(layout: &crate::store::Layout) {
    let path = layout.declined();
    if path.is_file() {
        return;
    }
    match crate::platform::open_create_write(&path, 0o600) {
        Ok(mut f) => {
            use std::io::Write as _;
            let _ = writeln!(
                f,
                "# This machine declined the bundled ALab toolset.\n\
                 # `atpkg seed` will not re-install it (without this, the next launch would\n\
                 # simply put back everything `uninstall --all` just removed).\n\
                 # Removed automatically by any explicit install."
            );
        }
        Err(e) => eprintln!(
            "atpkg: could not record the decline at {}: {e} — the bundled seed may reinstall \
             on the next launch; set [packages].seed_install = false in aterm.toml to be sure",
            path.display()
        ),
    }
}

/// Forget a decline — any EXPLICIT install is an unambiguous change of mind, and undoing
/// one must never require finding a marker file.
fn clear_decline(layout: &crate::store::Layout) {
    let path = layout.declined();
    if path.is_file() {
        let _ = std::fs::remove_file(&path);
    }
}

/// Record each freshly-installed `requires` dependency of `parent` into `status.toml`
/// (each carries its own SIGNED tree_root for `verify`). Shared by [`do_install`] and the
/// default-set bootstrap's ungrouped arm — identical writes at both.
fn record_installed_deps(
    layout: &crate::store::Layout,
    parent: &str,
    index_build: u64,
    deps: &[crate::flow::DepOutcome],
) {
    for dep in deps {
        match &dep.result {
            crate::flow::DepResult::Installed { build, tree_root } => record_status(
                layout,
                &dep.program,
                crate::ProgramStatus {
                    installed_build: Some(*build),
                    state: crate::state::managed(*build, index_build),
                    tree_root: tree_root.clone(),
                },
                format!("pulled in {} (required by {parent})", dep.program),
            ),
            // An OS-installed dependency (clt under brew) carries its own canonical
            // row — `installed via …` or `needs admin` — written only when it changes,
            // so a pass that re-proves it says nothing.
            crate::flow::DepResult::Protocol(outcome) => {
                record_state_if_changed(layout, &dep.program, outcome.state(&dep.program));
            }
            // A dependency the user's own copy satisfies: the canonical `system:` row,
            // dated when a managed copy was retired for it earlier.
            crate::flow::DepResult::System(path) => {
                let retired = layout.retired_date(&dep.program);
                record_state_if_changed(
                    layout,
                    &dep.program,
                    crate::state::system(path, retired.as_deref()),
                );
            }
            crate::flow::DepResult::AlreadyPresent(_) | crate::flow::DepResult::Skipped(_) => {}
        }
    }
}

/// Record an OS-installer lane's outcome as `program`'s row (no build, no root — nothing
/// of it lives in the store) and say it in the pass log; the aggregate sentence is the
/// state itself. Shared by every verb that can meet a [`crate::ProtocolOutcome`].
fn record_protocol_outcome(
    layout: &crate::store::Layout,
    program: &str,
    outcome: &crate::ProtocolOutcome,
) -> String {
    let state = outcome.state(program);
    record_status(
        layout,
        program,
        crate::ProgramStatus {
            installed_build: None,
            state: state.clone(),
            tree_root: String::new(),
        },
        format!("{program}: {state}"),
    );
    state
}

/// Install/force-upgrade one program to its `channel`-pinned build, advancing the durable
/// anti-rollback floor and recording status. Shared by `install` and `update`; `channel`
/// is the config-resolved `[packages].channel` (default `stable`) threaded from the verb
/// edge so this stays testable without process state.
fn do_install(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    channel: &str,
    program: &str,
) -> Result<crate::InstallReport, crate::FlowError> {
    // The ACTIVE build (what the shim points at), not the max COMPLETE build on disk — a
    // staged-but-never-activated build equal to the pin must not make this report
    // up-to-date while the user keeps running the older active build (#19).
    let installed = crate::active_builds(layout).get(program).copied();
    let floor = build_floor(layout);
    let req = crate::InstallRequest {
        channel,
        program,
        triple: current_triple(),
        installed,
    };
    // THE EXPLICIT DOOR FORGETS A DIGEST REFUSAL. An unattended pass that met a pinned
    // build whose signed `sha256` the published asset does not match records
    // that verdict and stops re-downloading the asset on every six-hourly tick
    // (`store::record_stage_refusal`). A person typing this verb is the one event that can
    // mean "the publish — or the proxy that mangled the transfer — is fixed, try now", so
    // this door drops the memo and pays for the download again. Only the verbs a person
    // types reach here (`install <p>`, and `update <p>` on a program that is not yet
    // installed); the loop's own lanes go through `flow::install`/`apply_channel` and keep
    // the cooldown.
    crate::store::clear_stage_refusals(layout, program);
    // `program → pinned asset name` for everything this pass resolves (the program AND its
    // `requires` pull-ins) — filled even for a member whose download then fails, which is
    // what lets the pass-end gc below spare that member's `.part` resume state.
    let mut resolved_assets = std::collections::BTreeMap::new();
    let result = crate::flow::install_collecting_assets(
        fetcher,
        layout,
        &effective_anchor(layout),
        &req,
        floor,
        now_unix(),
        &mut resolved_assets,
    );
    match &result {
        Ok(r) => {
            // Advance the durable high-water floor to the index we just trusted (§8 gate 3).
            advance_floors(layout, r.index_build, r.roster_seq);
            // Record each pulled-in dependency FIRST (§17), so the main program's outcome
            // remains the final aggregate.
            record_installed_deps(layout, program, r.index_build, &r.dependencies);
            // An OS-installed member: its row is the lane's canonical state, nothing was
            // staged, and there is no store to sweep or hook to refresh for it.
            if let Some(outcome) = &r.protocol {
                record_protocol_outcome(layout, program, outcome);
                return result;
            }
            let outcome = if r.already_current {
                // THE SAME QUALIFICATION THE GROUP ARM MAKES, and it is RECORDED here
                // rather than merely printed: this string is `status.toml`'s outcome, the
                // detail line Settings ▸ Packages shows, and the sentence a person reads
                // when they ask why nothing happened. "up to date" about a build the
                // machine does not run is the answer to a question nobody asked.
                match crate::seam::dissent_if_armed(layout, program) {
                    Some(why) => format!(
                        "up to date ({program} build {}) — but not what this machine runs: {why}",
                        r.build
                    ),
                    None => format!("up to date ({program} build {})", r.build),
                }
            } else {
                format!("installed {program} build {}", r.build)
            };
            record_status(
                layout,
                program,
                crate::ProgramStatus {
                    installed_build: Some(r.build),
                    state: crate::state::managed(r.build, r.index_build),
                    // Preserve the recorded signed root when this was a no-op (already
                    // current): an empty root would wipe the attestation `atpkg verify` needs.
                    tree_root: effective_tree_root(layout, program, &r.tree_root),
                },
                outcome,
            );
            // Reclaim superseded builds after a real activation (best-effort; never fails the
            // install). Runs at the CLI edge — not inside flow.rs — so flow's unit tests
            // stay hermetic w.r.t. the developer's real store. Satisfies "GC after every
            // successful activate".
            //
            // The report is dropped ON PURPOSE here, unlike in the prefix-wide verbs. This
            // pass sweeps the whole store, but the program THIS verb just activated is
            // freshly linked and freshly shimmed, so it is witnessed; any abstention is about
            // some other program the user did not ask about. Repeating that on every single
            // install is how a warning becomes wallpaper — `atpkg gc` and `atpkg doctor` are
            // the verbs whose job is to say it.
            //
            // The SPARING form, not plain `run`: this pass holds the verified pinned asset
            // name for everything it resolved, and a `requires` pull-in whose download
            // failed (the main install still succeeds — deps are best-effort) has a `.part`
            // here that plain `run`'s staging sweep would destroy, refetching the dep from
            // byte 0 next pass. Programs outside the map are swept exactly as before.
            if !r.already_current {
                let _ = crate::gc::run_keeping_pinned_partials(layout, &|p| {
                    resolved_assets.get(p).cloned()
                });
            }
            // Refresh the interactive-shell PATH hook (append-not-prepend, §16). At the CLI
            // edge — not inside flow.rs — so flow's synthetic-layout tests never write the
            // real ~/.aterm. Best-effort; writes OUTSIDE the hashed store tree.
            if !r.already_current {
                crate::hooks::refresh(layout);
            }
        }
        Err(e) => match failed_install_state(e) {
            Some(state) => record_status(
                layout,
                program,
                failure_row(layout, program, state),
                format!("install {program}: {e}"),
            ),
            // The attempt is still WITNESSED — it just is not witnessed as a program.
            None => record_outcome(layout, format!("install {program}: {e}")),
        },
    }
    result
}

/// The `install` verb's argv: ONE program name (or `--default-set`) plus an optional
/// `--elevate=sudo | osascript | never`, in either order. The flag names how an
/// OS-installed member (`pkg`, `softwareupdate`) may be elevated on THIS invocation:
/// `sudo` (the terminal door's default when stdin is a TTY), `osascript` (the GUI's
/// administrator dialog — the button's spelling), `never` (record the state, prompt
/// nothing). Absent ⇒ the TTY rule ([`crate::elevate::door_elevation`]). The flag
/// belongs to the ONE-PROGRAM form alone: the grammar is decided by
/// [`parse_install_argv`] before anything dispatches.
fn cmd_install_argv(rest: &[String]) -> ExitCode {
    match parse_install_argv(rest) {
        Ok((program, explicit)) => cmd_install_elevated(program, explicit, "install"),
        Err(refusal) => refusal,
    }
}

/// [`cmd_install_argv`]'s grammar: the one operand and the explicit elevation, or the
/// usage EXIT of a refusal this function has already narrated on stderr.
///
/// SPLIT FROM THE VERB so the grammar is decidable without dispatching. The
/// `--default-set` route applies this machine's `[machine]` settings (Universal Control,
/// a migrating walk of `$HOME`) and then reaches for the network, so a test that called
/// the verb to learn what an argv MEANS would do all of that to find out.
///
/// The refusals carry a built `ExitCode`, not a `u8` the caller converts: every exit
/// code built in this file must be a literal or one of the two named relays, which is
/// what lets `contention_exit_code_is_temp_fail_and_unshared` enumerate the codes the
/// CLI can exit with and prove none of them is contention's 75. That scan reads this
/// file as TEXT, so the sentence above may not spell the constructor it looks for.
fn parse_install_argv(
    rest: &[String],
) -> Result<(Option<&String>, Option<crate::Elevation>), ExitCode> {
    let mut explicit: Option<crate::Elevation> = None;
    let mut operands: Vec<&String> = Vec::new();
    for arg in rest {
        if let Some(mode) = arg.strip_prefix("--elevate=") {
            explicit = Some(match mode {
                "sudo" => crate::Elevation::Sudo,
                "osascript" => crate::Elevation::Osascript,
                "never" => crate::Elevation::Deferred,
                other => {
                    eprintln!(
                        "atpkg install: --elevate={other:?} is not one of sudo | osascript | never"
                    );
                    return Err(ExitCode::from(2));
                }
            });
        } else {
            operands.push(arg);
        }
    }
    if operands.len() > 1 {
        eprintln!(
            "atpkg install: one program at a time (got {}); the whole toolset is \
             `aterm pkg install --default-set`",
            operands.len()
        );
        return Err(ExitCode::from(2));
    }
    // AN ELEVATION THE WHOLE SET COULD NEVER APPLY IS REFUSED, NOT DROPPED. The pair
    // parsed cleanly and then vanished: [`cmd_install_elevated`] returns into
    // `cmd_install_default_set()` at its `--default-set` branch, ABOVE the one
    // `set_elevation(door_elevation(explicit, …))` call it makes — so `aterm pkg install
    // --default-set --elevate=osascript` exited 0 with the pass still Deferred, every
    // OS-installed member recorded `needs admin — run: aterm pkg install <name>`, and not
    // a word about the flag that had just asked for the administrator dialog. The
    // whole-set pass never elevates by construction ([`crate::elevate`], and the manual's
    // "The unattended pass never elevates"), so the honest answer is the usage exit plus
    // the spelling that DOES elevate — audit D-12's "flag accepted and ignored" class,
    // the one `repair --dry-run` closed for the zero-arity verbs.
    if explicit.is_some()
        && operands
            .first()
            .is_some_and(|p| p.as_str() == "--default-set")
    {
        eprintln!(
            "atpkg install: --elevate=… does not apply to `--default-set` — the whole-set \
             pass never elevates; it records `needs admin` for a member that needs one and \
             waits for the explicit door"
        );
        eprintln!("atpkg:   one program, elevated: aterm pkg install <program> --elevate=…");
        eprintln!("atpkg:   the whole toolset:     aterm pkg install --default-set");
        return Err(ExitCode::from(2));
    }
    Ok((operands.first().copied(), explicit))
}

/// `atpkg install <program>` — resolve+verify the signed index from the configured account,
/// then install/force-upgrade the program's channel-pinned build for this triple (download →
/// verify_and_stage → activate → shim). Inert unless a root key is pinned at build time.
/// The one line that explains why a failure reported under `<verb>` is about to
/// recommend `install`. `None` when the reader typed `install` themselves, where
/// the remedy needs no explanation and the extra line would be noise.
fn routed_install_note(invoked_as: &str, program: &str) -> Option<String> {
    (invoked_as != "install").then(|| {
        format!(
            "atpkg:   `{invoked_as} {program}` on a program that is not installed is a \
             fresh install, which is why the remedy below says install"
        )
    })
}

/// [`cmd_install`] reached from `update <program>` on a program that is NOT
/// installed. Same path, same behaviour -- only the verb the messages name
/// changes, so a reader is never told that a command they did not type failed.
fn cmd_install_for_update(program: &String) -> ExitCode {
    cmd_install_elevated(Some(program), None, "update")
}

/// `atpkg install <program>` — resolve+verify the signed index from the configured
/// account, then install/force-upgrade the program's channel-pinned build for this
/// triple (download → verify_and_stage → activate → shim). Inert unless a root key is
/// pinned at build time. `atpkg install --default-set` routes to the explicit whole-set
/// bootstrap ([`cmd_install_default_set`]).
///
/// Takes an explicit elevation policy (`--elevate=…`), or `None` for the
/// TTY rule. THE EXPLICIT DOOR for an OS-installed member: this is the one verb that may
/// elevate, and only on the thread that runs it — every unattended pass stays Deferred.
fn cmd_install_elevated(
    program: Option<&String>,
    explicit: Option<crate::Elevation>,
    invoked_as: &str,
) -> ExitCode {
    let Some(program) = program else {
        // A bare `install` is nearly always someone asking for the toolset, and the product
        // default IS the whole set — so say the spelling that grants it rather than a usage
        // line that leaves them to guess which of the two forms they wanted.
        eprintln!("usage: atpkg install <program> | atpkg install --default-set");
        eprintln!(
            "atpkg:   for the whole ALab toolset (the usual answer): aterm pkg install --default-set"
        );
        return ExitCode::from(2);
    };
    if program == "--default-set" {
        return cmd_install_default_set();
    }
    // A FLAG IS NOT A PROGRAM NAME. A mistyped or unsupported option — the
    // measured case was `--progress-file`, whose value the arg scanner strips
    // only in the forms it knows — otherwise lands here as the program to
    // install, gets RECORDED as a wanted program, and then sits in the local
    // record as a row `doctor` has to explain away. Refuse it at the door and
    // name the two spellings that work.
    if program.starts_with('-') {
        eprintln!("atpkg: {program:?} is a command-line flag, not a program name");
        eprintln!("atpkg:   one program:      aterm pkg install <program>");
        eprintln!("atpkg:   the whole toolset: aterm pkg install --default-set");
        return ExitCode::from(2);
    }
    if !manager_enabled() {
        eprintln!("atpkg: disabled (no root key pinned) — refusing to install");
        return ExitCode::from(1);
    }
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let cfg = crate::config::cached();
    // Asking for this program back lifts its removal record — otherwise the user
    // would install it, and then have to know about a marker file to stop the next
    // unattended pass treating it as "absent by request" again.
    clear_removed(&layout, std::slice::from_ref(program));
    // The door's elevation policy, for THIS thread only: an explicit `--elevate=…`, else
    // sudo when a terminal can take its prompt, else deferred (a piped `aterm pkg install
    // brew` records `needs admin` and says so — it never hangs on a password prompt).
    crate::elevate::set_elevation(crate::elevate::door_elevation(
        explicit,
        crate::elevate::stdin_is_tty(),
    ));
    cmd_install_with(
        program,
        &layout,
        cfg,
        &*resolve_fetcher(&layout),
        invoked_as,
    )
}

/// The body of [`cmd_install`] against an ALREADY-BUILT fetcher, so a caller that has one
/// does not construct a second.
///
/// Building a `GithubFetcher` is not free: it runs the whole token chain (a keychain probe
/// plus up to three `gh auth token` spawns — hundreds of ms of blocking subprocess latency
/// for an answer that cannot differ within one process), and a fresh fetcher also throws
/// away the first one's memoized release listings + index candidates, so the entire signed
/// index (a listing plus up to 20 × 2 asset downloads) is re-fetched. `atpkg update
/// <ungrouped-program>` paid both twice because it re-entered through `cmd_install`.
fn cmd_install_with(
    program: &str,
    layout: &crate::store::Layout,
    cfg: &crate::config::PackagesConfig,
    fetcher: &dyn crate::flow::Fetcher,
    // THE VERB THE USER TYPED, for messaging only. `update <p>` on a program
    // that is not installed routes here deliberately (one install path), and
    // this function used to answer under the name `install` regardless -- so
    // `atpkg update status` replied `atpkg: install status failed: …`. A reader
    // who is told their command failed under a name they did not type cannot
    // tell whether the tool misheard them or they misused a different verb, and
    // an audit of this CLI recorded exactly that confusion (D-4).
    invoked_as: &str,
) -> ExitCode {
    reconcile_links(layout, cfg);
    if let Some(path) = explicit_install_door(layout, fetcher, program) {
        println!(
            "atpkg: {program}: nothing to install — the system install at {} satisfies it \
             (remove that install and re-run to have atpkg manage {program})",
            path.display()
        );
        return ExitCode::SUCCESS;
    }
    match do_install(layout, fetcher, cfg.channel(), program) {
        Ok(r) => {
            if let Some(outcome) = &r.protocol {
                // The canonical state IS the answer: present at its `provides` path, or
                // waiting on an elevation this invocation could not supply.
                println!("atpkg: {}: {}", r.program, outcome.state(&r.program));
                if outcome.is_deferred() {
                    println!(
                        "atpkg:   {} installs through {} and needs an administrator: run \
                         `aterm pkg install {}` in a terminal (sudo asks there), or with \
                         --elevate=osascript for the system dialog",
                        r.program,
                        outcome.protocol(),
                        r.program
                    );
                }
            } else if r.already_current {
                println!("atpkg: {} already current (build {})", r.program, r.build);
                // …CURRENT IN THE STORE. Whether it is what the machine COMPILES WITH is a
                // second question, and until 2026-09-17 no install or update lane asked it:
                // see [`crate::seam::dissent_line`] for the two weeks this sentence stood
                // over a hand-placed toolchain 859 commits behind the pin.
                if let Some(why) = crate::seam::dissent_if_armed(layout, &r.program) {
                    println!("atpkg:   but not what this machine runs: {why}");
                }
            } else {
                println!(
                    "atpkg: installed {} build {} (shims: {})",
                    r.program,
                    r.build,
                    if r.shimmed.is_empty() {
                        "none".into()
                    } else {
                        r.shimmed.join(", ")
                    }
                );
            }
            for dep in &r.dependencies {
                match &dep.result {
                    crate::flow::DepResult::Installed { build, .. } => {
                        println!(
                            "atpkg:   pulled in {} build {build} (required)",
                            dep.program
                        );
                    }
                    crate::flow::DepResult::Skipped(why) => {
                        eprintln!("atpkg:   skipped required dep {} — {why}", dep.program);
                    }
                    crate::flow::DepResult::Protocol(outcome) => {
                        println!(
                            "atpkg:   {} (required): {}",
                            dep.program,
                            outcome.state(&dep.program)
                        );
                    }
                    crate::flow::DepResult::System(path) => {
                        println!(
                            "atpkg:   {} (required): {}",
                            dep.program,
                            crate::state::system(
                                path,
                                layout.retired_date(&dep.program).as_deref()
                            )
                        );
                    }
                    crate::flow::DepResult::AlreadyPresent(_) => {}
                }
            }
            // THE SEAM AND THE EXEC ROOTS, on the single-program door too.
            //
            // `cmd_update_all_code`, `install_default_set_inner`, `cmd_seed` and
            // `cmd_rollback` have all re-asserted the rustup seam and reconciled the exec
            // roots at the end of their pass since those calls were written; the lane a
            // PERSON types — `aterm pkg install <p>`, `aterm pkg update <p>`, which both
            // end here — never did. That asymmetry is not a detail: the seam is how
            // `cargo +trust` and every `rust-toolchain.toml` find the managed compiler, and
            // the verb a user reaches for when they suspect their compiler is stale is
            // `aterm pkg update trust`. On m3 that verb answered "up to date" for two weeks
            // and did not once look at, or heal, the `~/.rustup/toolchains/trust` entry
            // that was answering for it (2026-09-17).
            //
            // Both are best-effort and both print only what CHANGED or was refused, so an
            // install on a healthy machine gains no output. A FOREIGN entry is refused
            // rather than re-pointed — `reassert` never follows or replaces a link atpkg did
            // not lay — and the refusal is both printed here and recorded, which is what
            // turns it into a `doctor` fault instead of a line in a scrollback.
            reassert_rustup_seam(layout);
            let _ = reconcile_exec_roots(layout, crate::seam::Depth::Shallow);
            ExitCode::SUCCESS
        }
        Err(crate::FlowError::Linked(p)) => {
            // A dev-linked program is managed from its checkout — a hard skip, not a failure.
            println!(
                "atpkg: {p} is dev-linked; run `aterm pkg unlink {p}` to install from the registry"
            );
            ExitCode::SUCCESS
        }
        // A target the pinned build does not serve is a STATE, never an error (design §2):
        // say the canonical words (the record already carries them) and exit clean.
        Err(e @ crate::FlowError::NoArtifact(_)) => {
            let state = canonical_non_error_state(&e)
                .unwrap_or_else(|| crate::state::unavailable(current_triple(), ""));
            println!("atpkg: {program}: {state}");
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg: {invoked_as} {program} failed: {e}");
            if let Some(state) = canonical_non_error_state(&e) {
                eprintln!("atpkg: {program}: {state}");
            }
            // Say WHY a verb the reader did not type is about to be recommended.
            // Without this line the remedy reads like a non-sequitur.
            if let Some(note) = routed_install_note(invoked_as, program) {
                eprintln!("{note}");
            }
            print_unreachable_followup(&e, &format!("aterm pkg install {program}"));
            ExitCode::from(1)
        }
    }
}

/// `atpkg rollback <program>` — re-point the program (and its shims + channel `current`) to
/// the highest RETAINED build strictly below current that still passes the floor/yank gate.
/// Consults the SIGNED index (so the gate is authoritative); advances the durable floor to
/// the index it trusted. Warns if the program is in a coherence group (a per-program rollback
/// splits the locked tuple).
fn cmd_rollback(program: Option<&String>) -> ExitCode {
    let Some(program) = program else {
        eprintln!("usage: atpkg rollback <program>");
        return ExitCode::from(2);
    };
    if !manager_enabled() {
        eprintln!("atpkg: disabled (no paper master pinned) — cannot roll back");
        return ExitCode::from(1);
    }
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let floor = build_floor(&layout);
    let fetcher = resolve_fetcher(&layout);
    match crate::flow::rollback(
        &*fetcher,
        &layout,
        &effective_anchor(&layout),
        crate::config::cached().channel(),
        program,
        floor,
        now_unix(),
    ) {
        Ok(r) => {
            // Advance both durable ratchets to what we just trusted (§8 gate 3).
            advance_floors(&layout, r.index_build, r.roster_seq);
            record_status(
                &layout,
                program,
                crate::ProgramStatus {
                    installed_build: Some(r.to_build),
                    state: crate::state::managed(r.to_build, r.index_build),
                    // The rolled-back build's signed root is not carried in the rollback
                    // report, so it is written unset here and re-derived immediately below.
                    // Carrying the PREVIOUS build's root instead would be far worse than
                    // leaving it empty: it would attest bytes that are no longer there.
                    tree_root: String::new(),
                },
                format!("rolled back {program} {} -> {}", r.from_build, r.to_build),
            );
            // CLOSE THE WINDOW WHERE IT OPENS. This used to be left for "a later `atpkg
            // update`", but `recover_missing_roots` has exactly ONE call site and it is
            // `cmd_update_all` — so `atpkg update <program>`, the verb a user most naturally
            // reaches for right after rolling that program back, never repaired it, and
            // `atpkg verify` failed closed on a perfectly good rollback until an unrelated
            // whole-set pass happened to run. The fetcher is already in hand and the index
            // it just trusted is the right authority to ask.
            recover_missing_roots(&layout, &*fetcher, None);
            if let Some(g) = &r.coherence_group {
                eprintln!(
                    "atpkg: warning — {program} is in coherence group '{g}'; rolling back one \
                     member alone splits the locked tuple. Consider `aterm pkg update` to re-cohere."
                );
            }
            println!(
                "atpkg: rolled back {program} from build {} to {}; `aterm pkg pin {program}` to hold it there",
                r.from_build, r.to_build
            );
            if program == crate::seam::SEAM_PROGRAM {
                reassert_rustup_seam(&layout);
                // The rollback's own shim lay already laid the target build's root and
                // routed what it rendered; this is the pass-level reconcile every verb that
                // moves trust ends with — a trust shim that lay did not render is routed,
                // and a root that could not be laid is said. The rolled-off build stays on
                // disk, and keeps its root, until gc takes both.
                let _ = reconcile_exec_roots(&layout, crate::seam::Depth::Shallow);
            }
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg: rollback {program} failed: {e}");
            print_unreachable_followup(&e, &format!("aterm pkg rollback {program}"));
            ExitCode::from(1)
        }
    }
}

/// `atpkg pin <program>` / `atpkg unpin <program>` — freeze (or release) a program against
/// `update`. A pin is purely LOCAL upgrade-suppression state (no index/network); it is
/// consulted strictly AFTER the floor/yank gate, so it can never resurrect a tombstoned or
/// below-floor build.
fn cmd_pin(program: Option<&String>, pinned: bool) -> ExitCode {
    let verb = if pinned { "pin" } else { "unpin" };
    let Some(program) = program else {
        eprintln!("usage: atpkg {verb} <program>");
        return ExitCode::from(2);
    };
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    // Pinning something not installed is meaningless.
    if !crate::active_builds(&layout).contains_key(program) {
        eprintln!(
            "atpkg: {program} is not installed — nothing to {verb} (aterm pkg list shows \
             what is)"
        );
        return ExitCode::from(1);
    }
    match crate::pin::set_pinned(&layout, program, pinned) {
        Ok(_) => {
            println!(
                "atpkg: {program} {}",
                if pinned {
                    "pinned (held against update)"
                } else {
                    "unpinned"
                }
            );
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg: {verb} {program} failed: {e}");
            ExitCode::from(1)
        }
    }
}

/// `atpkg update [program]` — the update router. No arg ⇒ update EVERY installed program
/// ([`cmd_update_all`], the background loop's verb). With a program ⇒ [`cmd_update_one`],
/// which routes a coherence-GROUP member through the transactional path (so a locked tuple
/// can never split, §11) and an ungrouped program through the single-program install path.
fn cmd_update(program: Option<&String>) -> ExitCode {
    match program {
        Some(p) => cmd_update_one(p),
        None => cmd_update_all(),
    }
}

/// `atpkg update` (no arg) — silently upgrade every installed program to its channel pin,
/// applying **coherence groups atomically** (§7): the `rustc`-locked tuple moves all-or-
/// nothing (stage-all → flip-all → rollback-on-failure), while an ungrouped tool applies
/// independently so one failure never wedges the tuple. A local pin holds a group on its
/// current builds (surfaced as a pin-hold). With `[packages].auto_install = true` the pass
/// ALSO bootstraps missing index default-set members (§11 batteries-included,
/// [`install_default_set`]) — the loop verb the GUI runs every 6h finally fills an empty
/// store instead of no-opping forever. GC runs once after the whole apply.
fn cmd_update_all() -> ExitCode {
    // The pass's own verdict, then the contract's other half: `install_default_set`
    // may have announced a network install from inside this verb, and only the verb
    // knows how it ended. A lane that already printed its terminal is unaffected.
    let code = cmd_update_all_code();
    answer_announcement(code == 0);
    ExitCode::from(code)
}

/// The `*index*` row after `update`'s set-completion lane, returning whether the pass
/// has now resolved the signed index. On an EMPTY store that lane IS the pass — the
/// apply lane, the row's other writer, never runs — so its unresolved index is recorded
/// exactly as the apply lane records one (and is not a check: see [`record_success`]),
/// and its resolved index retires the row the same way. When the apply lane already
/// resolved this pass, a set-completion miss neither un-resolves it nor re-opens the row
/// the apply lane just cleared.
fn settle_net_lane_index(
    layout: &crate::store::Layout,
    apply_resolved: bool,
    net_index_error: Option<&str>,
) -> bool {
    match net_index_error {
        None => {
            clear_status_row(layout, "*index*");
            true
        }
        Some(_) if apply_resolved => true,
        Some(e) => {
            record_status(
                layout,
                "*index*",
                crate::ProgramStatus {
                    installed_build: None,
                    state: format!("error: {e}"),
                    tree_root: String::new(),
                },
                format!("update failed: {e}"),
            );
            false
        }
    }
}

/// The update pass's ONE failure exit: say it, name the next act, and record the durable
/// `*index*` row. Shared by the pass's index resolve and its channel apply — which used
/// to be a single call, the resolve living inside `apply_channel` — so both halves still
/// print the same sentence and leave the same row.
fn update_pass_failed(layout: &crate::store::Layout, e: &crate::FlowError) -> u8 {
    eprintln!("atpkg: update failed: {e}");
    // Same follow-up the single-program lane prints: the old Display carried a false
    // "retries automatically" tail, and dropping it without a replacement left this lane
    // stating a network failure with no next act.
    print_unreachable_followup(e, "aterm pkg update");
    record_status(
        layout,
        "*index*",
        crate::ProgramStatus {
            installed_build: None,
            state: format!("error: {e}"),
            tree_root: String::new(),
        },
        format!("update failed: {e}"),
    );
    1
}

/// [`cmd_update_all`]'s body, returning the raw exit code so the announcement can be
/// answered with the pass's real verdict rather than a guess read off its output.
fn cmd_update_all_code() -> u8 {
    // R5: THE MACHINE SETTINGS, FIRST — before the manager gate, the index, the network,
    // before anything in this pass that can fail. They used to run at the very end,
    // inside `if failures == 0`, which coupled a per-host preference to a completely
    // clean toolchain pass it has nothing to do with: on a machine where one program
    // aborts at stage (measured 2026-09-14: `trust`/`clean` aborted, `claude`/`codex`
    // refused by a provenance tag, `last_success_at` never written) the doctor
    // promised "the next update pass disables it" on every run, forever, and nothing
    // ever did. Above `manager_enabled()` too: that gate is about the NETWORK verbs
    // (no root key, `ATPKG_DISABLE`), and host settings are not a network verb.
    apply_machine_settings_once();
    if !manager_enabled() {
        eprintln!("atpkg: disabled (no root key pinned) — nothing to update");
        return 1;
    }
    let Some(layout) = layout() else {
        return 1;
    };
    // THE HOLDER DID THIS PASS: a launch child that queued behind another pass and
    // finds the record stamped since it started owes nothing (2026-09-18).
    if let Some(ago) = pass_finished_while_we_waited(&layout) {
        println!("{}", stood_down_after_wait_line("update", ago));
        return 0;
    }
    let cfg = crate::config::cached();
    // Reconcile `[packages.links]` first, so a config-declared dev-link is in place
    // BEFORE the apply decides what it manages (linked programs hard-skip, §13).
    reconcile_links(&layout, cfg);
    // The ACTIVE build per program (what the shims point at), NOT the max complete build
    // on disk — so `decide` never treats a staged-but-unactivated build as the running one
    // (which would silently skip a needed re-flip, #19).
    // THE SHIM VIEW, deliberately. Its SILENCE for a tombstoned program is the
    // self-heal: `decide` then returns Install for the build `store/<p>/current`
    // already names, the transaction re-stages it (`Staged::was_live` exists for
    // exactly that member), and the shims come back. Widening this to include the
    // authority link — which I did, to close what an independent derivation called a
    // "one-way door" — REMOVED that silence and made the door real: a tombstoned
    // program whose pin had not moved decided UpToDate forever, a rollback re-pointed
    // its shims into the revoked build, and `uninstall --all` on a declined machine
    // revived it. The derivation's claim was wrong and so was my fix
    // (2026-08-20 round-13 audit).
    let installed: std::collections::BTreeMap<String, u64> = crate::active_builds(&layout);
    // WHO gets set-completion: a machine that has ADOPTED the toolset (the seed bootstrap
    // or an explicit "Install ALab toolset" already laid the whole set down), or one whose
    // owner ticked `auto_install` to authorize a from-scratch network bootstrap. The two
    // are different questions — see `store::Layout::adopted`. Reading only the config bit
    // meant a program published AFTER a user installed never reached them, so their
    // "entire ALab toolchain" quietly decayed into "whatever was published the day they
    // installed".
    // `declined` outranks BOTH. It is checked here and not only in the seed lane
    // because this is the pass the 6-hour loop runs: honouring a removal on the
    // local lane while the network lane quietly reinstalled the whole set on the
    // next tick made the decline look like it worked for exactly one launch.
    let complete_the_set = should_complete_set(
        cfg.auto_install(),
        adopted(&layout),
        declined(&layout),
        cfg.unreadable,
    );
    if installed.is_empty() && !complete_the_set {
        println!("{EMPTY_UPDATE}");
        return 0;
    }
    let fetcher = resolve_fetcher(&layout);
    let mut failures = 0u32;
    // Whether EITHER lane of this pass resolved the signed index — the only pass
    // `last_success_at` may stamp ([`record_success`]). The apply lane exits early when
    // it cannot resolve; the set-completion lane does not, and on an empty store it is
    // the whole pass, so a failure count alone cannot say which kind of pass this was.
    let mut index_resolved = false;
    // The index build the channel apply resolved, for the pass-end freshness stamp.
    let mut resolved_index_build: Option<u64> = None;
    // `program → pinned asset name` for everything EITHER lane of this pass resolved —
    // the pass-end gc's sparing input, so a member whose download failed mid-pass keeps
    // its `.part` resume state instead of losing it to this pass's own closing sweep.
    let mut resolved_assets: std::collections::BTreeMap<String, String> =
        std::collections::BTreeMap::new();
    // The ONE index this pass verifies, once a lane has resolved it — handed to every
    // lane below instead of being re-resolved by each ([`reusable_index`]).
    let mut pass_index: Option<crate::TrustedIndex> = None;
    if !installed.is_empty() {
        let floor = build_floor(&layout);
        // THE PASS'S ONE VERIFY-SELECT, hoisted out of `apply_channel` so the lanes
        // below read the index it trusted rather than resolve it again: the attestation
        // repair, the alias reconcile, the set completion and the empty-store diagnosis
        // each used to re-run the whole cache read + roster admit + signature checks for
        // the same answer, two to four times per six-hourly tick.
        let index = match crate::resolve_verified_index(
            &*fetcher,
            &layout,
            &effective_anchor(&layout),
            floor,
            now_unix(),
        ) {
            Ok(index) => index,
            Err(e) => return update_pass_failed(&layout, &e),
        };
        index_resolved = true;
        let report = match crate::apply_channel_with(
            &*fetcher,
            &layout,
            &index,
            cfg.channel(),
            current_triple(),
            &installed,
            // The invoking user's `[packages].exclude` — read HERE, at the CLI
            // edge that owns "whose config speaks", and handed down as data so
            // flow.rs stays hermetic (round-13 audit). This is the wire that
            // makes `uninstall`'s "keep the set, drop just this one" advice
            // true on the unattended tick instead of a promise the next pass
            // reversed with a multi-GB reinstall.
            cfg.exclude(),
        ) {
            Ok(r) => r,
            Err(e) => return update_pass_failed(&layout, &e),
        };
        // Advance the durable anti-rollback floor to the index we just trusted (§8 gate 3).
        advance_floors(&layout, report.index_build, report.roster_seq);
        // The build this pass resolved, for the freshness stamp at the pass end (the
        // `report` name is taken by the gc report there).
        resolved_index_build = Some(report.index_build);
        // What the apply resolved, kept for the pass-end gc's sparing closure — a member
        // whose group aborted on a failed download is in here, and is the reason this
        // exists (its `.part` is the resume state the next pass continues from).
        resolved_assets.extend(report.resolved_assets.clone());
        // AND CLEAR THE FAILURE THIS PASS JUST DISPROVED. The `*index*` row is
        // written when a resolve fails and was never removed by anything, so a
        // single transient network error — a hotel wifi captive portal, a laptop
        // waking mid-poll — left `atpkg doctor` and Settings ▸ Packages reporting a
        // broken toolset forever, on a machine that had been updating cleanly every
        // six hours since (2026-08-20 round-8 audit).
        clear_status_row(&layout, "*index*");
        // REPAIR A MEMBER THAT WENT LIVE WITHOUT ITS ATTESTATION. A pass killed
        // inside the flip window leaves the program installed and working but with
        // no recorded signed root, and `atpkg verify` fails closed on that forever
        // while `seed` says "fully installed" and `update` says "up to date" — so
        // nothing repaired it. Re-derive the root from the same authority the
        // install used, never a local recomputation (2026-08-20 round-8 audit).
        recover_missing_roots(&layout, &*fetcher, Some(&index));
        // …and the SENTENCE that row left behind. `status.outcome` is what Settings
        // ▸ Packages prints as its detail line, so clearing the row while leaving
        // "update failed: …" in place swaps one stale verdict for a quieter one.
        record_outcome(
            &layout,
            format!("up to date (index build {})", report.index_build),
        );
        failures = report_channel_apply(&layout, &installed, &report);
        for p in &report.skipped_linked {
            println!("atpkg: {p} dev-linked — skipped");
        }
        // The set-completion lane below runs its own alias + shadow reconcile at its
        // end; a pass that only applies runs them here, so every pass lays the aliases
        // of what it keeps (offline, from the index this pass just verified and cached)
        // and says who is SHADOWED.
        if !complete_the_set {
            // From the index THIS pass verified. `cached_index` re-read
            // `index-cache.toml` and re-ran the whole roster admit + signature check to
            // answer a question the pass already had the answer to — and it skips the
            // freshness gate this index has passed.
            reconcile_aliases(&layout, &index);
            reconcile_shadowed(
                &layout,
                report.index_build,
                std::env::var_os("PATH").as_deref(),
            );
        }
        // On to the set-completion lane and the empty-store diagnosis below.
        pass_index = Some(index);
    }
    // §11 batteries-included: install the index default-set members not yet installed
    // (include/exclude-narrowed; linked/yanked members skip; per-program failures are loud
    // but never block the rest). This is what keeps an ADOPTED machine's toolset COMPLETE
    // as the suite grows, and what an `auto_install` machine uses to bootstrap from empty.
    if complete_the_set {
        // ANNOUNCE BEFORE ACTING, exactly as the local seed lane does. This pass can
        // pull GIGABYTES over the network — an Intel Mac taking the x86_64 set, a
        // seedless cut, a seal past its horizon, an app updated before it ever
        // provisioned — and it ran completely silently: the GUI spawns it with
        // stdout discarded, so a user could watch multiple GB arrive with nothing on
        // screen ever mentioning it. That is the same defect the seed lane's marker
        // contract exists to prevent, on the lane that is MORE surprising because
        // nothing local prompted it.
        let before_net = crate::active_builds(&layout);
        // The consent the line states is the one that holds on this machine: installing
        // aterm, with `seed_install = false` as the switch that would have stopped it, only
        // where the seed pass itself adopted the set ([`set_completion_consent`]).
        let net = install_default_set(
            &layout,
            &*fetcher,
            &effective_anchor(&layout),
            cfg,
            ProvisionLane::Network,
            set_completion_consent(
                cfg.auto_install(),
                cfg.seed_install(),
                adopted_by_seed(&layout),
            ),
            now_unix(),
            pass_index.as_ref(),
        );
        failures += net.failures;
        index_resolved = settle_net_lane_index(&layout, index_resolved, net.index_error.as_deref());
        let announced = net.announced;
        // The net lane's resolutions join the apply lane's for the same sparing closure.
        resolved_assets.extend(net.resolved_assets);
        let mut arrived: Vec<String> = crate::active_builds(&layout)
            .keys()
            .filter(|k| !before_net.contains_key(k.as_str()))
            .cloned()
            .collect();
        if !arrived.is_empty() {
            arrived.sort();
            emit_marker_line(&format!(
                "atpkg: {NET_INSTALLED_MARKER}{}",
                arrived.join(", ")
            ));
            println!("{SEED_FOLLOW_ON}");
        } else if announced {
            // ALWAYS ANSWER THE ANNOUNCEMENT (the seed lane's law): a pass that
            // said "installing over the network" and then installed nothing
            // must say so, or the held notice outlives its own truth.
            emit_marker_line(&format!(
                "atpkg: {NET_FAILED_MARKER}network provisioning installed nothing — see the \
                 lines above for each program's reason"
            ));
        }
        // Retire or refresh a stale `*seed*` pending-consent row against what
        // this pass just proved. The seed lane — the row's only other writer —
        // hard-skips once the store is non-empty, so nothing else ever
        // reconciled it: a row written 2026-08-12 was still advertising the
        // legacy toolset layout on 2026-08-27, several index builds later.
        // Only a CLEAN pass speaks; a failed one proves nothing about the offer.
        if net.failures == 0 {
            reconcile_seed_status(&layout, &net.remaining_missing);
        }
    }
    // Reclaim superseded builds once after the whole channel apply (all group activations
    // done). Best-effort; never fails the update. This verb sweeps the WHOLE prefix, so an
    // abstention here is about a program it did try to keep current — reported, or the disk
    // grows after every update with nothing on screen ever mentioning it.
    //
    // The SPARING form, not plain `run`: this pass just resolved the current pins, so a
    // member whose download failed mid-pass keeps its `.part` — otherwise this very sweep
    // destroyed the resume state and the next 6-hour tick refetched a multi-GB archive
    // from byte 0 (resume-across-passes survived only a process kill). Programs the pass
    // resolved nothing for are swept exactly as before; `atpkg gc` still reclaims all.
    let report =
        crate::gc::run_keeping_pinned_partials(&layout, &|p| resolved_assets.get(p).cloned());
    print_gc_sweeps("update", &report);
    print_gc_abstentions("update", &report);
    // THE EXEC ROOTS, over the store this pass leaves (after its gc, so a build it just
    // reclaimed is not given a root first). This is what heals a machine that is ALREADY
    // up to date on a trust build whose tippy refuses its own `bin/rustc` copy: the apply
    // short-circuits that program before any shim is laid, so without this no pass would
    // ever route its shims. Best-effort like the gc above; a root that cannot be laid is
    // said on stderr and `aterm pkg repair` names it as undone.
    let _ = reconcile_exec_roots(&layout, crate::seam::Depth::Shallow);
    // Refresh the interactive-shell PATH hook at the CLI edge (§16), best-effort.
    crate::hooks::refresh(&layout);
    if failures == 0 {
        // A PASS THAT INSTALLED NOTHING ON AN EMPTY STORE IS NOT A SUCCESS TO SHOW.
        // Every group clean-skips when the index publishes no artifact for this
        // triple, so `failures` stays zero and the verb exited 0 with nothing
        // installed and no status written — and the Settings button that runs it
        // reported a green "toolchain up to date" on a machine where the toolchain
        // can never arrive. Say the true thing instead, durably
        // (2026-08-20 round-8 audit).
        if crate::active_builds(&layout).is_empty() {
            // STDERR AND EXIT 2, matching `cmd_install_default_set`. The GUI keeps
            // stderr and discards stdout, and it maps 2 to the honest "not a
            // temporary error" sentence while 1 is a retryable failure it renders as
            // a bare exit code — so printing the reason on stdout and exiting 1
            // produced exactly the opaque message these fixes set out to kill
            // (2026-08-20 round-9 audit).
            // DO NOT BLAME THE CPU FOR EVERY EMPTY STORE. "Nothing installed and
            // nothing installed this pass" has more than one cause: a triple the
            // index does not serve, a translated process reporting a triple that is
            // not this Mac's, and a channel that revoked everything. The seed lane
            // already distinguishes these; this one asserted the architecture answer
            // for all of them (2026-08-20 round-9 audit).
            if running_translated() {
                eprintln!(
                    "atpkg: nothing is installed — this process is running under \
                     Rosetta translation, so {} is not this Mac's real architecture. \
                     Relaunch aterm natively and the toolset installs.",
                    current_triple()
                );
                return 2;
            }
            let serves_us = match reusable_index(pass_index.as_ref(), now_unix()) {
                // The apply lane verified an index this very pass; asking the cache and
                // the signatures again for one diagnostic sentence is work, not caution.
                Some(index) => Some(!seed_serviceable(&layout, &*fetcher, index, cfg).is_empty()),
                None => crate::resolve_verified_index(
                    &*fetcher,
                    &layout,
                    &effective_anchor(&layout),
                    build_floor(&layout),
                    now_unix(),
                )
                .ok()
                .map(|index| !seed_serviceable(&layout, &*fetcher, &index, cfg).is_empty()),
            };
            let (said, state) = match serves_us {
                Some(true) => {
                    // The index DOES publish for this triple, so something else refused
                    // every member — a revocation, a floor, a filter. Saying "no build
                    // for your architecture" here sends the user to fix the wrong thing.
                    (
                        format!(
                            "nothing is installed and nothing was installable — the index \
                             publishes builds for {} but every one was refused (revoked, \
                             below the floor, or filtered out); see `aterm pkg doctor`",
                            current_triple()
                        ),
                        "unavailable: every published build was refused",
                    )
                }
                Some(false) => (
                    format!(
                        "nothing is installed and nothing was installable — no published \
                         build was found for this machine's architecture ({})",
                        current_triple()
                    ),
                    "unavailable: no build for this architecture",
                ),
                // `None` = the index never RESOLVED — an offline machine, a rate
                // limit, a proxy. Blaming the CPU here (as this arm used to) told
                // a user with a network hiccup that their machine can never be
                // served, the exact inversion of the truth (audit-2 item 6).
                None => (
                    "nothing is installed — the signed index could not be reached, so \
                     nothing could be checked or fetched. When the connection is back, \
                     re-run: aterm pkg install --default-set"
                        .to_string(),
                    "unavailable: index unreachable",
                ),
            };
            eprintln!("atpkg: {said}");
            record_status(
                &layout,
                "*toolset*",
                crate::ProgramStatus {
                    installed_build: None,
                    state: String::from(state),
                    tree_root: String::new(),
                },
                said,
            );
            return 2;
        }
        // The toolset is present: retire any "unavailable" verdict an earlier
        // pass recorded. Only the seed lane cleared this row, and a machine
        // whose triple the index did not serve yet reaches the toolset through
        // THIS lane once artifacts are published (2026-08-20 round-9 audit).
        clear_status_row(&layout, "*toolset*");
        // R6: which managed agents are at the pin, for the pull-down row.
        print_managed_current(&layout, &*fetcher, cfg);
        print_provenance_carriers(&layout);
        // (R5's machine settings ran at the TOP of this pass — see the note there.)
        // A pass that resolved the index and applied it clean: THE event R3's
        // "never checked" waits for.
        record_success(&layout);
        record_index_freshness(&layout, resolved_index_build);
        0
    } else {
        // A pass that resolved the index and RAN TO ITS END with member failures —
        // each recorded in its own row — is a check that ran, and is stamped as one.
        // Until 2026-09-14 this exit stamped nothing, so one failing member (claude,
        // refused at stage on the owner's machine) kept every read-only verb printing
        // "no update check has run yet on this machine — packages cannot be updated
        // until the first pass completes" for as long as it failed: false in both
        // halves, and pointing at the very command that had just run.
        // ONLY A PASS THAT RESOLVED THE INDEX, THOUGH. An adopted or `auto_install`
        // machine with an EMPTY store skips the apply lane, so an offline launch-time
        // pass reaches here through the set-completion lane's unresolved index alone —
        // and stamping it silenced "never checked" and gave `doctor` a 0-day age on a
        // machine that had never once reached the index (2026-09-15 audit).
        if index_resolved {
            record_success(&layout);
            record_index_freshness(&layout, resolved_index_build);
        }
        1
    }
}

/// §11 batteries-included bootstrap: install every SIGNED-index default-set member
/// (narrowed by the narrowing-only `[packages].include`/`exclude`) that is not yet
/// installed. Shared by `update`'s `auto_install = true` arm and the explicit
/// `atpkg install --default-set` one-shot (what the Settings "Install ALab toolset"
/// button calls). Every gate is the install flow's own (reachability, freshness,
/// floor/yank via `decide`, dev-link hard-skip, sig/sha256/tree_root) — this only
/// picks WHICH programs to ask for. The pass resolves + verifies the index ONCE and
/// partitions the missing members by coherence group (§7): a grouped tuple (the
/// `rustc`-locked trust set) fresh-installs all-or-nothing against that ONE index
/// state via [`crate::flow::bootstrap_group`] — a mid-pass index publish or a
/// per-member failure can never activate a version-split or partial tuple — while
/// an ungrouped member keeps the per-member install path (with its signed
/// `requires` resolution, §17). Per-program/group failures are LOUD but never
/// fatal to the rest; returns the hard-failure count. A missing-triple artifact
/// (member or whole tuple), an app-bundle member (the app updates itself), a
/// dev-link, and a tombstoned pin are all SKIPS, not failures — the loop must not
/// scream every 6h about states that are correct. GC + the shell-hook refresh run
/// once at the caller's CLI edge (the `cmd_update_all` precedent), keeping this
/// hermetically testable.
/// What a default-set pass did, for the caller's marker bookkeeping: how many
/// members failed, and whether the `net-starting:` announcement was printed
/// (an announcement DEMANDS a terminal answer — `net-installed:` or
/// `net-failed:` — so the caller must know one was opened). On the
/// sealed-payload lane `announced` is always false: that lane's announcement
/// (and its terminal) belongs to `cmd_seed`, not to this pass.
struct DefaultSetOutcome {
    failures: u32,
    announced: bool,
    /// `Some(reason)` when the pass never reached a signed index it could act on — the
    /// index did not resolve (offline, a rate limit, a proxy) or names no channel for
    /// this machine, the two early exits the apply lane reports as its own `*index*`
    /// failure — so it checked NOTHING; `None` once it did. `update` reads this: on an
    /// empty store this lane is the whole pass, and its unresolved index used to be
    /// stamped as a completed check (`last_success_at`), silencing "never checked" and
    /// giving `doctor` a 0-day age on a machine that had never reached the index.
    index_error: Option<String>,
    /// `program → pinned asset name` for every member the pass resolved far enough to
    /// select an artifact — a member whose download then FAILED included. GC runs at the
    /// CALLER's edge (the `cmd_update_all` precedent), so this rides the outcome out to
    /// where the pass-end `gc::run_keeping_pinned_partials` builds its sparing closure.
    resolved_assets: std::collections::BTreeMap<String, String>,
    /// Members the pass REFUSED because their coherence tuple was narrowed (a
    /// config include/exclude, or a member removed on purpose) — sorted. They are
    /// deliberately absent from the announcement and the plan; the caller's
    /// summary needs them so "default set complete" cannot claim members the
    /// pass just declined to install (observed live 2026-08-27).
    skipped_narrowed: Vec<String>,
    /// `wanted ∧ absent` at pass end — what a CLEAN pass proved still awaits
    /// install here (failed members included; narrowed/unserved members not).
    /// The `*seed*` pending-consent row reconciles against this, so a row
    /// written in one era cannot keep advertising another era's toolset.
    remaining_missing: Vec<String>,
}

/// WHERE a default-set pass's bytes come from — named by the CALLER, which owns
/// the fetcher, because the pass's announcement must describe the actual
/// transport. `net-starting:` claims the wire; during the sealed-payload seed
/// (a purely local, lsof-verified zero-socket extraction) that claim was a
/// fabrication the GUI then repeated on its progress card, since the markers
/// are parsed byte-for-byte (crates/aterm-gui, `parse_seed_line`).
#[derive(Clone, Copy, PartialEq, Eq, Debug)]
enum ProvisionLane {
    /// [`resolve_fetcher`]'s chain: the network is the index authority and
    /// bytes can cross the wire (the chain decides per asset — a local
    /// bootstrap leg may serve some), so `net-starting:` is the honest
    /// announcement.
    Network,
    /// `cmd_seed`'s bare [`crate::DirFetcher`] over the in-DMG sealed
    /// registry: LOCAL by construction — that is the lane's whole consent
    /// argument (see `cmd_seed`), so no byte can cross a socket. This lane's
    /// announcement is the caller's own `seed-starting:` marker, printed with
    /// the signed size BEFORE the pass runs; the pass itself announces
    /// nothing — a second card-opening marker would double-announce, and a
    /// `net-starting:` one would lie.
    SealedPayload,
}

impl ProvisionLane {
    /// The R5 progress-file pass name this lane runs under — the ONE naming
    /// authority, so the writer sites cannot drift from each other (the GUI
    /// reads these strings back from the progress file).
    fn pass_name(self) -> &'static str {
        match self {
            ProvisionLane::Network => "net",
            ProvisionLane::SealedPayload => "seed",
        }
    }
}

/// Who consented to a default-set pass — named by the caller beside its [`ProvisionLane`],
/// because the wire lane's announcement states the consent and the switches that stop the
/// pass, and those differ per caller. [`NET_OFF_SWITCH`] is true of every pass; the two clauses
/// [`Consent::announced`] adds around it only of a pass completing a set the seed pass adopted.
#[derive(Clone, Copy, PartialEq, Eq, Debug)]
enum Consent {
    /// Nobody asked: the pass runs because installing aterm adopted the toolset. The line
    /// names both that consent and `[packages].seed_install = false`, read before adoption is
    /// recorded, which is the switch that would have stopped it.
    InstallingAterm,
    /// No consent claimed beyond the one every pass carries: the line names the uninstall
    /// alone. The explicit verb's own pass (running it is the consent, and it never reads
    /// `seed_install`), and every `update` pass the seed pass's adoption does not stand behind —
    /// or no longer speaks for, once `auto_install = true` or a `seed_install = false` set
    /// after adoption overrides it ([`set_completion_consent`]).
    Explicit,
}

impl Consent {
    /// The clauses the wire lane's announcement wraps around [`NET_OFF_SWITCH`]: the consent
    /// ahead of it and the switch that would have prevented the pass after it, each only where
    /// it is true. Parenthesis-free, as that phrase is, for the toolchain bar's sake.
    fn announced(self) -> (&'static str, &'static str) {
        match self {
            Consent::InstallingAterm => (
                "installing aterm is the consent for this; ",
                ", or with `[packages].seed_install = false` in aterm.toml before the first \
                 launch",
            ),
            Consent::Explicit => ("", ""),
        }
    }
}

/// The consent `cmd_update_all`'s set-completion pass states — pure, so the choice is pinned
/// by a test. "Installing aterm is the consent" holds only where the seed pass adopted the
/// machine ([`adopted_by_seed`]) and neither key overrides that: `auto_install = true`
/// completes the set whatever `seed_install` says, and a key set false after adoption can no
/// longer stop the loop. Every other pass gets [`Consent::Explicit`], naming the decline alone.
fn set_completion_consent(
    auto_install: bool,
    seed_install: bool,
    adopted_by_seed: bool,
) -> Consent {
    if adopted_by_seed && seed_install && !auto_install {
        Consent::InstallingAterm
    } else {
        Consent::Explicit
    }
}

/// The off switch every wire-lane announcement names, whoever consented ([`Consent`]). Kept
/// free of parentheses, as the clauses around it are: the GUI's toolchain bar reads the sizes
/// out of the line's trailing `(…)`, which must stay the last one on the line.
///
/// The timing is stated because `uninstall` is a store mutator like the pass itself and the
/// dispatch edge holds the writer lock throughout (`mutator_store_lock`), so a bare "opt out
/// with" would send the reader into "another atpkg process holds the store lock" mid-download.
const NET_OFF_SWITCH: &str = "opt out once this pass ends with `aterm pkg uninstall --all`, which \
                              removes the set and records the decline";

/// The wire lane's announcement, if `lane` owes one — pure, so the
/// phantom-network defect stays pinned by a test rather than an lsof session:
/// the sealed-payload lane returns `None` for EVERY set, and only a non-empty
/// network pass speaks (the ordinary every-6-hours no-op tick stays silent).
///
/// Names the signed sizes — what comes over the wire and what stays on disk
/// ([`seed_install_bytes`], rendered by [`size_parenthetical`] exactly as the seed lane's line
/// does) — and the off switch ([`NET_OFF_SWITCH`]), with the consent ahead of it and the
/// `seed_install` switch after it only where `consent` makes them true
/// ([`Consent::announced`]).
fn net_announcement(
    lane: ProvisionLane,
    consent: Consent,
    will_install: &[String],
    sizes: SignedSizes,
) -> Option<String> {
    if lane == ProvisionLane::SealedPayload || will_install.is_empty() {
        return None;
    }
    let (consent_clause, seed_clause) = consent.announced();
    Some(format!(
        "atpkg: {NET_STARTING_MARKER}installing {} program(s) over the network: {} — \
         {consent_clause}{NET_OFF_SWITCH}{seed_clause}{}",
        will_install.len(),
        will_install.join(", "),
        size_parenthetical(sizes)
    ))
}

/// The §11 default-set pass over the process `PATH` ([`install_default_set_with_path`]).
///
/// `index` is the caller's already-verified index when its pass resolved one: `atpkg
/// update` hands in the index its apply lane trusted, so this lane does not repeat that
/// whole verify-select ([`reusable_index`]). `None` — the explicit toolset verb, the seed
/// lane — resolves one here, exactly as before.
#[allow(
    clippy::too_many_arguments,
    reason = "the default-set pass's whole verified context, plus the caller's \
              already-resolved index"
)]
fn install_default_set(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    anchor: &crate::Anchor,
    cfg: &crate::config::PackagesConfig,
    lane: ProvisionLane,
    consent: Consent,
    now: i64,
    index: Option<&crate::TrustedIndex>,
) -> DefaultSetOutcome {
    install_default_set_with_path(
        layout,
        fetcher,
        anchor,
        cfg,
        lane,
        consent,
        now,
        index,
        std::env::var_os("PATH").as_deref(),
    )
}

/// [`install_default_set`] over an INJECTED `PATH`, so a pass is testable without
/// reading the developer's environment — the same split, for the same reason, as
/// [`reconcile_system_satisfied`] / [`reconcile_system_satisfied_with`].
///
/// It is not a nicety. A pass answers two questions against `PATH` — "is a
/// foreign copy of this tool ahead of ours" (`reconcile_shadowed`) and "is this
/// program already satisfied by a system install"
/// (`reconcile_system_satisfied_with`) — and both used to read the process
/// environment from inside the pass. So a test's temp prefix was judged against
/// the REAL machine: on any developer box with aterm's own toolchain installed
/// (`~/Library/Application Support/aterm/pkg/bin` — i.e. the machine this is
/// developed on), `ay` resolves outside the fixture prefix and the recorded row
/// reads `managed <b> — SHADOWED by …` where the fixture expects `pinned by
/// index …`. Two tests failed exactly there, on exactly the machines most likely
/// to run them, and read as flakes rather than as the environment leak they were.
#[allow(
    clippy::too_many_arguments,
    reason = "[`install_default_set`]'s inputs plus the injected `PATH` the shadow \
              and system-satisfied probes read"
)]
fn install_default_set_with_path(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    anchor: &crate::Anchor,
    cfg: &crate::config::PackagesConfig,
    lane: ProvisionLane,
    consent: Consent,
    now: i64,
    index: Option<&crate::TrustedIndex>,
    path_var: Option<&std::ffi::OsStr>,
) -> DefaultSetOutcome {
    // Live-progress pass ownership (R5): when the GUI opted in via `--progress-file`
    // and no pass is live yet, this pass IS the "net" pass — begun here so its start
    // truncates the file, ended here so the terminal snapshot (pid cleared,
    // `ended_unix` stamped) is written on every exit path. The seed lane begins its
    // own "seed" pass BEFORE calling in, so `begin_pass` declines and the seed lane
    // keeps ownership — but that decline is a LIVENESS accident, not a lane rule:
    // a sealed seed whose own `begin_pass` failed (unwritable progress file) must
    // run with NO pass, never under a fabricated "net" one. So the lane gates this
    // channel exactly as it gates the stdout announcement (`net_announcement`).
    let owned_pass = lane == ProvisionLane::Network
        && progress_path().is_some_and(|p| crate::progress::begin_pass(p, lane.pass_name()));
    let out = install_default_set_inner(
        layout, fetcher, anchor, cfg, lane, consent, now, index, path_var,
    );
    if owned_pass {
        crate::progress::end_pass();
    }
    out
}

/// [`install_default_set`]'s body, split so pass ownership above cannot leak on any
/// of the early returns below.
#[allow(
    clippy::too_many_arguments,
    reason = "[`install_default_set_with_path`]'s inputs, split only so pass \
              ownership cannot leak on an early return"
)]
fn install_default_set_inner(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    anchor: &crate::Anchor,
    cfg: &crate::config::PackagesConfig,
    lane: ProvisionLane,
    consent: Consent,
    now: i64,
    index: Option<&crate::TrustedIndex>,
    path_var: Option<&std::ffi::OsStr>,
) -> DefaultSetOutcome {
    let floor = build_floor(layout);
    // THE CALLER'S INDEX WHEN IT HAS ONE (`atpkg update`'s pass resolved one for its
    // apply lane), else one resolved here (`install --default-set`, the seed lane).
    // Re-using it skips a repeat of the whole verify-select — the cache read + TOML
    // parse, the roster admit, the master and machine signature checks, the
    // roster-generation observation — for an answer that cannot differ inside one pass.
    // It skips no GATE: [`reusable_index`] re-checks the signed freshness window, and an
    // index that lapsed mid-pass falls through to the resolve below and meets `Stale`
    // there, exactly as it always did.
    let resolved;
    let index = match reusable_index(index, now) {
        Some(index) => index,
        None => {
            resolved = match crate::resolve_verified_index(fetcher, layout, anchor, floor, now) {
                Ok(i) => i,
                Err(e) => {
                    eprintln!("atpkg: default-set bootstrap: cannot resolve the signed index: {e}");
                    print_unreachable_followup(&e, "aterm pkg install --default-set");
                    return DefaultSetOutcome {
                        failures: 1,
                        announced: false,
                        index_error: Some(e.to_string()),
                        resolved_assets: std::collections::BTreeMap::new(),
                        skipped_narrowed: Vec::new(),
                        remaining_missing: Vec::new(),
                    };
                }
            };
            &resolved
        }
    };
    // Fail-closed diagnostics for `[packages.links]` fetch overrides: a program the
    // signed index does not name is unreachable BY CONSTRUCTION (§5) — the override
    // can never fetch it, so say so plainly instead of silently never acting.
    // (`home: None` is fine — only Repo-shaped values matter here.)
    for (program, value) in &cfg.links {
        if matches!(
            crate::classify_link(value, None),
            crate::LinkTarget::Repo(_)
        ) && !index.is_program(program)
        {
            eprintln!(
                "atpkg: [packages.links] {program} = {value:?} — the signed index does not \
                 name {program}; refusing the fetch override (no unsigned installs from slugs)"
            );
        }
    }
    // The channel, looked up ONCE on the one resolved index — every group decision
    // below reads the SAME signed state, so a mid-pass publish can't split a tuple.
    let Some(ch) = index.channel_for(cfg.channel(), current_triple()) else {
        let e = crate::FlowError::NoChannel(cfg.channel().to_string());
        eprintln!("atpkg: default-set bootstrap failed: {e}");
        return DefaultSetOutcome {
            failures: 1,
            announced: false,
            index_error: Some(e.to_string()),
            resolved_assets: std::collections::BTreeMap::new(),
            skipped_narrowed: Vec::new(),
            remaining_missing: Vec::new(),
        };
    };
    let ch = &ch;
    let mut installed = crate::active_builds(layout);
    let mut wanted = wanted_programs(layout, index, cfg);
    // Programs the user removed on purpose are not "missing" — they are absent by
    // request. Filtering them ONLY in the seed lane's prescan was not enough: THIS
    // function is what the 6-hour pass runs, so an adopted machine reinstalled an
    // uninstalled program on the next tick regardless. Set-completion means keeping
    // the set the user has, not overruling their removals.
    for p in removed_programs(layout) {
        wanted.remove(&p);
    }
    // SYSTEM SATISFACTION, reconciled EVERY pass (owner decision for `gh`/`emacs`): a
    // member declaring `system = "<bin>"` whose binary is already on PATH outside the
    // managed prefix is satisfied — no download, no shim, a `satisfied by system`
    // status row — and a managed copy it may have had is retired so the system binary
    // is the only one. If the system install later disappears the name simply stays
    // wanted-and-missing here and installs through its artifact like any other member.
    for p in reconcile_system_satisfied_with(layout, index, &wanted, &mut installed, path_var) {
        wanted.remove(&p);
    }
    // SERVE THIS TRIPLE OR SAY NOTHING. `wanted` so far is what the signed
    // index NAMES — which, on a machine whose triple the registry publishes no
    // artifacts for (an Intel Mac before x86_64 lands; Linux/Windows today),
    // is a list of programs that can never arrive here. Unfiltered, it turned
    // every 6-hour tick into theater: "installing N program(s)" announced, N
    // failures counted, a failure pill raised — forever — and N pending stubs
    // laid on PATH whose promise ("open aterm, it provisions") could never
    // come true. The seed prescan (`seed_serviceable`) already applies this
    // rule with the same probe; the network lane now applies it BEFORE the
    // stub reconcile and the announcement, so unserved programs neither
    // announce, nor count as failures, nor leave stubs. The probe proves
    // missing-ness from a verified manifest and DEFERS on any fetch failure
    // (`group_missing_triple`), so an offline tick drops nothing — the real
    // stage still fails loudly there. One `*toolset*` status row records the
    // truth for `__pending`'s not-running fallback to surface.
    let triple = current_triple();
    let mut unserved: Vec<String> = Vec::new();
    for group in crate::plan_groups(index, ch) {
        let missing: Vec<String> = group
            .members
            .iter()
            .filter(|m| wanted.contains(m.as_str()) && !installed.contains_key(m.as_str()))
            .cloned()
            .collect();
        if missing.is_empty()
            || group
                .members
                .iter()
                .any(|m| crate::linkmode::is_linked(layout, m))
        {
            continue;
        }
        let probe: &[String] = match &group.group {
            Some(_) => &group.members,
            None => &missing,
        };
        if crate::flow::group_missing_triple(fetcher, index, cfg.channel(), triple, probe).is_some()
        {
            unserved.extend(missing);
        }
    }
    if !unserved.is_empty() {
        unserved.sort();
        // Every unserved member gets ITS OWN canonical row — `unavailable on <target>:
        // <hint>`, with the program's `unavailable_hint` when the index carries one —
        // in the words the pass log prints (design §2). A member absent on this target
        // is a normal state, never a fault: `doctor`'s problem scan does not match it.
        let recorded = crate::status::read(layout);
        for p in &unserved {
            wanted.remove(p.as_str());
            // A program this machine has DISABLED keeps the row that says so. The
            // update lane records `aborted: stage` on a program whose current build was
            // recalled while its coherence group's replacement pin is not published
            // here (`disable_revoked_currents`), so that doctor counts it; this prescan
            // runs in the same pass on adopted machines and used to repaint that row
            // `unavailable on <target>: …` — a benign state doctor does not match — so
            // the fault vanished before anyone read it. The measurement is the one
            // `repair` makes: the recorded row, or a tombstone shim among the program's
            // exposed tools. It is still unserved here: not announced, not counted, no
            // stub.
            if program_disabled_here(layout, recorded.as_ref(), p) {
                println!(
                    "atpkg: {p}: no build for this machine ({triple}) — and its current \
                     build is disabled here (see `aterm pkg doctor`); the row stays"
                );
                continue;
            }
            let hint = index
                .program(p)
                .and_then(|prog| prog.unavailable_hint.clone())
                .unwrap_or_default();
            let state = crate::state::unavailable(triple, &hint);
            println!("atpkg: {p}: {state}");
            record_status(
                layout,
                p,
                crate::ProgramStatus {
                    installed_build: None,
                    state,
                    tree_root: String::new(),
                },
                format!("unserved for {triple}: {}", unserved.join(", ")),
            );
        }
        println!(
            "atpkg: {} program(s) have no build for this machine ({triple}): {} — skipped, \
             not failed; they install automatically once builds publish",
            unserved.len(),
            unserved.join(", ")
        );
        // The toolset-wide BLOCKED verdict is reserved for a machine the index serves
        // NOTHING on (an Intel Mac before x86_64 lands): the pending stubs and doctor read
        // it as "nothing is coming". A partial matrix — gh and emacs absent on Linux while
        // the ALab set installs fine — must not raise it: `wanted` now holds exactly the
        // members this target CAN carry (served, already installed, or dev-linked), so
        // the machine is blocked as a whole only when nothing at all is left in it.
        if wanted.is_empty() {
            record_status(
                layout,
                "*toolset*",
                crate::ProgramStatus {
                    installed_build: None,
                    state: String::from("blocked: no build for this architecture"),
                    tree_root: String::new(),
                },
                format!("unserved for {triple}: {}", unserved.join(", ")),
            );
        }
    }
    // OS-INSTALLED MEMBERS (`pkg`, `softwareupdate`, `system-pm` — Homebrew, the Command
    // Line Tools, and every member the platform's own manager resolves), reconciled
    // every pass BEFORE the plan is announced: each is proven by its `provides` path
    // (present ⇒ `installed via <protocol>: <path>`, nothing moves) or, absent, recorded
    // `needs admin — run: aterm pkg install <name>` when it needs elevation — this pass
    // never elevates — or `unavailable on <target>` when its manager is missing; a
    // user-scoped `system-pm` member installs here through that manager. Handled here,
    // and dropped from `wanted`, so the announcement, the
    // progress card and the pending stubs never promise to install something only the
    // explicit door can, and the six-hourly tick stays silent once the row is written.
    let (handled, protocol_failures) = reconcile_protocol_members(
        layout, fetcher, anchor, index, cfg, now, &wanted, &installed,
    );
    for p in handled {
        wanted.remove(&p);
    }
    let mut failures = protocol_failures;
    // EXTRAS' CONSENT STUBS (owner decision 2026-08-26): every index-listed `extra`
    // keeps a stub on PATH — listed, pinned, NEVER auto-installed; typing the name
    // asks — unless the user removed it on purpose, excluded it, a system install
    // satisfies it, it is already installed or dev-linked, or this triple cannot be
    // served (the same probe as above, so no stub is ever laid for bytes that cannot
    // move here). Opted-in extras are in `wanted` already; the kind still says
    // "extra", so the stub never asks twice and the pass keeps them current.
    let mut extras = extra_stub_candidates(layout, index, cfg, &installed);
    let mut unserved_extras: Vec<String> = Vec::new();
    extras.retain(|name| {
        let served = crate::flow::group_missing_triple(
            fetcher,
            index,
            cfg.channel(),
            triple,
            std::slice::from_ref(name),
        )
        .is_none();
        if !served {
            unserved_extras.push(name.clone());
        }
        served
    });
    // THE EXTRAS' ROWS (design §2): every listed program carries ONE canonical state, an
    // extra included. Not opted in and served here ⇒ `extra — not installed (opt in:
    // aterm pkg install <name>)`; not served here ⇒ its own `unavailable on <target>:
    // <hint>` row, exactly like an unserved member. Opted-in extras are in `wanted` and
    // are recorded by their install like any member. Written only on change, so the
    // silent six-hourly tick leaves status.toml alone.
    for name in &unserved_extras {
        let hint = index
            .program(name)
            .and_then(|prog| prog.unavailable_hint.clone())
            .unwrap_or_default();
        record_state_if_changed(layout, name, crate::state::unavailable(triple, &hint));
    }
    for name in extras.iter().filter(|n| !wanted.contains(n.as_str())) {
        record_state_if_changed(layout, name, crate::state::extra_not_installed(name));
    }
    // THE AGENT PROGRAMS' ROWS (owner decision 2026-09-10): a wanted agent program that
    // is not installed yet reads `agent program — installing` — never `extra — not
    // installed (opt in: …)`, the row an older pass wrote under the opt-in policy, which
    // this corrects the first pass after the client lands.
    for name in wanted
        .iter()
        .filter(|n| crate::stub::is_agent_program(n) && !installed.contains_key(n.as_str()))
    {
        record_state_if_changed(layout, name, crate::state::agent_installing());
    }
    // A FOREIGN COPY OF AN EXTRA (2026-09-10 audit): an extra with a copy of its own name
    // on `PATH` gets no consent stub (laid ahead of it, the stub would hijack a working
    // install) — and, until now, no row either, so `status` listed nothing at all for a
    // program the user runs every day. Say what runs, in the canonical words:
    // `system: <path> — not managed by aterm`.
    {
        let removed = layout.removed_programs();
        for name in index.programs.keys().filter(|n| {
            index.is_extra(n)
                && !wanted.contains(n.as_str())
                && !extras.contains(n.as_str())
                && !installed.contains_key(n.as_str())
                && !removed.contains(n.as_str())
                && !cfg.exclude().contains(n)
        }) {
            if let Some(path) = crate::vendor::system_binary_on_path(&layout.prefix, name, path_var)
            {
                record_state_if_changed(layout, name, crate::state::system(&path, None));
            }
        }
    }
    // A coherence tuple with a member NARROWED OUT — a `[packages]` include/exclude,
    // or a member removed on purpose — cannot install whole, and the group arm
    // below refuses it. Deciding that only AT the arm let one pass contradict
    // itself three lines apart (observed live 2026-08-27: `net-starting:`
    // announced trust-cg/ir/vc, the arm then refused the tuple over the
    // removed member `trust` while blaming include/exclude, the stub reconcile
    // laid PATH stubs promising the very install just refused, and the summary
    // claimed the set complete). Decide it HERE, before the stubs and the
    // announcement: name the true per-member cause once, then narrow the refused
    // members out of `wanted` so no stub, announcement, plan entry, or
    // completion claim ever speaks for them. The arm's own check stays as the
    // transaction's safety net; this prescan is what keeps it unreachable.
    let removed_markers = removed_programs(layout);
    let mut skipped_narrowed: Vec<String> = Vec::new();
    for group in crate::plan_groups(index, ch) {
        let Some(g) = &group.group else {
            continue;
        };
        let missing: Vec<String> = group
            .members
            .iter()
            .filter(|m| wanted.contains(m.as_str()) && !installed.contains_key(m.as_str()))
            .cloned()
            .collect();
        if missing.is_empty() {
            continue;
        }
        let narrowed_out: Vec<String> = group
            .members
            .iter()
            .filter(|m| !wanted.contains(m.as_str()) && !installed.contains_key(m.as_str()))
            .map(|m| {
                if removed_markers.contains(m.as_str()) {
                    format!("{m} (removed on this machine — `aterm pkg install --default-set` restores it)")
                } else {
                    format!("{m} ([packages] include/exclude)")
                }
            })
            .collect();
        if narrowed_out.is_empty() {
            continue;
        }
        eprintln!(
            "atpkg: coherence group '{g}' cannot install whole — narrowed out: {}; a locked \
             tuple installs whole, so its remaining member(s) are skipped: {}",
            narrowed_out.join(", "),
            missing.join(", ")
        );
        skipped_narrowed.extend(missing);
    }
    for p in &skipped_narrowed {
        wanted.remove(p.as_str());
    }
    skipped_narrowed.sort();
    // Pending-stub reconcile at the index resolve (R6): the SIGNED set replaces the
    // compiled roster the adoption-time stubs were laid from — newly listed names
    // gain stubs (PATH coverage before their bytes move), de-listed/removed/
    // installed names lose theirs, and every kept stub is rewritten so its embedded
    // atpkg path survives app relocation/self-update. `wanted` is already
    // triple-filtered above, so a stub is only ever laid for a program whose
    // bytes CAN move on this machine.
    let requires_of = requires_of(index);
    crate::stub::reconcile_with_requires(layout, &wanted, &extras, &installed, &requires_of);
    // ANNOUNCE BEFORE ACTING — the seed lane's law, now kept on the wire lane
    // too (the block comment at the caller had argued for it while nothing
    // announced). The set named is exactly what the loop below will attempt:
    // wanted ∧ absent, minus dev-linked groups (announcing a member the loop
    // then skips for a dev link would be a lie). Printed only when non-empty,
    // so the ordinary every-6-hours no-op tick stays silent — and only on the
    // NETWORK lane: the sealed-payload seed already announced itself with
    // `seed-starting:` and moves no byte over any socket, so `net-starting:`
    // there was a fabricated network claim (see [`ProvisionLane`]).
    let mut will_install: Vec<String> = Vec::new();
    for group in crate::plan_groups(index, ch) {
        if group
            .members
            .iter()
            .any(|m| crate::linkmode::is_linked(layout, m))
        {
            continue;
        }
        will_install.extend(
            group
                .members
                .iter()
                .filter(|m| wanted.contains(m.as_str()) && !installed.contains_key(m.as_str()))
                .cloned(),
        );
    }
    will_install.sort();
    // The signed sizes, derived only when the wire lane is about to speak. No extra fetch for
    // a manifest the triple probe above already pulled — the network fetcher memoises per
    // `(repo, program, build)` — and a member whose probe fetch failed is retried once here,
    // the first miss ending the sums.
    let sizes = if lane == ProvisionLane::Network && !will_install.is_empty() {
        seed_install_bytes(fetcher, index, cfg, &will_install)
    } else {
        SignedSizes::UNKNOWN
    };
    let announcement = net_announcement(lane, consent, &will_install, sizes);
    let announced = announcement.is_some();
    if let Some(line) = &announcement {
        emit_marker_line(line);
    }
    // The pass's plan, in PLAN ORDER, with each group's freshly-installable members —
    // materialized once so the priority queue below can permute what remains between
    // items without ever re-deciding WHAT is planned (reorder-only, §4).
    let groups = crate::plan_groups(index, ch);
    let plan: Vec<(usize, Vec<String>)> = groups
        .iter()
        .enumerate()
        .filter_map(|(i, group)| {
            let missing: Vec<String> = group
                .members
                .iter()
                .filter(|m| wanted.contains(m.as_str()) && !installed.contains_key(m.as_str()))
                .cloned()
                .collect();
            (!missing.is_empty()).then_some((i, missing))
        })
        .collect();
    // Every planned member, for the reorder-only intersection: a bump line naming
    // anything OUTSIDE this set — unknown, already installed, removed, garbage — is
    // ignored. The bump file can only permute installs the signed index authorized.
    let plannable: std::collections::BTreeSet<String> = plan
        .iter()
        .flat_map(|(_, missing)| missing.iter().cloned())
        .collect();
    // The live-progress plan (R5): per-program signed sizes fetched only when a sink
    // is live (the GUI lane) — N tiny verified manifest reads fix the overall bar's
    // denominator honestly before any byte moves. Best-effort: a miss degrades that
    // row to unmetered, never fails the pass.
    if let Some(sink) = crate::progress::active() {
        let planned: Vec<(String, u64)> = plan
            .iter()
            .flat_map(|(_, missing)| missing.iter())
            .map(|m| {
                let size =
                    crate::flow::planned_artifact_size(fetcher, index, ch, m, current_triple())
                        .unwrap_or(0);
                (m.clone(), size)
            })
            .collect();
        sink.plan(&planned);
    }
    // `program → pinned asset name` for every member either arm below resolves — filled
    // even when the member's download then fails, and carried out on the outcome so the
    // CALLER's pass-end gc can spare that member's `.part` resume state.
    let mut resolved_assets: std::collections::BTreeMap<String, String> =
        std::collections::BTreeMap::new();
    // Every channel-pinned program some group covers (grouped tuple or singleton);
    // wanted members left over are unpinned and fail loudly below.
    let mut pinned_members: std::collections::BTreeSet<String> = std::collections::BTreeSet::new();
    for group in &groups {
        pinned_members.extend(group.members.iter().cloned());
    }
    // THE PRIORITY QUEUE (§4): between items, re-read the bump file and stably
    // re-sort the REMAINDER — bumped items first, in bump (first-mention) order, plan
    // order as the tiebreak. The current item always finishes completely first
    // (in-flight bytes are never abandoned); bumping any member bumps its whole
    // group (the transactional activation is untouched); and a bumped program's
    // UNMET REQUIREMENTS ride ahead of it (§17.10), each progress row saying which
    // program pulled it ("bumped with brew"), so a jump never lifts a program above
    // what it needs. The bump file is consumed read-only here and deleted only at a
    // clean pass end.
    let mut remaining: Vec<(usize, Vec<String>)> = plan;
    while !remaining.is_empty() {
        let bump: Vec<String> = crate::progress::read_bump(layout)
            .into_iter()
            .filter(|n| plannable.contains(n))
            .collect();
        if !bump.is_empty() {
            let pulled = bump_with_requires(index, &groups, &bump, &plannable);
            let order: Vec<String> = pulled.iter().map(|(n, _)| n.clone()).collect();
            resort_bumped(&mut remaining, &order);
            if let Some(sink) = crate::progress::active() {
                for (name, with) in &pulled {
                    match with {
                        Some(w) => sink.bumped_with(name, w),
                        None => sink.bumped(name),
                    }
                }
                let order: Vec<String> = remaining
                    .iter()
                    .flat_map(|(_, missing)| missing.iter().cloned())
                    .collect();
                sink.queue(&order);
            }
        }
        let (gi, missing) = remaining.remove(0);
        let group = &groups[gi];
        // Dev-linked HARD-SKIP (§13): a singleton skips itself; a coherence tuple with
        // ANY linked member skips WHOLE (can't partial-install a locked group over a
        // dev link) — the same rule as `apply_channel`.
        if group
            .members
            .iter()
            .any(|m| crate::linkmode::is_linked(layout, m))
        {
            match &group.group {
                Some(g) => {
                    println!("atpkg: coherence group '{g}' has a dev-linked member — skipped whole")
                }
                None => println!("atpkg: {} dev-linked — skipped", group.members[0]),
            }
            for m in &missing {
                note_finished(m, crate::progress::Phase::Skipped, None);
            }
            continue;
        }
        // THE REQUIREMENT GATE (§17.10): a program never starts — never downloads a
        // byte — while one of its `requires` (a tuple's: the union of its members') is
        // not installed, system-satisfied or installed through its protocol. The plan
        // is dependency-first, so a requirement still unmet here failed this pass,
        // deferred (`needs admin`), was never opted in (an extra — never opted in on a
        // dependent's behalf), or cannot exist on this target. The member records
        // `blocked by <dep>: <dep state>` — the dependency's own row quoted, so the
        // line names whose act unblocks it — a DEFERRED state retried every pass, never
        // a failure; `record_state_if_changed` keeps the six-hourly tick silent.
        let requires = crate::apply::group_requires(index, group);
        if let Some((dep, dep_state)) = crate::requires::unmet_requirement(layout, index, &requires)
        {
            let state = crate::state::blocked(&dep, &dep_state);
            for m in &missing {
                record_state_if_changed(layout, m, state.clone());
                note_finished(m, crate::progress::Phase::Skipped, Some(state.clone()));
            }
            continue;
        }
        match &group.group {
            // A coherence tuple fresh-installs ALL-OR-NOTHING against the one index
            // resolved above (§7) — see [`bootstrap_group`].
            Some(g) => {
                failures += bootstrap_group(
                    layout,
                    fetcher,
                    index,
                    cfg,
                    g,
                    group,
                    &wanted,
                    &installed,
                    &missing,
                    &mut resolved_assets,
                );
            }
            // An ungrouped member can move alone (§7) — see [`bootstrap_singleton`].
            None => {
                failures += bootstrap_singleton(
                    layout,
                    fetcher,
                    anchor,
                    cfg,
                    &group.members[0],
                    now,
                    &mut resolved_assets,
                );
            }
        }
    }
    // Clean pass end consumes the bump file: every admitted line was either acted on
    // or proven outside the plan. A FAILED pass keeps it — the failed program's
    // priority survives to the retry pass, exactly like its `.part`.
    if failures == 0 {
        crate::progress::clear_bump(layout);
    }
    // Second stub reconcile, against what ACTUALLY landed: a program whose real
    // shims do not expose its own name would otherwise keep a stale "installing"
    // stub until the next pass.
    let now_active = crate::active_builds(layout);
    crate::stub::reconcile_with_requires(layout, &wanted, &extras, &now_active, &requires_of);
    // The reroute stubs ride the same reconcile: the embedded atpkg path is refreshed
    // and a row that left the table is swept (philosophy §4).
    lay_reroute_stubs(layout);
    reassert_rustup_seam(layout);
    // ALIAS reconcile over what is live NOW: every ALab program's `alab-<tool>` names
    // stand beside its shims (an install a pre-alias client made gets them here), and a
    // program the signed index no longer calls ALab's own loses them.
    reconcile_aliases(layout, index);
    // EXEC-ROOT reconcile over what is live NOW, after the aliases so the ones just laid
    // are rendered through a root that stands ([`reconcile_exec_roots`]): a trust member
    // this lane found up to date gets its shims routed here, one it just installed had its
    // root laid by the install itself.
    let _ = reconcile_exec_roots(layout, crate::seam::Depth::Shallow);
    // SHADOW reconcile (design S5), over what is live NOW: a managed member whose
    // exposed name a foreign copy precedes on PATH is recorded as SHADOWED — said, never
    // fixed — with `type alab-<tool> for the managed one` on the log line when the alias
    // just reconciled is what answers.
    reconcile_shadowed(layout, index.index_build, path_var);
    // Parity with the per-member install path: a wanted member the channel does not
    // PIN fails loudly as NotPinned — silence would hide a half-published index.
    for program in &wanted {
        if pinned_members.contains(program)
            || installed.contains_key(program)
            || crate::linkmode::is_linked(layout, program)
        {
            continue;
        }
        let e = crate::FlowError::NotPinned(program.clone());
        failures += 1;
        eprintln!("atpkg: bootstrap install {program} failed: {e} (continuing)");
        record_bootstrap_error(layout, program, &e);
    }
    // What a CLEAN pass proved still awaits install (the narrowed/unserved were
    // removed from `wanted` above, so this is pending work, not refused work) —
    // the `*seed*` row reconcile's input at both callers' edges.
    let remaining_missing: Vec<String> = wanted
        .iter()
        .filter(|p| !now_active.contains_key(p.as_str()))
        .cloned()
        .collect();
    DefaultSetOutcome {
        failures,
        announced,
        index_error: None,
        resolved_assets,
        skipped_narrowed,
        remaining_missing,
    }
}

/// Stably re-sort the remaining plan by the bump file's admitted names: a group
/// containing ANY bumped member moves to the front (bumping a member bumps the
/// whole group — the transactional activation stays whole), ordered by the
/// earliest bump mention among its members, with PLAN ORDER as the tiebreak
/// (`sort_by_key` is stable). Pure over its inputs, so the reorder-only property —
/// the output is a PERMUTATION of the input, never new work — is directly testable.
fn resort_bumped(remaining: &mut [(usize, Vec<String>)], bump: &[String]) {
    let rank_of = |missing: &[String]| {
        missing
            .iter()
            .filter_map(|m| bump.iter().position(|b| b == m))
            .min()
            .unwrap_or(usize::MAX)
    };
    remaining.sort_by_key(|(_, missing)| rank_of(missing));
}

/// The grouped (coherence-tuple) arm of [`install_default_set`]: refuse a config-narrowed
/// partial tuple, clean-skip a tuple with no artifact for this triple, else fresh-install
/// the WHOLE group all-or-nothing against the caller's ONE resolved index (§7) via
/// [`crate::flow::bootstrap_group`] — stage-all → flip-all → rollback, exactly the update
/// path's transaction, so no failure mode can activate a partial or version-split trust
/// toolchain. Returns the hard-failure count (0 or 1); skips are never failures.
#[allow(
    clippy::too_many_arguments,
    reason = "the group arm consumes install_default_set's whole resolved context: layout, \
              fetcher, the ONE verified index, config, the group + its name, the \
              wanted/installed/missing member sets the narrowing check and prescan read, \
              and the pass's resolved-asset collector the pass-end gc sparing reads"
)]
fn bootstrap_group(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    index: &crate::TrustedIndex,
    cfg: &crate::config::PackagesConfig,
    g: &str,
    group: &crate::Group,
    wanted: &std::collections::BTreeSet<String>,
    installed: &std::collections::BTreeMap<String, u64>,
    missing: &[String],
    resolved_assets: &mut std::collections::BTreeMap<String, String>,
) -> u32 {
    // If the narrowing include/exclude leaves part of the tuple absent,
    // refuse the WHOLE group — a deliberately partial tuple is exactly
    // the split state the transaction exists to prevent. Loud diagnostic,
    // not a loop failure (config-induced; correct every 6h pass).
    let narrowed_out: Vec<&String> = group
        .members
        .iter()
        .filter(|m| !wanted.contains(m.as_str()) && !installed.contains_key(m.as_str()))
        .collect();
    if !narrowed_out.is_empty() {
        eprintln!(
            "atpkg: [packages] include/exclude leaves {narrowed_out:?} out of \
             coherence group '{g}' — a locked tuple installs whole; skipping the group"
        );
        for m in missing {
            note_finished(m, crate::progress::Phase::Skipped, None);
        }
        return 0;
    }
    // Missing-triple prescan: the singleton NoArtifact clean-skip doctrine
    // lifted to the tuple — a group that cannot fully exist on this host
    // is a correct state, not a 6h scream.
    if let Some(m) =
        crate::flow::group_missing_triple(fetcher, index, cfg.channel(), current_triple(), missing)
    {
        println!(
            "atpkg: {m}: no artifact for {} — coherence group '{g}' skipped whole \
             (§6 clean skip)",
            current_triple()
        );
        for m in missing {
            note_finished(m, crate::progress::Phase::Skipped, None);
        }
        return 0;
    }
    match crate::flow::bootstrap_group(
        fetcher,
        layout,
        index,
        cfg.channel(),
        current_triple(),
        group,
        installed,
        resolved_assets,
    ) {
        Ok((crate::TxnOutcome::Applied(_), applied)) => {
            // Advance the durable floor to the ONE index the whole group
            // trusted (§8 gate 3).
            advance_floors(layout, index.index_build, index.roster_seq());
            for (m, a) in &applied {
                record_status(
                    layout,
                    m,
                    crate::ProgramStatus {
                        installed_build: Some(a.build),
                        state: crate::state::managed(a.build, index.index_build),
                        tree_root: effective_tree_root(layout, m, &a.tree_root),
                    },
                    format!(
                        "bootstrap installed {m} build {} (coherence group '{g}')",
                        a.build
                    ),
                );
                println!(
                    "atpkg: installed {m} build {} (default set, coherence group '{g}')",
                    a.build
                );
                note_finished(m, crate::progress::Phase::Done, None);
            }
            0
        }
        Ok((crate::TxnOutcome::UpToDate, _)) => {
            for m in missing {
                note_finished(m, crate::progress::Phase::Skipped, None);
            }
            0
        }
        Ok((crate::TxnOutcome::Pinned(held), _)) => {
            println!("atpkg: coherence group '{g}' held by local pin {held:?} — skipped");
            for m in missing {
                note_finished(m, crate::progress::Phase::Skipped, None);
            }
            0
        }
        Ok((crate::TxnOutcome::Blocked { dep, dep_state }, _)) => {
            // Unreachable in practice — this caller's own gate held the group before
            // it got here — but the transaction body applies the one rule too, so the
            // arm says the same words rather than nothing.
            let state = crate::state::blocked(&dep, &dep_state);
            for m in missing {
                record_state_if_changed(layout, m, state.clone());
                note_finished(m, crate::progress::Phase::Skipped, Some(state.clone()));
            }
            0
        }
        Ok((crate::TxnOutcome::Unpublished { member, triple, .. }, _)) => {
            // The transaction's own missing-triple hold, which fires only for a tuple with a
            // member already installed. A failed fetch proves nothing to either prescan, so
            // this is reached when a manifest a prescan could not fetch was fetched by the
            // walk and has no artifact for this triple: the same clean skip, in the same
            // words.
            println!(
                "atpkg: {member}: no artifact for {triple} — coherence group '{g}' skipped \
                 whole (§6 clean skip)"
            );
            for m in missing {
                note_finished(m, crate::progress::Phase::Skipped, None);
            }
            0
        }
        Ok((crate::TxnOutcome::Tombstoned(members), _)) => {
            eprintln!(
                "atpkg: coherence group '{g}': pins tombstoned for {members:?} — \
                 nothing installed"
            );
            // A CANONICAL ROW, not silence: a yanked pin used to leave the missing
            // member with no row at all, so `status` listed nothing for a program the
            // index names and the toolset read as merely incomplete. The words are the
            // update lane's (`TOMBSTONED_PIN`), and doctor counts them as the fault they are.
            for m in missing {
                let state = crate::state::TOMBSTONED_PIN.to_string();
                record_state_if_changed(layout, m, state.clone());
                note_finished(m, crate::progress::Phase::Skipped, Some(state));
            }
            0
        }
        Ok((
            crate::TxnOutcome::Aborted {
                failed,
                during_flip,
                why,
            },
            _,
        )) => {
            let phase = if during_flip {
                "flip (already-flipped members rolled back)"
            } else {
                "stage (nothing flipped)"
            };
            let cause = clip_cause(&why);
            eprintln!(
                "atpkg: bootstrap of coherence group '{g}' aborted at {failed} \
                 during {phase} — {cause}; no member left changed (all-or-nothing, continuing)"
            );
            record_status(
                layout,
                &failed,
                // An already-installed member keeps its honest build AND its honest
                // attestation; a fresh member records neither. An all-or-nothing
                // transaction that aborted changed no member's bytes, so erasing the signed
                // root here made `atpkg verify` fail closed on a program the abort had
                // explicitly protected — the one site the round-8 "keep the recorded root"
                // sweep missed, on the very line below a field already making that argument.
                // Same primitive as every other failure arm: the store, not the caller's
                // snapshot, is asked what survived.
                failure_row(
                    layout,
                    &failed,
                    format!("error: coherence group '{g}' bootstrap aborted — {cause}"),
                ),
                format!("bootstrap group '{g}' aborted at {failed} — {cause}"),
            );
            // Honest terminal rows: the member that failed carries the reason; its
            // siblings did not install either (all-or-nothing), and saying so beats
            // a row frozen mid-phase forever.
            for m in missing {
                let why = if *m == failed {
                    format!("coherence group '{g}' bootstrap aborted at {failed}")
                } else {
                    format!("coherence group '{g}' aborted at {failed} — nothing changed")
                };
                note_finished(m, crate::progress::Phase::Failed, Some(why));
            }
            1
        }
        Err(e) => {
            eprintln!("atpkg: bootstrap of coherence group '{g}' failed: {e} (continuing)");
            for m in missing {
                note_finished(
                    m,
                    crate::progress::Phase::Failed,
                    Some(format!("coherence group '{g}' bootstrap failed: {e}")),
                );
            }
            1
        }
    }
}

/// The ungrouped arm of [`install_default_set`]: the per-member install path, which keeps
/// its signed `requires` dependency resolution (§17). Re-reads the durable floor so a
/// floor advance recorded earlier in the pass is never undercut by the entry-time
/// snapshot. Returns the hard-failure count (0 or 1); the correct non-failure states
/// (dev-link, missing triple, app-bundle, tombstoned pin) are skips.
fn bootstrap_singleton(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    anchor: &crate::Anchor,
    cfg: &crate::config::PackagesConfig,
    program: &str,
    now: i64,
    resolved_assets: &mut std::collections::BTreeMap<String, String>,
) -> u32 {
    let floor = build_floor(layout);
    let req = crate::InstallRequest {
        channel: cfg.channel(),
        program,
        triple: current_triple(),
        installed: None,
    };
    // The collecting form: on the failure arm below there is no report, and the failed
    // download is exactly the member whose `.part` the caller's pass-end gc must spare.
    match crate::flow::install_collecting_assets(
        fetcher,
        layout,
        anchor,
        &req,
        floor,
        now,
        resolved_assets,
    ) {
        Ok(r) => {
            // Advance the durable floor to the index this install trusted
            // (§8 gate 3).
            advance_floors(layout, r.index_build, r.roster_seq);
            record_installed_deps(layout, program, r.index_build, &r.dependencies);
            // An OS-installed member: the lane's canonical row, said only when it
            // changes (the unattended pass never elevates, so `needs admin` recurs
            // every six hours and must not re-announce itself each time).
            if let Some(outcome) = &r.protocol {
                record_state_if_changed(layout, program, outcome.state(program));
                note_finished(
                    program,
                    if outcome.is_deferred() {
                        crate::progress::Phase::Skipped
                    } else {
                        crate::progress::Phase::Done
                    },
                    None,
                );
                return 0;
            }
            let state = crate::state::managed(r.build, r.index_build);
            record_status(
                layout,
                program,
                crate::ProgramStatus {
                    installed_build: Some(r.build),
                    state: state.clone(),
                    tree_root: effective_tree_root(layout, program, &r.tree_root),
                },
                format!("bootstrap installed {program} build {}", r.build),
            );
            println!(
                "atpkg: installed {program} build {} (default set) — {state}",
                r.build
            );
            note_finished(program, crate::progress::Phase::Done, None);
            0
        }
        // Correct non-failure states — skipped quietly-but-visibly:
        Err(crate::FlowError::Linked(p)) => {
            println!("atpkg: {p} dev-linked — skipped");
            note_finished(program, crate::progress::Phase::Skipped, None);
            0
        }
        // A target the pinned build does not serve: a CANONICAL STATE, not a failure
        // (design §2). The pass log says the same words the row records.
        Err(e @ crate::FlowError::NoArtifact(_)) => {
            let state = canonical_non_error_state(&e)
                .unwrap_or_else(|| crate::state::unavailable(current_triple(), ""));
            println!("atpkg: {program}: {state} — skipped, not failed");
            record_status(
                layout,
                program,
                failure_row(layout, program, state),
                format!("bootstrap install {program}: {e}"),
            );
            note_finished(program, crate::progress::Phase::Skipped, None);
            0
        }
        Err(crate::FlowError::AppBundleRefused(_)) => {
            println!(
                "atpkg: {program}: app-bundle member — aterm updates itself in-session through \
                 its own updater, skipped"
            );
            note_finished(program, crate::progress::Phase::Skipped, None);
            0
        }
        Err(e @ crate::FlowError::Tombstoned(_)) => {
            eprintln!("atpkg: {program}: {e} — nothing installed");
            // The same canonical row the group arm and the update lane write.
            let state = crate::state::TOMBSTONED_PIN.to_string();
            record_state_if_changed(layout, program, state.clone());
            note_finished(program, crate::progress::Phase::Skipped, Some(state));
            0
        }
        Err(e) => {
            eprintln!("atpkg: bootstrap install {program} failed: {e} (continuing)");
            record_bootstrap_error(layout, program, &e);
            note_finished(program, crate::progress::Phase::Failed, Some(e.to_string()));
            1
        }
    }
}

/// Record one hard bootstrap failure for `program` into `status.toml` — shared by the
/// ungrouped install arm and [`install_default_set`]'s unpinned-member sweep, which
/// perform identical writes.
fn record_bootstrap_error(layout: &crate::store::Layout, program: &str, e: &crate::FlowError) {
    let state = canonical_non_error_state(e).unwrap_or_else(|| format!("error: {e}"));
    record_status(
        layout,
        program,
        failure_row(layout, program, state),
        format!("bootstrap install {program}: {e}"),
    );
}

/// `atpkg install --default-set` — the explicit one-shot §11 bootstrap: install every
/// missing default-set member NOW (the Settings "Install ALab toolset" button's verb;
/// the config-consent-free twin of the `auto_install = true` loop arm — running it IS
/// the consent). Exit 1 iff any member hard-failed; skips are honest and free.
fn cmd_install_default_set() -> ExitCode {
    // THE LANE THIS GUARD EXISTS FOR. This verb announces `net-starting:` from inside
    // `install_default_set` and then ends through four separate returns, none of which
    // printed a marker — so the held "Installing the ALab toolchain…" card had no
    // answer at all, and its only reader had to guess one from an absence (2026-09-11).
    let code = cmd_install_default_set_code();
    answer_announcement(code == 0);
    ExitCode::from(code)
}

/// [`cmd_install_default_set`]'s body, returning the raw exit code: exit 2 ("ran fine,
/// installed nothing, and never will here") is neither success nor a retryable failure,
/// and the announcement must be answered with which of the three actually happened.
fn cmd_install_default_set_code() -> u8 {
    // R5: the machine settings first, above the manager gate and everything that can
    // fail (see the note in `cmd_update_all_code`).
    apply_machine_settings_once();
    if !manager_enabled() {
        eprintln!("atpkg: disabled (no root key pinned) — refusing to install");
        return 1;
    }
    let Some(layout) = layout() else {
        return 1;
    };
    let cfg = crate::config::cached();
    reconcile_links(&layout, cfg);
    // Asking for the toolset is an unambiguous change of mind about any earlier
    // removal; undoing a decline must never mean hunting for a marker file. The
    // per-program removals go too — "install the whole set" plainly includes them.
    // CLEARED BEFORE THE PASS, not after: the pass narrows `wanted` by these very
    // markers, so the old after-ordering made the one verb whose doctrine says
    // "change of mind" still honour the old mind for exactly one pass — a removed
    // coherence-group member narrowed its whole tuple out of the very install
    // that was asked to restore it (observed live 2026-08-27: a legacy-era
    // `trust` removal kept trust-cg/ir/vc out of the explicit toolset install,
    // which then reported the set complete).
    clear_decline(&layout);
    let all_removed: Vec<String> = removed_programs(&layout).into_iter().collect();
    clear_removed(&layout, &all_removed);
    // The seed pass's claim on the set goes too. From here on the owner's own act stands
    // behind it, and this verb never reads `seed_install`, so no later update pass may say
    // "installing aterm is the consent" or name that key ([`clear_seed_adoption`]).
    clear_seed_adoption(&layout);
    let fetcher = resolve_fetcher(&layout);
    let before = crate::active_builds(&layout);
    // Running this verb is the consent, and it never reads `seed_install`: its announcement
    // names neither the first launch's consent nor that key, only the decline that stops
    // every pass ([`Consent::Explicit`]).
    let failures_outcome = install_default_set(
        &layout,
        &*fetcher,
        &effective_anchor(&layout),
        cfg,
        ProvisionLane::Network,
        Consent::Explicit,
        now_unix(),
        None,
    );
    let failures = failures_outcome.failures;
    let activated = crate::active_builds(&layout)
        .keys()
        .filter(|k| !before.contains_key(k.as_str()))
        .count();
    // The explicit "Install ALab toolset" act IS adoption — when it laid something
    // down, and when it found the whole set already on disk ([`default_set_adopts`]).
    // Adoption grants unattended network set-completion later (`cmd_update_all`), so
    // it is NOT recorded after a pass that installed nothing on a machine that received
    // no toolset at all (the "nothing lands here" exit below), nor over members the
    // pass skipped or still owes.
    if default_set_adopts(
        activated,
        failures,
        !before.is_empty(),
        &failures_outcome.skipped_narrowed,
        &failures_outcome.remaining_missing,
    ) {
        record_adoption(&layout);
    }
    // GC + shell-hook refresh once at the CLI edge (the cmd_update_all precedent) — including
    // its disclosure of what the pass abstained on, for the same reason: this verb walks the
    // whole prefix, so a skip here is not about a program the user never mentioned.
    //
    // The SPARING form, not plain `run`: a member whose multi-GB download failed mid-pass
    // keeps its `.part` — this very sweep used to destroy the resume state, so retrying
    // the bootstrap refetched from byte 0. Programs the pass resolved nothing for are
    // swept exactly as before; the standalone `atpkg gc` still reclaims everything.
    let report = crate::gc::run_keeping_pinned_partials(&layout, &|p| {
        failures_outcome.resolved_assets.get(p).cloned()
    });
    print_gc_sweeps("install-default-set", &report);
    print_gc_abstentions("install-default-set", &report);
    crate::hooks::refresh(&layout);
    if failures > 0 {
        return 1;
    }
    // The `*seed*` offer, if one was ever announced, is answered by what this
    // CLEAN pass just proved: retired when nothing remains to offer (the offer
    // was taken — or was stale, see `reconcile_seed_status`), refreshed when a
    // remainder still awaits consent. `record_status` MERGES the program map, so
    // a row nobody reconciles lives forever and Settings ▸ Packages keeps
    // advertising an install the user already performed.
    reconcile_seed_status(&layout, &failures_outcome.remaining_missing);
    let skipped = &failures_outcome.skipped_narrowed;
    // "Zero failures" is NOT "it worked". Every member can clean-skip — no artifact
    // for this triple (§6), all dev-linked, all tombstoned — and that path used to
    // print "default set complete" and exit 0, which the Packages page renders as
    // "ALab toolset install completed" over an empty program list. Telling a user in
    // green that a multi-GB toolchain installed when nothing happened is the exact
    // silent-and-green shape this codebase keeps having to root out, so the
    // no-op case gets its own words and its own exit code. The same shape hid a
    // second lie one size smaller: "default set already complete" over members the
    // pass had just REFUSED (a narrowed tuple), so a summary with skips now names
    // them instead of claiming completeness (observed live 2026-08-27).
    if activated == 0 {
        let already = !before.is_empty();
        if already {
            if skipped.is_empty() {
                println!("atpkg: default set already complete — nothing to install");
            } else {
                println!(
                    "atpkg: default set: nothing installed — {} program(s) skipped: {} \
                     (reasons above)",
                    skipped.len(),
                    skipped.join(", ")
                );
            }
            print_managed_current(&layout, &*fetcher, cfg);
            print_provenance_carriers(&layout);
            record_success(&layout);
            record_index_freshness(&layout, None);
            return 0;
        }
        println!(
            "atpkg: nothing was installed — the signed index pins no program with a build \
             for this machine ({}). This is not a failure to retry; nothing lands here \
             until an artifact for this architecture is published.",
            current_triple()
        );
        // Exit 2, not 0 and not 1: the GUI maps 0 to "Succeeded" (which would be the
        // lie) and 1 to a retryable failure (which would be false hope).
        return 2;
    }
    if skipped.is_empty() {
        println!("atpkg: default set complete ({activated} program(s) installed)");
    } else {
        println!(
            "atpkg: default set: {activated} program(s) installed, {} skipped: {} \
             (reasons above)",
            skipped.len(),
            skipped.join(", ")
        );
    }
    print_managed_current(&layout, &*fetcher, cfg);
    print_provenance_carriers(&layout);
    record_success(&layout);
    record_index_freshness(&layout, None);
    0
}

/// `atpkg seed` — the batteries-included first-run bootstrap (§9.1/§11),
/// resurrected 2026-08-17 as the SIGNED lane only (the keyless source-build
/// lane stays deleted, `ba832933`): if a release cut sealed a signed seed
/// registry beside this executable, fill the EMPTY store from it — zero
/// network required — through the UNCHANGED anchor + freshness + floor +
/// sha256 + `tree_root` gates. The verb the GUI spawns once per launch.
///
/// The seed is a BOOTSTRAP source (see [`resolve_fetcher`]): once the store
/// holds anything, updates belong to the network + cache path, so the lane is
/// skipped entirely rather than resolving an index it will not use. Consent:
/// `[packages].seed_install` (default TRUE — the bytes are already on disk,
/// sealed under the app's own code signature, so installing the app is the
/// consent; ~3.2 GB lands in the store on extraction). `false` announces the
/// offer instead ([`announce_pending_seed`]) and Settings ▸ Packages carries
/// the act. Store mutation is serialized by the store-wide lock held at the
/// dispatch edge (`seed` is in [`verb_mutates_store`]). Every skip is loud
/// and exit-0 — absence of a seed is a legal state, not a failure.
///
/// # This verb is LOCAL-ONLY, and that is a consent property, not an optimization
///
/// The fetcher here is a bare [`crate::DirFetcher`] over the sealed registry —
/// **never** [`resolve_fetcher`]'s chain. `seed_install` defaults TRUE on
/// exactly one argument: the bytes already shipped inside the app, so laying
/// them down costs disk and no network. Handing this path the chain would make
/// that argument false. The chain's network leg is the index AUTHORITY, so the
/// moment the published index outranks the sealed one (the registry publishes
/// independently of app cuts — §9.2), its pins name builds the seed never
/// sealed, every asset misses by build-qualified name, and the "local bytes"
/// bootstrap silently pulls gigabytes over a metered link with
/// `auto_install = false` still set. `auto_install` is the switch that exists
/// to stop precisely that, and it gates the NETWORK bootstrap
/// ([`cmd_update_all`]) — which runs moments later on the same launch, and is
/// where a machine that wants fresher-than-sealed bytes gets them, with
/// consent. Staleness here is therefore correct and self-healing: the seed
/// lays down what it carries, the consented network pass upgrades it.
fn cmd_seed(rest: &[String]) -> ExitCode {
    if !rest.is_empty() {
        eprintln!("usage: atpkg seed");
        return ExitCode::from(2);
    }
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    // R5: the machine settings first — BEFORE the decline check, because a user who
    // removed the toolset still has a cursor that roams and a home Spotlight walks,
    // and before the seal, the network and every other thing this lane can fail at
    // (see the note in `cmd_update_all_code`).
    apply_machine_settings_once();
    if declined(&layout) {
        // The user removed this toolset deliberately (`uninstall --all`). The seed
        // lane installs whatever the store lacks, so without this check it would
        // undo that on the very next launch.
        println!(
            "atpkg: the ALab toolset was removed on this machine — not re-installing it. \
             `aterm pkg install --default-set` brings it back."
        );
        return ExitCode::SUCCESS;
    }
    // INSTALLING ATERM IS WANTING THE TOOLSET. Adoption is recorded here — on the
    // first run of this lane, before any question of whether a seal exists —
    // because the sealed registry is an optimisation for HOW the toolchain
    // arrives, never a condition on WHETHER it does.
    //
    // Recording it only after a successful seed install left a hole exactly where
    // the product could least afford one: a machine that got the LEAN container
    // (every Intel Mac today, and any deliberately seedless cut) has no seal, so it
    // was never adopted, so `cmd_update_all` short-circuited on
    // `installed.is_empty() && !complete_the_set` and printed "nothing installed to
    // update" — forever. That user could never receive the ALab toolchain, not even
    // now that x86_64 artifacts publish, unless they hand-edited
    // `[packages].auto_install = true` into a config file that does not exist until
    // the app has already run. "Installing aterm installs the packages" has to be
    // true on every Mac, not only the ones the seal happens to serve.
    //
    // `declined` is checked above, so a deliberate `uninstall --all` still wins —
    // and so does `seed_install = false`, which is checked BEFORE this line for the
    // same reason. Adopting first made that flag a lie: the machine became adopted,
    // `cmd_update_all` then saw `complete_the_set` and installed the entire ~3.2 GB
    // set over the NETWORK seconds later, bypassing `auto_install` — the exact
    // unconsented multi-GB path the bare local `DirFetcher` exists to prevent. The
    // one documented way to say "not on my disk" has to be honoured before anything
    // records a wish for the toolset.
    let cfg = crate::config::cached();
    if !cfg.seed_install() {
        // Declining the lay-down is not adoption. Announce what is on offer (if
        // anything) and leave the store exactly as it was.
        if let Some(seed_dir) = crate::bundled_seed_dir()
            && manager_enabled()
        {
            let fetcher = crate::DirFetcher::new(seed_dir.clone());
            announce_pending_seed(&layout, &fetcher, cfg, &seed_dir);
        } else {
            println!("atpkg: [packages].seed_install = false — not installing the ALab toolset");
        }
        return ExitCode::SUCCESS;
    }
    // A DISABLED MANAGER ADOPTS NOTHING. `ATPKG_DISABLE` (or a build that pins no
    // root key) is the kill switch every other mutating lane consults before it
    // touches the store, and it must be consulted here BEFORE adoption for the same
    // reason `seed_install = false` is: this used to run only after the adoption
    // marker, the pending stubs, the reroute stubs and the rustup seam were all
    // written. The laid stubs then told the user "an open aterm window starts a pass
    // within seconds … or run: aterm pkg update" — a pass that, under the same
    // switch, answers "disabled — nothing to update" — and the durable adoption made
    // the first tick after the switch was lifted install the whole set over the
    // network, a consent this lane never obtained. Machine settings (above) are host
    // state, not the store, and stay exempt as in `cmd_update_all_code`.
    if !manager_enabled() {
        match crate::bundled_seed_dir() {
            Some(seed_dir) => println!(
                "atpkg: bundled seed present ({}) but the manager is disabled — lane skipped \
                 (fail-closed)",
                seed_dir.display()
            ),
            None => println!("atpkg: the manager is disabled — lane skipped (fail-closed)"),
        }
        return ExitCode::SUCCESS;
    }
    // Whether THIS run is the one adopting: the whole-set disk gate below takes back
    // only the adoption it recorded (see [`refuse_seed_for_disk`]), never one an
    // earlier launch or an explicit `install --default-set` made.
    let adopted_by_this_run = !adopted(&layout);
    // Who adopted rides beside the marker, and only when this call creates it: an update
    // pass says "installing aterm is the consent" only on a machine the seed pass adopted.
    record_seed_adoption(&layout);
    // PENDING STUBS AT ADOPTION (R6): the instant this machine wants the toolset,
    // every default-set name resolves on PATH — BEFORE any question of whether a
    // seal exists, before a single network byte moves. Running one prints the live
    // install state (`atpkg __pending`) instead of "command not found". Compiled
    // roster only; the first index resolve reconciles it against the signed set.
    crate::stub::lay_adoption_stubs(&layout);
    // REROUTE STUBS beside them (philosophy §4): the upstream Rust names answer inside a
    // session from the first seed on, not only once the spawn seam has laid them.
    lay_reroute_stubs(&layout);
    reassert_rustup_seam(&layout);
    // The EXEC ROOTS beside the seam, and for the same reason the seam is re-asserted
    // here: this lane runs at every launch, and most launches return below without
    // installing anything (no seal, a spent seal, nothing the seal can still serve). A
    // machine already on a trust build whose tippy refuses its own `bin/rustc` copy is
    // healed at the next launch rather than at the next networked pass; a launch that
    // does install reaches the default-set lane's reconcile as well.
    let _ = reconcile_exec_roots(&layout, crate::seam::Depth::Shallow);
    let Some(seed_dir) = crate::bundled_seed_dir() else {
        // DO NOT PROMISE WHAT THE INDEX CANNOT DELIVER. This used to assert the
        // toolset would be "kept current and complete from here on" without asking
        // whether the index publishes anything for THIS machine — false on every
        // Intel Mac, which is the lean container's entire audience. (The sentence
        // also carried a 14-space run from a lost line continuation.)
        report_seedless_posture(&layout);
        return ExitCode::SUCCESS;
    };
    // LOCAL-ONLY by construction (see the consent note above): the sealed
    // registry, served through the identical verify + floor + freshness gates
    // as any network source — a dir holds bytes, not trust.
    let fetcher = crate::DirFetcher::new(seed_dir.clone());
    // Resolve the sealed index FIRST, so an unusable seed is a loud SKIP rather
    // than the hard failure `install_default_set` reports for an unresolvable
    // index. Every reason a seed can be unusable here is a correct state the
    // once-per-launch pass must not scream about: freshness lapsed (the DMG
    // outlived its horizon), or the store's durable floor already sits above
    // the sealed index_build (this machine has trusted something newer). In
    // both cases the consented network lane is the answer, not an error.
    let anchor = effective_anchor(&layout);
    let index = match crate::resolve_verified_index(
        &fetcher,
        &layout,
        &anchor,
        build_floor(&layout),
        now_unix(),
    ) {
        Ok(i) => i,
        Err(e) => {
            println!(
                "atpkg: bundled seed present ({}) but its index is not usable here: {e} \
                 — leaving bootstrap to the network lane",
                seed_dir.display()
            );
            return ExitCode::SUCCESS;
        }
    };
    // WHAT THIS SEAL CAN STILL DO FOR THIS MACHINE, decided before anything is
    // announced or extracted. Two questions, and the old code answered neither:
    //
    //  * Is there work left? The gate used to be "the store is empty", which made
    //    the lane single-shot: a first launch interrupted halfway (the extraction
    //    is minutes long) left some members installed, and every later launch then
    //    returned early and never touched the seal again — stranding the rest on a
    //    network that, for the sealed builds, is a strictly worse source. Asking
    //    instead for the members this store LACKS makes the lane resumable.
    //    Bootstrap-only-ness is not weakened: it is enforced by the seed being a
    //    bare local `DirFetcher` outranked by any newer network index, not by the
    //    store happening to be empty.
    //  * Can it serve THIS triple at all? A seal carrying no artifact for this Mac's
    //    architecture answers no. Coverage is per program, not per catalogue: the
    //    standalone programs publish x86_64-apple-darwin beside aarch64-apple-darwin,
    //    while the rustc coherence tuple is aarch64-only, so a seal can serve an Intel
    //    Mac in part or not at all. Saying "installing…" first and discovering the
    //    answer afterwards is how a user ends up watching a progress notice that never
    //    resolves.
    let wanted = seed_serviceable(&layout, &fetcher, &index, cfg);
    if wanted.is_empty() {
        return finish_unusable_seed(&layout, &seed_dir, &fetcher, &index, cfg);
    }
    // Parity with the other mutating network verbs: a config-declared dev-link
    // must exist BEFORE the pass decides what it manages (linked members
    // hard-skip, §13) — a dev box's first run must not pave over a checkout.
    reconcile_links(&layout, cfg);
    // WHOLE-SET DISK GATE, before a single byte is extracted. The per-member and
    // per-group preflights inside `flow` each ask "does THIS member fit", which is
    // the wrong question for a first run: on a laptop with a few GB free the small
    // solvers install, the trust tuple then fails its own gate, and the machine is
    // left permanently partial — provers but no compiler — with the cause recorded
    // nowhere. Asking for the total first turns that into one honest refusal that
    // names the number, and leaves the seal intact for a retry after the user frees
    // space.
    // THE size, derived ONCE from the sealed manifests at print time and reused by
    // every line below (the disk gate, its status row, and the announcement).
    // Three derivations rendered three ways is how one install came to quote three
    // different figures for the same bytes (install.sh's "~4.2 GB" header, this
    // lane's "~4.7 GB", 4.08 GB measured). Unit convention: rendered ONLY via
    // [`crate::cost::human_bytes`] — see [`seed_announcement`]. The on-disk figure
    // only: the sealed payload moves no network bytes, so there is no download to
    // disclose.
    let signed_bytes = seed_install_bytes(&fetcher, &index, cfg, &wanted).disk;
    if let Some(need) = signed_bytes {
        let have = crate::freespace::available_bytes(&layout.prefix);
        if refuse_seed_for_disk(&layout, need, have, adopted_by_this_run) {
            emit_marker_line(&format!(
                "atpkg: {SEED_FAILED_MARKER}the ALab toolset needs {} free (plus a \
                 {} reserve) and this disk has {} — nothing was installed, and \
                 the bundled copy is kept for when space is available",
                crate::cost::human_bytes(need),
                crate::cost::human_bytes(crate::cost::FREE_FLOOR),
                crate::cost::human_bytes(have.unwrap_or(0))
            ));
            return ExitCode::SUCCESS;
        }
    }
    // ANNOUNCE BEFORE ACTING, with the SIGNED size rather than a guess. Extracting
    // the seal is minutes of work and gigabytes of disk, and until this marker the
    // only honest description of a first launch was "the app silently starts
    // consuming disk". The GUI streams this child's stdout, so the notice lands
    // while it happens rather than after (crates/aterm-gui, `parse_seed_line`).
    emit_marker_line(&seed_announcement(wanted.len(), signed_bytes));
    let before = crate::active_builds(&layout);
    // The SEED progress pass (R5): same writer, `pass: "seed"` — verify/extract/link
    // phases with no download rows (the fetcher is a local dir; the sealed-seed
    // lane's bytes are untouched). Owned here so `install_default_set` sees a live
    // pass and does not begin its own "net" one. The lane rides alongside for the
    // same reason in the other channel: SealedPayload must never announce
    // `net-starting:`, which claimed a network install for a lane whose whole
    // consent argument is that it never touches one (see [`ProvisionLane`]).
    let owned_pass = progress_path()
        .is_some_and(|p| crate::progress::begin_pass(p, ProvisionLane::SealedPayload.pass_name()));
    let failures = install_default_set(
        &layout,
        &fetcher,
        &anchor,
        cfg,
        ProvisionLane::SealedPayload,
        Consent::InstallingAterm,
        now_unix(),
        None,
    )
    .failures;
    if owned_pass {
        crate::progress::end_pass();
    }
    // New shims must reach interactive shells without a relaunch.
    crate::hooks::refresh(&layout);
    let after = crate::active_builds(&layout);
    let mut new: Vec<String> = after
        .keys()
        .filter(|k| !before.contains_key(k.as_str()))
        .cloned()
        .collect();
    if !new.is_empty() {
        new.sort();
        // Mark what the SEED laid down as PROVISIONAL. The seal is a cut-time
        // snapshot; the update pass that runs seconds later replaces whatever the
        // published index has moved, and under GC's ordinary live+1-rollback rule
        // each replaced seed build would be retained — a second ~3.2 GB copy of
        // `trust` preserving a state nobody ran. See `crate::provisional`.
        let provisional: Vec<(String, u64)> = new
            .iter()
            .filter_map(|p| after.get(p.as_str()).map(|b| (p.clone(), *b)))
            .collect();
        crate::provisional::record(&layout, &provisional);
        // The offer, if one was ever announced, is now TAKEN — retire the row
        // before announcing the install, or Settings ▸ Packages keeps
        // advertising a pending offer the user already has on disk
        // (`record_status` MERGES, so nobody else ever removes it).
        clear_seed_status(&layout);
        // The stable marker the GUI parses — change it and the first-run
        // notice goes blind (crates/aterm-gui, spawn_pkg_update_check).
        emit_marker_line(&format!("atpkg: {SEED_INSTALLED_MARKER}{}", new.join(", ")));
        // The marker above is the GUI's (byte-stable, parsed); this line is the human's:
        // an install that lands 10 commands and never says how to run one is a dead end.
        println!("{SEED_FOLLOW_ON}");
    }
    // RECLAIM only on a CLEAN pass. The payload is dead weight once extracted —
    // ~600 MB on top of the gigabytes it expanded into — but "extracted" has to
    // mean all of it. A partial pass (a disk that filled, one corrupt artifact)
    // leaves members the seal still holds locally, and deleting it there would
    // force the exact bytes that were on this disk a second ago to come back over
    // the network. `failures == 0` is what separates "consumed" from "interrupted",
    // and the lane is resumable now, so keeping it is what lets the next launch
    // finish the job.
    //
    // Deleting is signature-SAFE only because of WHERE the payload lives: a
    // `.lproj` directory, sealed `optional = true` by codesign's built-in rules,
    // so the bundle still verifies with it entirely absent (measured — see
    // `crate::bundled::SEED_DIR_NAME`). Doing this to a normally-sealed resource
    // would break `codesign --verify` and, with it, the updater's verification of
    // the installed bundle at the next apply.
    if failures == 0 {
        // The toolset is in: retire any standing "blocked"/"unavailable" row this
        // pass just disproved, so a disk-space or architecture verdict from an
        // earlier launch stops being Settings' answer forever.
        clear_status_row(&layout, "*toolset*");
        reclaim_bundled_seed(&seed_dir);
        // (R5's machine settings ran at the TOP of this lane.)
    } else {
        println!(
            "atpkg: keeping the bundled seed — {failures} member(s) did not install, and the \
             seal still holds them locally for the next attempt"
        );
    }
    // ALWAYS ANSWER THE ANNOUNCEMENT. `seed-starting:` already put "Installing the
    // ALab toolchain…" on screen; if this pass then installs nothing and emits no
    // terminal marker, that notice is the last word the user ever gets — an install
    // that announced itself, delivered nothing, and never said why. `new` being
    // empty here means every member failed (a clean skip cannot happen: the
    // serviceable prescan ran before the announcement), so this is the failure
    // marker the GUI needs to retire the notice honestly.
    if new.is_empty() {
        emit_marker_line(&format!(
            "atpkg: {SEED_FAILED_MARKER}no ALab program could be installed from the bundled \
             registry ({failures} failed) — the toolset will be retried on the next launch \
             and can also come from the network"
        ));
    } else if failures > 0 {
        // PARTIAL. This is the likeliest real first-launch failure — a laptop
        // without ~4 GB free installs the small tools and then the disk preflight
        // refuses the trust tuple — and it used to print ONLY `seed-installed:`,
        // so the user got "✓ ALab toolchain installed" with no compiler present.
        // A green tick over a missing compiler is worse than an error, because
        // nothing prompts the user to look. The marker carries the count so the
        // notice can say what is actually true.
        emit_marker_line(&format!(
            "atpkg: {SEED_PARTIAL_MARKER}{} installed, {failures} could not be installed — the \
             rest is retried on the next launch (Settings ▸ Packages shows which)",
            new.len()
        ));
    }
    // ANSWER WHAT THIS VERB ANNOUNCED. A no-op today — every path below the
    // announcement above prints a terminal of its own — and kept precisely so it
    // stays a no-op: a future early return between the announcement and here
    // cannot leave the GUI's held card unanswered, because answering is no longer
    // that return's job (2026-09-11).
    answer_announcement(failures == 0);
    if failures == 0 {
        ExitCode::SUCCESS
    } else {
        ExitCode::from(1)
    }
}

/// The channel-pinned members this SEAL can still install on THIS machine: wanted by
/// config, not already active, and carrying an artifact for this triple.
///
/// The triple test is the one that matters for honesty. Artifact coverage is per
/// program, not per catalogue: the standalone programs publish `x86_64-apple-darwin`
/// beside `aarch64-apple-darwin`, while the rustc coherence tuple (`trust`, `trust-ir`,
/// `trust-cg`, `trust-vc`) is `aarch64-apple-darwin` only. So this returns the part of
/// the seal that can be served here — possibly nothing — and the caller can say which
/// BEFORE announcing an install, rather than printing "installing…", clean-skipping
/// every member inside `install_default_set`, and leaving a progress notice that never
/// resolves.
///
/// Group granularity matches the installer's: a coherence tuple is all-or-nothing, so a
/// tuple with any member lacking this triple contributes nothing (the same rule
/// `announce_pending_seed` applies, and the same one `install_default_set` acts on).
fn seed_serviceable(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    index: &crate::TrustedIndex,
    cfg: &crate::config::PackagesConfig,
) -> Vec<String> {
    let Some(ch) = index.channel_for(cfg.channel(), current_triple()) else {
        return Vec::new();
    };
    let ch = &ch;
    let installed = crate::active_builds(layout);
    let wanted = wanted_programs(layout, index, cfg);
    // Programs the user removed on purpose are NOT missing — they are absent by
    // request, and the lane's "install whatever the store lacks" rule would
    // otherwise reinstate them on the next launch.
    let removed = removed_programs(layout);
    let triple = current_triple();
    let mut out: Vec<String> = Vec::new();
    for group in crate::plan_groups(index, ch) {
        let missing: Vec<String> = group
            .members
            .iter()
            .filter(|m| {
                wanted.contains(m.as_str())
                    && !installed.contains_key(m.as_str())
                    && !removed.contains(m.as_str())
            })
            .cloned()
            .collect();
        if missing.is_empty()
            || group
                .members
                .iter()
                .any(|m| crate::linkmode::is_linked(layout, m))
        {
            continue;
        }
        // A coherence tuple applies ALL-OR-NOTHING, and `bootstrap_group` refuses the
        // whole group when any member is neither wanted nor installed — exactly what
        // an `exclude` entry or a prior `uninstall <member>` produces. Announcing the
        // remaining members anyway meant the lane advertised programs it would then
        // silently refuse, reported zero failures, and walked out having installed
        // nothing it promised. The prescan has to apply the installer's own rule.
        if group.group.is_some()
            && !group
                .members
                .iter()
                .all(|m| wanted.contains(m.as_str()) || installed.contains_key(m.as_str()))
        {
            continue;
        }
        let probe: &[String] = match &group.group {
            Some(_) => &group.members,
            None => &missing,
        };
        if crate::flow::group_missing_triple(fetcher, index, cfg.channel(), triple, probe).is_none()
        {
            out.extend(missing);
        }
    }
    out.sort();
    out
}

/// THE SEED'S WHOLE-SET DISK REFUSAL: whether `need` signed bytes (plus the
/// [`crate::cost::FREE_FLOOR`] reserve) do NOT fit in `have`, and if so the durable
/// half of the refusal — the `*toolset*` blocked row, and taking back the adoption
/// this run recorded. Unknown free space (`None`) is not a refusal. The caller prints
/// the `seed-failed:` marker (process-global announcement state stays out of here).
///
/// Why adoption is taken back: `cmd_seed` adopts before it knows whether the seal
/// fits, and the GUI runs `atpkg update` seconds after every seed. That pass saw
/// `adopted` and completed the set over the NETWORK lane, which has only per-member
/// and per-group preflights, so the small members installed one by one and the
/// multi-GB trust tuple then failed its own gate. The refusal was undone within
/// seconds, leaving the "provers but no compiler" state this gate exists to prevent.
/// An unadopted empty store makes that update an empty no-op; the next launch's seed
/// re-adopts and re-asks, and installs from the seal once space is free.
///
/// `adopted_by_this_run = false` (an earlier launch, or an explicit
/// `install --default-set`) keeps adoption: a disk shortfall never revokes an act
/// this lane did not make.
fn refuse_seed_for_disk(
    layout: &crate::store::Layout,
    need: u64,
    have: Option<u64>,
    adopted_by_this_run: bool,
) -> bool {
    if have.is_none_or(|a| crate::cost::disk_ok(need, a, crate::cost::FREE_FLOOR)) {
        return false;
    }
    record_status(
        layout,
        "*toolset*",
        crate::ProgramStatus {
            installed_build: None,
            state: String::from("blocked: insufficient disk space"),
            tree_root: String::new(),
        },
        format!("needs {} free", crate::cost::human_bytes(need)),
    );
    if adopted_by_this_run {
        clear_adoption(layout);
    }
    true
}

/// The signed sizes of a set — [`seed_install_bytes`]' answer, and the input of
/// [`size_parenthetical`]. The two figures are independent: a registry row that
/// declares only `disk_installed` still discloses that.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
struct SignedSizes {
    /// What the wire lane moves (`[cost].download_bytes`, summed); `None` when
    /// unknown for any member.
    download: Option<u64>,
    /// What stays when the install is finished (`[cost].disk_installed`, summed);
    /// `None` when unknown for any member.
    disk: Option<u64>,
}

impl SignedSizes {
    /// Neither figure known — the line prints no size at all.
    const UNKNOWN: Self = Self {
        download: None,
        disk: None,
    };
}

/// The signed sizes of `members` for this triple, summed from the pinned manifests' `[cost]`
/// row ([`crate::manifest::Cost`]): `download_bytes` for the wire lane, `disk_installed` for
/// what stays. The sealed seed quotes the on-disk figure only, the wire lane both.
///
/// A figure is `None` when any member's manifest is unavailable (the first miss ends both
/// sums) or its entry is zero: a partial sum would understate the commitment.
fn seed_install_bytes(
    fetcher: &dyn crate::flow::Fetcher,
    index: &crate::TrustedIndex,
    cfg: &crate::config::PackagesConfig,
    members: &[String],
) -> SignedSizes {
    let triple = current_triple();
    // [`crate::TrustedIndex::channel_for`], not a `channels` scan: the channel a host sees
    // is the per-target view, so the figures announced are the ones this triple's pins will
    // actually fetch.
    let Some(ch) = index.channel_for(cfg.channel(), triple) else {
        return SignedSizes::UNKNOWN;
    };
    let ch = &ch;
    let (mut download, mut disk) = (Some(0u64), Some(0u64));
    for m in members {
        let Some((_, _, pkg)) = crate::flow::verified_pkg(fetcher, index, ch, m) else {
            return SignedSizes::UNKNOWN;
        };
        let Some(art) = pkg.artifact_for(triple) else {
            return SignedSizes::UNKNOWN;
        };
        download = sum_nonzero(download, art.cost.download_bytes);
        disk = sum_nonzero(disk, art.cost.disk_installed);
    }
    SignedSizes {
        download: download.filter(|t| *t > 0),
        disk: disk.filter(|t| *t > 0),
    }
}

/// One step of a None-on-zero sum: a zero (undeclared) entry poisons the total.
fn sum_nonzero(total: Option<u64>, n: u64) -> Option<u64> {
    total.filter(|_| n > 0).map(|t| t.saturating_add(n))
}

/// The trailing size parenthetical both announcing lanes end with — one renderer
/// ([`crate::cost::human_bytes`]) so the figures a user is told cannot drift across surfaces.
/// The GUI's toolchain bar reads it out of the line's last `(…)` and clips at 40 characters,
/// so the arms are built to fit: two 9-character figures (up to `999.9 GiB`) render in 39.
/// The one overflow is the two-figure arm with BOTH in the `1000.0–1023.9 MiB` band, which
/// renders 41 and clips "on disk" to "on dis…" with both numbers intact — a set whose
/// download packs several-fold never lands there. An unknown figure prints nothing: no
/// number beats a wrong one.
fn size_parenthetical(sizes: SignedSizes) -> String {
    use crate::cost::human_bytes;
    match (sizes.download, sizes.disk) {
        (Some(d), Some(k)) => format!(
            " (~{} download, ~{} on disk)",
            human_bytes(d),
            human_bytes(k)
        ),
        (None, Some(k)) => format!(" (~{} on disk when finished)", human_bytes(k)),
        (Some(d), None) => format!(" (~{} download)", human_bytes(d)),
        (None, None) => String::new(),
    }
}

/// The seed lane's announcement line (`seed-starting:` — the marker that opens the
/// GUI's "Installing…" card, crates/aterm-gui `parse_seed_line`), built PURE so
/// both of its honesty properties are testable:
///
/// * the marker is the LOCAL lane's ([`SEED_STARTING_MARKER`]), never the wire
///   lane's — the sealed payload moves zero bytes over any socket, and
///   [`net_announcement`] is where the network lane speaks;
/// * the size is ONE number in ONE unit: the signed sum [`seed_install_bytes`]
///   derives from the sealed manifests at print time, rendered by
///   [`crate::cost::human_bytes`] — the crate's single byte renderer (binary
///   units, "GiB", one decimal), the same tiers flow's disk-preflight errors,
///   `doctor`'s free-space report, and the release cut transcript print
///   (`aterm-release::bundle::human_bytes` mirrors it deliberately). One install
///   used to quote three different figures for the same bytes because every
///   surface derived and rendered its own; install.sh's fixed header text is the
///   remaining out-of-crate quote and must carry the same GiB figure.
///
/// `None` size prints no size at all: no number is more honest than a
/// confidently wrong one (the [`seed_install_bytes`] contract). On-disk only: the sealed
/// payload moves no network bytes, so there is no download figure to disclose.
fn seed_announcement(count: usize, signed_bytes: Option<u64>) -> String {
    format!(
        "atpkg: {SEED_STARTING_MARKER}installing {count} ALab program(s) from the bundled \
         registry{}",
        size_parenthetical(SignedSizes {
            download: None,
            disk: signed_bytes,
        })
    )
}

/// The seal cannot serve this machine: say so honestly, reclaim it, and DO NOT claim
/// the network will make up the difference.
///
/// That claim would usually be false. The seal is staged from the published index, so
/// a program the seal cannot serve for this triple is overwhelmingly one the registry
/// does not publish for that triple either. Telling that user "the toolchain will come
/// from the network instead" sends them to wait for something that is not coming.
///
/// Emits the `seed-unusable:` marker so the GUI can retire the in-progress notice with
/// a real answer instead of leaving "Installing…" on screen forever.
fn finish_unusable_seed(
    layout: &crate::store::Layout,
    seed_dir: &std::path::Path,
    fetcher: &dyn crate::flow::Fetcher,
    index: &crate::TrustedIndex,
    cfg: &crate::config::PackagesConfig,
) -> ExitCode {
    // Distinguish "already done" from "can never be done here" — they deserve
    // different words, and only one of them is a disappointment.
    let complete = !crate::active_builds(layout).is_empty()
        && index
            .channels
            .iter()
            .find(|c| c.name == cfg.channel())
            .is_some_and(|ch| {
                let installed = crate::active_builds(layout);
                crate::plan_groups(index, ch)
                    .iter()
                    .flat_map(|g| g.members.iter())
                    .all(|m| installed.contains_key(m.as_str()))
            });
    clear_seed_status(layout);
    // Does this verdict justify deleting the seal? Only `complete` and the
    // architecture branch below say yes.
    let mut permanent = complete;
    if complete {
        println!("atpkg: the bundled seed is fully installed — nothing left to lay down");
    } else if crate::active_builds(layout).is_empty() {
        // Nothing installed and nothing installable — but do NOT blame the
        // architecture reflexively. An empty serviceable set has three causes, and
        // saying "no build for your Mac" to someone who actually excluded every
        // program, or whose whole toolset is dev-linked, is a confident wrong answer.
        let _ = fetcher;
        let removed = removed_programs(layout);
        permanent = false;
        if !removed.is_empty() {
            emit_marker_line(&format!(
                "atpkg: {SEED_UNUSABLE_MARKER}every program the bundled registry offers was \
                 removed on this machine ({}) — nothing to install. \
                 `aterm pkg install <program>` brings one back",
                removed.iter().cloned().collect::<Vec<_>>().join(", ")
            ));
        } else if !index.channels.iter().any(|c| c.name == cfg.channel()) {
            // A channel the seal does not carry. REVERSIBLE — one line of config —
            // and it used to fall through to the architecture arm below, which set
            // `permanent = true` and DELETED the seal while blaming the CPU. The
            // comment on the reclaim names this exact case as one that must be kept;
            // the ladder simply had no branch for it, so the code contradicted its
            // own stated rule and destroyed ~600 MB over a typo.
            emit_marker_line(&format!(
                "atpkg: {SEED_UNUSABLE_MARKER}the bundled registry carries no '{}' channel \
                 (it has: {}) — nothing was installed. Fix [packages].channel in \
                 aterm.toml; the bundled toolchain is kept.",
                cfg.channel(),
                index
                    .channels
                    .iter()
                    .map(|c| c.name.as_str())
                    .collect::<Vec<_>>()
                    .join(", ")
            ));
        } else if !cfg.include().is_empty() || !cfg.exclude().is_empty() {
            emit_marker_line(&format!(
                "atpkg: {SEED_UNUSABLE_MARKER}[packages].include/exclude narrows the bundled \
                 registry to nothing installable on this machine ({}) — widen the filters \
                 to receive the toolset",
                current_triple()
            ));
        } else if running_translated() {
            // NOT PERMANENT, AND NOT THIS MACHINE'S ARCHITECTURE. `current_triple()`
            // is a compile-time cfg, so the x86_64 slice of the universal binary
            // reports x86_64 even on Apple Silicon — which is what runs when someone
            // ticks "Open using Rosetta", or under any translated parent. Treating
            // that as "the CPU will not change" deleted the seal from an M-series
            // Mac that could have installed every program natively, and unticking
            // the box does not bring it back: only re-downloading the DMG does
            // (2026-08-20 round-8 audit). tools/install.sh already refuses to decide
            // a container from the running process's arch for this exact reason.
            println!(
                "atpkg: keeping the bundled seed — this process is running under \
                 Rosetta translation, so {} is not this Mac's real architecture. \
                 Relaunch aterm natively (Finder ▸ Get Info ▸ uncheck \"Open using \
                 Rosetta\") and the sealed toolchain installs.",
                current_triple()
            );
        } else {
            // The only permanent one in this arm: the CPU will not change.
            permanent = true;
            emit_marker_line(&format!(
                "atpkg: {SEED_UNUSABLE_MARKER}the bundled toolchain has no build for this machine's \
                 architecture ({}) — no ALab programs were installed from it",
                current_triple()
            ));
            // AND LEAVE A DURABLE TRACE. Without this the whole first session had no
            // status.toml at all, so Settings ▸ Packages — the surface the notice
            // sends the user to — said "atpkg has not run yet" on a machine where it
            // had run correctly and reached a definite verdict. An honest "no" is
            // still an answer, and the absence of one reads as a broken install
            // (2026-08-20 round-8 audit).
            record_status(
                layout,
                "*toolset*",
                crate::ProgramStatus {
                    installed_build: None,
                    state: String::from("unavailable: no build for this architecture"),
                    tree_root: String::new(),
                },
                format!(
                    "the bundled toolchain carries no build for {}",
                    current_triple()
                ),
            );
        }
    } else {
        println!(
            "atpkg: the bundled seed has nothing further to install on this machine ({})",
            current_triple()
        );
    }
    // RECLAIM ONLY ON A PERMANENT VERDICT. Deleting ~600 MB is irreversible, so it
    // must not hinge on something the user can change their mind about in one line
    // of config. Two verdicts are permanent enough: `complete` (the payload was
    // consumed — it is genuinely spent) and no-artifact-for-this-triple (the CPU
    // will not change). The others — every offered program removed, include/exclude
    // narrowing the set to nothing, a channel the seal does not carry — are
    // reversible: widen the filter or reinstall one program and the seal would have
    // served, except it is gone. That turns a typo into a permanent loss of the
    // offline bootstrap this whole feature exists to provide.
    if permanent {
        reclaim_bundled_seed(seed_dir);
    } else {
        println!(
            "atpkg: keeping the bundled seed — nothing is installable under the current \
             configuration, but that is reversible and the seal would serve if it changes"
        );
    }
    ExitCode::SUCCESS
}

/// Is this process running under Rosetta translation?
///
/// `sysctl sysctl.proc_translated` is the documented answer (1 = translated). It
/// matters here because [`current_triple`] is a compile-time `cfg(target_arch)`: the
/// x86_64 slice of the shipped universal binary reports `x86_64-apple-darwin` on an
/// Apple Silicon Mac, and any verdict of the form "no build for this architecture"
/// is then a statement about the SLICE, not the machine. Anything irreversible
/// keyed on that verdict must ask this first.
///
/// Fail-safe: an unreadable sysctl answers `false`, which is the pre-existing
/// behaviour on every non-translated process.
#[cfg(target_os = "macos")]
fn running_translated() -> bool {
    std::process::Command::new("/usr/sbin/sysctl")
        .args(["-n", "sysctl.proc_translated"])
        .output()
        .ok()
        .filter(|out| out.status.success())
        .is_some_and(|out| String::from_utf8_lossy(&out.stdout).trim() == "1")
}

#[cfg(not(target_os = "macos"))]
const fn running_translated() -> bool {
    false
}

/// Say something TRUE about a machine with no sealed registry, and leave a durable
/// trace of it.
///
/// Two very different situations reach here and they deserve different sentences: a
/// machine whose architecture the index publishes for (the toolset really will arrive
/// over the network) and one it does not (nothing arrives until someone publishes an
/// artifact). Assuming the optimistic one made the product promise every Intel user a
/// toolchain at first launch and then never mention it again.
///
/// It also records `status.toml`, because the ABSENCE of that file is what makes
/// Settings — the one surface a puzzled user is pointed at — say "atpkg has not run
/// yet" on a machine where it has run correctly on every single launch.
fn report_seedless_posture(layout: &crate::store::Layout) {
    let cfg = crate::config::cached();
    let triple = current_triple();
    // No seal here, so the only thing to consult is the network index. A resolve
    // failure is not the question being asked (offline is not "unsupported"), so it
    // degrades to the generic sentence rather than an alarming one.
    //
    // `seed_serviceable` answers "what is left to install HERE", so it returns empty
    // for two opposite states: nothing is published for this CPU, and everything is
    // already installed. The second one is the product's happy path — the seal is
    // consumed and reclaimed after a successful first run — and reading empty as
    // "unsupported architecture" told every fully provisioned Apple Silicon Mac that
    // its own architecture was unserved, once per launch, forever: a stdout marker, a
    // GUI warning pill, a `*toolset*` row reading "unavailable", and a Settings detail
    // line saying no build exists (2026-08-20 round-8 audit). Ask the other question
    // first — and BEFORE the network, because its answer decides whether the resolve
    // can matter at all (see [`seedless_serves_us`]).
    let serves_us = seedless_serves_us(!crate::active_builds(layout).is_empty(), || {
        let fetcher = resolve_fetcher(layout);
        crate::resolve_verified_index(
            &*fetcher,
            layout,
            &effective_anchor(layout),
            build_floor(layout),
            now_unix(),
        )
        .ok()
        .map(|index| !seed_serviceable(layout, &*fetcher, &index, cfg).is_empty())
    });
    if serves_us == Some(false) {
        emit_marker_line(&format!(
            "atpkg: {SEED_UNUSABLE_MARKER}the signed index publishes no artifact for this \
             machine's architecture ({triple}) — nothing was installed, and nothing arrives \
             until one is published"
        ));
        record_status(
            layout,
            "*toolset*",
            crate::ProgramStatus {
                installed_build: None,
                state: String::from("unavailable: no build for this architecture"),
                tree_root: String::new(),
            },
            format!("the signed index publishes no artifact for {triple}"),
        );
    } else {
        println!(
            "atpkg: no bundled toolchain in this app — the ALab toolset installs from the \
             signed index instead: aterm pkg install --default-set (the windowed app also \
             runs this pass on its 6-hour loop)"
        );
    }
}

/// Whether the signed index serves this machine, for the seedless lane's one sentence:
/// `Some(true)` served, `Some(false)` no build for this architecture, `None` the index
/// never resolved.
///
/// THE CHEAP QUESTION FIRST. A store that holds any build is served by definition, so
/// `resolve` — a fetcher, a metered `api.github.com` releases listing, the index-cache
/// parse, the roster/index verification and a `roster.floor` record, all under the store
/// lock `seed` holds — is never run for it. It used to run first on every GUI launch of a
/// provisioned Mac (no release since v0.63.0 seals a seed, so every launch takes this
/// lane), and its answer could not change the output: `Ok` became `Some(true)` through the
/// `active_builds` disjunct, `Err` became `None`, and both print the same generic line.
/// Offline, that was up to three 30 s `api_get` attempts holding `store.lock` (a typed
/// `aterm pkg install` exits 75 meanwhile), followed seconds later by the update pass
/// listing the identical releases again (2026-09-15 audit). Only an EMPTY store — where the
/// unserved-architecture verdict is the whole point — pays for the resolve.
fn seedless_serves_us(
    store_holds_builds: bool,
    resolve: impl FnOnce() -> Option<bool>,
) -> Option<bool> {
    if store_holds_builds {
        Some(true)
    } else {
        resolve()
    }
}

/// Delete the consumed bundled seed, reclaiming its disk (§9.1).
///
/// Best-effort by design, and silent on the ordinary failure: the app bundle may
/// legitimately not be ours to write (installed to `/Applications` by another
/// admin, or on a read-only mount), and a machine that keeps its seed is merely
/// carrying dead weight — never broken. The disk-space win is not worth turning
/// a successful toolchain install into a failure.
///
/// Only ever called after a seed install SUCCEEDED, so the bytes are provably
/// redundant: they are now extracted in the store, and the seed lane refuses to
/// read them again on a non-empty store.
/// Is this seal inside a BUILD OUTPUT rather than an installed app?
///
/// `seed_dir` is `<app>/Contents/Resources/<SEED_DIR_NAME>`, so the directory
/// holding the bundle is four levels up. `aterm-release`'s `bundle::assemble`
/// writes `.metadata_never_index` into every directory it produces a bundle in —
/// originally to keep a build-output app out of Spotlight, which makes it exactly
/// the durable "this is not an install" marker this needs, written by the only
/// thing that knows.
///
/// Fail-safe by construction: an unreadable or absent marker reads as "this is a
/// real install", which is the pre-existing behaviour.
fn is_build_output_bundle(seed_dir: &std::path::Path) -> bool {
    seed_dir
        .ancestors()
        .nth(4)
        .is_some_and(|dir| dir.join(".metadata_never_index").exists())
}

fn reclaim_bundled_seed(seed_dir: &std::path::Path) {
    // NOT OURS TO DELETE. `/Applications/aterm.app` is shared by every account on
    // the Mac, and the store this pass just filled belongs to ONE of them. If the
    // bundle is not owned by us, deleting the payload would reclaim our disk by
    // taking the offline batteries away from every other user on the machine —
    // they would each fall back to a network bootstrap for bytes that were sitting
    // right there. Owning the bundle is the closest available proxy for "this app
    // is mine to modify", and it also covers the root-owned/managed-fleet case,
    // where the delete would fail anyway.
    #[cfg(unix)]
    {
        use std::os::unix::fs::MetadataExt as _;
        let ours = std::fs::symlink_metadata(seed_dir)
            .map(|m| m.uid() == crate::platform::our_uid())
            .unwrap_or(false);
        if !ours {
            println!(
                "atpkg: leaving the bundled seed in place — {} is not owned by this user, and \
                 other accounts on this machine may still need it to install offline",
                seed_dir.display()
            );
            return;
        }
    }
    // NOR IS A BUILD OUTPUT. A bundle under a `dist/` the cutter marked
    // `.metadata_never_index` is an ARTIFACT, not an install: something produced it
    // and may still be signing, packaging or verifying it. On 2026-08-19 this exact
    // delete removed a gigabyte from a release the cutter was mid-package — the app
    // WAS ours and the payload WAS spent for this machine, so every test above
    // passed, and the artifact was corrupted anyway. The same rule spares a
    // developer who builds locally and runs their own `dist/aterm.app`: their next
    // `make` would otherwise find the batteries silently gone.
    //
    // Reclaiming is a disk optimisation. Refusing to do it costs a directory; doing
    // it to something that is not an install costs someone their release.
    if is_build_output_bundle(seed_dir) {
        println!(
            "atpkg: leaving the bundled seed in place — {} sits in a build output \
             directory, not an install; the payload belongs to whatever produced it",
            seed_dir.display()
        );
        return;
    }
    /// Recursive byte count, best-effort: this only sizes a log line, so an
    /// unreadable entry contributes zero rather than derailing the reclaim.
    fn dir_bytes(dir: &std::path::Path) -> u64 {
        let Ok(entries) = std::fs::read_dir(dir) else {
            return 0;
        };
        entries
            .filter_map(Result::ok)
            .map(|e| match e.file_type() {
                Ok(t) if t.is_dir() => dir_bytes(&e.path()),
                _ => e.metadata().map(|m| m.len()).unwrap_or(0),
            })
            .sum()
    }
    let freed = dir_bytes(seed_dir);
    match std::fs::remove_dir_all(seed_dir) {
        Ok(()) => println!(
            "atpkg: reclaimed the bundled seed ({:.0} MB) — a provisioned store never reads \
             it again",
            freed as f64 / 1_000_000.0
        ),
        Err(e) if e.kind() == std::io::ErrorKind::PermissionDenied => {
            // Common and harmless: say it once, at a level that does not read as
            // an install failure.
            println!(
                "atpkg: the bundled seed stays on disk (no write access to {}) — harmless, \
                 just unreclaimed space",
                seed_dir.display()
            );
        }
        Err(e) => println!(
            "atpkg: could not reclaim the bundled seed at {}: {e} (harmless — the toolchain \
             is installed either way)",
            seed_dir.display()
        ),
    }
}

/// Retire the `*seed*` pending-consent row (the offer was taken, or nothing is
/// installable here). `record_status` MERGES the program map, so a row nobody
/// removes lives forever — Settings ▸ Packages would keep advertising an offer
/// the user already accepted.
fn clear_seed_status(layout: &crate::store::Layout) {
    let Some(mut status) = crate::status::read(layout) else {
        return;
    };
    if status.programs.remove("*seed*").is_none() {
        return;
    }
    status.updated_at = now_rfc3339();
    status.outcome = "bundled seed: nothing pending".to_string();
    let _ = crate::status::write(layout, &status);
}

/// Retire the `*seed*` row and NOTHING ELSE. [`clear_seed_status`] also rewrites
/// `status.outcome` to "bundled seed: nothing pending" — right for the seed verbs,
/// whose pass that sentence describes, and a clobber for `cmd_update_all`, which
/// has just recorded `up to date (index build N)` and would lose it to a sentence
/// about a seed that never ran (2026-09-10 audit, adjudicated disagreement 6).
fn retire_seed_row(layout: &crate::store::Layout) {
    let Some(mut status) = crate::status::read(layout) else {
        return;
    };
    if status.programs.remove("*seed*").is_none() {
        return;
    }
    status.updated_at = now_rfc3339();
    let _ = crate::status::write(layout, &status);
}

/// Reconcile the `*seed*` pending-consent row against what a just-finished CLEAN
/// network pass PROVED: retired when nothing remains to offer, rewritten when the
/// remaining offer differs from the recorded one, untouched when no row exists.
/// `record_status` MERGES the program map, and the row's only other writer
/// ([`announce_pending_seed`]) lives on the seed lane, which hard-skips once the
/// store is non-empty — so a row written in one era kept advertising another
/// era's toolset layout, 15 days and several index builds later (observed live
/// 2026-08-27). Callers pass a clean pass's `remaining_missing` only: a failed
/// pass proves nothing about the offer.
///
/// ROW-ONLY: the aggregate `outcome` sentence belongs to the pass that just ran
/// (`up to date (index build N)` for the update lane) and is left alone — except
/// when it IS the seed offer's own sentence, which follows the row so Settings ▸
/// Packages never shows one list in the row and another in the detail line.
fn reconcile_seed_status(layout: &crate::store::Layout, remaining_missing: &[String]) {
    const OFFER: &str = "ALab toolchain ready: ";
    if remaining_missing.is_empty() {
        retire_seed_row(layout);
        return;
    }
    let Some(mut status) = crate::status::read(layout) else {
        return;
    };
    let Some(row) = status.programs.get_mut("*seed*") else {
        return;
    };
    let list = remaining_missing.join(", ");
    let state = format!("pending-consent: {list}");
    if row.state == state {
        return;
    }
    row.state = state;
    row.installed_build = None;
    row.tree_root = String::new();
    if status.outcome.starts_with(OFFER) {
        // The SAME sentence `announce_pending_seed` writes — a drifted wording
        // here would read as a different offer.
        status.outcome = format!("{OFFER}{list} — Install ALab toolset below");
    }
    status.updated_at = now_rfc3339();
    let _ = crate::status::write(layout, &status);
}

/// The consent-pending half of the bundled-seed lane (§11, the
/// `[packages].seed_install = false` posture): resolve the ONE verified index
/// through the chain, count the channel-pinned installable members not yet
/// installed, and say so — a stable stdout marker line (`seed-pending: …`,
/// what the GUI's launch-time seed run parses for the first-run notice) plus a
/// `status.toml` entry so Settings ▸ Packages shows the same truth.
/// Announcement only: nothing is downloaded or extracted, and a failure to
/// resolve the index here is itself only announced (the seed is an offer, not
/// an obligation).
fn announce_pending_seed(
    layout: &crate::store::Layout,
    fetcher: &dyn crate::flow::Fetcher,
    cfg: &crate::config::PackagesConfig,
    seed_dir: &std::path::Path,
) {
    let floor = build_floor(layout);
    let index = match crate::resolve_verified_index(
        fetcher,
        layout,
        &effective_anchor(layout),
        floor,
        now_unix(),
    ) {
        Ok(i) => i,
        Err(e) => {
            println!(
                "atpkg: bundled seed present ({}) but its index did not verify: {e}",
                seed_dir.display()
            );
            return;
        }
    };
    let Some(ch) = index.channel_for(cfg.channel(), current_triple()) else {
        println!(
            "atpkg: bundled seed present ({}) but names no '{}' channel — nothing to offer",
            seed_dir.display(),
            cfg.channel()
        );
        return;
    };
    let ch = &ch;
    let installed = crate::active_builds(layout);
    let wanted = wanted_programs(layout, &index, cfg);
    // A program with no artifact for THIS triple can never be installed here
    // (`install_default_set` clean-skips it, §6), so offering it would be a
    // pill and a status row the user can never satisfy. Narrow to what the
    // seed can actually lay down on this machine via the §11 bootstrap prescan
    // ([`crate::flow::group_missing_triple`]).
    //
    // The prescan runs at the SAME GRANULARITY the install uses, and that is the
    // whole point of doing it per group rather than per program: a coherence
    // tuple is all-or-nothing, so `install_default_set` passes the whole member
    // slice and clean-skips the ENTIRE group when any one member lacks an
    // artifact for this triple. A per-member prescan disagreed with that — for a
    // mixed-coverage tuple it advertised the members that DO have artifacts,
    // which neither advertised route would then install, and via the GUI the
    // failure was silent-and-green (a clean skip is zero failures, so the
    // Packages page reported "Succeeded" over an empty store).
    let triple = current_triple();
    let mut missing: Vec<String> = Vec::new();
    for group in crate::plan_groups(&index, ch) {
        let group_missing: Vec<String> = group
            .members
            .iter()
            .filter(|m| wanted.contains(m.as_str()) && !installed.contains_key(m.as_str()))
            .cloned()
            .collect();
        if group_missing.is_empty() {
            continue;
        }
        // Ungrouped members carry a one-element slice, so this is the identical
        // call for both shapes — the difference is only what the slice spans.
        let probe: &[String] = match &group.group {
            Some(_) => &group.members,
            None => &group_missing,
        };
        if crate::flow::group_missing_triple(fetcher, &index, cfg.channel(), triple, probe)
            .is_none()
        {
            missing.extend(group_missing);
        }
    }
    if missing.is_empty() {
        // Nothing left to offer: retire any stale pending-consent row so the
        // Packages page cannot keep showing an offer that is already taken
        // (record_status MERGES the program map, so an untouched row lingers
        // forever — adversarial review 2026-07-30).
        clear_seed_status(layout);
        return;
    }
    missing.sort();
    let list = missing.join(", ");
    // The stable marker the GUI parses — change it and the first-run notice
    // goes blind (crates/aterm-gui, spawn_pkg_update_check).
    emit_marker_line(&format!(
        "atpkg: {SEED_PENDING_MARKER}{} program(s) ready to install from the bundled seed: {list} \
         (Settings ▸ Packages ▸ Install ALab toolset, or `aterm pkg install --default-set`)",
        missing.len()
    ));
    // Value-first so the offer survives the Packages card's truncation width
    // (UX review 2026-07-30: "bundled seed offers: ay · 2026-…" truncated the
    // payload away); the page's own "Install ALab toolset" button is the act.
    record_status(
        layout,
        "*seed*",
        crate::ProgramStatus {
            installed_build: None,
            state: format!("pending-consent: {list}"),
            tree_root: String::new(),
        },
        format!("ALab toolchain ready: {list} — Install ALab toolset below"),
    );
}

/// Reconcile the `[packages.links]` config at the CLI edge of every network verb:
/// a local-checkout value becomes a MANAGED dev-link ([`crate::linkmode`] —
/// idempotent; registry management hard-skips it until unlinked/removed from
/// config + `atpkg unlink`), while a HAND-MADE link pointing at a DIFFERENT
/// checkout is REFUSED loudly and never touched (a developer's live dev loop is
/// not ours to re-point). A missing checkout is loud. `owner/repo` values are the
/// FETCHER's business ([`github_fetcher`] overrides), not link state. Never fatal:
/// link problems must not block the update pass itself.
///
/// The foreign-link refusal is check-then-act (`linked_checkout` read →
/// `crate::link` overwrite) — sound only because every caller is a store-MUTATING
/// verb (`install`, `install --default-set`, `update`) already holding the
/// store-wide single-writer lock ([`crate::lock`]) from the [`main_entry`] dispatch
/// edge, so no second process can slip a link in between the read and the write.
/// Keep it that way: never call this from a lock-free (read-only) verb.
fn reconcile_links(layout: &crate::store::Layout, cfg: &crate::config::PackagesConfig) {
    let home = aterm_types::dirs::home_dir();
    for (program, value) in &cfg.links {
        match crate::classify_link(value, home.as_deref()) {
            crate::LinkTarget::Checkout(want) => {
                // Absolutize exactly as `link` records, so equality below compares
                // like with like (the marker stores an absolute checkout).
                let want = std::path::absolute(&want).unwrap_or(want);
                match crate::linked_checkout(layout, program) {
                    None => {
                        if !want.is_dir() {
                            eprintln!(
                                "atpkg: [packages.links] {program}: checkout {} does not \
                                 exist — not linking",
                                want.display()
                            );
                            continue;
                        }
                        match crate::link(layout, program, &want, &default_link_bins(program)) {
                            Ok(out) => println!(
                                "atpkg: dev-linked {program} from [packages.links] ({})",
                                if out.linked.is_empty() {
                                    "no bins".into()
                                } else {
                                    out.linked.join(", ")
                                }
                            ),
                            Err(e) => {
                                eprintln!("atpkg: [packages.links] {program}: link failed: {e}")
                            }
                        }
                    }
                    Some(existing) if existing == want => {
                        // Already ours at the right target: idempotent re-assert
                        // (picks up newly-built bins), quiet on the happy path.
                        if let Err(e) = crate::refresh(layout, program) {
                            eprintln!("atpkg: [packages.links] {program}: refresh failed: {e}");
                        }
                    }
                    Some(existing) => {
                        eprintln!(
                            "atpkg: [packages.links] {program}: REFUSING to touch the \
                             existing dev-link at {} (config wants {}); run `aterm pkg \
                             unlink {program}` first if the config target is right",
                            existing.display(),
                            want.display()
                        );
                    }
                }
            }
            crate::LinkTarget::Repo(_) => {} // fetch override — applied in the fetcher
            crate::LinkTarget::Invalid => {
                eprintln!(
                    "atpkg: [packages.links] {program} = {value:?} is neither an absolute/~ \
                     checkout path nor a valid owner/repo — ignored (fail-closed)"
                );
            }
        }
    }
}

/// The conventional default bin for a link with no explicit bin list —
/// `target/release/<program>` WITH the platform executable extension (shared by
/// `cmd_link` and the `[packages.links]` reconciliation so the two can never drift).
fn default_link_bins(program: &str) -> Vec<std::path::PathBuf> {
    vec![std::path::PathBuf::from(format!(
        "target/release/{program}{}",
        std::env::consts::EXE_SUFFIX
    ))]
}

/// The checkout-relative bins to dev-link when `checkout` is a SYSROOT rather than a cargo
/// project — every name the installed build exposes, under `bin/`.
///
/// WHY A SYSROOT NEEDS ITS OWN ANSWER. `default_link_bins` returns
/// `target/release/<program>`, which is right for a single-binary program and wrong for a
/// toolchain: `trust` publishes ONE program that exposes fifteen commands, and those commands
/// find their own `lib/rustlib` by walking up from the binary they were invoked as (`targo`
/// authenticates its frontend and derives the sysroot from `frontend.parent()`). Linking
/// `target/release/trust` would therefore shim a path that does not exist, and linking a lone
/// `bin/targo` out of a sysroot would shim a binary whose parent has no sibling `lib/` — a
/// toolchain that resolves and then cannot compile. Both failures are silent at link time.
///
/// So the unit for a sysroot is the ROOT: shim every exposed name to `<root>/bin/<name>`, so
/// each one keeps its real neighbours. The exposed set is read from the INSTALLED manifest
/// rather than from `<root>/bin`'s listing, because the manifest is the signed record of what
/// this program is supposed to put on PATH; a dev build with an extra binary lying in `bin/`
/// must not silently gain a shim, and one MISSING an exposed name must fail rather than
/// quietly shim fewer commands than the registry build did.
///
/// Returns `None` when this is not a sysroot link, so the caller falls back unchanged.
fn sysroot_link_bins(
    layout: &crate::store::Layout,
    program: &str,
    checkout: &std::path::Path,
) -> Option<Result<Vec<std::path::PathBuf>, Vec<String>>> {
    // A sysroot is bin/ + lib/rustlib. Requiring BOTH is what keeps an ordinary cargo
    // checkout that happens to have a `bin/` directory on the single-binary path.
    if !checkout.join("bin").is_dir() || !checkout.join("lib").join("rustlib").is_dir() {
        return None;
    }
    let exposes = crate::installed_exposes(layout, program)?;
    if exposes.is_empty() {
        return None;
    }
    // COMPLETENESS IS THE WHOLE POINT, and partial is worse than refused. Linking only the
    // names a dev build happens to carry leaves the REST pointing at the registry build, so
    // `trustc` would be the dev compiler while `trust-wp` stayed on the shipped one — two
    // toolchains with different sysroots, mixed on one PATH, silently. Measured: a stage2
    // built without the trust-wp tools linked 10 of 15 and left 5 shims on build 6459.
    //
    // So a missing exposed name aborts the LINK rather than shrinking it. The user then
    // either builds the missing tools or names an explicit rel-bin list to say they meant a
    // partial link.
    let mut bins = Vec::with_capacity(exposes.len());
    let mut missing = Vec::new();
    for name in &exposes {
        let rel =
            std::path::PathBuf::from("bin").join(format!("{name}{}", std::env::consts::EXE_SUFFIX));
        if checkout.join(&rel).is_file() {
            bins.push(rel);
        } else {
            missing.push(name.clone());
        }
    }
    if missing.is_empty() {
        missing.sort();
        Some(Ok(bins))
    } else {
        missing.sort();
        Some(Err(missing))
    }
}

/// `atpkg update <program>` — update ONE program (§11 tuple-split fix). A coherence-GROUP
/// member is routed through the transactional [`crate::flow::apply_program`] so its whole
/// tuple stages/flips/rolls-back atomically and can never move alone; an ungrouped program
/// keeps the single-program install path. A read-only [`crate::flow::plan_update`] picks the
/// path AND yields the authoritative `decide()` result, so the ungrouped local-pin gate is
/// applied strictly AFTER it (never hiding a Tombstone).
fn cmd_update_one(program: &String) -> ExitCode {
    if !manager_enabled() {
        eprintln!("atpkg: disabled — nothing to update");
        return ExitCode::from(1);
    }
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let cfg = crate::config::cached();
    reconcile_links(&layout, cfg);
    let installed = crate::active_builds(&layout);
    if !installed.contains_key(program) {
        // Update of a not-installed program = a fresh explicit install (single path).
        return cmd_install_for_update(program);
    }
    let cur = installed.get(program).copied();
    let floor = build_floor(&layout);
    let fetcher = resolve_fetcher(&layout);
    let plan = match crate::flow::plan_update(
        &*fetcher,
        &layout,
        &effective_anchor(&layout),
        cfg.channel(),
        current_triple(),
        program,
        cur,
        floor,
        now_unix(),
    ) {
        Ok(x) => x,
        Err(e) => {
            eprintln!("atpkg: update {program} failed: {e}");
            print_unreachable_followup(&e, &format!("aterm pkg update {program}"));
            return ExitCode::from(1);
        }
    };
    let decision = plan.decision;
    if plan.group.is_some() {
        // Coherence-group member (§11): the transactional path. Pin is consulted inside
        // apply_program's per-group gate.
        let report = match crate::flow::apply_program(
            &*fetcher,
            &layout,
            &effective_anchor(&layout),
            cfg.channel(),
            current_triple(),
            program,
            &installed,
            // Same wire as cmd_update_all: an excluded ABSENT sibling must not
            // ride back in on this program's coherence tuple.
            cfg.exclude(),
            floor,
            now_unix(),
        ) {
            Ok(r) => r,
            Err(e) => {
                eprintln!("atpkg: update {program} failed: {e}");
                print_unreachable_followup(&e, &format!("aterm pkg update {program}"));
                return ExitCode::from(1);
            }
        };
        advance_floors(&layout, report.index_build, report.roster_seq);
        let failures = report_channel_apply(&layout, &installed, &report);
        for p in &report.skipped_linked {
            println!("atpkg: {p} dev-linked — skipped");
        }
        // GC after every successful activate — the same policy (and order: GC, then the
        // shell-hook refresh) as `cmd_update_all` and `do_install`, which the ungrouped
        // path below reaches through `cmd_install`. Best-effort; never fails the update.
        // The SPARING form, for the same reason as those verbs: a tuple member whose
        // download failed aborted its group but resolved its pin first, and its `.part`
        // is the resume state the next attempt continues from.
        let gc = crate::gc::run_keeping_pinned_partials(&layout, &|p| {
            report.resolved_assets.get(p).cloned()
        });
        print_gc_sweeps("update", &gc);
        print_gc_abstentions("update", &gc);
        // THE SEAM AND THE EXEC ROOTS — the same two acts `cmd_update_all_code` performs
        // after its gc, and for the same reason. This is the branch `aterm pkg update trust`
        // takes (trust is in the `rustc` coherence group), so it is literally the lane that
        // printed `atpkg: rustc up to date` on m3 for two weeks without ever inspecting the
        // `~/.rustup/toolchains/trust` entry that decided what the machine compiled with.
        // Order matches the pass: gc, then the roots, then the shell hook.
        reassert_rustup_seam(&layout);
        let _ = reconcile_exec_roots(&layout, crate::seam::Depth::Shallow);
        crate::hooks::refresh(&layout);
        return if failures == 0 {
            ExitCode::SUCCESS
        } else {
            ExitCode::from(1)
        };
    }
    // Ungrouped: LOCAL PIN GATE, strictly AFTER the authoritative decide() in `decision`,
    // suppression-only — only an Install (upgrade) is held; Tombstone/UpToDate/NotPinned fall
    // through to the honest single path untouched. CRITICAL: the pin may hold only while the
    // CURRENTLY-active build is itself still gate-valid (`plan.current_build_ok`). If the
    // current build was yanked/floored, decide() returned Install to force-upgrade OFF it —
    // honoring the pin would keep a revoked build running, so the pin is IGNORED there.
    if decision == crate::ApplyDecision::Install
        && plan.current_build_ok
        && let Some(c) = cur
        && crate::pin::is_pinned(&layout, program)
    {
        println!(
            "atpkg: {program} held by local pin (build {c}); `aterm pkg unpin {program}` to allow updates"
        );
        return ExitCode::SUCCESS;
    }
    if decision == crate::ApplyDecision::Install
        && cur.is_some()
        && crate::pin::is_pinned(&layout, program)
    {
        // Pinned but the current build is no longer gate-valid: force the upgrade and say so.
        eprintln!(
            "atpkg: {program} is pinned, but its current build is yanked/below floor — \
             force-upgrading off it (a pin never keeps a revoked build running)"
        );
    }
    // Re-enter the single-program install path with the fetcher THIS verb already built
    // and already warmed (token chain + index candidates), instead of `cmd_install`'s
    // second one. Everything `cmd_install` would re-do first — `manager_enabled`,
    // `layout()`, `cfg` — ran above with the same answers, and `reconcile_links` still
    // runs inside `cmd_install_with` exactly as before, so the output is unchanged.
    cmd_install_with(program, &layout, cfg, &*fetcher, "update")
}

/// Report a channel-apply outcome per coherence group and record per-program status,
/// returning the number of ABORTED groups (the caller's exit-code input). Shared by
/// [`cmd_update_all`] and [`cmd_update_one`].
fn report_channel_apply(
    layout: &crate::store::Layout,
    installed: &std::collections::BTreeMap<String, u64>,
    report: &crate::ChannelApplyReport,
) -> u32 {
    // Post-apply ACTIVE builds (shim-derived), for accurate per-program status.
    let post: std::collections::BTreeMap<String, u64> = crate::active_builds(layout);
    // The rows as they stood BEFORE this pass wrote any — what the UpToDate arm reads to
    // lift a `blocked by` row the requirement gate no longer holds.
    let before: std::collections::BTreeMap<String, String> = crate::status::read(layout)
        .map(|s| s.programs.into_iter().map(|(n, r)| (n, r.state)).collect())
        .unwrap_or_default();
    let was_blocked = |prog: &str| {
        before
            .get(prog)
            .is_some_and(|s| crate::state::blocked_by(s).is_some())
    };
    // …and the missing-triple hold's `held: …` row (the Unpublished arm below), which the
    // same arm lifts once the tuple is whole on a pin this host is served.
    let was_held = |prog: &str| {
        before
            .get(prog)
            .is_some_and(|s| s.starts_with(crate::state::HELD_PREFIX))
    };
    let mut failures = 0u32;
    // The members that failed, by name: the aggregate `outcome` used to be whichever
    // member was recorded LAST (`record_status` overwrites it), so Settings' one line
    // after a pass with four failures named one of them (audit 2026-09-14).
    let mut failed_names: Vec<String> = Vec::new();
    for (group, outcome) in &report.groups {
        let label = group
            .group
            .clone()
            .unwrap_or_else(|| group.members.join("+"));
        match outcome {
            crate::TxnOutcome::UpToDate => {
                println!("atpkg: {label} up to date");
                // …OF WHAT THIS MACHINE ACTUALLY RUNS, or the line above is a verdict about
                // a build nobody uses. `atpkg: rustc up to date` is exactly what this arm
                // printed for two weeks on m3 while every compile went through a
                // hand-placed `~/.rustup/toolchains/trust` 859 commits behind the pin; both
                // halves were true of the store and neither was true of the machine.
                // `doctor` had reported that seam twice, which is the wrong verb to have to
                // guess you should run. Said per GROUP, beside the verdict it qualifies —
                // and recorded below, so Settings ▸ Packages carries it too.
                for prog in &group.members {
                    if let Some(why) = crate::seam::dissent_if_armed(layout, prog) {
                        println!(
                            "atpkg: {label} up to date — but not what this machine runs: {why}"
                        );
                    }
                }
                // THE GATE LIFTS ITS OWN ROW. A member the `Blocked` arm below recorded
                // `blocked by <dep>: …` whose dependency has since come back — and whose
                // pin has not moved — is UpToDate: nothing flips, so no other arm would
                // rewrite the row, and the block would outlive the fact it stated.
                // Restore the canonical managed row, build and attestation kept.
                //
                // THE SAME ARM MIGRATES AND RE-STAMPS. A row spelled `active` was
                // written by a pre-`managed` client and no pass ever rewrote it; a
                // `managed N — pinned by index 15` row stays at 15 through six index
                // builds because only an install rewrites it. One healthy store then
                // read as three spellings — `pinned by index 15`, `active`, `pinned
                // by index 21` — and the owner took the 15s for stale tools
                // (2026-09-10 audit). An UpToDate member's row now always names THIS
                // pass's index; the outcome sentence the pass already recorded is kept
                // (only the lifted-block case narrates, as before).
                for prog in &group.members {
                    let Some(build) = post.get(prog).copied() else {
                        continue;
                    };
                    let state = crate::state::managed(build, report.index_build);
                    let row = crate::ProgramStatus {
                        installed_build: Some(build),
                        state: state.clone(),
                        tree_root: effective_tree_root(layout, prog, ""),
                    };
                    if was_blocked(prog) || was_held(prog) {
                        let lifted = if was_blocked(prog) { "blocked" } else { "held" };
                        println!("atpkg: {prog}: {state} (no longer {lifted})");
                        record_status(layout, prog, row, format!("group {label}: up to date"));
                        continue;
                    }
                    let prior = before.get(prog.as_str()).map(String::as_str);
                    if up_to_date_row_needs_rewrite(prior, build, report.index_build) {
                        record_row_keeping_outcome(layout, prog, row);
                    }
                }
            }
            crate::TxnOutcome::Applied(members) => {
                println!("atpkg: {label} updated ({})", members.join(", "));
                for prog in &group.members {
                    // Only the members actually flipped carry a fresh signed tree_root; an
                    // untouched (UpToDate) sibling reports none, so preserve its recorded root
                    // rather than wiping the attestation `atpkg verify` needs.
                    let new_root = report
                        .applied
                        .get(prog)
                        .map(|a| a.tree_root.clone())
                        .unwrap_or_default();
                    let build = report
                        .applied
                        .get(prog)
                        .map(|a| a.build)
                        .or_else(|| post.get(prog).copied())
                        .or_else(|| installed.get(prog).copied())
                        .unwrap_or(0);
                    record_status(
                        layout,
                        prog,
                        crate::ProgramStatus {
                            installed_build: post.get(prog).copied(),
                            state: crate::state::managed(build, report.index_build),
                            tree_root: effective_tree_root(layout, prog, &new_root),
                        },
                        format!("group {label}: updated"),
                    );
                }
            }
            crate::TxnOutcome::Tombstoned(members) => {
                eprintln!(
                    "atpkg: {label} NOT updated — pinned build yanked/below floor: {}",
                    members.join(", ")
                );
                for prog in members {
                    record_status(
                        layout,
                        prog,
                        crate::ProgramStatus {
                            installed_build: installed.get(prog).copied(),
                            // KEEP THE RECORDED ROOT. These arms describe a program
                            // that is installed and whose tree did NOT change — a
                            // held pin, a tombstoned pin, the post-state of an
                            // aborted transaction. Writing an empty root there
                            // DELETED the signed attestation of bytes nothing had
                            // touched, and `atpkg verify` treats an empty root as
                            // fail-closed, so a single `atpkg pin trust` turned into
                            // "reinstall 3.2 GB to enable verification"
                            // (2026-08-20 round-8 audit).
                            state: crate::state::TOMBSTONED_PIN.into(),
                            tree_root: effective_tree_root(layout, prog, ""),
                        },
                        format!("group {label}: tombstoned"),
                    );
                }
            }
            crate::TxnOutcome::Pinned(members) => {
                println!("atpkg: {label} held by pin ({})", members.join(", "));
                for prog in members {
                    record_status(
                        layout,
                        prog,
                        crate::ProgramStatus {
                            installed_build: installed.get(prog).copied(),
                            state: "pinned: held against update".into(),
                            tree_root: effective_tree_root(layout, prog, ""),
                        },
                        format!("group {label}: held by pin"),
                    );
                }
            }
            crate::TxnOutcome::Blocked { dep, dep_state } => {
                // THE REQUIREMENT GATE'S ROW on the update lane (§17.10): the group is
                // held on its current builds because `dep` is not installed, dev-linked,
                // system-satisfied or installed through its protocol. The same words the
                // set-completion lane records, and the same doctrine as the held and
                // tombstoned arms above: the bytes did not move, so the build and the
                // signed root stay on the record. Deferred, never a failure — the next
                // pass retries, and the UpToDate arm lifts the row once the dependency
                // is back.
                let state = crate::state::blocked(dep, dep_state);
                println!("atpkg: {label} NOT updated — {state}");
                for prog in &group.members {
                    record_status(
                        layout,
                        prog,
                        crate::ProgramStatus {
                            installed_build: installed.get(prog).copied(),
                            state: state.clone(),
                            tree_root: effective_tree_root(layout, prog, ""),
                        },
                        format!("group {label}: {state}"),
                    );
                }
            }
            crate::TxnOutcome::Unpublished {
                member,
                build,
                triple,
            } => {
                // The missing-triple hold's rows — the update-lane twin of the bootstrap's
                // clean skip: the new pin cannot fully exist on this host, so the tuple stays
                // whole on its current builds and nothing moved. A correct state, never a
                // failure; one row per installed member, and the UpToDate arm lifts them.
                println!(
                    "atpkg: {label} NOT updated — {member}'s pinned build {build} is not \
                     published for {triple}; staying on the current builds"
                );
                for prog in &group.members {
                    let Some(current) = installed.get(prog).copied() else {
                        continue;
                    };
                    let owner = (prog != member).then_some(member.as_str());
                    let state = crate::state::held_unpublished(owner, *build, triple, current);
                    record_status(
                        layout,
                        prog,
                        crate::ProgramStatus {
                            installed_build: Some(current),
                            state: state.clone(),
                            tree_root: effective_tree_root(layout, prog, ""),
                        },
                        format!("group {label}: {state}"),
                    );
                }
            }
            crate::TxnOutcome::Aborted {
                failed,
                during_flip,
                why,
            } => {
                failures += 1;
                failed_names.push(failed.clone());
                let phase = if *during_flip {
                    "flip (rolled back)"
                } else {
                    "stage"
                };
                // THE CAUSE RIDES EVERYWHERE THE ABORT DOES (2026-09-15): the stderr line,
                // the member's own row (`aborted: <phase> — <cause>`, the prefix kept for
                // every reader that keys on it), the pass outcome, and a MARKER on stdout
                // the window turns into a row on the toolchain lane — an aborted group
                // used to reach the log alone, and the 2026-09-14 incident's
                // "rustc update ABORTED at trust during stage" was on no screen.
                let cause = clip_cause(why);
                eprintln!(
                    "atpkg: {label} update ABORTED at {failed} during {phase} — {cause}; the \
                     group stays coherent on its previous builds"
                );
                emit_marker_line(&format!(
                    "atpkg: {GROUP_ABORTED_MARKER}{label} update aborted at {failed} during \
                     {phase}: {cause}"
                ));
                record_status(
                    layout,
                    failed,
                    crate::ProgramStatus {
                        installed_build: post.get(failed).copied(),
                        state: format!("aborted: {phase} — {cause}"),
                        tree_root: effective_tree_root(layout, failed, ""),
                    },
                    format!("group {label}: aborted at {failed} during {phase} — {cause}"),
                );
            }
        }
    }
    if failed_names.len() >= 2 {
        record_outcome(
            layout,
            format!(
                "{} program(s) failed: {} — see their rows",
                failed_names.len(),
                failed_names.join(", ")
            ),
        );
    }
    failures
}

/// `atpkg verify-index <master-pubkey-b64> <index.toml> <index.toml.sig> <roster.toml>
/// <roster.toml.sig>` — run the WHOLE client chain over files on disk, for operators and
/// the publish pipeline's self-check. Exit 0 iff a machine the given paper master's roster
/// authorizes signed that index and the index's own attribution agrees.
///
/// It takes the MASTER key rather than an index key because that is the only key the
/// client pins: checking `index.toml` against "some public key" would prove nothing a
/// client would act on. The roster floor is 0 (this verb has no store to ratchet) and the
/// clock is the real one, so a lapsed roster fails here exactly as it would on a user's
/// machine — which is the point of a pipeline self-check.
fn cmd_verify_index(args: &[String]) -> ExitCode {
    let [master, index, sig, roster, roster_sig] = args else {
        eprintln!(
            "usage: atpkg verify-index <master-pubkey-b64> <index.toml> <index.toml.sig> \
             <aterm-machines.toml> <aterm-machines.toml.sig>"
        );
        return ExitCode::from(2);
    };
    let (Ok(raw), Ok(sig_bytes), Ok(roster_bytes), Ok(roster_sig_bytes)) = (
        std::fs::read(index),
        std::fs::read(sig),
        std::fs::read(roster),
        std::fs::read(roster_sig),
    ) else {
        eprintln!("atpkg: cannot read the index, the roster, or one of their signatures");
        return ExitCode::from(1);
    };
    let anchor = crate::Anchor::of(vec![master.clone()], 0);
    let Ok(admitted) = crate::admit_roster(&anchor, roster_bytes, &roster_sig_bytes, now_unix())
    else {
        // Opaque on purpose — no verification oracle (§8).
        eprintln!("FAIL: the roster did not verify under that master, or is stale");
        return ExitCode::from(1);
    };
    match admitted.authorize_index(raw, &sig_bytes) {
        Ok((idx, _)) => {
            println!(
                "OK: index build {} signed by machine {} under roster seq {}",
                idx.index_build,
                idx.attribution().machine_id,
                idx.roster_seq()
            );
            ExitCode::SUCCESS
        }
        // Post-verify, not a crypto oracle: the signature held, and it is the index's own
        // already-authenticated bytes that cannot be honoured. Named, so the publisher
        // fixes the row instead of re-keying a machine.
        Err(why) => {
            match index_post_verify_refusal(&why) {
                Some(reason) => eprintln!("FAIL: {reason}"),
                // Every CRYPTO verdict stays opaque (§8: no verification oracle).
                None => eprintln!("FAIL: no machine on that roster signed this index"),
            }
            ExitCode::from(1)
        }
    }
}

/// What an [`crate::sig::TrustedRoster::authorize_index`] refusal means to a PUBLISHER,
/// when it means anything nameable: `Some(reason)` for the POST-VERIFY variants — the
/// signature already held, and the index's own authenticated bytes are the problem — and
/// `None` for every crypto verdict, which stays deliberately opaque (§8).
///
/// WHY A CATCH-ALL WAS WRONG HERE. `authorize_index` runs the Ed25519 check FIRST
/// (`authorize_bytes`), then [`crate::manifest::parse_index`], then the attribution bind —
/// so [`crate::Reject::Malformed`], [`crate::Reject::Schema`],
/// [`crate::Reject::Unattributed`] and [`crate::Reject::SeqMismatch`] are only ever
/// reached with a valid signature already in hand. Reporting them as "no machine on that
/// roster signed this index" sent the publisher to the PAPER MASTER to re-join or re-key a
/// machine when the fix is one line of the index they just authored: a duplicate key or a
/// wrong-typed field from a script edit reads as `Malformed`, and the `roster_seq` that
/// `tools/atpkg-index.sh` parses out of the roster with `sed … | head -n1` reads as
/// `SeqMismatch` when it is misread — which that script tells the operator in as many
/// words ("comes back as `Reject::SeqMismatch` and the publish stops"), a name this verb
/// never printed. It is the same reasoning [`crate::Reject::Requires`] already had an arm
/// of its own for, applied to the rest of the post-verify set.
///
/// [`crate::Reject::NotAuthorized`] is deliberately NOT named. It is the one verdict
/// reachable from BOTH sides — `Roster::authorize_appcast` returns it when the roster has
/// no live machine at all (pre-verify), and the bind returns it for a `machine_id` naming
/// a different machine than the key that verified (post-verify) — so the opaque line is
/// both true of it and the only safe thing to say.
fn index_post_verify_refusal(why: &crate::Reject) -> Option<String> {
    match why {
        crate::Reject::Requires(edge) => Some(format!(
            "the index verified, but its requires relation cannot be honoured: {edge}"
        )),
        crate::Reject::Malformed => Some(
            "the index verified, but no client can read it — malformed: not UTF-8, not \
             TOML, or a required field missing, duplicated or wrong-typed"
                .to_string(),
        ),
        crate::Reject::Schema => Some(format!(
            "the index verified, but its `schema` is newer than this build reads (this \
             build understands schema {})",
            crate::manifest::SUPPORTED_SCHEMA
        )),
        crate::Reject::Unattributed => Some(
            "the index verified, but carries no `machine_id`, so no client can attribute \
             it — add the signing machine's id to the index; its key is not the problem"
                .to_string(),
        ),
        crate::Reject::SeqMismatch => Some(
            "the index verified, but its `roster_seq` is absent or NEWER than the roster \
             shipped beside it — fix the index's `roster_seq` (or ship the roster it was \
             cut under); the machine's key is not the problem"
                .to_string(),
        ),
        // Disabled/BadKey/BadSig/Verify/Revoked/NotAuthorized/Expired/Stale/Rollback, and
        // the two pkg-only post-verify variants no index parse can produce.
        _ => None,
    }
}

/// `atpkg verify-pkg <master-pubkey-b64> <pkg-*.toml> <pkg.sig> <aterm-machines.toml>
/// <aterm-machines.toml.sig>` — prove a package manifest was signed by a machine the
/// given paper master's roster authorizes. The pkg sibling of [`cmd_verify_index`], and
/// it exists for the same operator: the mirror re-verifies every document it republishes
/// with the client's own chain, and before this verb it had no way to do that for
/// `pkg-*.toml` short of hand-rolling Ed25519 in shell — so it shipped with an honest
/// comment saying pkg manifests were NOT crypto-verified. That comment collapses to one
/// call to this.
///
/// Same authority rule as the index (`TrustedRoster::authorize_bytes`): any listed,
/// unrevoked, unexpired machine on the admitted generation. No attribution bind, because
/// a pkg manifest carries none — the ID printed comes from the signature that verified.
///
/// The signature is only HALF the chain a client runs, so this verb runs the other half
/// too ([`pkg_post_verify_refusal`]): `OK` means a client can both authenticate AND read
/// this manifest, never the signature alone.
fn cmd_verify_pkg(args: &[String]) -> ExitCode {
    let [master, pkg, sig, roster, roster_sig] = args else {
        eprintln!(
            "usage: atpkg verify-pkg <master-pubkey-b64> <pkg-*.toml> <pkg.sig> \
             <aterm-machines.toml> <aterm-machines.toml.sig>"
        );
        return ExitCode::from(2);
    };
    let (Ok(raw), Ok(sig_bytes), Ok(roster_bytes), Ok(roster_sig_bytes)) = (
        std::fs::read(pkg),
        std::fs::read(sig),
        std::fs::read(roster),
        std::fs::read(roster_sig),
    ) else {
        eprintln!("atpkg: cannot read the manifest, the roster, or one of their signatures");
        return ExitCode::from(1);
    };
    let anchor = crate::Anchor::of(vec![master.clone()], 0);
    let Ok(admitted) = crate::admit_roster(&anchor, roster_bytes, &roster_sig_bytes, now_unix())
    else {
        // Opaque on purpose — no verification oracle (§8).
        eprintln!("FAIL: the roster did not verify under that master, or is stale");
        return ExitCode::from(1);
    };
    match admitted.authorize_bytes(raw, &sig_bytes) {
        Ok((verified, who)) => {
            // THE SIGNATURE IS HALF THE CHAIN. A client parses these bytes the moment
            // they verify and drops the program on any refusal, so a self-check that
            // stopped at the signature reported OK on manifests the whole fleet refuses.
            if let Some(why) = pkg_post_verify_refusal(&verified) {
                eprintln!(
                    "FAIL: machine {} signed this manifest, but no client can read it — \
                     {why}",
                    who.machine_id
                );
                return ExitCode::from(1);
            }
            println!(
                "OK: manifest signed by machine {} under roster seq {}",
                who.machine_id, who.roster_seq
            );
            ExitCode::SUCCESS
        }
        Err(_) => {
            eprintln!("FAIL: no machine on that roster signed this manifest");
            ExitCode::from(1)
        }
    }
}

/// The CLIENT's post-verify gates over an already-authenticated `pkg-*.toml`
/// ([`crate::manifest::parse_pkg`]): `None` when the manifest parses exactly as a client
/// reads it, `Some(reason)` naming the fix when every client would refuse it.
///
/// WHY A SIGNATURE CHECK ALONE WAS A FALSE OK. `crate::flow` reaches a manifest's bytes
/// through `verify_pkg(raw, &sig)` and then IMMEDIATELY through `parse_pkg(&verified)`,
/// discarding the candidate on any refusal — no message, no diagnosis, the program simply
/// reads as not installable. This verb is the publish pipeline's and the public mirror's
/// only client-side check (`tools/atpkg-publish-lib.sh`, `tools/atpkg-mirror-public.sh`
/// step 1), both of which call it "the client's own rule" — so before this, a manifest
/// with a `shim_env` entry no shim can lay, an `https` row missing `asset`/`sha256`, the
/// retired `kind = "vendor-fetch"`, or a `schema` this build cannot read passed the
/// self-check, was mirrored, and was then refused fleet-wide — discovered by users, after
/// the tag and the build number had been spent. [`crate::Reject::ShimEnv`] promises in its
/// own doc that THIS verb names the fix, and [`crate::Reject::RetiredKind`] that the
/// authoring machine's own `atpkg install` does; each carries its reason, and this is where
/// the publish pipeline reads both.
///
/// Naming them is not a verification oracle (§8), exactly as [`cmd_verify_index`] naming
/// [`crate::Reject::Requires`] is not: every variant reachable here is a PARSE failure
/// over bytes whose signature already verified.
fn pkg_post_verify_refusal(verified: &crate::VerifiedBytes) -> Option<String> {
    match crate::manifest::parse_pkg(verified) {
        Ok(_) => None,
        Err(crate::Reject::ShimEnv(why)) => Some(format!("shim_env: {why}")),
        Err(crate::Reject::RetiredKind(why)) => Some(format!("retired row: {why}")),
        Err(crate::Reject::Schema) => Some(format!(
            "its `schema` is newer than this build reads (this build understands \
             schema {})",
            crate::manifest::SUPPORTED_SCHEMA
        )),
        Err(crate::Reject::Malformed) => Some(
            "malformed: not UTF-8, not TOML, a required field missing or duplicated, or a \
             `github-release`/`https` row with no `asset`/`sha256` (a `pkg` row, no \
             `url`/`sha256`)"
                .to_string(),
        ),
        // Unreachable from a parse over authenticated bytes — total rather than silent,
        // so a new post-verify gate can never fall through this verb as an OK.
        Err(other) => Some(format!("refused post-verify: {other:?}")),
    }
}

/// `atpkg verify [program]` (§12) — re-attest the installed store against the last-trusted
/// SIGNED `tree_root` recorded in `status.toml` at install/update. A drift/integrity audit;
/// compares the recomputed `tree_root` against the signed value, never an unsigned hash. No
/// network. Exit 0 iff every audited program matches.
fn cmd_verify(program: Option<&String>) -> ExitCode {
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let outcomes = match program {
        Some(p) => vec![(p.clone(), crate::verify::verify_program(&layout, p))],
        None => crate::verify::verify_all(&layout),
    };
    if outcomes.is_empty() {
        println!("{EMPTY_VERIFY}");
        return ExitCode::SUCCESS;
    }
    let mut bad = 0u32;
    for (name, o) in &outcomes {
        use crate::verify::VerifyOutcome::{
            BuildMismatch, Drift, Match, NoSignedRoot, NotInstalled, Unreadable, WiredSysroot,
        };
        match o {
            Match { build } => {
                println!("atpkg: {name} build {build} OK (matches signed tree_root)")
            }
            WiredSysroot { build } => println!(
                "atpkg: {name} build {build} OK (rustup-linked sysroot bundle; verified at install, \
                 not tree-attestable after install-time wiring — use a self-contained bundle for full \
                 attestation)"
            ),
            Drift { build, .. } => {
                bad += 1;
                eprintln!(
                    "atpkg: {name} build {build} DRIFT — store tree does not match the signed tree_root"
                );
            }
            NoSignedRoot { .. } => {
                bad += 1;
                eprintln!(
                    "atpkg: {name} — no signed tree_root recorded; aterm pkg install {name} \
                     reinstalls it and records one"
                );
            }
            NotInstalled => {
                bad += 1;
                eprintln!("{}", not_installed_fix(name));
            }
            Unreadable { build, error } => {
                bad += 1;
                eprintln!("atpkg: {name} build {build} unreadable: {error}");
            }
            BuildMismatch { active, recorded } => {
                bad += 1;
                eprintln!(
                    "atpkg: {name} active build {active} differs from recorded {recorded:?} — cannot attest; update/reinstall"
                );
            }
        }
    }
    if bad == 0 {
        ExitCode::SUCCESS
    } else {
        ExitCode::from(1)
    }
}

/// `atpkg link <program> <checkout> [rel-bin…]` (§13) — dev-link curated bins from a sibling
/// checkout into `bin/` (all through the sensitive-name deny-list), marking the program so
/// `update`/`apply` HARD-SKIP it until `atpkg unlink`. Default bin when none given:
/// `target/release/<program>`.
fn cmd_link(rest: &[String]) -> ExitCode {
    let (Some(program), Some(checkout)) = (rest.first(), rest.get(1)) else {
        eprintln!("usage: atpkg link <program> <checkout> [rel-bin…]");
        return ExitCode::from(2);
    };
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let checkout_path = std::path::Path::new(checkout);
    let bins: Vec<std::path::PathBuf> = if rest.len() > 2 {
        rest[2..].iter().map(std::path::PathBuf::from).collect()
    } else if let Some(sysroot) = sysroot_link_bins(&layout, program, checkout_path) {
        // A TOOLCHAIN, not a cargo project: shim every exposed name out of `<root>/bin` so
        // each keeps its sibling `lib/` (see `sysroot_link_bins`). An explicit rel-bin list
        // still wins — this only replaces the DEFAULT.
        match sysroot {
            Ok(bins) => {
                println!(
                    "atpkg: {checkout} is a sysroot — linking {} exposed command(s) from bin/",
                    bins.len()
                );
                bins
            }
            Err(missing) => {
                eprintln!(
                    "atpkg: refusing to dev-link {program}: {} of its exposed command(s) are \
                     missing from {checkout}/bin — {}",
                    missing.len(),
                    missing.join(", ")
                );
                eprintln!(
                    "atpkg:   linking the rest would leave those commands on the INSTALLED \
                     build while the others moved to your checkout — one PATH, two toolchains,"
                );
                eprintln!(
                    "atpkg:   different sysroots. Build the missing tools, or pass an explicit \
                     rel-bin list to say you meant a partial link."
                );
                return ExitCode::from(1);
            }
        }
    } else {
        // Default to the conventional cargo release bin, WITH the platform executable
        // extension (`target/release/<program>.exe` on Windows) — else `src.is_file()`
        // never matches the real exe and the link silently yields NoBins. Shared with
        // the `[packages.links]` reconciliation.
        default_link_bins(program)
    };
    match crate::link(&layout, program, checkout_path, &bins) {
        Ok(out) => {
            println!(
                "atpkg: dev-linked {program} ({}); `aterm pkg unlink {program}` to release it",
                if out.linked.is_empty() {
                    "no bins".into()
                } else {
                    out.linked.join(", ")
                }
            );
            for r in &out.refused {
                eprintln!("atpkg:   refused sensitive bin name {r} (deny-list)");
            }
            // The seam follows the link from this moment, not from the next six-hourly
            // pass: otherwise `targo` on PATH and `cargo +trust` are two compilers under
            // one name.
            if program == crate::seam::SEAM_PROGRAM {
                reassert_rustup_seam(&layout);
            }
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg: link {program} failed: {e}");
            ExitCode::from(2)
        }
    }
}

/// `atpkg unlink <program>` (§13) — remove a program's dev links and its marker, then PUT
/// THE INSTALLED BUILD BACK ON PATH. A pin is preserved across the cycle.
///
/// RESTORATION IS PART OF UNLINK, not a follow-up the user is expected to remember. Removing
/// the dev shims alone leaves every command the link covered simply ABSENT: measured on a
/// 15-command toolchain, a link/unlink cycle took `targo`, `trustc` and `tippy` off PATH
/// entirely while leaving the commands the dev build had not carried still pointing at the
/// installed build. "Your compiler disappeared" is not an acceptable resting state for a
/// verb whose whole job is to undo something, and a partial PATH is the same split-toolchain
/// hazard `link` now refuses to create.
///
/// The store build is guaranteed to still be there: gc's claim union counts the per-program
/// `current` link, so a dev-linked program's build is never reclaimable (measured — gc
/// reports "nothing to reclaim" while linked). Restoration is therefore just re-asserting
/// shims that already have a target, never a download.
///
/// Best-effort by design: a program that was never installed (dev-linked from the start) has
/// nothing to restore, and that is reported rather than treated as failure.
fn cmd_unlink(program: Option<&String>) -> ExitCode {
    let Some(program) = program else {
        eprintln!("usage: atpkg unlink <program>");
        return ExitCode::from(2);
    };
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    // Read the marker BEFORE `unlink` removes it: it is the only record of which names this
    // link took over, and restoring anything else would touch programs that were never linked.
    let owned = crate::linked_tool_names(&layout, program);
    match crate::unlink(&layout, program) {
        Ok(()) => {
            println!("atpkg: unlinked {program} (any pin is preserved)");
            match restore_installed_shims(&layout, program, owned.as_deref()) {
                Ok(0) => println!(
                    "atpkg:   no installed build to restore — {program} is off PATH until \
                     `aterm pkg install {program}`"
                ),
                Ok(n) => println!("atpkg:   restored {n} command(s) from the installed build"),
                Err(e) => eprintln!(
                    "atpkg:   WARNING: could not restore the installed build's shims ({e}); \
                     run `aterm pkg install {program}` to put it back on PATH"
                ),
            }
            // …and rustup's `trust` returns to the installed build with the shims: rebuilt
            // from the store, or — with no build to restore — detached, never left dangling.
            if program == crate::seam::SEAM_PROGRAM {
                if std::fs::symlink_metadata(crate::seam::store_current(&layout)).is_ok() {
                    reassert_rustup_seam(&layout);
                } else {
                    detach_rustup_seams(&layout);
                }
            }
            ExitCode::SUCCESS
        }
        Err(e) => {
            eprintln!("atpkg: unlink {program} failed: {e}");
            ExitCode::from(1)
        }
    }
}

/// Re-assert `program`'s shims from its CURRENT store build. Returns how many were written,
/// or 0 when the program has no installed build.
///
/// `owned` is the set the link took over (from its marker), and restoration is confined to
/// it — see the note inside on why the build's `bin/` listing is NOT a safe substitute. Each
/// name must also exist in the installed build, and still passes the same admission the
/// install path uses (`ToolName::new` — the sensitive-name deny-list), so a restore can no
/// more shadow `git`/`sudo` than an install can.
fn restore_installed_shims(
    layout: &crate::store::Layout,
    program: &str,
    owned: Option<&[String]>,
) -> std::io::Result<usize> {
    let current = layout.program_current(program);
    if !current.exists() {
        return Ok(0);
    }
    let build_dir = std::fs::canonicalize(&current)?;
    let bin = build_dir.join("bin");
    if !bin.is_dir() {
        return Ok(0);
    }
    // ONLY the names the link owned. A sysroot bundle's `bin/` carries backends belonging to
    // OTHER atpkg programs — `trust`'s carries `ay`, `clean`, `ty` — so restoring from the
    // directory listing repoints those programs' shims at this build. Measured: it clobbered
    // all three onto `store/trust/6459`. The marker's set is scoped to this link, which makes
    // the restore symmetric with the takeover.
    let Some(owned) = owned else {
        return Ok(0);
    };
    let mut tools = Vec::new();
    for name in owned {
        // Still require the binary to exist in the installed build: the dev checkout and the
        // registry build need not expose the same set, and a shim with nothing behind it is
        // worse than an absent one.
        let file = bin.join(format!("{name}{}", std::env::consts::EXE_SUFFIX));
        if !file.is_file() {
            continue;
        }
        if let Some(tool) = crate::store::ToolName::new(name) {
            tools.push(tool);
        }
    }
    if tools.is_empty() {
        return Ok(0);
    }
    // The alias policy, offline: the §14 cached index's verdict when one is cached, else
    // what is on disk — a dev link takes over the PLAIN names only and the aliases kept
    // naming the store copy throughout, so the restore must neither sweep those aliases
    // nor invent any for a vendor tool.
    let aliases = alias_policy_offline(layout, program);
    // The store build's own `shim_env` (its sidecar, design S7): the dev link took the
    // plain names over with plain shims, and the restore lays them back as the signed
    // manifest declared them.
    let env = crate::shim_env::read_sidecar(&build_dir);
    crate::activate::install_tools_env(layout, &build_dir, &tools, aliases, &env)?;
    Ok(tools.len())
}

/// The `alab-<tool>` alias policy for `program` on a verb that fetches no index: the §14
/// cached index's signed entry decides ([`crate::activate::Aliases::for_program`]) when
/// one is cached; with nothing cached, the aliases already laid on disk are the policy
/// ([`crate::activate::Aliases::laid_for`]) — so a vendor tool never grows an alias on
/// this path unless a signed entry said it was ALab's, and an ALab tool a pre-alias client
/// installed keeps whatever the pass has laid so far.
fn alias_policy_offline(layout: &crate::store::Layout, program: &str) -> crate::activate::Aliases {
    match cached_index(layout) {
        Some(index) => crate::activate::Aliases::for_program(program, index.program(program)),
        None => crate::activate::Aliases::laid_for(layout, program),
    }
}

/// `atpkg refresh [program…]` (§13) — re-assert dev links from their markers (picking up
/// newly-built bins). No arg ⇒ refresh every dev-linked program. Per-name isolation: one
/// failure never blocks the rest.
fn cmd_refresh(rest: &[String]) -> ExitCode {
    let Some(layout) = layout() else {
        return ExitCode::from(1);
    };
    let names: Vec<String> = if rest.is_empty() {
        crate::linked_programs(&layout)
    } else {
        rest.to_vec()
    };
    if names.is_empty() {
        println!("atpkg: nothing dev-linked to refresh");
        return ExitCode::SUCCESS;
    }
    let mut failed = 0u32;
    for program in &names {
        match crate::refresh(&layout, program) {
            Ok(out) => println!(
                "atpkg: refreshed {program} ({})",
                if out.linked.is_empty() {
                    "no bins".into()
                } else {
                    out.linked.join(", ")
                }
            ),
            Err(e) => {
                failed += 1;
                eprintln!("atpkg: refresh {program} failed: {e}");
            }
        }
    }
    if failed == 0 {
        ExitCode::SUCCESS
    } else {
        ExitCode::from(1)
    }
}

#[cfg(all(test, unix))]
mod tests {
    /// THE PRODUCTION HALF of this file, for the source scans below.
    ///
    /// The anchor is the test module's OWN attribute. `#[cfg(test)]` — what these
    /// scans used to split on — never matched this module, which is
    /// `#[cfg(all(test, unix))]`: the split landed on the first scan's own string
    /// literal instead, so "production" silently included ~2 800 lines of tests, and
    /// the first real `#[cfg(test)]` item to appear in production code (the
    /// announcement ledger's reset seam, 2026-09-11) truncated every scan to the
    /// first few thousand lines and turned them all vacuous at once. An anchor that
    /// can miss is the same defect these scans exist to catch, so this one panics
    /// rather than falling back to the whole file.
    fn production_half(src: &str) -> &str {
        let (before, _) = src
            .split_once("#[cfg(all(test, unix))]")
            .expect("this file's test module is gated `#[cfg(all(test, unix))]`");
        assert!(
            before.contains("fn cmd_seed") && !before.contains("mod tests {"),
            "the production half must hold the verbs and none of the tests"
        );
        before
    }

    /// EVERY FIXTURE PASS RUNS AGAINST AN EMPTY `PATH`.
    ///
    /// Deliberately shadowing [`super::install_default_set`] for the whole test
    /// module, rather than editing sixteen call sites that a seventeenth could
    /// then forget: a pass consults `PATH` twice — for a foreign copy ahead of
    /// ours (`reconcile_shadowed`) and for a system install that already
    /// satisfies a program (`reconcile_system_satisfied_with`) — and a fixture that
    /// reads the DEVELOPER's `PATH` is judging a temp prefix against the real
    /// machine.
    ///
    /// That is not hypothetical and it is not a flake. On any box with aterm's
    /// own toolchain installed — `~/Library/Application Support/aterm/pkg/bin`,
    /// i.e. the machine this crate is developed and dogfooded on — `ay` resolves
    /// outside the fixture prefix, so the recorded row reads `managed 18 —
    /// SHADOWED by …` where the fixture expects `pinned by index 41`, and
    /// `a_program_whose_requirement_is_unmet_…` and
    /// `an_index_listed_extra_gets_a_consent_stub_…` failed for exactly the
    /// people most likely to run them. The shipping behaviour was right the
    /// whole time; only the test's environment was ambient.
    ///
    /// An EMPTY value, not the real one filtered: it is the only setting that
    /// cannot acquire a foreign binary later. A pass that wants to exercise
    /// shadowing calls `super::install_default_set_with_path` and says which
    /// `PATH` it means.
    ///
    /// The consent is the first launch's: it shapes only the wire lane's announcement, whose
    /// words `net_announcement`'s own tests pin.
    fn install_default_set(
        layout: &crate::store::Layout,
        fetcher: &dyn crate::flow::Fetcher,
        anchor: &crate::Anchor,
        cfg: &crate::config::PackagesConfig,
        lane: super::ProvisionLane,
        now: i64,
    ) -> super::DefaultSetOutcome {
        super::install_default_set_with_path(
            layout,
            fetcher,
            anchor,
            cfg,
            lane,
            super::Consent::InstallingAterm,
            now,
            None,
            Some(std::ffi::OsStr::new("")),
        )
    }

    /// A FLAG IS NOT A PROGRAM. Measured on a live box: a mistyped
    /// `atpkg install --progress-file` recorded the flag as a wanted program,
    /// and `doctor` then carried a standing warning about a row that "cannot be
    /// a program name". Refusing at the door is the fix; the exit code is the
    /// usage code, not the failure code, because the user's command was
    /// malformed rather than their system broken.
    #[test]
    fn install_refuses_a_flag_as_a_program_name() {
        for flag in ["--progress-file", "--wait-lock", "-v", "--default-sett"] {
            let code = super::cmd_install_elevated(Some(&flag.to_string()), None, "install");
            assert_eq!(
                code,
                std::process::ExitCode::from(2),
                "{flag:?} must be refused as a usage error, never recorded"
            );
        }
        // The one flag-shaped argument that IS a verb must reach its branch
        // rather than the refusal. Asserted structurally — in a test process
        // with no root key pinned `--default-set` also exits 2, from the
        // manager gate rather than from this guard, so the exit code cannot
        // tell the two apart.
        let src = include_str!("cli.rs");
        let default_set_at = src
            .find("if program == \"--default-set\" {")
            .expect("the --default-set branch");
        let guard_at = src
            .find("is a command-line flag, not a program name")
            .expect("the flag guard");
        assert!(
            default_set_at < guard_at,
            "--default-set must be answered BEFORE the flag guard, or the \
             documented whole-toolset spelling would refuse itself"
        );
    }

    /// The `--wait-lock <secs>` edge grammar (2026-09-10), mirror of the
    /// `--progress-file` reasoning above: stripped for the three provisioning verbs
    /// wherever it sits, the LAST parsable value wins, and an unparsable value — or a
    /// bare flag at the END of argv — is consumed and means "no wait", never a program
    /// name. (A valueless flag mid-argv eats the next token instead; no spawner
    /// produces that shape — see `take_wait_lock_flag`.)
    #[test]
    fn wait_lock_flag_is_stripped_at_the_edge_and_degrades_to_no_wait() {
        let argv = |parts: &[&str]| parts.iter().map(ToString::to_string).collect::<Vec<_>>();
        let secs = |n: u64| Some(std::time::Duration::from_secs(n));

        let mut args = argv(&["seed", "--wait-lock", "5"]);
        assert_eq!(super::take_wait_lock_flag(&mut args), secs(5));
        assert_eq!(args, argv(&["seed"]));

        let mut args = argv(&["install", "--default-set", "--wait-lock", "9"]);
        assert_eq!(super::take_wait_lock_flag(&mut args), secs(9));
        assert_eq!(args, argv(&["install", "--default-set"]));

        let mut args = argv(&["update", "--wait-lock"]);
        assert_eq!(super::take_wait_lock_flag(&mut args), None, "bare: no wait");
        assert_eq!(args, argv(&["update"]));

        let mut args = argv(&["seed", "--wait-lock", "soon"]);
        assert_eq!(
            super::take_wait_lock_flag(&mut args),
            None,
            "unparsable: no wait"
        );
        assert_eq!(args, argv(&["seed"]), "…and the value is consumed");

        let mut args = argv(&["seed", "--wait-lock", "1", "--wait-lock", "2"]);
        assert_eq!(super::take_wait_lock_flag(&mut args), secs(2), "LAST wins");
        assert_eq!(args, argv(&["seed"]));

        let mut args = argv(&["update"]);
        assert_eq!(super::take_wait_lock_flag(&mut args), None);
        assert_eq!(args, argv(&["update"]), "absent: untouched");
    }

    /// "My window is gone" is a CHANGE of parent from the spawner the edge recorded
    /// — never "pid 1" on its own: the macOS orphan (launchd) and the Linux
    /// subreaper orphan (any other pid) both count; the spawner itself never does,
    /// even when it IS init (a scheduler's job); a parent nobody can read (Windows)
    /// and a spawner nobody named never count — those wait out the bound instead.
    #[test]
    fn an_orphaned_waiter_is_one_whose_parent_changed_from_its_spawner() {
        assert!(
            !super::is_orphaned(Some(15359), Some(15359)),
            "still the window"
        );
        assert!(super::is_orphaned(Some(15359), Some(1)), "macOS: launchd");
        assert!(
            super::is_orphaned(Some(15359), Some(4242)),
            "Linux: a subreaper, not pid 1"
        );
        assert!(
            !super::is_orphaned(Some(15359), None),
            "Windows: the window named itself but the parent cannot be read — \
             never judged gone (this was an orphan at the first contended poll)"
        );
        assert!(
            !super::is_orphaned(Some(1), Some(1)),
            "a launchd agent's unnamed job: init IS its spawner, and it is not an \
             orphan (this was a silent, instant exit 75)"
        );
        assert!(
            !super::is_orphaned(None, Some(1)),
            "no spawner named: nothing to compare against, init included"
        );
        assert!(
            !super::is_orphaned(None, Some(4242)),
            "no spawner named and not init: never on the change rule alone"
        );
        assert!(!super::is_orphaned(None, None));
        assert_eq!(super::SPAWNER_PID_ENV, "ATPKG_SPAWNER_PID");
    }

    /// The `lock-waiting:` marker is printed by ONE production site — the lock edge
    /// (`mutator_store_lock`) — with the `atpkg: ` prefix the GUI strips, and the
    /// literal itself appears once (the constant). A second emission site would let
    /// a verb body announce a wait it is not in.
    #[test]
    fn the_lock_waiting_marker_has_exactly_one_production_emission_site() {
        let src = include_str!("cli.rs");
        // `production_half`, not a split on `#[cfg(test)]`: that literal first
        // appears INSIDE production now (the announcement ledger's reset seam), so
        // the old split truncated this scan to the first few thousand lines.
        let production = production_half(src);
        assert!(production.len() < src.len());
        assert_eq!(
            production.matches("LOCK_WAITING_MARKER}").count(),
            1,
            "exactly one production emission site"
        );
        assert!(
            production.contains("atpkg: {LOCK_WAITING_MARKER}"),
            "the emission carries the `atpkg: ` prefix parse_seed_line strips"
        );
        assert_eq!(
            production.matches("\"lock-waiting: \"").count(),
            1,
            "the literal lives in the constant and nowhere else"
        );
        let edge = production
            .find("fn mutator_store_lock")
            .expect("the lock edge");
        let edge_end = production[edge + 1..]
            .find("\nfn ")
            .map(|i| edge + 1 + i)
            .expect("a function after the lock edge");
        let emission = production
            .find("{LOCK_WAITING_MARKER}")
            .expect("the emission");
        assert!(
            edge < emission && emission < edge_end,
            "the emission is inside the lock edge, not in a verb body"
        );
        // Its answer (`lock-acquired:`, 2026-09-14) has the same one site, AFTER the
        // announcement in the same function: a verb body cannot claim a lock it
        // did not wait for.
        assert_eq!(
            production.matches("LOCK_ACQUIRED_MARKER}").count(),
            1,
            "exactly one production emission site for the answer"
        );
        assert_eq!(production.matches("\"lock-acquired: \"").count(), 1);
        let answer = production
            .find("{LOCK_ACQUIRED_MARKER}")
            .expect("the answer's emission");
        assert!(
            emission < answer && answer < edge_end,
            "the answer is inside the lock edge, after the announcement"
        );
    }
    use super::*;
    use std::os::unix::fs::PermissionsExt;

    /// The `install` argv: `--elevate=` takes exactly the three spellings, in either
    /// position; anything else is a usage error before any store is touched, and so is
    /// a second operand. (`--elevate=…` alone falls through to the usage line: no
    /// program named.)
    #[test]
    fn install_parses_the_elevate_flag_and_refuses_the_rest_at_the_door() {
        let two = std::process::ExitCode::from(2);
        assert_eq!(cmd_install_argv(&["--elevate=bogus".to_string()]), two);
        assert_eq!(
            cmd_install_argv(&["brew".to_string(), "--elevate=root".to_string()]),
            two
        );
        assert_eq!(
            cmd_install_argv(&["brew".to_string(), "clt".to_string()]),
            two
        );
        assert_eq!(cmd_install_argv(&["--elevate=osascript".to_string()]), two);
        // A flag-shaped operand is still refused as a program name, whatever the
        // elevation asked for.
        assert_eq!(
            cmd_install_argv(&["--elevate=sudo".to_string(), "--foo".to_string()]),
            two
        );
        // The three admitted spellings are exactly the ones the dispatch-edge guard lets
        // through after the verb, so `install --elevate=osascript brew` reaches here.
        let (_, allowed) = NAME_TAKING_VERBS
            .iter()
            .find(|(v, _)| *v == "install")
            .unwrap();
        for mode in ["sudo", "osascript", "never"] {
            assert!(
                allowed.contains(&format!("--elevate={mode}").as_str()),
                "{mode}"
            );
        }
    }

    /// AN ELEVATION THE WHOLE SET COULD NEVER APPLY IS REFUSED, NOT DROPPED.
    ///
    /// `--default-set --elevate=…` parsed cleanly and then vanished: the value reached
    /// [`cmd_install_elevated`], which returns into `cmd_install_default_set()` at its
    /// `--default-set` branch ABOVE the one `set_elevation(door_elevation(explicit, …))`
    /// call, so the whole-set pass ran Deferred — `aterm pkg install --default-set
    /// --elevate=osascript` exited 0 having elevated nothing, recorded `needs admin —
    /// run: aterm pkg install <name>` for every member that needs one, and said not a
    /// word about the flag. The pass never elevates by design, so the grammar answers
    /// with the usage exit instead.
    ///
    /// Asserted on [`parse_install_argv`], never on the verb: reaching
    /// `cmd_install_default_set` would apply this machine's `[machine]` settings and
    /// walk `$HOME` to find out what an argv means.
    #[test]
    fn an_elevation_the_whole_set_could_never_apply_is_refused_not_dropped() {
        for mode in ["sudo", "osascript", "never"] {
            let flag = format!("--elevate={mode}");
            let set = "--default-set".to_string();
            assert_eq!(
                parse_install_argv(&[set.clone(), flag.clone()]),
                Err(ExitCode::from(2)),
                "install --default-set --elevate={mode} must be refused, not dropped"
            );
            assert_eq!(
                parse_install_argv(&[flag, set]),
                Err(ExitCode::from(2)),
                "install --elevate={mode} --default-set — the other order, same answer"
            );
        }
        // NON-REGRESSION: neither half is refused on its own. The whole set still
        // installs, and one program still takes the door with the elevation it named.
        let set = vec!["--default-set".to_string()];
        assert_eq!(parse_install_argv(&set), Ok((Some(&set[0]), None)));
        let door = vec!["brew".to_string(), "--elevate=osascript".to_string()];
        assert_eq!(
            parse_install_argv(&door),
            Ok((Some(&door[0]), Some(crate::Elevation::Osascript))),
            "one program keeps the door it asked for"
        );
    }

    /// The door's elevation rule is [`crate::elevate::door_elevation`]; this pins that
    /// the verb is the ONLY place it is set — an unattended pass never names it.
    #[test]
    fn only_the_explicit_door_sets_an_elevation_policy() {
        let src = include_str!("cli.rs");
        // Spelled in two halves so this test's own text is not a call site.
        let needle = format!("{}{}", "crate::elevate::set_elev", "ation(");
        let sets: Vec<usize> = src.match_indices(&needle).map(|(i, _)| i).collect();
        assert_eq!(sets.len(), 1, "one call site, in cmd_install_elevated");
        let door = src.find("fn cmd_install_elevated(").unwrap();
        let next_fn = src[door + 1..].find("\nfn ").map(|i| door + 1 + i).unwrap();
        assert!(
            door < sets[0] && sets[0] < next_fn,
            "set_elevation lives inside cmd_install_elevated"
        );
    }

    /// A translated process must never reach a permanent "wrong architecture"
    /// verdict: `current_triple()` is a compile-time cfg, so the x86_64 slice of the
    /// universal binary says x86_64 on an Apple Silicon Mac, and the verdict that
    /// follows deleted the seal from a machine that could have installed every
    /// program natively (2026-08-20 round-8 audit).
    #[test]
    fn a_translated_process_is_not_evidence_about_this_mac() {
        // On the native arm64 slice this is false, which is the pre-existing path.
        // The point of the test is that the decision CONSULTS it at all: the arch
        // arm of `finish_unusable_seed` must be gated on it before setting
        // `permanent`, or the reclaim is keyed on a compile-time constant.
        let src = include_str!("cli.rs");
        let arm = src
            .find("the bundled toolchain has no build for this machine's")
            .expect("the architecture verdict");
        let guard = src[..arm]
            .rfind("running_translated()")
            .expect("a translation check before the architecture verdict");
        let permanent = src[..arm]
            .rfind("permanent = true;")
            .expect("the permanent assignment");
        assert!(
            guard < permanent,
            "the translation check must come BEFORE the permanent architecture verdict \
             it gates"
        );
        assert!(!running_translated() || cfg!(target_arch = "x86_64"));
    }

    /// An unattended update must not undo a deliberate uninstall. The coherence
    /// rule ("a group with any installed member is applied whole, missing siblings
    /// pulled in") re-downloaded ~3.2 GB of `trust` on the next tick after the user
    /// removed it (2026-08-20 round-8 audit).
    #[test]
    fn a_removed_program_is_read_from_the_layout_by_every_lane() {
        // The prefix goes through the REAL `store::resolve`, so it must be one the
        // chain check admits: strictly under the home directory, a private (0700) real
        // directory. The process temp dir (`/var/folders/…` on macOS) is neither under
        // the home directory nor root-owned, so `vet_prefix` fell back to the DEFAULT
        // prefix — and this test then wrote `trust` into the real store's removed
        // record where that prefix existed, or died on a missing directory where it
        // did not.
        let home = aterm_types::dirs::home_dir().expect("a home directory");
        let root = home.join(format!(".atpkg-test-removed-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&root);
        std::fs::create_dir_all(&root).unwrap();
        std::fs::set_permissions(&root, std::fs::Permissions::from_mode(0o700)).unwrap();
        let layout = crate::store::resolve(Some(&root)).expect("layout");
        assert_eq!(
            layout.prefix, root,
            "PRECONDITION: the vet admits the configured prefix — a fallback here would \
             aim this test at the real store"
        );
        std::fs::write(layout.removed(), "# deliberate\ntrust\n\n").unwrap();
        let removed = layout.removed_programs();
        assert!(
            removed.contains("trust"),
            "the durable record is the answer"
        );
        assert_eq!(removed.len(), 1, "comments and blanks are not programs");
        // The CLI reader and the flow reader must be the same answer, or the
        // unattended lane disagrees with the verb the user ran.
        assert_eq!(removed, removed_programs(&layout));
        let _ = std::fs::remove_dir_all(&root);
    }

    /// A seal inside a build output belongs to whatever produced it. Deleting one
    /// took a gigabyte out of a release mid-package on 2026-08-19; every other
    /// guard passed, because the app really was ours and the payload really was
    /// spent for this machine.
    #[test]
    fn a_seal_in_a_build_output_is_not_ours_to_reclaim() {
        let root = std::env::temp_dir().join(format!("atpkg-buildout-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&root);
        let seed_in = |app: &std::path::Path| app.join("Contents/Resources/toolchain-seed.lproj");

        // The cutter's own marker, written by `aterm-release`'s bundle::assemble.
        let cut = root.join("dist/cut-app");
        std::fs::create_dir_all(seed_in(&cut.join("aterm.app"))).unwrap();
        std::fs::write(cut.join(".metadata_never_index"), "").unwrap();
        assert!(
            is_build_output_bundle(&seed_in(&cut.join("aterm.app"))),
            "a bundle beside .metadata_never_index is an artifact, not an install"
        );

        // A real install has no such marker beside it, and still reclaims.
        let installed = root.join("Applications");
        std::fs::create_dir_all(seed_in(&installed.join("aterm.app"))).unwrap();
        assert!(
            !is_build_output_bundle(&seed_in(&installed.join("aterm.app"))),
            "an ordinary install must still reclaim its spent payload"
        );

        // Fail-safe: an unreadable/absent marker reads as a real install.
        assert!(!is_build_output_bundle(std::path::Path::new(
            "/nonexistent/aterm.app/Contents/Resources/toolchain-seed.lproj"
        )));
        let _ = std::fs::remove_dir_all(&root);
    }

    /// [`VERBS`] must name EXACTLY the verbs `main_entry`'s match dispatches —
    /// no more (advertising a verb that no longer exists) and no fewer (a
    /// working verb the hint never mentions).
    ///
    /// Derived from this file's own source rather than from a second hand-kept
    /// list, because a second list is the thing that rotted. `sync` and `seed`
    /// were each advertised for months after deletion; both would have failed
    /// this the day their arm was removed.
    #[test]
    fn verb_hint_matches_dispatch() {
        let src = include_str!("cli.rs");
        // The dispatch arms, in source order: `        Some("<verb>") =>`.
        let dispatched: Vec<&str> = src
            .lines()
            .filter_map(|line| line.trim().strip_prefix("Some(\""))
            .filter_map(|rest| rest.split_once("\")"))
            .filter(|(_, tail)| tail.trim_start().starts_with("=>"))
            .map(|(verb, _)| verb)
            .collect();

        // Non-vacuity: if the extraction ever stops matching the source shape it
        // would silently compare two empty lists and pass.
        assert!(
            dispatched.len() > 10,
            "extracted only {} arm(s) — the scraper stopped matching the match block, \
             so this test was about to pass vacuously: {dispatched:?}",
            dispatched.len()
        );

        assert_eq!(
            dispatched, VERBS,
            "the unknown-verb hint and the dispatch table disagree.\n  \
             dispatched: {dispatched:?}\n  advertised: {VERBS:?}\n\
             A verb in `advertised` but not `dispatched` tells the user to run \
             something that only reprints this error."
        );
    }

    /// [`VERB_TIERS`] must be an EXACT partition of [`VERBS`]: every dispatched verb
    /// placed in exactly one tier, and no tier naming a verb that does not dispatch.
    /// Without this, a verb added to the match with no tier would still parse but
    /// silently vanish from bare `atpkg`, `--help`, and the unknown-verb hint at once —
    /// the same drift that killed `sync` and `seed` when the roster was hand-kept.
    #[test]
    fn verb_tiers_partition_the_roster() {
        let mut seen: std::collections::BTreeSet<&str> = std::collections::BTreeSet::new();
        for (tier, verbs) in VERB_TIERS {
            for v in *verbs {
                assert!(
                    seen.insert(v),
                    "{v} appears in more than one tier (second: {tier}) — a verb has ONE home"
                );
                assert!(
                    VERBS.contains(v),
                    "{v} is tiered ({tier}) but not dispatched — the tiers advertise it \
                     while the match rejects it"
                );
            }
        }
        for v in VERBS {
            assert!(
                seen.contains(v),
                "{v} dispatches but has no tier — it would vanish from every human surface"
            );
        }
    }

    /// The shared tier block renders each advertised verb exactly once as a word —
    /// `status` folded into `doctor (or: status)` rather than dropped.
    #[test]
    fn tier_block_renders_every_verb_once() {
        let block = verb_tier_lines().join("\n");
        for v in VERBS {
            let count = block
                .split(|c: char| !(c.is_alphanumeric() || c == '-'))
                .filter(|w| w == v)
                .count();
            assert_eq!(
                count, 1,
                "{v} must appear exactly once in the tier block:\n{block}"
            );
        }
        assert!(
            block.contains("doctor (or: status)"),
            "status renders attached to doctor, not as a sibling:\n{block}"
        );
    }

    /// `doctor --json` swallowed the flag and exited 0 with the human report
    /// (audit D-12). A zero-arity verb refuses ANY argument at the usage exit,
    /// naming the verb, the argument and the arity — while `--help` still
    /// answers with usage via the dispatch edge, and the bare forms run.
    ///
    /// `repair` joined the three on 2026-09-16. It was dispatched as a bare
    /// `run_repair(layout())`, so `aterm pkg repair --dry-run` — or any other flag
    /// or operand — was swallowed while the verb rewrote the user's shell wiring
    /// and re-laid every shim at exit 0. The helper had always refused; what the
    /// arm lacked was the CALL, which is why the scan below is part of this test.
    #[test]
    fn zero_arity_verbs_refuse_arguments_instead_of_swallowing_them() {
        const ZERO_ARITY: [&str; 4] = ["doctor", "status", "gc", "repair"];
        for verb in ZERO_ARITY {
            let rest = vec!["--json".to_string()];
            let code = zero_arity(&rest, verb).expect("an argument must be refused");
            assert_eq!(code, ExitCode::from(2), "{verb} refusal is the usage exit");
            assert!(
                zero_arity(&[], verb).is_none(),
                "bare `{verb}` must proceed"
            );
        }
        // NON-VACUITY, and the half that actually regressed: the helper refuses for
        // ANY verb name, so the loop above says nothing about what a user's argv
        // reaches. Each of these verbs' dispatch arm must route through it. Read from
        // the production half, so this test's own text can never be the evidence.
        let production = production_half(include_str!("cli.rs"));
        for verb in ZERO_ARITY {
            let head = format!("Some(\"{verb}\") =>");
            let start = production
                .find(&head)
                .unwrap_or_else(|| panic!("`{verb}` must have a dispatch arm"));
            let rest = &production[start + head.len()..];
            // The arm ends where the next one begins — match arms sit at one indent
            // inside `main_entry`'s `match verb`.
            let arm = &rest[..rest.find("\n        Some(\"").unwrap_or(rest.len())];
            assert!(
                arm.contains(&format!("zero_arity(&args[1..], \"{verb}\")")),
                "`{verb}` dispatches without the arity guard, so an argument it takes \
                 none of is swallowed at exit 0 — on a mutator that means the store and \
                 the rc wiring change under a flag the user believes was honoured:{arm}"
            );
        }
        // `seed` is zero-arity too and refuses INLINE rather than through the helper,
        // so the roster above must not be read as "the only verbs that may not
        // swallow". Pinned so that check cannot quietly go the way `repair`'s did.
        let seed_at = production.find("\nfn cmd_seed(").expect("the seed verb");
        let seed = &production[seed_at..];
        let seed = &seed[..seed[1..].find("\nfn ").map_or(seed.len(), |i| i + 1)];
        assert!(
            seed.contains("if !rest.is_empty() {") && seed.contains("usage: atpkg seed"),
            "cmd_seed must still refuse the arguments it takes none of"
        );
    }

    /// A reader must never be told that a command they did not type failed.
    /// `atpkg update status` used to answer `atpkg: install status failed: …`
    /// because `update` on a not-installed program routes through the single
    /// install path (AUDIT-cli-per-verb-help-2026-08-31.md, D-4). The routing is
    /// right; answering under the wrong verb was not.
    #[test]
    fn a_routed_failure_names_the_verb_the_reader_typed() {
        let note = routed_install_note("update", "status").expect("update is a routed verb");
        assert!(note.contains("`update status`"), "{note}");
        assert!(
            note.contains("fresh install"),
            "the note must say WHY the remedy names a different verb: {note}"
        );
        assert!(
            !note.contains("install status failed"),
            "the note explains the remedy; it must not restate the failure: {note}"
        );
        // Typed `install` directly: the remedy is self-explanatory, so no note.
        assert_eq!(routed_install_note("install", "rustc"), None);
    }

    /// `help <x>` for an `x` that is not a verb must FAIL, not print the roster
    /// at exit 0. Three byte-identical pages at rc 0 for `install`, `gc` and
    /// `zzz-total-nonsense` is what made this a false success: a page of text
    /// after `help <x>` reads as the help for x (audit D-C).
    #[test]
    fn help_for_an_unknown_topic_is_a_usage_error_not_a_roster() {
        assert_eq!(
            cmd_verb_help("zzz-total-nonsense"),
            ExitCode::from(2),
            "an unknown topic must exit 2"
        );
        // A real verb still answers, at 0.
        assert_eq!(
            cmd_verb_help("install"),
            ExitCode::SUCCESS,
            "`install` is a verb; its help is a success"
        );
        // Every advertised verb is answerable, or `help` sends a reader to a
        // usage error for a verb the roster itself just offered them.
        for verb in dispatch_roster() {
            assert!(
                usage_of(verb).is_some(),
                "`atpkg help {verb}` would now exit 2 for a verb atpkg dispatches"
            );
        }
    }

    /// The suggestion engine: a one-slip typo gets the fix, gibberish gets silence —
    /// a far-fetched guess erodes trust in the near ones.
    #[test]
    fn did_you_mean_suggests_and_gives_up() {
        assert_eq!(did_you_mean("instal"), Some("install"));
        assert_eq!(did_you_mean("udpate"), Some("update"));
        // The newest verb is in the roster, so a one-slip typo of it gets the fix too.
        assert_eq!(did_you_mean("noindx"), Some("noindex"));
        assert_eq!(did_you_mean("frobnicate"), None);
    }

    /// The bare posture's script contract: the enabled HEAD never moves (counts ride
    /// after the em dash), the disabled line is byte-exact, and the tail names exactly
    /// one `next:` act.
    #[test]
    fn bare_posture_head_is_stable() {
        for counts in [None, Some((0, 0)), Some((10, 10))] {
            let lines = bare_lines(counts);
            assert!(
                lines[0].starts_with("atpkg: enabled (root key pinned)"),
                "the stable head leads: {:?}",
                lines[0]
            );
            let nexts = lines
                .iter()
                .filter(|l| l.starts_with("atpkg: next:"))
                .count();
            assert_eq!(
                nexts,
                usize::from(counts.is_some()),
                "one next act with a store, none without: {lines:?}"
            );
        }
        // Fresh machine: the one act is the install, and it says what it does.
        assert!(
            bare_lines(Some((0, 0)))
                .last()
                .unwrap()
                .contains("aterm pkg install --default-set"),
            "an empty store's next act is the whole-set install"
        );
    }

    /// NO DEAD ENDS: every empty/negative answer carries a `aterm pkg …` act the user
    /// can paste — the fact alone ("ty is not installed") always raises exactly one
    /// follow-up question, and the first hour should never need a second guess.
    #[test]
    fn empty_answers_name_a_next_command() {
        let family: Vec<String> = vec![
            EMPTY_LIST_HUMAN.to_string(),
            EMPTY_UPDATE.to_string(),
            EMPTY_VERIFY.to_string(),
            SEED_FOLLOW_ON.to_string(),
            not_installed_fix("ty"),
        ];
        for answer in &family {
            assert!(
                answer.contains("aterm pkg "),
                "an empty/negative answer must name its next command: {answer}"
            );
        }
    }

    /// The piped `list` form is a SCRIPT CONTRACT: `program\tbuild\tnotes`, headerless,
    /// ascending builds with superseded rows before the live one — byte-identical to
    /// every earlier release, dev-link and pin notes included.
    /// A program DEV-LINKED FROM THE START has no store build, and `list` used to omit
    /// it entirely — on a Linux box with the Trust stage2 dev-linked, `atpkg list`
    /// showed only claude and codex (2026-09-14). It gets a row in both forms, with `-`
    /// for the build it does not have, and counts as one program and one dev-link.
    #[test]
    fn a_program_dev_linked_from_the_start_is_listed_without_a_store_build() {
        let installed: Vec<(String, u64)> = vec![("claude".to_string(), 7)];
        let linked_only = vec!["trust".to_string()];
        let live: std::collections::BTreeMap<String, u64> = [("claude".to_string(), 7)].into();
        let linked: std::collections::BTreeMap<String, Option<std::path::PathBuf>> = [(
            "trust".to_string(),
            Some(std::path::PathBuf::from("/h/trust/build/host/stage2")),
        )]
        .into();
        let pinned = std::collections::BTreeSet::new();

        let porcelain = list_porcelain_lines(&installed, &linked_only, &live, &linked, &pinned);
        assert_eq!(
            porcelain[0], "trust\t-\tDEV-LINKED -> /h/trust/build/host/stage2",
            "{porcelain:?}"
        );
        assert_eq!(
            porcelain[1], "claude\t7\tlive",
            "installed rows are byte-identical to before: {porcelain:?}"
        );

        let human = list_human_lines(&installed, &linked_only, &live, &linked, &pinned);
        assert!(
            human[0].starts_with("atpkg: 2 program(s) — 1 live, 1 dev-linked"),
            "{human:?}"
        );
        let row = human
            .iter()
            .find(|l| l.contains("trust"))
            .expect("a trust row");
        assert!(
            row.contains("DEV-LINKED -> /h/trust/build/host/stage2"),
            "{row}"
        );
        assert!(
            row.contains("(no store build — linked from the start)"),
            "{row}"
        );
        assert!(
            !row.contains("shadowed"),
            "nothing is shadowed when there is no store build: {row}"
        );
    }

    #[test]
    fn list_porcelain_is_stable() {
        let installed = vec![
            ("ay".to_string(), 7987),
            ("ay".to_string(), 8256),
            ("clean".to_string(), 7345),
        ];
        let live: std::collections::BTreeMap<String, u64> =
            [("ay".to_string(), 8256), ("clean".to_string(), 7345)].into();
        let linked = std::collections::BTreeMap::new();
        let mut pinned = std::collections::BTreeSet::new();
        pinned.insert("clean".to_string());
        assert_eq!(
            list_porcelain_lines(&installed, &[], &live, &linked, &pinned),
            vec![
                "ay\t7987\tsuperseded (kept for rollback)".to_string(),
                "ay\t8256\tlive".to_string(),
                "clean\t7345\tlive  pinned".to_string(),
            ],
        );
    }

    /// The human report leads with the verdict-shaped summary, folds superseded rows
    /// into a per-program count, and appends its one `next` act only when some program
    /// has no live build (counts only, no health adjective — "healthy" is doctor's word).
    #[test]
    fn list_human_report_summarizes_and_names_doctor_only_when_needed() {
        let installed = vec![
            ("ay".to_string(), 7987),
            ("ay".to_string(), 8256),
            ("clean".to_string(), 7345),
        ];
        let live: std::collections::BTreeMap<String, u64> =
            [("ay".to_string(), 8256), ("clean".to_string(), 7345)].into();
        let none_linked = std::collections::BTreeMap::new();
        let none_pinned = std::collections::BTreeSet::new();
        let lines = list_human_lines(&installed, &[], &live, &none_linked, &none_pinned);
        assert_eq!(lines[0], "atpkg: 2 program(s) — 2 live");
        assert!(
            lines
                .iter()
                .any(|l| l.contains("(1 older build(s) kept for rollback)")),
            "the superseded build survives as a count: {lines:?}"
        );
        assert!(
            !lines.iter().any(|l| l.contains("next")),
            "a healthy listing names no next act: {lines:?}"
        );
        // A program with builds on disk and nothing live is the row that needs doctor.
        let dead_live: std::collections::BTreeMap<String, u64> = [("ay".to_string(), 8256)].into();
        let lines = list_human_lines(&installed, &[], &dead_live, &none_linked, &none_pinned);
        assert_eq!(
            lines[0],
            "atpkg: 2 program(s) — 1 live, 1 without a live build"
        );
        assert!(
            lines.iter().any(|l| l.contains("NO LIVE BUILD")),
            "the dead program is loud: {lines:?}"
        );
        assert_eq!(
            lines.last().unwrap(),
            "atpkg: next — aterm pkg doctor",
            "exactly one next act, at the end: {lines:?}"
        );
    }

    /// `list` with no resolvable prefix exits 1 like every sibling read verb — it used
    /// to print the error and exit 0, a silent green in an already-broken environment.
    #[test]
    fn list_exit_codes() {
        assert_eq!(
            format!("{:?}", run_list(None, true)),
            format!("{:?}", ExitCode::from(1)),
            "no layout is a structural failure, not a success"
        );
    }

    /// UNINSTALLING WHAT IS NOT THERE IS A REFUSAL, NOT A SUCCESS. This used to print
    /// "uninstalled ty", exit 0, and mint a durable removed-marker that silently
    /// suppressed future set-completion — a fabricated success with a hidden state
    /// change. Partial-install debris (a store tree with no shims) must still clean.
    #[test]
    fn uninstall_not_installed_refuses() {
        let layout = temp_layout("uninstall-absent");
        assert_eq!(
            format!("{:?}", uninstall_one(&layout, "ty")),
            format!("{:?}", ExitCode::from(1)),
            "nothing bears the name, so nothing was uninstalled"
        );
        assert!(
            !removed_programs(&layout).contains("ty"),
            "no removed-marker is minted for a program that was never here"
        );
        // Partial debris: a store tree with no shims is still this verb's to clean.
        std::fs::create_dir_all(layout.build_dir("ty", 7)).unwrap();
        assert_eq!(
            format!("{:?}", uninstall_one(&layout, "ty")),
            format!("{:?}", ExitCode::SUCCESS),
            "debris cleans as before"
        );
        assert!(
            !layout.prefix.join("store").join("ty").exists(),
            "the debris really came off"
        );
        assert!(
            removed_programs(&layout).contains("ty"),
            "a real removal still records itself for the seed lane"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// `uninstall --all` MAY ONLY REPORT WHAT IT ACTUALLY REMOVED.
    ///
    /// The sweep walks the union of the live builds and EVERY status row, and
    /// `ops::uninstall` returns Ok for a name with no store tree under the prefix. So a
    /// system copy (`system: /opt/homebrew/bin/gh`), an OS-level install obtained through
    /// another protocol (`installed via pkg: …` — the Command Line Tools), an extra
    /// nobody opted in to and an agent program still queued were all counted as removals,
    /// printed in `atpkg: removed …`, and had their rows wiped — while both real binaries
    /// stayed exactly where they were and still ran. The design says the opposite
    /// (`docs/DESIGN-which-copy-runs-2026-08-27.md` §S11): "system copies are never
    /// touched and the output says so".
    #[test]
    fn uninstall_all_removes_only_what_aterm_installed() {
        let layout = temp_layout("uninstall-all-foreign");
        make_live(&layout, "ay", 18);
        let gh = crate::state::system(std::path::Path::new("/opt/homebrew/bin/gh"), None);
        let clt = crate::state::installed_via(
            "pkg",
            std::path::Path::new("/Library/Developer/CommandLineTools"),
        );
        for (program, build, state) in [
            ("ay", Some(18), crate::state::managed(18, 41)),
            ("gh", None, gh.clone()),
            ("clt", None, clt.clone()),
            ("ty", None, crate::state::extra_not_installed("ty")),
            ("codex", None, crate::state::agent_installing()),
        ] {
            record_status(
                &layout,
                program,
                crate::ProgramStatus {
                    installed_build: build,
                    state,
                    tree_root: String::new(),
                },
                "up to date".into(),
            );
        }

        let targets = uninstall_all_targets(&layout);
        assert!(
            targets.contains("ay") && targets.contains("gh") && targets.contains("clt"),
            "precondition: the sweep walks the live build AND every row: {targets:?}"
        );
        let sweep = sweep_uninstall_targets(&layout, &targets);

        assert_eq!(
            sweep.removed,
            ["ay"],
            "only the managed copy was ever this manager's to remove"
        );
        assert_eq!(sweep.failures, 0, "and nothing failed");
        assert_eq!(
            sweep.left_alone,
            [
                "clt (installed via pkg: /Library/Developer/CommandLineTools)",
                "gh (system copy: /opt/homebrew/bin/gh)",
            ],
            "the two copies still on this machine are NAMED, never claimed as removed"
        );
        assert!(
            !program_present(&layout, "ay"),
            "the managed copy really came off"
        );

        let rows = crate::status::read(&layout)
            .expect("record present")
            .programs;
        assert_eq!(
            rows.get("gh").map(|r| r.state.as_str()),
            Some(gh.as_str()),
            "a system row survives: it is where that copy lives, and clearing it only \
             made the next pass re-discover it"
        );
        assert_eq!(
            rows.get("clt").map(|r| r.state.as_str()),
            Some(clt.as_str()),
            "…and so does the OS install's row"
        );
        assert!(
            !rows.contains_key("ay") && !rows.contains_key("ty") && !rows.contains_key("codex"),
            "a row that outlived its program still goes — it just is not a removal: {rows:?}"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// THE SPELLING RULE CANNOT REGRESS AT THE SHAPES THAT ALREADY SLIPPED. Anything a
    /// user is invited to TYPE is spelled `aterm pkg …`; the bare `atpkg` spelling in a
    /// remedy only works because of the argv0 alias, which a first-hour user has no way
    /// to know about. Source-level, because these strings are scattered across four
    /// files and every one of them drifted at least once.
    #[test]
    fn remedies_are_spelled_as_typed() {
        for (file, source) in [
            ("cli.rs", include_str!("cli.rs")),
            ("doctor.rs", include_str!("doctor.rs")),
            ("flow.rs", include_str!("flow.rs")),
            ("activate.rs", include_str!("activate.rs")),
        ] {
            // Needles assembled at runtime so this test's own source (inside
            // cli.rs's include_str!) never contains them.
            for head in ["try: ", "run `", ": "] {
                let needle = format!(
                    "{head}atpkg {}",
                    if head == ": " {
                        "install --default-set"
                    } else {
                        ""
                    }
                );
                assert!(
                    !source.contains(&needle),
                    "{file} still spells a remedy as `atpkg` ({needle:?}) — remedies say \
                     `aterm pkg`, the spelling a user can paste"
                );
            }
        }
    }

    /// ATPKG NEVER TELLS THE USER TO RESTART FOR AN APP UPDATE. The app applies a staged
    /// build to itself in-session (aterm-gui's overlap handoff: automatic within ~2 min by
    /// default, one click otherwise) and the shells keep running, so any atpkg sentence that
    /// asks for a reopen is false — `atpkg doctor` said exactly that over the live mechanism
    /// until 2026-08-30. Source-level, across every file that prints about the app; the
    /// shell twin is `tools/grep_guard.sh` B10. The two Rosetta remedies are allow-listed by
    /// their anchor: running natively instead of under translation genuinely needs a fresh
    /// launch and is not about an update.
    #[test]
    fn no_atpkg_string_prompts_an_app_restart() {
        // Needles and the allow-list anchor are assembled at runtime so this test's own
        // source (inside cli.rs's include_str!) never contains them.
        let needles: Vec<String> = [
            ("waiting for a", "restart"),
            ("restart", "aterm"),
            ("restart", "the app"),
            ("relaunch", "aterm"),
            ("reopen", "aterm"),
            ("quit and", "reopen"),
        ]
        .iter()
        .map(|(a, b)| format!("{a} {b}"))
        .collect();
        let allowed = format!("{} {} natively", "Relaunch", "aterm");
        for (file, source) in [
            ("cli.rs", include_str!("cli.rs")),
            ("doctor.rs", include_str!("doctor.rs")),
            ("flow.rs", include_str!("flow.rs")),
            ("status.rs", include_str!("status.rs")),
            ("state.rs", include_str!("state.rs")),
        ] {
            for (n, line) in source.lines().enumerate() {
                if line.contains(&allowed) {
                    continue;
                }
                let lower = line.to_lowercase();
                for needle in &needles {
                    assert!(
                        !lower.contains(needle.as_str()),
                        "{file}:{} prompts an app restart ({needle:?}): {}\n\
                         aterm applies an update in-session — in place, your shells keep \
                         running — and atpkg never asks for a reopen",
                        n + 1,
                        line.trim()
                    );
                }
            }
        }
    }

    /// A cause is one sentence: the first line, bounded, an ellipsis when cut.
    #[test]
    fn clip_cause_keeps_the_first_line_bounded() {
        assert_eq!(
            clip_cause("staging failed: io: disk full\ncurl said more"),
            "staging failed: io: disk full"
        );
        assert_eq!(clip_cause("  spaced  \n"), "spaced");
        let long = "x".repeat(300);
        let clipped = clip_cause(&long);
        assert_eq!(clipped.chars().count(), 241, "{clipped}");
        assert!(clipped.ends_with('…'));
    }

    /// Only an AGENT program's stub passes through, and only to a foreign copy on PATH
    /// outside the managed prefix: `trust` never does, and `claude` with no other copy
    /// prints its state and stops as before (2026-09-15).
    #[cfg(unix)]
    #[test]
    fn only_an_agent_program_with_a_foreign_copy_passes_through() {
        use std::os::unix::fs::PermissionsExt as _;
        let l = temp_layout("passthrough");
        let foreign = l.prefix.parent().unwrap().join("foreign-bin");
        std::fs::create_dir_all(&foreign).unwrap();
        for name in ["claude", "trust"] {
            let exe = foreign.join(name);
            std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
            std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        }
        let path = std::env::join_paths([l.agents_dir(), foreign.clone(), l.bin_dir()]).unwrap();
        assert_eq!(
            pending_passthrough(&l, "claude", Some(&path)),
            Some(foreign.join("claude")),
            "an agent program runs the user's own copy meanwhile"
        );
        assert_eq!(
            pending_passthrough(&l, "trust", Some(&path)),
            None,
            "a toolchain program's stub never passes through"
        );
        let managed_only = std::env::join_paths([l.agents_dir(), l.bin_dir()]).unwrap();
        assert_eq!(
            pending_passthrough(&l, "claude", Some(&managed_only)),
            None,
            "no foreign copy: the stub prints its state and stops"
        );
        let _ = std::fs::remove_dir_all(&foreign);
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// `atpkg repair` names each rc it edited. It used to print one "shell integration
    /// rewritten" line whether it had re-laid a block the user deleted, failed to write the
    /// rc, or found no rc at all — so re-laying a user's opt-out went unannounced. Pinned over
    /// real passes on a synthetic home: a re-laid opt-out is named with the way back out, an
    /// rc already wired gets no line, and every refusal says what happened. No line claims
    /// "rc wiring" wholesale.
    #[test]
    fn repair_names_the_rc_it_relaid_over_an_opt_out() {
        use crate::hooks::{HookPass, RcOutcome, RcWiring};
        let layout = temp_layout("repair-rc");
        let home =
            std::env::temp_dir().join(format!("atpkg-main-repair-rc-home-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&home);
        std::fs::create_dir_all(&home).unwrap();
        std::fs::set_permissions(&home, std::fs::Permissions::from_mode(0o700)).unwrap();
        let zshrc = home.join(".zshrc");
        let mine = "export FOO=1\n";
        std::fs::write(&zshrc, mine).unwrap();
        let rewritten = "repair: ~/.aterm/shell.d hooks rewritten".to_string();

        // atpkg wires ~/.zshrc once (which records it in the ledger), and the user
        // deletes the block afterwards — the opt-out, exactly as the block words it.
        let first = crate::hooks::pass_at(&layout, &home, RcWiring::HonorOptOut);
        assert_eq!(first.rc(), [(".zshrc", RcOutcome::Appended)]);
        std::fs::write(&zshrc, mine).unwrap();

        let unattended = crate::hooks::pass_at(&layout, &home, RcWiring::HonorOptOut);
        assert_eq!(unattended.rc(), [(".zshrc", RcOutcome::OptOutKept)]);
        assert_eq!(std::fs::read_to_string(&zshrc).unwrap(), mine);

        let repaired = crate::hooks::pass_at(&layout, &home, RcWiring::Rewire);
        assert_eq!(repaired.rc(), [(".zshrc", RcOutcome::RelaidOverOptOut)]);
        assert!(
            std::fs::read_to_string(&zshrc)
                .unwrap()
                .contains(crate::hooks::RC_BEGIN)
        );
        let lines = super::repair_hook_lines(&repaired);
        assert_eq!(lines.first(), Some(&rewritten), "{lines:?}");
        assert!(
            lines
                .iter()
                .any(|l| l.contains("re-laid atpkg's block in ~/.zshrc")
                    && l.contains("you had deleted it")
                    && l.contains("delete it again to opt out")),
            "the re-laid opt-out is named, with the way back out: {lines:?}"
        );
        assert!(
            !lines.iter().any(|l| l.contains("rc wiring")),
            "no wholesale claim: {lines:?}"
        );

        // A second repair finds the block in place: nothing happened to the rc, no line.
        let again = crate::hooks::pass_at(&layout, &home, RcWiring::Rewire);
        assert_eq!(again.rc(), [(".zshrc", RcOutcome::AlreadyWired)]);
        assert_eq!(super::repair_hook_lines(&again), vec![rewritten.clone()]);

        // An rc linked into a read-only directory (a home-manager store's shape): the
        // write is refused, the target is untouched, and repair says so.
        // Non-root only: root can write a 0555 directory, so the refusal never happens.
        if crate::platform::our_uid() != 0 {
            let store = home.join("ro-store");
            std::fs::create_dir_all(&store).unwrap();
            std::fs::write(store.join("zshrc"), mine).unwrap();
            std::fs::remove_file(&zshrc).unwrap();
            std::os::unix::fs::symlink(store.join("zshrc"), &zshrc).unwrap();
            std::fs::set_permissions(&store, std::fs::Permissions::from_mode(0o555)).unwrap();
            let ro = crate::hooks::pass_at(&layout, &home, RcWiring::Rewire);
            std::fs::set_permissions(&store, std::fs::Permissions::from_mode(0o755)).unwrap();
            assert_eq!(ro.rc(), [(".zshrc", RcOutcome::WriteFailed)]);
            assert_eq!(std::fs::read_to_string(store.join("zshrc")).unwrap(), mine);
            let lines = super::repair_hook_lines(&ro);
            assert!(
                lines
                    .iter()
                    .any(|l| l.contains("~/.zshrc could NOT be rewritten")),
                "{lines:?}"
            );
        }

        // No rc at all: said, not implied — and the roster is named, not summarised.
        std::fs::remove_file(&zshrc).unwrap();
        let none = crate::hooks::pass_at(&layout, &home, RcWiring::Rewire);
        assert!(none.rc().is_empty());
        assert_eq!(
            super::repair_hook_lines(&none),
            vec![
                rewritten,
                "repair: none of ~/.zshrc, ~/.bashrc, ~/.bash_profile, \
                 ~/.config/fish/config.fish exists, so no rc was wired (atpkg never \
                 creates one)"
                    .to_string()
            ]
        );

        // ~/.aterm refused (a symlink): nothing touched, and the one line says so.
        let refused = home.join("refused-home");
        std::fs::create_dir_all(refused.join("elsewhere")).unwrap();
        std::os::unix::fs::symlink(refused.join("elsewhere"), refused.join(".aterm")).unwrap();
        std::fs::write(refused.join(".zshrc"), mine).unwrap();
        let pass = crate::hooks::pass_at(&layout, &refused, RcWiring::Rewire);
        assert_eq!(pass, HookPass::NotHardened);
        let lines = super::repair_hook_lines(&pass);
        assert!(
            lines.len() == 1 && lines[0].contains("no rc was read or written"),
            "{lines:?}"
        );
        assert_eq!(
            std::fs::read_to_string(refused.join(".zshrc")).unwrap(),
            mine
        );

        let _ = std::fs::remove_dir_all(&home);
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    fn temp_layout(label: &str) -> crate::store::Layout {
        let p = std::env::temp_dir().join(format!("atpkg-main-{label}-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&p);
        std::fs::create_dir_all(&p).unwrap();
        std::fs::set_permissions(&p, std::fs::Permissions::from_mode(0o700)).unwrap();
        crate::store::Layout { prefix: p }
    }

    /// A parsed index shaped by hand: `(name, extra, system)` per program. No channels —
    /// the set-planning helpers under test read `[programs]` only.
    fn index_of(programs: &[(&str, bool, Option<&str>)]) -> crate::manifest::Index {
        crate::manifest::Index {
            schema: 2,
            index_build: 1,
            generated_at: String::new(),
            valid_until: String::new(),
            machine_id: None,
            roster_seq: None,
            programs: programs
                .iter()
                .map(|(name, extra, system)| {
                    (
                        (*name).to_string(),
                        crate::manifest::Program {
                            repo: (*name).to_string(),
                            policy: String::new(),
                            coherence_group: None,
                            extra: *extra,
                            system: system.map(str::to_string),
                            unavailable_hint: None,
                            requires: vec![],
                        },
                    )
                })
                .collect(),
            channels: vec![],
        }
    }

    /// The pass lists `bin/` once, not once per installed program: the reading half shares one
    /// [`crate::ops::BinScan`] taken at the top of [`reconcile_aliases`]. The one residual
    /// per-program listing is `prune_stale_shims`, deliberately — it deletes files on the
    /// user's `PATH`, so it reads the directory itself at the moment it decides. Pinning the
    /// cost at `1 + N`, not `1 + 2N`, catches an edit that prunes from a stale snapshot or puts
    /// the shared scan back inside the loop.
    #[test]
    fn the_alias_pass_lists_bin_once_and_per_program_only_where_it_deletes() {
        let layout = temp_layout("alias-pass-scans");
        let programs = ["pa", "pb", "pc", "pd", "pe", "pf"];
        for p in programs {
            let build = layout.build_dir(p, 7);
            std::fs::create_dir_all(build.join("bin")).unwrap();
            std::fs::write(build.join("bin").join(p), b"#!/bin/true\n").unwrap();
            crate::activate::install_shims(
                &layout,
                &build,
                std::slice::from_ref(&p.to_string()),
                crate::activate::Aliases::Alab,
            )
            .unwrap();
        }
        let rows: Vec<(&str, bool, Option<&str>)> =
            programs.iter().map(|p| (*p, false, None)).collect();
        let index = index_of(&rows);
        // Measure the steady state: the first pass lays whatever aliases are missing, the
        // second is the six-hourly tick that finds everything already right and writes
        // nothing. That second pass is the one whose reads are being counted.
        reconcile_aliases(&layout, &index);
        crate::ops::reset_bin_scans();
        reconcile_aliases(&layout, &index);
        let scans = crate::ops::bin_scans();
        assert_eq!(
            scans,
            programs.len() + 1,
            "one shared scan for the whole pass and one prune scan per program (it deletes) \
             — got {scans} for {} programs",
            programs.len()
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// The shadow pass lists `bin/` once, whatever is installed. [`reconcile_shadowed`] only
    /// reads, prints and records — nothing in it writes to or deletes from `bin/` — so its
    /// whole cost is the one photograph, with no per-program remainder to allow for.
    #[test]
    fn the_shadow_pass_lists_bin_once_for_the_whole_machine() {
        let layout = temp_layout("shadow-pass-scans");
        for p in ["qa", "qb", "qc", "qd"] {
            let build = layout.build_dir(p, 3);
            std::fs::create_dir_all(build.join("bin")).unwrap();
            std::fs::write(build.join("bin").join(p), b"#!/bin/true\n").unwrap();
            crate::activate::install_shims(
                &layout,
                &build,
                std::slice::from_ref(&p.to_string()),
                crate::activate::Aliases::Off,
            )
            .unwrap();
        }
        crate::ops::reset_bin_scans();
        let _ = reconcile_shadowed(&layout, 1, None);
        let scans = crate::ops::bin_scans();
        assert_eq!(
            scans, 1,
            "the shadow reconcile reads `bin/` once for the whole machine — got {scans}"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    fn names(v: &[&str]) -> std::collections::BTreeSet<String> {
        v.iter().map(|s| (*s).to_string()).collect()
    }

    /// EXTRAS: out of the default set until this machine opts in by name at the explicit
    /// door; the opt-in is a durable marker every later pass unions in; `uninstall` of the
    /// extra withdraws it, and a decline (`uninstall --all`) withdraws them all.
    #[test]
    fn an_extra_joins_the_wanted_set_only_by_opt_in_and_leaves_with_uninstall() {
        let layout = temp_layout("optin");
        let index = index_of(&[("ay", false, None), ("vendorx", true, None)]);
        let cfg = crate::config::PackagesConfig::default();
        assert_eq!(wanted_programs(&layout, &index, &cfg), names(&["ay"]));
        // The explicit door records the opt-in for an extra — and ONLY for an extra.
        assert!(
            !note_extra_optin(&layout, &index, "ay"),
            "not an extra: no marker"
        );
        assert!(
            !note_extra_optin(&layout, &index, "dotfiles"),
            "unlisted: no marker"
        );
        assert!(layout.optins().is_empty());
        assert!(note_extra_optin(&layout, &index, "vendorx"));
        assert!(layout.optin_exists("vendorx"));
        assert_eq!(
            wanted_programs(&layout, &index, &cfg),
            names(&["ay", "vendorx"])
        );
        // A config exclude still beats the marker.
        let narrowed = crate::config::PackagesConfig {
            exclude: Some(vec!["vendorx".to_string()]),
            ..Default::default()
        };
        assert_eq!(wanted_programs(&layout, &index, &narrowed), names(&["ay"]));
        // `uninstall vendorx` withdraws the opt-in (the store tree is the thing being removed).
        std::fs::create_dir_all(layout.build_dir("vendorx", 1)).unwrap();
        uninstall_and_retire(&layout, "vendorx").unwrap();
        assert!(!layout.optin_exists("vendorx"));
        assert_eq!(wanted_programs(&layout, &index, &cfg), names(&["ay"]));
        // A decline withdraws every opt-in at once.
        assert!(note_extra_optin(&layout, &index, "vendorx"));
        layout.clear_all_optins();
        assert!(layout.optins().is_empty());
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    // `which <program>` for a bundle whose build exposes no command of its own name
    // (`trust` → trustc/targo, `trust-mc` → trust-mc-driver) answers with the managed
    // store dir and the names it DOES put on PATH; a same-named unrelated binary on PATH
    // (Homebrew's p11-kit `trust`) is named as exactly that, after the truth — never as
    // "the system copy" of a program it is not (2026-09-10 audit).
    #[cfg(unix)]
    #[test]
    fn which_for_a_bundle_program_names_the_store_and_its_exposed_tools() {
        let layout = temp_layout("which-bundle");
        let build = layout.build_dir("tm", 20065);
        std::fs::create_dir_all(build.join("bin")).unwrap();
        for exe in ["tm-driver", "tm-compiler"] {
            std::fs::write(build.join("bin").join(exe), b"#!/bin/sh\nexit 0\n").unwrap();
            std::fs::set_permissions(
                build.join("bin").join(exe),
                std::fs::Permissions::from_mode(0o755),
            )
            .unwrap();
        }
        // Only the driver is exposed — the compiler is an internal, like the real pack.
        crate::activate::install_shims(
            &layout,
            &build,
            &["tm-driver".to_string()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        crate::activate::activate_channel(&layout, "stable", &build).unwrap();
        crate::store::mark_build_ready(&build).unwrap();
        record_status(
            &layout,
            "tm",
            crate::ProgramStatus {
                installed_build: Some(20065),
                state: crate::state::managed(20065, 21),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        // A same-named UNRELATED binary on PATH (p11-kit's `trust`).
        let brew = layout.prefix.parent().unwrap().join(format!(
            "atpkg-main-which-bundle-brew-{}",
            std::process::id()
        ));
        let _ = std::fs::remove_dir_all(&brew);
        std::fs::create_dir_all(&brew).unwrap();
        std::fs::write(brew.join("tm"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(brew.join("tm"), std::fs::Permissions::from_mode(0o755)).unwrap();
        let path = std::env::join_paths([brew.clone(), layout.bin_dir()]).unwrap();
        let line = which_line(&layout, "tm", Some(&path)).unwrap();
        let mut lines = line.lines();
        assert_eq!(
            lines.next().unwrap(),
            format!(
                "tm → {} (bundle; exposes tm-driver — run `aterm pkg which tm-driver`) — \
                 managed 20065 — pinned by index 21",
                build.display()
            )
        );
        assert_eq!(
            lines.next().unwrap(),
            format!(
                "{} on PATH is an unrelated binary, not this program",
                brew.join("tm").display()
            )
        );
        assert!(lines.next().is_none());
        // Without the impostor: one line.
        let line = which_line(&layout, "tm", Some(std::ffi::OsStr::new(""))).unwrap();
        assert_eq!(line.lines().count(), 1);
        // The exposed tool itself still answers by the ordinary shim arm.
        let line = which_line(&layout, "tm-driver", Some(std::ffi::OsStr::new(""))).unwrap();
        assert!(line.starts_with("tm-driver → "), "{line}");
        assert!(line.contains("managed 20065"), "{line}");
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&brew);
    }

    /// `atpkg which` for an AGENT PROGRAM (owner decision 2026-09-10): with `agents/`
    /// first on `PATH` the managed copy is what runs — the canonical managed line — and a
    /// SECOND line names every foreign copy it out-ranked, home-abbreviated, with the
    /// version its real location spells; nothing is executed to learn it. Without the
    /// agents dir on `PATH` (a shell the hook never reached) the same foreign copy is
    /// reported SHADOWING, as for any member.
    #[cfg(unix)]
    #[test]
    fn which_for_an_agent_program_names_the_managed_copy_and_the_out_ranked_foreign_copies() {
        let layout = temp_layout("which-agent");
        let build = layout.build_dir("claude", 2026091001);
        std::fs::create_dir_all(build.join("bin")).unwrap();
        std::fs::write(build.join("bin/claude"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(
            build.join("bin/claude"),
            std::fs::Permissions::from_mode(0o755),
        )
        .unwrap();
        crate::activate::install_shims(
            &layout,
            &build,
            &["claude".to_string()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        let claude = crate::store::ToolName::new("claude").unwrap();
        assert!(layout.agent_shim(&claude).exists(), "the twin is laid");
        record_status(
            &layout,
            "claude",
            crate::ProgramStatus {
                installed_build: Some(2026091001),
                state: crate::state::managed(2026091001, 21),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        // Two foreign copies shaped like the real machine: Anthropic's native install
        // (`~/.local/bin/claude → …/versions/2.1.267`) and a Homebrew cask
        // (`…/Caskroom/claude-code/2.1.204/claude`), reached through a symlink each.
        let root = layout.prefix.parent().unwrap().join(format!(
            "atpkg-main-which-agent-foreign-{}",
            std::process::id()
        ));
        let _ = std::fs::remove_dir_all(&root);
        let native_real = root.join("share/claude/versions/2.1.267");
        let cask_real = root.join("Caskroom/claude-code/2.1.204/claude");
        for real in [&native_real, &cask_real] {
            std::fs::create_dir_all(real.parent().unwrap()).unwrap();
            std::fs::write(real, b"#!/bin/sh\nexit 0\n").unwrap();
            std::fs::set_permissions(real, std::fs::Permissions::from_mode(0o755)).unwrap();
        }
        let local = root.join("local-bin");
        let brew = root.join("brew-bin");
        std::fs::create_dir_all(&local).unwrap();
        std::fs::create_dir_all(&brew).unwrap();
        std::os::unix::fs::symlink(&native_real, local.join("claude")).unwrap();
        std::os::unix::fs::symlink(&cask_real, brew.join("claude")).unwrap();
        let agents_first = std::env::join_paths([
            layout.agents_dir(),
            local.clone(),
            brew.clone(),
            layout.bin_dir(),
        ])
        .unwrap();
        let line = which_line(&layout, "claude", Some(&agents_first)).unwrap();
        let mut lines = line.lines();
        // (…and, since 2026-09-19, the self-update sentence closes the managed line —
        // `crate::selfupdate`: the twin answers `claude update` with the standard
        // update — ahead of the foreign-copies line.)
        assert_eq!(
            lines.next().unwrap(),
            format!(
                "claude → {} → {} — managed 2026091001 — pinned by index 21 — `claude update` \
                 here runs `aterm pkg update claude`",
                layout.shim(&claude).display(),
                crate::which(&layout, "claude").unwrap().display()
            )
        );
        assert_eq!(
            lines.next().unwrap(),
            format!(
                "foreign copies out-ranked: {} (2.1.267), {} (2.1.204)",
                local.join("claude").display(),
                brew.join("claude").display()
            )
        );
        assert_eq!(lines.next(), None);
        // No foreign copy at all: the managed line alone.
        let managed_only = std::env::join_paths([layout.agents_dir(), layout.bin_dir()]).unwrap();
        assert_eq!(
            which_line(&layout, "claude", Some(&managed_only))
                .unwrap()
                .lines()
                .count(),
            1
        );
        // The agents dir NOT on PATH (a shell from before the install): the native
        // install ahead of bin/ runs in THAT shell — and since 2026-09-16 the answer
        // says exactly that, with the in-place remedy, instead of the machine-wide
        // `SHADOWED by` every other member gets (owner, 2026-09-16: "it is telling me
        // to open a new tab. NO! all the latest and best MUST WORK IN THE SAME TAB");
        // the foreign copies are still listed — as what they are in THIS shell, on its
        // PATH, not "out-ranked" (review, 2026-09-16) — so the picture is whole.
        let no_agents = std::env::join_paths([local.clone(), layout.bin_dir()]).unwrap();
        let shadowed = which_line(&layout, "claude", Some(&no_agents)).unwrap();
        assert_eq!(
            shadowed,
            format!(
                "claude → {} — {}\nforeign copies on this shell's PATH: {} (2.1.267)",
                local.join("claude").display(),
                crate::state::agent_shadowed_in_shell(
                    2026091001,
                    &local.join("claude"),
                    &layout.agents_dir(),
                    false,
                    &shell_remedy_command(&layout),
                ),
                local.join("claude").display()
            )
        );
        assert!(
            !shadowed.contains("here runs"),
            "the twin is not what runs in that shell, so no self-update sentence: {shadowed}"
        );
        let _ = std::fs::remove_dir_all(&root);
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// The version a foreign copy's path spells: the LAST dotted-digits component, or
    /// nothing — never a guess.
    #[test]
    fn version_from_components_reads_the_last_dotted_digits_component() {
        let v = |p: &str| version_from_components(std::path::Path::new(p));
        assert_eq!(
            v("/Users//x/.local/share/claude/versions/2.1.267").as_deref(),
            Some("2.1.267")
        );
        assert_eq!(
            v("/opt/homebrew/Caskroom/claude-code/2.1.204/claude").as_deref(),
            Some("2.1.204")
        );
        assert_eq!(
            v("/opt/homebrew/Caskroom/codex/0.151.0/bin/codex").as_deref(),
            Some("0.151.0")
        );
        assert_eq!(v("/usr/local/bin/claude"), None);
        assert_eq!(
            v("/x/1.2.3/y/4.5/claude").as_deref(),
            Some("4.5"),
            "last wins"
        );
        assert_eq!(v("/x/v1.2.3/claude"), None, "digits only");
        assert_eq!(v("/x/.hidden/claude"), None, "a dot alone is not a version");
    }

    /// R3 at the CLI edge: against a scratch prefix with no `status.toml` the read-only
    /// verbs' guard prints the contract line (on stderr — it returns `true` when it did);
    /// a failed pass's record (a `*index*` error row, `updated_at` fresh) does not
    /// silence it; only [`record_success`] — the stamp the success exits of `update`
    /// and `install --default-set` write — does, after which the guard is silent.
    #[test]
    fn read_only_verbs_warn_on_stderr_until_a_pass_has_succeeded() {
        let layout = temp_layout("never-checked");
        assert!(
            warn_if_never_checked(&layout),
            "no status.toml: the line prints"
        );
        record_status(
            &layout,
            "*index*",
            crate::ProgramStatus {
                installed_build: None,
                state: "error: index unreachable".into(),
                tree_root: String::new(),
            },
            "update failed: index unreachable".into(),
        );
        assert!(
            warn_if_never_checked(&layout),
            "a failed pass's record is not a completed check"
        );
        record_success(&layout);
        assert!(!warn_if_never_checked(&layout), "stamped: silent");
        let back = crate::status::read(&layout).unwrap();
        assert!(!back.last_success_at.is_empty());
        // A later per-program write CARRIES the stamp rather than dropping it.
        record_status(
            &layout,
            "ay",
            crate::ProgramStatus {
                installed_build: Some(18),
                state: crate::state::managed(18, 41),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        assert_eq!(
            crate::status::read(&layout).unwrap().last_success_at,
            back.last_success_at,
            "record_status carries last_success_at"
        );
        assert!(!warn_if_never_checked(&layout));
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    // `machine-settings:` is the contract line the GUI parses for the pull-down row:
    // entries `; `-separated, the Spotlight count as `spotlight-noindex N dir(s)
    // migrated`, Universal Control as `universal-control disabled`; nothing printed for
    // an empty pass (the callers check).
    #[test]
    fn machine_write_failure_is_explicit_without_losing_partial_changes() {
        use crate::machine::UniversalControlOutcome;
        let spotlight = spotlight_entry(2);
        let mut entries = vec![spotlight.clone()];
        let failure =
            record_universal_control_outcome(UniversalControlOutcome::WriteFailed, &mut entries)
                .expect("a failed write must be reported even when the verb exits 0");
        assert_eq!(
            MACHINE_APPLY_FAILED_PREFIX,
            "atpkg: machine settings failed — "
        );
        assert_eq!(
            failure.strip_prefix(MACHINE_APPLY_FAILED_PREFIX),
            Some(
                "Universal Control could not be disabled for this host — the preference write did not land (`aterm pkg doctor` reads the keys)"
            )
        );
        assert_eq!(
            entries.as_slice(),
            std::slice::from_ref(&spotlight),
            "the successful action survives"
        );
        assert_eq!(
            machine_settings_line(&entries),
            "machine-settings: spotlight-noindex 2 dir(s) migrated",
            "a failed Universal Control write cannot mint its success event"
        );
        for outcome in [
            UniversalControlOutcome::NotApplicable,
            UniversalControlOutcome::AlreadyDisabled,
        ] {
            assert!(record_universal_control_outcome(outcome, &mut entries).is_none());
            assert_eq!(entries.as_slice(), std::slice::from_ref(&spotlight));
        }
        // The genuine success arm remains distinct and carries its revert.
        let success =
            record_universal_control_outcome(UniversalControlOutcome::Disabled, &mut entries)
                .unwrap();
        assert!(!success.starts_with(MACHINE_APPLY_FAILED_PREFIX));
        assert!(success.contains(crate::machine::UNIVERSAL_CONTROL_REVERT));
        assert_eq!(
            entries,
            [
                spotlight,
                crate::machine::UNIVERSAL_CONTROL_ENTRY.to_string()
            ]
        );
    }

    #[test]
    fn the_machine_settings_marker_is_byte_stable() {
        assert_eq!(MACHINE_SETTINGS_MARKER, "machine-settings: ");
        assert_eq!(spotlight_entry(73), "spotlight-noindex 73 dir(s) migrated");
        assert_eq!(
            machine_settings_line(&[
                spotlight_entry(73),
                crate::machine::UNIVERSAL_CONTROL_ENTRY.to_string()
            ]),
            "machine-settings: spotlight-noindex 73 dir(s) migrated; universal-control disabled"
        );
        assert_eq!(
            machine_settings_line(&[crate::machine::UNIVERSAL_CONTROL_ENTRY.to_string()]),
            "machine-settings: universal-control disabled"
        );
        // The automatic pass's log filter: what it changed, plus every skip the user has
        // to act on — never the routine not-a-repo / live-build / not-applicable skips
        // or already-excluded rows. A crate whose cargo config cannot be pointed is
        // skipped (nothing renamed) on EVERY pass; before 2026-09-12 that was silent
        // there forever (review of audit K11).
        {
            use crate::noindex::Applied;
            let skip = |reason: &str| Applied::Skipped {
                path: PathBuf::from("/h/p/target"),
                reason: reason.to_string(),
            };
            let printed: Vec<String> = render_applied(&[
                skip(
                    "/h/p/.cargo/config.toml could not be edited into valid TOML — point \
                     cargo at /h/p/target.noindex yourself — nothing was renamed",
                ),
                skip(
                    "/h/p/target.noindex already exists and is never merged into — remove or \
                     rename it, then re-run",
                ),
                skip(
                    "not beside a Cargo.toml — its pointer is an env var this cannot \
                     re-point; `aterm pkg noindex apply <dir>` migrates it by name",
                ),
                skip("a build holds /h/p/target/debug/.cargo-lock — retried next pass"),
                skip("not applicable"),
                skip("not applicable — Spotlight is a macOS index"),
                Applied::AlreadyExcluded(PathBuf::from("/h/q/target.noindex")),
            ])
            .into_iter()
            .filter(|l| noindex_pass_prints(l))
            .collect();
            assert_eq!(printed.len(), 2, "{printed:#?}");
            assert!(printed[0].starts_with("skipped /h/p/target — "));
            assert!(printed[0].contains("could not be edited into valid TOML"));
            assert!(printed[1].contains("already exists and is never merged into"));
        }
        // The apply renderer's words, over one of each outcome — the git-checkout line
        // is the contract spelling from the 2026-09-10 design rule.
        use crate::noindex::{Applied, ConfigNote, ExcludeNote, Pointer};
        let lines = render_applied(&[
            Applied::Migrated {
                from: PathBuf::from("/r/target"),
                to: PathBuf::from("/r/target.noindex"),
                config: ConfigNote::Written(PathBuf::from("/r/.cargo/config.toml")),
            },
            Applied::Migrated {
                from: PathBuf::from("/g/target"),
                to: PathBuf::from("/g/target.noindex"),
                config: ConfigNote::Linked {
                    link: PathBuf::from("/g/target"),
                    exclude: ExcludeNote::Added(PathBuf::from("/g/.git/info/exclude")),
                    link_exclude: ExcludeNote::AlreadyIgnored,
                },
            },
            Applied::Migrated {
                from: PathBuf::from("/h/target"),
                to: PathBuf::from("/h/target.noindex"),
                config: ConfigNote::Linked {
                    link: PathBuf::from("/h/target"),
                    exclude: ExcludeNote::AlreadyIgnored,
                    link_exclude: ExcludeNote::NotIgnored(
                        "the link shows as untracked, as the directory did".into(),
                    ),
                },
            },
            Applied::Migrated {
                from: PathBuf::from("/d/target"),
                to: PathBuf::from("/d/target.noindex"),
                config: ConfigNote::Linked {
                    link: PathBuf::from("/d/target"),
                    exclude: ExcludeNote::Added(PathBuf::from("/d/.git/info/exclude")),
                    link_exclude: ExcludeNote::Added(PathBuf::from("/d/.git/info/exclude")),
                },
            },
            Applied::AlreadyExcluded(PathBuf::from("/s/target.noindex")),
            Applied::Skipped {
                path: PathBuf::from("/t/target"),
                reason: "a build holds /t/target/debug/.cargo-lock — retried next pass".into(),
            },
        ]);
        assert_eq!(
            lines,
            vec![
                "migrated /r/target -> /r/target.noindex".to_string(),
                "  cargo: wrote [build] target-dir into /r/.cargo/config.toml".to_string(),
                "migrated /g/target -> /g/target.noindex (symlink /g/target left in place; no \
                 config edit: git checkout)"
                    .to_string(),
                "  git: /g/target.noindex added to /g/.git/info/exclude — the working tree \
                 stays clean"
                    .to_string(),
                "migrated /h/target -> /h/target.noindex (symlink /h/target left in place; no \
                 config edit: git checkout)"
                    .to_string(),
                "  git: /h/target.noindex is already ignored — the working tree stays clean"
                    .to_string(),
                "  git: /h/target is not gitignored here — the link shows as untracked, as the \
                 directory did"
                    .to_string(),
                "migrated /d/target -> /d/target.noindex (symlink /d/target left in place; no \
                 config edit: git checkout)"
                    .to_string(),
                "  git: /d/target.noindex added to /d/.git/info/exclude — the working tree \
                 stays clean"
                    .to_string(),
                "  git: /d/target added to /d/.git/info/exclude — the ignore entry is \
                 directory-only and does not match the link; the working tree stays clean"
                    .to_string(),
                "already excluded — /s/target.noindex (nothing to do)".to_string(),
                "skipped /t/target — a build holds /t/target/debug/.cargo-lock — retried next \
                 pass"
                    .to_string(),
                "4 migrated, 1 already excluded, 1 skipped".to_string(),
            ]
        );
        // Dry run: the plan names the pointer that would stand — the symlink for a git
        // checkout, the config elsewhere, nothing for a free-standing directory.
        let lines = render_applied(&[
            Applied::Planned {
                from: PathBuf::from("/g/target"),
                to: PathBuf::from("/g/target.noindex"),
                pointer: Pointer::Symlink,
            },
            Applied::Planned {
                from: PathBuf::from("/r/target"),
                to: PathBuf::from("/r/target.noindex"),
                pointer: Pointer::Config(PathBuf::from("/r/.cargo/config.toml")),
            },
            Applied::Planned {
                from: PathBuf::from("/f/free"),
                to: PathBuf::from("/f/free.noindex"),
                pointer: Pointer::None,
            },
        ]);
        assert_eq!(
            lines,
            vec![
                "would rename /g/target -> /g/target.noindex".to_string(),
                "  cargo: would leave a symlink /g/target in place; no config edit: git checkout"
                    .to_string(),
                "would rename /r/target -> /r/target.noindex".to_string(),
                "  cargo: would write [build] target-dir into /r/.cargo/config.toml".to_string(),
                "would rename /f/free -> /f/free.noindex".to_string(),
                "  cargo: no Cargo.toml beside it — nothing would be re-pointed".to_string(),
                "3 would migrate, 0 already excluded, 0 skipped (dry run)".to_string(),
            ]
        );
    }

    /// The stdout markers are a cross-crate contract (`crates/aterm-gui` parses them
    /// byte-for-byte): pin the two added 2026-09-10 beside the nine before them, and the
    /// `managed-current:` payload shape the GUI splits on `; `.
    #[test]
    fn the_shadowed_and_managed_current_markers_are_byte_stable() {
        assert_eq!(SHADOWED_MARKER, "shadowed: ");
        assert_eq!(MANAGED_CURRENT_MARKER, "managed-current: ");
        assert_eq!(LOCK_WAITING_MARKER, "lock-waiting: ");
        assert_eq!(LOCK_ACQUIRED_MARKER, "lock-acquired: ");
        assert_eq!(
            managed_current_line(&[]),
            None,
            "omitted when nothing qualifies"
        );
        assert_eq!(
            managed_current_line(&[
                ("claude".into(), Some("2.1.267".into()), 2026091001),
                ("codex".into(), Some("0.154.0".into()), 2026091001),
            ])
            .as_deref(),
            Some(
                "managed-current: claude 2.1.267 (build 2026091001); codex 0.154.0 (build 2026091001)"
            )
        );
        assert_eq!(
            managed_current_line(&[("claude".into(), None, 2026091001)]).as_deref(),
            Some("managed-current: claude (build 2026091001)"),
            "no version is no guess"
        );
    }

    /// THE ONE REMEDY (2026-09-16): where the hook file for the invoking shell exists
    /// (the parent process's, `caller_shell`; `$SHELL` only when that is no shell) it is
    /// sourced in place — `. ~/.aterm/shell.d/00-atpkg.zsh`, `source …fish`, the dot-source for
    /// pwsh — wired rc or not, and NEVER `exec $SHELL`, which inside an aterm tab drops
    /// the tab's shell integration (measured on the owner's machine: the zsh wrapper
    /// consumes `ATERM_ORIGINAL_ZDOTDIR`, so a re-exec'd zsh loads no integration; the
    /// in-place source moved `which claude` from `~/.local/bin/claude` to
    /// `<prefix>/agents/claude` with `ATPKG_AGENTS` exported). Only a shell without the
    /// hook file gets a PATH line, in its own dialect.
    #[test]
    fn the_remedy_sources_the_hook_in_place_and_never_execs_the_shell() {
        let layout = temp_layout("remedy");
        let home = std::env::temp_dir().join(format!("atpkg-remedy-home-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&home);
        let shell_d = home.join(".aterm").join("shell.d");
        std::fs::create_dir_all(&shell_d).unwrap();
        let agents = layout.agents_dir().display().to_string();
        // No hook written yet (nor an rc that could be wired): the PATH line, per dialect.
        assert_eq!(
            shell_remedy_for(Some("zsh"), Some(&home), &layout),
            format!("export PATH=\"{agents}:$PATH\"")
        );
        assert_eq!(
            shell_remedy_for(Some("fish"), Some(&home), &layout),
            format!("set -gx PATH \"{agents}\" $PATH")
        );
        assert_eq!(
            shell_remedy_for(Some("pwsh"), Some(&home), &layout),
            format!(
                "$env:PATH = \"{agents}{}$env:PATH\"",
                if cfg!(windows) { ";" } else { ":" }
            )
        );
        assert_eq!(
            shell_remedy_for(None, Some(&home), &layout),
            format!("export PATH=\"{agents}:$PATH\"")
        );
        assert_eq!(
            shell_remedy_for(Some("zsh"), None, &layout),
            format!("export PATH=\"{agents}:$PATH\"")
        );
        // The hooks written (what every pass does): the source line, and the rc's wiring
        // makes no difference — an rc block is never written here, and the remedy is the
        // same one the block itself runs.
        crate::hooks::write_hooks(&shell_d, &layout.bin_dir(), &layout.agents_dir()).unwrap();
        assert_eq!(
            crate::hooks::rc_hook_wired(&home, "zsh"),
            Some((false, "00-atpkg.zsh"))
        );
        assert_eq!(
            shell_remedy_for(Some("zsh"), Some(&home), &layout),
            ". ~/.aterm/shell.d/00-atpkg.zsh"
        );
        assert_eq!(
            shell_remedy_for(Some("bash"), Some(&home), &layout),
            ". ~/.aterm/shell.d/00-atpkg.bash"
        );
        // `sh` has no hook it can source (the bash hook is not POSIX sh): the PATH line.
        assert_eq!(
            shell_remedy_for(Some("sh"), Some(&home), &layout),
            format!("export PATH=\"{agents}:$PATH\"")
        );
        assert_eq!(
            shell_remedy_for(Some("fish"), Some(&home), &layout),
            "source ~/.aterm/shell.d/00-atpkg.fish"
        );
        assert_eq!(
            shell_remedy_for(Some("pwsh"), Some(&home), &layout),
            ". ~/.aterm/shell.d/00-atpkg.ps1"
        );
        assert_eq!(
            shell_remedy_for(Some("powershell"), Some(&home), &layout),
            ". ~/.aterm/shell.d/00-atpkg.ps1"
        );
        // A wired rc changes nothing: still the source, never `exec $SHELL`.
        std::fs::write(
            home.join(".zshrc"),
            "# >>> atpkg shell integration >>>\n. ~/.aterm/shell.d/00-atpkg.zsh\n# <<< atpkg shell integration <<<\n",
        )
        .unwrap();
        assert_eq!(
            crate::hooks::rc_hook_wired(&home, "zsh"),
            Some((true, "00-atpkg.zsh"))
        );
        assert_eq!(
            shell_remedy_for(Some("zsh"), Some(&home), &layout),
            ". ~/.aterm/shell.d/00-atpkg.zsh"
        );
        // A shell atpkg has no hook for: the PATH line.
        assert_eq!(
            shell_remedy_for(Some("nu"), Some(&home), &layout),
            format!("export PATH=\"{agents}:$PATH\"")
        );
        let _ = std::fs::remove_dir_all(&home);
    }

    /// `atpkg which` (design S6) and the SHADOW reconcile (design S5), over a real store:
    /// a managed shim answers `managed <build> — pinned by index <N>` with the shim → store
    /// resolution; a foreign copy AHEAD of the managed bin/ turns that into `SHADOWED by
    /// <path>` (recorded by the pass, never fixed); a foreign copy with NO managed shim is
    /// the `system:` state; a consent stub is the `extra` state; nothing is the fix line.
    #[cfg(unix)]
    #[test]
    fn which_names_the_copy_that_runs_and_the_pass_records_shadowing() {
        let layout = temp_layout("which");
        let build = layout.build_dir("trust", 6808);
        std::fs::create_dir_all(build.join("bin")).unwrap();
        std::fs::write(build.join("bin/trust"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(
            build.join("bin/trust"),
            std::fs::Permissions::from_mode(0o755),
        )
        .unwrap();
        crate::activate::install_shims(
            &layout,
            &build,
            &["trust".to_string()],
            crate::activate::Aliases::Alab,
        )
        .unwrap();
        record_status(
            &layout,
            "trust",
            crate::ProgramStatus {
                installed_build: Some(6808),
                state: crate::state::managed(6808, 41),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        let managed_only = std::env::join_paths([layout.bin_dir()]).unwrap();
        assert_eq!(
            which_line(&layout, "trust", Some(&managed_only)).unwrap(),
            format!(
                "trust → {} → {} — managed 6808 — pinned by index 41",
                layout
                    .shim(&crate::store::ToolName::new("trust").unwrap())
                    .display(),
                crate::which(&layout, "trust").unwrap().display()
            )
        );
        // A foreign copy AHEAD of the managed bin/.
        let local = layout
            .prefix
            .parent()
            .unwrap()
            .join(format!("atpkg-main-which-local-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&local);
        std::fs::create_dir_all(&local).unwrap();
        for name in ["trust", "gh"] {
            let exe = local.join(name);
            std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
            std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        }
        let ahead = std::env::join_paths([local.clone(), layout.bin_dir()]).unwrap();
        let shadowed = crate::state::shadowed(6808, &local.join("trust"));
        // trust is ALab's own, so its alias is laid: the answer carries the ONE trailing
        // fix-line after the canonical state (the p11-kit `trust` story, §17.11).
        assert_eq!(
            which_line(&layout, "trust", Some(&ahead)).unwrap(),
            format!(
                "trust → {} — {shadowed} — type alab-trust for the managed one",
                local.join("trust").display()
            )
        );
        assert_eq!(
            alias_fix(
                &layout,
                &crate::store::ToolName::new("trust").unwrap(),
                "trust",
                Some(&ahead)
            )
            .as_deref(),
            Some("type alab-trust for the managed one"),
            "the pass log composes the same sentence after the same state"
        );
        // A foreign `alab-trust` AHEAD of the managed bin/ too: the alias would not run
        // the managed copy either, so the fix-line is withheld (never a promise that does
        // not answer) and `which alab-trust` reports the alias as SHADOWED itself.
        {
            let foreign_alias = local.join("alab-trust");
            std::fs::write(&foreign_alias, b"#!/bin/sh\nexit 0\n").unwrap();
            std::fs::set_permissions(&foreign_alias, std::fs::Permissions::from_mode(0o755))
                .unwrap();
            assert_eq!(
                alias_fix(
                    &layout,
                    &crate::store::ToolName::new("trust").unwrap(),
                    "trust",
                    Some(&ahead)
                ),
                None,
                "no fix-line when the alias is shadowed too"
            );
            assert_eq!(
                which_line(&layout, "trust", Some(&ahead)).unwrap(),
                format!("trust → {} — {shadowed}", local.join("trust").display())
            );
            assert_eq!(
                which_line(&layout, "alab-trust", Some(&ahead)).unwrap(),
                format!(
                    "alab-trust → {} — {}",
                    foreign_alias.display(),
                    crate::state::shadowed(6808, &foreign_alias)
                )
            );
            std::fs::remove_file(&foreign_alias).unwrap();
        }
        // The containment is ANCHORED: an `alab-<x>` that resolves into a `store/<x>/…`
        // tail OUTSIDE this prefix (a hand-made link into a checkout) earns no fix-line.
        {
            let checkout = local.join("store").join("trust").join("1").join("bin");
            std::fs::create_dir_all(&checkout).unwrap();
            std::fs::write(checkout.join("trust"), b"#!/bin/sh\nexit 0\n").unwrap();
            let alias_shim = layout.shim(&crate::store::ToolName::new("alab-trust").unwrap());
            let managed_target = crate::platform::resolve_shim(&alias_shim).unwrap();
            std::fs::remove_file(&alias_shim).unwrap();
            std::os::unix::fs::symlink(checkout.join("trust"), &alias_shim).unwrap();
            assert_eq!(
                alias_fix(
                    &layout,
                    &crate::store::ToolName::new("trust").unwrap(),
                    "trust",
                    Some(&ahead)
                ),
                None,
                "a link outside the prefix is not the managed copy"
            );
            std::fs::remove_file(&alias_shim).unwrap();
            std::os::unix::fs::symlink(&managed_target, &alias_shim).unwrap();
        }
        // The alias itself resolves the managed copy, in the same canonical words as the
        // plain name — whatever PATH holds ahead of the managed bin/.
        let alias_shim = layout
            .shim(&crate::store::ToolName::new("alab-trust").unwrap())
            .display()
            .to_string();
        assert_eq!(
            which_line(&layout, "alab-trust", Some(&ahead)).unwrap(),
            format!(
                "alab-trust → {alias_shim} → {} — managed 6808 — pinned by index 41",
                crate::which(&layout, "trust").unwrap().display()
            )
        );
        // The pass records it — the CANONICAL row, never the fix-line — and un-records
        // it when PATH changes back.
        assert_eq!(
            reconcile_shadowed(&layout, 41, Some(&ahead)),
            vec!["trust".to_string()]
        );
        assert_eq!(
            crate::status::read(&layout).unwrap().programs["trust"].state,
            shadowed
        );
        assert!(
            !crate::status::read(&layout).unwrap().programs["trust"]
                .state
                .contains("alab-"),
            "the recorded state is exactly the canonical spelling"
        );
        assert!(
            crate::which(&layout, "trust").is_some() && local.join("trust").exists(),
            "SHADOWED is said, never fixed: both copies are untouched"
        );
        assert!(reconcile_shadowed(&layout, 41, Some(&managed_only)).is_empty());
        assert_eq!(
            crate::status::read(&layout).unwrap().programs["trust"].state,
            crate::state::managed(6808, 41)
        );
        // Behind the managed bin/ the foreign copy does not run: no shadow.
        let behind = std::env::join_paths([layout.bin_dir(), local.clone()]).unwrap();
        assert!(reconcile_shadowed(&layout, 41, Some(&behind)).is_empty());
        // AN AGENT PROGRAM (2026-09-16). Its `agents/` twin is laid and current, so a
        // foreign copy ahead on THIS PATH is one shell's state: `which` names it as
        // such with the in-place remedy, the pass records the managed row (never
        // SHADOWED), names it on no `shadowed:` marker, and the out-ranked copies are
        // still listed. With `agents/` first, the managed answer as before.
        let cbuild = layout.build_dir("claude", 2_026_091_601);
        std::fs::create_dir_all(cbuild.join("bin")).unwrap();
        std::fs::write(cbuild.join("bin/claude"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(
            cbuild.join("bin/claude"),
            std::fs::Permissions::from_mode(0o755),
        )
        .unwrap();
        crate::activate::install_shims(
            &layout,
            &cbuild,
            &["claude".to_string()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        crate::store::mark_build_ready(&cbuild).unwrap();
        assert!(agent_twin_current(&layout, "claude", 2_026_091_601));
        assert!(
            !agent_twin_current(&layout, "claude", 2_026_091_700),
            "not that build"
        );
        assert!(
            !agent_twin_current(&layout, "trust", 6808),
            "no twin for an ALab tool"
        );
        let exe = local.join("claude");
        std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        let no_agents = std::env::join_paths([local.clone(), layout.bin_dir()]).unwrap();
        let remedy = shell_remedy_command(&layout);
        let expect_absent = crate::state::agent_shadowed_in_shell(
            2_026_091_601,
            &exe,
            &layout.agents_dir(),
            false,
            &remedy,
        );
        let line = which_line(&layout, "claude", Some(&no_agents)).unwrap();
        assert_eq!(
            line,
            format!(
                "claude → {} — {expect_absent}\n{}",
                exe.display(),
                foreign_copies_line_with(FOREIGN_ON_THIS_SHELL, std::slice::from_ref(&exe))
            )
        );
        assert!(
            line.contains(&format!("`{remedy}` here picks the managed copy up")),
            "{line}"
        );
        assert!(
            remedy.contains("~/.aterm/shell.d/00-atpkg.")
                || remedy.starts_with("export PATH=\"")
                || remedy.starts_with("set -gx PATH ")
                || remedy.starts_with("$env:PATH = "),
            "the hook source, or a PATH line in the shell's dialect: {remedy}"
        );
        assert!(
            !remedy.starts_with("exec "),
            "never `exec $SHELL` — it drops the tab's shell integration (2026-09-16): {remedy}"
        );
        assert!(
            !line.contains("new tab") && !line.contains("remove or reorder"),
            "{line}"
        );
        assert!(
            !line.contains("out-ranked"),
            "the running copy is out-ranked by nothing here: {line}"
        );
        let agents_behind =
            std::env::join_paths([local.clone(), layout.bin_dir(), layout.agents_dir()]).unwrap();
        assert!(
            which_line(&layout, "claude", Some(&agents_behind))
                .unwrap()
                .contains(&crate::state::agent_shadowed_in_shell(
                    2_026_091_601,
                    &exe,
                    &layout.agents_dir(),
                    true,
                    &remedy,
                )),
            "agents/ behind the foreign copy is named as such"
        );
        let agents_first =
            std::env::join_paths([layout.agents_dir(), local.clone(), layout.bin_dir()]).unwrap();
        let managed = which_line(&layout, "claude", Some(&agents_first)).unwrap();
        assert!(
            managed.starts_with(&format!(
                "claude → {} → ",
                layout
                    .shim(&crate::store::ToolName::new("claude").unwrap())
                    .display()
            )) && managed.contains("managed 2026091601"),
            "agents/ first: the managed copy runs: {managed}"
        );
        // The pass: nothing shadowed, the managed row recorded, the tail printed.
        assert!(
            !reconcile_shadowed(&layout, 41, Some(&no_agents)).contains(&"claude".to_string()),
            "an agent program with a current twin is never on the shadowed marker"
        );
        assert!(
            !crate::status::read(&layout)
                .and_then(|s| s.programs.get("claude").cloned())
                .is_some_and(|r| crate::state::shadowed_by(&r.state).is_some()),
            "never recorded as SHADOWED"
        );
        // …and a row an OLDER client recorded as SHADOWED is put back to managed.
        record_status(
            &layout,
            "claude",
            crate::ProgramStatus {
                installed_build: Some(2_026_091_601),
                state: crate::state::shadowed(2_026_091_601, &exe),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        assert!(!reconcile_shadowed(&layout, 41, Some(&no_agents)).contains(&"claude".to_string()));
        assert_eq!(
            crate::status::read(&layout).unwrap().programs["claude"].state,
            crate::state::managed(2_026_091_601, 41)
        );
        // With the twin GONE (a lane that could not lay it), the plain SHADOWED row is
        // the truth again: nothing in front of PATH vouches for the managed copy.
        std::fs::remove_file(layout.agent_shim(&crate::store::ToolName::new("claude").unwrap()))
            .unwrap();
        assert!(!agent_twin_current(&layout, "claude", 2_026_091_601));
        assert!(
            reconcile_shadowed(&layout, 41, Some(&no_agents)).contains(&"claude".to_string()),
            "no twin: on the shadowed marker like any program"
        );
        assert_eq!(
            crate::status::read(&layout).unwrap().programs["claude"].state,
            crate::state::shadowed(2_026_091_601, &exe)
        );
        crate::activate::reconcile_agents(&layout);
        // A foreign copy with no managed shim: the system state, dated when a marker says so.
        assert_eq!(
            which_line(&layout, "gh", Some(&ahead)).unwrap(),
            format!(
                "gh → {} — {}",
                local.join("gh").display(),
                crate::state::system(&local.join("gh"), None)
            )
        );
        layout.record_retired("gh", "2026-08-27").unwrap();
        assert_eq!(layout.retired_date("gh").as_deref(), Some("2026-08-27"));
        assert!(
            which_line(&layout, "gh", Some(&ahead))
                .unwrap()
                .ends_with("(managed copy retired 2026-08-27)")
        );
        layout.clear_retired("gh");
        assert_eq!(layout.retired_date("gh"), None);
        // A consent stub for an extra: the opt-in spelling.
        crate::stub::write_pending_stub_kind(
            &layout,
            &crate::store::ToolName::new("emacs").unwrap(),
            crate::stub::StubKind::Extra,
        )
        .unwrap();
        let line = which_line(&layout, "emacs", Some(&managed_only)).unwrap();
        assert!(line.starts_with("emacs → "), "{line}");
        assert!(line.contains("(consent stub)"), "{line}");
        assert!(
            line.ends_with(&crate::state::extra_not_installed("emacs")),
            "{line}"
        );
        // An extra that is BOTH a consent stub and shadowed by a foreign copy: PATH
        // order decides what runs. Foreign copy ahead of the managed bin/ ⇒ it runs and
        // atpkg does not manage it; behind it ⇒ the stub runs and asks.
        let exe = local.join("emacs");
        std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        assert_eq!(
            which_line(&layout, "emacs", Some(&ahead)).unwrap(),
            format!(
                "emacs → {} — {}",
                exe.display(),
                crate::state::system(&exe, None)
            )
        );
        let stub_first = std::env::join_paths([layout.bin_dir(), local.clone()]).unwrap();
        let line = which_line(&layout, "emacs", Some(&stub_first)).unwrap();
        assert!(line.contains("(consent stub)"), "{line}");
        assert!(
            line.ends_with(&crate::state::extra_not_installed("emacs")),
            "{line}"
        );
        // Opted in and installed, the extra is a managed member like any other: the
        // foreign copy ahead reads SHADOWED.
        let cbuild = layout.build_dir("emacs", 7);
        std::fs::create_dir_all(cbuild.join("bin")).unwrap();
        std::fs::write(cbuild.join("bin/emacs"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(
            cbuild.join("bin/emacs"),
            std::fs::Permissions::from_mode(0o755),
        )
        .unwrap();
        crate::activate::install_shims(
            &layout,
            &cbuild,
            &["emacs".to_string()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        assert_eq!(
            which_line(&layout, "emacs", Some(&ahead)).unwrap(),
            format!(
                "emacs → {} — {}",
                exe.display(),
                crate::state::shadowed(7, &exe)
            )
        );
        let _ = std::fs::remove_file(&exe);
        // An EXTRA-kind stub under an AGENT PROGRAM's name is stale, never a consent
        // stub: the agent programs are default-set members the pass installs unasked
        // (owner decision 2026-09-10; the reconcile files them default-set whatever
        // set the index put them in), and the line says so (audit 2026-09-14).
        crate::stub::write_pending_stub_kind(
            &layout,
            &crate::store::ToolName::new("codex").unwrap(),
            crate::stub::StubKind::Extra,
        )
        .unwrap();
        let line = which_line(&layout, "codex", Some(&managed_only)).unwrap();
        assert!(line.contains("(pending stub)"), "{line}");
        assert!(!line.contains("consent"), "{line}");
        assert!(line.contains("the next pass installs it"), "{line}");
        let _ = std::fs::remove_file(layout.shim(&crate::store::ToolName::new("codex").unwrap()));
        // A recorded non-shim state reads back verbatim.
        record_status(
            &layout,
            "homebrew",
            crate::ProgramStatus {
                installed_build: None,
                state: crate::state::needs_admin("homebrew"),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        assert_eq!(
            which_line(&layout, "homebrew", Some(&managed_only)).unwrap(),
            format!(
                "homebrew → (nothing runs) — {}",
                crate::state::needs_admin("homebrew")
            )
        );
        // Nothing: the fix line, as an error.
        assert_eq!(
            which_line(&layout, "nothing", Some(&managed_only)).unwrap_err(),
            not_installed_fix("nothing")
        );
        // The REVERSE case, honestly: an `alab-<x>` nothing answers to says why there is
        // no alias instead of promising `aterm pkg install alab-x`. emacs is installed
        // above with the policy Off (a vendor extra); with no cached index the line can
        // only say the tool is installed and not aliased. An unknown base gets the plain
        // name's fix line.
        let err = which_line(&layout, "alab-emacs", Some(&managed_only)).unwrap_err();
        assert!(err.starts_with("atpkg: no alias alab-emacs — "), "{err}");
        assert!(err.ends_with("type emacs"), "{err}");
        assert_eq!(
            which_line(&layout, "alab-nothing", Some(&managed_only)).unwrap_err(),
            format!(
                "atpkg: no alias alab-nothing — {}",
                not_installed_fix("nothing")
            )
        );
        // The cached-index arms, driven directly: a system-satisfiable member and an
        // extra never alias; an ALab tool that is not installed gets its own fix line.
        let index = index_of(&[
            ("ay", false, None),
            ("gh", false, Some("gh")),
            ("codex", true, None),
        ]);
        assert_eq!(
            no_alias_line_for(&layout, "alab-gh", "gh", index.program("gh")),
            "atpkg: no alias alab-gh — gh is not one of ALab's own tools (a system copy may \
             satisfy it: system = \"gh\"); type gh"
        );
        assert_eq!(
            no_alias_line_for(&layout, "alab-codex", "codex", index.program("codex")),
            "atpkg: no alias alab-codex — codex is not one of ALab's own tools (a vendor's); \
             type codex"
        );
        assert_eq!(
            no_alias_line_for(&layout, "alab-ay", "ay", index.program("ay")),
            format!(
                "atpkg: alab-ay names nothing yet — {}",
                not_installed_fix("ay")
            )
        );
        assert!(which_line(&layout, "alab-sudo", Some(&managed_only)).is_err());
        assert!(which_line(&layout, "../x", Some(&managed_only)).is_err());
        let _ = std::fs::remove_dir_all(&local);
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// `aterm pkg which` for a REROUTED upstream name (design S6, philosophy §4): the
    /// answer is the row — the branded command(s) with `<args>`, the escape variable —
    /// and the stub's state, for a name the deny-list refuses (`cargo`) and one it admits
    /// (`rustfmt`) alike; a foreign occupant is named, never claimed; every other name
    /// keeps its behaviour (`rustup` is not a row and stays refused as a tool name).
    #[cfg(unix)]
    #[test]
    fn which_answers_a_rerouted_name_from_the_table() {
        let layout = temp_layout("which-reroute");
        let empty = std::ffi::OsString::new();
        // Before any stub is laid the row still answers, and says so.
        let line = which_line(&layout, "cargo", Some(&empty)).unwrap();
        assert!(line.contains("(reroute stub, missing)"), "{line}");
        crate::reroute::lay(&layout).unwrap();
        let cargo = which_line(&layout, "cargo", Some(&empty)).unwrap();
        assert_eq!(cargo.lines().count(), 1, "{cargo}");
        assert!(
            cargo.starts_with(&format!(
                "cargo → {} (reroute stub, laid) — rerouted inside aterm sessions: announced, \
                 naming ",
                crate::reroute::stub_path(&layout, "cargo").display()
            )),
            "{cargo}"
        );
        for needle in [
            "'targo trust <args>' / 'targo --unverified <args>'",
            "'cargo clippy' → 'targo tippy'",
            // The 2026-09-08 ruling, on the "which copy runs and why" surface:
            // a SIGNPOST row is not a refusal and must not read as one here.
            ", then run upstream ",
            "(ATERM_NO_REROUTE=1 restores upstream 'cargo'.)",
        ] {
            assert!(!cargo.contains("refused"), "{cargo}");
            assert!(cargo.contains(needle), "{needle:?} missing from {cargo}");
        }
        let rustfmt = which_line(&layout, "rustfmt", Some(&empty)).unwrap();
        assert!(rustfmt.contains("runs 'trustfmt <args>'"), "{rustfmt}");
        assert!(
            rustfmt.contains("ATERM_NO_REROUTE=1 restores upstream 'rustfmt'"),
            "{rustfmt}"
        );
        // A foreign occupant of a row's name: named as such, never claimed as ours.
        std::fs::write(
            crate::reroute::stub_path(&layout, "z3"),
            "#!/bin/sh\nexit 0\n",
        )
        .unwrap();
        let z3 = which_line(&layout, "z3", Some(&empty)).unwrap();
        assert!(z3.contains("(reroute stub, foreign)"), "{z3}");
        assert!(
            z3.contains("ATERM_Z3_IS_ORACLE=1 reaches the real z3"),
            "{z3}"
        );
        // Not a row: the deny-list still answers.
        assert_eq!(
            which_line(&layout, "rustup", Some(&empty)).unwrap_err(),
            "atpkg: \"rustup\" is not a tool name"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A dev-linked program whose own name is not a tool: `which trust` names the checkout
    /// and the tools it exposes — and still names p11-kit's `trust` as unrelated.
    #[test]
    fn which_names_a_dev_linked_program_by_its_checkout_and_exposes() {
        let bins = vec!["trustc".to_string(), "targo".to_string()];
        let line = dev_linked_program_line(
            "trust",
            Some(std::path::Path::new("/h/trust/build/host/stage2")),
            &bins,
            Some(std::path::Path::new("/usr/bin/trust")),
        );
        assert!(line.starts_with("trust → /h/trust/build/host/stage2 (dev-linked; exposes targo, trustc — run `aterm pkg which targo`)"), "{line}");
        assert!(
            line.contains("`aterm pkg unlink trust` returns it to the index"),
            "{line}"
        );
        assert!(
            line.contains("/usr/bin/trust on PATH is an unrelated binary"),
            "{line}"
        );
        assert!(
            !line.contains("not installed"),
            "a linked program is not 'not installed': {line}"
        );
        let bare = dev_linked_program_line("trust", None, &[], None);
        assert!(
            bare.contains("exposes no command of its own") && !bare.contains('\n'),
            "{bare}"
        );
    }

    /// The p11-kit collision (2026-09-14, Linux): `which trust` answered
    /// `trust → /usr/bin/trust — system: /usr/bin/trust — not managed by aterm`,
    /// presenting a certificate-store tool as the Trust compiler, with no word that the
    /// program is unserved on this target. The truth leads, the foreign binary is named
    /// for what it is, and the phrase that misled — "system" as in "the system copy of
    /// this program" — is gone.
    #[test]
    fn an_uninstalled_index_program_names_a_same_named_binary_as_unrelated() {
        let path = std::path::Path::new("/usr/bin/trust");
        let unserved =
            "unavailable on x86_64-unknown-linux-gnu: no build is published for this target";
        let line = index_program_shadowed_by_unrelated_line("trust", Some(unserved), path);
        assert!(
            line.starts_with("trust → (nothing runs) — unavailable on x86_64"),
            "{line}"
        );
        assert!(
            line.contains("/usr/bin/trust on PATH is an unrelated binary, not this program"),
            "{line}"
        );
        assert!(
            !line.contains("system:"),
            "the misleading 'system copy' phrasing must not survive: {line}"
        );

        // No recorded row: plain not-installed, with the fix — still never "system".
        let line = index_program_shadowed_by_unrelated_line("trust", None, path);
        assert!(
            line.contains("not installed (fix: aterm pkg install trust)"),
            "{line}"
        );
        assert!(line.contains("unrelated binary"), "{line}");
        assert!(!line.contains("system:"), "{line}");

        // A row that does not explain absence (e.g. a managed pin) gets the plain answer too.
        let line = index_program_shadowed_by_unrelated_line(
            "trust",
            Some("managed 8595 — index 30"),
            path,
        );
        assert!(line.contains("not installed"), "{line}");
    }

    /// THE SELF-UPDATE VERB (2026-09-19, `crate::selfupdate`), in-process, every ending
    /// that is not the child: usage exit 2 (a `--help` operand too — non-mutating); not a
    /// tool name exit 2; a program with no row forwards to the `bin/` shim verbatim with
    /// nothing printed (a twin laid by a newer roster must still run the tool); the
    /// prefix cross-check refuses with the exact line when the store atpkg resolves here
    /// differs from the operand, and proceeds when it agrees or cannot resolve at all;
    /// `install stable`, `install 2.1.200` and `update --foo` are declined with the exact
    /// lines and the fake shim never runs — and there is no escape hatch: the first cut's
    /// environment knob was removed the same day at the owner's direction, so a declined
    /// shape is declined whatever the environment says, and the plan has no input that
    /// could say otherwise; `update --help` prints the help note and forwards verbatim;
    /// the disabled manager refuses with its line, exit 1, nothing run; the hidden verb
    /// is in no roster and takes no lock — and the store lock file never comes into
    /// being for any of them. Then THE 75 ENDING (§8), in-process against a lock THIS
    /// test holds: `selfupdate_check` with a ONE-SECOND bound — the parameter that
    /// replaced the wait-bound env knob — runs the REAL dev `atpkg` (the bin beside this
    /// test executable, through a wrapper named `atpkg` that confines it to a temp HOME
    /// and captures its pipes) as `update claude --wait-lock 1`; the child waits its
    /// bound at the held lock and exits 75 with its lock sentence on stderr and its
    /// `seed-busy:` marker on stdout, and the verb relays `CONTENDED_EXIT` after a
    /// contended line that renders the bound as `1 s`. The forward endings are pinned on
    /// the PLAN — the command the verb would `exec`, run here as a child so its argv is
    /// observed on a log — because an `exec` would replace the test process; the
    /// enablement is the plan's input (production reads `manager_enabled()`) and the
    /// child's confinement rides the wrapper, because `std::env::set_var` is a data race
    /// in a threaded test binary (the rule `install.rs` records).
    #[cfg(unix)]
    #[test]
    fn cmd_selfupdate_refuses_forwards_and_never_takes_the_lock() {
        use crate::selfupdate::{self, Decline};
        let layout = temp_layout("selfupdate");
        let other = temp_layout("selfupdate-other");
        let claude = crate::store::ToolName::new("claude").unwrap();
        let ay = crate::store::ToolName::new("ay").unwrap();
        let row = selfupdate::row_for("claude").unwrap();
        let log = layout.prefix.join("fake.log");
        // Fake `bin/` shims that record their argv, one per line, and exit 0.
        std::fs::create_dir_all(layout.bin_dir()).unwrap();
        for tool in [&claude, &ay] {
            let shim = layout.shim(tool);
            let mut body = String::from("#!/bin/sh\nprintf '%s\\n' \"$@\" >> '");
            body.push_str(&log.display().to_string());
            body.push_str("'\nexit 0\n");
            std::fs::write(&shim, body).unwrap();
            std::fs::set_permissions(&shim, std::fs::Permissions::from_mode(0o755)).unwrap();
        }
        let args = |v: &[&str]| v.iter().map(|s| (*s).to_string()).collect::<Vec<_>>();
        let logged = || {
            let text = std::fs::read_to_string(&log).unwrap_or_default();
            let _ = std::fs::remove_file(&log);
            text
        };
        // Runs a planned forward as a child (the verb would `exec` it) and answers its
        // note and the argv the fake shim recorded.
        let forwarded = |plan: SelfUpdatePlan| -> (Option<String>, String) {
            match plan {
                SelfUpdatePlan::Forward { mut command, note } => {
                    assert!(
                        command
                            .get_program()
                            .to_string_lossy()
                            .ends_with("/bin/claude")
                            || command.get_program().to_string_lossy().ends_with("/bin/ay"),
                        "the bin/ shim, never the twin: {command:?}"
                    );
                    let out = command.output().unwrap();
                    assert!(out.status.success(), "{out:?}");
                    (note, logged())
                }
                other => panic!("expected a forward, got {other:?}"),
            }
        };
        // 1. Usage and the tool-name gate, through the verb itself (nothing runs).
        assert_eq!(cmd_selfupdate(&[]), ExitCode::from(2));
        assert_eq!(cmd_selfupdate(&args(&["--help"])), ExitCode::from(2));
        assert_eq!(
            main_entry(vec!["__selfupdate".into(), "--help".into()]),
            ExitCode::from(2),
            "dispatched, and non-mutating"
        );
        assert_eq!(
            cmd_selfupdate(&args(&["sudo", "--", "update"])),
            ExitCode::from(2),
            "a sensitive name is not a tool name"
        );
        assert_eq!(
            cmd_selfupdate(&args(&["", "--", "update"])),
            ExitCode::from(2)
        );
        // 2. No row: forwards verbatim, silent.
        let (note, argv) = forwarded(selfupdate_plan(
            &layout,
            &ay,
            &args(&["update", "--flag"]),
            Some(&layout),
            true,
        ));
        assert_eq!(note, None);
        assert_eq!(argv, "update\n--flag\n");
        // 3. The prefix cross-check.
        match selfupdate_plan(&layout, &claude, &args(&["update"]), Some(&other), true) {
            SelfUpdatePlan::Refuse(line) => assert_eq!(
                line,
                selfupdate::prefix_mismatch_line(row, &layout.prefix, &other.prefix)
            ),
            other => panic!("{other:?}"),
        }
        for resolved in [Some(&layout), None] {
            match selfupdate_plan(&layout, &claude, &args(&["update"]), resolved, true) {
                SelfUpdatePlan::Check { row: r, words } => {
                    assert_eq!(r.program, "claude");
                    assert_eq!(words, "update");
                }
                other => panic!("{resolved:?}: {other:?}"),
            }
        }
        // 4. Declined shapes: the exact line, and the fake never runs. No input of the
        // plan can turn a decline into a forward — the escape hatch is gone.
        for (typed, decline) in [
            (
                &["install", "stable"][..],
                Decline::NotAChannel {
                    verb: "install",
                    target: "stable",
                },
            ),
            (
                &["install", "2.1.200"],
                Decline::Version {
                    verb: "install",
                    target: "2.1.200",
                },
            ),
        ] {
            for enabled in [true, false] {
                match selfupdate_plan(&layout, &claude, &args(typed), Some(&layout), enabled) {
                    SelfUpdatePlan::Refuse(line) => {
                        assert_eq!(
                            line,
                            selfupdate::declined_line(row, &decline),
                            "{typed:?} enabled={enabled}"
                        );
                    }
                    other => panic!("{typed:?} enabled={enabled}: {other:?}"),
                }
            }
        }
        let rest = args(&["--foo"]);
        match selfupdate_plan(
            &layout,
            &claude,
            &args(&["update", "--foo"]),
            Some(&layout),
            true,
        ) {
            SelfUpdatePlan::Refuse(line) => assert_eq!(
                line,
                selfupdate::declined_line(
                    row,
                    &Decline::Shape {
                        verb: "update",
                        rest: &rest
                    }
                )
            ),
            other => panic!("{other:?}"),
        }
        assert!(!log.exists(), "a declined shape runs nothing");
        // 5. Help: the note, then the vendor's own help, verbatim — enabled or not.
        for enabled in [true, false] {
            let (note, argv) = forwarded(selfupdate_plan(
                &layout,
                &claude,
                &args(&["update", "--help"]),
                Some(&layout),
                enabled,
            ));
            assert_eq!(
                note.as_deref(),
                Some(selfupdate::help_line(row).as_str()),
                "enabled={enabled}"
            );
            assert_eq!(argv, "update\n--help\n", "enabled={enabled}");
        }
        // A pass-through shape: forwards silently.
        let (note, argv) = forwarded(selfupdate_plan(
            &layout,
            &claude,
            &args(&["--bare", "update"]),
            Some(&layout),
            true,
        ));
        assert_eq!(note, None);
        assert_eq!(argv, "--bare\nupdate\n");
        // 6. The manager disabled: its line, exit 1, nothing run (no active build in this
        // layout — the fake shim names no store target — so the line says so), for
        // every check shape.
        for typed in [
            &["update"][..],
            &["upgrade"],
            &["install"],
            &["install", "latest", "--force"],
        ] {
            match selfupdate_plan(&layout, &claude, &args(typed), Some(&layout), false) {
                SelfUpdatePlan::Disabled(line) => {
                    assert_eq!(line, selfupdate::disabled_line(row, None), "{typed:?}");
                }
                other => panic!("{typed:?}: {other:?}"),
            }
        }
        assert!(!log.exists(), "nothing ran under a disabled manager");
        // 7. The verb is machinery, not vocabulary, and takes no lock of its own.
        assert!(!dispatch_roster().contains(&selfupdate::HIDDEN_VERB));
        assert!(!verb_mutates_store(selfupdate::HIDDEN_VERB));
        assert_eq!(usage_of(selfupdate::HIDDEN_VERB), None);
        assert!(
            !layout.store_lock().exists() && !other.store_lock().exists(),
            "no ending above touches the store lock — only the child does"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&other.prefix);
        // 8. THE 75 ENDING, against a lock THIS test holds. The child is the real dev
        // `atpkg`, run through a wrapper named `atpkg` (so `child_argv` hands it the argv
        // verbatim) that records that argv, pins HOME to a temp directory — the default
        // prefix under that HOME is the layout whose lock is held here, and the registry
        // is a local directory, so no real store and no network — and captures the
        // child's two pipes to files this lane reads back. A one-second bound: the
        // parameter that replaced the env knob, and inside the 2 s announcement grace,
        // so the child prints no `lock-waiting:` line, only its `seed-busy:` terminal.
        let home =
            std::env::temp_dir().join(format!("atpkg-main-selfupdate-held-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&home);
        std::fs::create_dir_all(&home).unwrap();
        let held = crate::store::Layout {
            prefix: crate::store::default_prefix(&home),
        };
        assert!(
            held.prefix.starts_with(&home),
            "the default prefix sits under the temp HOME: {}",
            held.prefix.display()
        );
        let guard = crate::lock::try_lock_store(&held).expect("this test holds the store lock");
        let dev_atpkg = dev_atpkg_binary();
        let wrapper_dir = home.join("wrapper");
        std::fs::create_dir_all(&wrapper_dir).unwrap();
        let wrapper = wrapper_dir.join("atpkg");
        let child_argv = home.join("child.argv");
        let child_out = home.join("child.stdout");
        let child_err = home.join("child.stderr");
        std::fs::write(
            &wrapper,
            format!(
                "#!/bin/sh\nprintf '%s\\n' \"$@\" >> '{}'\nexport HOME='{}' \
                 XDG_CONFIG_HOME='{}' ATPKG_REGISTRY='dir:{}' ATPKG_BUNDLED_SEED=off\n\
                 unset ATPKG_DISABLE\nexec '{}' \"$@\" >'{}' 2>'{}'\n",
                child_argv.display(),
                home.display(),
                home.join("config").display(),
                home.join("registry").display(),
                dev_atpkg.display(),
                child_out.display(),
                child_err.display()
            ),
        )
        .unwrap();
        std::fs::set_permissions(&wrapper, std::fs::Permissions::from_mode(0o755)).unwrap();
        let started = std::time::Instant::now();
        let code = selfupdate_check(&held, row, "update", &wrapper, 1);
        let elapsed = started.elapsed();
        drop(guard);
        let child_stdout = std::fs::read_to_string(&child_out).unwrap_or_default();
        let child_stderr = std::fs::read_to_string(&child_err).unwrap_or_default();
        let shown = format!("child stdout:\n{child_stdout}\nchild stderr:\n{child_stderr}");
        assert_eq!(
            code,
            ExitCode::from(crate::lock::CONTENDED_EXIT),
            "the child's 75 relayed: {shown}"
        );
        assert_eq!(
            std::fs::read_to_string(&child_argv).unwrap_or_default(),
            "update\nclaude\n--wait-lock\n1\n",
            "the standard update with the test's own bound"
        );
        assert!(
            elapsed >= std::time::Duration::from_secs(1)
                && elapsed < std::time::Duration::from_secs(20),
            "the child waited its bound and no longer: {elapsed:?}\n{shown}"
        );
        let lock = held.store_lock().display().to_string();
        assert!(
            child_stderr.lines().any(|l| {
                l.starts_with("atpkg: another atpkg process holds the store lock at")
                    && l.contains(&lock)
            }),
            "the child's own lock sentence names the held lock: {shown}"
        );
        assert!(
            child_stdout.lines().any(|l| l
                == format!(
                    "atpkg: {SEED_BUSY_MARKER}another atpkg process holds the store lock \
                     \u{2014} that pass is doing this work and this one stood aside"
                )),
            "the wait lane's terminal marker, on stdout: {shown}"
        );
        assert!(
            !child_stdout.contains(LOCK_WAITING_MARKER),
            "a 1 s wait ends inside the 2 s announcement grace: {shown}"
        );
        assert!(
            selfupdate::contended_line(row, None, 1)
                .contains("held the store for the whole 1 s wait"),
            "the contended line the verb printed renders the bound it passed"
        );
        let _ = std::fs::remove_dir_all(&home);
    }

    /// The dev `atpkg` binary beside this test executable — `target/<profile>/atpkg`,
    /// two levels up from `deps/` — which `targo --unverified test -p atpkg` builds for
    /// the integration lanes before any test runs. LOUD when absent: a lane that drives
    /// the real child against a held lock must not pass on a fake one.
    #[cfg(unix)]
    fn dev_atpkg_binary() -> std::path::PathBuf {
        let exe = std::env::current_exe().expect("this test executable");
        let candidate = exe
            .parent()
            .and_then(std::path::Path::parent)
            .map(|dir| dir.join("atpkg"))
            .expect("target/<profile>/deps/<test> has a profile directory");
        assert!(
            is_executable_regular_file(&candidate),
            "no dev atpkg binary at {} — `targo --unverified test -p atpkg` builds it (the \
             integration lanes need it too); this lane drives the real child against a \
             lock it holds",
            candidate.display()
        );
        candidate
    }

    /// DESIGN S7 on the `which` surface: a managed shim that exports its manifest's
    /// `shim_env` answers with the canonical state and ONE trailing sentence —
    /// `self-update off (DISABLE_AUTOUPDATER=1)` — never inside the state; a foreign copy
    /// ahead on PATH runs WITHOUT the env, so the SHADOWED answer carries no such line;
    /// and a plain shim never earns it. AND (2026-09-19, `crate::selfupdate`) a rostered
    /// agent program's line ends with `` `claude update` here runs `aterm pkg update
    /// claude` `` whether or not the shim exports an env — codex, which exports none,
    /// gets it too — while a shadowed row and a non-agent program get no such sentence.
    /// "Here" is shell-local, so the sentence is withheld (review, 2026-09-19) in a shell
    /// whose `PATH` has `bin/` and not `agents/` (that shell runs `bin/claude`, which
    /// carries no block — the first cut pinned the sentence in exactly that shell), when
    /// the twin is not laid at all, and when the laid twin is yesterday's landing-only
    /// twin (it resolves where `bin/` does and still runs the vendor's updater until a
    /// pass re-lays it).
    #[cfg(unix)]
    #[test]
    fn which_names_the_self_update_switch_as_a_trailing_fix_line() {
        let layout = temp_layout("which-env");
        let build = layout.build_dir("claude", 2026082701);
        std::fs::create_dir_all(build.join("bin")).unwrap();
        std::fs::write(build.join("bin/claude"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(
            build.join("bin/claude"),
            std::fs::Permissions::from_mode(0o755),
        )
        .unwrap();
        let env = crate::shim_env::ShimEnv::admit(&["DISABLE_AUTOUPDATER=1".to_string()]).unwrap();
        crate::activate::install_tools_env(
            &layout,
            &build,
            &[crate::store::ToolName::new("claude").unwrap()],
            crate::activate::Aliases::Off,
            &env,
        )
        .unwrap();
        record_status(
            &layout,
            "claude",
            crate::ProgramStatus {
                installed_build: Some(2026082701),
                state: crate::state::managed(2026082701, 41),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        let shim = layout.shim(&crate::store::ToolName::new("claude").unwrap());
        // The aterm-tab shape: agents/ first, so the twin — laid with its block by
        // `install_tools_env` — is what `claude update` runs here.
        let agents_first = std::env::join_paths([layout.agents_dir(), layout.bin_dir()]).unwrap();
        assert_eq!(
            which_line(&layout, "claude", Some(&agents_first)).unwrap(),
            format!(
                "claude → {} → {} — managed 2026082701 — pinned by index 41 — self-update off \
                 (DISABLE_AUTOUPDATER=1) — `claude update` here runs `aterm pkg update claude`",
                shim.display(),
                crate::which(&layout, "claude").unwrap().display()
            )
        );
        assert_eq!(
            shim_env_fix(&shim).as_deref(),
            Some("self-update off (DISABLE_AUTOUPDATER=1)")
        );
        // NEGATIVE CONTROL — a shell whose PATH has bin/ and not agents/ (opened before
        // the install that introduced agents/; the manual's own case): no foreign copy
        // ahead, so the managed arm answers, and `claude update` typed there execs
        // `bin/claude` — no block, `DISABLE_AUTOUPDATER=1` only — i.e. the vendor's
        // updater. The sentence would be false, so it is withheld; everything else on
        // the line is unchanged.
        let bin_only = std::env::join_paths([layout.bin_dir()]).unwrap();
        assert_eq!(
            which_line(&layout, "claude", Some(&bin_only)).unwrap(),
            format!(
                "claude → {} → {} — managed 2026082701 — pinned by index 41 — self-update off \
                 (DISABLE_AUTOUPDATER=1)",
                shim.display(),
                crate::which(&layout, "claude").unwrap().display()
            ),
            "no agents/ on this PATH: `claude update` here is the vendor's"
        );
        // A foreign copy ahead on PATH: it runs, without the env — no fix-line about it.
        let local = layout
            .prefix
            .parent()
            .unwrap()
            .join(format!("atpkg-main-which-env-local-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&local);
        std::fs::create_dir_all(&local).unwrap();
        let exe = local.join("claude");
        std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        let ahead = std::env::join_paths([local.clone(), layout.bin_dir()]).unwrap();
        // (claude is an AGENT program with its twin laid, so since 2026-09-16 the row
        // is the shell-local `SHADOWED in this shell by …` with the in-place remedy —
        // owner, 2026-09-16: "all the latest and best MUST WORK IN THE SAME TAB" —
        // plus the out-ranked copies line; still no word about self-update.)
        let line = which_line(&layout, "claude", Some(&ahead)).unwrap();
        assert_eq!(
            line,
            format!(
                "claude → {} — {}\nforeign copies on this shell's PATH: {}",
                exe.display(),
                crate::state::agent_shadowed_in_shell(
                    2026082701,
                    &exe,
                    &layout.agents_dir(),
                    false,
                    &shell_remedy_command(&layout),
                ),
                exe.display()
            ),
            "the system copy runs without the env: nothing to say about self-update"
        );
        assert!(!line.contains("self-update"), "{line}");
        assert!(
            !line.contains("here runs"),
            "a shadowed row never claims the twin answers the verb: {line}"
        );
        // NEGATIVE CONTROLS on the twin itself, agents/ first on PATH both times: the twin
        // deleted (agents/ unmakeable, a lane that failed) — `bin/claude` runs; and
        // yesterday's LANDING-ONLY twin (laid by the installed app's older atpkg; it
        // resolves exactly where bin/ does, so `agent_twin_current` alone would pass it)
        // — `claude update` there is the vendor's updater until a pass re-lays the
        // block. Neither earns the sentence; the rest of the managed line stands.
        let claude_tool = crate::store::ToolName::new("claude").unwrap();
        let twin = layout.agent_shim(&claude_tool);
        let target = crate::which(&layout, "claude").unwrap();
        std::fs::remove_file(&twin).unwrap();
        let no_twin = which_line(&layout, "claude", Some(&agents_first)).unwrap();
        assert!(
            no_twin.ends_with(" — pinned by index 41 — self-update off (DISABLE_AUTOUPDATER=1)"),
            "{no_twin}"
        );
        assert!(!no_twin.contains("here runs"), "no twin laid: {no_twin}");
        let landing_only = crate::platform::landing_prelude(
            "claude",
            &layout.prefix,
            &layout.landing_marker(&claude_tool),
            &crate::stub::embedded_atpkg_path(),
        );
        crate::platform::install_twin_to_env(&twin, &target, &env, &landing_only).unwrap();
        assert!(
            agent_twin_current(&layout, "claude", 2026082701),
            "the landing-only twin resolves where bin/ does — target equality is not enough"
        );
        assert!(!agent_twin_intercepts(&layout, "claude", 2026082701));
        let stale = which_line(&layout, "claude", Some(&agents_first)).unwrap();
        assert!(
            stale.ends_with(" — pinned by index 41 — self-update off (DISABLE_AUTOUPDATER=1)"),
            "{stale}"
        );
        assert!(
            !stale.contains("here runs"),
            "a twin without the block: {stale}"
        );
        // A plain shim (no env declared) earns no fix-line — and still the sentence
        // (`install_tools` re-lays the twin, block and all, through `reconcile_agents`).
        crate::activate::install_tools(
            &layout,
            &build,
            &[crate::store::ToolName::new("claude").unwrap()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        assert!(agent_twin_intercepts(&layout, "claude", 2026082701));
        assert_eq!(shim_env_fix(&shim), None);
        let plain = which_line(&layout, "claude", Some(&agents_first)).unwrap();
        assert!(!plain.contains("self-update"), "{plain}");
        assert!(
            plain.ends_with(
                " — pinned by index 41 — `claude update` here runs `aterm pkg update claude`"
            ),
            "{plain}"
        );
        // codex: no shim_env at all, so no fix-line — and the sentence, its own verb.
        let codex_build = layout.build_dir("codex", 2026091901);
        std::fs::create_dir_all(codex_build.join("bin")).unwrap();
        std::fs::write(codex_build.join("bin/codex"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(
            codex_build.join("bin/codex"),
            std::fs::Permissions::from_mode(0o755),
        )
        .unwrap();
        crate::activate::install_tools(
            &layout,
            &codex_build,
            &[crate::store::ToolName::new("codex").unwrap()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        record_status(
            &layout,
            "codex",
            crate::ProgramStatus {
                installed_build: Some(2026091901),
                state: crate::state::managed(2026091901, 41),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        let codex_shim = layout.shim(&crate::store::ToolName::new("codex").unwrap());
        assert_eq!(shim_env_fix(&codex_shim), None);
        assert_eq!(
            which_line(&layout, "codex", Some(&agents_first)).unwrap(),
            format!(
                "codex → {} → {} — managed 2026091901 — pinned by index 41 — `codex update` \
                 here runs `aterm pkg update codex`",
                codex_shim.display(),
                crate::which(&layout, "codex").unwrap().display()
            )
        );
        // A non-agent program: no sentence, whatever its env.
        let ay_build = layout.build_dir("ay", 18);
        std::fs::create_dir_all(ay_build.join("bin")).unwrap();
        std::fs::write(ay_build.join("bin/ay"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(
            ay_build.join("bin/ay"),
            std::fs::Permissions::from_mode(0o755),
        )
        .unwrap();
        crate::activate::install_tools_env(
            &layout,
            &ay_build,
            &[crate::store::ToolName::new("ay").unwrap()],
            crate::activate::Aliases::Off,
            &env,
        )
        .unwrap();
        let ay_line = which_line(&layout, "ay", Some(&agents_first)).unwrap();
        assert!(ay_line.contains("self-update off"), "{ay_line}");
        assert!(!ay_line.contains("here runs"), "{ay_line}");
        let _ = std::fs::remove_dir_all(&local);
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// The predicate behind the `which` sentence, over the bytes every twin renderer
    /// lays (pure, cfg-free, so the Windows shape is pinned from here like every `.cmd`
    /// test): the `sh` twin WITH the block carries the hand-over; the landing-only `sh`
    /// twin, the `bin/` shim and the `.cmd` twin — whose self-update prelude renders the
    /// empty string, so on Windows `claude update` is the vendor's — do not. That last
    /// case is what keeps `aterm pkg which claude` from claiming `here runs …` on a
    /// Windows host by construction (review, 2026-09-19).
    #[test]
    fn the_hand_over_predicate_reads_the_sh_block_and_never_the_cmd_twin_or_bin() {
        use std::path::Path;
        let prefix = Path::new("/Users//u/Library/Application Support/aterm/pkg");
        let marker = Path::new("/Users//u/Library/Application Support/aterm/pkg/landing/claude");
        let atpkg = Path::new("/Applications/aterm.app/Contents/MacOS/atpkg");
        let target = Path::new(
            "/Users//u/Library/Application Support/aterm/pkg/store/claude/2026091902/bin/claude",
        );
        let env = crate::shim_env::ShimEnv::admit(&["DISABLE_AUTOUPDATER=1".to_string()]).unwrap();
        let verbs = crate::selfupdate::verbs_of("claude");
        let mut with_block = crate::platform::sh_selfupdate_prelude("claude", prefix, atpkg, verbs);
        let landing = crate::platform::sh_landing_prelude("claude", prefix, marker, atpkg);
        with_block.push_str(&landing);
        assert!(twin_carries_hand_over(
            crate::platform::sh_shim_content_twin(target, &env, None, &with_block).as_bytes()
        ));
        assert!(!twin_carries_hand_over(
            crate::platform::sh_shim_content_twin(target, &env, None, &landing).as_bytes()
        ));
        assert!(!twin_carries_hand_over(
            crate::platform::sh_shim_content_env(target, &env).as_bytes()
        ));
        let cmd_prefix = Path::new("C:\\Program Files (x86)\\aterm\\pkg");
        let cmd_marker = Path::new("C:\\Program Files (x86)\\aterm\\pkg\\landing\\claude");
        let cmd_atpkg = Path::new("C:\\Program Files (x86)\\aterm\\app\\atpkg.exe");
        let cmd_target = Path::new(
            "C:\\Program Files (x86)\\aterm\\pkg\\store\\claude\\2026091601\\bin\\claude.exe",
        );
        let mut cmd_prelude =
            crate::platform::cmd_selfupdate_prelude("claude", cmd_prefix, cmd_atpkg, verbs);
        cmd_prelude.push_str(&crate::platform::cmd_landing_prelude(
            "claude", cmd_prefix, cmd_marker, cmd_atpkg,
        ));
        assert!(
            !twin_carries_hand_over(
                crate::platform::cmd_shim_content_twin(cmd_target, &env, &cmd_prelude).as_bytes()
            ),
            "the .cmd twin renders no block, so the sentence is never claimed on Windows"
        );
        assert!(!twin_carries_hand_over(b""));
    }

    /// SYSTEM SATISFACTION: a `system = "gh"` member with `gh` on PATH outside the prefix is
    /// satisfied (recorded, announced, not planned); a managed copy it had is retired so the
    /// system binary wins; the managed `bin/` on PATH never counts; a member without the key
    /// is never satisfied, whatever PATH holds.
    #[cfg(unix)]
    #[test]
    fn a_system_install_satisfies_a_default_set_member_and_retires_the_managed_copy() {
        let layout = temp_layout("system");
        let index = index_of(&[("ay", false, None), ("gh", false, Some("gh"))]);
        let sys = layout
            .prefix
            .parent()
            .unwrap()
            .join(format!("atpkg-main-system-bin-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&sys);
        std::fs::create_dir_all(&sys).unwrap();
        for name in ["gh", "ay"] {
            let exe = sys.join(name);
            std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
            std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        }
        let path = std::env::join_paths([layout.bin_dir(), sys.clone()]).unwrap();
        let wanted = names(&["ay", "gh"]);
        // 1. Nothing managed yet: gh is satisfied, ay (no `system` key) is not.
        let mut installed = std::collections::BTreeMap::new();
        assert_eq!(
            reconcile_system_satisfied_with(&layout, &index, &wanted, &mut installed, Some(&path)),
            vec!["gh".to_string()]
        );
        let row = crate::status::read(&layout).unwrap().programs["gh"].clone();
        assert_eq!(row.installed_build, None);
        assert_eq!(
            row.state,
            crate::state::system(&sys.join("gh"), None),
            "the row is the canonical system state, naming the path, with no retirement yet"
        );
        assert_eq!(layout.retired_date("gh"), None);
        // 2. A managed copy exists: it is retired and the plan no longer counts it installed.
        let build = layout.build_dir("gh", 7);
        std::fs::create_dir_all(build.join("bin")).unwrap();
        std::fs::write(build.join("bin/gh"), b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(build.join("bin/gh"), std::fs::Permissions::from_mode(0o755))
            .unwrap();
        crate::activate::install_shims(
            &layout,
            &build,
            &["gh".to_string()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        let mut installed = crate::active_builds(&layout);
        assert_eq!(
            installed.get("gh"),
            Some(&7),
            "PRECONDITION: the managed copy is live"
        );
        assert_eq!(
            reconcile_system_satisfied_with(&layout, &index, &wanted, &mut installed, Some(&path)),
            vec!["gh".to_string()]
        );
        assert!(!installed.contains_key("gh"));
        assert!(
            crate::which(&layout, "gh").is_none(),
            "the managed shim is gone"
        );
        assert!(!build.exists(), "the managed store tree is gone");
        // The retirement is DATED: the marker holds today, and the row says so.
        let date = layout
            .retired_date("gh")
            .expect("a retired managed copy leaves a dated marker");
        assert_eq!(date.len(), 10, "{date}");
        let row = crate::status::read(&layout).unwrap().programs["gh"].clone();
        assert_eq!(
            row.state,
            crate::state::system(&sys.join("gh"), Some(&date))
        );
        assert_eq!(
            crate::state::system_retired(&row.state),
            Some(date.as_str())
        );
        // `which` reads the same facts back, in the same words.
        assert_eq!(
            which_line(&layout, "gh", Some(&path)).unwrap(),
            format!(
                "gh → {} — {}",
                sys.join("gh").display(),
                crate::state::system(&sys.join("gh"), Some(&date))
            )
        );
        assert!(
            !layout.removed().exists(),
            "NOT a removal on purpose: no `removed` marker, so the member returns if the \
             system binary goes away"
        );
        // 3. Only the managed bin/ on PATH: not satisfied — the member stays wanted.
        let managed_only = std::env::join_paths([layout.bin_dir()]).unwrap();
        let mut none = std::collections::BTreeMap::new();
        assert!(
            reconcile_system_satisfied_with(
                &layout,
                &index,
                &wanted,
                &mut none,
                Some(&managed_only)
            )
            .is_empty()
        );
        assert!(
            reconcile_system_satisfied_with(&layout, &index, &wanted, &mut none, None).is_empty()
        );
        let _ = std::fs::remove_dir_all(&sys);
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// EXTRAS' CONSENT STUBS are laid for index-listed extras only, and NOT for one the
    /// user removed on purpose, excluded, already has installed, or that a system install
    /// on PATH (outside the managed prefix) satisfies. Default-set members are never
    /// candidates, whatever PATH holds.
    #[cfg(unix)]
    #[test]
    fn extra_stub_candidates_skip_removed_excluded_installed_and_system_satisfied() {
        let layout = temp_layout("extra-cands");
        let index = index_of(&[
            ("ay", false, None),
            ("gh", false, Some("gh")),
            ("vendorx", true, None),
            ("vendory", true, Some("vendory")),
            // An AGENT PROGRAM the index still flags (build 21 does): never a consent
            // stub candidate — it is default-set on this client (2026-09-10).
            ("claude", true, None),
        ]);
        let sys = layout
            .prefix
            .parent()
            .unwrap()
            .join(format!("atpkg-main-extra-sys-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&sys);
        std::fs::create_dir_all(&sys).unwrap();
        for name in ["vendory", "gh"] {
            let exe = sys.join(name);
            std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
            std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        }
        let path = std::env::join_paths([layout.bin_dir(), sys.clone()]).unwrap();
        let cfg = crate::config::PackagesConfig::default();
        let none = std::collections::BTreeMap::new();
        // vendory: satisfied by the system install; vendorx: a candidate; ay/gh: not extras.
        assert_eq!(
            extra_stub_candidates_with(&layout, &index, &cfg, &none, Some(&path)),
            names(&["vendorx"])
        );
        // No system vendory reachable: both extras qualify.
        assert_eq!(
            extra_stub_candidates_with(&layout, &index, &cfg, &none, None),
            names(&["vendory", "vendorx"])
        );
        // Removed on purpose: no stub nagging the user back.
        std::fs::write(layout.removed(), "vendorx\n").unwrap();
        assert_eq!(
            extra_stub_candidates_with(&layout, &index, &cfg, &none, None),
            names(&["vendory"])
        );
        std::fs::remove_file(layout.removed()).unwrap();
        // Excluded by config.
        let narrowed = crate::config::PackagesConfig {
            exclude: Some(vec!["vendorx".to_string()]),
            ..Default::default()
        };
        assert_eq!(
            extra_stub_candidates_with(&layout, &index, &narrowed, &none, None),
            names(&["vendory"])
        );
        // Installed: the real shim is there, nothing pends.
        let installed: std::collections::BTreeMap<String, u64> =
            [("vendorx".to_string(), 1)].into_iter().collect();
        assert_eq!(
            extra_stub_candidates_with(&layout, &index, &cfg, &installed, None),
            names(&["vendory"])
        );
        // A FOREIGN copy under the program's OWN name (no `system =` flag on vendorx): a
        // brew vendorx on PATH means the name already answers, and a stub laid ahead of it
        // would hijack it with a consent prompt — so no stub. Remove it and vendorx pends
        // again.
        let exe = sys.join("vendorx");
        std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        assert_eq!(
            extra_stub_candidates_with(&layout, &index, &cfg, &none, Some(&path)),
            names(&[]),
            "a foreign vendorx on PATH must suppress the consent stub"
        );
        std::fs::remove_file(&exe).unwrap();
        assert_eq!(
            extra_stub_candidates_with(&layout, &index, &cfg, &none, Some(&path)),
            names(&["vendorx"])
        );
        let _ = std::fs::remove_dir_all(&sys);
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A refused vendor row is a fact about THIS machine's install attempt (the row was
    /// signed and reached the client), so it lands on the record like any other stage fault.
    #[test]
    fn a_refused_vendor_row_is_recorded_as_an_error_state() {
        assert_eq!(
            failed_install_state(&crate::FlowError::VendorRefused("url host is evil".into())),
            Some(String::from(
                "error: artifact row refused: url host is evil"
            ))
        );
    }

    /// Every outcome an OS-installer lane can answer records its CANONICAL row — the
    /// same words the pass log prints — with no build and no root: `installed via
    /// <manager>: <path>`, `needs admin`, `unavailable on <target>`. None reads as a
    /// doctor fault.
    #[test]
    fn a_protocol_outcome_records_its_canonical_row() {
        let layout = temp_layout("protocol-outcome-rows");
        let emacs = layout.prefix.join("usr-bin-emacs");
        for (program, outcome) in [
            (
                "emacs",
                crate::ProtocolOutcome::Installed {
                    protocol: "apt",
                    path: emacs.clone(),
                },
            ),
            (
                "brew",
                crate::ProtocolOutcome::NeedsAdmin { protocol: "pkg" },
            ),
            (
                "emacs-win",
                crate::ProtocolOutcome::Unavailable {
                    protocol: "winget",
                    target: "aarch64-pc-windows-msvc".into(),
                    hint: "winget is not on PATH".into(),
                },
            ),
        ] {
            let state = record_protocol_outcome(&layout, program, &outcome);
            assert_eq!(state, outcome.state(program));
            let status = crate::status::read(&layout).expect("recorded");
            let row = status.programs.get(program).expect(program);
            assert_eq!(row.state, state);
            assert_eq!(row.installed_build, None);
            assert!(row.tree_root.is_empty());
        }
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(
            status.programs["emacs"].state,
            crate::state::installed_via("apt", &emacs)
        );
        assert_eq!(
            status.programs["brew"].state,
            crate::state::needs_admin("brew")
        );
        assert_eq!(
            status.programs["emacs-win"].state,
            "unavailable on aarch64-pc-windows-msvc: winget is not on PATH"
        );
        assert!(
            crate::doctor::recorded_problems(Some(&status)).is_empty(),
            "none of the lane outcomes is a fault"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// The triple this client reports for itself is one of the six the schema serves —
    /// a row can only ever be selected under a spelling `TARGETS` carries.
    #[test]
    fn the_current_triple_is_a_schema_target() {
        assert!(
            crate::manifest::TARGETS.contains(&current_triple()),
            "{} is not in TARGETS",
            current_triple()
        );
    }

    /// A verdict the AUTHORITY never gave must not become a resident of the record.
    ///
    /// `NotReachable` means the signed index resolved, verified, and does not name the
    /// token — a fact about the request, not about this store. Recording it as a program
    /// state invents a program, and `atpkg doctor` reads every such row back as a missing
    /// member of the toolset.
    #[test]
    fn a_name_the_index_does_not_name_is_never_recorded() {
        assert_eq!(
            failed_install_state(&crate::FlowError::NotReachable("--help".into(), vec![])),
            None,
            "the index does not name it, so there is no program to remember"
        );
        assert_eq!(
            failed_install_state(&crate::FlowError::Linked("ay".into())),
            Some(String::from("dev-linked (skipped)")),
            "a dev-linked program is a benign hard-skip (§13), and still says so"
        );
        // THE OFFLINE FAMILY, all raised before the program lookup ever happens. Recording
        // any of these against a program says the program is broken; what happened is that
        // the machine could not reach the channel.
        for env in [
            crate::FlowError::NoIndex,
            crate::FlowError::Unreachable("dns".into()),
            crate::FlowError::Stale,
        ] {
            assert_eq!(
                failed_install_state(&env),
                None,
                "{env} is a fact about the network, not about any program"
            );
        }
        // …while a genuine per-program fact about THIS machine still lands on the record:
        // no row for this target is the canonical `unavailable on` STATE (never an error).
        assert_eq!(
            failed_install_state(&crate::FlowError::NoArtifact("aarch64".into())),
            Some(crate::state::unavailable("aarch64", "")),
            "no build for this target is a real, durable, non-error fact about the program"
        );
        assert_eq!(
            failed_install_state(&crate::FlowError::PkgVerify),
            Some(format!("error: {}", crate::FlowError::PkgVerify)),
            "a manifest that failed its signature check is a fault worth remembering"
        );
    }

    /// Put `program` genuinely LIVE in `layout` at `build` — real build tree, real shims,
    /// real channel pointer — so `active_builds` reports it the way it does on a machine.
    /// A stubbed record would not exercise the thing under test.
    fn make_live(layout: &crate::store::Layout, program: &str, build: u64) {
        let dir = layout.build_dir(program, build);
        std::fs::create_dir_all(dir.join("bin")).unwrap();
        let t = crate::store::ToolName::new(program).unwrap();
        std::fs::write(dir.join("bin").join(t.exe_file()), b"#!/bin/true\n").unwrap();
        crate::activate::install_shims(
            layout,
            &dir,
            &[program.to_string()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        crate::activate::activate_channel(layout, "stable", &dir).unwrap();
        crate::store::mark_build_ready(&dir).unwrap();
    }

    /// A FAILED INSTALL UNINSTALLS NOTHING — so it must not report that it did.
    ///
    /// An upgrade that dies at download/stage/activate leaves the previous build activated,
    /// shimmed and working. Every failure arm nonetheless wrote `installed_build: None` and
    /// `tree_root: ""`, which says the program is gone AND unattested. The second is
    /// destructive: `crate::verify` fails closed on an empty root, so ONE failed upgrade
    /// turned `atpkg verify` into a permanent "cannot verify" for bytes that were never
    /// touched — recoverable only by reinstalling gigabytes to re-record a root that had
    /// been correct the whole time.
    #[test]
    fn a_failed_install_keeps_the_live_build_and_its_attestation() {
        let layout = temp_layout("failure-row");
        make_live(&layout, "ay", 18);
        record_status(
            &layout,
            "ay",
            crate::ProgramStatus {
                installed_build: Some(18),
                state: "active".into(),
                tree_root: "0fda8eaefbfae96ac95ffb83afce208e".into(),
            },
            "up to date".into(),
        );

        // An upgrade to 20 dies mid-flight.
        let row = failure_row(&layout, "ay", String::from("error: download failed"));
        assert_eq!(
            row.installed_build,
            Some(18),
            "build 18 is still activated and shimmed; the record must not claim otherwise"
        );
        assert_eq!(
            row.tree_root, "0fda8eaefbfae96ac95ffb83afce208e",
            "the signed attestation for untouched bytes survives a failed upgrade"
        );
        assert_eq!(
            row.state, "error: download failed",
            "the failure is still reported"
        );

        // …while a program that was never installed still records nothing to preserve.
        let fresh = failure_row(&layout, "clean", String::from("error: download failed"));
        assert_eq!(fresh.installed_build, None);
        assert!(fresh.tree_root.is_empty());

        // THE RECORD DISAGREES WITH THE STORE — the case the two fields are read from
        // different sources, and the one that makes stapling them together dangerous. The
        // store is now live at 20 while the record still says 18/root18. Taking the build
        // from the store and the root from the record would produce "build 20, attested by
        // build 18's signature", which DISARMS verify's `BuildMismatch` guard and lands in
        // the tree comparison: `Drift` (a tampering accusation against untouched bytes) or,
        // worse, `Match` — affirming a build no signature ever covered.
        make_live(&layout, "ay", 20);
        assert_eq!(
            crate::active_builds(&layout).get("ay").copied(),
            Some(20),
            "precondition: the store moved on and the record has not caught up"
        );
        let stitched = failure_row(&layout, "ay", String::from("error: download failed"));
        assert_eq!(
            stitched.installed_build,
            Some(20),
            "the store is ground truth for what is live"
        );
        assert!(
            stitched.tree_root.is_empty(),
            "an attestation captured for build 18 must NOT be stapled onto build 20 — an \
             honest `NoSignedRoot` beats a false `Drift`, and beats a false `Match` by \
             considerably more"
        );

        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A TOMBSTONED PROGRAM KEEPS ITS ATTESTATION. Its shims were replaced by failing stubs
    /// so `active_builds` goes silent, but its bytes and their signed root sit untouched in
    /// the store — and the build-agreement rule above must not read that silence as
    /// disagreement and throw the root away on the strength of a yanked pin.
    #[test]
    fn a_tombstoned_program_does_not_lose_its_signed_root() {
        let layout = temp_layout("tombstone-root");
        record_status(
            &layout,
            "trust",
            crate::ProgramStatus {
                installed_build: Some(6459),
                state: "active".into(),
                tree_root: "abc123".into(),
            },
            "up to date".into(),
        );
        // No shims at all: exactly what `active_builds` sees for a tombstoned program.
        assert_eq!(crate::active_builds(&layout).get("trust").copied(), None);

        let row = failure_row(&layout, "trust", String::from("tombstoned: pin yanked"));
        assert_eq!(
            row.tree_root, "abc123",
            "nothing contradicts the recorded root, so it survives"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A COMPLETE build dir with an executable `bin/<program>` and its ready marker — the
    /// shape `list_installed` reports — for the repair tests below.
    fn seed_ready_build(layout: &crate::store::Layout, program: &str, build: u64) -> PathBuf {
        let dir = layout.build_dir(program, build);
        std::fs::create_dir_all(dir.join("bin")).unwrap();
        let exe = dir.join("bin").join(program);
        std::fs::write(&exe, b"#!/bin/sh\nexit 0\n").unwrap();
        std::fs::set_permissions(&exe, std::fs::Permissions::from_mode(0o755)).unwrap();
        crate::store::mark_build_ready(&dir).unwrap();
        dir
    }

    /// Lay `program`'s shims at `dir` and point both `current` links at it.
    fn activate_seeded(layout: &crate::store::Layout, program: &str, dir: &Path) {
        crate::activate::install_shims(
            layout,
            dir,
            &[program.to_string()],
            crate::activate::Aliases::Off,
        )
        .unwrap();
        crate::activate::activate_channel(layout, "stable", dir).unwrap();
    }

    /// REPAIR NEVER REVIVES A REVOKED BUILD (audit K2, 2026-09-12).
    ///
    /// A tombstone keeps the revoked build's tree, marker and `current` links; only its
    /// shim turns into a failing notice, which resolves to nothing. Repair used to read
    /// that silence as "no exposes recorded", fall back to the program's name and lay a
    /// live shim over the tombstone — making a yanked/below-floor build runnable again.
    /// Refused both on the recorded `tombstoned:` row and on the tombstone shim alone.
    #[test]
    fn repair_does_not_revive_a_tombstoned_build() {
        for with_status_row in [true, false] {
            let label = if with_status_row {
                "repair-tomb-row"
            } else {
                "repair-tomb-shim"
            };
            let l = temp_layout(label);
            let d17 = seed_ready_build(&l, "ay", 17);
            activate_seeded(&l, "ay", &d17);
            if with_status_row {
                record_status(
                    &l,
                    "ay",
                    crate::ProgramStatus {
                        installed_build: Some(17),
                        state: "tombstoned: pin yanked/below floor".into(),
                        tree_root: String::new(),
                    },
                    "tombstoned".into(),
                );
            }
            let ay = crate::store::ToolName::new("ay").unwrap();
            crate::activate::install_tombstone_shim(&l, &ay).unwrap();
            assert!(
                crate::platform::resolve_shim(&l.shim(&ay)).is_none(),
                "precondition: the tombstone forwards nowhere"
            );

            let (_, _, revoked, _, _) = relay_shims(&l);

            assert_eq!(revoked, ["ay@17"], "{label}: named as still revoked");
            assert_eq!(
                crate::platform::resolve_shim(&l.shim(&ay)),
                None,
                "{label}: repair laid a live shim over the tombstone"
            );
            assert!(
                std::fs::read_to_string(l.shim(&ay))
                    .unwrap()
                    .contains("was yanked/revoked"),
                "{label}: the failing notice is still what `ay` runs"
            );
            let _ = std::fs::remove_dir_all(&l.prefix);
        }
    }

    /// A LAY THAT FAILS REACHES THE EXIT CODE (audit 2026-09-12). A provenance-tracked
    /// installer whose untracked lane cannot run REFUSES to lay shims; repair printed the
    /// refusal and then said "done", exit 0, so a scripted `repair && …` could not tell
    /// fixed from refused. Every program whose shims could not be re-laid now comes back in
    /// the fifth slot and `run_repair` exits 1 on it. Forced here the way any lay fails: an
    /// unwritable `bin/`.
    #[cfg(unix)]
    #[test]
    fn a_shim_that_cannot_be_relaid_is_named_and_not_swallowed() {
        let l = temp_layout("repair-failed-lay");
        let d17 = seed_ready_build(&l, "ay", 17);
        activate_seeded(&l, "ay", &d17);
        let ay = crate::store::ToolName::new("ay").unwrap();
        // The seeded activation already laid `ay`; a lay that finds its shim in place and
        // identical writes nothing, so take it away — and a read-only `bin/` is no
        // obstacle either (the lay re-asserts the directory's mode). A DIRECTORY where
        // the shim goes is: no rename lands a file over it.
        std::fs::remove_file(l.shim(&ay)).unwrap();
        std::fs::create_dir(l.shim(&ay)).unwrap();

        let report = relay_shims(&l);

        let (_, _, _, _, failed) = &report;
        assert_eq!(
            failed.len(),
            1,
            "one program could not be re-laid: report={report:?}"
        );
        assert!(failed[0].starts_with("ay@17"), "{failed:?}");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// REPAIR RESPECTS A ROLLBACK (audit K2, 2026-09-12). `rollback` keeps the build it
    /// rolled off until gc; repair must re-lay the build `current` selects, not the highest
    /// one on disk — or it silently undoes the rollback and mints the very
    /// `ChannelShimMismatch` doctor then reports.
    #[test]
    fn repair_respects_rollback() {
        let l = temp_layout("repair-rollback");
        let d18 = seed_ready_build(&l, "ay", 18);
        let d19 = seed_ready_build(&l, "ay", 19);
        activate_seeded(&l, "ay", &d19);
        // The rollback: shims and both links back on 18, 19 retained on disk.
        activate_seeded(&l, "ay", &d18);
        let ay = crate::store::ToolName::new("ay").unwrap();

        let _ = relay_shims(&l);

        let target = crate::platform::resolve_shim(&l.shim(&ay)).expect("a live shim");
        assert!(
            target.starts_with(&d18),
            "repair re-pointed the shim off the rollback target: {}",
            target.display()
        );
        assert!(
            crate::gc::live_builds(&l).diverged().is_empty(),
            "{:?}",
            crate::gc::live_builds(&l).diverged()
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// A COMPLETE trust build in the shape bundles 8571/8589/8590/8595 ship: `bin/rustc` a
    /// separate file beside `bin/trustc` whose bytes differ (the ad-hoc signature bakes each
    /// file's name in), a tippy that refuses that pair, and a `lib/` with a driver dylib —
    /// with its ready marker, `store/trust/current` pointing at it, and PLAIN shims for
    /// `trustc`, `targo`, `tippy` and `alab-tippy` written the way a client before exec roots
    /// laid them. Returns the build dir.
    fn seed_affected_trust(layout: &crate::store::Layout, build: u64) -> PathBuf {
        let dir = layout.build_dir("trust", build);
        let bin = dir.join("bin");
        std::fs::create_dir_all(&bin).unwrap();
        std::fs::create_dir_all(dir.join("lib")).unwrap();
        std::fs::write(
            dir.join("lib").join("librustc_driver-5cfd.dylib"),
            b"driver",
        )
        .unwrap();
        for tool in ["targo", "tippy", "targo-tippy", "tippy-driver"] {
            std::fs::write(bin.join(tool), format!("{tool} {build}")).unwrap();
        }
        std::fs::write(bin.join("trustc"), b"frontend / signature: trustc").unwrap();
        std::fs::write(bin.join("rustc"), b"frontend / signature: rustc_").unwrap();
        crate::store::mark_build_ready(&dir).unwrap();
        crate::activate::atomic_symlink(&dir, &layout.program_current("trust")).unwrap();
        crate::activate::activate_channel(layout, "stable", &dir).unwrap();
        layout.ensure_dir(&layout.bin_dir()).unwrap();
        for (name, tool) in [
            ("trustc", "trustc"),
            ("targo", "targo"),
            ("tippy", "tippy"),
            ("alab-tippy", "tippy"),
        ] {
            let shim = layout.bin_dir().join(name);
            std::fs::write(
                &shim,
                crate::platform::sh_shim_content_env(
                    &bin.join(tool),
                    &crate::shim_env::ShimEnv::NONE,
                ),
            )
            .unwrap();
            std::fs::set_permissions(&shim, std::fs::Permissions::from_mode(0o755)).unwrap();
        }
        dir
    }

    /// Whether the shim `bin/<name>` carries an exec-root guard line.
    fn routes(layout: &crate::store::Layout, name: &str) -> bool {
        std::fs::read_to_string(layout.bin_dir().join(name))
            .unwrap()
            .lines()
            .any(|l| l.starts_with("[ ! -h "))
    }

    /// THE PASS HEALS AN UP-TO-DATE AFFECTED MACHINE WITHOUT AN INSTALL. A machine already
    /// on trust 8595 with the plain shims an older client laid is short-circuited by the
    /// apply before any shim is written; [`reconcile_exec_roots`] alone — no
    /// `install_tools` — lays `compat/trust/8595` and routes every trust shim, the `alab-`
    /// alias included, and answers `true`. A second call writes nothing. And the verbs that
    /// must heal call it where the design puts them — each call pinned AFTER its anchor and
    /// BEFORE the first return or branch that would skip it on the lane that needs it: the
    /// update pass after its gc and before `if failures == 0` (a pass with one failed
    /// program still heals), the default-set lane after its alias reconcile, `seed` beside
    /// the rustup seam and before the seedless return (a Mac with no bundled seed returns
    /// there, at every launch), a trust rollback after its seam, and `repair` at `Deep`
    /// through [`repair_store`], before its "done". This pins the order of the source text,
    /// not a run of those verbs, which write the account's real `defaults` or rc wiring from
    /// inside a test process; a reviewer's mutations that moved the seed call below the
    /// seedless return, or the pass's inside `if failures == 0`, passed the anchor-only form
    /// of this test (2026-09-15).
    #[test]
    fn the_pass_reconcile_routes_an_up_to_date_affected_install() {
        let l = temp_layout("exec-root-pass");
        seed_affected_trust(&l, 8595);
        let shims = ["trustc", "targo", "tippy", "alab-tippy"];
        assert!(shims.iter().all(|s| !routes(&l, s)), "precondition: plain");

        assert!(reconcile_exec_roots(&l, crate::seam::Depth::Shallow));
        let root = crate::compat::root_dir(&l, 8595);
        assert!(root.join("bin").join("rustc").is_file());
        for s in shims {
            assert!(routes(&l, s), "{s} routes");
        }
        let modified = |p: PathBuf| std::fs::metadata(p).unwrap().modified().unwrap();
        let before: Vec<_> = shims
            .iter()
            .map(|s| modified(l.bin_dir().join(s)))
            .collect();
        std::thread::sleep(std::time::Duration::from_millis(20));
        assert!(reconcile_exec_roots(&l, crate::seam::Depth::Deep));
        let after: Vec<_> = shims
            .iter()
            .map(|s| modified(l.bin_dir().join(s)))
            .collect();
        assert_eq!(before, after, "a second reconcile re-lays nothing");

        let production = production_half(include_str!("cli.rs"));
        let body = |name: &str| {
            let start = production
                .find(&format!("\nfn {name}("))
                .unwrap_or_else(|| panic!("fn {name}"));
            let end = production[start + 1..]
                .find("\nfn ")
                .map_or(production.len(), |i| start + 1 + i);
            &production[start..end]
        };
        // `call` appears after `anchor` and before the first `until` after `anchor`.
        let between = |name: &str, anchor: &str, call: &str, until: &str| {
            let b = body(name);
            let a = b.find(anchor).unwrap_or_else(|| panic!("{name}: {anchor}"));
            let c = b[a..]
                .find(call)
                .unwrap_or_else(|| panic!("{name} calls `{call}` after `{anchor}`"));
            let u = b[a..]
                .find(until)
                .unwrap_or_else(|| panic!("{name}: `{until}` after `{anchor}`"));
            assert!(c < u, "{name} calls `{call}` before `{until}`");
        };
        const SHALLOW: &str = "reconcile_exec_roots(&layout, crate::seam::Depth::Shallow)";
        between(
            "cmd_update_all_code",
            "print_gc_abstentions(\"update\"",
            SHALLOW,
            "if failures == 0 {",
        );
        between(
            "install_default_set_inner",
            "reconcile_aliases(layout, index);",
            "reconcile_exec_roots(layout, crate::seam::Depth::Shallow)",
            "reconcile_shadowed(",
        );
        between(
            "cmd_seed",
            "reassert_rustup_seam(&layout);",
            SHALLOW,
            "let Some(seed_dir) = crate::bundled_seed_dir() else {",
        );
        between(
            "cmd_rollback",
            "reassert_rustup_seam(&layout);",
            SHALLOW,
            "ExitCode::SUCCESS",
        );
        between(
            "repair_store",
            "relay_shims(layout)",
            "reconcile_exec_roots(layout, crate::seam::Depth::Deep)",
            "repair: done",
        );
        // THE LANES A PERSON TYPES (2026-09-17). Every whole-set pass above has re-asserted
        // the rustup seam and reconciled the exec roots since those calls were written;
        // `aterm pkg install <p>` and `aterm pkg update <p>` — which end in
        // `cmd_install_with`, or in `cmd_update_one`'s coherence-group branch — never did.
        // That is the asymmetry that let `aterm pkg update trust` answer "up to date" for
        // two weeks on m3 without once looking at the `~/.rustup/toolchains/trust` entry
        // that decided what the machine actually compiled with. The seam comes first in
        // both, matching every other lane: a root laid over a seam that was just re-pointed
        // is the order `cmd_seed` and `cmd_rollback` already use.
        for lane in ["cmd_install_with", "cmd_update_one"] {
            let b = body(lane);
            let seam = b
                .find("reassert_rustup_seam(")
                .unwrap_or_else(|| panic!("{lane} must re-assert the rustup seam"));
            let roots = b
                .find("reconcile_exec_roots(")
                .unwrap_or_else(|| panic!("{lane} must reconcile the exec roots"));
            assert!(
                seam < roots,
                "{lane}: the seam is re-asserted before the roots"
            );
        }
        // …and the verdict those lanes print is qualified by what the machine RUNS, not
        // only by what the store holds ([`crate::seam::dissent_line`]).
        for (lane, count) in [
            ("cmd_install_with", 1usize),
            ("do_install", 1),
            ("report_channel_apply", 1),
        ] {
            assert_eq!(
                body(lane).matches("dissent_if_armed(").count(),
                count,
                "{lane} must qualify its up-to-date verdict with what this machine runs"
            );
        }
        assert!(body("run_repair").contains("repair_store(&layout)"));
        assert_eq!(
            body("cmd_uninstall_all")
                .matches("remove_exec_roots(&layout);")
                .count(),
            2,
            "both branches of uninstall --all remove compat/"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// REPAIR ROUTES, AND SAYS SO IN ITS EXIT CODE WHEN IT CANNOT. Over an affected build
    /// with plain shims, [`repair_store`] re-lays them through the root it lays and exits
    /// 0; with the root unlayable (an unreadable directory in the build's `lib/`, standing
    /// in for the EXDEV of a store on another volume) every shim is still re-laid — plain,
    /// running the store path as before — and repair exits 1, because doctor's fix for a
    /// PATH tippy that cannot run is exactly this verb.
    #[test]
    fn repair_routes_the_trust_shims_and_exits_1_when_the_root_cannot_be_laid() {
        let l = temp_layout("exec-root-repair");
        seed_affected_trust(&l, 8595);
        assert_eq!(repair_store(&l), ExitCode::SUCCESS);
        for s in ["trustc", "targo", "tippy", "alab-tippy"] {
            assert!(routes(&l, s), "{s} routes after repair");
        }
        let _ = std::fs::remove_dir_all(&l.prefix);

        if crate::platform::our_uid() == 0 {
            return; // root reads a 0000 directory; the failure cannot be staged
        }
        let l = temp_layout("exec-root-repair-fail");
        let dir = seed_affected_trust(&l, 8595);
        let locked = dir.join("lib").join("locked");
        std::fs::create_dir_all(&locked).unwrap();
        std::fs::set_permissions(&locked, std::fs::Permissions::from_mode(0o000)).unwrap();
        let code = repair_store(&l);
        std::fs::set_permissions(&locked, std::fs::Permissions::from_mode(0o700)).unwrap();
        assert_eq!(code, ExitCode::from(1));
        for s in ["trustc", "targo", "tippy", "alab-tippy"] {
            assert!(!routes(&l, s), "{s} stays plain");
            assert!(
                crate::platform::resolve_shim(&l.bin_dir().join(s)).is_some(),
                "{s} is still a live shim"
            );
        }
        assert!(std::fs::symlink_metadata(crate::compat::root_dir(&l, 8595)).is_err());
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// A ROW MUST NOT OUTLIVE ITS PROGRAM.
    ///
    /// `active_builds` reads shims, so doctor went quiet after an uninstall — but Settings ▸
    /// Packages reads the RECORD, and listed a deleted program as `active` at its last build
    /// forever. The stale-row reconciler cannot fix this and correctly declines to: the
    /// signed index still names the program, it is this MACHINE that no longer carries it.
    #[test]
    fn uninstalling_retires_the_program_row() {
        let layout = temp_layout("retire-row");
        make_live(&layout, "ay", 18);
        make_live(&layout, "clean", 7);
        record_status(
            &layout,
            "ay",
            crate::ProgramStatus {
                installed_build: Some(18),
                state: "active".into(),
                tree_root: "deadbeef".into(),
            },
            "up to date".into(),
        );
        record_status(
            &layout,
            "clean",
            crate::ProgramStatus {
                installed_build: Some(7),
                state: "active".into(),
                tree_root: "cafe".into(),
            },
            "up to date".into(),
        );
        assert!(
            crate::active_builds(&layout).contains_key("ay"),
            "precondition: ay is genuinely live"
        );

        // THE REAL PATH the CLI arm runs — not the primitive it happens to call.
        uninstall_and_retire(&layout, "ay").expect("uninstall succeeds");

        assert!(
            !crate::active_builds(&layout).contains_key("ay"),
            "the program really came off"
        );
        assert!(
            removed_programs(&layout).contains("ay"),
            "and the seed lane is told not to put it back"
        );
        let s = crate::status::read(&layout).expect("record present");
        assert!(
            !s.programs.contains_key("ay"),
            "the removed program's row is retired, not left reading `active`"
        );
        assert!(
            s.programs.contains_key("clean"),
            "and retiring one program does not disturb the others"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// ROW DELETION IS A NEW CAPABILITY, so the names it accepts are gated.
    ///
    /// `ops::uninstall`'s shape rule rejects only empty/`.`/`..`/separators/NUL, so before
    /// the gate `atpkg uninstall '*toolset*'` returned Ok and DELETED a bookkeeping row that
    /// has its own owner and its own reaper, and `atpkg uninstall --help` appended a flag to
    /// the removed-markers file — which would then suppress a program named `--help` forever,
    /// a fittingly absurd end for the bug that started this.
    #[test]
    fn retirement_refuses_names_that_are_not_programs() {
        let layout = temp_layout("retire-gate");
        record_status(
            &layout,
            "*toolset*",
            crate::ProgramStatus {
                installed_build: None,
                state: "unavailable: no build for this architecture".into(),
                tree_root: String::new(),
            },
            "no build published".into(),
        );

        assert!(
            uninstall_and_retire(&layout, "*toolset*").is_err(),
            "a sentinel bookkeeping row is not a program and is not this verb's to retire"
        );
        assert!(
            crate::status::read(&layout)
                .expect("record present")
                .programs
                .contains_key("*toolset*"),
            "…and it is still there"
        );
        assert!(
            uninstall_and_retire(&layout, "--help").is_err(),
            "a flag is not a program name here either"
        );
        assert!(
            !removed_programs(&layout).contains("--help"),
            "no removed-marker is minted for a flag"
        );
        // The removal intent is recorded BEFORE the delete, so the shape rule `ops::uninstall`
        // enforces must refuse first too — a path-shaped name is not a program to remember.
        assert!(uninstall_and_retire(&layout, "a/b").is_err());
        assert!(
            removed_programs(&layout).is_empty(),
            "no removed-marker is minted for a name the delete would refuse"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// THE REMOVAL INTENT LANDS BEFORE THE DELETE.
    ///
    /// `ops::uninstall` takes the shims off first and then spends seconds in `remove_dir_all`
    /// on the store tree (3.4 GB for trust). The removed marker and the adoption clear used to
    /// land only after that returned, so a kill inside the delete left the program absent to
    /// `active_builds` and recorded nowhere, and the next six-hourly pass reinstalled it on
    /// the machine whose owner had just removed it. A kill is not reproducible in a test; a
    /// subtree the delete cannot empty stops `remove_dir_all` at the same point and leaves the
    /// same machine state.
    #[cfg(unix)]
    #[test]
    fn a_delete_cut_short_still_leaves_the_removal_recorded() {
        let layout = temp_layout("uninstall-cut-short");
        make_live(&layout, "ay", 18);
        std::fs::write(layout.adopted(), b"").unwrap();
        let locked = layout.build_dir("ay", 18).join("locked");
        std::fs::create_dir_all(&locked).unwrap();
        std::fs::write(locked.join("payload"), b"x").unwrap();
        std::fs::set_permissions(&locked, std::fs::Permissions::from_mode(0o500)).unwrap();
        if std::fs::write(locked.join("probe"), b"x").is_ok() {
            // Privileged (root in a container): permissions cannot stop the delete here.
            std::fs::set_permissions(&locked, std::fs::Permissions::from_mode(0o700)).unwrap();
            let _ = std::fs::remove_dir_all(&layout.prefix);
            return;
        }

        let code = uninstall_one(&layout, "ay");
        std::fs::set_permissions(&locked, std::fs::Permissions::from_mode(0o700)).unwrap();

        assert_eq!(
            format!("{code:?}"),
            format!("{:?}", ExitCode::from(1)),
            "precondition: the delete really stopped part-way"
        );
        assert!(
            !crate::active_builds(&layout).contains_key("ay") && program_present(&layout, "ay"),
            "precondition: shims gone, tree half there — what a kill mid-delete leaves"
        );
        assert!(
            layout.removed_programs().contains("ay"),
            "the unattended lanes are told the removal was deliberate"
        );
        assert!(
            !adopted(&layout),
            "and set-completion was opted out of before the delete"
        );
        assert!(
            std::fs::read_dir(&layout.prefix)
                .unwrap()
                .flatten()
                .all(|e| !e.file_name().to_string_lossy().starts_with("removed.tmp-")),
            "the marker is renamed into place, leaving no temp file behind"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// AN OFFLINE FIRST RUN MUST STILL LEAVE A WITNESS.
    ///
    /// Suppressing the environmental failures' per-program rows routes their reason to the
    /// outcome sentence and NOWHERE else — so the sentence has to survive a virgin store.
    /// While `record_outcome` early-returned when no record existed, a first-run offline
    /// install recorded nothing at all: exactly the failed-seed machine that gets pointed
    /// at `pkg doctor`, and the one case doctor's fallback to this sentence exists for.
    #[test]
    fn the_outcome_sentence_creates_the_record_it_needs() {
        let layout = temp_layout("virgin-outcome");
        assert!(
            crate::status::read(&layout).is_none(),
            "precondition: this store has never been written to"
        );
        record_outcome(
            &layout,
            String::from("install ay: no signature-valid index"),
        );
        let s = crate::status::read(&layout).expect("a first-run failure still leaves a witness");
        assert_eq!(s.outcome, "install ay: no signature-valid index");
        assert!(
            s.programs.is_empty(),
            "the witness records what happened without blaming a program for the network"
        );
        assert_eq!(s.schema, 1, "a created record is a well-formed one");
    }

    /// The record is a DERIVED memo; the signed index is the authority on what exists.
    ///
    /// Pins the four conditions together, because each one alone is a different bug: prune
    /// a sentinel and the bookkeeping rows lose their reapers; prune a live-but-dropped
    /// program and `atpkg verify` fails closed forever on an erased attestation.
    #[test]
    fn the_record_reconciles_against_the_signed_index() {
        let row = |state: &str| crate::ProgramStatus {
            installed_build: None,
            state: state.into(),
            tree_root: String::new(),
        };
        let recorded = std::collections::BTreeMap::from([
            (
                "--help".to_string(),
                row("error: --help is not named in the signed index"),
            ),
            (
                "ayy".to_string(),
                row("error: ayy is not named in the signed index"),
            ),
            ("ay".to_string(), row("active")),
            (
                "*toolset*".to_string(),
                row("unavailable: no build for this Mac"),
            ),
            ("retired".to_string(), row("active")),
        ]);
        let live = std::collections::BTreeMap::from([
            ("ay".to_string(), 18_u64),
            ("retired".to_string(), 5_u64),
        ]);
        let pruned = prunable_status_rows(&recorded, &live, &|n| n == "ay");

        assert_eq!(
            pruned,
            vec!["--help".to_string(), "ayy".to_string()],
            "only the rows naming nothing the index knows and nothing the store carries"
        );
        assert!(
            !pruned.contains(&"*toolset*".to_string()),
            "sentinel bookkeeping rows have their own owners and their own reapers"
        );
        assert!(
            !pruned.contains(&"retired".to_string()),
            "a program dropped upstream while still LIVE keeps its row — deleting it would \
             erase the attestation `atpkg verify` fails closed without"
        );
    }

    /// The silent update loop calls `record_status` once PER program; recording one
    /// must not wipe the others' last-known state (regression for the clobber bug).
    #[test]
    fn record_status_merges_programs_rather_than_clobbering() {
        let layout = temp_layout("merge");
        record_status(
            &layout,
            "ay",
            crate::ProgramStatus {
                installed_build: Some(18),
                state: "active".into(),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        record_status(
            &layout,
            "trust",
            crate::ProgramStatus {
                installed_build: Some(4821),
                state: "active".into(),
                tree_root: String::new(),
            },
            "staged 4821".into(),
        );
        let s = crate::status::read(&layout).expect("status present");
        assert!(
            s.programs.contains_key("ay"),
            "first program preserved across the second write"
        );
        assert!(s.programs.contains_key("trust"), "second program recorded");
        assert_eq!(s.programs["ay"].installed_build, Some(18));
        assert_eq!(s.programs["trust"].installed_build, Some(4821));
        assert_eq!(
            s.outcome, "staged 4821",
            "aggregate outcome reflects the latest pass"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A DIAGNOSTIC WRITE MUST NOT DESTROY A RECORD IT COULD NOT READ. An unreadable
    /// `status.toml` — a symlink (the `O_NOFOLLOW` open refuses it), a hand edit that
    /// broke the TOML, non-UTF-8 bytes, an oversize file — reads as `None`, exactly like
    /// an absent one, and the rewriting writers used to rename their single row over it:
    /// every other program's signed `tree_root` (so `atpkg verify` fails closed), the
    /// `seams` list `uninstall --all` walks to detach the rustup toolchain, and
    /// `last_success_at` (so every read-only verb says no check has ever run) all gone,
    /// with nothing on stderr. One offline tick was enough.
    #[test]
    fn status_writers_refuse_to_replace_an_unreadable_record() {
        let layout = temp_layout("unreadable-status");
        record_status(
            &layout,
            "trust",
            crate::ProgramStatus {
                installed_build: Some(6808),
                state: "active".into(),
                tree_root: "abc123".into(),
            },
            "up to date".into(),
        );
        record_success(&layout);
        let good = std::fs::read_to_string(layout.status()).unwrap();
        assert!(
            good.contains("abc123"),
            "the attestation is on disk to lose"
        );
        let junk = "this is not valid toml {{{\n";
        let corrupt = good + junk;
        std::fs::write(layout.status(), &corrupt).unwrap();

        // The failed-pass row: the exact write the 6-hour tick makes when it is offline.
        record_status(
            &layout,
            "*index*",
            crate::ProgramStatus {
                installed_build: None,
                state: "error: index unreachable".into(),
                tree_root: String::new(),
            },
            "update failed: index unreachable".into(),
        );
        assert_eq!(
            std::fs::read_to_string(layout.status()).unwrap(),
            corrupt,
            "record_status left the unreadable record alone"
        );
        record_outcome(&layout, "idle: no token".into());
        assert_eq!(
            std::fs::read_to_string(layout.status()).unwrap(),
            corrupt,
            "record_outcome left the unreadable record alone"
        );
        // Readable again once the operator drops the bad line, and nothing was lost.
        std::fs::write(layout.status(), &corrupt[..corrupt.len() - junk.len()]).unwrap();
        let back = crate::status::read(&layout).expect("the repaired record parses");
        assert_eq!(
            back.programs["trust"].tree_root, "abc123",
            "the signed root survived the failed pass"
        );
        assert!(
            !back.last_success_at.is_empty(),
            "and so did the stamp that keeps NEVER_CHECKED_LINE quiet"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// HELP AND THE NAME-OPERAND GATE MUST NOT DRIFT. [`VERB_USAGE`] is
    /// hand-written prose and [`NAME_TAKING_VERBS`] is what the dispatch edge
    /// accepts where a program name normally sits. A flag added to the second
    /// without the first is accepted-but-undocumented, precisely the state
    /// `--elevate` occupied when an audit concluded atpkg required sudo.
    ///
    /// Checks the option NAME, not the whole spelling: `install`'s usage collapses three
    /// values into `--elevate=sudo|osascript|never`, so `--elevate=never` is deliberately
    /// not a substring of it. Naming the option is what makes it discoverable; the values
    /// are the usage line's own business.
    #[test]
    fn name_operand_usage_names_every_accepted_flag() {
        for (verb, allowed) in NAME_TAKING_VERBS {
            let Some(usage) = usage_of(verb) else {
                panic!("{verb} takes flags but has no usage line");
            };
            for flag in *allowed {
                let name = flag.split('=').next().unwrap_or(flag);
                assert!(
                    usage.contains(name),
                    "`atpkg {verb}` accepts {flag} but its usage never names {name}:\n  {usage}"
                );
            }
        }
    }

    /// The visible option parsers outside the name-operand gate also name every
    /// accepted flag in their per-verb help. `--progress-file` and `--wait-lock` are
    /// omitted deliberately: they are hidden machine channels the GUI adds to its
    /// spawns and atpkg's own `main_entry` strips before verb dispatch, and neither
    /// is a user-facing CLI option.
    #[test]
    fn remaining_visible_option_usage_names_every_accepted_flag() {
        for (verb, flags) in [
            ("list", &["--porcelain"][..]),
            ("relocate", &["--sign", "--advisory"][..]),
            // Both of `noindex`'s flags are subcommand-specific, which is precisely why
            // the usage line has to name them: a reader who cannot see that `--depth`
            // belongs to `scan` learns it from a usage error instead.
            ("noindex", &["--depth", "--dry-run", "--all"][..]),
        ] {
            let usage = usage_of(verb).expect("visible option parser has usage");
            for flag in flags {
                assert!(
                    usage.contains(flag),
                    "`atpkg {verb}` accepts {flag} but its usage omits it:\n  {usage}"
                );
            }
        }
    }

    /// THE FIX THIS TEST PINS (2026-09-14): the `[machine]` settings run at the TOP of
    /// every pass, ahead of the first thing that can fail — never inside `if failures
    /// == 0`. They used to sit there, at the very end of `update`, which coupled a
    /// per-host preference to a completely clean toolchain pass: on a machine where one
    /// program aborted at stage, `last_success_at` was never written, the doctor said
    /// "the next update pass disables it" on every run, and nothing ever did.
    ///
    /// Scrapes this file's source, in the idiom of `verb_hint_matches_dispatch`: for each
    /// of the three passes the window runs, the call must appear BEFORE the pass's first
    /// `if failures == 0`, and there must be exactly one call per pass — a second,
    /// tail-of-pass copy is how the gate grows back.
    ///
    /// (The SECOND law below — that the apply is above the edge's own lock — lost
    /// its test to a deletion that left this `#[test]` and both docs orphaned onto
    /// one function; `duplicated attribute` is what found it. The surviving body is
    /// this first law's. Restoring the second is a separate job: it needs a test
    /// written, not a doc moved.)
    /// THE EDGE IS ABOVE EVERY BODY, SO THE APPLY MUST BE ABOVE THE EDGE'S LOCK.
    ///
    /// `machine_settings_apply_before_anything_can_fail_in_every_pass` scrapes the pass
    /// BODIES, and a body is entered only after `mutator_store_lock()` has succeeded —
    /// so it could not see the failure it was written to prevent: a launch whose seed
    /// queued behind another holder waited the whole `--wait-lock` bound (the window
    /// passes 30 minutes) before the host settings were touched, and a pass refused at
    /// the edge (a wedged holder with `auto_update = false`, an unwritable prefix)
    /// applied nothing at all. This pins the ordering the edge itself must hold.
    #[test]
    fn the_machine_settings_are_applied_before_the_store_lock_is_taken() {
        let src = include_str!("cli.rs");
        let entry = src
            .find("\npub fn main_entry(")
            .expect("main_entry is the dispatch edge");
        let body = &src[entry..];
        let apply = body
            .find("apply_machine_settings_once()")
            .expect("the edge applies the [machine] settings");
        let lock = body
            .find("mutator_store_lock()")
            .expect("the edge takes the store lock");
        assert!(
            apply < lock,
            "the [machine] settings need no store lock and must not wait for one: the \
             apply is at byte {apply} of main_entry and the lock at {lock}"
        );
        // And the verb roster that triggers it is the long passes, never a quick
        // mutator — a `$HOME` scan bolted onto `pin` is a cost nobody asked for.
        for verb in ["install", "seed", "update"] {
            assert!(
                verb_applies_machine_settings(verb),
                "{verb} runs a pass that owes the machine its settings"
            );
            assert!(verb_mutates_store(verb), "{verb} also takes the lock");
        }
        for verb in ["pin", "unpin", "gc", "link", "unlink", "refresh", "repair"] {
            assert!(
                !verb_applies_machine_settings(verb),
                "{verb} is a quick mutator: it must not carry the scan"
            );
        }
        assert!(
            !verb_applies_machine_settings("machine"),
            "`machine` takes no store lock, so the edge never applies for it — its own \
             handler calls the RAW apply so an explicit gesture always re-reads"
        );
    }

    /// The explicit gesture re-applies; the passes replay. `machine apply` must reach
    /// the raw function, or a second press in one process would report a cached answer.
    #[test]
    fn the_explicit_gesture_does_not_come_through_the_once_wrapper() {
        let src = include_str!("cli.rs");
        let start = src.find("\nfn cmd_machine(").expect("cmd_machine");
        let rest = &src[start + 1..];
        let end = rest[3..].find("\nfn ").map_or(rest.len(), |i| i + 3);
        let body = &rest[..end];
        assert!(
            body.contains("let entries = apply_machine_settings();"),
            "cmd_machine's apply arm must call the raw apply"
        );
        assert!(
            !body.contains("let entries = apply_machine_settings_once();"),
            "a cached answer is not an apply"
        );
    }

    #[test]
    fn machine_settings_apply_before_anything_can_fail_in_every_pass() {
        let src = include_str!("cli.rs");
        let body_of = |name: &str| -> &str {
            let start = src
                .find(&format!("\nfn {name}("))
                .unwrap_or_else(|| panic!("{name} not found"));
            let rest = &src[start + 1..];
            // A fn body ends at the next top-level `fn ` — every pass here is followed
            // by one.
            let end = rest[3..].find("\nfn ").map_or(rest.len(), |i| i + 3);
            &rest[..end]
        };
        // Per pass: how many GATES (`if`, `match`, `let … else`) and how many `return`s
        // at any indent may precede the call. update and install: NONE — the call is the
        // first statement. seed: its usage check (`if` + `return`) and its `layout()`
        // let-else (a gate + a `return`), both of which are refusals of the INVOCATION,
        // not of the machine.
        let allowed = [
            ("cmd_update_all_code", 0usize, 0usize),
            ("cmd_install_default_set_code", 0, 0),
            ("cmd_seed", 2, 2),
        ];
        for (pass, ifs, returns) in allowed {
            let body = body_of(pass);
            let calls: Vec<usize> = body
                .match_indices("apply_machine_settings_once()")
                .map(|(i, _)| i)
                .collect();
            assert_eq!(
                calls.len(),
                1,
                "{pass}: exactly one apply_machine_settings_once() call, at the top — found {}",
                calls.len()
            );
            let pre = &body[..calls[0]];
            // EVERY return, at ANY indent, and every gate shape — not just `if`. The
            // old count looked for `\n    if ` and an 8-space `return `, so a gate
            // written as a `match` (or a return nested one level deeper) was invisible
            // to the very test that exists to keep gates off this path.
            let found_returns = pre
                .lines()
                .filter(|line| {
                    let t = line.trim_start();
                    t.starts_with("return ") || t.starts_with("return;") || t == "return"
                })
                .count();
            let gates = pre
                .lines()
                .filter(|line| {
                    let t = line.trim_start();
                    t.starts_with("if ")
                        || t.starts_with("match ")
                        || t.starts_with("} else if ")
                        || (t.starts_with("let ") && t.contains(" else "))
                        || (t.starts_with("let ") && t.ends_with("match {"))
                })
                .count();
            assert_eq!(
                (gates, found_returns),
                (ifs, returns),
                "{pass}: a new gate or return above the machine settings — they must be the \
                 first thing that can act"
            );
            // The CODE form — indented, braced — so a comment quoting the old gate is
            // not what gets found.
            if let Some(gate) = body.find("\n    if failures == 0 {") {
                assert!(
                    calls[0] < gate,
                    "{pass}: the machine settings must apply BEFORE `if failures == 0`, not \
                     inside it"
                );
            }
            if let Some(gate) = body.find("if !manager_enabled()") {
                assert!(
                    calls[0] < gate,
                    "{pass}: apply_machine_settings_once() must precede the manager gate — host \
                     settings are not a network verb"
                );
            }
            // And before the network: the first fetcher/index touch in every pass. The
            // probes are CODE shapes, so a comment mentioning a fetcher cannot match.
            for touch in ["let fetcher =", "resolve_fetcher(", "resolve_index", "net_"] {
                if let Some(at) = body.find(touch) {
                    assert!(
                        calls[0] < at,
                        "{pass}: apply_machine_settings_once() must precede `{touch}`"
                    );
                }
            }
        }
    }

    /// `apply_machine_settings` prints the `machine-state:` record after its
    /// `machine-settings:` change line, from the same builder bare `atpkg machine` uses, so a
    /// window that reads the change has the state a line later and spawns no second walk of
    /// `$HOME`. Pinned on the body's order of prints, which the window's card depends on.
    #[test]
    fn the_apply_prints_its_state_record_after_its_change_line() {
        let src = include_str!("cli.rs");
        let start = src.find("\nfn apply_machine_settings(").unwrap();
        let end = src[start..].find("\n}\n").map(|i| start + i).unwrap();
        let body = &src[start..end];
        let change = body
            .find("machine_settings_line(&entries)")
            .expect("the apply prints its change line");
        let record = body
            .find("print_machine_state_after_apply(")
            .expect("the apply prints its own machine-state: record");
        assert!(
            record > change,
            "the record must FOLLOW the change line: the window opens its expectation on \
             the change and answers it with the record"
        );
        let printer = src.find("\nfn print_machine_state_after_apply(").unwrap();
        let printer_end = src[printer..].find("\n}\n").map(|i| printer + i).unwrap();
        assert!(
            src[printer..printer_end].contains("MACHINE_STATE_MARKER"),
            "the pass's record rides the read's own marker, so one parser serves both"
        );
        assert!(
            src[printer..printer_end].contains("crate::machine::machine_state_line("),
            "…and its own builder"
        );
        // Once: `universal_control()` warns on stderr about a spelling it does not know, so
        // the record must reuse the apply's answer rather than ask again.
        assert_eq!(
            body.matches("cfg.universal_control()").count(),
            1,
            "apply_machine_settings reads the policy once and hands it to the record"
        );
        assert!(
            !src[printer..printer_end].contains("universal_control()"),
            "the printer takes the policy, never re-reads it"
        );
    }

    /// A pass that moved nothing and failed nothing reports from the walk it made: its scan's
    /// counts, and nothing left to migrate — every exposed dir it left was a refusal a dry run
    /// refuses identically. A pass that moved something walks again, because the pre-apply
    /// scan no longer describes the disk.
    #[cfg(target_os = "macos")]
    #[test]
    fn the_census_after_an_apply_is_the_scan_unless_something_moved() {
        use crate::noindex::{Applied, Budget, CACHEDIR_TAG_SIGNATURE, ConfigNote, DOCTOR_DEPTH};
        let home =
            std::env::temp_dir().join(format!("atpkg-census-after-apply-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&home);
        // One repo whose target dir is exposed by name.
        let repo = home.join("repo");
        let target = repo.join("target");
        std::fs::create_dir_all(&target).unwrap();
        std::fs::write(repo.join("Cargo.toml"), "[package]\nname = \"x\"\n").unwrap();
        std::fs::write(
            target.join("CACHEDIR.TAG"),
            format!("{CACHEDIR_TAG_SIGNATURE}\n# cargo\n"),
        )
        .unwrap();
        let scan = crate::noindex::scan(&home, DOCTOR_DEPTH, &Budget::DOCTOR);
        assert_eq!(
            scan.counts(),
            (1, 0),
            "precondition: one exposed target dir"
        );
        // Nothing moved (say, a live build refused it): the scan is the answer, and
        // nothing is left for an apply to do.
        let refused = vec![Applied::Skipped {
            path: target.clone(),
            reason: "a build holds it".into(),
        }];
        let census = spotlight_after_apply(&home, &scan, &refused);
        assert_eq!(
            (
                census.exposed,
                census.hidden,
                census.would_migrate,
                census.complete
            ),
            (1, 0, 0, true)
        );
        // Something moved: the disk changed under the scan, so the census walks again
        // and reports what is there now.
        let hidden = repo.join("target.noindex");
        std::fs::rename(&target, &hidden).unwrap();
        let moved = vec![Applied::Migrated {
            from: target.clone(),
            to: hidden.clone(),
            config: ConfigNote::Untouched("test".into()),
        }];
        let census = spotlight_after_apply(&home, &scan, &moved);
        assert_eq!(
            (census.exposed, census.hidden, census.would_migrate),
            (0, 1, 0),
            "after the move the record must say hidden, from a fresh walk"
        );
        let _ = std::fs::remove_dir_all(&home);
    }

    /// The synthetic-home rule, on its inputs: the same path is the real machine; any
    /// other path is not, and the reason names both so a `sudo -H` reader learns why.
    /// `defaults` ignores `$HOME` (measured 2026-09-14), which is why this exists.
    #[test]
    fn a_redirected_home_is_a_synthetic_machine() {
        use std::path::Path;
        assert_eq!(
            machine_home_mismatch_between(Path::new("/Users//me"), Path::new("/Users//me")),
            None
        );
        let why = machine_home_mismatch_between(Path::new("/tmp/fixture"), Path::new("/Users//me"))
            .expect("a temp HOME is not the account home");
        assert!(
            why.contains("/tmp/fixture") && why.contains("/Users//me"),
            "{why}"
        );
        assert!(why.contains("follow the account"), "{why}");
    }

    /// FAIL-CLOSED: an account that cannot be resolved is not proof that HOME is it.
    /// (Some, Some) same → account; different → mismatch naming both; (Some, None) →
    /// unresolved, naming the reason; (None, _) → the account (an unset HOME falls back
    /// to the passwd entry in `home_dir()`).
    #[test]
    fn an_unresolvable_account_is_a_synthetic_machine() {
        use crate::machine::HomePosture;
        use std::path::Path;
        let me = Path::new("/Users//me");
        let fixture = Path::new("/tmp/fixture");
        assert_eq!(
            machine_home_verdict(Some(me), Some(me), false),
            (HomePosture::Account, None)
        );
        assert_eq!(
            machine_home_verdict(Some(fixture), Some(me), true).0,
            HomePosture::Account,
            "the filesystem's answer wins over the spelling"
        );
        let (p, why) = machine_home_verdict(Some(fixture), Some(me), false);
        assert_eq!(p, HomePosture::Mismatch);
        let why = why.unwrap();
        assert!(
            why.contains("/tmp/fixture") && why.contains("/Users//me"),
            "{why}"
        );
        let (p, why) = machine_home_verdict(Some(fixture), None, false);
        assert_eq!(p, HomePosture::Unresolved);
        assert!(why.unwrap().contains("could not be resolved"));
        assert_eq!(
            machine_home_verdict(None, Some(me), false).0,
            HomePosture::Account
        );
        assert_eq!(
            machine_home_verdict(None, None, false).0,
            HomePosture::Account
        );
    }

    /// The same directory under another spelling IS the real home; a different one, or
    /// a missing one, is not.
    #[cfg(unix)]
    #[test]
    fn the_same_directory_under_another_spelling_is_the_real_home() {
        // PID-scoped, like every other temp fixture in this crate: with fixed path strings a
        // second run of this suite on the same machine raced this one on one directory and one
        // symlink name, and the loser panicked in `symlink(...).unwrap()`.
        let t =
            std::env::temp_dir().join(format!("atpkg-same-directory-test-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&t);
        std::fs::create_dir_all(&t).unwrap();
        let link =
            std::env::temp_dir().join(format!("atpkg-same-directory-link-{}", std::process::id()));
        let _ = std::fs::remove_file(&link);
        std::os::unix::fs::symlink(&t, &link).unwrap();
        assert!(same_directory(&link, &t), "a symlink to it is it");
        assert!(same_directory(&t.join("."), &t), "a dotted spelling is it");
        assert!(
            !same_directory(&t, &std::env::temp_dir()),
            "the parent is not it"
        );
        assert!(
            !same_directory(&t.join("missing"), &t),
            "a missing path proves nothing"
        );
        let _ = std::fs::remove_file(&link);
        let _ = std::fs::remove_dir_all(&t);
    }

    /// The SUPPORTED gate precedes the home guard in `apply_machine_settings`, so a
    /// Linux pass with a redirected HOME prints nothing about settings it does not have.
    #[test]
    fn the_platform_gate_precedes_the_home_guard() {
        let src = include_str!("cli.rs");
        let start = src.find("\nfn apply_machine_settings(").unwrap();
        let body = &src[start..];
        let body = &body[..body[3..].find("\nfn ").map_or(body.len(), |i| i + 3)];
        let gate = body
            .find("if !crate::noindex::SUPPORTED {")
            .expect("the platform gate");
        // `machine_refusal` carries both refusals now — the unreadable config and the
        // synthetic home — and it is still the thing the platform gate must precede.
        let guard = body.find("machine_refusal(").expect("the refusal guard");
        assert!(
            gate < guard,
            "SUPPORTED must be checked before the refusal guard speaks"
        );
    }

    /// A CONFIG THAT DOES NOT PARSE CHANGES NOTHING. Both `[machine]` defaults ACT, so
    /// "malformed ⇒ defaults" meant a typo three tables away silently re-enabled what the
    /// owner had switched off. The refusal is first — before the home question — because
    /// it is the cheaper one and the one the owner can fix.
    #[test]
    fn an_unreadable_config_refuses_before_anything_is_measured() {
        let unreadable = crate::config::MachineConfig {
            unreadable: true,
            ..crate::config::MachineConfig::default()
        };
        let reason = machine_refusal(&unreadable).expect("an unreadable config refuses");
        assert!(reason.contains("does not parse"), "{reason}");
        assert!(reason.contains("aterm pkg machine apply"), "{reason}");
        // And it refuses even though the resolved values look like the defaults.
        assert!(unreadable.spotlight_noindex());
        assert_eq!(
            unreadable.universal_control(),
            crate::config::UniversalControlPolicy::Off
        );
    }

    /// `machine`'s grammar is two spellings and nothing else: bare (read) and `apply`.
    /// Anything more is a usage error, so a typo never silently reads when someone meant
    /// to apply.
    #[test]
    fn machine_grammar_is_exact() {
        // Both accepted spellings are answered by `--help` without acting, like every
        // verb; and a third word is refused. The read/apply bodies touch the real
        // machine, so they are exercised by hand, not here.
        let argv = |s: &str| -> Vec<String> { s.split_whitespace().map(String::from).collect() };
        assert_eq!(
            cmd_machine(&argv("check")),
            ExitCode::from(2),
            "`check` is not a word: bare is the read"
        );
        assert_eq!(cmd_machine(&argv("apply --dry-run")), ExitCode::from(2));
        assert_eq!(cmd_machine(&argv("apply extra")), ExitCode::from(2));
        assert!(usage_of("machine").is_some_and(|u| u.contains("apply")));
    }

    /// The `noindex` grammar, as a table: the default subcommand (`scan`), the default
    /// root (the cwd — left `None` by the parser so it stays pure), the default depth,
    /// and the part actually worth pinning — a flag on the WRONG subcommand is a usage
    /// error, not a silent ignore.
    ///
    /// A table rather than a run, because the alternative is discovering this grammar by
    /// invoking a verb whose `verify` form waits 20 seconds on Spotlight and whose
    /// `migrate` form renames a directory.
    #[test]
    fn noindex_grammar_is_exact() {
        let argv = |s: &str| -> Vec<String> { s.split_whitespace().map(String::from).collect() };
        let job = |s: &str| parse_noindex(&argv(s)).ok();
        let scan = |root: Option<&str>, depth: usize| {
            Some(NoindexJob::Scan {
                root: root.map(ToOwned::to_owned),
                depth,
            })
        };
        // Bare `noindex` is a scan of the cwd at the verb's own depth — the subcommand
        // and the root are both optional, and `scan` is the default.
        assert_eq!(job(""), scan(None, crate::noindex::VERB_DEPTH));
        assert_eq!(job("scan"), scan(None, crate::noindex::VERB_DEPTH));
        assert_eq!(job("/src"), scan(Some("/src"), crate::noindex::VERB_DEPTH));
        assert_eq!(job("scan /src --depth 2"), scan(Some("/src"), 2));
        assert_eq!(job("--depth 2 /src"), scan(Some("/src"), 2));
        assert_eq!(
            job("verify /src/target"),
            Some(NoindexJob::Verify {
                dir: String::from("/src/target")
            })
        );
        assert_eq!(
            job("migrate /src/target --dry-run"),
            Some(NoindexJob::Migrate {
                dir: String::from("/src/target"),
                dry_run: true
            })
        );
        // A directory named like a subcommand is still a directory once the subcommand
        // slot is filled.
        assert_eq!(
            job("migrate scan"),
            Some(NoindexJob::Migrate {
                dir: String::from("scan"),
                dry_run: false
            })
        );
        // `apply`: `--all` (the doctor's scan) or any number of directories, dry-runnable.
        assert_eq!(
            job("apply --all"),
            Some(NoindexJob::Apply {
                all: true,
                dirs: vec![],
                dry_run: false
            })
        );
        assert_eq!(
            job("apply --all --dry-run"),
            Some(NoindexJob::Apply {
                all: true,
                dirs: vec![],
                dry_run: true
            })
        );
        assert_eq!(
            job("apply /a/target /b/target"),
            Some(NoindexJob::Apply {
                all: false,
                dirs: vec![String::from("/a/target"), String::from("/b/target")],
                dry_run: false
            })
        );
        for wrong in [
            // `apply` wants --all or directories, never both, never neither, no --depth.
            "apply",
            "apply --all /a",
            "apply /a --depth 2",
            "apply --all --depth 2",
            "scan --all",
            "migrate /d --all",
            // A flag on the wrong subcommand: refused, never accepted-and-ignored. The
            // ignored spelling would answer "nothing would change" about a form that
            // changes nothing anyway — true, and the wrong answer to what was asked.
            "scan --dry-run",
            "verify /d --dry-run",
            "verify /d --depth 3",
            "migrate /d --depth 3",
            // The operand these two cannot do without. There is no sensible default
            // directory to rename or probe.
            "verify",
            "migrate",
            // `--depth` wants a number in range and says so, rather than falling back to
            // the default: `--depth 0` would walk nothing and report a clean tree.
            "--depth",
            "--depth 0",
            "--depth 33",
            "--depth six",
            // Unknown flags and second operands are errors, not leftovers.
            "--json",
            "scan /a /b",
        ] {
            assert!(
                parse_noindex(&argv(wrong)).is_err(),
                "`atpkg noindex {wrong}` must be a usage error, not a silent default"
            );
        }
    }

    /// `noindex`'s `args[1]` is a SUBCOMMAND, not a program name, so it must stay out of
    /// [`NAME_TAKING_VERBS`] exactly as `relocate` does. In that table the dispatch edge
    /// refuses any unlisted `-`-leading first operand before the verb body runs, so
    /// `atpkg noindex --depth 3` would die as "unknown option for `atpkg noindex`" while
    /// `--depth` is a documented flag of the verb — the same shape as the audit finding
    /// where `--elevate` was real, documented, and invisible at the edge.
    #[test]
    fn noindex_owns_its_own_flags_at_the_dispatch_edge() {
        assert!(
            !NAME_TAKING_VERBS.iter().any(|(name, _)| *name == "noindex"),
            "noindex reads args[1] as a subcommand, not a program name"
        );
        assert!(
            parse_noindex(&[String::from("--depth"), String::from("3")]).is_ok(),
            "a leading flag is the verb's own business"
        );
    }

    /// Every dispatchable verb must be able to say what it accepts. The audit
    /// that prompted this found 35 of 55 CLI verbs unable to answer that basic
    /// question; a new verb must not be able to land in the same state.
    #[test]
    fn every_verb_has_a_usage_line() {
        for verb in VERBS {
            assert!(
                usage_of(verb).is_some(),
                "verb `{verb}` has no VERB_USAGE entry, so `atpkg {verb} --help` \
                 cannot say what it accepts"
            );
        }
        // And nothing in the table names a verb that does not dispatch.
        for (name, _) in VERB_USAGE {
            assert!(
                VERBS.contains(name),
                "VERB_USAGE names `{name}`, which is not a dispatchable verb"
            );
        }
    }

    /// `--help` must ANSWER, never act. `gc --help` used to reclaim the rollback
    /// window; a third of the audited surface did something when handed `--help`.
    #[test]
    fn help_flag_is_answered_for_every_verb_and_never_dispatches() {
        for verb in VERBS {
            for spelling in ["--help", "-h"] {
                let argv: Vec<std::ffi::OsString> = vec![(*verb).into(), spelling.into()];
                // Reaching a verb body would need the store lock or the network;
                // returning SUCCESS here is the observable proof it short-circuited.
                let code = main_entry(argv);
                assert_eq!(
                    format!("{code:?}"),
                    format!("{:?}", ExitCode::SUCCESS),
                    "`atpkg {verb} {spelling}` must answer with usage and exit 0"
                );
            }
        }
    }

    /// A `--help` that belongs to the TOOL is forwarded, not answered: `run` looks
    /// only at its first operand, and a literal `--` ends the scan for every verb.
    #[test]
    fn a_tools_own_help_flag_is_forwarded_by_run_and_stopped_by_a_separator() {
        let s = |xs: &[&str]| xs.iter().map(|x| (*x).to_string()).collect::<Vec<_>>();
        assert!(help_flag_addresses_atpkg("run", &s(&["--help"])));
        assert!(help_flag_addresses_atpkg("run", &s(&["-h"])));
        assert!(!help_flag_addresses_atpkg("run", &s(&["rg", "--help"])));
        assert!(!help_flag_addresses_atpkg(
            "run",
            &s(&["rg", "--", "--help"])
        ));
        assert!(!help_flag_addresses_atpkg("run", &s(&[])));
        assert!(help_flag_addresses_atpkg("install", &s(&["ay", "--help"])));
        assert!(!help_flag_addresses_atpkg("install", &s(&["--", "--help"])));
        assert!(!help_flag_addresses_atpkg("install", &s(&[])));
    }

    /// THE single-writer verb roster ([`crate::lock`]): every verb that mutates
    /// the store (stage/activate/discard, shims, links, pins, gc) takes the
    /// store-wide lock at the dispatch edge; every read-only verb stays
    /// lock-free. Exhaustive over the dispatch table so a new verb must be
    /// classified deliberately.
    #[test]
    fn store_lock_verb_roster_is_exact() {
        // EXHAUSTIVE over VERBS: a verb added to the dispatch without a place in
        // one of these two lists fails here rather than silently defaulting to
        // lock-free. The `seed` verb landed while this test only enumerated the
        // verbs it already knew, so it classified nothing and proved nothing
        // about the new store mutator.
        let mut classified: std::collections::BTreeSet<&str> = std::collections::BTreeSet::new();
        for mutator in [
            "install",
            "seed",
            "update",
            "rollback",
            "uninstall",
            "gc",
            "link",
            "unlink",
            "refresh",
            "pin",
            "unpin",
            // `repair` re-lays shims, so it writes the store and must hold the lock.
            "repair",
        ] {
            classified.insert(mutator);
            assert!(
                verb_mutates_store(mutator),
                "{mutator} mutates the store and must take the lock"
            );
        }
        for read_only in [
            "doctor",
            // `status` is the same read-only report under the name people reach for.
            "status",
            "which",
            "list",
            "run",
            "verify",
            "tree-root",
            "verify-index",
            "verify-pkg",
            "relocate",
            // `noindex` writes nothing under <prefix> — its probe file is removed by a
            // Drop guard and its one rename lives inside the tree the user named — so a
            // storage-hygiene scan never queues behind an install.
            "noindex",
            // `machine` writes a per-host preference and renames build output in the
            // user's own trees; nothing under <prefix> moves.
            "machine",
            "help",
            "-h",
            "--help",
        ] {
            classified.insert(read_only);
            assert!(
                !verb_mutates_store(read_only),
                "{read_only} is read-only and must stay lock-free"
            );
        }
        let unclassified: Vec<&&str> = VERBS.iter().filter(|v| !classified.contains(*v)).collect();
        assert!(
            unclassified.is_empty(),
            "every advertised verb must be classified as a store mutator or read-only here \
             (a new verb defaults to lock-free, which is the dangerous direction): \
             {unclassified:?}"
        );
    }

    /// The bundled seed is a BOOTSTRAP source and nothing else (§9.1) — the
    /// premise both 2026-07-30 teeth rest on. A populated store must never
    /// re-arm the chain: that is what kept sealed pins from rolling installed
    /// builds backwards on the unattended pass, and what keeps a seed-leg
    /// success from standing in for a network answer once there is real state
    /// to protect. Pinned here because the inline version of this rule shipped
    /// twice with no test.
    #[test]
    fn the_seed_leg_joins_only_on_an_empty_store() {
        let seed =
            std::path::PathBuf::from("/Applications/aterm.app/Contents/Resources/toolchain-seed");
        assert_eq!(
            seed_bootstrap_leg(Some(seed.clone()), true),
            Some(seed.clone()),
            "an empty store with a sealed seed is exactly the bootstrap case"
        );
        assert_eq!(
            seed_bootstrap_leg(Some(seed), false),
            None,
            "a populated store must NOT re-arm the seed chain — the seed is never an \
             update source (downgrade + cache-masking teeth, adversarial review 2026-07-30)"
        );
        // No seed sealed: network-only in both store states, never a chain over
        // a path that does not exist.
        assert_eq!(seed_bootstrap_leg(None, true), None);
        assert_eq!(seed_bootstrap_leg(None, false), None);
    }

    /// A PROVISIONED MAC'S LAUNCH NEVER TOUCHES THE NETWORK FOR ITS SEEDLESS SENTENCE.
    /// Every launch since v0.63.0 is seedless, and `report_seedless_posture` resolved the
    /// signed index (a metered releases listing, verification, a floor record — under the
    /// store lock) BEFORE asking whether anything was installed, though for a non-empty
    /// store both outcomes printed the same line. The resolve now runs only for an empty
    /// store, where its verdict is the point.
    #[test]
    fn the_seedless_lane_resolves_the_index_only_for_an_empty_store() {
        use std::cell::Cell;
        let calls = Cell::new(0u32);
        let resolve = |answer: Option<bool>| {
            let calls = &calls;
            move || {
                calls.set(calls.get() + 1);
                answer
            }
        };
        // A store that holds builds is served, whatever the index would have said — and
        // the index is never asked.
        for answer in [Some(true), Some(false), None] {
            assert_eq!(seedless_serves_us(true, resolve(answer)), Some(true));
        }
        assert_eq!(
            calls.get(),
            0,
            "a provisioned store must not resolve the index for the seedless sentence"
        );
        // An empty store asks exactly once and reports what the index said.
        for answer in [Some(true), Some(false), None] {
            assert_eq!(seedless_serves_us(false, resolve(answer)), answer);
        }
        assert_eq!(calls.get(), 3, "an empty store resolves once per verdict");

        // NON-VACUITY: the verb routes through the helper, feeds it the shim view, and
        // builds the fetcher and resolves the index only INSIDE the deferred closure.
        let src = include_str!("cli.rs");
        let production = production_half(src);
        let start = production
            .find("fn report_seedless_posture")
            .expect("the seedless posture reporter");
        let end = production[start..]
            .find("\nfn ")
            .map(|i| start + i)
            .expect("a function after report_seedless_posture");
        let body = &production[start..end];
        let routed = whole_calls(body, "seedless_serves_us(");
        assert_eq!(
            routed.len(),
            1,
            "report_seedless_posture must decide through seedless_serves_us: {body}"
        );
        let call = &routed[0];
        let holds = call
            .find("!crate::active_builds(layout).is_empty()")
            .expect("the store-holds-builds argument is the shim view");
        let fetcher = call
            .find("resolve_fetcher(layout)")
            .expect("the fetcher is built inside the call");
        assert!(
            holds < fetcher,
            "the store question is answered before the fetcher exists"
        );
        let outside = body.replace(call.as_str(), "");
        for costly in [
            "resolve_fetcher(",
            "resolve_verified_index(",
            "seed_serviceable(",
        ] {
            assert!(
                !outside.contains(costly),
                "{costly} must run only inside the deferred resolve, never ahead of the \
                 store question"
            );
        }
    }

    /// ADOPTION is what keeps a user's toolchain the WHOLE toolchain as the suite grows
    /// (§11): consent to a multi-GB download is asked once (`auto_install`), but a machine
    /// that already runs the set gets newly-published members without being asked again.
    /// The lifecycle is small enough to pin exactly, and getting it wrong is silent in both
    /// directions — a missing marker decays the toolset, a sticky one reinstalls what the
    /// user deliberately removed.
    #[test]
    fn adoption_is_recorded_by_the_whole_set_and_forgotten_by_uninstall() {
        let dir = std::env::temp_dir().join(format!("atpkg-adopt-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&dir);
        std::fs::create_dir_all(&dir).unwrap();
        let layout = crate::store::Layout {
            prefix: dir.clone(),
        };

        // A fresh store has NOT adopted: the consent question is still open.
        assert!(!adopted(&layout), "a fresh store has adopted nothing");

        // A whole-set install records it, and is idempotent.
        record_adoption(&layout);
        assert!(adopted(&layout), "the whole-set install adopts");
        let first = std::fs::read(layout.adopted()).unwrap();
        record_adoption(&layout);
        assert_eq!(
            std::fs::read(layout.adopted()).unwrap(),
            first,
            "re-adopting rewrites nothing"
        );

        // The marker is 0600 like every other durable store file.
        #[cfg(unix)]
        {
            use std::os::unix::fs::PermissionsExt as _;
            let mode = std::fs::metadata(layout.adopted())
                .unwrap()
                .permissions()
                .mode();
            assert_eq!(mode & 0o777, 0o600, "adoption marker must be 0600");
        }

        // Adoption is the file's EXISTENCE — never a parsed value, so a garbled or
        // truncated write can never read as some other answer.
        std::fs::write(layout.adopted(), b"\0\0garbage").unwrap();
        assert!(adopted(&layout), "existence is the whole predicate");

        // Uninstall forgets it: set-completion must never undo an explicit removal.
        clear_adoption(&layout);
        assert!(!adopted(&layout), "uninstall opts out of set completion");
        // Clearing what is already clear is not an error.
        clear_adoption(&layout);

        // The whole-set verb's decision table: an install adopts; a set found ALREADY
        // COMPLETE adopts (the re-adopt the uninstall message and doctor's re-seed remedy
        // both lead to — it recorded nothing until 2026-09-14); a machine with no builds
        // before and none after, a failed member, a skipped member or an owed member does
        // not.
        let none: Vec<String> = Vec::new();
        let some = vec![String::from("trust")];
        assert!(
            default_set_adopts(1, 0, false, &none, &none),
            "installed: adopts"
        );
        assert!(
            default_set_adopts(1, 2, true, &some, &some),
            "installed something: adopts"
        );
        assert!(
            default_set_adopts(0, 0, true, &none, &none),
            "already complete: adopts"
        );
        assert!(
            !default_set_adopts(0, 0, false, &none, &none),
            "no toolset before or after: not adopted"
        );
        assert!(
            !default_set_adopts(0, 1, true, &none, &none),
            "a failure: not adopted"
        );
        assert!(
            !default_set_adopts(0, 0, true, &some, &none),
            "a skip: not adopted"
        );
        assert!(
            !default_set_adopts(0, 0, true, &none, &some),
            "owed: not adopted"
        );
        assert!(!adopted(&layout));

        let _ = std::fs::remove_dir_all(&dir);
    }

    /// THE SEED'S DISK REFUSAL MUST NOT LEAVE ADOPTION BEHIND. `cmd_seed` adopts before
    /// its whole-set disk gate, and the GUI runs `atpkg update` seconds after the seed:
    /// an adopted store made that pass complete the set over the network, member by
    /// member, so the small solvers arrived and the trust tuple failed its own gate —
    /// the partial state the refusal exists to prevent.
    #[test]
    fn a_seed_disk_refusal_takes_back_the_adoption_it_recorded() {
        const GIB: u64 = 1 << 30;
        let layout = temp_layout("seed-disk-refusal");

        // First launch, ~3 GiB free, a 4 GiB seal: this run adopted, then refused.
        let adopted_by_this_run = !adopted(&layout);
        assert!(adopted_by_this_run, "a fresh store is adopted by this run");
        record_adoption(&layout);
        assert!(refuse_seed_for_disk(
            &layout,
            4 * GIB,
            Some(3 * GIB),
            adopted_by_this_run
        ));
        assert!(
            !adopted(&layout),
            "the refusal takes back this run's adoption"
        );
        assert!(
            !should_complete_set(false, adopted(&layout), declined(&layout), false),
            "the update pass after the seed must not complete the set over the network"
        );
        let status = crate::status::read(&layout).expect("the refusal is recorded");
        assert_eq!(
            status.programs["*toolset*"].state,
            "blocked: insufficient disk space"
        );

        // A seal that fits, or free space that cannot be read, is not a refusal and
        // leaves adoption alone.
        record_adoption(&layout);
        assert!(!refuse_seed_for_disk(&layout, GIB, Some(10 * GIB), true));
        assert!(!refuse_seed_for_disk(&layout, 4 * GIB, None, true));
        assert!(adopted(&layout), "no refusal, no change");

        // Adopted before this run: the shortfall is still refused, but it never
        // revokes adoption this lane did not record.
        assert!(refuse_seed_for_disk(&layout, 4 * GIB, Some(3 * GIB), false));
        assert!(adopted(&layout), "an earlier adoption survives the refusal");

        // NON-VACUITY: `cmd_seed` routes its gate through the helper, and captures the
        // flag BEFORE it records adoption (after, it would always read false).
        let src = include_str!("cli.rs");
        let production = production_half(src);
        let start = production.find("fn cmd_seed").expect("the seed verb");
        let end = production[start..]
            .find("\nfn ")
            .map(|i| start + i)
            .expect("a function after cmd_seed");
        let seed_body = &production[start..end];
        let flag = seed_body
            .find("let adopted_by_this_run = !adopted(&layout);")
            .expect("cmd_seed captures whether this run adopts");
        // The seed lane's adoption is [`record_seed_adoption`] — `record_adoption` plus the
        // record of who adopted — so the name searched for is that one.
        let adopt = seed_body
            .find("record_seed_adoption(&layout);")
            .expect("cmd_seed adopts");
        assert!(
            flag < adopt,
            "the flag must be read before adoption is recorded"
        );
        assert!(
            seed_body.contains("refuse_seed_for_disk(&layout, need, have, adopted_by_this_run)"),
            "cmd_seed's disk gate must go through refuse_seed_for_disk"
        );
        assert!(
            !seed_body.contains("\"blocked: insufficient disk space\""),
            "the blocked row has one writer, the helper"
        );

        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A TARGETED removal must stick too. The resumable seed lane installs whatever
    /// the store lacks, so without a per-program record `atpkg uninstall ny` came
    /// back on the next launch — the manager undoing a deliberate act, which is the
    /// single most infuriating thing a package manager can do. Asking for it back
    /// lifts the record, so nobody has to learn a marker file exists.
    #[test]
    fn a_single_program_removal_is_not_undone_by_the_seed_lane() {
        let dir = std::env::temp_dir().join(format!("atpkg-rm-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&dir);
        std::fs::create_dir_all(&dir).unwrap();
        let layout = crate::store::Layout {
            prefix: dir.clone(),
        };

        assert!(removed_programs(&layout).is_empty());
        record_removed(&layout, "ny");
        record_removed(&layout, "clean");
        record_removed(&layout, "ny"); // idempotent
        let rm = removed_programs(&layout);
        assert!(rm.contains("ny") && rm.contains("clean"), "{rm:?}");
        assert_eq!(rm.len(), 2, "no duplicate from the repeat: {rm:?}");
        // The comment header must never read as a program name.
        assert!(!rm.iter().any(|p| p.starts_with('#')), "{rm:?}");

        clear_removed(&layout, &["ny".to_string()]);
        let rm = removed_programs(&layout);
        assert!(
            !rm.contains("ny"),
            "an explicit install lifts just that one"
        );
        assert!(rm.contains("clean"), "and leaves the others alone");

        clear_removed(&layout, &["clean".to_string()]);
        assert!(
            removed_programs(&layout).is_empty(),
            "emptying the set removes the file rather than leaving a header behind"
        );
        assert!(!layout.removed().exists());

        let _ = std::fs::remove_dir_all(&dir);
    }

    /// A removal that could NOT be recorded must be SAID. The write is already
    /// temp+rename, so a kill cannot shorten the list — but an unwritable prefix (a
    /// read-only store, a full disk, a `removed` path something else now owns) left
    /// `uninstall` printing "uninstalled ay" and exiting 0 with nothing recorded, and
    /// this file is the only thing keeping the program out of the unattended lanes:
    /// `seed_serviceable` and `install_default_set_inner` both filter on
    /// `removed_programs`, and `cmd_seed` records adoption again on every launch, so the
    /// next one reinstalls exactly what the user removed. The failure now comes back
    /// from `write_removed`, the recorder prints it, and the entries already recorded
    /// survive it — the rename either replaces the record or leaves it alone.
    #[cfg(unix)]
    #[test]
    fn a_removal_record_that_cannot_be_written_is_reported_not_swallowed() {
        use std::os::unix::fs::PermissionsExt as _;
        let dir = std::env::temp_dir().join(format!("atpkg-rm-ro-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&dir);
        std::fs::create_dir_all(&dir).unwrap();
        let layout = crate::store::Layout {
            prefix: dir.clone(),
        };

        record_removed(&layout, "ny");
        assert!(removed_programs(&layout).contains("ny"));

        // The prefix goes read-only: the temp file cannot be created, and the rename
        // that would publish it could not land either. Permissions are restored before
        // anything is asserted, so a failure here still leaves a removable directory.
        std::fs::set_permissions(&dir, std::fs::Permissions::from_mode(0o500)).unwrap();
        let mut all = removed_programs(&layout);
        all.insert("clean".to_string());
        let published = write_removed(&layout, &all);
        std::fs::set_permissions(&dir, std::fs::Permissions::from_mode(0o700)).unwrap();
        assert!(
            published.is_err(),
            "an unwritable prefix must come back as an error, never a silent success"
        );

        // The removal already recorded is intact — never truncated in place — and no
        // half-written temp file is left for the next reader to find.
        assert_eq!(
            removed_programs(&layout),
            std::collections::BTreeSet::from(["ny".to_string()]),
            "a failed write must not cost the removals already recorded"
        );
        let strays: Vec<String> = std::fs::read_dir(&dir)
            .unwrap()
            .flatten()
            .map(|e| e.file_name().to_string_lossy().into_owned())
            .filter(|n| n != "removed")
            .collect();
        assert!(
            strays.is_empty(),
            "no temp file may survive the failure: {strays:?}"
        );

        // And the failure is SURFACED, not merely returned: `record_removed` is called
        // from `uninstall_and_retire`, whose caller prints "uninstalled {program}" and
        // exits 0 whatever happened, so stderr is the only place the loss can appear.
        let src = include_str!("cli.rs");
        let start = src.find("fn record_removed(").expect("the recorder");
        let end = src[start..]
            .find("\n}\n")
            .map(|i| start + i)
            .expect("its end");
        assert!(
            src[start..end].contains("eprintln!("),
            "a removal that could not be recorded must be printed, like every other \
             marker write's failure"
        );

        let _ = std::fs::remove_dir_all(&dir);
    }

    /// REMOVAL MUST STICK. The seed lane is resumable — it installs whatever the
    /// store lacks, so an interrupted first run can finish later — and that same
    /// property means it would happily reinstall everything `uninstall --all` just
    /// removed, on the very next launch. The decline marker is what stops that, and
    /// an explicit install is what lifts it. Both directions are pinned here because
    /// getting either wrong is user-hostile in a way tests are the only defence
    /// against: one way the manager fights the user, the other way a change of mind
    /// requires deleting a file nobody documented.
    #[test]
    fn declining_the_toolset_survives_until_an_explicit_install() {
        let dir = std::env::temp_dir().join(format!("atpkg-decline-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&dir);
        std::fs::create_dir_all(&dir).unwrap();
        let layout = crate::store::Layout {
            prefix: dir.clone(),
        };

        assert!(!declined(&layout), "a fresh store has declined nothing");
        record_decline(&layout);
        assert!(declined(&layout), "uninstall --all declines the toolset");
        // Idempotent, and 0600 like every other durable marker.
        record_decline(&layout);
        assert!(declined(&layout));
        #[cfg(unix)]
        {
            use std::os::unix::fs::PermissionsExt as _;
            let mode = std::fs::metadata(layout.declined())
                .unwrap()
                .permissions()
                .mode();
            assert_eq!(mode & 0o777, 0o600, "decline marker must be 0600");
        }
        // Existence is the predicate — never a parsed value.
        std::fs::write(layout.declined(), b"\0garbage").unwrap();
        assert!(declined(&layout));

        clear_decline(&layout);
        assert!(!declined(&layout), "an explicit install lifts the decline");
        clear_decline(&layout); // clearing what is clear is not an error

        // Decline and adoption are INDEPENDENT facts: declining must not read as
        // adoption, or a removed toolset would still get set-completion.
        record_decline(&layout);
        assert!(!adopted(&layout), "declining is not adopting");

        let _ = std::fs::remove_dir_all(&dir);
    }

    /// The PRODUCING half of the cross-crate seed-marker contract
    /// (crates/aterm-gui `parse_seed_markers` is the consuming half, and its
    /// own tests feed it strings it wrote itself — which is why the contract
    /// broke undetected once already). These are the exact prefixes `cmd_seed`
    /// and `announce_pending_seed` print; if either moves, the GUI's first-run
    /// notice goes blind, so the literals are pinned on this side too.
    #[test]
    fn the_seed_marker_prefixes_are_the_ones_the_gui_parses() {
        // Search PRODUCTION code only. The previous version searched the whole file,
        // including this test — whose own array contains the very literals it looks
        // for — so it matched itself and passed with every `println!` deleted. A
        // test that cannot fail is worse than no test: it is a standing claim that
        // the cross-crate contract is checked when nothing checks it.
        let src = include_str!("cli.rs");
        let production = production_half(src);
        assert!(
            production.len() < src.len(),
            "the test module must be excluded, or this test can match itself"
        );
        // EMITTED, and emitted THROUGH THE ONE PRINTER. The prefixes used to be
        // duplicated as literals at each print site — the very duplication the marker
        // constants' doc comment blames for breaking this contract once — so the
        // check is now against the constant and against the call that records it in
        // the announcement ledger. A marker printed any other way is invisible to the
        // ledger, which is what decides whether an announcement was ever answered.
        let emissions: Vec<String> = whole_calls(production, "emit_marker_line(")
            .into_iter()
            .chain(whole_calls(production, "emit_marker_line_lossy("))
            .collect();
        for (marker, konst) in [
            (SEED_STARTING_MARKER, "SEED_STARTING_MARKER"),
            (SEED_INSTALLED_MARKER, "SEED_INSTALLED_MARKER"),
            (SEED_PENDING_MARKER, "SEED_PENDING_MARKER"),
            (SEED_UNUSABLE_MARKER, "SEED_UNUSABLE_MARKER"),
            (SEED_FAILED_MARKER, "SEED_FAILED_MARKER"),
            (SEED_PARTIAL_MARKER, "SEED_PARTIAL_MARKER"),
            (SEED_DONE_MARKER, "SEED_DONE_MARKER"),
            (SEED_BUSY_MARKER, "SEED_BUSY_MARKER"),
            (NET_STARTING_MARKER, "NET_STARTING_MARKER"),
            (NET_INSTALLED_MARKER, "NET_INSTALLED_MARKER"),
            (NET_FAILED_MARKER, "NET_FAILED_MARKER"),
        ] {
            assert!(
                emissions
                    .iter()
                    .any(|call| call.contains(konst) || call.contains(marker))
                    // The two announcements are built by their own helpers and handed
                    // to the printer as a finished line.
                    || production.contains(&format!("{{{konst}}}")),
                "the {marker:?} marker must be EMITTED by production code through \
                 `emit_marker_line` — crates/aterm-gui's parse_seed_line strips exactly \
                 this prefix, and a marker nothing prints leaves its announcement \
                 unanswered on screen"
            );
        }
    }

    /// A SEALED-PAYLOAD SEED IS NOT A NETWORK INSTALL. The in-DMG seed lane is
    /// local by construction (`cmd_seed`'s bare `DirFetcher` — zero sockets,
    /// lsof-verified in the 2026-08 audit), yet `install_default_set` announced it
    /// with "net-starting: installing N program(s) over the network", and the GUI —
    /// which parses these markers byte-for-byte — repeated the fabricated network
    /// install on its progress card. The wire announcement is now lane-gated and
    /// pure, so the lie stays pinned here instead of by an lsof session.
    #[test]
    fn a_seed_from_the_sealed_payload_never_prints_the_net_starting_marker() {
        let two = vec!["ay".to_string(), "trust".to_string()];
        // The network lane still announces before acting…
        let line = net_announcement(
            ProvisionLane::Network,
            Consent::InstallingAterm,
            &two,
            SignedSizes::UNKNOWN,
        )
        .expect("a non-empty network pass announces before acting");
        assert!(
            line.starts_with(&format!("atpkg: {NET_STARTING_MARKER}")),
            "the GUI strips exactly this prefix: {line}"
        );
        assert!(line.contains("ay, trust"), "{line}");
        // …stays silent on the ordinary no-op tick, whoever consented…
        let known = SignedSizes {
            download: Some(1 << 28),
            disk: Some(1 << 30),
        };
        for consent in [Consent::InstallingAterm, Consent::Explicit] {
            assert!(net_announcement(ProvisionLane::Network, consent, &[], known).is_none());
            // …and the sealed-payload lane never claims the wire, whatever the set, the
            // sizes or the consent: its announcement is `cmd_seed`'s `seed-starting:` line.
            for sizes in [SignedSizes::UNKNOWN, known] {
                assert!(
                    net_announcement(ProvisionLane::SealedPayload, consent, &two, sizes).is_none()
                );
                assert!(
                    net_announcement(ProvisionLane::SealedPayload, consent, &[], sizes).is_none()
                );
            }
        }
        // The local lane's announcement carries the LOCAL marker, and never the
        // wire's — the GUI opens the same card off either, so the marker is the
        // only place the transport claim lives.
        let seed = seed_announcement(10, None);
        assert!(
            seed.starts_with(&format!("atpkg: {SEED_STARTING_MARKER}")),
            "{seed}"
        );
        assert!(!seed.contains(NET_STARTING_MARKER), "{seed}");

        // NON-VACUITY, in two halves. (1) `cmd_seed` actually wires the sealed
        // lane: its body names SealedPayload and never touches the wire marker.
        // (2) The wire marker reaches stdout through `net_announcement` alone, so
        // the `None`s above are the whole story, not one suppressed call site.
        let src = include_str!("cli.rs");
        let production = production_half(src);
        let start = production.find("fn cmd_seed").expect("the seed verb");
        let end = production[start..]
            .find("\nfn ")
            .map(|i| start + i)
            .expect("a function after cmd_seed");
        let seed_body = &production[start..end];
        assert!(
            seed_body.contains("ProvisionLane::SealedPayload"),
            "cmd_seed must name the sealed-payload lane"
        );
        assert!(
            !seed_body.contains("NET_STARTING"),
            "the seed verb must never reach for the wire announcement"
        );
        assert_eq!(
            production.matches("NET_STARTING_MARKER}").count(),
            1,
            "the wire announcement must have exactly ONE production emission site \
             (inside net_announcement), or the lane gate can be walked around"
        );
    }

    /// THE MARKER CONTRACT HAS A POSITIVE TERMINAL, and the ledger knows which half
    /// of the contract each marker keeps.
    ///
    /// Until 2026-09-11 every terminal in this contract reported a PROBLEM
    /// (`seed-failed:`, `seed-partial:`, `seed-unusable:`, `net-failed:`) beside the
    /// two install rosters, so a pass that announced itself, ended well and had no
    /// roster to name printed nothing at all — and its only reader, the GUI, had to
    /// infer an outcome from that silence. It inferred "failed", and put "⚠ ALab
    /// toolchain install failed" over a clean run on a healthy machine.
    #[test]
    fn the_marker_table_covers_every_marker_and_names_the_two_that_open() {
        use super::announcement::MARKERS;
        let opens: Vec<&str> = MARKERS
            .iter()
            .filter(|(_, o)| *o)
            .map(|(m, _)| *m)
            .collect();
        assert_eq!(
            opens,
            vec![SEED_STARTING_MARKER, NET_STARTING_MARKER],
            "exactly two markers OPEN an announcement; every other one answers"
        );
        assert!(
            MARKERS.iter().any(|(m, _)| *m == SEED_DONE_MARKER),
            "the positive terminal must be in the table or it answers nothing"
        );
        // NON-VACUITY: every marker const this module publishes is classified. A new
        // marker that is not in the table silently stops answering announcements —
        // the exact failure mode this contract's own doc comment warns about.
        for marker in [
            SEED_STARTING_MARKER,
            SEED_INSTALLED_MARKER,
            SEED_PENDING_MARKER,
            SEED_UNUSABLE_MARKER,
            SEED_FAILED_MARKER,
            SEED_PARTIAL_MARKER,
            SEED_DONE_MARKER,
            SEED_BUSY_MARKER,
            NET_STARTING_MARKER,
            NET_INSTALLED_MARKER,
            NET_FAILED_MARKER,
        ] {
            assert!(
                MARKERS.iter().any(|(m, _)| *m == marker),
                "{marker:?} is published as part of the contract but classified nowhere"
            );
        }
        // …and `lock-waiting:`, which is NEITHER half, stays out of the table: a
        // `lock-waiting:` line answers nothing and opens nothing, so the ledger
        // must read straight past it (a child that waited and then announced is
        // an OPEN announcement, not an answered one).
        assert!(
            !MARKERS.iter().any(|(m, _)| *m == LOCK_WAITING_MARKER),
            "`lock-waiting:` is neither an announcement nor an answer"
        );
        // …and so does `lock-acquired:`, which answers the WAIT, not an announcement:
        // were it an answer, a child that waited, acquired and then announced would
        // look answered before it began.
        assert!(
            !MARKERS.iter().any(|(m, _)| *m == LOCK_ACQUIRED_MARKER),
            "`lock-acquired:` is neither an announcement nor an answer"
        );
        use super::announcement;
        announcement::reset();
        announcement::note(&format!(
            "atpkg: {LOCK_WAITING_MARKER}another atpkg process holds the store lock"
        ));
        assert!(!announcement::is_open());
        announcement::note(&format!(
            "atpkg: {LOCK_ACQUIRED_MARKER}the other process finished"
        ));
        assert!(!announcement::is_open());
        announcement::note(&format!(
            "atpkg: {SEED_STARTING_MARKER}installing 2 program(s)"
        ));
        assert!(
            announcement::is_open(),
            "a wait before the announcement did not pre-answer it"
        );
        announcement::reset();
    }

    /// The ledger reads the same bytes the GUI parses, and an INFORMATIONAL row is not
    /// an answer. `managed-current:` / `machine-settings:` are printed at the end of
    /// every pass, announced or not; counting one as a terminal is round 9's rule read
    /// backwards, and it would let a pass retire a card it had not finished with.
    #[test]
    fn only_a_terminal_marker_closes_an_announcement() {
        use super::announcement;
        announcement::reset();
        assert!(!announcement::is_open(), "nothing announced, nothing open");

        announcement::note("atpkg: this is an ordinary sentence");
        assert!(!announcement::is_open());

        announcement::note(&format!(
            "atpkg: {NET_STARTING_MARKER}installing 2 program(s)"
        ));
        assert!(announcement::is_open(), "the announcement opened");

        announcement::note("atpkg: managed-current: claude 2.1.267 (build 2026091001)");
        announcement::note("atpkg: machine-settings: universal-control disabled");
        assert!(
            announcement::is_open(),
            "an R6 row says what the machine HAS, not how the pass ended"
        );

        announcement::note(&format!("atpkg: {SEED_DONE_MARKER}the pass finished"));
        assert!(!announcement::is_open(), "the positive terminal answers");
        announcement::reset();
    }

    /// THE ROUND-10 PROPERTY, at its source. On a provisioned Mac the passes announce
    /// nothing — the seal is reclaimed after the first success, so `atpkg seed` prints
    /// a plain sentence and exits 0, and `atpkg update`'s plan is empty so
    /// `net_announcement` returns `None`. Nothing may be printed at the end of those
    /// passes, or the warning card is back on screen at every launch of a healthy
    /// machine (2026-08-20 round-10 audit).
    #[test]
    fn a_pass_that_announced_nothing_answers_nothing() {
        use super::announcement;
        announcement::reset();
        // The steady state's own words, verbatim in shape: no marker anywhere.
        announcement::note("atpkg: the store already holds the ALab toolset");
        assert!(!announcement::is_open());
        // `answer_announcement` is what runs at every announcing verb's tail; with no
        // announcement open it must be a no-op, for BOTH verdicts.
        for ok in [true, false] {
            answer_announcement(ok);
            assert!(
                !announcement::is_open(),
                "a verb that announced nothing has nothing to answer (ok={ok})"
            );
        }
        announcement::reset();
    }

    /// EVERY MARKER GOES OUT THROUGH THE ONE PRINTER. The ledger is bookkeeping done
    /// at the moment a marker reaches stdout, so a marker printed by a bare `println!`
    /// is invisible to it — the announcement would stay "open" after being answered,
    /// or an announcement would open without being recorded. The contract's own doc
    /// comment already records that duplicating these strings broke it once.
    #[test]
    fn no_production_line_prints_a_marker_outside_the_one_printer() {
        let src = include_str!("cli.rs");
        let production = production_half(src);
        // Whole macro invocations, not single lines: these calls wrap, and the marker
        // is usually on the line after `println!(`. `writeln!` too: the waiter's
        // printer is a bare write with the error ignored, and a marker written that
        // way OUTSIDE it would be just as invisible to the ledger.
        for call in whole_calls(production, "println!(")
            .into_iter()
            .chain(whole_calls(production, "print!("))
            .chain(whole_calls(production, "writeln!("))
        {
            for (marker, _) in super::announcement::MARKERS {
                let konst = format!("{}_MARKER", marker.trim_end_matches(": ").to_uppercase())
                    .replace('-', "_");
                assert!(
                    !call.contains(marker) && !call.contains(&konst),
                    "a marker must reach stdout through `emit_marker_line`, not this: {call}"
                );
            }
        }
        // NON-VACUITY of the scan itself: it must actually be finding the print
        // macros, or the loop above passes by seeing nothing.
        assert!(
            whole_calls(production, "println!(").len() > 100,
            "the scanner found almost no println! calls — it is not reading this file"
        );
        // …and the one printer really is used — and its lossy twin exactly once, by
        // the lock edge, for the waiter's `seed-busy:` line.
        assert!(
            production.matches("emit_marker_line(").count() >= 9,
            "every marker site must go through the one printer"
        );
        let lossy = whole_calls(production, "emit_marker_line_lossy(");
        assert_eq!(
            lossy.len(),
            2,
            "the lossy printer has ONE call site (plus its definition): {lossy:?}"
        );
        assert!(
            lossy.iter().any(|call| call.contains("SEED_BUSY_MARKER")),
            "the lossy printer's one caller is the waiter's stood-aside terminal"
        );
        let edge = production
            .find("fn mutator_store_lock")
            .expect("the lock edge");
        let edge_end = production[edge + 1..]
            .find("\nfn ")
            .map(|i| edge + 1 + i)
            .expect("a function after the lock edge");
        let call = production
            .find("emit_marker_line_lossy(&format")
            .expect("the call");
        assert!(
            edge < call && call < edge_end,
            "the lossy call is inside the lock edge, not in a verb body"
        );
    }

    /// Every `<head>…)` invocation in `src`, brace/paren-matched so a wrapped macro
    /// call is returned whole. String literals are skipped so a `)` inside one cannot
    /// close the call early.
    fn whole_calls(src: &str, head: &str) -> Vec<String> {
        let bytes = src.as_bytes();
        let mut out = Vec::new();
        let mut from = 0;
        while let Some(rel) = src[from..].find(head) {
            let start = from + rel;
            let mut i = start + head.len();
            let mut depth = 1usize;
            let mut in_str = false;
            while i < bytes.len() {
                let c = bytes[i];
                if in_str {
                    if c == b'\\' {
                        i += 2;
                        continue;
                    }
                    if c == b'"' {
                        in_str = false;
                    }
                } else {
                    match c {
                        b'"' => in_str = true,
                        b'(' => depth += 1,
                        b')' => {
                            depth -= 1;
                            if depth == 0 {
                                break;
                            }
                        }
                        _ => {}
                    }
                }
                i += 1;
            }
            out.push(src[start..i.min(bytes.len())].to_string());
            from = start + head.len();
        }
        out
    }

    /// ONE INSTALL, ONE SIZE. A single first run used to quote three figures for
    /// the same bytes — install.sh's fixed "~4.2 GB" header, this lane's
    /// "~4.7 GB on disk when finished", 4.08 GB measured — because every surface
    /// derived and rendered its own number. Within atpkg the size is now derived
    /// ONCE per run (`seed_install_bytes`, the sealed manifests' signed
    /// `disk_installed` sum, at print time) and rendered ONLY by
    /// `cost::human_bytes` (binary units, "GiB", one decimal — the renderer flow's
    /// disk errors, doctor's free-space report, and the release cut transcript
    /// already share).
    #[test]
    fn the_seed_lane_quotes_one_size_in_one_unit() {
        // The announcement renders the signed sum through the ONE renderer…
        let line = seed_announcement(10, Some(4_380_000_000));
        assert!(
            line.starts_with(&format!("atpkg: {SEED_STARTING_MARKER}")),
            "the GUI strips exactly this prefix: {line}"
        );
        assert!(
            line.contains("(~4.1 GiB on disk when finished)"),
            "the size must be cost::human_bytes of the signed sum: {line}"
        );
        assert_eq!(
            line.matches("4.1 GiB").count(),
            1,
            "one figure, stated once: {line}"
        );
        // …and an unavailable sum prints NO size: no number is more honest than a
        // confidently wrong one.
        let bare = seed_announcement(2, None);
        assert!(!bare.contains("GiB") && !bare.contains("GB"), "{bare}");

        // NON-VACUITY: no production line in this file renders bytes on its own.
        // The ad-hoc decimal spellings (`as f64 / 1e9` under a "GB" label) are
        // exactly what let the figures drift apart across surfaces.
        let src = include_str!("cli.rs");
        let production = production_half(src);
        assert!(
            !production.contains("1e9"),
            "no ad-hoc byte rendering in the CLI — every size goes through \
             cost::human_bytes"
        );
        assert!(
            !production.contains("{:.1} GB") && !production.contains("{:.0} GB"),
            "no decimal-GB format fragment may return"
        );
    }

    /// The wire lane says what it is about to do, and how to stop it: the lean app has no
    /// seal, so the whole set arrives here on every current-release Mac. The way out is timed
    /// honestly — `uninstall --all` cannot run until the pass releases the store lock, so the
    /// line says "once this pass ends" rather than sending the reader into that refusal.
    #[test]
    fn the_network_lane_names_the_size_and_the_off_switches() {
        // The set a first launch names, from the compiled lists the client installs from,
        // sorted as `install_default_set_inner` sorts `will_install`.
        let mut set: Vec<String> = crate::stub::DEFAULT_SET_STUB_NAMES
            .iter()
            .map(|(n, _)| (*n).to_string())
            .chain(crate::stub::AGENT_PROGRAMS.iter().map(|n| (*n).to_string()))
            .collect();
        set.sort();
        set.dedup();
        // 3_650_000_000 B = 3.399… GiB and 1_100_000_000 B = 1.024… GiB, so the one
        // renderer's rounding is exercised on both figures of the same `[cost]` row.
        let both = SignedSizes {
            download: Some(1_100_000_000),
            disk: Some(3_650_000_000),
        };
        let line = net_announcement(ProvisionLane::Network, Consent::InstallingAterm, &set, both)
            .expect("a non-empty network pass announces before acting");
        // The GUI strips exactly this prefix…
        assert!(
            line.starts_with(&format!(
                "atpkg: {NET_STARTING_MARKER}installing {} program(s) over the network: ay, \
                 claude, ",
                set.len()
            )),
            "{line}"
        );
        // …each size is cost::human_bytes of its signed sum, stated once…
        assert_eq!(
            line.matches("3.4 GiB").count(),
            1,
            "one on-disk figure, once: {line}"
        );
        assert_eq!(
            line.matches("1.0 GiB").count(),
            1,
            "one download figure, once: {line}"
        );
        // …both off switches are named, spelled as typed…
        assert!(line.contains("`aterm pkg uninstall --all`"), "{line}");
        assert!(line.contains("`[packages].seed_install = false`"), "{line}");
        assert!(line.contains("before the first launch"), "{line}");
        // …the uninstall verb is timed honestly — refused while the pass holds the store
        // lock — so the line says "once this pass ends" before naming it…
        let ends = line.find("once this pass ends").expect("the timing");
        let verb = line.find("`aterm pkg uninstall --all`").expect("the verb");
        assert!(
            ends < verb,
            "the timing must qualify the verb, not trail it: {line}"
        );
        assert!(line.contains("records the decline"), "{line}");
        assert!(
            !line.contains("opt out with `aterm pkg uninstall"),
            "untimed: {line}"
        );
        // …the whole line owns exactly two '(' — "program(s)" and the size — so a
        // parenthesis can arrive from neither the switch phrase nor the format…
        assert_eq!(line.matches('(').count(), 2, "{line}");
        // …and the sizes are the line's trailing parenthetical, which is where the toolchain
        // bar reads them, so the off-switch phrase must bring no parenthesis of its own…
        let (_, tail) = line.rsplit_once('(').expect("a parenthetical");
        assert_eq!(tail, "~1.0 GiB download, ~3.4 GiB on disk)", "{line}");
        let (consent_clause, seed_clause) = Consent::InstallingAterm.announced();
        for phrase in [NET_OFF_SWITCH, consent_clause, seed_clause] {
            assert!(
                !phrase.contains(['(', ')']),
                "the bar would read the wrong parenthetical: {phrase}"
            );
        }
        // …shown whole: the bar clips the text inside the parentheses at 40 characters, and
        // the figures this line exists to disclose may not be what gets clipped…
        let inside = tail.strip_suffix(')').expect("closed");
        assert!(
            inside.chars().count() <= 40,
            "{inside:?} would be clipped on the toolchain bar"
        );
        assert_eq!(crate::progress::sanitize_for_tty(inside, 40), inside);
        // …and the whole line fits the log, whose sink elides a record body past
        // aterm_log::MAX_RECORD_BYTES = 512. The sizes are last on the line, so they are the
        // first casualty of any growth. atpkg does not depend on aterm-log, so the cap is
        // pinned here by hand: change both or neither.
        const MAX_RECORD_BYTES: usize = 512; // = aterm_log::MAX_RECORD_BYTES
        let detail = line
            .strip_prefix(&format!("atpkg: {NET_STARTING_MARKER}"))
            .expect("the GUI strips exactly this prefix");
        let logged = "atpkg install pass starting: ".len() + detail.len();
        assert!(
            logged <= MAX_RECORD_BYTES,
            "the log sink would elide the sizes: {logged} bytes as logged, cap \
             {MAX_RECORD_BYTES}: {line}"
        );
        // The explicit verb is its own consent: it never reads `seed_install`, so its line is
        // the same line with exactly those two clauses gone — the sizes, the timed uninstall
        // switch and the two '(' all stay.
        let explicit = net_announcement(ProvisionLane::Network, Consent::Explicit, &set, both)
            .expect("a non-empty network pass announces before acting");
        assert!(
            !explicit.contains("installing aterm is the consent"),
            "{explicit}"
        );
        assert!(!explicit.contains("seed_install"), "{explicit}");
        assert!(!explicit.contains("before the first launch"), "{explicit}");
        assert_eq!(
            explicit,
            line.replacen(consent_clause, "", 1)
                .replacen(seed_clause, "", 1),
            "the explicit line differs by exactly the two clauses"
        );
        assert!(
            explicit.ends_with(&format!(
                " — {NET_OFF_SWITCH} (~1.0 GiB download, ~3.4 GiB on disk)"
            )),
            "{explicit}"
        );
        assert_eq!(explicit.matches('(').count(), 2, "{explicit}");
        // One figure known prints that figure alone, in its own tier: 970_000_000 B is under
        // 1 GiB, so the renderer says "925.1 MiB". The two figures need not share a unit, and
        // neither is rounded up to match the other.
        let disk_only = net_announcement(
            ProvisionLane::Network,
            Consent::InstallingAterm,
            &set,
            SignedSizes {
                download: None,
                disk: Some(3_650_000_000),
            },
        )
        .expect("announces");
        assert!(
            disk_only.ends_with(" (~3.4 GiB on disk when finished)"),
            "{disk_only}"
        );
        assert_eq!(disk_only.matches("GiB").count(), 1, "{disk_only}");
        let download_only = net_announcement(
            ProvisionLane::Network,
            Consent::InstallingAterm,
            &set,
            SignedSizes {
                download: Some(970_000_000),
                disk: None,
            },
        )
        .expect("announces");
        assert!(
            download_only.ends_with(" (~925.1 MiB download)"),
            "{download_only}"
        );
        assert_eq!(download_only.matches("MiB").count(), 1, "{download_only}");
        assert!(!download_only.contains("GiB"), "{download_only}");
        // No signed sum ⇒ no size at all (no number beats a wrong one), the
        // switches still named, and the last '(' is then "program(s)", whose
        // tail does not close — the bar shows "starting…" rather than a guess.
        let bare = net_announcement(
            ProvisionLane::Network,
            Consent::InstallingAterm,
            &set,
            SignedSizes::UNKNOWN,
        )
        .expect("a non-empty network pass announces before acting");
        assert!(!bare.contains("GiB") && !bare.contains("GB"), "{bare}");
        assert!(bare.contains(NET_OFF_SWITCH), "{bare}");
        let (_, tail) = bare.rsplit_once('(').expect("program(s)");
        assert!(!tail.ends_with(')'), "{bare}");
    }

    /// Who adopted decides what the line may claim: "installing aterm is the consent", with
    /// `seed_install = false` as the switch, is true only of an update pass on a machine the
    /// seed pass adopted, with neither key overriding it. A machine the explicit verb adopted
    /// carries no record. The record's own life cycle is
    /// `who_adopted_is_recorded_only_when_the_seed_pass_creates_adoption`.
    #[test]
    fn only_a_pass_the_first_launch_adopted_says_installing_aterm_is_the_consent() {
        // The one case that may state the first launch's consent…
        assert_eq!(
            set_completion_consent(false, true, true),
            Consent::InstallingAterm
        );
        // …and the seven that may not. The first is the default config on a
        // machine the explicit verb adopted, or on one adopted before the record.
        for (auto_install, seed_install, by_seed) in [
            (false, true, false),
            (true, true, true),
            (true, true, false),
            (false, false, true),
            (false, false, false),
            (true, false, true),
            (true, false, false),
        ] {
            assert_eq!(
                set_completion_consent(auto_install, seed_install, by_seed),
                Consent::Explicit,
                "auto_install={auto_install} seed_install={seed_install} \
                 adopted_by_seed={by_seed}"
            );
        }

        // Up to the verb's own closing brace, not the next top-level `fn`: the items between
        // `cmd_update_all` and the next function include `Consent` itself, whose arms would
        // satisfy any search. Each verb wraps a `_code` worker, so the scans read the worker.
        fn body_of(src: &'static str, signature: &str) -> &'static str {
            let start = src.find(signature).expect("the verb");
            let end = src[start..]
                .find("\n}\n")
                .map(|i| start + i)
                .expect("the verb's closing brace");
            &src[start..end]
        }
        let src = include_str!("cli.rs");
        let verb = body_of(src, "fn cmd_install_default_set_code(");
        assert!(
            verb.contains("Consent::Explicit"),
            "the explicit verb is its own consent"
        );
        assert!(
            !verb.contains("Consent::InstallingAterm"),
            "the explicit verb must never claim the first launch's consent"
        );
        let update = body_of(src, "fn cmd_update_all_code(");
        // Whitespace-free, so the formatter's line breaks cannot hide the call.
        let squeezed: String = update.chars().filter(|c| !c.is_whitespace()).collect();
        assert!(
            squeezed.contains(
                "set_completion_consent(cfg.auto_install(),cfg.seed_install(),adopted_by_seed(&layout)"
            ),
            "the update pass asks which consent holds on this machine, including who adopted it"
        );
        assert!(
            !update.contains("Consent::InstallingAterm") && !update.contains("Consent::Explicit"),
            "the update pass must not hard-code a consent"
        );
        // Who adopted is written by the seed verb alone and cleared by the explicit one.
        assert!(
            verb.contains("clear_seed_adoption(&layout)") && !verb.contains("record_seed_adoption"),
            "the explicit verb takes the seed pass's claim over and never writes it"
        );
        let seed = body_of(src, "fn cmd_seed(");
        assert!(
            seed.contains("record_seed_adoption(&layout)")
                && !seed.contains("record_adoption(&layout)"),
            "the seed pass adopts through the door that records who adopted"
        );
    }

    /// Who adopted, across the verbs that change it: the record behind
    /// `set_completion_consent`'s third input. The seed pass writes it only when it creates
    /// adoption, since stamping a marker it found would hand a machine the explicit verb
    /// adopted back to the first launch's consent. Neither file alone vouches for anything.
    #[test]
    fn who_adopted_is_recorded_only_when_the_seed_pass_creates_adoption() {
        let layout = temp_layout("who-adopted");
        // What `cmd_update_all` asks, with the default keys.
        let consent = |layout: &crate::store::Layout| {
            set_completion_consent(false, true, adopted_by_seed(layout))
        };

        // The first launch adopts, and says so.
        record_seed_adoption(&layout);
        assert!(adopted(&layout) && adopted_by_seed(&layout));
        assert_eq!(consent(&layout), Consent::InstallingAterm);
        #[cfg(unix)]
        {
            use std::os::unix::fs::PermissionsExt as _;
            let mode = std::fs::metadata(layout.adopted_by_seed())
                .unwrap()
                .permissions()
                .mode();
            assert_eq!(mode & 0o777, 0o600, "the record is 0600 like the marker");
        }
        // Every later launch re-runs the seed pass: a no-op.
        let first = std::fs::read(layout.adopted_by_seed()).unwrap();
        record_seed_adoption(&layout);
        assert_eq!(std::fs::read(layout.adopted_by_seed()).unwrap(), first);

        // Default keys, `uninstall --all`, then Settings "Install ALab toolset":
        // uninstall forgets both files, the explicit verb clears the record and
        // adopts, and the next launch's seed pass finds a marker and records nothing.
        clear_adoption(&layout);
        assert!(!adopted(&layout) && !layout.adopted_by_seed().exists());
        clear_seed_adoption(&layout);
        record_adoption(&layout);
        record_seed_adoption(&layout);
        assert!(adopted(&layout) && !adopted_by_seed(&layout));
        assert_eq!(
            consent(&layout),
            Consent::Explicit,
            "the explicit verb adopted this machine; its passes must not claim the first launch"
        );

        // The explicit verb on a machine the seed pass adopted takes the claim over.
        clear_adoption(&layout);
        record_seed_adoption(&layout);
        assert_eq!(consent(&layout), Consent::InstallingAterm);
        clear_seed_adoption(&layout);
        assert!(adopted(&layout), "clearing the record is not un-adopting");
        assert_eq!(consent(&layout), Consent::Explicit);
        // Clearing what is already clear is not an error.
        clear_seed_adoption(&layout);

        // A record without the marker vouches for nothing…
        clear_adoption(&layout);
        std::fs::write(layout.adopted_by_seed(), b"stray").unwrap();
        assert!(!adopted_by_seed(&layout));
        // …and a marker that predates the record claims no more than the uninstall.
        clear_seed_adoption(&layout);
        record_adoption(&layout);
        assert_eq!(consent(&layout), Consent::Explicit);

        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// The None-on-zero sum behind both figures: one undeclared (zero) entry
    /// poisons its total, the other figure is unaffected, and an already-poisoned
    /// total stays `None` whatever follows.
    #[test]
    fn a_signed_sum_is_poisoned_by_one_undeclared_entry() {
        assert_eq!(sum_nonzero(Some(0), 5), Some(5));
        assert_eq!(sum_nonzero(Some(5), 7), Some(12));
        assert_eq!(sum_nonzero(Some(5), 0), None);
        assert_eq!(sum_nonzero(None, 7), None);
        assert_eq!(sum_nonzero(Some(u64::MAX), 1), Some(u64::MAX));
        // The renderer prints only what is known.
        let known = SignedSizes {
            download: Some(1 << 30),
            disk: Some(1 << 30),
        };
        assert_eq!(
            size_parenthetical(known),
            " (~1.0 GiB download, ~1.0 GiB on disk)"
        );
        assert_eq!(size_parenthetical(SignedSizes::UNKNOWN), "");
    }

    /// THE CONSENT POLICY, as a table. Every consent regression this branch shipped
    /// lived in this one boolean, and none of them had a test — reverting either
    /// round-3 fix left the whole suite green, which is precisely why they shipped.
    #[test]
    fn set_completion_honours_decline_over_adoption_and_auto_install() {
        // Nothing asked for: the default posture installs nothing over the network.
        assert!(!should_complete_set(false, false, false, false));
        // Adopted (installing aterm is wanting the toolset) ⇒ keep the set complete.
        assert!(should_complete_set(false, true, false, false));
        // The explicit network-consent switch also enables it.
        assert!(should_complete_set(true, false, false, false));
        // DECLINED outranks BOTH. Honouring a removal on the local lane while the
        // 6-hour network pass quietly reinstalled the set made the decline work for
        // exactly one launch.
        assert!(
            !should_complete_set(false, true, true, false),
            "a declined machine must not be re-completed just because it once adopted"
        );
        assert!(
            !should_complete_set(true, true, true, false),
            "declining outranks the auto_install switch too — it is the later, more \
             specific act"
        );
        assert!(!should_complete_set(true, false, true, false));
        // AN UNREADABLE [packages] TABLE OUTRANKS BOTH CONSENTS TOO (2026-09-16 audit).
        // This lane is narrowed by `[packages].exclude`, and a table that did not parse
        // has none to narrow it with: running it anyway reinstalls, over the network,
        // the very program the owner excluded — on a machine whose only mistake was a
        // TOML typo, possibly in the GUI-owned part of the file.
        assert!(
            !should_complete_set(false, true, false, true),
            "an adopted machine waits for the config to be readable before the set is \
             completed from an exclude list nobody could read"
        );
        assert!(
            !should_complete_set(true, true, false, true),
            "the auto_install switch does not survive an unreadable table either"
        );
    }

    /// Read-only surfaces keep working WHILE a mutator holds the store lock — the
    /// lock serializes writers only, never observation (`list`/`which`/status/verify
    /// must stay live during a long install).
    #[test]
    fn read_only_paths_need_no_store_lock() {
        let layout = temp_layout("readonly-lock");
        // A concurrent mutator: a second Layout over the SAME prefix holds the lock.
        let holder = crate::store::Layout {
            prefix: layout.prefix.clone(),
        };
        let _held = crate::lock::try_lock_store(&holder).expect("mutator takes the lock");
        // Every read-only surface the lock-free verbs are built on completes normally.
        assert!(crate::list_installed(&layout).is_empty());
        assert!(crate::which(&layout, "ay").is_none());
        assert!(crate::status::read(&layout).is_none());
        assert!(crate::verify::verify_all(&layout).is_empty());
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// REGRESSION: no ambient state may supply the verification anchor.
    ///
    /// `ATPKG_ROOTKEY_OVERRIDE` used to swap which root key anchored verification,
    /// and could supply one to a build that had none — an environment variable
    /// deciding trust. The anchor is now the committed paper-master keyset and only
    /// that, so setting the old variable must change nothing.
    #[test]
    fn no_environment_variable_can_supply_the_verification_anchor() {
        // NO `set_var` HERE, deliberately. The claim is that the anchor is a
        // COMPILED constant and the variable is not consulted at all — and
        // `effective_anchor` reads no environment (only the store's own durable
        // roster floor), which is the property under test. Mutating the
        // process-global environment to demonstrate that would race every other
        // test in this binary (cargo runs them on threads) to prove something the
        // source already settles, and the lint that flags it is right.
        let layout = temp_layout("anchor");
        assert_eq!(
            effective_anchor(&layout).is_armed(),
            !crate::PKG_TRUST_ANCHORS.is_empty(),
            "the committed keyset is the only thing that arms the manager"
        );
        assert!(
            effective_anchor(&layout).is_armed(),
            "and in THIS tree it is armed (2026-08-15), so the CLI's anchor is live"
        );
        // NON-VACUITY: the reader must contain no env lookup at all, so this
        // cannot pass by the variable merely being unset in this process.
        assert!(
            !include_str!("cli.rs").contains("ATPKG_ROOTKEY_OVERRIDE\""),
            "no env-var lookup may reach the anchor"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// Enablement follows the COMPILED keyset and the opt-out, and nothing else.
    /// `ATPKG_DISABLE` may only ever subtract authority — a kill switch that can
    /// turn the manager off is safe; one that could turn it on was not, which is
    /// why the root-key override is gone.
    #[test]
    fn enablement_follows_the_compiled_anchor_and_the_kill_switch_only() {
        // No compiled anchor ⇒ inert. There is no longer any way to supply one
        // at runtime, so this state can only be changed by a commit. The empty
        // slice below is a FIXTURE, not the shipped state: this tree ships ARMED
        // (2026-08-15) — see `no_environment_variable_can_supply_the_verification_anchor`,
        // which asserts the live anchor a few lines up.
        assert!(!crate::manager_enabled_with(&[], false));
        assert!(!crate::manager_enabled_with(&[], true));
        // A compiled keyset ⇒ enabled...
        assert!(crate::manager_enabled_with(&["PINNED_KEY"], false));
        // ...and the opt-out wins even with a valid keyset present.
        assert!(!crate::manager_enabled_with(&["PINNED_KEY"], true));
    }

    /// The two durable ratchets advance TOGETHER and INDEPENDENTLY: an index build and a
    /// roster generation are different counters over different documents, and folding
    /// them into one high-water would let either one's advance refuse the other's
    /// perfectly current document.
    #[test]
    fn advance_floors_ratchets_both_counters_separately() {
        let layout = temp_layout("floors");
        std::fs::create_dir_all(&layout.prefix).unwrap();
        std::fs::set_permissions(&layout.prefix, std::fs::Permissions::from_mode(0o700)).unwrap();
        advance_floors(&layout, 41, 3);
        assert_eq!(crate::sig::Floor::new(layout.floor()).current(), 41);
        assert_eq!(crate::sig::Floor::new(layout.roster_floor()).current(), 3);
        // An index republish that does NOT bump the roster leaves the roster floor put,
        // so the generation still in use is not ratcheted out from under the client.
        advance_floors(&layout, 42, 3);
        assert_eq!(crate::sig::Floor::new(layout.floor()).current(), 42);
        assert_eq!(crate::sig::Floor::new(layout.roster_floor()).current(), 3);
        // A revocation bumps the roster without re-cutting the index — symmetrically.
        advance_floors(&layout, 42, 4);
        assert_eq!(crate::sig::Floor::new(layout.floor()).current(), 42);
        assert_eq!(crate::sig::Floor::new(layout.roster_floor()).current(), 4);
        // And the anchor the CLI builds carries the roster floor it just recorded.
        assert_eq!(effective_anchor(&layout).roster_floor, 4);
        // NON-VACUITY: they are genuinely separate files.
        assert_ne!(layout.floor(), layout.roster_floor());
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A BUILD FLOOR ONE MACHINE DROVE OUT OF REACH IS RE-BASED BY THE NEXT GENERATION.
    ///
    /// `index_build` sits inside MACHINE-signed bytes, so any rostered machine can set it to
    /// the u64 ceiling and — under a purely monotonic ratchet — put the store permanently
    /// above every index the owner will ever publish, including the one that revokes that
    /// machine. Bricked AND unrevocable, by a tier the roster exists to be able to revoke.
    ///
    /// So the floor is scoped to the generation that recorded it: a strictly newer
    /// master-signed generation re-bases it. Only the paper master can mint a generation, so
    /// this lever is the master's alone.
    ///
    /// MUTATION: replace the `roster_seq > generation.current()` arm in `advance_floors`
    /// with the plain `check_and_record` and the rescue assertion fails with the floor still
    /// at `u64::MAX`.
    #[test]
    fn a_newer_generation_rebases_a_floor_a_machine_drove_out_of_reach() {
        let layout = temp_layout("floor-poison");
        std::fs::create_dir_all(&layout.prefix).unwrap();
        std::fs::set_permissions(&layout.prefix, std::fs::Permissions::from_mode(0o700)).unwrap();

        // A machine on generation 3 publishes the TOML integer ceiling.
        advance_floors(&layout, u64::MAX, 3);
        assert_eq!(build_floor(&layout).index_build, u64::MAX);
        assert_eq!(build_floor(&layout).roster_seq, 3);
        // PRECONDITION: within that same generation the floor is immovable — this really is
        // the trap, not a floor that would have relaxed on its own.
        advance_floors(&layout, 101, 3);
        assert_eq!(
            build_floor(&layout).index_build,
            u64::MAX,
            "same generation ⇒ monotonic, exactly as before"
        );

        // The owner revokes that machine and republishes at a sane build under generation 4.
        advance_floors(&layout, 101, 4);
        assert_eq!(
            build_floor(&layout),
            crate::sig::BuildFloor {
                index_build: 101,
                roster_seq: 4
            },
            "a newer master-signed generation re-bases the floor it inherited"
        );
        // ...and from there it ratchets again, so the rescue is one step, not a hole.
        advance_floors(&layout, 100, 4);
        assert_eq!(build_floor(&layout).index_build, 101);
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    // ---- token chain precedence (pure split — no env mutation, no subprocess) ----

    /// `$ATPKG_TOKEN` wins outright and the fallback chain is NOT invoked (laziness:
    /// no keychain/`gh` probe runs when the dedicated env var suffices); an empty
    /// env value falls through; the chain's label passes through; nothing at all is
    /// the anonymous empty token.
    #[test]
    fn token_precedence_atpkg_env_then_chain_then_anonymous() {
        // env wins AND the chain closure is provably never called.
        let (t, src) = pick_pkg_token(Some("ghp_dedicated".into()), || {
            panic!("chain must not run when ATPKG_TOKEN is set")
        });
        assert_eq!(t, "ghp_dedicated");
        assert_eq!(src.as_deref(), Some("$ATPKG_TOKEN"));
        // Empty env value is treated as absent → the chain supplies token + source.
        let (t, src) = pick_pkg_token(Some(String::new()), || {
            Some(("gho_ambient".into(), "gh auth token".into()))
        });
        assert_eq!(t, "gho_ambient");
        assert_eq!(src.as_deref(), Some("gh auth token"));
        // No env → the chain.
        let (t, src) = pick_pkg_token(None, || {
            Some((
                "ghp_keychain".into(),
                "keychain item aterm-update-token".into(),
            ))
        });
        assert_eq!(t, "ghp_keychain");
        assert_eq!(src.as_deref(), Some("keychain item aterm-update-token"));
        // Nothing anywhere → the anonymous empty token, no source to report.
        let (t, src) = pick_pkg_token(None, || None);
        assert!(t.is_empty());
        assert_eq!(src, None);
    }

    /// The chain-engagement predicate mirrors `token.rs::needs_ambient_credential`
    /// by construction (same constants); pin the behavior at both poles so the
    /// mirror can never silently drift: ONLY the compiled public channel slug is
    /// anonymous by construction, any other owner OR repo runs the full chain.
    #[test]
    fn only_the_public_channel_slug_skips_the_credential_chain() {
        assert!(
            !engages_credential_chain(
                aterm_update_core::DEFAULT_OWNER,
                aterm_update_core::DEFAULT_REPO
            ),
            "the compiled public channel needs no ambient credential"
        );
        assert!(
            engages_credential_chain("someone-else", aterm_update_core::DEFAULT_REPO),
            "a foreign owner engages the full chain"
        );
        assert!(
            engages_credential_chain(aterm_update_core::DEFAULT_OWNER, "some-other-repo"),
            "a foreign repo engages the full chain"
        );
        // NON-VACUITY for the destination tests below: the compiled index default
        // IS the public channel slug, so the default account resolves to the
        // anonymous-by-construction destination.
        assert!(!engages_credential_chain(
            aterm_update_core::ATPKG_INDEX_OWNER,
            crate::manifest::INDEX_REPO
        ));
    }

    /// REGRESSION (adversarial review 2026-08-11): keying the credential chain to
    /// the index destination ALONE starved the `[packages.links]` lane — on the
    /// compiled default account the index slug is the public channel, so the
    /// chain's gate short-circuited and a links override to a private repo went
    /// out anonymous (404 on a machine whose keychain / 0600-file / `gh auth`
    /// credential had worked the round before). The pick must range over EVERY
    /// destination the fetcher reaches: index slug + all override slugs.
    #[test]
    fn credential_destination_covers_index_and_links_overrides() {
        use std::collections::BTreeMap;
        let (default_owner, default_repo) = (
            aterm_update_core::ATPKG_INDEX_OWNER,
            crate::manifest::INDEX_REPO,
        );
        // Default account + no overrides → the public channel slug: the chain
        // stays anonymous by construction (the LIVE registry's proven install
        // lane — no ambient PAT is ever gathered for it).
        let dest = credential_destination(default_owner, default_repo, &BTreeMap::new());
        assert_eq!(dest, (default_owner.to_string(), default_repo.to_string()));
        assert!(
            !engages_credential_chain(&dest.0, &dest.1),
            "default + no overrides must stay anonymous by construction"
        );
        // Default account + a links override to a possibly-private repo → the
        // OVERRIDE slug, which engages the full chain (the regression case).
        let mut overrides = BTreeMap::new();
        overrides.insert("myprog".to_string(), "someorg/private-repo".to_string());
        let dest = credential_destination(default_owner, default_repo, &overrides);
        assert_eq!(
            dest,
            ("someorg".to_string(), "private-repo".to_string()),
            "a links override must key the chain to the override's slug"
        );
        assert!(
            engages_credential_chain(&dest.0, &dest.1),
            "the links-override destination must engage the full chain"
        );
        // An override that IS the public channel slug engages nothing — the pick
        // falls back to the (equally public) index slug: still anonymous.
        let mut public_override = BTreeMap::new();
        public_override.insert(
            "myprog".to_string(),
            format!("{default_owner}/{default_repo}"),
        );
        let dest = credential_destination(default_owner, default_repo, &public_override);
        assert!(
            !engages_credential_chain(&dest.0, &dest.1),
            "a public-slug override must not conjure a credential lookup"
        );
        // Repointed account → the INDEX slug engages the chain and wins even with
        // overrides present: a repoint governs every non-overridden fetch, so it
        // is the destination the one shared token must serve first.
        let dest = credential_destination("my-private-org", default_repo, &overrides);
        assert_eq!(
            dest,
            ("my-private-org".to_string(), default_repo.to_string()),
            "a repointed account keys the chain to the index slug"
        );
        assert!(
            engages_credential_chain(&dest.0, &dest.1),
            "the repointed-account destination must engage the full chain"
        );
    }

    // ---- [packages.links] reconciliation + the auto-install bootstrap ----
    // Signed-fixture helpers mirroring flow.rs's (a real USTAR+zstd archive, a
    // root-signed index, release-signed pkg manifests) but laid out as a DIR
    // REGISTRY so the production `DirFetcher` serves them (§14).

    use ring::signature::{Ed25519KeyPair, KeyPair};
    use std::io::Write as _;
    use std::path::{Path, PathBuf};

    use crate::sig::testkit;

    /// The synthetic paper master and the one machine it rosters — the SAME fixture the
    /// whole crate signs with, so a CLI test cannot accidentally prove something under a
    /// trust shape no other layer uses.
    const ROOT_SEED: [u8; 32] = testkit::MASTER_SEED;
    const RELEASE_SEED: [u8; 32] = testkit::MACHINE_SEED;

    /// The anchor the dir-registry tests resolve under: armed with the synthetic master,
    /// roster floor 0.
    fn test_anchor() -> crate::Anchor {
        crate::Anchor::of(vec![pk(&ROOT_SEED)], 0)
    }

    /// Publish the master-signed roster beside a `dir:` registry's index. Without it the
    /// DirFetcher yields no candidate at all — a registry is index PLUS the generation
    /// that authorized its signer, never one without the other.
    fn write_roster(dir: &Path) {
        let (bytes, sig) = testkit::published_roster();
        std::fs::write(
            dir.join(aterm_update_core::roster::ROSTER_ASSET),
            bytes.as_slice(),
        )
        .unwrap();
        std::fs::write(
            dir.join(aterm_update_core::roster::ROSTER_SIG_ASSET),
            sig.as_slice(),
        )
        .unwrap();
    }

    fn kp(seed: &[u8; 32]) -> Ed25519KeyPair {
        Ed25519KeyPair::from_seed_unchecked(seed).unwrap()
    }
    fn pk(seed: &[u8; 32]) -> String {
        aterm_codec::base64::encode(kp(seed).public_key().as_ref()).expect("32-byte key")
    }
    fn sign(seed: &[u8; 32], msg: &[u8]) -> Vec<u8> {
        kp(seed).sign(msg).as_ref().to_vec()
    }

    fn scratch(label: &str) -> PathBuf {
        let d = std::env::temp_dir().join(format!("atpkg-cli-{label}-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&d);
        std::fs::create_dir_all(&d).unwrap();
        std::fs::set_permissions(&d, std::fs::Permissions::from_mode(0o700)).unwrap();
        d
    }

    /// A raw USTAR + zstd archive shipping `bin/<prog>`.
    fn make_archive(dir: &Path, prog: &str, build: u64) -> PathBuf {
        fn entry(name: &str, content: &[u8]) -> Vec<u8> {
            let mut h = [0u8; 512];
            let nb = name.as_bytes();
            h[..nb.len()].copy_from_slice(nb);
            h[100..108].copy_from_slice(b"0000755\0");
            h[108..116].copy_from_slice(b"0000000\0");
            h[116..124].copy_from_slice(b"0000000\0");
            h[124..136].copy_from_slice(format!("{:011o}\0", content.len()).as_bytes());
            h[136..148].copy_from_slice(b"00000000000\0");
            h[148..156].copy_from_slice(b"        ");
            h[156] = b'0';
            h[257..263].copy_from_slice(b"ustar\0");
            h[263..265].copy_from_slice(b"00");
            let sum: u32 = h.iter().map(|&b| u32::from(b)).sum();
            h[148..156].copy_from_slice(format!("{sum:06o}\0 ").as_bytes());
            let mut out = h.to_vec();
            out.extend_from_slice(content);
            out.resize(out.len() + (512 - content.len() % 512) % 512, 0);
            out
        }
        let mut tar = Vec::new();
        tar.extend(entry(&format!("bin/{prog}"), b"#!/bin/true\n"));
        tar.resize(tar.len() + 1024, 0);
        let path = dir.join(format!("{prog}-{build}.tar.zst"));
        let f = std::fs::File::create(&path).unwrap();
        let mut enc = zstd::Encoder::new(f, 0).unwrap();
        enc.write_all(&tar).unwrap();
        enc.finish().unwrap();
        path
    }

    /// Write one program's release-signed `pkg-<prog>-<build>.toml` (+ `.sig`) beside its
    /// real archive, with the archive's genuine sha256 + tree_root baked in.
    fn write_pkg(dir: &Path, prog: &str, build: u64) {
        write_pkg_for_triple(dir, prog, build, current_triple());
    }

    /// [`write_pkg`] with an explicit artifact `target` — a manifest whose one
    /// artifact serves a FOREIGN triple is how the registry looks from every
    /// machine the publisher does not build for.
    fn write_pkg_for_triple(dir: &Path, prog: &str, build: u64, triple: &str) {
        let archive = make_archive(dir, prog, build);
        let sha = crate::tree::file_sha256(&archive).unwrap();
        let probe = dir.join(format!("probe-{prog}"));
        crate::extract::extract_tar_zst(&archive, &probe, 10_000_000, 10_000).unwrap();
        let root = crate::tree::tree_root(&probe).unwrap();
        let _ = std::fs::remove_dir_all(&probe);
        let body = format!(
            "schema = 2\nprogram = \"{prog}\"\nversion = \"0.1\"\nbuild_number = {build}\n\
             exposes = [\"{prog}\"]\n\
             [[artifact]]\ntarget = \"{triple}\"\nkind = \"binary\"\n\
             asset = \"{prog}-{build}.tar.zst\"\nsha256 = \"{sha}\"\ntree_root = \"{root}\"\n\
             size = 100\n[artifact.cost]\ndisk_installed = 1048576\n"
        );
        let name = dir.join(format!("pkg-{prog}-{build}.toml"));
        std::fs::write(&name, body.as_bytes()).unwrap();
        std::fs::write(
            dir.join(format!("pkg-{prog}-{build}.toml.sig")),
            sign(&RELEASE_SEED, body.as_bytes()),
        )
        .unwrap();
    }

    /// Write the root-signed index for a 4-program default set on `channel`:
    /// `ay` (installable), `ny` (a second installable, for exclude), `zz`
    /// (pin YANKED — must never install), `lk` (installable, dev-linked in
    /// the tests) — plus real signed pkgs/archives for the non-yanked members.
    fn write_registry(dir: &Path, channel: &str) {
        write_registry_at(dir, channel, 41);
    }

    /// As [`write_registry`], with an explicit `index_build` (the seed-admission tests
    /// need a sealed index strictly above / equal to the durable floor).
    fn write_registry_at(dir: &Path, channel: &str, index_build: u64) {
        let index_body = format!(
            "schema = 2\nindex_build = {index_build}\nvalid_until = \"2026-07-05T12:00:00Z\"\n\
             machine_id = \"{id}\"\nroster_seq = {seq}\n\
             [programs.ay]\nrepo = \"ay\"\n[programs.ny]\nrepo = \"ny\"\n\
             [programs.zz]\nrepo = \"zz\"\n[programs.lk]\nrepo = \"lk\"\n\
             [[channels]]\nname = \"{channel}\"\nchannel_build = 1\nmin_build = 0\n\
             yanked = [\"zz@5\"]\npin = {{ ay = 18, ny = 7, zz = 5, lk = 3 }}\n",
            id = testkit::MACHINE_ID,
            seq = testkit::SEQ
        );
        std::fs::write(dir.join("index.toml"), index_body.as_bytes()).unwrap();
        // The index is MACHINE-signed now; only the roster is signed by the master.
        std::fs::write(
            dir.join("index.toml.sig"),
            sign(&RELEASE_SEED, index_body.as_bytes()),
        )
        .unwrap();
        write_roster(dir);
        write_pkg(dir, "ay", 18);
        write_pkg(dir, "ny", 7);
        write_pkg(dir, "lk", 3);
    }

    /// A fake checkout with a built `target/release/<prog>` bin.
    fn checkout(label: &str, prog: &str) -> PathBuf {
        let d = scratch(&format!("co-{label}"));
        std::fs::create_dir_all(d.join("target/release")).unwrap();
        std::fs::write(d.join("target/release").join(prog), b"#!/bin/true\n").unwrap();
        d
    }

    /// A COUNTING [`crate::DirFetcher`]. Every index resolve reaches the fetcher through
    /// `index_candidates` (the `dir:` fetcher publishes no `index_identities`, so the §14
    /// identity hit path is not in play here), which makes the counter the instrument for
    /// "the pass verify-selects ONCE": without it, a lane that quietly resolved a second
    /// time would still pass every functional assertion.
    struct CountingFetcher {
        inner: crate::DirFetcher,
        resolves: std::cell::Cell<u32>,
    }

    impl CountingFetcher {
        fn new(dir: PathBuf) -> Self {
            Self {
                inner: crate::DirFetcher::new(dir),
                resolves: std::cell::Cell::new(0),
            }
        }

        fn resolves(&self) -> u32 {
            self.resolves.get()
        }
    }

    impl crate::flow::Fetcher for CountingFetcher {
        fn index_candidates(&self) -> Result<Vec<crate::Candidate>, String> {
            self.resolves.set(self.resolves.get() + 1);
            crate::flow::Fetcher::index_candidates(&self.inner)
        }

        fn pkg_manifest(
            &self,
            repo: &str,
            program: &str,
            build: u64,
        ) -> Result<(Vec<u8>, Vec<u8>), String> {
            crate::flow::Fetcher::pkg_manifest(&self.inner, repo, program, build)
        }

        fn download(&self, repo: &str, asset: &str, dest: &Path) -> Result<(), String> {
            crate::flow::Fetcher::download(&self.inner, repo, asset, dest)
        }

        fn source_id(&self) -> String {
            crate::flow::Fetcher::source_id(&self.inner)
        }
    }

    /// ONE PASS, ONE VERIFY-SELECT. `apply_channel` resolved and verified the signed
    /// index, and then every other lane of the SAME pass resolved it again: the
    /// set-completion lane an adopted machine runs every tick, the attestation repair,
    /// the alias reconcile (through the §14 cache), the empty-store diagnosis. Each
    /// repeat re-read and TOML-parsed `index-cache.toml`, re-ran the master + machine
    /// signature checks over every candidate, re-admitted the roster and re-observed its
    /// generation — for an answer that cannot differ inside one pass, and not one byte of
    /// it network work. The pass resolves once and hands that index down.
    ///
    /// The attestation repair reads the REAL clock, so a fixture index whose signed
    /// window is long past cannot exercise it here; its wiring is pinned by shape in the
    /// verb test below.
    #[test]
    fn a_pass_verify_selects_the_index_once_and_hands_it_to_every_lane() {
        let dir = scratch("one-resolve");
        write_registry(&dir, "stable");
        let layout = temp_layout("one-resolve");
        let fetcher = CountingFetcher::new(dir.clone());
        // `ny` and `zz` are narrowed out, and both measured passes below run over a
        // COMPLETE set: what is under test is the LANE's own verify-select, and a lane
        // that still has members to install also runs the per-program `flow::install`
        // path, which resolves for itself (the loop `apply_channel`'s own doc contrasts
        // itself with) and would drown the number being measured.
        let cfg = crate::config::PackagesConfig {
            exclude: Some(vec!["ny".into(), "zz".into()]),
            ..Default::default()
        };
        // The pass's one verify-select, as the apply lane runs it.
        let index = crate::resolve_verified_index(
            &fetcher,
            &layout,
            &test_anchor(),
            build_floor(&layout),
            0,
        )
        .expect("the signed fixture index verifies");
        assert_eq!(fetcher.resolves(), 1, "the pass resolved its index");

        // Lay the set down first (this pass installs, so it resolves per program too).
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.index_error, None);
        assert_eq!(
            crate::active_builds(&layout).get("ay").copied(),
            Some(18),
            "a real pass: the missing members installed"
        );
        let settled = fetcher.resolves();

        // THE FINDING, on the shape the six-hourly tick actually meets: a complete set,
        // nothing to install. Handed the pass's index, the set-completion lane
        // verify-selects NOTHING.
        let out = super::install_default_set_with_path(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            super::Consent::InstallingAterm,
            0,
            Some(&index),
            Some(std::ffi::OsStr::new("")),
        );
        assert_eq!(
            out.index_error, None,
            "the handed index IS the pass's index"
        );
        assert_eq!(
            fetcher.resolves(),
            settled,
            "the set-completion lane re-used the pass's index"
        );

        // …while the same pass with no index in hand resolves one for itself: that second
        // verify-select per tick is exactly what the update pass used to pay.
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.index_error, None);
        assert_eq!(
            fetcher.resolves(),
            settled + 1,
            "a lane holding no index resolves one"
        );
        let settled = fetcher.resolves();

        // AND NO GATE IS SKIPPED. Past the signed `valid_until` the held index is refused
        // re-use, the lane resolves for itself, and that resolve meets the identical
        // freshness refusal it always did.
        let lapsed = 4_000_000_000;
        assert!(
            reusable_index(Some(&index), lapsed).is_none(),
            "a lapsed freshness window is never re-used"
        );
        let out = super::install_default_set_with_path(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            super::Consent::InstallingAterm,
            lapsed,
            Some(&index),
            Some(std::ffi::OsStr::new("")),
        );
        assert!(
            out.index_error.is_some(),
            "a lapsed index is refused, not quietly re-used"
        );
        assert_eq!(
            fetcher.resolves(),
            settled + 1,
            "the lapsed index fell through to a real resolve, which refused it"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// The wiring of the pass itself, pinned by SHAPE — the house style for
    /// `cmd_update_all_code`, which reads the GLOBAL layout and so cannot be driven by a
    /// scratch one. One verify-select at the top, threaded into every lane below.
    #[test]
    fn the_update_verb_threads_its_one_index_through_every_lane() {
        let production = production_half(include_str!("cli.rs"));
        let start = production
            .find("fn cmd_update_all_code() -> u8 {")
            .expect("the update verb");
        let end = production[start..]
            .find("\nfn ")
            .map(|i| start + i)
            .expect("a function after the verb");
        let body = &production[start..end];
        assert!(
            body.contains("crate::apply_channel_with("),
            "the apply runs against the index the pass itself resolved"
        );
        assert!(
            !body.contains("crate::apply_channel("),
            "…so the pass never calls the resolving wrapper, which would resolve twice"
        );
        assert!(
            !body.contains("cached_index("),
            "the alias reconcile reads the pass's index, not a second verify-select of \
             the §14 cache (which skips the freshness gate as well)"
        );
        assert!(
            body.contains("recover_missing_roots(&layout, &*fetcher, Some(&index))"),
            "the attestation repair is handed the pass's index"
        );
        assert!(
            body.contains("pass_index.as_ref(),"),
            "the set-completion lane is handed it too"
        );
        assert!(
            body.contains("match reusable_index(pass_index.as_ref(), now_unix())"),
            "and so is the empty-store diagnosis, through the freshness re-gate"
        );
    }

    // THE auto-install decision matrix (§11), against the production DirFetcher over a
    // real signed dir registry: a missing default-set member INSTALLS; an `exclude`d
    // member is narrowed out; a dev-linked member hard-skips; a yanked pin never
    // installs; an already-installed member is left to the update pass. None of the
    // skips count as failures (the 6h loop must not scream about correct states).
    #[test]
    fn default_set_bootstrap_installs_missing_and_skips_excluded_linked_yanked() {
        let dir = scratch("bootstrap");
        write_registry(&dir, "stable");
        let layout = temp_layout("bootstrap");
        // Dev-link `lk` (the hard-skip member).
        let co = checkout("bootstrap", "lk");
        crate::link(&layout, "lk", &co, &[PathBuf::from("target/release/lk")]).unwrap();
        let cfg = crate::config::PackagesConfig {
            exclude: Some(vec!["ny".into()]),
            ..Default::default()
        };
        let fetcher = crate::DirFetcher::new(dir.clone());
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert_eq!(failures, 0, "skips are never failures");
        let active = crate::active_builds(&layout);
        assert_eq!(
            active.get("ay").copied(),
            Some(18),
            "missing member installed"
        );
        assert!(!active.contains_key("ny"), "excluded member narrowed out");
        assert!(!active.contains_key("zz"), "yanked pin never installs");
        assert!(!active.contains_key("lk"), "dev-linked member hard-skipped");
        assert!(
            crate::is_linked(&layout, "lk"),
            "the dev link survives untouched"
        );
        // Idempotence: a second pass finds ay installed and re-installs nothing.
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert_eq!(failures, 0);
        assert_eq!(crate::active_builds(&layout).get("ay").copied(), Some(18));
        // The durable floor advanced to the trusted index (§8 gate 3).
        assert_eq!(crate::sig::Floor::new(layout.floor()).current(), 41);
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
        let _ = std::fs::remove_dir_all(&co);
    }

    // THE YANK LIST IS HONOURED BY EVERY LANE, WITH ONE STATE STRING. `zz` is pinned at
    // 5 and `yanked = ["zz@5"]`: the gate decides Tombstone; the default-set pass skips
    // it (no failure, no store dir, no shim) and records `tombstoned: pin yanked/below
    // floor`; the update lane tombstones a LIVE zz@5 with the same row and takes its
    // shims off PATH. Supply side will yank trust-mc 20065 — this is what a client does
    // with that.
    #[test]
    fn a_yanked_pinned_build_is_skipped_with_a_recorded_tombstone_row() {
        let dir = scratch("yank-row");
        write_registry(&dir, "stable");
        let layout = temp_layout("yank-row");
        let fetcher = crate::DirFetcher::new(dir.clone());
        // The pure gate, first.
        let index = crate::resolve_verified_index(
            &fetcher,
            &layout,
            &test_anchor(),
            build_floor(&layout),
            0,
        )
        .expect("the signed fixture index verifies");
        let ch = index
            .channels
            .iter()
            .find(|c| c.name == "stable")
            .expect("the stable channel");
        assert!(crate::gate::is_yanked(ch, "zz", 5));
        assert_eq!(
            crate::gate::decide(ch, "zz", None),
            crate::ApplyDecision::Tombstone
        );
        assert_eq!(
            crate::gate::decide(ch, "zz", Some(5)),
            crate::ApplyDecision::Tombstone,
            "a live copy of the yanked pin is not kept either"
        );
        // The default-set lane: skipped, said, recorded.
        let cfg = crate::config::PackagesConfig {
            exclude: Some(vec!["ny".into()]),
            ..Default::default()
        };
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0, "a yanked pin is a skip, never a failure");
        let active = crate::active_builds(&layout);
        assert!(!active.contains_key("zz"), "the yanked pin never installs");
        assert!(
            !layout.build_dir("zz", 5).exists(),
            "not a byte of the yanked build is staged"
        );
        assert!(crate::which(&layout, "zz").is_none(), "no shim promises it");
        let status = crate::status::read(&layout).expect("a status record");
        assert_eq!(status.programs["zz"].state, crate::state::TOMBSTONED_PIN);
        assert_eq!(status.programs["zz"].installed_build, None);
        assert!(
            crate::state::TOMBSTONED_PIN.starts_with("tombstoned:"),
            "doctor reads the row as a fault by this prefix"
        );
        // The update lane over a LIVE copy of the yanked build: tombstoned with the
        // same words, shims off PATH.
        make_live(&layout, "zz", 5);
        assert_eq!(crate::active_builds(&layout).get("zz").copied(), Some(5));
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert!(
            matches!(outcome_of(&report, "zz"), crate::TxnOutcome::Tombstoned(_)),
            "{:?}",
            report.groups
        );
        let status = crate::status::read(&layout).expect("a status record");
        assert_eq!(status.programs["zz"].state, crate::state::TOMBSTONED_PIN);
        assert!(
            !crate::active_builds(&layout).contains_key("zz"),
            "a tombstoned program's shims no longer resolve to a build"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// AUDIT-2 ITEM 2 (+ design §2): a program the registry publishes NO artifact for
    /// on this triple neither announces, nor counts as a failure, nor leaves a pending
    /// stub on PATH — and ITS OWN status row records the canonical `unavailable on
    /// <target>: <hint>` state, so a pre-existing stub's `__pending` tells the truth
    /// instead of promising a launch will provision it. Served members in the same pass
    /// still install normally, and BECAUSE one does, the toolset-wide `blocked` verdict
    /// (reserved for a machine the index serves nothing on) is NOT raised: a partial
    /// matrix is a normal state, never a fault `doctor` reports.
    #[test]
    fn unserved_triple_members_skip_quietly_with_a_recorded_reason() {
        let dir = scratch("unserved");
        write_registry(&dir, "stable");
        // ay's one artifact serves a machine this test is not running on.
        write_pkg_for_triple(&dir, "ay", 18, "riscv64gc-unknown-linux-gnu");
        let layout = temp_layout("unserved");
        let cfg = crate::config::PackagesConfig::default();
        let fetcher = crate::DirFetcher::new(dir.clone());
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(
            out.failures, 0,
            "unserved is a correct state, not a failure"
        );
        let active = crate::active_builds(&layout);
        assert!(!active.contains_key("ay"), "no artifact, no install");
        assert_eq!(
            active.get("ny").copied(),
            Some(7),
            "served members still install in the same pass"
        );
        assert!(
            !crate::stub::pending_stub_exists(&layout, "ay"),
            "no stub for a program whose bytes can never move here"
        );
        let status = crate::status::read(&layout).expect("status recorded");
        let row = status.programs.get("ay").expect("ay's own row");
        assert_eq!(row.state, crate::state::unavailable(current_triple(), ""));
        assert!(
            !status.programs.contains_key("*toolset*"),
            "ny was served, so the machine is not blocked as a whole: {:?}",
            status.programs.keys().collect::<Vec<_>>()
        );
        // (`zz`'s yanked pin IS a fault — its `tombstoned:` row is the point of
        // `a_yanked_pinned_build_is_skipped_with_a_recorded_tombstone_row`.)
        assert!(
            crate::doctor::recorded_problems(Some(&status))
                .iter()
                .all(|p| !p.starts_with("ay:")),
            "an unserved member is a state, never a doctor fault"
        );
        let lines = pending_state_lines(&layout, "ay");
        assert!(
            lines
                .iter()
                .any(|l| l.contains(&crate::state::unavailable(current_triple(), ""))),
            "__pending surfaces the recorded reason, not the launch promise: {lines:?}"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// A two-program registry: `ay` (default set) and `xc` (`extra = true`), the
    /// extra's one artifact serving `extra_triple`.
    fn write_registry_with_extra(dir: &Path, channel: &str, extra_triple: &str) {
        let index_body = format!(
            "schema = 2\nindex_build = 41\nvalid_until = \"2026-07-05T12:00:00Z\"\n\
             machine_id = \"{id}\"\nroster_seq = {seq}\n\
             [programs.ay]\nrepo = \"ay\"\n[programs.xc]\nrepo = \"xc\"\nextra = true\n\
             [[channels]]\nname = \"{channel}\"\nchannel_build = 1\nmin_build = 0\n\
             pin = {{ ay = 18, xc = 9 }}\n",
            id = testkit::MACHINE_ID,
            seq = testkit::SEQ
        );
        std::fs::write(dir.join("index.toml"), index_body.as_bytes()).unwrap();
        std::fs::write(
            dir.join("index.toml.sig"),
            sign(&RELEASE_SEED, index_body.as_bytes()),
        )
        .unwrap();
        write_roster(dir);
        write_pkg(dir, "ay", 18);
        write_pkg_for_triple(dir, "xc", 9, extra_triple);
    }

    /// A three-program registry for the dependency tests: `ay` (default set, `requires
    /// = ["xc"]`), `ny` (default set, no requires), `xc` (`extra = true`) — every pkg
    /// served for this triple.
    fn write_registry_with_requires(dir: &Path) {
        let index_body = format!(
            "schema = 2\nindex_build = 41\nvalid_until = \"2026-07-05T12:00:00Z\"\n\
             machine_id = \"{id}\"\nroster_seq = {seq}\n\
             [programs.ay]\nrepo = \"ay\"\nrequires = [\"xc\"]\n\
             [programs.ny]\nrepo = \"ny\"\n\
             [programs.xc]\nrepo = \"xc\"\nextra = true\n\
             [[channels]]\nname = \"stable\"\nchannel_build = 1\nmin_build = 0\n\
             pin = {{ ay = 18, ny = 7, xc = 9 }}\n",
            id = testkit::MACHINE_ID,
            seq = testkit::SEQ
        );
        std::fs::write(dir.join("index.toml"), index_body.as_bytes()).unwrap();
        std::fs::write(
            dir.join("index.toml.sig"),
            sign(&RELEASE_SEED, index_body.as_bytes()),
        )
        .unwrap();
        write_roster(dir);
        write_pkg(dir, "ay", 18);
        write_pkg(dir, "ny", 7);
        write_pkg(dir, "xc", 9);
    }

    /// THE BLOCKED STATE AND ITS RETRY (§17.10): `ay` requires the extra `xc`. Pass 1:
    /// xc is not opted in — it is never opted in on ay's behalf — so ay records
    /// `blocked by xc: extra — not installed (opt in: aterm pkg install xc)`, downloads
    /// nothing, counts as no failure, is no doctor fault, and `which` / `__pending` say
    /// the row's words; ny installs beside it. Pass 2, after the user opts xc in: the
    /// plan puts xc before ay (dependency-first), both install, and ay's row is the
    /// ordinary managed state — the block was a deferral, not a verdict.
    #[cfg(unix)]
    #[test]
    fn a_program_whose_requirement_is_unmet_is_blocked_not_failed_and_retried_next_pass() {
        let dir = scratch("blocked-retry");
        write_registry_with_requires(&dir);
        let layout = temp_layout("blocked-retry");
        let cfg = crate::config::PackagesConfig::default();
        let fetcher = crate::DirFetcher::new(dir.clone());
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0, "blocked is a deferral, never a failure");
        let active = crate::active_builds(&layout);
        assert!(!active.contains_key("ay"), "ay waits on xc");
        assert!(
            !active.contains_key("xc"),
            "an extra is never opted in on a dependent's behalf"
        );
        assert_eq!(
            active.get("ny").copied(),
            Some(7),
            "the rest of the set installs"
        );
        assert!(
            !layout.staging_dir("ay").exists(),
            "a blocked program never downloads a byte"
        );
        assert!(
            !out.resolved_assets.contains_key("ay"),
            "…and never resolved an artifact to download"
        );
        let blocked = crate::state::blocked("xc", &crate::state::extra_not_installed("xc"));
        let status = crate::status::read(&layout).expect("recorded");
        assert_eq!(status.programs["ay"].state, blocked);
        assert_eq!(
            status.programs["xc"].state,
            crate::state::extra_not_installed("xc")
        );
        assert!(
            crate::doctor::recorded_problems(Some(&status)).is_empty(),
            "a blocked row is never a doctor fault: {:?}",
            crate::doctor::recorded_problems(Some(&status))
        );
        // The surfaces say the row's words.
        let managed_only = std::env::join_paths([layout.bin_dir()]).unwrap();
        assert_eq!(
            which_line(&layout, "ay", Some(&managed_only)).unwrap(),
            format!(
                "ay → {} (pending stub) — {blocked}",
                layout
                    .shim(&crate::store::ToolName::new("ay").unwrap())
                    .display()
            ),
            "the stub is on PATH, and the row says why it waits"
        );
        crate::stub::remove_stub(&layout, "ay");
        assert_eq!(
            which_line(&layout, "ay", Some(&managed_only)).unwrap(),
            format!("ay → (nothing runs) — {blocked}")
        );
        let lines = pending_state_lines(&layout, "ay");
        assert!(
            lines
                .iter()
                .any(|l| l.contains("ay is waiting on xc") && l.contains("aterm pkg install ay")),
            "{lines:?}"
        );
        assert_eq!(
            bump_contents(&layout),
            "ay\n",
            "still worth a bump: xc may move"
        );
        // A second pass with nothing changed re-records nothing and installs nothing.
        let before = std::fs::read_to_string(layout.status()).unwrap();
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0);
        assert_eq!(
            std::fs::read_to_string(layout.status()).unwrap(),
            before,
            "the silent tick leaves status.toml alone"
        );
        // THE RETRY: the user opts xc in; the next pass installs xc first, then ay.
        layout.record_optin("xc").unwrap();
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0);
        let active = crate::active_builds(&layout);
        assert_eq!(active.get("xc").copied(), Some(9));
        assert_eq!(
            active.get("ay").copied(),
            Some(18),
            "unblocked and installed"
        );
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ay"].state, crate::state::managed(18, 41));
        assert_eq!(status.programs["xc"].state, crate::state::managed(9, 41));
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// THE REQUIREMENT GATE'S READING (§17.10), row by row: an active build, a dev link,
    /// a `system:` row whose path exists and an `installed via` row whose path exists
    /// are MET; a `needs admin` row, an `error:` row and an `unavailable on` row are
    /// unmet with the row itself as the dependency's state; a `system:` / `installed
    /// via` row whose path is GONE, a stale `managed` row and no row at all read `not
    /// installed` — unless the dependency is an extra not opted in, which reads the
    /// canonical extra state. The FIRST unmet requirement, in order, is the one named.
    #[cfg(unix)]
    #[test]
    fn unmet_requirement_reads_the_store_and_the_record_in_order() {
        use crate::requires::unmet_requirement;
        let layout = temp_layout("unmet");
        let index = index_of(&[
            ("ay", false, None),
            ("gh", false, Some("gh")),
            ("xc", true, None),
            ("clt", false, None),
            ("brew", false, None),
        ]);
        let present = layout.prefix.join("present-bin");
        std::fs::write(&present, "x").unwrap();
        let gone = layout.prefix.join("gone-bin");
        let row = |state: String| crate::ProgramStatus {
            installed_build: None,
            state,
            tree_root: String::new(),
        };
        for (name, state) in [
            ("gh", crate::state::system(&present, None)),
            (
                "clt",
                crate::state::installed_via("softwareupdate", &present),
            ),
            ("brew", crate::state::needs_admin("brew")),
            ("ny", String::from("error: boom")),
            ("emacs", crate::state::unavailable("t", "h")),
            ("gone-sys", crate::state::system(&gone, None)),
            ("gone-via", crate::state::installed_via("pkg", &gone)),
            ("stale", crate::state::managed(3, 41)),
        ] {
            record_status(&layout, name, row(state), "x".into());
        }
        make_live(&layout, "ay", 18);
        let s = |v: &[&str]| -> Vec<String> { v.iter().map(|x| (*x).to_string()).collect() };
        assert_eq!(unmet_requirement(&layout, &index, &[]), None);
        assert_eq!(
            unmet_requirement(&layout, &index, &s(&["ay", "gh", "clt"])),
            None,
            "active, system-satisfied (path exists), installed via (path exists)"
        );
        assert_eq!(
            unmet_requirement(&layout, &index, &s(&["ay", "brew"])),
            Some(("brew".into(), crate::state::needs_admin("brew")))
        );
        assert_eq!(
            unmet_requirement(&layout, &index, &s(&["ny", "brew"])),
            Some(("ny".into(), "error: boom".into())),
            "the first unmet, in order"
        );
        assert_eq!(
            unmet_requirement(&layout, &index, &s(&["emacs"])),
            Some(("emacs".into(), crate::state::unavailable("t", "h")))
        );
        for stale in ["gone-sys", "gone-via", "stale", "never"] {
            assert_eq!(
                unmet_requirement(&layout, &index, &s(&[stale])),
                Some((stale.into(), "not installed".into())),
                "{stale}"
            );
        }
        assert_eq!(
            unmet_requirement(&layout, &index, &s(&["xc"])),
            Some(("xc".into(), crate::state::extra_not_installed("xc")))
        );
        layout.record_optin("xc").unwrap();
        assert_eq!(
            unmet_requirement(&layout, &index, &s(&["xc"])),
            Some(("xc".into(), "not installed".into())),
            "opted in but not yet installed"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    /// A two-program registry for the update-lane gate: `ay` (`requires = ["gh"]`,
    /// pinned at 18 with a real pkg) and `gh` (`system = "gh"`, pinned, its pkg
    /// deliberately ABSENT — a system-satisfied requirement is never fetched, and the
    /// update lane never fetches for a member that is not installed).
    fn write_registry_requiring_system(dir: &Path) {
        let index_body = format!(
            "schema = 2\nindex_build = 41\nvalid_until = \"2026-07-05T12:00:00Z\"\n\
             machine_id = \"{id}\"\nroster_seq = {seq}\n\
             [programs.ay]\nrepo = \"ay\"\nrequires = [\"gh\"]\n\
             [programs.gh]\nrepo = \"gh\"\nsystem = \"gh\"\n\
             [[channels]]\nname = \"stable\"\nchannel_build = 1\nmin_build = 0\n\
             pin = {{ ay = 18, gh = 3 }}\n",
            id = testkit::MACHINE_ID,
            seq = testkit::SEQ
        );
        std::fs::write(dir.join("index.toml"), index_body.as_bytes()).unwrap();
        std::fs::write(
            dir.join("index.toml.sig"),
            sign(&RELEASE_SEED, index_body.as_bytes()),
        )
        .unwrap();
        write_roster(dir);
        write_pkg(dir, "ay", 18);
    }

    /// One update pass over `layout`, exactly as `cmd_update_all` runs it — the channel
    /// apply over the live builds, then the per-group report that writes the rows —
    /// returning the report and its failure count.
    fn update_pass(
        layout: &crate::store::Layout,
        fetcher: &crate::DirFetcher,
    ) -> (crate::ChannelApplyReport, u32) {
        let installed = crate::active_builds(layout);
        let report = crate::apply_channel(
            fetcher,
            layout,
            &test_anchor(),
            "stable",
            current_triple(),
            &installed,
            &[],
            build_floor(layout),
            0,
        )
        .expect("the channel applies");
        let failures = report_channel_apply(layout, &installed, &report);
        (report, failures)
    }

    /// The outcome the report carries for `program`'s group.
    fn outcome_of<'a>(
        report: &'a crate::ChannelApplyReport,
        program: &str,
    ) -> &'a crate::TxnOutcome {
        &report
            .groups
            .iter()
            .find(|(g, _)| g.members.iter().any(|m| m == program))
            .unwrap_or_else(|| panic!("{program}'s group is in the report: {:?}", report.groups))
            .1
    }

    /// THE UPDATE LANE HONOURS THE REQUIREMENT GATE (§17.10): `ay` (installed at 17,
    /// pinned at 18) requires `xc`, which was opted in, installed, and then uninstalled.
    /// The pass holds ay on 17 — `blocked by xc: not installed`, its build and signed
    /// root kept on the row, no failure, no doctor fault, not a byte staged — and moves
    /// it to 18 the pass after xc is back. Blocked again later (xc gone a second time),
    /// ay stays on 18 with the row saying so; xc back with nothing left to move, the
    /// UpToDate pass lifts the row to the ordinary managed state.
    // An UpToDate pass rewrites every live member's row to name THIS pass's index —
    // migrating the legacy `active` spelling and re-stamping `pinned by index 15` to the
    // current build — while keeping the pass's own outcome sentence, and leaving every
    // other spelling (a SHADOWED row, a fault) to the arm that owns it.
    #[test]
    fn an_up_to_date_pass_migrates_legacy_rows_and_restamps_the_index() {
        assert!(up_to_date_row_needs_rewrite(None, 18, 41));
        assert!(up_to_date_row_needs_rewrite(Some("active"), 18, 41));
        assert!(up_to_date_row_needs_rewrite(
            Some(&crate::state::managed(18, 15)),
            18,
            41
        ));
        assert!(!up_to_date_row_needs_rewrite(
            Some(&crate::state::managed(18, 41)),
            18,
            41
        ));
        for other in [
            crate::state::shadowed(18, Path::new("/opt/homebrew/bin/ay")),
            "tombstoned: pin yanked/below floor".to_string(),
            crate::state::system(Path::new("/p"), None),
        ] {
            assert!(
                !up_to_date_row_needs_rewrite(Some(&other), 18, 41),
                "{other} belongs to another arm"
            );
        }
        // THE FAULT LIFT (observed live 2026-09-14): a row a failed pass wrote is the
        // one thing no later pass took back, so `clean`/`nn`/`ny` read `aborted: stage`
        // for days beside the healthy builds they were running and doctor said PROBLEM.
        // A member this pass finds live at its pin has, by that fact, no such fault.
        for fault in [
            "aborted: stage",
            "aborted: flip (rolled back)",
            "error: coherence group 'trust' bootstrap aborted",
        ] {
            assert!(
                up_to_date_row_needs_rewrite(Some(fault), 18, 41),
                "{fault} is over — the UpToDate pass lifts it"
            );
        }
        let dir = scratch("update-restamp");
        write_registry(&dir, "stable"); // index_build 41, ay pinned at 18
        let layout = temp_layout("update-restamp");
        let fetcher = crate::DirFetcher::new(dir.clone());
        make_live(&layout, "ay", 18);
        record_status(
            &layout,
            "ay",
            crate::ProgramStatus {
                installed_build: Some(18),
                state: "active".into(),
                tree_root: "abc".into(),
            },
            "up to date (index build 41)".into(),
        );
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert!(matches!(
            outcome_of(&report, "ay"),
            crate::TxnOutcome::UpToDate
        ));
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ay"].state, crate::state::managed(18, 41));
        assert_eq!(status.programs["ay"].installed_build, Some(18));
        assert_eq!(
            status.programs["ay"].tree_root, "abc",
            "the attestation survives a row rewrite"
        );
        assert_eq!(
            status.outcome, "up to date (index build 41)",
            "a row migration never speaks for the pass"
        );
        // A stale index stamp is refreshed the same way.
        record_status(
            &layout,
            "ay",
            crate::ProgramStatus {
                installed_build: Some(18),
                state: crate::state::managed(18, 15),
                tree_root: "abc".into(),
            },
            "up to date (index build 41)".into(),
        );
        let _ = update_pass(&layout, &fetcher);
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ay"].state, crate::state::managed(18, 41));
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    #[test]
    fn the_update_pass_holds_an_installed_dependent_whose_dependency_was_uninstalled() {
        let dir = scratch("update-blocked");
        write_registry_with_requires(&dir);
        let layout = temp_layout("update-blocked");
        let fetcher = crate::DirFetcher::new(dir.clone());
        make_live(&layout, "ay", 17);
        record_status(
            &layout,
            "ay",
            crate::ProgramStatus {
                installed_build: Some(17),
                state: crate::state::managed(17, 40),
                tree_root: "root17".into(),
            },
            "up to date".into(),
        );
        // xc: opted in and once installed — its stale row still says so — but gone from
        // the store.
        layout.record_optin("xc").unwrap();
        record_status(
            &layout,
            "xc",
            crate::ProgramStatus {
                installed_build: None,
                state: crate::state::managed(9, 40),
                tree_root: String::new(),
            },
            "up to date".into(),
        );

        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0, "a block is a deferral, never a failure");
        let blocked_by_xc = crate::TxnOutcome::Blocked {
            dep: "xc".into(),
            dep_state: "not installed".into(),
        };
        assert_eq!(*outcome_of(&report, "ay"), blocked_by_xc);
        assert!(
            report
                .groups
                .iter()
                .all(|(g, _)| !g.members.iter().any(|m| m == "xc")),
            "the update lane never installs a missing dependency: {:?}",
            report.groups
        );
        assert_eq!(
            crate::active_builds(&layout).get("ay").copied(),
            Some(17),
            "held on its current build"
        );
        assert!(
            !layout.build_dir("ay", 18).exists() && !layout.staging_dir("ay").exists(),
            "not a byte staged"
        );
        assert!(
            !report.resolved_assets.contains_key("ay"),
            "…and no artifact resolved"
        );
        let blocked = crate::state::blocked("xc", "not installed");
        let status = crate::status::read(&layout).expect("recorded");
        assert_eq!(status.programs["ay"].state, blocked);
        assert_eq!(
            status.programs["ay"].installed_build,
            Some(17),
            "the build stays on the row"
        );
        assert_eq!(
            status.programs["ay"].tree_root, "root17",
            "the attestation stays on the row"
        );
        assert!(
            crate::doctor::recorded_problems(Some(&status)).is_empty(),
            "a blocked row is never a doctor fault: {:?}",
            crate::doctor::recorded_problems(Some(&status))
        );

        // xc back: the next pass moves ay to its pin.
        make_live(&layout, "xc", 9);
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(
            *outcome_of(&report, "ay"),
            crate::TxnOutcome::Applied(vec!["ay".into()])
        );
        assert_eq!(crate::active_builds(&layout).get("ay").copied(), Some(18));
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ay"].state, crate::state::managed(18, 41));
        let root18 = status.programs["ay"].tree_root.clone();
        assert!(
            !root18.is_empty(),
            "the applied build's signed root is recorded"
        );

        // xc gone again: ay is held on 18, the row says why, the root survives.
        std::fs::remove_file(layout.shim(&crate::store::ToolName::new("xc").unwrap())).unwrap();
        assert!(!crate::active_builds(&layout).contains_key("xc"));
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(*outcome_of(&report, "ay"), blocked_by_xc);
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ay"].state, blocked);
        assert_eq!(status.programs["ay"].installed_build, Some(18));
        assert_eq!(status.programs["ay"].tree_root, root18);

        // xc back and ay already on its pin: the UpToDate pass lifts the row.
        make_live(&layout, "xc", 9);
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(*outcome_of(&report, "ay"), crate::TxnOutcome::UpToDate);
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(
            status.programs["ay"].state,
            crate::state::managed(18, 41),
            "the block lifts with nothing left to move"
        );
        assert_eq!(status.programs["ay"].installed_build, Some(18));
        assert_eq!(status.programs["ay"].tree_root, root18);
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// A SYSTEM-SATISFIED DEPENDENCY COUNTS on the update lane (rule 2 on the `requires`
    /// path): `ay` requires `gh`, and gh's row says the user's own copy satisfies it — ay
    /// updates, and gh is never fetched. When that copy is gone (the row's path no
    /// longer exists) the same row no longer counts, and ay is held on its build with
    /// `blocked by gh: not installed`.
    #[test]
    fn the_update_pass_counts_a_system_satisfied_dependency() {
        let dir = scratch("update-system-dep");
        write_registry_requiring_system(&dir);
        let layout = temp_layout("update-system-dep");
        let fetcher = crate::DirFetcher::new(dir.clone());
        make_live(&layout, "ay", 17);
        let gh = layout.prefix.join("opt-homebrew-bin-gh");
        std::fs::write(&gh, "gh").unwrap();
        record_status(
            &layout,
            "gh",
            crate::ProgramStatus {
                installed_build: None,
                state: crate::state::system(&gh, None),
                tree_root: String::new(),
            },
            "up to date".into(),
        );
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(
            *outcome_of(&report, "ay"),
            crate::TxnOutcome::Applied(vec!["ay".into()])
        );
        assert_eq!(crate::active_builds(&layout).get("ay").copied(), Some(18));
        assert!(
            report
                .groups
                .iter()
                .all(|(g, _)| !g.members.iter().any(|m| m == "gh")),
            "a system-satisfied member is never an update: {:?}",
            report.groups
        );
        // The user's copy gone: the row still says `system:`, but it no longer counts.
        std::fs::remove_file(&gh).unwrap();
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(
            *outcome_of(&report, "ay"),
            crate::TxnOutcome::Blocked {
                dep: "gh".into(),
                dep_state: "not installed".into()
            }
        );
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(
            status.programs["ay"].state,
            crate::state::blocked("gh", "not installed")
        );
        assert_eq!(status.programs["ay"].installed_build, Some(18));
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// An installed `rustc`-locked tuple's registry: the channel moves `ta`@3 + `tb`@5 to
    /// `ta`@4 + `tb`@6, with tb's new build published only for `tb_triple` (how it looks from a
    /// host the publisher does not build that pin for), and `yanked` as the deny-list.
    fn write_installed_group_registry(dir: &Path, tb_triple: &str, yanked: &[&str]) {
        write_group_index(dir, "ta = 4, tb = 6", yanked);
        write_pkg(dir, "ta", 4);
        write_pkg_for_triple(dir, "tb", 6, tb_triple);
    }

    /// The signed index (build 51) of a `rustc`-locked tuple `ta` + `tb` pinned at `pins`,
    /// with `yanked` as the channel's deny-list, published beside its roster.
    fn write_group_index(dir: &Path, pins: &str, yanked: &[&str]) {
        let yanked: Vec<String> = yanked.iter().map(|y| format!("\"{y}\"")).collect();
        let index_body = format!(
            "schema = 2\nindex_build = 51\nvalid_until = \"2026-07-05T12:00:00Z\"\n\
             machine_id = \"{id}\"\nroster_seq = {seq}\n\
             [programs.ta]\nrepo = \"ta\"\ncoherence_group = \"rustc\"\n\
             [programs.tb]\nrepo = \"tb\"\ncoherence_group = \"rustc\"\n\
             [[channels]]\nname = \"stable\"\nchannel_build = 1\nmin_build = 0\n\
             yanked = [{yanked}]\npin = {{ {pins} }}\n",
            id = testkit::MACHINE_ID,
            seq = testkit::SEQ,
            yanked = yanked.join(", ")
        );
        std::fs::write(dir.join("index.toml"), index_body.as_bytes()).unwrap();
        std::fs::write(
            dir.join("index.toml.sig"),
            sign(&RELEASE_SEED, index_body.as_bytes()),
        )
        .unwrap();
        write_roster(dir);
    }

    /// `ta`@3 + `tb`@5 genuinely live, each with a managed row and a recorded root.
    fn make_installed_group_live(layout: &crate::store::Layout) {
        for (p, b) in [("ta", 3u64), ("tb", 5u64)] {
            make_live(layout, p, b);
            record_status(
                layout,
                p,
                crate::ProgramStatus {
                    installed_build: Some(b),
                    state: crate::state::managed(b, 50),
                    tree_root: format!("root{b}"),
                },
                "up to date (index build 50)".into(),
            );
        }
    }

    /// A tuple that cannot fully exist on this host is a correct state, not a failure
    /// ([`crate::flow::group_missing_triple`]): the group is held, no failure is counted, each
    /// installed member's row says which build is unpublished and which one stays, the live
    /// links do not move, and nothing is staged. The pass after the build publishes moves it.
    #[test]
    fn an_installed_group_whose_new_pin_is_unpublished_here_is_held_not_failed() {
        let dir = scratch("update-held-triple");
        write_installed_group_registry(&dir, "riscv64gc-unknown-linux-gnu", &[]);
        let layout = temp_layout("update-held-triple");
        let fetcher = crate::DirFetcher::new(dir.clone());
        make_installed_group_live(&layout);
        let links = |layout: &crate::store::Layout| {
            [
                std::fs::read_link(layout.program_current("ta")).unwrap(),
                std::fs::read_link(layout.program_current("tb")).unwrap(),
                std::fs::read_link(layout.channel_current("stable")).unwrap(),
            ]
        };
        let before = links(&layout);

        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(
            failures, 0,
            "an unpublished pin is a hold, never a failure: {:?}",
            report.groups
        );
        assert_eq!(
            *outcome_of(&report, "tb"),
            crate::TxnOutcome::Unpublished {
                member: "tb".into(),
                build: 6,
                triple: current_triple().into(),
            },
            "the group is held, not aborted"
        );
        let active = crate::active_builds(&layout);
        assert_eq!(
            (active.get("ta").copied(), active.get("tb").copied()),
            (Some(3), Some(5)),
            "both members stay on their current builds"
        );
        assert_eq!(links(&layout), before, "the live links never moved");
        for (p, new) in [("ta", 4), ("tb", 6)] {
            assert!(!layout.build_dir(p, new).exists(), "{p}@{new} was staged");
            assert!(!layout.staging_dir(p).exists(), "{p} touched staging/");
        }
        assert!(
            report.resolved_assets.is_empty(),
            "nothing was resolved for download: {:?}",
            report.resolved_assets
        );
        let triple = current_triple();
        let status = crate::status::read(&layout).expect("recorded");
        assert_eq!(
            status.programs["tb"].state,
            format!("held: pinned build 6 is not published for {triple}; staying on build 5")
        );
        assert_eq!(
            status.programs["ta"].state,
            format!("held: tb's pinned build 6 is not published for {triple}; staying on build 3")
        );
        for (p, b) in [("ta", 3u64), ("tb", 5u64)] {
            assert_eq!(
                status.programs[p].installed_build,
                Some(b),
                "{p}'s build stays on the row"
            );
            assert_eq!(
                status.programs[p].tree_root,
                format!("root{b}"),
                "{p}'s attestation stays on the row"
            );
        }
        assert!(
            crate::doctor::recorded_problems(Some(&status)).is_empty(),
            "a held row is never a doctor fault: {:?}",
            crate::doctor::recorded_problems(Some(&status))
        );

        // The next tick says the same thing and still counts nothing.
        let (_, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0, "the hold is steady across passes");

        // The channel steps back to the builds this host runs: the UpToDate pass lifts the
        // held rows to the ordinary managed state, build and attestation kept.
        write_group_index(&dir, "ta = 3, tb = 5", &[]);
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(*outcome_of(&report, "tb"), crate::TxnOutcome::UpToDate);
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ta"].state, crate::state::managed(3, 51));
        assert_eq!(status.programs["tb"].state, crate::state::managed(5, 51));
        assert_eq!(status.programs["tb"].tree_root, "root5");

        // The build publishes for this triple and the channel moves on again: the next
        // pass moves the whole tuple.
        write_group_index(&dir, "ta = 4, tb = 6", &[]);
        write_pkg(&dir, "tb", 6);
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(
            *outcome_of(&report, "tb"),
            crate::TxnOutcome::Applied(vec!["ta".into(), "tb".into()])
        );
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ta"].state, crate::state::managed(4, 51));
        assert_eq!(status.programs["tb"].state, crate::state::managed(6, 51));
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// …but a hold never keeps a revoked build running: quietly holding the tuple would leave
    /// a yanked build live on exactly the host that has no replacement. The pass disables that
    /// build's commands and aborts loudly instead, and stays loud — later passes hold the tuple
    /// quietly for the member still installed, while the disabled row is still a doctor fault.
    #[test]
    fn an_unpublished_pin_never_quietly_holds_a_revoked_current_build() {
        let dir = scratch("update-held-revoked");
        write_installed_group_registry(&dir, "riscv64gc-unknown-linux-gnu", &["ta@3"]);
        let layout = temp_layout("update-held-revoked");
        let fetcher = crate::DirFetcher::new(dir.clone());
        make_installed_group_live(&layout);

        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(
            failures, 1,
            "a revoked build with no replacement here is a fault: {:?}",
            report.groups
        );
        assert!(
            matches!(
                outcome_of(&report, "ta"),
                crate::TxnOutcome::Aborted { failed, during_flip: false, why }
                    if failed == "ta" && why.contains("is not published for")
            ),
            "never a quiet hold over a revoked build — and the abort names what it disabled, \
             and why: {:?}",
            outcome_of(&report, "ta")
        );
        // The line this pass printed for ta names the build this triple lacks: tb's new
        // pin. ta's own replacement, ta@4, is published here.
        let triple = current_triple();
        assert_eq!(
            crate::flow::recalled_unpublished_notice("ta", "tb", 6, triple),
            format!(
                "atpkg: ta was recalled and its coherence group's new pin (tb build 6) is not \
                 published for {triple} — its commands are disabled until it is."
            )
        );
        // A live shim is an `exec` stub too — a regular file — so what marks the tombstone
        // is that it forwards NOWHERE and says why, and that the store stops counting the
        // revoked build as live.
        let shim = layout.shim(&crate::store::ToolName::new("ta").unwrap());
        assert_eq!(
            crate::platform::resolve_shim(&shim),
            None,
            "the revoked ta@3 must be disabled — its shim replaced by a tombstone"
        );
        let out = std::process::Command::new(&shim).output().unwrap();
        assert!(
            !out.status.success()
                && String::from_utf8_lossy(&out.stderr).contains("yanked/revoked"),
            "…that refuses to run and says why: {out:?}"
        );
        assert!(
            !crate::active_builds(&layout).contains_key("ta"),
            "the revoked build no longer reads as live"
        );
        assert_eq!(
            crate::active_builds(&layout).get("tb").copied(),
            Some(5),
            "the valid sibling keeps running"
        );
        for (p, new) in [("ta", 4), ("tb", 6)] {
            assert!(!layout.build_dir(p, new).exists(), "{p}@{new} was staged");
            assert!(!layout.staging_dir(p).exists(), "{p} touched staging/");
        }
        let status = crate::status::read(&layout).unwrap();
        assert!(
            status.programs["ta"].state.starts_with("aborted: stage — "),
            "{}",
            status.programs["ta"].state
        );
        assert!(
            status
                .outcome
                .starts_with("group rustc: aborted at ta during stage — "),
            "{}",
            status.outcome
        );
        let problems = crate::doctor::recorded_problems(Some(&status));
        assert!(
            problems.len() == 1 && problems[0].starts_with("ta: aborted: stage"),
            "…and doctor counts it against the disabled program: {problems:?}"
        );

        // The next pass: the tombstone took ta out of `active_builds`, so only tb is seen
        // installed and tb's unpublished pin holds the tuple quietly. ta must not go quiet
        // with it — still disabled, and still a doctor fault on its own row.
        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0, "{:?}", report.groups);
        assert_eq!(
            *outcome_of(&report, "tb"),
            crate::TxnOutcome::Unpublished {
                member: "tb".into(),
                build: 6,
                triple: triple.into(),
            }
        );
        assert_eq!(
            crate::platform::resolve_shim(&shim),
            None,
            "ta stays disabled"
        );
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(
            status.programs["tb"].state,
            crate::state::held_unpublished(None, 6, triple, 5)
        );
        assert!(
            status.programs["ta"].state.starts_with("aborted: stage — "),
            "the disabled program's row never reads as healthy: {}",
            status.programs["ta"].state
        );
        let problems = crate::doctor::recorded_problems(Some(&status));
        assert!(
            problems.len() == 1 && problems[0].starts_with("ta: aborted: stage"),
            "doctor still counts the disabled program: {problems:?}"
        );
        // …AND SO DOES THE SET-COMPLETION PRESCAN THAT RUNS NEXT IN THE SAME PASS on an
        // adopted machine: ta is not installed any more and not published here, so the
        // prescan counts it unserved — but its row is a fault this machine recorded on
        // purpose, and repainting it `unavailable on <triple>: …` (a benign state the
        // doctor does not match) hid the fault before anyone read it. The prescan's
        // measurement is the one below: the recorded row, or a tombstone shim.
        assert!(
            program_disabled_here(&layout, Some(&status), "ta"),
            "ta reads as disabled: its row says so and its shim is a tombstone"
        );
        assert!(
            !program_disabled_here(&layout, Some(&status), "tb"),
            "tb is held, not disabled"
        );
        // Either measurement alone is enough: a row that says it with no shim on disk,
        // and a tombstone shim under a row that says nothing.
        let mut rows = status.clone();
        rows.programs.remove("ta");
        assert!(
            program_disabled_here(&layout, Some(&rows), "ta"),
            "the tombstone shim alone reads as disabled"
        );
        std::fs::remove_file(&shim).unwrap();
        assert!(
            program_disabled_here(&layout, Some(&status), "ta"),
            "the recorded `aborted: stage` row alone reads as disabled"
        );
        assert!(!program_disabled_here(&layout, Some(&rows), "ta"));
        assert!(!program_disabled_here(&layout, None, "ta"));
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// The hold is only for a pin this host cannot run: the same installed tuple whose new pin
    /// is published for this triple upgrades as it always did — staged whole, flipped whole,
    /// managed rows naming the new index, the new signed roots recorded.
    #[test]
    fn an_installed_group_whose_new_pin_is_published_here_upgrades_as_before() {
        let dir = scratch("update-group-served");
        write_installed_group_registry(&dir, current_triple(), &[]);
        let layout = temp_layout("update-group-served");
        let fetcher = crate::DirFetcher::new(dir.clone());
        make_installed_group_live(&layout);

        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0);
        assert_eq!(
            *outcome_of(&report, "ta"),
            crate::TxnOutcome::Applied(vec!["ta".into(), "tb".into()])
        );
        let active = crate::active_builds(&layout);
        assert_eq!(
            (active.get("ta").copied(), active.get("tb").copied()),
            (Some(4), Some(6))
        );
        assert_eq!(
            report.resolved_assets.get("tb").map(String::as_str),
            Some("tb-6.tar.zst")
        );
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ta"].state, crate::state::managed(4, 51));
        assert_eq!(status.programs["tb"].state, crate::state::managed(6, 51));
        for p in ["ta", "tb"] {
            let root = &status.programs[p].tree_root;
            assert!(
                !root.is_empty() && !root.starts_with("root"),
                "{p}'s new signed root is recorded: {root:?}"
            );
        }
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// The hold reads only the members the pass would move. `ta` is up to date on build 3,
    /// whose signed manifest has no row for this triple; `tb`'s current build 5 is yanked, so
    /// `decide` force-upgrades it to 6, which is published here. Nothing of ta's is staged, so
    /// tb moves to 6 as before: no hold, no abort, no failure, and ta's commands keep working.
    #[test]
    fn an_up_to_date_member_never_holds_or_aborts_a_siblings_upgrade() {
        let dir = scratch("update-uptodate-unserved");
        write_group_index(&dir, "ta = 3, tb = 6", &["tb@5"]);
        write_pkg_for_triple(&dir, "ta", 3, "riscv64gc-unknown-linux-gnu");
        write_pkg(&dir, "tb", 6);
        let layout = temp_layout("update-uptodate-unserved");
        let fetcher = crate::DirFetcher::new(dir.clone());
        make_installed_group_live(&layout);

        let (report, failures) = update_pass(&layout, &fetcher);
        assert_eq!(failures, 0, "{:?}", report.groups);
        assert_eq!(
            *outcome_of(&report, "tb"),
            crate::TxnOutcome::Applied(vec!["tb".into()]),
            "the sibling's force-upgrade applies as before"
        );
        let active = crate::active_builds(&layout);
        assert_eq!(
            (active.get("ta").copied(), active.get("tb").copied()),
            (Some(3), Some(6))
        );
        let shim = layout.shim(&crate::store::ToolName::new("ta").unwrap());
        assert!(
            crate::platform::resolve_shim(&shim).is_some(),
            "ta's commands were never disabled"
        );
        let status = crate::status::read(&layout).unwrap();
        assert_eq!(status.programs["ta"].state, crate::state::managed(3, 51));
        assert_eq!(status.programs["tb"].state, crate::state::managed(6, 51));
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// The hold needs proof bound to the pin. A genuinely signed manifest served where tb@6's
    /// belongs, but naming another build or another program, says nothing about tb@6: the pass
    /// takes the loud path — the stage refuses the mismatch, one failure, the ordinary
    /// `aborted: stage` row — never the quiet hold.
    #[test]
    fn a_manifest_that_does_not_bind_to_the_pin_never_proves_it_unpublished() {
        for (label, prog, build) in [("build", "tb", 7u64), ("program", "ta", 6u64)] {
            let dir = scratch(&format!("update-unbound-{label}"));
            write_group_index(&dir, "ta = 4, tb = 6", &[]);
            write_pkg(&dir, "ta", 4);
            write_pkg_for_triple(&dir, prog, build, "riscv64gc-unknown-linux-gnu");
            for ext in ["toml", "toml.sig"] {
                std::fs::rename(
                    dir.join(format!("pkg-{prog}-{build}.{ext}")),
                    dir.join(format!("pkg-tb-6.{ext}")),
                )
                .unwrap();
            }
            let layout = temp_layout(&format!("update-unbound-{label}"));
            let fetcher = crate::DirFetcher::new(dir.clone());
            make_installed_group_live(&layout);

            let (report, failures) = update_pass(&layout, &fetcher);
            assert_eq!(
                failures, 1,
                "{label}: an unbound manifest proves nothing, so the stage fails loudly: {:?}",
                report.groups
            );
            assert!(
                matches!(
                    outcome_of(&report, "tb"),
                    crate::TxnOutcome::Aborted { failed, during_flip: false, .. } if failed == "tb"
                ),
                "{label}: the old loud path, never a quiet hold: {:?}",
                outcome_of(&report, "tb")
            );
            let active = crate::active_builds(&layout);
            assert_eq!(
                (active.get("ta").copied(), active.get("tb").copied()),
                (Some(3), Some(5)),
                "{label}: nothing moved"
            );
            let status = crate::status::read(&layout).unwrap();
            assert!(
                status.programs["tb"].state.starts_with("aborted: stage"),
                "{label}: {}",
                status.programs["tb"].state
            );
            let _ = std::fs::remove_dir_all(&layout.prefix);
            let _ = std::fs::remove_dir_all(&dir);
        }
    }

    /// `dependents_of` is the reverse relation, in index order — what the uninstall
    /// warning names.
    #[test]
    fn dependents_of_is_the_reverse_of_requires() {
        let mut index = index_of(&[
            ("ay", false, None),
            ("brew", false, None),
            ("clt", false, None),
        ]);
        index.programs.get_mut("brew").unwrap().requires = vec!["clt".into()];
        index.programs.get_mut("ay").unwrap().requires = vec!["clt".into(), "brew".into()];
        assert_eq!(
            dependents_of(&index, "clt"),
            vec!["ay".to_string(), "brew".to_string()]
        );
        assert_eq!(dependents_of(&index, "brew"), vec!["ay".to_string()]);
        assert!(dependents_of(&index, "ay").is_empty());
        assert!(dependents_of(&index, "ghost").is_empty());
        assert_eq!(
            requires_of(&index).into_iter().collect::<Vec<_>>(),
            vec![
                (
                    "ay".to_string(),
                    vec!["clt".to_string(), "brew".to_string()]
                ),
                ("brew".to_string(), vec!["clt".to_string()]),
            ]
        );
    }

    /// EXTRAS AT THE INDEX RESOLVE (owner decision 2026-08-26): a program the signed
    /// index lists with `extra = true` is NOT planned, announced or installed by the
    /// default-set pass — but it gets a CONSENT stub on PATH (kind `Extra`), so typing
    /// its name asks instead of "command not found". Once opted in, the next pass
    /// installs it and the real shim replaces the stub.
    #[cfg(unix)]
    #[test]
    fn an_index_listed_extra_gets_a_consent_stub_and_no_install_until_opted_in() {
        let dir = scratch("extra-stub");
        write_registry_with_extra(&dir, "stable", current_triple());
        let layout = temp_layout("extra-stub");
        let cfg = crate::config::PackagesConfig::default();
        let fetcher = crate::DirFetcher::new(dir.clone());
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0);
        let active = crate::active_builds(&layout);
        assert_eq!(active.get("ay").copied(), Some(18), "the member installs");
        assert!(
            !active.contains_key("xc"),
            "an extra is never auto-installed"
        );
        assert_eq!(
            crate::stub::pending_stub_kind(&layout, "xc"),
            Some(crate::stub::StubKind::Extra),
            "the consent stub is on PATH, marked as an extra"
        );
        assert!(!layout.optin_exists("xc"));
        // The extra's ROW is the canonical opt-in state (design §2) — the same words
        // `which` and the Packages row say — and it is no doctor fault.
        let status = crate::status::read(&layout).expect("status recorded");
        assert_eq!(
            status.programs["xc"].state,
            crate::state::extra_not_installed("xc")
        );
        assert_eq!(status.programs["xc"].installed_build, None);
        assert!(
            crate::state::managed_pin(&status.programs["ay"].state).is_some_and(|(b, _)| b == 18),
            "the member's row is the managed state: {}",
            status.programs["ay"].state
        );
        assert!(crate::doctor::recorded_problems(Some(&status)).is_empty());
        // A second pass changes nothing: the stub stays, the extra stays out, and the
        // row is not rewritten when it already says so.
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0);
        assert!(!crate::active_builds(&layout).contains_key("xc"));
        assert_eq!(
            crate::stub::pending_stub_kind(&layout, "xc"),
            Some(crate::stub::StubKind::Extra)
        );
        assert!(!record_state_if_changed(
            &layout,
            "xc",
            crate::state::extra_not_installed("xc")
        ));
        // Opted in (what the consent stub records on `y`): the next pass installs
        // it, and the stub gives way to the real shim — atomically, over the stub.
        layout.record_optin("xc").unwrap();
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0);
        assert_eq!(crate::active_builds(&layout).get("xc").copied(), Some(9));
        assert!(!crate::stub::pending_stub_exists(&layout, "xc"));
        assert!(
            crate::which(&layout, "xc").is_some(),
            "the real shim resolves"
        );
        // …and the row flips to the managed state, in the same words.
        let row = crate::status::read(&layout).unwrap().programs["xc"].clone();
        assert!(
            crate::state::managed_pin(&row.state).is_some_and(|(b, _)| b == 9),
            "{}",
            row.state
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// An extra the registry publishes NO artifact for on this triple gets no stub
    /// either — the consent question would be one the pass could never honour — and
    /// the members still install; nothing counts as a failure.
    #[cfg(unix)]
    #[test]
    fn an_extra_with_no_build_for_this_triple_gets_no_stub() {
        let dir = scratch("extra-unserved");
        write_registry_with_extra(&dir, "stable", "riscv64gc-unknown-linux-gnu");
        let layout = temp_layout("extra-unserved");
        let cfg = crate::config::PackagesConfig::default();
        let fetcher = crate::DirFetcher::new(dir.clone());
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0);
        assert!(crate::active_builds(&layout).contains_key("ay"));
        assert!(
            !crate::stub::pending_stub_exists(&layout, "xc"),
            "no stub for an extra whose bytes cannot move here"
        );
        // Its row is the canonical `unavailable on <target>` state — a state, never a
        // fault — and the machine is not blocked as a whole (ay was served).
        let status = crate::status::read(&layout).expect("status recorded");
        assert_eq!(
            status.programs["xc"].state,
            crate::state::unavailable(current_triple(), "")
        );
        assert!(!status.programs.contains_key("*toolset*"));
        assert!(crate::doctor::recorded_problems(Some(&status)).is_empty());
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    // Channel THREADING: the bootstrap resolves the CONFIG channel, not a hardcoded
    // "stable" — a registry publishing only `nightly` installs iff the config says so.
    #[test]
    fn default_set_bootstrap_uses_the_config_channel() {
        let dir = scratch("channel");
        write_registry(&dir, "nightly");
        let fetcher = crate::DirFetcher::new(dir.clone());
        // Default config (channel "stable") against a nightly-only index: the missing
        // channel is one loud pass-level failure, nothing installs.
        let layout = temp_layout("channel-default");
        let cfg = crate::config::PackagesConfig::default();
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert!(failures > 0, "a missing channel is loud, not silent");
        assert!(crate::active_builds(&layout).is_empty());
        let _ = std::fs::remove_dir_all(&layout.prefix);
        // channel = "nightly" threads through and installs.
        let layout = temp_layout("channel-nightly");
        let cfg = crate::config::parse_packages("[packages]\nchannel = \"nightly\"\n");
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert_eq!(failures, 0);
        assert_eq!(
            crate::active_builds(&layout).get("ay").copied(),
            Some(18),
            "the config channel reached the install request"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    // AN UNRESOLVED INDEX IS NOT A CHECK. An adopted machine with an EMPTY store runs
    // `update` through the set-completion lane alone; offline (or on a channel the index
    // does not publish) that lane fails before it checks anything, and the failed-pass
    // exit used to stamp `last_success_at` regardless — silencing "never checked" and
    // giving `doctor` a 0-day age on a machine that had never reached the index. The
    // outcome now says whether the index resolved, the lane records the apply lane's
    // `*index*` row, and a resolved pass retires it.
    #[test]
    fn an_empty_store_pass_that_never_resolved_the_index_is_not_stamped() {
        let cfg = crate::config::PackagesConfig::default();
        // Offline: nothing at the index URL.
        let empty = scratch("unresolved-offline");
        let layout = temp_layout("unresolved-offline");
        let out = install_default_set(
            &layout,
            &crate::DirFetcher::new(empty.clone()),
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 1);
        assert!(
            out.index_error.is_some(),
            "an unreachable index is reported"
        );
        assert!(
            !settle_net_lane_index(&layout, false, out.index_error.as_deref()),
            "an empty store's pass resolved nothing"
        );
        let row = crate::status::read(&layout).expect("the *index* row is written");
        assert!(
            row.programs
                .get("*index*")
                .is_some_and(|p| p.state.starts_with("error: ")),
            "the lane records the apply lane's own *index* failure row"
        );
        assert!(
            warn_if_never_checked(&layout),
            "an unresolved pass is not a completed check"
        );
        // …but a miss after the apply lane resolved this pass changes nothing.
        let quiet = temp_layout("unresolved-after-apply");
        assert!(settle_net_lane_index(&quiet, true, Some("offline")));
        assert!(crate::status::read(&quiet).is_none(), "no row re-opened");
        let _ = std::fs::remove_dir_all(&quiet.prefix);
        // A channel the index does not publish: resolved, but nothing to act on — the
        // apply lane's `Err(NoChannel)`, so the same verdict.
        let dir = scratch("unresolved-channel");
        write_registry(&dir, "nightly");
        let fetcher = crate::DirFetcher::new(dir.clone());
        let nightly_only = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert!(
            nightly_only.index_error.is_some(),
            "a missing channel is no check"
        );
        // The index resolves on the configured channel: the pass checked, and the
        // failure row it disproved is retired.
        let nightly = crate::config::parse_packages("[packages]\nchannel = \"nightly\"\n");
        let ok = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &nightly,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(ok.index_error, None);
        assert!(settle_net_lane_index(
            &layout,
            false,
            ok.index_error.as_deref()
        ));
        assert!(
            !crate::status::read(&layout)
                .unwrap()
                .programs
                .contains_key("*index*"),
            "a resolved pass retires the *index* row"
        );
        // And the verb's failed-pass exit stamps ONLY a resolved pass — a source-shape
        // pin in the house style, because `update` reads the GLOBAL layout.
        let src = include_str!("cli.rs");
        let start = src
            .find("fn cmd_update_all_code() -> u8 {")
            .expect("the update verb");
        let end = src[start..]
            .find("\nfn ")
            .map(|i| start + i)
            .expect("a function after the verb");
        let body = &src[start..end];
        let tail = &body[body.rfind("} else {").expect("the failed-pass exit")..];
        let guard = tail.find("if index_resolved {").expect("the stamp guard");
        assert!(
            guard < tail.find("record_success(&layout)").expect("the stamp"),
            "the failed-pass exit must stamp only a pass that resolved the index"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&empty);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// Write a root-signed index whose default set is the `rustc`-locked coherence
    /// tuple `ta`+`tb` plus the ungrouped singleton `ay` — the §7 bootstrap fixture.
    /// `write_tb_pkg = false` leaves tb's pkg manifest unpublished, so its stage
    /// fails and the group transaction must abort WHOLE (never a split tuple).
    fn write_group_registry(dir: &Path, channel: &str, write_tb_pkg: bool) {
        let index_body = format!(
            "schema = 2\nindex_build = 51\nvalid_until = \"2026-07-05T12:00:00Z\"\n\
             machine_id = \"{id}\"\nroster_seq = {seq}\n\
             [programs.ay]\nrepo = \"ay\"\n\
             [programs.ta]\nrepo = \"ta\"\ncoherence_group = \"rustc\"\n\
             [programs.tb]\nrepo = \"tb\"\ncoherence_group = \"rustc\"\n\
             [[channels]]\nname = \"{channel}\"\nchannel_build = 1\nmin_build = 0\n\
             pin = {{ ay = 18, ta = 4, tb = 6 }}\n",
            id = testkit::MACHINE_ID,
            seq = testkit::SEQ
        );
        std::fs::write(dir.join("index.toml"), index_body.as_bytes()).unwrap();
        // The index is MACHINE-signed now; only the roster is signed by the master.
        std::fs::write(
            dir.join("index.toml.sig"),
            sign(&RELEASE_SEED, index_body.as_bytes()),
        )
        .unwrap();
        write_roster(dir);
        write_pkg(dir, "ay", 18);
        write_pkg(dir, "ta", 4);
        if write_tb_pkg {
            write_pkg(dir, "tb", 6);
        }
    }

    // §7 bootstrap happy path: a missing coherence tuple fresh-installs WHOLE through
    // the group transaction (one resolved index for the whole pass), alongside the
    // ungrouped singleton; the durable floor advances to the one trusted index.
    #[test]
    fn default_set_bootstrap_installs_a_coherence_group_atomically() {
        let dir = scratch("group-ok");
        write_group_registry(&dir, "stable", true);
        let layout = temp_layout("group-ok");
        let cfg = crate::config::PackagesConfig::default();
        let fetcher = crate::DirFetcher::new(dir.clone());
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert_eq!(failures, 0);
        let active = crate::active_builds(&layout);
        assert_eq!(active.get("ta").copied(), Some(4), "tuple member installed");
        assert_eq!(active.get("tb").copied(), Some(6), "tuple member installed");
        assert_eq!(active.get("ay").copied(), Some(18), "singleton installed");
        assert_eq!(crate::sig::Floor::new(layout.floor()).current(), 51);
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    // §7 all-or-nothing: a tuple member whose pkg manifest is unpublished aborts the
    // WHOLE group — the sibling that staged fine is neither activated nor left on
    // disk (a complete-but-inactive build would mis-read as active next run), while
    // the ungrouped singleton still installs. Exactly ONE loud failure for the group.
    #[test]
    fn default_set_bootstrap_never_splits_a_coherence_group() {
        let dir = scratch("group-abort");
        write_group_registry(&dir, "stable", false);
        let layout = temp_layout("group-abort");
        let cfg = crate::config::PackagesConfig::default();
        let fetcher = crate::DirFetcher::new(dir.clone());
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert_eq!(failures, 1, "one failure for the aborted group");
        let active = crate::active_builds(&layout);
        assert!(
            !active.contains_key("ta"),
            "no partial tuple: ta not active"
        );
        assert!(
            !active.contains_key("tb"),
            "no partial tuple: tb not active"
        );
        assert!(
            !layout.build_dir("ta", 4).exists(),
            "the staged sibling was discarded on abort"
        );
        assert_eq!(
            active.get("ay").copied(),
            Some(18),
            "the singleton still installs"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    /// A [`crate::DirFetcher`] whose first request for `program`'s manifest fails, as a dropped
    /// connection does; every later request is served. Nothing remembers a failed fetch, so a
    /// prescan that met the failure proved nothing and a later walk, asking again, succeeds.
    struct FirstManifestFetchFails {
        inner: crate::DirFetcher,
        program: &'static str,
        tripped: std::cell::Cell<bool>,
    }
    impl crate::flow::Fetcher for FirstManifestFetchFails {
        fn index_candidates(&self) -> Result<Vec<crate::Candidate>, String> {
            crate::flow::Fetcher::index_candidates(&self.inner)
        }
        fn pkg_manifest(
            &self,
            repo: &str,
            program: &str,
            build: u64,
        ) -> Result<(Vec<u8>, Vec<u8>), String> {
            if program == self.program && !self.tripped.replace(true) {
                return Err(format!("{program}@{build}: connection reset"));
            }
            crate::flow::Fetcher::pkg_manifest(&self.inner, repo, program, build)
        }
        fn download(&self, repo: &str, asset: &str, dest: &Path) -> Result<(), String> {
            crate::flow::Fetcher::download(&self.inner, repo, asset, dest)
        }
        fn source_id(&self) -> String {
            crate::flow::Fetcher::source_id(&self.inner)
        }
    }

    // The bootstrap arm of the missing-triple hold. A partly installed tuple: ta@3 is live,
    // tb is missing, the channel pins ta = 4 (published only for a foreign triple) and tb = 6.
    // The prescan's request for ta@4's manifest fails and proves nothing, so the walk fetches
    // it and finds no artifact for this triple. A tuple that cannot fully exist here is a clean
    // skip, never a failure: nothing staged, nothing resolved.
    #[test]
    fn default_set_bootstrap_skips_a_partial_tuple_whose_installed_pin_is_unpublished() {
        let dir = scratch("group-partial-unpublished");
        write_group_index(&dir, "ta = 4, tb = 6", &[]);
        write_pkg_for_triple(&dir, "ta", 4, "riscv64gc-unknown-linux-gnu");
        write_pkg(&dir, "tb", 6);
        let layout = temp_layout("group-partial-unpublished");
        make_live(&layout, "ta", 3);
        let fetcher = FirstManifestFetchFails {
            inner: crate::DirFetcher::new(dir.clone()),
            program: "ta",
            tripped: std::cell::Cell::new(false),
        };
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &crate::config::PackagesConfig::default(),
            ProvisionLane::Network,
            0,
        );
        assert!(
            fetcher.tripped.get(),
            "a prescan's request met the dropped fetch"
        );
        assert_eq!(
            out.failures, 0,
            "an unpublished pin is a clean skip, never a failure"
        );
        let active = crate::active_builds(&layout);
        assert_eq!(active.get("ta").copied(), Some(3), "ta keeps its build");
        assert!(!active.contains_key("tb"), "tb is not installed");
        for (p, b) in [("ta", 4u64), ("tb", 6u64)] {
            assert!(!layout.build_dir(p, b).exists(), "{p}@{b} was staged");
            assert!(!layout.staging_dir(p).exists(), "{p} touched staging/");
        }
        assert!(
            out.resolved_assets.is_empty(),
            "nothing was resolved for download: {:?}",
            out.resolved_assets
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    // §7 narrowing: an `exclude` that leaves part of a coherence tuple out refuses
    // the WHOLE group (loud diagnostic, not a failure) — a deliberately partial
    // tuple is exactly the split the transaction exists to prevent.
    #[test]
    fn default_set_bootstrap_refuses_a_narrowed_partial_tuple() {
        let dir = scratch("group-narrowed");
        write_group_registry(&dir, "stable", true);
        let layout = temp_layout("group-narrowed");
        let cfg = crate::config::PackagesConfig {
            exclude: Some(vec!["tb".into()]),
            ..Default::default()
        };
        let fetcher = crate::DirFetcher::new(dir.clone());
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert_eq!(
            failures, 0,
            "a config-narrowed tuple is a diagnostic, not a failure"
        );
        let active = crate::active_builds(&layout);
        assert!(
            !active.contains_key("ta"),
            "no partial tuple over a narrowing"
        );
        assert!(!active.contains_key("tb"));
        assert_eq!(
            active.get("ay").copied(),
            Some(18),
            "the singleton still installs"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    // The narrowed-tuple refusal is decided at PLANNING, not at the group arm:
    // the refused members ride out on the outcome (`skipped_narrowed`), are NOT
    // counted as still-pending (`remaining_missing`), and no pending stub
    // survives for them — a stub is a name on PATH promising exactly the
    // install the pass just refused. Observed live 2026-08-27: the arm-time
    // decision announced trust-cg/ir/vc, refused them one line later, left
    // their stubs behind, and the summary then claimed the set complete.
    #[test]
    fn a_narrowed_tuple_is_refused_at_planning_and_reported_on_the_outcome() {
        let dir = scratch("group-narrowed-plan");
        write_group_registry(&dir, "stable", true);
        let layout = temp_layout("group-narrowed-plan");
        let cfg = crate::config::PackagesConfig {
            exclude: Some(vec!["tb".into()]),
            ..Default::default()
        };
        let fetcher = crate::DirFetcher::new(dir.clone());
        let out = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        );
        assert_eq!(out.failures, 0, "a narrowed tuple is never a failure");
        assert_eq!(
            out.skipped_narrowed,
            vec!["ta".to_string()],
            "the refused member is reported, not silently dropped"
        );
        assert!(
            out.remaining_missing.is_empty(),
            "a refused member is not 'still coming': {:?}",
            out.remaining_missing
        );
        assert!(
            !crate::stub::pending_stub_exists(&layout, "ta"),
            "no stub may promise the install this pass refused"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    // The explicit toolset verb clears removal/decline markers BEFORE its pass:
    // `install_default_set` narrows `wanted` by those very markers, so the old
    // after-ordering honoured the old mind for exactly the pass that was asked
    // to change it — a legacy `trust` removal kept trust-cg/ir/vc out of the
    // explicit toolset install, which then reported the set complete
    // (2026-08-27, live). A source-shape test in the house style, because the
    // verb reads the GLOBAL layout and so cannot be driven by a scratch one.
    #[test]
    fn the_toolset_verb_changes_its_mind_before_its_pass() {
        let src = include_str!("cli.rs");
        let start = src
            // The BODY, not the one-line wrapper that answers the announcement:
            // `find` takes the first match, and the wrapper has neither the clears
            // nor the pass call in it.
            .find("fn cmd_install_default_set_code")
            .expect("the toolset verb");
        let end = src[start..]
            .find("\nfn ")
            .map(|i| start + i)
            .expect("a function after the verb");
        let body = &src[start..end];
        let clear = body
            .find("clear_removed")
            .expect("the change-of-mind clear");
        let pass = body.find("= install_default_set(").expect("the pass call");
        assert!(
            clear < pass,
            "removal markers must be cleared BEFORE the pass that narrows by them"
        );
        assert!(
            body.find("clear_decline").expect("the decline clear") < pass,
            "the decline must be cleared BEFORE the pass that honours it"
        );
    }

    // The `*seed*` pending-consent row reconciles against what a clean pass
    // PROVED: rewritten when the remaining offer moved, retired when nothing
    // remains, untouched when no row exists. Its only other writer lives on the
    // seed lane, which hard-skips once the store is non-empty — a 2026-08-12
    // row was still advertising the legacy toolset layout on 2026-08-27.
    #[test]
    fn a_stale_seed_offer_is_reconciled_by_a_clean_pass() {
        let layout = temp_layout("seed-row-reconcile");
        // No row: a reconcile writes nothing (no resurrected offers).
        reconcile_seed_status(&layout, &["na".to_string()]);
        assert!(
            crate::status::read(&layout).is_none_or(|s| !s.programs.contains_key("*seed*")),
            "reconcile must never CREATE an offer row"
        );
        record_status(
            &layout,
            "*seed*",
            crate::ProgramStatus {
                installed_build: None,
                state: "pending-consent: old-a, old-b".into(),
                tree_root: String::new(),
            },
            "ALab toolchain ready: old-a, old-b — Install ALab toolset below".into(),
        );
        reconcile_seed_status(&layout, &["na".to_string(), "nb".to_string()]);
        let status = crate::status::read(&layout).expect("a status record");
        assert_eq!(
            status.programs["*seed*"].state, "pending-consent: na, nb",
            "the recorded offer follows the pass's proof"
        );
        assert!(
            status.outcome.contains("na, nb"),
            "the outcome sentence names the CURRENT offer: {}",
            status.outcome
        );
        // The update lane records its own verdict BEFORE the reconcile; retiring
        // the row must not swap that verdict for a sentence about a seed that never
        // ran (`clear_seed_status` does exactly that — 2026-09-10 audit).
        record_outcome(&layout, "up to date (index build 21)".into());
        reconcile_seed_status(&layout, &[]);
        let status = crate::status::read(&layout).expect("a status record");
        assert!(
            !status.programs.contains_key("*seed*"),
            "nothing left to offer retires the row"
        );
        assert_eq!(
            status.outcome, "up to date (index build 21)",
            "retiring the row is ROW-ONLY: the pass's own outcome survives"
        );
        // And a refresh over a non-offer outcome leaves that outcome alone too.
        record_status(
            &layout,
            "*seed*",
            crate::ProgramStatus {
                installed_build: None,
                state: "pending-consent: old".into(),
                tree_root: String::new(),
            },
            "up to date (index build 21)".into(),
        );
        reconcile_seed_status(&layout, &["nc".to_string()]);
        let status = crate::status::read(&layout).expect("a status record");
        assert_eq!(status.programs["*seed*"].state, "pending-consent: nc");
        assert_eq!(status.outcome, "up to date (index build 21)");
        let _ = std::fs::remove_dir_all(&layout.prefix);
    }

    // A `[packages.links]` owner/repo override for a program the signed index does NOT
    // name can never install anything (reachability §5 is structural); the bootstrap
    // only says so loudly. Fail-closed: no unsigned installs from slugs.
    #[test]
    fn unindexed_repo_override_never_installs() {
        let dir = scratch("unindexed");
        write_registry(&dir, "stable");
        let layout = temp_layout("unindexed");
        let cfg =
            crate::config::parse_packages("[packages.links]\nghost = \"alabsystems/ghost\"\n");
        let fetcher = crate::DirFetcher::new(dir.clone());
        let failures = install_default_set(
            &layout,
            &fetcher,
            &test_anchor(),
            &cfg,
            ProvisionLane::Network,
            0,
        )
        .failures;
        assert_eq!(
            failures, 0,
            "the refusal is a loud diagnostic, not a loop failure"
        );
        assert!(
            !crate::active_builds(&layout).contains_key("ghost"),
            "an unindexed program is unreachable by construction"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&dir);
    }

    // [packages.links] reconciliation: a config checkout path becomes a managed
    // dev-link (idempotent across passes); a missing checkout links nothing; an
    // invalid value acts on nothing.
    #[test]
    fn reconcile_links_creates_and_reasserts_config_links() {
        let layout = temp_layout("reconcile");
        let co = checkout("reconcile", "ay");
        let cfg = crate::config::parse_packages(&format!(
            "[packages.links]\nay = {:?}\nmissing = \"/nonexistent/checkout\"\nbad = \"a b\"\n",
            co.display().to_string()
        ));
        reconcile_links(&layout, &cfg);
        assert!(
            crate::is_linked(&layout, "ay"),
            "config path became a dev-link"
        );
        assert_eq!(
            crate::linked_checkout(&layout, "ay").unwrap(),
            std::path::absolute(&co).unwrap()
        );
        assert!(
            !crate::is_linked(&layout, "missing"),
            "missing checkout links nothing"
        );
        assert!(
            !crate::is_linked(&layout, "bad"),
            "invalid value acts on nothing"
        );
        // Idempotent second pass: still linked, same checkout, and a newly-built bin
        // is picked up by the quiet refresh.
        std::fs::write(co.join("target/release").join("ay2"), b"#!/bin/true\n").unwrap();
        let cfg2 = crate::config::parse_packages(&format!(
            "[packages.links]\nay = {:?}\n",
            co.display().to_string()
        ));
        reconcile_links(&layout, &cfg2);
        assert!(crate::is_linked(&layout, "ay"));
        assert_eq!(
            crate::linked_checkout(&layout, "ay").unwrap(),
            std::path::absolute(&co).unwrap()
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&co);
    }

    // The FOREIGN-link refusal: a hand-made dev-link pointing at a DIFFERENT checkout
    // is never re-pointed by config — the link (and its shim target) survive untouched.
    #[test]
    fn reconcile_links_refuses_to_touch_a_foreign_link() {
        let layout = temp_layout("foreign");
        let hand = checkout("foreign-hand", "ay");
        let config_wants = checkout("foreign-cfg", "ay");
        crate::link(&layout, "ay", &hand, &[PathBuf::from("target/release/ay")]).unwrap();
        let cfg = crate::config::parse_packages(&format!(
            "[packages.links]\nay = {:?}\n",
            config_wants.display().to_string()
        ));
        reconcile_links(&layout, &cfg);
        assert_eq!(
            crate::linked_checkout(&layout, "ay").unwrap(),
            std::path::absolute(&hand).unwrap(),
            "the hand-made link's checkout is untouched"
        );
        assert_eq!(
            crate::platform::resolve_shim(
                &layout.shim(&crate::store::ToolName::new("ay").unwrap())
            )
            .unwrap(),
            std::path::absolute(&hand)
                .unwrap()
                .join("target/release/ay"),
            "the shim still points into the hand-made checkout"
        );
        let _ = std::fs::remove_dir_all(&layout.prefix);
        let _ = std::fs::remove_dir_all(&hand);
        let _ = std::fs::remove_dir_all(&config_wants);
    }

    // -----------------------------------------------------------------------
    // `__pending` — the five honest states, the honesty edge, escape stripping
    // (R6; design §5).
    // -----------------------------------------------------------------------

    fn now_secs() -> u64 {
        std::time::SystemTime::now()
            .duration_since(std::time::SystemTime::UNIX_EPOCH)
            .unwrap()
            .as_secs()
    }

    /// A live-looking progress snapshot: this test process's pid, a fresh heartbeat.
    fn live_snapshot(programs: &str, queue: &str) -> String {
        format!(
            "{{\"v\":1,\"pid\":{},\"pass\":\"net\",\"heartbeat_unix\":{},\
             \"overall\":{{\"programs_done\":2,\"programs_total\":9}},\
             \"queue\":{queue},\"programs\":{programs}}}",
            std::process::id(),
            now_secs()
        )
    }

    fn bump_contents(l: &crate::store::Layout) -> String {
        std::fs::read_to_string(l.bump_file()).unwrap_or_default()
    }

    /// [`pending_state`] collected NON-interactively (no TTY, no aterm): the lines
    /// only — the spelling of the four default-set states and the honesty edge.
    fn pending_state_lines(layout: &crate::store::Layout, tool: &str) -> Vec<String> {
        let mut out: Vec<String> = Vec::new();
        let mut say = |line: String| out.push(line);
        let mut io = PendingIo {
            say: &mut say,
            ask: None,
            gui_present: false,
        };
        pending_state(layout, tool, &mut io);
        out
    }

    /// State: NOT STARTED — no progress file, no status row. The honest line names
    /// both next acts, and the bump is still written so the next pass front-loads
    /// the program the user actually wanted.
    #[test]
    fn pending_not_started_names_the_next_act() {
        let l = temp_layout("pend-notstarted");
        let lines = pending_state_lines(&l, "trust");
        assert!(
            lines[0].contains("trust:"),
            "leads with what the tool IS: {lines:?}"
        );
        assert!(
            lines
                .iter()
                .any(|x| x.contains("nothing is installing right now")
                    && x.contains("open aterm")
                    && x.contains("aterm pkg update")),
            "the not-started state names its fixes: {lines:?}"
        );
        assert_eq!(
            bump_contents(&l),
            "trust\n",
            "the wish is recorded for the next pass"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// State: INSTALLING NOW — a live snapshot's download row renders live MB and
    /// the overall count.
    #[test]
    fn pending_installing_now_shows_live_bytes() {
        let l = temp_layout("pend-installing");
        std::fs::write(
            l.progress_file(),
            live_snapshot(
                "{\"trust\":{\"phase\":\"download\",\"bytes_done\":4100000,\"bytes_total\":9800000}}",
                "[\"trust\"]",
            ),
        )
        .unwrap();
        let lines = pending_state_lines(&l, "trust");
        assert!(
            lines.iter().any(|x| x.contains("2 of 9 done")),
            "overall progress renders: {lines:?}"
        );
        assert!(
            lines
                .iter()
                .any(|x| x.contains("downloading NOW") && x.contains("4.1 of 9.8 MB")),
            "live MB from the snapshot: {lines:?}"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// State: QUEUED — the row is queued behind a current program: position + the
    /// BUMP claim, and the bump file really carries the name (a claimed bump must
    /// have been written).
    #[test]
    fn pending_queued_bumps_and_says_so() {
        let l = temp_layout("pend-queued");
        std::fs::write(
            l.progress_file(),
            live_snapshot(
                "{\"robi\":{\"phase\":\"download\"},\"trust\":{\"phase\":\"queued\"}}",
                "[\"robi\",\"ty\",\"trust\"]",
            ),
        )
        .unwrap();
        let lines = pending_state_lines(&l, "trust");
        assert!(
            lines.iter().any(|x| x.contains("queued 3 of 3")
                && x.contains("BUMPED")
                && x.contains("after robi finishes")),
            "position + bump + current program: {lines:?}"
        );
        assert_eq!(bump_contents(&l), "trust\n");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// State: SKIPPED — a TERMINAL phase, never a wait. `Sink::finished` has already
    /// retired the program from the queue and counted it done, so nothing later in this
    /// pass installs it, and the reorder-only bump file cannot put it back. The stub says
    /// the RECORDED reason (control-stripped, like every other untrusted echo) and claims
    /// NO bump — it used to answer a member blocked by an unmet requirement with "queued
    /// and now BUMPED to install next", a promise the running pass never kept.
    #[test]
    fn pending_skipped_says_the_recorded_reason_and_claims_no_bump() {
        let l = temp_layout("pend-skipped");
        std::fs::write(
            l.progress_file(),
            live_snapshot(
                "{\"robi\":{\"phase\":\"download\"},\
                 \"trust\":{\"phase\":\"skipped\",\
                 \"error\":\"blocked by xc: \\u001b[2Jextra — not installed\"}}",
                "[\"robi\"]",
            ),
        )
        .unwrap();
        let lines = pending_state_lines(&l, "trust");
        let said = lines
            .iter()
            .find(|x| x.contains("SKIPPED"))
            .expect("the skip renders");
        assert!(
            said.contains("blocked by xc") && said.contains("[2Jextra"),
            "the recorded reason surfaces, control-stripped: {said}"
        );
        assert!(!said.contains('\u{1b}'), "no ESC byte reaches the TTY");
        assert!(
            !lines.iter().any(|x| x.contains("BUMPED")),
            "a terminal skip is off the queue: no bump can restart it: {lines:?}"
        );
        assert_eq!(bump_contents(&l), "", "and no bump was written");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// The same skip with NO recorded reason — a dev link, a narrowed tuple, an
    /// already-current group: the honest generic line, its next act named, and still
    /// neither a bump nor a claim of one.
    #[test]
    fn pending_skipped_without_a_reason_promises_no_bump_either() {
        let l = temp_layout("pend-skipped-bare");
        std::fs::write(
            l.progress_file(),
            live_snapshot(
                "{\"robi\":{\"phase\":\"download\"},\"trust\":{\"phase\":\"skipped\"}}",
                "[\"robi\"]",
            ),
        )
        .unwrap();
        let lines = pending_state_lines(&l, "trust");
        assert!(
            lines.iter().any(
                |x| x.contains("SKIPPED by this pass") && x.contains("aterm pkg install trust")
            ),
            "the generic skip names its next act: {lines:?}"
        );
        assert!(
            !lines.iter().any(|x| x.contains("BUMPED")),
            "nothing in this pass installs it: {lines:?}"
        );
        assert_eq!(bump_contents(&l), "");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// State: FAILED — the recorded error surfaces WITH its next act, and control
    /// characters in it are stripped before the TTY (escape-sequence injection).
    #[test]
    fn pending_failed_names_the_error_and_strips_escapes() {
        let l = temp_layout("pend-failed");
        record_status(
            &l,
            "trust",
            crate::ProgramStatus {
                installed_build: None,
                state: "error: mirror said \u{1b}[2Jno".into(),
                tree_root: String::new(),
            },
            "bootstrap install trust: failed".into(),
        );
        let lines = pending_state_lines(&l, "trust");
        let failed = lines
            .iter()
            .find(|x| x.contains("FAILED"))
            .expect("the failure renders");
        assert!(
            failed.contains("mirror said [2Jno"),
            "escapes stripped: {failed}"
        );
        assert!(!failed.contains('\u{1b}'), "no ESC byte reaches the TTY");
        assert!(
            failed.contains("fix: aterm pkg update"),
            "every failure names its next act"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// A STALE snapshot renders only not-running states: yesterday's "downloading"
    /// must never be today's claim, however live the file looks otherwise.
    #[test]
    fn pending_stale_heartbeat_renders_not_running() {
        let l = temp_layout("pend-stale");
        let stale = format!(
            "{{\"v\":1,\"pid\":{},\"heartbeat_unix\":{},\
             \"programs\":{{\"trust\":{{\"phase\":\"download\",\"bytes_done\":1}}}}}}",
            std::process::id(),
            now_secs() - crate::progress::HEARTBEAT_STALE_SECS - 5
        );
        std::fs::write(l.progress_file(), stale).unwrap();
        let lines = pending_state_lines(&l, "trust");
        assert!(
            lines
                .iter()
                .any(|x| x.contains("nothing is installing right now")),
            "stale = not running: {lines:?}"
        );
        assert!(
            !lines.iter().any(|x| x.contains("downloading")),
            "a dead installer's file never claims live progress: {lines:?}"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// THE HONESTY EDGE: a live resolved-pass snapshot that does NOT plan the
    /// program says "no longer part of the default set" — and claims NO bump,
    /// because the reorder-only intersection would silently drop it.
    #[test]
    fn pending_delisted_name_is_told_the_truth_and_not_bumped() {
        let l = temp_layout("pend-delisted");
        std::fs::write(
            l.progress_file(),
            live_snapshot("{\"ty\":{\"phase\":\"download\"}}", "[\"ty\"]"),
        )
        .unwrap();
        let lines = pending_state_lines(&l, "trust");
        assert!(
            lines
                .iter()
                .any(|x| x.contains("no longer part of the default set")),
            "{lines:?}"
        );
        assert!(
            !lines.iter().any(|x| x.contains("BUMPED")),
            "no bump claim the installer would drop: {lines:?}"
        );
        assert_eq!(bump_contents(&l), "", "and no bump was written");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// A name the shim gate refuses is never echoed back — the one universally safe
    /// answer for it.
    #[test]
    fn pending_refuses_inadmissible_names() {
        let l = temp_layout("pend-refuse");
        let lines = pending_state_lines(&l, "../sudo");
        assert_eq!(
            lines,
            vec!["atpkg: that is not an installable program name".to_string()]
        );
        assert_eq!(bump_contents(&l), "");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    // -----------------------------------------------------------------------
    // `__pending` — the EXTRAS' consent state (owner decision 2026-08-26).
    // -----------------------------------------------------------------------

    fn lay_extra_stub(l: &crate::store::Layout, name: &str) {
        crate::stub::write_pending_stub_kind(
            l,
            &crate::store::ToolName::new(name).unwrap(),
            crate::stub::StubKind::Extra,
        )
        .unwrap();
    }

    /// Drive [`pending_state`] with a terminal that answers `answer` to every
    /// question, recording the prompts verbatim: `(lines, prompts, next)`.
    fn pending_with_answer(
        l: &crate::store::Layout,
        tool: &str,
        answer: bool,
        gui_present: bool,
    ) -> (Vec<String>, Vec<String>, PendingNext) {
        let mut lines: Vec<String> = Vec::new();
        let mut prompts: Vec<String> = Vec::new();
        let mut say = |line: String| lines.push(line);
        let mut ask = |prompt: &str| {
            prompts.push(prompt.to_string());
            answer
        };
        let mut io = PendingIo {
            say: &mut say,
            ask: Some(&mut ask),
            gui_present,
        };
        let next = pending_state(l, tool, &mut io);
        (lines, prompts, next)
    }

    /// NON-INTERACTIVE: an extra's stub (or, with no stub laid, the compiled roster)
    /// puts `__pending` in the "not opted in" state — the vendor-named line plus the
    /// opt-in spelling — and with no terminal to answer it NEVER prompts, opts in, or
    /// bumps (a bump for an unplanned name could only trigger a pass that installs
    /// nothing).
    #[test]
    fn pending_extra_not_opted_in_never_prompts_without_a_tty() {
        let l = temp_layout("pend-extra-notty");
        lay_extra_stub(&l, "vendorx");
        let lines = pending_state_lines(&l, "vendorx");
        assert!(
            lines[0].contains("an extra the signed index lists") && lines[0].contains("EXTRA"),
            "the honest generic line and the word for what it is: {lines:?}"
        );
        assert!(
            lines
                .iter()
                .any(|x| x.contains("aterm pkg install vendorx")),
            "the opt-in spelling: {lines:?}"
        );
        assert!(
            !lines.iter().any(|x| x.contains("[y/N]")),
            "never a prompt without a TTY: {lines:?}"
        );
        assert!(!l.optin_exists("vendorx"), "no consent was given");
        assert_eq!(bump_contents(&l), "", "nothing to order: no bump");
        // No stub laid at all: the compiled roster answers — and for an AGENT PROGRAM
        // (owner decision 2026-09-10) the answer is DEFAULT-SET: the vendor line, "not
        // installed yet", no consent question, no opt-in marker, and a bump (it is
        // coming, so ordering it is honest).
        let lines = pending_state_lines(&l, "claude");
        assert!(
            lines[0].contains("Anthropic Claude Code")
                && lines[0].contains("downloads.claude.ai")
                && lines[0].contains("not installed yet")
                && !lines[0].contains("EXTRA"),
            "{lines:?}"
        );
        assert!(
            !l.optin_exists("claude"),
            "no opt-in marker: none is needed"
        );
        assert_eq!(bump_contents(&l), "claude\n");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// AN AGENT PROGRAM WITH A STALE CONSENT STUB — laid by a pass that ran before this
    /// client (index 21 still flags `claude` as an extra) — is never asked about: the
    /// compiled AGENT_PROGRAMS roster outranks the stub's kind, so typing `claude` gets
    /// the default-set states (bumped, coming), records no opt-in and asks no question
    /// even with a terminal to answer it.
    #[test]
    fn pending_agent_program_is_never_asked_even_over_a_stale_consent_stub() {
        let l = temp_layout("pend-agent-stale-stub");
        lay_extra_stub(&l, "claude");
        let (lines, prompts, next) = pending_with_answer(&l, "claude", true, false);
        assert!(
            prompts.is_empty(),
            "never `Install claude? [y/N]`: {prompts:?}"
        );
        assert_eq!(next, PendingNext::Done);
        assert!(!l.optin_exists("claude"));
        assert!(
            lines[0].contains("Anthropic Claude Code") && lines[0].contains("not installed yet"),
            "{lines:?}"
        );
        assert!(
            !lines.iter().any(|x| x.contains("EXTRA")),
            "never called an extra: {lines:?}"
        );
        assert_eq!(bump_contents(&l), "claude\n");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// `N` (or a bare Enter) on a TTY: the question is asked with the owner's
    /// spelling, nothing is recorded, nothing is bumped, the later spelling is named.
    #[test]
    fn pending_extra_declined_on_a_tty_records_nothing() {
        let l = temp_layout("pend-extra-no");
        lay_extra_stub(&l, "vendorx");
        let (lines, prompts, next) = pending_with_answer(&l, "vendorx", false, false);
        assert_eq!(prompts, vec!["Install vendorx? [y/N] ".to_string()]);
        assert_eq!(next, PendingNext::Done);
        assert!(
            lines
                .iter()
                .any(|x| x.contains("not installed") && x.contains("aterm pkg install vendorx")),
            "{lines:?}"
        );
        assert!(!l.optin_exists("vendorx"));
        assert_eq!(bump_contents(&l), "");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// `y`, headless, nothing running: the opt-in marker lands (what adds the work),
    /// the bump is written (what orders it), and the caller is told to install
    /// inline — the stub's own process is the only one that can move the bytes.
    #[test]
    fn pending_extra_consent_records_optin_bumps_and_installs_inline_when_headless() {
        let l = temp_layout("pend-extra-yes");
        lay_extra_stub(&l, "vendorx");
        let (lines, prompts, next) = pending_with_answer(&l, "vendorx", true, false);
        assert_eq!(prompts.len(), 1, "asked exactly once");
        assert_eq!(next, PendingNext::InstallInline);
        assert!(l.optin_exists("vendorx"), "consent is durable");
        assert_eq!(bump_contents(&l), "vendorx\n");
        assert!(
            lines.iter().any(|x| x.contains("installing vendorx now")),
            "{lines:?}"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// `y` inside a running aterm: the bump mtime is the trigger — no inline install
    /// (the GUI's pass owns the store and the progress card); the line names the
    /// watch's timing and the manual spelling.
    #[test]
    fn pending_extra_consent_inside_aterm_hands_the_install_to_the_bump_watch() {
        let l = temp_layout("pend-extra-gui");
        lay_extra_stub(&l, "vendory");
        let (lines, _, next) = pending_with_answer(&l, "vendory", true, true);
        assert_eq!(next, PendingNext::Done);
        assert!(l.optin_exists("vendory"));
        assert_eq!(bump_contents(&l), "vendory\n");
        assert!(
            lines
                .iter()
                .any(|x| x.contains("aterm picks vendory up")
                    && x.contains("aterm pkg install vendory")),
            "{lines:?}"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// `y` while a pass is running (live snapshot): no inline install — the single
    /// writer holds the store — and the bump front-loads the NEXT pass, which the
    /// opt-in marker has just made plan this program.
    #[test]
    fn pending_extra_consent_while_a_pass_runs_defers_to_the_next_pass() {
        let l = temp_layout("pend-extra-running");
        lay_extra_stub(&l, "vendorx");
        std::fs::write(
            l.progress_file(),
            live_snapshot("{\"trust\":{\"phase\":\"download\"}}", "[\"trust\"]"),
        )
        .unwrap();
        let (lines, _, next) = pending_with_answer(&l, "vendorx", true, false);
        assert_eq!(
            next,
            PendingNext::Done,
            "never inline over a live installer"
        );
        assert!(l.optin_exists("vendorx"));
        assert_eq!(bump_contents(&l), "vendorx\n");
        assert!(
            lines
                .iter()
                .any(|x| x.contains("a pass is installing now") && x.contains("bumped")),
            "{lines:?}"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// Once opted in, an extra tells the ORDINARY truths: it is in the wanted set,
    /// so queued/installing/failed/not-running apply — here queued + BUMPED — and
    /// the question is never asked again.
    #[test]
    fn pending_opted_in_extra_follows_the_regular_states() {
        let l = temp_layout("pend-extra-optedin");
        lay_extra_stub(&l, "vendorx");
        l.record_optin("vendorx").unwrap();
        std::fs::write(
            l.progress_file(),
            live_snapshot(
                "{\"trust\":{\"phase\":\"download\"},\"vendorx\":{\"phase\":\"queued\"}}",
                "[\"trust\",\"vendorx\"]",
            ),
        )
        .unwrap();
        let (lines, prompts, next) = pending_with_answer(&l, "vendorx", false, false);
        assert!(
            prompts.is_empty(),
            "opted in: no question, whatever the terminal"
        );
        assert_eq!(next, PendingNext::Done);
        assert!(
            lines[0].contains("not installed yet") && !lines[0].contains("EXTRA"),
            "{lines:?}"
        );
        assert!(
            lines
                .iter()
                .any(|x| x.contains("BUMPED") && x.contains("after trust finishes")),
            "{lines:?}"
        );
        assert_eq!(bump_contents(&l), "vendorx\n");
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// THE CONSENT QUESTION LISTS WHAT THE EXTRA REQUIRES (§17.10): the stub carries the
    /// names from the signed index, the prompt reads `Install codex? It also needs: clt,
    /// brew [y/N]`, and without a terminal the line names them beside the opt-in
    /// spelling. Consenting opts in codex ONLY — never the requirements.
    #[test]
    fn pending_extra_consent_names_the_requires_and_opts_in_only_the_extra() {
        let l = temp_layout("pend-extra-requires");
        crate::stub::write_pending_stub_with(
            &l,
            &crate::store::ToolName::new("vendorx").unwrap(),
            crate::stub::StubKind::Extra,
            &["clt".to_string(), "brew".to_string()],
        )
        .unwrap();
        // No terminal: the names ride the honest line, no prompt, no opt-in.
        let lines = pending_state_lines(&l, "vendorx");
        assert!(
            lines
                .iter()
                .any(|x| x.contains("vendorx also needs: clt, brew")),
            "{lines:?}"
        );
        assert!(!l.optin_exists("vendorx"));
        let (_, prompts, next) = pending_with_answer(&l, "vendorx", true, true);
        assert_eq!(
            prompts,
            vec!["Install vendorx? It also needs: clt, brew [y/N] ".to_string()]
        );
        assert_eq!(next, PendingNext::Done);
        assert!(l.optin_exists("vendorx"));
        assert!(
            !l.optin_exists("clt") && !l.optin_exists("brew"),
            "a requirement is never opted in on the extra's behalf"
        );
        // A stub with no requires asks the plain question.
        lay_extra_stub(&l, "vendory");
        let (_, prompts, _) = pending_with_answer(&l, "vendory", false, true);
        assert_eq!(prompts, vec!["Install vendory? [y/N] ".to_string()]);
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    /// A DEFAULT-SET stub for a name the compiled roster calls an extra: the stub on
    /// disk (written from the signed index) wins — bumped, not questioned.
    #[test]
    fn pending_trusts_the_stub_kind_over_the_compiled_roster() {
        let l = temp_layout("pend-extra-kind");
        crate::stub::write_pending_stub(&l, &crate::store::ToolName::new("codex").unwrap())
            .unwrap();
        let (lines, prompts, next) = pending_with_answer(&l, "codex", false, false);
        assert!(prompts.is_empty(), "{prompts:?}");
        assert_eq!(next, PendingNext::Done);
        assert_eq!(bump_contents(&l), "codex\n");
        assert!(
            lines
                .iter()
                .any(|x| x.contains("nothing is installing right now")),
            "{lines:?}"
        );
        let _ = std::fs::remove_dir_all(&l.prefix);
    }

    // -----------------------------------------------------------------------
    // The priority queue — reorder-only by construction (design §4).
    // -----------------------------------------------------------------------

    fn plan_of(groups: &[&[&str]]) -> Vec<(usize, Vec<String>)> {
        groups
            .iter()
            .enumerate()
            .map(|(i, members)| (i, members.iter().map(|m| (*m).to_string()).collect()))
            .collect()
    }

    /// The permutation property: whatever the bump says, the output is a
    /// rearrangement of the input — never new work, never lost work.
    #[test]
    fn resort_bumped_is_a_permutation() {
        let original = plan_of(&[&["ay"], &["trust", "trust-cg"], &["ty"]]);
        for bump in [
            vec![],
            vec!["ty".to_string()],
            vec![
                "nonsense".to_string(),
                "trust-cg".to_string(),
                "ty".to_string(),
            ],
            vec!["ay".to_string(), "ay".to_string()],
        ] {
            let mut sorted = original.clone();
            resort_bumped(&mut sorted, &bump);
            let mut a = original.clone();
            let mut b = sorted.clone();
            a.sort();
            b.sort();
            assert_eq!(a, b, "bump {bump:?} must only permute the plan");
        }
    }

    /// Bumped-first in bump order, plan order as the stable tiebreak; names outside
    /// the plan have no effect at all.
    #[test]
    fn resort_bumped_orders_bumped_first_then_plan_order() {
        let mut plan = plan_of(&[&["ay"], &["clean"], &["ny"], &["ty"]]);
        resort_bumped(&mut plan, &["ty".to_string(), "clean".to_string()]);
        let order: Vec<usize> = plan.iter().map(|(i, _)| *i).collect();
        assert_eq!(order, vec![3, 1, 0, 2], "bump order first, then plan order");
        // Unknown names are inert — the reorder-only intersection already dropped
        // anything unplanned, and even raw they cannot move a thing.
        let mut plan = plan_of(&[&["ay"], &["clean"]]);
        resort_bumped(&mut plan, &["sudo".to_string(), "zzz".to_string()]);
        let order: Vec<usize> = plan.iter().map(|(i, _)| *i).collect();
        assert_eq!(order, vec![0, 1]);
    }

    /// A BUMP PULLS THE BUMPED PROGRAM'S UNMET REQUIREMENTS AHEAD OF IT (§17.10),
    /// transitively and dependency-first, each tagged with the program that pulled it;
    /// a requirement outside the plan pulls nothing; a group's requirements are the
    /// union of its members'; and the re-sorted plan puts the requirement's group
    /// before the bumped one — a bump can never lift a program above what it needs.
    #[test]
    fn a_bump_pulls_the_unmet_requires_ahead_of_the_bumped_program() {
        let mut index = index_of(&[
            ("ay", false, None),
            ("brew", false, None),
            ("clt", false, None),
            ("ta", false, None),
            ("tb", false, None),
            ("ny", false, None),
        ]);
        index.programs.get_mut("brew").unwrap().requires = vec!["clt".into()];
        index.programs.get_mut("ay").unwrap().requires = vec!["brew".into()];
        index.programs.get_mut("tb").unwrap().requires = vec!["ny".into(), "ay".into()];
        for m in ["ta", "tb"] {
            index.programs.get_mut(m).unwrap().coherence_group = Some("rustc".into());
        }
        let group = |g: Option<&str>, members: &[&str]| crate::Group {
            group: g.map(str::to_string),
            members: members.iter().map(|m| (*m).to_string()).collect(),
        };
        let groups = vec![
            group(Some("rustc"), &["ta", "tb"]),
            group(None, &["ay"]),
            group(None, &["brew"]),
            group(None, &["clt"]),
            group(None, &["ny"]),
        ];
        let all = names(&["ta", "tb", "ay", "brew", "clt", "ny"]);
        let some = |s: &str| Some(s.to_string());
        // brew alone: clt rides ahead, tagged.
        assert_eq!(
            bump_with_requires(&index, &groups, &["brew".to_string()], &all),
            vec![
                ("clt".to_string(), some("brew")),
                ("brew".to_string(), None)
            ]
        );
        // ay: the transitive chain, dependency-first.
        assert_eq!(
            bump_with_requires(&index, &groups, &["ay".to_string()], &all),
            vec![
                ("clt".to_string(), some("ay")),
                ("brew".to_string(), some("ay")),
                ("ay".to_string(), None)
            ]
        );
        // A tuple member: the GROUP's requirements (tb's ny and ay, and ay's chain).
        assert_eq!(
            bump_with_requires(&index, &groups, &["ta".to_string()], &all),
            vec![
                ("ny".to_string(), some("ta")),
                ("clt".to_string(), some("ta")),
                ("brew".to_string(), some("ta")),
                ("ay".to_string(), some("ta")),
                ("ta".to_string(), None)
            ]
        );
        // Two bumps: first mention wins, the user's own ask included.
        assert_eq!(
            bump_with_requires(
                &index,
                &groups,
                &["clt".to_string(), "brew".to_string()],
                &all
            ),
            vec![("clt".to_string(), None), ("brew".to_string(), None)]
        );
        // A requirement outside the plan (clt already installed, say) pulls nothing.
        let without_clt = names(&["ta", "tb", "ay", "brew", "ny"]);
        assert_eq!(
            bump_with_requires(&index, &groups, &["brew".to_string()], &without_clt),
            vec![("brew".to_string(), None)]
        );
        // And the re-sort: bumping brew puts clt's group FIRST, brew's second, the rest
        // in plan order — never brew above clt.
        let mut plan = plan_of(&[&["ta", "tb"], &["ay"], &["brew"], &["clt"], &["ny"]]);
        let pulled = bump_with_requires(&index, &groups, &["brew".to_string()], &all);
        let order: Vec<String> = pulled.iter().map(|(n, _)| n.clone()).collect();
        resort_bumped(&mut plan, &order);
        let idx: Vec<usize> = plan.iter().map(|(i, _)| *i).collect();
        assert_eq!(idx, vec![3, 2, 0, 1, 4]);
    }

    /// Bumping ANY member bumps its WHOLE group — the tuple stays transactional,
    /// the queue jump is group-granular.
    #[test]
    fn bumping_a_member_bumps_the_whole_group() {
        let mut plan = plan_of(&[&["ay"], &["trust", "trust-cg", "trust-ir"], &["ty"]]);
        resort_bumped(&mut plan, &["trust-ir".to_string()]);
        let order: Vec<usize> = plan.iter().map(|(i, _)| *i).collect();
        assert_eq!(
            order,
            vec![1, 0, 2],
            "the group containing the bumped member moves whole"
        );
        assert_eq!(
            plan[0].1,
            vec![
                "trust".to_string(),
                "trust-cg".to_string(),
                "trust-ir".to_string()
            ],
            "membership untouched — activation stays all-or-nothing"
        );
    }

    /// `verify-pkg` IS THE CLIENT'S WHOLE CHAIN, NOT HALF OF IT.
    ///
    /// `tools/atpkg-publish-lib.sh` and step 1 of `tools/atpkg-mirror-public.sh` call this
    /// verb "the client's own rule" and refuse to ship what it refuses — but it checked the
    /// SIGNATURE and printed OK, so a manifest that verifies and cannot be PARSED self-checked
    /// OK, was mirrored, and was then refused by every client (`parse_pkg(&verified).ok()?` →
    /// `None`) with no pipeline stage having said why. Every body below is signed by the
    /// roster's own machine, so the only thing under test is what the verb does once the
    /// signature holds; the refusal must also NAME the fix, which is what
    /// `Reject::ShimEnv`/`RetiredKind` promise in their own docs.
    #[test]
    fn verify_pkg_refuses_a_signed_manifest_no_client_can_parse() {
        const HEAD: &str = "schema = 2\nprogram = \"claude\"\nversion = \"2.1.231\"\n\
                            build_number = 2026082601\nexposes = [\"claude\"]\n";
        const ROW: &str = "[[artifact]]\ntarget = \"aarch64-apple-darwin\"\nkind = \"binary\"\n\
                           protocol = \"https\"\nurl = \"https://example.invalid/claude\"\n\
                           payload = \"raw-binary\"\nentry = \"claude\"\nasset = \"claude-arm64\"\n\
                           sha256 = \"d\"\ntree_root = \"abc\"\nsize = 1\n";

        let dir = scratch("verify-pkg-post-verify");
        let (roster_bytes, roster_sig) = testkit::published_roster();
        let roster = dir.join("aterm-machines.toml");
        let roster_sig_path = dir.join("aterm-machines.toml.sig");
        std::fs::write(&roster, roster_bytes.as_slice()).unwrap();
        std::fs::write(&roster_sig_path, roster_sig.as_slice()).unwrap();
        let master = pk(&ROOT_SEED);

        // The verb, over a manifest MACHINE-SIGNED through the real chain every time.
        let run = |label: &str, body: &str| -> String {
            let pkg = dir.join(format!("pkg-claude-{label}.toml"));
            let sig = dir.join(format!("pkg-claude-{label}.toml.sig"));
            std::fs::write(&pkg, body.as_bytes()).unwrap();
            std::fs::write(&sig, sign(&RELEASE_SEED, body.as_bytes())).unwrap();
            format!(
                "{:?}",
                super::cmd_verify_pkg(&[
                    master.clone(),
                    pkg.display().to_string(),
                    sig.display().to_string(),
                    roster.display().to_string(),
                    roster_sig_path.display().to_string(),
                ])
            )
        };
        let verified_ok = format!("{:?}", ExitCode::SUCCESS);
        let refused = format!("{:?}", ExitCode::from(1));

        // A manifest a client can read passes, by exactly the signature it always did.
        assert_eq!(
            run("good", &format!("{HEAD}{ROW}")),
            verified_ok,
            "a signed, parsable manifest must still verify"
        );

        // ...and every post-verify gate a client applies now fails the PIPELINE instead.
        for (label, body, names) in [
            (
                "shim-env",
                format!("{HEAD}shim_env = [\"disable_autoupdater=1\"]\n{ROW}"),
                "name is not [A-Z0-9_]+",
            ),
            (
                "no-sha256",
                format!("{HEAD}{}", ROW.replace("sha256 = \"d\"\n", "")),
                "malformed",
            ),
            (
                "retired-kind",
                format!(
                    "{HEAD}{}",
                    ROW.replace("kind = \"binary\"", "kind = \"vendor-fetch\"")
                ),
                "vendor-fetch",
            ),
            (
                "newer-schema",
                format!("{HEAD}{ROW}").replace("schema = 2", "schema = 3"),
                "schema",
            ),
        ] {
            assert_eq!(
                run(label, &body),
                refused,
                "{label}: a signed manifest no client can parse must not self-check OK"
            );
            let why = super::pkg_post_verify_refusal(&testkit::machine_signed(body.into_bytes()))
                .unwrap_or_else(|| panic!("{label}: parsed, so the verb had nothing to refuse"));
            assert!(
                why.contains(names),
                "{label}: the refusal must name the fix, got {why:?}"
            );
        }
        let _ = std::fs::remove_dir_all(&dir);
    }
    /// `verify-index` MUST NOT REPORT AN AUTHORING MISTAKE AS A SIGNATURE FAILURE.
    ///
    /// `TrustedRoster::authorize_index` verifies Ed25519 FIRST, then parses the index, then
    /// binds its attribution — so `Malformed`, `Schema`, `Unattributed` and `SeqMismatch`
    /// are only reachable once a machine on the roster HAS signed these exact bytes. The
    /// verb reported every one of them as "no machine on that roster signed this index",
    /// which points the publisher at the paper master — re-join, re-key — when the fix is
    /// one line of the index they just authored. `tools/atpkg-index.sh` even promises that
    /// a misread `roster_seq` "comes back as `Reject::SeqMismatch`", a name the verb never
    /// printed.
    ///
    /// Every body below is signed by the roster's OWN machine through the real chain, so
    /// the only thing under test is what the verb says once the signature holds — and the
    /// last case guards the other direction: a crypto verdict stays opaque (§8).
    #[test]
    fn verify_index_names_the_post_verify_mistake_instead_of_blaming_the_signature() {
        let dir = scratch("verify-index-post-verify");
        let (roster_bytes, roster_sig) = testkit::published_roster();
        let roster = dir.join("aterm-machines.toml");
        let roster_sig_path = dir.join("aterm-machines.toml.sig");
        std::fs::write(&roster, roster_bytes.as_slice()).unwrap();
        std::fs::write(&roster_sig_path, roster_sig.as_slice()).unwrap();
        let master = pk(&ROOT_SEED);

        // An index this chain accepts: attributed to the roster's machine, at its seq.
        let good = format!(
            "schema = 2\nindex_build = 41\nvalid_until = \"2099-01-01T00:00:00Z\"\n\
             machine_id = \"{id}\"\nroster_seq = {seq}\n[programs.ay]\nrepo = \"ay\"\n",
            id = testkit::MACHINE_ID,
            seq = testkit::SEQ
        );

        // The verb itself, over an index signed by `seed`.
        let run = |label: &str, body: &str, seed: &[u8; 32]| -> String {
            let index = dir.join(format!("index-{label}.toml"));
            let sig = dir.join(format!("index-{label}.toml.sig"));
            std::fs::write(&index, body.as_bytes()).unwrap();
            std::fs::write(&sig, sign(seed, body.as_bytes())).unwrap();
            format!(
                "{:?}",
                super::cmd_verify_index(&[
                    master.clone(),
                    index.display().to_string(),
                    sig.display().to_string(),
                    roster.display().to_string(),
                    roster_sig_path.display().to_string(),
                ])
            )
        };
        // What the verb PRINTS for those bytes: the refusal the same call hands it.
        let refusal = |body: &str, seed: &[u8; 32]| -> Option<String> {
            let sig = sign(seed, body.as_bytes());
            let Err(why) =
                testkit::trusted_roster().authorize_index(body.as_bytes().to_vec(), &sig)
            else {
                panic!("this fixture must be refused, or it proves nothing");
            };
            super::index_post_verify_refusal(&why)
        };
        let verified_ok = format!("{:?}", ExitCode::SUCCESS);
        let refused = format!("{:?}", ExitCode::from(1));

        // A correctly authored index still verifies, by exactly the chain it always did.
        assert_eq!(
            run("good", &good, &RELEASE_SEED),
            verified_ok,
            "a signed, parsable, correctly attributed index must still verify"
        );

        for (label, body, names) in [
            // The two an edit to the index authoring script produces.
            (
                "duplicate-key",
                good.replace("schema = 2\n", "schema = 2\nindex_build = 41\n"),
                "malformed",
            ),
            (
                "wrong-typed",
                good.replace("index_build = 41", "index_build = \"41\""),
                "malformed",
            ),
            (
                "newer-schema",
                good.replace("schema = 2", "schema = 3"),
                "schema",
            ),
            (
                "unattributed",
                good.replace(&format!("machine_id = \"{}\"\n", testkit::MACHINE_ID), ""),
                "machine_id",
            ),
            // `tools/atpkg-index.sh` reads `roster_seq` with `sed … | head -n1`; a misread
            // one lands here, and that script promises this refusal is named.
            (
                "seq-mismatch",
                good.replace(
                    &format!("roster_seq = {}", testkit::SEQ),
                    &format!("roster_seq = {}", testkit::SEQ + 1),
                ),
                "roster_seq",
            ),
            (
                "bad-requires",
                good.replace("repo = \"ay\"\n", "repo = \"ay\"\nrequires = [\"nope\"]\n"),
                "requires relation cannot be honoured",
            ),
        ] {
            assert_eq!(
                run(label, &body, &RELEASE_SEED),
                refused,
                "{label}: an index no client can read must not self-check OK"
            );
            let why = refusal(&body, &RELEASE_SEED).unwrap_or_else(|| {
                panic!("{label}: reported as \"no machine on that roster signed this index\"")
            });
            assert!(
                why.contains(names),
                "{label}: the refusal must name the fix, got {why:?}"
            );
            assert!(
                why.starts_with("the index verified"),
                "{label}: the signature HELD, so the refusal must not read as a signature \
                 failure, got {why:?}"
            );
        }

        // THE OTHER DIRECTION, unchanged: a signature that does not verify says nothing
        // beyond that, so the operator still gets the one opaque line (§8).
        assert_eq!(
            run("outsider", &good, &testkit::OUTSIDER_SEED),
            refused,
            "an index signed off-roster must still be refused"
        );
        assert_eq!(
            refusal(&good, &testkit::OUTSIDER_SEED),
            None,
            "a crypto verdict must stay opaque — no verification oracle"
        );
        let _ = std::fs::remove_dir_all(&dir);
    }
}
