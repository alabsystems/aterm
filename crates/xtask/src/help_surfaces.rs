// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 Andrew Yates

//! `xtask gate help-surfaces` — every CLI help surface in the workspace has been
//! READ against its handler, and a surface that appears or changes afterwards is
//! refused until it is read again.
//!
//! WHY THIS EXISTS. Between 2026-08-31 and 2026-09-10, 138 help/usage claims
//! across the alab repos were found to contradict the code that implements them
//! — rosters hand-typed beside derived sets, exit codes that no longer happen,
//! remedies naming verbs that do not do the thing (in this workspace: the ctl
//! catalog, atpkg's usage and doctor, the fleet/drive CLIs, aterm-dev, the
//! release cutter). Every one was found by reading a help surface against its
//! handler. That read is a dated fact about ONE version of the text, and nothing
//! made it durable: a new binary's usage text, or an edit to an existing one,
//! stayed unread until somebody thought to sweep again. This gate turns "all help
//! surfaces are read-verified" from a snapshot into an invariant of every green
//! `gate all` / `cargo test -p xtask`.
//!
//! WHAT A HELP SURFACE IS. A source file that carries user-facing usage/help
//! text, discovered mechanically: a string literal with a line that starts a
//! usage block (`Usage:`, `OPTIONS:`, `COMMANDS:`, `VERBS:`, …) or code that
//! exists only to print help (`fn usage`/`print_usage`/`help_text`/`cmd_help`, a
//! `USAGE`/`HELP`-segment const, a clap `#[command(`/`about =`/`long_about`).
//! [`NOT_HELP`] classifies what discovery over-finds (each with a reason), so
//! every discovered file is accounted for one way or the other.
//!
//! WHAT IS HASHED. For Rust source, the file's PROSE: every string literal plus
//! every doc comment (`///`, `//!`, `/** */`), in source order, with plain
//! comments, char literals and code dropped. A code-only edit leaves a verified
//! row alone; any change to text a user can read invalidates it. For a surface
//! that is not `.rs` — a shipped DOCUMENT: a skill file, a primer asset — every
//! LINE, because the whole file is the text ([`document_prose_hash`]). Until
//! 2026-09-22 those were hashed with the Rust extractor too, which on a 591-line
//! skill hashed 66 double-quoted fragments and passed every edit outside them.
//!
//! REFUSALS, each naming its remedy:
//!   R1 UNROSTERED  a discovered surface has no row in [`SURFACES`] or [`NOT_HELP`]
//!   R2 CHANGED     a surface's prose differs from the hash recorded when it was read
//!   R3 GONE        a rostered path no longer exists
//!   R4 STALE       a row's date is malformed or older than [`MAX_AGE_DAYS`]
//!   R5 DEAD_ALLOW  a [`NOT_HELP`] entry names a file that is gone or no longer discovered
//!   R6 DUPLICATE   a path appears twice
//!   R7 PROGRAM     a binary entry point (`src/main.rs`, `src/bin/*.rs`,
//!                  `src/bin/*/main.rs`, a `[[bin]] path`) under [`SCAN_ROOTS`] is in
//!                  neither list — every PROGRAM is accounted for, even one whose
//!                  help lives elsewhere (say where, in [`NOT_HELP`]) or that has none
//!   R8 EMBEDDED    a file a rostered surface pulls in with `include_str!` /
//!                  `include_bytes!` is in neither list. Such a file IS shipped help
//!                  text — it reaches the binary and the reader — but discovery walks
//!                  `*.rs`, and the only string the lexer sees at the macro is the
//!                  PATH, so its content never enters the including file's hash.
//!                  Measured 2026-09-13: the four primer skill assets, 1,281 lines of
//!                  agent-facing CLI documentation `include_str!`-ed into a rostered
//!                  surface and installed into agents' own context files, were
//!                  invisible to this gate and to every other.
//!
//! The remedy for R1/R2 is a READ, not a hash bump: the refusal line carries the
//! row to paste, dated today. Pasting it is the assertion that the text was read
//! against its handler on that date.
//!
//! WHAT CHANGED. An R2 line ends `(see: xtask gate help-surfaces --diff PATH)`.
//! That verb finds the version the recorded read was of — the newest commit in
//! `git log -- PATH` whose version hashes to the row — and prints a unified diff
//! of the PROSE ONLY from it to the working tree, item by item as the hasher sees
//! it ([`prose_lines`]; a long item continues on lines of its own): a literal
//! continued across source lines with no quote on the continuation still shows,
//! by the line that changed. A row recorded from an uncommitted tree matches no
//! commit; the verb says so and diffs from the commit that last touched the row
//! instead.

use std::collections::BTreeMap;
use std::fmt::Write as _;
use std::path::{Path, PathBuf};

/// One read-verified surface: workspace-relative path, prose hash
/// ([`prose_hash`], FNV-1a 64 as 16 hex digits), the `YYYY-MM-DD` it was read,
/// and by what method.
pub(crate) type SurfaceRow<'a> = (&'a str, &'a str, &'a str, &'a str);
/// A file discovery over-finds: path and why it is not a help surface.
pub(crate) type NotHelpRow<'a> = (&'a str, &'a str);

/// A read older than this is stale: the text may still be byte-identical, but
/// the code under it has had half a year to move.
const MAX_AGE_DAYS: i64 = 180;
/// Where discovery walks (workspace-relative). Test, example and bench trees are
/// skipped by [`excluded_path`].
const SCAN_ROOTS: &[&str] = &["crates"];

/// The roster. Every entry is a dated assertion; see the module doc.
const SURFACES: &[SurfaceRow<'static>] = &[
    // HELP_SURFACES_ROSTER_BEGIN
    (
        "crates/aterm-agent/src/supervise/run.rs",
        "9e24acc6ed7ca86b",
        "2026-09-24",
        "2026-09-24 (harness fix wave, laws review minor): ATTENTION_PREFIX's doc now says what it is — the limit episode's text prefix open_episode writes and close_episode unsets — and that ownership is the keyed entry (ATTENTION_OWNER, `owner=supervisor`), read against escalate.rs escalate/unset_if_ours and lifecycle.rs reconcile; the deleted `aterm harness: …` writer is no longer named; no other prose changed ; 2026-09-24 (harness fix wave, laws review minor): render_result's `cancel esc` line (always: every box is found by its `Esc to …` footer; aterm-phase's has_cancel field, always true, deleted) read against aterm-phase prompt.rs find_box/is_hint_footer; no other prose changed ; 2026-09-24 (harness fix wave, reliability review): read the handover field and set_handover docs against lifecycle.rs run_hosted (a stop with the flag raised releases only the claim and journals KEPT; any other end releases the badges as before) and reconcile (an idle point's `claude idle:`/`claude wall:` badge adopted like a box's), and aterm-gui harness_host's Worker::hand_over (raised before the stop when the policy generation moved and the session is still wanted); no claim contradicted ; 2026-09-24 (harness fix wave, safety review): read MAX_CHANGED_PRESSES' doc (5; the box handed over, never pressed with the generation fence dropped) and the changed_streak field doc against press.rs press_one_guarded (the fence sent whenever help key names it and the read carried a generation) and approval_loop's Press::Changed arm (box_reason set, Step::Review after the streak); the point-escalation arm's interrupt comment against aterm_phase::interrupted and decide_turn_end's first return; no claim contradicted ; 2026-09-24 (harness fix wave, E2E D1/D2): read the changed prose against the code: ATTENTION_BYTES' doc (the server's KEYED cap, aterm_types META_ATTENTION_KEYED_MAX = 200, which aterm-gui session_timeline's keyed write refuses past with `ERR attention too long (max 200 bytes)`, control_session.rs) against escalate's fit_bytes and refused_cap retry; the recorded guard strings now ending `\\s*$` against policy::guard ROW_END; the new test docs (a_refusal_naming_a_tighter_cap_is_sent_again_cut_to_it) against escalate; no claim contradicted ; read crates/aterm-agent/src/supervise/run.rs's changed prose against the code it names (lane B2, harness wave 2): SuperviseOpts' doc (policy is the one place a switch lives; auto_reads and dismiss_surveys are the CLI's opt-in gates) against approval_toggles/dismissing and SupervisorConfig::cli/hosted_with; ATTENTION_OWNER against escalate's `meta set attention owner=supervisor` and unset_if_ours' keyed unset (never the bare form); the Session fields program/claim/claim_said/claim_renew/claim_ttl_ms/supervisor_name/attention_ours and set_supervisor_name/set_claim_timing against claim.rs (take_claim, renew_claim_if_due from call, claim_refuses, release_claim) and approval_loop's decide_box (status program= per box, the bounded `await agent prompt` re-read); Pressing::Unconfirmed and MAX_FOCUS_STEPS against press_focused; the cwd doc against approval_ctx's fresh meta read; the rebrief and probe_ready claim gates; and the test prose this change rewrote (the Mock's program=/await agent/claims/keyed-attention model and the keyed-attention episode test, and the Mock's `agent=` verdict and the ordered request log `order` the Tier-1 binds of SupervisorClaim and SupervisorFocusChoice project) against the Mock's serve/call/call_counted arms; 2026-09-24 (lane B2 stage 2, the turn-end policy) read of run.rs's changed prose against the code it names: the module doc's turn-end paragraph and its --resume sentence against turn_end_loop.rs (turn_end_at_point observes once and decides, turn_end_execute types through `turn submit=guarded:` via type_guarded or accepts via accept_suggestion, a WaitUntil kept in turn_end_due and waited as wait_for_next's deadline, turn_end_now deciding again on the same review key only) and policy::turn_end::decide_turn_end; TURN_IDLE/TURN_TIMEOUT against type_guarded; RESET_GRACE's continuation clause against extend; Resume's rules doc against rules_text (policy.rules_file first, else --resume's file, read when typed); Episode/Retry docs (no probe_at/failed; raised) against open_episode/limit_after/limit_on_busy (a handled wall journals LIMITED and raises nothing; the notice again while waiting escalates once); set_resume/set_turn_end_timing against turn_end_cfg (resume_limits exactly when the loop resumes) and TurnEndState::new; hosted()'s doc against SuperviseOpts::hosted; the Session fields turn_end/turn_end_due/busy_since/wall_reset/turn_end_badge against await_turn_from's busy stamp, wall_reset_at's per-text cache and look's badge clear; look's and wait_for_next's docs against their bodies; the Caps guarded_submit doc against type_guarded; and the rewritten limit tests' and the Mock's held/turn_gates/screen_cols prose against the Mock's screen_json and `await seq` arms; no claim contradicted; 2026-09-24 (lane B2 fix round) read of run.rs's changed prose against the code it names: Caps.send_gen's doc against turn_end_loop.rs send_fence_known (probed from `help send`, cached) and type_fenced; the turn_end_seeded field doc against drive's once-per-session seeding from approvals::typed_at into TurnEndState::seed_acts; and the test prose (continue_request's `yield=0.2`, the Mock's fenced `send` arm answering `OK skipped reason=changed`/`OK skipped` or `OK seq=`) against type_guarded's argument list and the Mock's key arm it mirrors; 2026-09-24 final-integration glue read (grep_guard V1: the engine's matchers leave the allow-list): BUSY_FOOTER and SURVEY_ROW became busy_footer() and survey_row(), built from aterm-phase's anchors (`guard_regex(anchor(\"busy.interrupt\"))` is `esc.to.interrupt`, `^●.` + `guard_prefix(\"survey.question\", 4)` is `^●.How.is.Claude.doing`, pinned by aterm-phase's a_guard_prefix_is_the_leading_words_as_a_guard and by this file's survey tests, which still assert the exact `key if=^●.How.is.Claude.doing 0` requests); the survey EVENT line and the --help dismiss line print the same text through `{}`; busy_footer_up reads the same anchor; their docs say so; no printed text changed; 2026-09-24 final-integration glue read (one supervisor claim; a stopped loop writes nothing): SuperviseOpts::yield_when_held's doc against drive (take_claim then yield_to_holder before reconcile, and again at the top of every look) and claim.rs's yield_to_holder/CLAIM_HELD — hosted() sets it, the CLI's supervise_opts and the tests' auto() leave it off, and the loop's claim is the only one (release now holder-conditional); STOPPED_WRITE's doc and text against Session::call, which refuses every claim::is_input verb (key, send, turn, paste, feed-bin) once stopped() before renewing or reaching the transport, and run_hosted, which ends a stopped run as Ok (EXIT stopped); pinned by a_hosted_loop_behind_another_claim_ends_and_presses_nothing and a_stop_between_the_look_and_the_press_writes_nothing (the second fails with the fence removed: the guarded 1 reaches the transport)",
    ),
    (
        "crates/aterm-forge/src/budget.rs",
        "5f1d4201da04506e",
        "2026-09-16",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-forge); the package_dir mechanism sentence was false for a direct-path dep and is fixed here; 2026-09-14 drift sweep (lane forge, 46 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-16 dead-code sweep: `seed_from_live` and its five-line doc comment were deleted as unreferenced — nothing in the workspace called it, and the row's whole prose delta is that doc plus the two-piece `refusing to seed ... from an incomplete measurement` error string it owned. The surviving prose was re-read against its handlers and is unchanged: the seed-body doc still names `seed_shape` (which `unarmed` still calls), `run` still measures before it learns the file is empty, and `unarmed` still renders from that one measurement. No claim contradicted",
    ),
    (
        "crates/aterm-forge/src/attest.rs",
        "e3440d18f94bece1",
        "2026-09-14",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-forge); the [OB-1] exemption wrongly covered [OB-10] and is fixed here; 2026-09-14 drift sweep (lane forge, 46 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — ONE slip fixed before this row: `[OB-1]`'s doc said every vendor directory is claimed by a patch or by the direct-bundle record, where there is a THIRD shape it also accepts, a FIRST_PARTY_VENDORED row a member really reaches by `path = …`",
    ),
    (
        "crates/aterm-agent/src/fleet_cli.rs",
        "164ab0315abd0f19",
        "2026-09-24",
        "read against its parser/dispatch by the 2026-09-10 sweep; findings fixed in 412cf3acd; 2026-09-24: ctl_bin's sibling lookup now goes through drive_cli::sibling_ctl, beside the RESOLVED binary, which drops one duplicate `aterm-ctl` literal; ctl_bin's doc (`$ATERM_CTL`, else a sibling of this binary, else the bare name on PATH) still matches it",
    ),
    (
        "crates/aterm-agent/src/lib.rs",
        "cb63e26d1eee2ce2",
        "2026-09-24",
        "2026-09-24 (harness docs pass): read DRIVE_HELP's new SUPERVISING A WORKER preamble against the code: the window's host supervises every Claude Code session by default (aterm-gui harness_host.rs, one worker per session whose server-published program is claude, Session::run_hosted under SuperviseOpts::hosted_with), [harness] enabled/headless (supervise/config.rs SupervisorConfig defaults enabled=true headless=false; the Settings Harness row is prefs.rs EDIT_HARNESS_ENABLED), `supervisor=aterm-harness@<pid>` (harness_host::holder), escalation to keyed attention owner=supervisor (escalate.rs, ATTENTION_OWNER) raising the status_item row and one Herald notification; a watch behind a live claim watches only (claim.rs Claim::Behind, WATCHING) and one that holds it first parks the host (hosted() yield_when_held, CLAIM_HELD); no other prose changed ; 2026-09-24 (harness fix wave, laws review minor): DRIVE_HELP's phase entry says `then cancel esc` — render_result prints that line for every box (the `cancel none` arm read the always-true has_cancel and could never print); no other prose changed ; 2026-09-24 (harness fix wave, laws review): read DRIVE_HELP's supervise --auto-reads paragraph (the folder-trust dialog's trust option when its folder is exactly the session's cwd under a trust root, the defaults ~/aterm*, ~/ay*, $HOME/trust*, /private/tmp/claude-*; --auto-reads the switch for the whole approval policy) against supervise/policy/approval.rs trust_dialog (toggles.trust_dialog, cwd_known, the exact cwd compare, trust_roots.holds, the two-option shape) and run.rs approval_toggles (auto_reads gates every rule), SupervisorConfig::cli (ApprovalToggles::default, trust_dialog on) and the default trust_roots in config.rs; no other prose changed ; 2026-09-24 (harness fix wave, E2E D2): read DRIVE_HELP's limit sentence `cut at 200 bytes` against supervise/run.rs ATTENTION_BYTES (= aterm_types META_ATTENTION_KEYED_MAX, 200) and escalate's fit_bytes of the `limited: <message> reset=<when>` text; the old 256 was the bare field's cap, which a keyed write is refused under; no other prose changed ; read crates/aterm-agent/src/lib.rs (DRIVE_HELP's await-turn timeout sentence, supervise's TIMEOUT sentence, watch's dedup and last-line paragraphs, and the rewritten --reconnect-s entry) against supervise/run.rs Session::call/unserved/ride_out (the outage-wide window from the first unserved request, one RECONNECT and one RECONNECTED per outage, 0.5 s doubling to 8 s across it, Rode::Spent → TIMEOUT, no such session not yet an answer only inside an outage, ERR exited always final), drive's fresh-only handed reset, moved_past's check read, the fallback press's Pressing::Lost handling and look's stray check, spent_turn/render_phase's `no screen` reason, drive_cli.rs main_entry (an Err exits 1) and await-turn's timed_out → 124, CtlReply::lost's TURNED_AWAY lines, and aterm-ctl's resolve_path (--sock, then $ATERM_CONTROL_SOCK, then self_instance_sock, then aterm.sock) and the `instances` verb's per-instance socket column (crates/aterm-ctl/src/lib.rs), by feat/handoff-survival on 2026-09-12 after the adversarial review of f6845c667; one slip fixed before this row (watch's dedup said the point is reprinted when the connection was lost, where the code resets on an outage's first answer, turned-away requests included — reworded to an outage came); the rest of DRIVE_HELP unchanged since the round-4 read of 2026-09-12; 2026-09-13 read of DRIVE_HELP's round-7 additions (shown by `xtask gate help-surfaces --diff`, matched at 718dda231) — the watch/report synopsis lines, watch's --report paragraph, the report entry and the two examples — against drive_cli.rs parse_sub (--since through Mark::parse, --max-rows > 0, --report) and its report arm (an Err exits 1), supervise/report.rs (newest_turn over `history 8`, find_start/is_user_row/user_text/paste_like/paste_fits with MARKER_CHARS 60 and PASTE_CHARS 200, offscreen_snapshot's since=/tail= max= screen=1 read, parse_offscreen's stderr header and its NoHeader fallback to `text --json`, join's back_at/pin skip of re-shown rows the read got, assess, gap_after's one-row recheck, finish's blank trim, Report::header/render), phase.rs transcript_end/is_done_row/is_parked_above_composer, run.rs reported/reported_event_line, and Turn::run (prompt types with send + key enter, so it leaves no ledger turn); two slips fixed before this row (main-screen said `the screen alone`, where the one offscreen read still joins the rows archived since the mark when the worker left the alternate screen — it now says what the reason means; complete=1's conditions left out the host keeping an archive, which no-archive refuses) and the reasons paragraph reflowed (one line had run to 109 columns); 2026-09-13 read of DRIVE_HELP's round-8 additions (shown by `xtask gate help-surfaces --diff`, matched at 547991d5b) — the supervise and watch synopsis `[--dismiss-surveys]`, phase's `survey 0` paragraph, await-turn's `exactly like phase`, supervise's withheld-fallback clause and survey sentence, watch's `EVENT survey` paragraph and the --dismiss-surveys entry — against drive_cli.rs parse_sub (--dismiss-surveys refused off watch and supervise) and phase_reply (phase and await-turn both render_phase_and_survey, 124 on a timed-out turn), phase.rs survey_open (the `●` question row in column 0 right over the options row, then only blank rows, right-aligned hints and `⎿  Tip:` rows down to the top rule, never without the composer frame), run.rs survey/hand_survey/dismiss_survey/typed_draft/survey_event_line and press_one_guarded's Withheld arm, StopAtReview::say (stderr) and Lines::say (stdout), and the server's input_if_row_matches (the check and the key under one terminal lock, `OK skipped` when no visible row matches; crates/aterm-gui/src/control_input.rs); three slips fixed before this row (the guard was said to let no copy of the survey on the screen through, where only a quoted copy — indented, or under `⎿` — is sure not to match; supervise's survey sentence said --dismiss-surveys dismisses it and says DISMISSED, where the line comes only once a fresh read shows it gone; a `0` found in the composer was put down to the survey leaving first alone, where the code backspaces one an open survey did not take as well); 2026-09-13 read (2026-09-14 UTC) of DRIVE_HELP's round-9 additions (shown by `xtask gate help-surfaces --diff`, matched at 20cd46a07) — the supervise and watch synopsis `[--context-warn PCT]`, phase's `context <n>%` paragraph, await-turn's `survey 0` and `context <n>%` clause (the rest of that paragraph reflowed, its words unchanged), supervise's stderr sentence, watch's `EVENT context`/`EVENT compacted` paragraph and the --context-warn entry — against drive_cli.rs parse_sub (--context-warn refused off watch and supervise, a value 0 to 100) and DEFAULT_CONTEXT_WARN, phase_reply (phase and await-turn both render_phase_and_survey, context after survey 0), phase.rs context_left (status_block's from to the top rule, is_against_right_edge, context_reading's two spellings), run.rs watch_context (the inclusive threshold, the frame-and-no-box rule for gone, the 30-point rise, the re-arm), await_turn_from's per-read call, drive's arming at each run's start, StopAtReview::say (stderr), Lines::say (stdout) and the tests supervise_watches_one_run and the_context_watch_is_inclusive_and_needs_the_frame; one slip fixed before this row (phase's paragraph put the indicator under the status row alone, where with no status row it is read under the last transcript row); 2026-09-14 read of DRIVE_HELP's round-10 changes (shown by `xtask gate help-surfaces --diff`, matched at cc86c5cfe) — the report entry's archive-reset and archive-gap clauses and its no-turn start — against aterm-gui handoff_carry.rs (export, tail_from, encode_within, decode and adopted_ledger: one sidecar carries the ledger and the archive tail, and a dropped or busy-locked ledger leaves the adopted one empty), aterm-core alt_archive.rs AltArchive::import (the carried origin kept; Refused when its indices are out of range or the archive is off) and carry_rows, spawn.rs's install right after restore_checkpoint, supervise/report.rs gather_report/ledger_turn/assess (archive-reset only when the mark's origin is not the read's, so a dropped carry opens at marker=user-row and only --since an older mark says archive-reset) and seamless_carry_tests' end-to-end resize variant (archive-gap, duplicates allowed, no row lost); one slip fixed before this row (the entry implied a failed carry reports archive-reset, where a ledger it could not carry leaves no turn and the start is the last `❯` row — the no-turn sentence now names that cause); 2026-09-14 read of DRIVE_HELP's round-11 additions (the supervise/watch synopsis `[--journal FILE]`, report's `[--final | --messages]` and its two view paragraphs, the whole `ledger` entry, and the `--journal FILE` entry) against drive_cli.rs parse_sub (--journal on watch/supervise/ledger only, --final/--messages on report only and never both, --format text|md|html and --out on ledger only, ledger's --since through supervise::parse_since where every other verb's is a Mark), ledger_verb (the worker from @sid else journal_sid's single sid, else an actionable error; the ledger dispatched before the host preflight; --out through write_private at 0o600 and the `wrote <path>` reply), clock_anchor (fleet_sessions for the pid, local_instances for the socket, its birth time as the process clock's zero) and tz_offset_s/parse_zone (`date +%z`, UTC when it cannot run), supervise/ledger.rs gather (whoami, `history`, one `offscreen tail=20000 max=20000 screen=1`, `@self inbox --peek --meta`, `@self timeline`, each miss a SOURCES row), reply_sizes (report's own join and the `❯` row found by the turn's first 60 characters or a fitting `[Pasted text …]` row, a turn whose row is nowhere named in the span that holds it), summary/items (the latency from each EVENT idle/question to the next turn's start, only with the clock placed; `~` on every unplaced time; a carried turn with no time), render_text/render_md/ledger_html::render_html (the four lanes, the inline style and script, the `default-src 'none'` policy), supervise/blocks.rs (the head rules NOTICE_HEADS/is_call_head/the next-row `⎿` rule/is_group_row, and view_rows: Final is the last Message block and the first Done after it, Messages every User and Message block without its `⎿` runs plus each Done row) and report.rs render_view (`view=`/`kept=` after `last=`, `rows=` unchanged, View::All byte-identical to render); measured for this row: the control socket's birth time is within 0.12 s of the fabric bus's own stamps on the same three messages on this machine, where `ps -o lstart=` was 5.3 s early; the --final clause was sharpened after the live read of 2026-09-14 showed blocks.rs is_group_row takes a group STILL RUNNING (`Running 3 shell commands · 9m 13s…`) for a tool row too; no claim contradicted; 2026-09-14 read again on the merge branch of every DRIVE_HELP block this round added: the `ledger` block against drive_cli.rs ledger_verb (the worker is `@sid` else journal_sid, and a journal naming two sids names none) and ledger.rs reply_rows, whose request is `offscreen tail=20000 max=20000 screen=1` from LEDGER_MAX_ROWS = 20_000 exactly as the help spells it, and against gather/render_ledger, which name a source they could not read and still return a Reply::text, so the exit stays 0; the `--journal` block against journal.rs JournalRecord::of_line (the six kinds the loops write - event, approved, dismissed, reconnect, timeout, exit - and the eight phase words, every field taken from the line) and Journal::open (create + append, 0600 only on creation, one warning); `report --final|--messages` against report.rs render_view (` view=<name> kept=<n>` appended after the header's own `last=`, `rows=` still the whole report, View::All returning render() unchanged) and blocks.rs view_rows/head_kind/is_tool_head; --format, --out, --since and --journal ownership against parse_sub's per-verb refusals; no claim contradicted, and one slip fixed OUTSIDE the roster in blocks.rs's module doc, which named five of NOTICE_HEADS' eight notices and gave `searched memories` as a whole collapsed-group row where is_group_row requires the first clause capitalised; 2026-09-14 (feat/round-14-mail, round 14 D1/D2/D4) read of the --mail and task prose — DRIVE_HELP's synopsis lines, the watch, task and --mail entries and the journal's mail kind and report field (lib.rs); DRIVE_PAGE's watch, task, --mail and --journal entries (manual.rs); run.rs's module doc, SuperviseOpts::mail, Fold, MailIn, NoLane, Sink, the Review, StopAtReview, Lines and folded_event_line docs, supervise_mail, supervise_with, run_loop, watch_mail, watch_with, look's fold gate, hold_for_report, event_line_as, render_result_mail and the six mail test docs; the two skills' loop, watch, task and ledger paragraphs — against mail.rs Lane::serve (an inbox 1 --peek --meta baseline; await inbox since=<newest> kinds=<all nine> timeout <step>, re-armed on OK timeout, the kinds list dropped on ERR usage; inbox since=<id> --peek --meta, then inbox get <id> for the watched worker's report only; the MAIL line per row through the loop's sink; lane_off said once on any other ERR or a lapsed reconnect window), Lane::call's retry of a lost reply (pause doubling to pause_max, within reconnect), task() (post to=@sid kind=task [dl=<ms>] <text> from --inbox or @self, the offset from the OK's off=, one text --json read and `turn idle=600 timeout=2500 Inbox: task @<off>` only on Phase::Idle with submitted=1 as nudged, the inbox 1 baseline read BEFORE the post, await inbox … kinds=answer,report,ack re-armed on rows without re=<off>, the TIMEOUT line and EXIT_TIMEOUT), run.rs hold_for_report (pending drained; a delivery within the window folds at once, else recv_timeout to min(now + grace, deadline); Disconnected gives None so the line is as without the flag), look's Phase::Idle gate with the brief skipped only on Fold::Report, run_loop's thread::scope join (the lane returns within one mail_step) and drive_cli.rs parse_sub (--mail, --report-window and --idle-grace watch's and supervise's only; --inbox needs a leading @; --deadline, --wait and --no-nudge task's only), mail_needs_sid and task_opts (--wait bounded by --deadline, else --timeout); measured on the live worker s-1e918c4662a1b7b8bd43 (busy for the whole 25 s budget: TIMEOUT, the process ending 40 s in as the lane's parked wait was joined within its 20 s step, no MAIL lane off) and by the mock tests; no claim contradicted; 2026-09-15 read of DRIVE_HELP's --mail and task entries as the round-14 reviews (wake-economy, report-hook-safety) changed them — the one screen read per 20 s hold step, a superseded point said as idle-no-report with what followed (a footer tick held on), a report attributed to its turn (after the worker was read busy for it, or after the point; never one from before the last point handed over or between it and the next busy read; --report-window only for one from before the turn was seen to begin), the lane's parked wait cut short (aterm-ctl signalled) so supervise --mail returns at once and watch --mail ends with its last line, the resync from the bus offset after a self-update, and task --wait re-armed past the host's 600 s clamp — against supervise/run.rs hold_for_report (hold_step slices, review_key compare, Held::carried), MailIn::is_this_turns (floor, busy_at, handed, window), await_turn_from's busy_at stamp, look's floor reset on reported() points, run_loop's Ctl::interrupter call, lib.rs CtlClient::cutter (SIGTERM to the pid under the lock, the cut flag checked after spawn), supervise/mail.rs Lane::resync (inbox --peek --meta whole, rows above the last off, since= the successor's newest id) and task's re-arm (OK timeout → continue while ≥ 1 ms is left), by feat/round-14-mail on 2026-09-15; no claim contradicted; pinned by run.rs's a_question_turns_report_is_not_the_next_turns, a_second_report_of_a_turn_is_not_the_next_turns, a_mid_turn_report_older_than_the_window_is_still_its_turns, a_prompt_after_an_idle_is_seen_within_a_hold_step, supervise_mails_result_does_not_wait_for_the_lanes_parked_step and mail.rs's a_wait_re_arms_on_the_hosts_own_timeout_while_time_is_left, the_lane_hears_the_successors_rows_after_a_handoff; 2026-09-14 (2026-09-15 UTC) read, before the merge, of every prose line `xtask gate help-surfaces --diff` shows moved since main's recorded read (matched at 6318eb4f6): DRIVE_HELP's synopsis lines, the watch entry's --mail sentence, the whole task entry and the whole --mail entry — against supervise/mail.rs (Lane::serve: the `inbox 1 --peek --meta` baseline, `await inbox since=<id> kinds=<nine> timeout <step>` re-armed on OK timeout, the kinds list dropped on a usage error, the listing since=<id> and `inbox get` for the watched worker's report only; Lane::resync after a lost request, from the bus offset; task(): the baseline read before the post, `post to=@sid kind=task [dl=<ms>] <text>`, `task not posted: <ERR>` and `task not landed` for a reply without off=, one `text --json` read and `turn idle=600 timeout=2500 Inbox: task @<off>` on Phase::Idle only, submitted=1 as nudged, `await inbox … kinds=answer,report,ack` re-armed on the host's timeout while at least 1 ms is left, the TIMEOUT line and EXIT_TIMEOUT), supervise/run.rs (MailIn::is_this_turns: floor, busy_at, handed, window; hold_for_report: pending drained, the newest of this turn's folds, else recv_timeout in hold_step slices with one screen read per slice, an equal review_key holds on, else NoReport with the screen carried; look: the carried screen is the next turn unless busy; run_loop: the stop flag then the interrupter, thread::scope join; render_result_mail; folded_event_line), lib.rs CtlClient::cutter (SIGTERM to the registered pid, the cut flag checked after the spawn), drive_cli.rs parse_sub/mail_needs_sid/task_opts and aterm-gui fabric.rs cmd_post (WAIT_DEFAULT_MS = 30 s, `OK <id> off=<n>` on a landing, `ERR timeout id=<n>` past the wait); ONE claim contradicted and fixed in this commit: the task entry said a worker with round 12's wake hooks reads a task on its next Stop, where aterm-link hook.rs may_wake/accepted wakes only for a human or a sender whose OWNER (`s-…`) the hook's --accept-from lists — measured on the owner's live worker, whose hooks list the node id and whose inbox holds three of the manager's tasks, kind=task, none woken for — so the entry now says the manager's sid must be in --accept-from; no other claim contradicted; 2026-09-17 (feat/round-17-limits) read of DRIVE_HELP's round-17 prose — the watch synopsis lines' `[--resume [RULES]]`, the watch entry's usage-limit paragraph, the `--resume [RULES]` entry and the --journal entry's kind and phase lists — against supervise/run.rs look/limit_before/limit_after/escalate/close_episode/extend/probe/settle_probe/rebrief (the attention text and its 256-byte cut, the control post from the worker's session to --inbox else $ATERM_PARENT_SESSION_ID else skipped, one ESCALATED line an episode, EXTEND once a reset at reset + 10 min only when the deadline falls before it, `meta unset attention` on close, the probe's exact text and `turn idle=600 timeout=2500`, idle-and-empty-composer only with a draft deferred a step and a question left to the manager, the 120 s answer window, EVENT resumed/rebriefed/rebrief-failed/still-limited and the 10-then-30 min backoff never before a later reset), drive_cli.rs parse_sub's --resume arm (watch only; the next word unless a flag or an @sid), resume_opts (read now, never empty) and manager_sid, supervise/limit.rs parse_reset/reset_at/zone_offset_s, and journal.rs of_line's extend/escalated/cleared/probe kinds; no claim contradicted; 2026-09-17 (round-17 adversarial fixes): re-read DRIVE_HELP's watch limit paragraph (the span-from-the-print and 8-day clauses, the no-composer deferral, the busy-past-120-s wait, `no reaction within 120 s`, the same-text clause) and the --journal phase list (`rebrief-failed` added) against supervise/run.rs refresh_reset, RESET_HORIZON and extend, probe_ready, probe and settle_probe, and supervise/journal.rs's phase roster; 2026-09-17 read of DRIVE_HELP's usage-limit paragraph under watch, the `--resume [RULES]` entry, the two synopsis lines and the journal record's kind/phase lists at the rebase onto main (ca8ab9aef) against supervise/run.rs (escalate, close_episode, extend, probe_ready, probe, settle_probe, rebrief, arm_probe_at_reset, refresh_reset; ATTENTION_BYTES 256, RESET_GRACE 10 min, RESET_HORIZON 8 d, ANSWER_WINDOW 120 s, PROBE_BACKOFF 10/30 min, PROBE_IDLE/PROBE_TIMEOUT), drive_cli.rs (`--resume` refused off watch, the next word the file unless it starts with `-` or `@`, resume_opts's unreadable/empty refusal, manager_sid's --inbox else $ATERM_PARENT_SESSION_ID), journal.rs's kind list (mail|extend|escalated|cleared|probe) and phase list (…|resumed|still-limited|rebriefed|rebrief-failed), limit.rs (SAME_DAY 19 h, the nearest-year date, zone_offset_s via `date`), and the attention-badge claim against aterm-types control_verbs.rs's own `meta` help (the menu-bar status item badges `attention`); no claim contradicted, prose unchanged since the row's hash; 2026-09-19 (round 20) read of DRIVE_HELP's rewritten limit sentences — the auto-continue notice's text and `reset=1:50pm` / `shortly` a minute off (aterm-phase reset_of hands over `1:50pm` / `shortly`, limit::parse_reset reads both, SHORTLY = 60 s), busy under it reads busy (worker_phase: a hard busy_signal wins before limit_notice), the episode's end at the FIRST busy read after that notice (run.rs limit_on_busy under await_turn_from, unattended only) and at an answer or a box after a notice naming a reset (look's `worked || Prompt` close), `meta unset attention` + `CLEARED seq=<n> …`, and the probe a minute past an auto-continue time (arm_probe_at_reset's SHORTLY grace for resumes_itself) — against run.rs and limit.rs; the fourteen-hours figure is the watcher's own journal of 2026-09-17 (limited 13:50:18, CLEARED 05:28 next day); no claim contradicted; 2026-09-19 (round 20 review fixes) read of the added same-episode sentence — the wall again after the busy read, before an answer, sets the attention again, posts no mail (`ESCALATED … mail=skipped: the retry hit the wall again, the episode of seq=<m>`) and keeps the backoff — against run.rs open_episode/escalate(again)/Retry; no claim contradicted; 2026-09-17 round-19 read: DRIVE_HELP's watch paragraph (each decision told as `aterm ctl @sid story <verb> [<text>]` through a client of its own, one per journaled APPROVED / DISMISSED / RECONNECTED / TIMEOUT / EXIT / `EVENT context` / `EVENT compacted` line, never the command, an APPROVED told bare, the printed lines byte-identical) against supervise/run.rs story_of, Sink::record and drive_cli's watch arm; the ledger paragraph's LEDGER KEY sentence (⇧⌘L / Ctrl+Shift+L / `open_ledger`; `ledger @<focused sid> --format html --out <a 0600 file in the temp dir>`, opened in the browser) against aterm-gui ledger_key.rs ledger_argv / plan_for / launch / open_file, keybinding.rs BUILTIN_CMD_CHORDS and platform_defaults, app_input.rs's `l`/`L` arm and dispatch_action, and drive_cli's write_private (0600). No claim contradicted; 2026-09-19 read of the `watch` help's story-lane sentence against story_of and Journal::record (one post per journaled decision, never the command, the printed lines unchanged with or without a host that knows the verb) and the `ledger` help's ledger-key sentence against ledger_key::ledger_argv (`ledger @<sid> --format html --out <0600 temp file>`); 2026-09-21 read of the one addition the aterm-wrapper work made to this file (`pub mod harness;` and its doc naming the four pure judgments (the bounded ledger, the rm verdict, the usage view and the limit classifier) and the seams it deliberately does not touch) against crates/aterm-agent/src/harness/mod.rs: it is a module declaration, not a CLI surface, and DRIVE_HELP and every help string in this file are unchanged by it ; 2026-09-22 read of the ONE addition, `claude_composer_ready_pattern`, against \
         its callers and its engine: `harness::watch::fenced` and `compose_clear_line` are \
         the two places it is spelled into a `key if=<re>` line, and \
         `aterm_observe::row_matcher` is the engine the server compiles that guard with \
         (control_input.rs `compile_guard`). Every claim in the doc is a measurement a test \
         now drives through that engine over the measured approval box \
         (`aterm_phase::prompt::fixtures::bash_one_row`): the plain-caret pattern DOES match \
         ` \u{2771} 1. Yes` and the composer pattern does not, and no row of that box arms it. \
         The sentence about `aterm_phase::phase` separating an option row the same way is \
         `is_option_like`; the doc says the new pattern is STRICTER than that reader (any \
         leading digit, not just digits-then-dot) rather than claiming they agree. \
         `claude_prompt_ready_pattern` itself is unchanged, and the rest of this file was \
         not touched; 2026-09-22 (2) re-recorded after the second merge with origin/main, which brought its own read of this file; this round's claim is unchanged and re-checked: the report is the worker's SCREEN and `rows=` excludes the stamp line. No claim contradicted; 2026-09-22 read of DRIVE_HELP's `phase` entry (the parsed box now lists `note` between description and classify) against supervise::run::render_prompt, on top of the round-22 text this commit rebased onto; ; 2026-09-22 (the harness wire) read of the FOUR doc comments this change adds to `RelayClient` — `transport`, `request_line`, `request_counted` and `next_pushed` — against their own bodies in this same file. `transport` returns `&self.io` and its doc names the one caller and the one use: `harness::wire::CtlWire::arm` takes a `try_clone` of it so a connection parked in an `await` can be SHUT DOWN from the thread that decided the wait is over. `request_line` is `write_line` then `read_line` and returns the reply verbatim, interpreting nothing — and `write_line`'s existing embedded-terminator refusal is what the Errors section names. `request_counted`'s discriminator really is the first token after `OK ` parsing as a `usize`: a `Status`-framed reply (`OK schema=1 …`) and an `ERR …` both fall to the `None` arm and return with an EMPTY body and NO further read, which is what the doc says and is why a counted read cannot desynchronise a connection that answered a status line; the `MAX_TEXT_LINES` bound is the same one `read_text` already applied. `next_pushed` is `read_line` alone, writes nothing, and `read_line` returns `UnexpectedEof` at `n == 0`, which is the doc's stream-ended sentence. DRIVE_HELP and every other help string in this file are unchanged by this round. Earlier reads in this row still hold; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of DRIVE_HELP's `task` and `watch --mail` entries against supervise/mail.rs `task` (the nudge is unconditional over an idle screen; `--no-nudge` refused as unknown) and run.rs's fold. Pinned by task_parses_the_worker_its_flags_and_the_text. No claim contradicted; 2026-09-22 (round 25, item 2 — the watcher is the single supervisor) read of DRIVE_HELP's watch paragraph against supervise/run.rs `escalate_box`/`close_box`: one attention set + one kind=ask per box at the handover, cleared when the box has left; --auto-reads unchanged; supervise escalates nothing. Pinned by a_box_the_loop_does_not_approve_is_escalated_once_and_cleared_when_it_goes. No claim contradicted; 2026-09-23 (fix/claude-at-exec-time, item 2c) read of the drive watch help's box-escalation sentence (own first row cut at 64 cells, one ask per review point and asked again after the worker worked, not waiting to land, `mail=queued id=<n> (…)`) against run.rs escalate_box/approval_line/escalate/post_word — consistent; 2026-09-23 read of DRIVE_HELP's `classify` entry (prelex: comments, here-document bodies, $'…', ${…}, unterminated quotes; program words judged at heads only; path-qualified heads; the anywhere-refusals git -c/--output/--ext-diff/-O, >&file, sort --compress-program, rg --pre, printf -v, less +, date -s, hazard assignments; aterm ctl by verb and form; --allow-python empty by default) against supervise/classify.rs classify_command_with/prelex/danger_scan/head_from/aterm_ctl_read and its tests, and of `supervise`'s --auto-reads sentence (both readings, no vendor-note box) against run.rs auto_read; 2026-09-23 (lane B stage 2, the engine) read of DRIVE_HELP's supervise --auto-reads paragraph (one decider; option 1 `Yes` only; read-only under both readings outside bypass; rm breaker in bypass under /private/tmp/claude-<uid>/, $TMPDIR, /tmp/<x>/, <cwd>/target* with the cwd from meta; Read outside the secrets list; the press guarded by the judged row plus if-seq where `help key` names it; ERR halted parks until status hold=0, ERR busy/rate back off, then a fresh read and decision; the ledger at <state>/drive/<sid>.jsonl), both `--max-s ... 0 is no budget` sentences, the limit mail's fabric=connected condition and the rewritten escalation paragraph (claude <kind>: <subject> (<reason>), questions escalated, one ask per point only over a connected fabric, the box's own row awaited, reconcile/ADOPTED/CLEARED stale at the start, the badges cleared at the end unless the session is gone) against policy::approval::decide/ApprovalCtx::new/default_secrets, run.rs approval_loop/press/escalate/lifecycle, drive_cli::max_budget/supervise_transport and approvals::default_path, and RelayClient::request_bytes' doc against its body. No claim contradicted; 2026-09-24 merge read (harness/integrate: lane B over main at fed20fadf) of DRIVE_HELP's watch escalation paragraph, the one conflict: it is lane B's (the `claude <kind>: <subject> (<why>)` badge, questions escalated too, fabric=connected, the adopt/clear-on-start and clear-on-end sentences) carrying main's four fixes (the subject cut at 64 CELLS, else the box's own first row; posted without waiting for it to land; an ask per review point, the same box back after the worker worked asked again; a queued post journaled `mail=queued id=<n> (…)`), read against supervise/escalate.rs attention_text/kind_and_subject/escalate/escalate_point, run.rs cut_cells/approval_line/post_word and lifecycle.rs reconcile/release_attention; no claim contradicted; 2026-09-24 (harness/integrate, the fence switch) read of DRIVE_HELP's press sentence, now `if-gen=<the read's generation>` (`text --json`'s gen) where `help key` names that fence, even a box redrawn at the same seq after an alternate-screen re-entry, against press.rs press_one_guarded, guard.rs key_args/server_fences_gen, screen.rs parse_text_json's `generation` and aterm-gui control_input.rs's generation check; no claim contradicted; 2026-09-24 (lane B2 stage 2) read of DRIVE_HELP's rewritten limit prose — the continuation's backoff clause, the handled-at-once sentence (a passed reset continued at once raises nothing, journaled `LIMITED seq=<n> handled: …`, the notice again escalated then), the --resume paragraph (a minute past the reset, never on an auto-continue notice, at once when the screen leaves the notice with no busy spell; `keep going` or `keep going (standing rules: <file>)` read when typed, through `turn submit=guarded:<the composer row>`, only at an idle composer with nothing typed; `CONTINUED seq=<n> rule=usage-resume@v1 <text>`; the notice again waits 10 min then 30 from its appearance, never before a later reset, the same text moving no reset; `WAITING` journaled; a model's own limit waited out the same way), the --resume entry and the --journal kind/phase lists — against supervise/policy/turn_end.rs (decide_turn_end's wall_action and resume_at_reset: reset_grace 60 s, limit_backoff 10/30 min, resumes_by_itself → Nothing, ModelBucket with no fallback waited out when resuming; continuation_text), supervise/turn_end_loop.rs (type_guarded's guarded turn, composer_guard, speaks_claude, turn_end_execute's CONTINUED/TYPED lines and WAITING note, wall_reset_at's per-text cache), supervise/run.rs (turn_end_cfg: resume_limits exactly when the loop resumes; open_episode/limit_after's handled and raised; drive_cli's SupervisorConfig::cli), and journal.rs of_line's continued|typed|waiting|skipped|unverified|limited kinds with the rule id as phase; no claim contradicted; 2026-09-24 (lane B2 fix round) read of DRIVE_HELP's --resume continuation sentences against supervise/turn_end_loop.rs: the rules cut at 400 characters (RULES_CAP, capped_rules from rules_text); the fenced write `send if-gen=<g> if=<the composer row> -- <text>` then `key if-gen=… enter` only once the composer reads back as the text (type_fenced, squashed composer_draft), a moved screen writing nothing; the fallback `turn submit=guarded:<the text's end> yield=0.2` (type_guarded, composer_guard's last-word tail, TURN_YIELD) which pastes unfenced; and text written and not submitted escalated (left_typed)",
    ),
    (
        "crates/aterm-agent/src/harness/cli.rs",
        "0365ed1d9afb48e0",
        "2026-09-24",
        "2026-09-24 (harness fix wave, laws review minor): OWN_MARK deleted (no code referenced it; aterm_primer::HARNESS_MARK is the mark the sweep matches); no help text changed ; 2026-09-24 (harness fix wave, laws review): read RETIRED against aterm-gui harness_host (the window's supervisor under [harness], started by lib.rs) and sweep_legacy_hooks (remove_aterm_hooks_in at start-up, not in a headless instance, whatever agents_auto_prime says) and drive_cli's watch; no other prose changed ; 2026-09-24 (lane D2, the second harness stack deleted): the file was REWRITTEN, so this row replaces every earlier read of it (those described verbs that no longer exist). Read by its author against its own handler, the same file: USAGE lists exactly the four kept subcommands (usage, limits, disk, ledger) and `parse` accepts exactly those plus the four retired tombstones and the thirteen names in DELETED, each refused by name with the sentence USAGE's DELETED paragraph gives (checked: `the_deleted_verbs_are_refused_by_name_and_point_at_the_engine` asserts every DELETED verb is named in USAGE and refused). The OPTIONS block names exactly the flags `parse` accepts (--state, --config, --sock, --utc-offset, --apply, --json, -h/--help); the flags only deleted verbs took (--commands, --assume-spine, --nonce, --settings, --tree, --passes, --since, --write) are gone from both, asserted by the same test. `usage`'s paragraph matches `run_usage`/`fold_newest_transcript` (newest mtime only, source=transcript-newest, the shared-directory caveat printed); `limits`' matches `run_limits`/`limits_of` (one `text --json tail=40` read through Session::read_screen, the session defaulting to $ATERM_PARENT_SESSION_ID, exit 1 when the read fails); `disk`'s is unchanged in substance and matches `run_disk`/`disk_config` (report-only without both switches; rows journalled to <state>/disk.jsonl); `ledger`'s matches `ledger_path`/`run_ledger` (approvals::path_under of the aterm state root, the session defaulting the same way; `ledger disk` the journal). The retired-verb paragraph and the module doc's exit contracts match `answer_retired`/`run_retired`/`main_entry` unchanged.; 2026-09-24 (lane D2, second read) the disk report-only line now prints Refusal::NoClass.describe() alone — that text already opens with `report only:`, and the live run on a private headless instance printed it twice; nothing else in the prose moved; 2026-09-24 (lane D2 review fix) read of the disk-journal bound: the module doc's disk bullet, DISK_LEDGER_MAX_BYTES/DISK_LEDGER_KEEP_ROWS and bound_ledger's doc, and USAGE's sentence (cut back to its newest 1024 rows once it passes 1 MiB) against bound_ledger (metadata len over the bound, tail_lines' ring of keep rows, a 0600 sibling .tmp renamed over the journal, a missing or small file left alone) and run_disk (the cut before Journal::open, a failed cut said on stderr and the append going on); the USAGE numbers are pinned to the constants by the_disk_journal_is_cut_back_to_its_newest_rows_past_the_bound; 2026-09-24 (lane D2 review fix) read of USAGE's `limits` paragraph (with no @<sid> and no $ATERM_PARENT_SESSION_ID it refuses, exit 2, rather than read whichever session the socket defaults to) and the refusal text against run_limits_with (Env::target None returns exit 2 before any Ctl call; a named or inherited target is read and a failed read is exit 1 naming it), pinned by limits_with_no_session_to_read_refuses_and_reads_nothing; 2026-09-24 read of the `upgrade` verb this change adds (the header line, the one synopsis line, the `--dry-run` and `--every` option entries, the `upgrade` paragraph, and the `Cmd::Upgrade`/`run_upgrade`/`UPGRADE_MIN_EVERY_S` docs) against `parse` (the `upgrade` arm: an optional `s-` operand, the global `--dry-run`/`--json`, `--every` refused under 10), `run` (the `Cmd::Upgrade` arm) and `harness/upgrade_drive.rs` + `harness/upgrade.rs`: a newer build is `choose_target`'s strictly-newer pick, the native install only for a session whose mapped executable is under it; the notice is typed only on `gate_announce`'s Go (hold/hand, idle status, no busy footer, no approval box, empty composer, 20 s quiet); SIGTERM only after `transcript_has_ready` on the current marker AND `gate_restart` (no shell or caffeinate under the agent) AND a re-read just before the signal, never SIGKILL; the relaunch line sources `hooks::hook_file`'s hook when it exists and carries `rewrite_argv`'s flags plus `--resume <session>`; `--print`/`--worktree`/an unknown flag are `ArgvRefusal`s; re-ask every `REASK_S` = 30 min, `MAX_ASKS` = 4 then `gave-up`; state and `ledger.jsonl` under `<state>/upgrade/`, and `orphans` resumes an Exiting/Relaunched state whose agent is gone. Consistent (carried from main by the final integration: it was read against main's own cli.rs, and the verb, its synopsis, its two option entries and its paragraph are grafted onto this rewritten file word for word); 2026-09-24 final-integration merge read (harness/integrate: lane D2's rewrite over main's `upgrade`): the rewritten USAGE now lists `upgrade` beside the four read views and its header says so; OPTIONS gained `--dry-run` and `--every`, and `parse` has their arms (`--every` refused under UPGRADE_MIN_EVERY_S) and the `upgrade` arm (`s-` operand only), pinned by the_grammar_parses_what_the_help_text_advertises and the_grammar_refuses_what_it_does_not_know; main's second read (the switch in `HarnessConfig::load`) no longer holds and is not carried: the harness `config.toml` and its `config` verb stayed deleted, so the paragraph's switch sentence now names aterm.toml's `[harness] upgrade` beside `[harness] enabled`, read against `upgrade_drive::host_enabled` (both through `toml_switch`: absent ON, a value other than true/false OFF, an aterm.toml that exists but cannot be read OFF) and pinned by the_host_runs_by_default_and_either_switch_stops_it and a_switch_reads_off_on_a_value_it_cannot_read; `config`'s DELETED refusal alone adds where that switch went, pinned in the_deleted_verbs_are_refused_by_name_and_point_at_the_engine; the module doc's new `upgrade` bullet says it restarts the agent and is not a supervisor policy, which is `run_upgrade` calling `upgrade_drive::sweep`; 2026-09-24 (same integration, grep_guard V1): the `upgrade` paragraph names the native install's footer as Claude Code's own `Update installed` notice rather than quoting the anchor text, which V1 refuses outside aterm-phase (anchor `notice.update`) — the claim is unchanged, `upgrade::update_banner` still reads that footer; 2026-09-24 (same integration, the switch read like the window reads it): toml_switch's doc now says a quoted `\"true\"`/`\"false\"` is itself, as the window's [harness] parser (SupervisorConfig::set over the value's text) reads it, and only another value (`\"no\"`, `0`) is OFF; read against toml_switch and pinned by a_switch_reads_off_on_a_value_it_cannot_read's two quoted rows; 2026-09-24 (same integration, tippy's empty_line_after_doc_comments): answer_retired's doc and the retired-verbs section header sat above the grafted upgrade section; both moved back onto answer_retired, no word changed; 2026-09-24 merge read (origin/main dd2581889 into harness/integrate, shown by `gate help-surfaces --diff` from 6793b3b83: 9 hunks) of main's live-upgrade audit round grafted onto this branch's rewritten file, against `parse`, `run_upgrade`, `upgrade_pass`, `stranded`, `upgrade_line`, `harness_switch`, `toml_switch`, `toml_bool`, `misplaced_false`, `false_at`, `salvage_false` and `Env::misplaced_switch` here and harness/upgrade_drive.rs (taken whole from main, with this branch's `enabled` closure in `host`, `host_enabled` over `harness_switch`, and the `busy.interrupt` anchor). USAGE's `upgrade` paragraph: the multiplexer/foreign-pty hold (`terminal_owner`, `owned_by_aterm`, `held_back`), the launcher/no-job-control refusal (`Job::NoJobControl`, `not_a_job`) and the suspended/background wait (`foreground_shell`) are main's sentences over main's code, unchanged; `for 5 minutes` is `STALE_S` = 300 in `expired`. THE SWITCH, reconciled: main's `harness.enabled` + `cap.upgrade.enabled` (the deleted config.toml) is this branch's `[harness] enabled` + `[harness] upgrade` in aterm.toml; `a value other than true or false, or a file TOML cannot read, reads as off` is `toml_switch` (aterm_toml parse Err → false; `as_bool`, else a trimmed `\"true\"`/`\"false\"` string, else false; a non-table `harness` → false); `A HAND-RUN sweep obeys [harness] enabled too, re-read before every sweep` is `upgrade_pass`'s `harness_switch(.., \"enabled\")` at the top of every pass, `step=refused:bypassed` is `UPGRADE_BYPASSED` through `upgrade_line`, exit 1 unless `dry_run`, and `[harness] upgrade` is read by `host_enabled` alone; the stranded-restarts sentence is `stranded(in_flight(opts))`; `A false wins wherever it is ... and upgrade says on stderr where it found it` is `toml_switch`'s `false_at` arm and `Env::misplaced_switch` printed with the refusal (once per turn of the switch, `was_off`); the exit codes are `upgrade_pass`'s `busy:` match (`atpkg::lock::CONTENDED_EXIT` = 75, else 1) and `run_upgrade` returning the one-shot pass's code. The module doc's exit-contract bullet names `upgrade`'s 1 and 75, which is the same code. Pinned by the ported tests a_hand_run_upgrade_stands_down_while_the_master_switch_is_off, a_refused_upgrade_names_the_restarts_it_leaves_in_flight, every_pass_rereads_the_master_switch, a_hand_run_upgrade_that_could_not_sweep_does_not_exit_zero, a_kill_switch_written_below_another_header_still_reads_off and a_switch_reads_off_on_a_value_it_cannot_read; consistent; 2026-09-24 merge read (origin/main 182e44a03, over m3's latency integration 8986a9f18, into harness/integrate): this file is this branch's rewrite, taken whole. main's two changes to it since the merge base — run_hook_payload's one-ring hook ledger (8fc5f28fb, and the 2026-09-23 hook-ledger clause main recorded for it) and run_switch_now's post-park status reuse and presence nonce (c6fa30267, d84b10717) — are in code this branch deleted (the hook/statusline verbs and the watch loop), so neither they nor main's clause describing them are carried; USAGE, OPTIONS and the upgrade paragraph are unchanged by the merge. Consistent",
    ),
    (
        "crates/aterm-cli/src/lib.rs",
        "b18ee74b04a0a7b6",
        "2026-09-24",
        "re-read on 2026-09-18 for the v0.88.0 assembly: the only usage change since the 2026-09-12 sweep is the kitty-commands merge (2a06b13c2) — `list-kitty-commands` in the USAGE list and its module doc — read against the dispatcher (`\"list-kitty-commands\" => list_kitty_commands_report()`), which prints aterm-lexicon's trick rows with no banner exactly as the doc says, and against `diag_report`, which proves the subcommand dispatchable; the rest of the surface carries the reads recorded before this row; 2026-09-22 read of the ONE addition this change makes to this surface — the `Verb::Harness` row (name `harness`, usage `aterm harness <cmd>`, and its five blurb lines) — against its handler: `Verb::from_operand` resolves the name off `Verb::ALL`, so the word `--help` advertises is the word the front door routes and the word `is_tool_candidate` shields; the router arm in crates/aterm/src/main.rs hands the remaining operands to `aterm_agent::harness::cli::main_entry`, whose `parse` accepts exactly the eight subcommands the blurb names (hook, statusline, install, uninstall, status, usage, limits, ledger) and refuses every other word; the blurb's claim that every read verb answers with no hooks installed is the `Source::Spine` path, pinned by `every_read_verb_answers_from_the_spine_with_no_hook_ever_fired`. `argv0_alias` is None and `is_windowing` is false, both correct: no sibling binary of that name was ever shipped and the verb opens no terminal. Nothing else on this surface changed; 2026-09-22 (round 24, item 4) read of the `Link` variant's doc, which no longer lists `mirror` among `aterm link`'s subcommands, against aterm-link `cli::dispatch`. No claim contradicted; 2026-09-22 (round 25, item 0) read of the two reroute pins' docs against atpkg hooks.rs 03513b5d7: every hook body gates agents/ on the union of four markers and never sets the handoff; a login zsh leads agents/ only inside aterm. Pinned by reroute_env_names_match_atpkg and a_login_zsh_demotes_the_seams_front_insert_and_the_rc_hook_leads_agents_only_inside_aterm; 2026-09-23 (Phase 3 pass work) read of the `[machine]` explain-config paragraph and its test against the code it now describes: atpkg's dispatch edge applies the settings only behind `machine_table_changed()` (cli.rs main_entry), `apply_machine_settings` records the table it applied (`machine::record_applied`, `<prefix>/machine.applied`), the window runs `machine apply` after its launch seed (aterm-gui `spawn_pkg_update_check` → `spawn_machine_settings_once`) and every interactive session at launch (aterm main.rs `spawn_detached_machine_apply`) — so `which the window and every interactive terminal session run as they start`, `when the [machine] table changed since they were last applied` and `the next apply disables it again` hold; no banned launch wording; 2026-09-23 p3/reconciled read (shown by `xtask gate help-surfaces --diff`): the explain-config `[machine]` paragraph, reflowed, now says the window applies the settings as it opens and a terminal session once a day (the day's first interactive one) — read against aterm main.rs machine_apply_due (pkg_check::claim on machine-apply.stamp, MACHINE_APPLY_EVERY_SECS) and aterm-gui spawn_machine_settings_once; the test's needles changed with it; no banned launch wording — consistent; 2026-09-23 (Phase 4, one path) re-read: HELP_HEAD and both ENVIRONMENT blocks no longer teach `ATERM_SESSION_MODEL` — it is a development seam read through `aterm_types::dev_seam!` (`session_model_seam`, `session_model_armed_from_env`), so a shipped binary does not read it and `--help` does not offer it; the `ATERM_VERBOSE` line no longer claims the summary names the session model. The reroute handoff docs name the internal `--no-reroute` marker `ATERM_REROUTE_PASSTHROUGH`, which is what `reroute_dir_from_env` reads (pinned equal to atpkg by `reroute_env_names_match_atpkg`).; 2026-09-23 (Phase 4 review fixes) read of the three changed lines, shown by `gate help-surfaces --diff`: the `--no-reroute` marker is spelled `__ATERM_REROUTE_PASSTHROUGH` (the internal spelling, `atpkg::reroute::PASSTHROUGH_ENV`, pinned equal by reroute_env_names_match_atpkg) in `PASSTHROUGH_ENV` and the two docs of `reroute_dir` / `reroute_dir_from_env`, which read exactly that variable. No claim contradicted; 2026-09-23 (lane D M1) read of the decision-B retirement of `aterm harness hook|statusline|install|uninstall`: the Verb::Harness front-door lines name status/usage/limits/ledger and watch/switch model, and no longer offer the hook bridge; the read verbs open no socket, so the lines say they answer with no hook installed and nothing about the screen; 2026-09-24 (lane D2) read of the `harness` verb's four-line blurb and the Verb::Harness doc against aterm_agent::harness::cli::USAGE and parse: the four read views it names (usage | limits | disk | ledger) are exactly the kept subcommands, and the acting verbs it used to name (watch, switch model) are refused by parse as deleted; 2026-09-24 final-integration merge read (lane D2 over main's live upgrade): the Harness verb's front-door lines and its enum doc now name `upgrade` beside the four read views, and say `aterm drive` supervises while `upgrade` only restarts an agent onto a newer build — read against harness::cli's USAGE and `parse` (the `upgrade` arm) and `run_upgrade`; nothing else in the file moved; 2026-09-23 ux/wp6 review read of the one changed line of the [machine] paragraph: the Universal Control revert is now credited to `aterm pkg machine`, which prints machine::doctor_line's ok row with its `revert:` clause (UNIVERSAL_CONTROL_REVERT and the keep-it-off half) — the default `aterm pkg doctor` no longer prints ok rows (doctor::present) — consistent; 2026-09-24 merge read (origin/main dd2581889 into harness/integrate, shown by `gate help-surfaces --diff`: 1 hunk) — the [machine] paragraph's revert line credited to `aterm pkg machine` is main's text, read by main's ux/wp6 clause in this row; this branch's `harness` lines are unchanged; consistent",
    ),
    (
        // Linux rebase read, 2026-09-23: update_verb and the strict saved/live
        // provider implement the documented native transaction, trial health and
        // local baseline. automatic() reads [update] enabled at launch; auto_apply
        // is rechecked before replacement. No retired environment veto remains.
        // Final rebase preserves upstream's separately audited harness-upgrade
        // paragraph unchanged alongside this Linux update section.
        "crates/aterm-cli/src/manual.rs",
        "4c7ac28b8b993d64",
        "2026-09-24",
        "2026-09-24 (harness docs pass): read the harness topic's new tagline, the posts-no-mail sentence (no caller of Session::set_manager in aterm-gui; escalate.rs mail=skipped without a manager), `Codex sessions are not supervised yet` (harness_host.rs module doc), the one-supervisor sentence (claim.rs), SEE IT (status/ls supervisor= program= agent=, harness ledger over approvals::path_under, the journal beside it approvals.rs journal path) and THE [harness] TABLE (every key and default against supervise/config.rs KEYS, SupervisorConfig::default, ApprovalToggles::default, set's refusal of an unknown key, model_fallback empty -> None, TurnEndTiming min_work 2 min and retry_backoff 1/5/15 min, reset_grace 60 s; live reload per harness_host policy generation); the drive page's SUPERVISING preamble, its classify entry (shell-first reading, heads-only program words, the write forms refused anywhere per classify.rs) and supervise entry (the approval policy's four rules, policy/approval.rs decide) and --resume (the continuation `keep going (standing rules: …)`, turn_end_loop rules_text; the probe question is gone) against drive_cli.rs and supervise/; no other prose changed ; 2026-09-24 (harness fix wave, laws review): read the harness page's WHAT IT IS paragraph against aterm-gui harness_host/lib.rs (the host started by default under Config::harness_policy_at_launch — escalate_only after a failed launch load; a headless instance only with headless = true), SupervisorConfig::default (continue on at 6 an hour, model_fallback opus, the four approval rules) and supervise/policy/approval.rs decide (the four approving rules, never a don't-ask-again option), turn_end's model restore at the reset, and escalate's keyed attention; and the hooks paragraph against sweep_legacy_hooks → aterm_primer::remove_aterm_hooks_in (strip_marked_hooks and restore_harness_statusline) run at window start whatever agents_auto_prime says; the manual test the_harness_page_offers_no_hook_bridge updated ; re-read on 2026-09-23 (the provenance lane; merged the same day with main's Phase 5 \
         deletions — the `seed` and legacy-bundle lines — which left this reading's paragraph \
         byte-identical, re-measured over the merged page): `aterm help pkg`'s PROVENANCE gotcha \
         said the lane copies the helper when \"the binary itself is tagged or quarantined\" \
         and records a tagged install only \"when even that lane cannot run\". Read against \
         atpkg::stage_helper::plan_for_exe, which now measures the binary, every `.app` above \
         it in any case, and its `Contents/MacOS/` wrapper (provenance::taint_candidates — \
         a clean binary inside a stamped bundle runs tracked, measured with a launchd job), and against the staging lane's \
         witness path, which keeps and records a build whose lane ran but laid tagged files \
         (the eight `<build>.tracked-install` records the owner's Mac held on 2026-09-22 were \
         that case, by inference from their dates). Merged the same day with main's removal of \
         `ATPKG_REFUSE_TRACKED_INSTALL`: the paragraph keeps this reading of the lane and \
         main's `[packages] tracked_install = \"refuse\"` as the one refusal spelling, read \
         against the merged lay.rs tracked_policy. \
         Earlier: re-read on 2026-09-18 for the v0.88.0 assembly: the kitty-commands merge \
         (2a06b13c2) added the `aterm list-kitty-commands` index line and the `help kitty` \
         page (KITTY_PAGE_HEAD plus the generated vocabulary); read against the topic router \
         (`pet | cat | tricks | kitty-commands | list-kitty-commands => kitty`, so every \
         alias the page names resolves), `kitty_page` (the vocabulary printed IS the \
         compiled TrickLexicon, as the page claims) and aterm-gui's `[sparkle_words] tricks` \
         config (SparkleTricksConfig carries the `enabled` key the page documents); the \
         page's behavioural sentences describe aterm_effects::typed_tricks and \
         PetBrain::note_trick and are the feature author's, not re-derived in this read; the \
         earlier reads this row carried still hold; 2026-09-19 (round 20) read of \
         DRIVE_PAGE's watch limit clause — cleared at the first busy read after the \
         auto-continue notice (`continuing automatically at 1:50pm` its time the reset, \
         `continuing shortly` a minute; the row stays on the screen while the worker \
         resumes), when it answers after a notice naming a reset, and the probe a minute \
         past an auto-continue time — against aterm-agent run.rs limit_on_busy / look / \
         arm_probe_at_reset and limit.rs parse_reset + SHORTLY, the same claims DRIVE_HELP \
         makes at length; no claim contradicted; 2026-09-19 (round 20 review fixes) read of \
         the added clause — the wall again before the worker answers is the same episode, \
         mailed once — against run.rs open_episode/escalate(again); no claim contradicted; \
         2026-09-19 read of DRIVE_PAGE's one changed hunk at the rebase onto main 3d584667d \
         (`xtask gate help-surfaces --diff`, matched at 5a48e1bf7: watch's limit clause, 3 \
         lines out, 11 in) — the attention cleared when the worker WORKS again: at the first \
         busy read after the auto-continue notice (its time the reset, `continuing shortly` \
         a minute, the row still on the screen; the wall again before an answer the same \
         episode, mailed once) and when it answers after a notice naming a reset; the probe \
         a minute past an auto-continue time — against aterm-agent run.rs await_turn_from (a \
         busy read is shown to limit_on_busy when review.unattended(); a soft busy is not \
         busy there, worker_phase's order), limit_on_busy (a resumes_itself episode only; \
         close_episode at the read's seq, Retry kept, rebrief_due), look's `worked || \
         Prompt` close for a notice naming a reset and its retry reset on any non-limited \
         point, open_episode/escalate(again: `meta set attention`, no post, `mail=skipped: \
         the retry hit the wall again, the episode of seq=<m>`), arm_probe_at_reset (SHORTLY \
         grace for resumes_itself, a failed>0 backoff kept), limit.rs parse_reset \
         (`continuing automatically at <time>` a clock time, `shortly`/`continuing shortly` \
         SHORTLY = 60 s) and resumes_by_itself, aterm-phase worker_phase (a hard busy wins \
         before limit_notice) and reset_of (`1:50pm` / `shortly` handed over as printed); no \
         claim contradicted, nothing fixed; the rest of the page is main's 2026-09-18 read; \
         2026-09-18 round-19 read of the IN THE WINDOW block added to FABRIC_PAGE and the \
         `aterm` topic's window sentence against crates/aterm-gui/src/menu.rs FABRIC_MENU / \
         DRIVING_MENU / VIEW_MENU / WINDOW_MENU (the row labels and their order), \
         app_fabric_menu.rs (Inbox… is `inbox --peek --meta`, metadata only; the halt pair \
         is cmd_hold with HoldIssuer::Owner, LOCAL origin, a fleet hold greying both rows \
         with hold_row_reason; Fabric Status… and Turn Fabric On…/Off… run `aterm fabric` / \
         `on` / `off` as a child, on/off behind confirmation(); the toggles write \
         PRESENCE_BAND_KEY = presence.band / PRESENCE_RIM_KEY = presence.rim), \
         menu_chrome_lines (`aterm ctl chrome` lists the tree, submenu lines included), \
         from_invoke_name (every row an `invoke <Name>`) and canonical_invoke_name with \
         every_pre_round_19_invoke_name_still_resolves (every pre-round-19 name still \
         works). No claim contradicted; 2026-09-19 read of the fabric page's IN THE WINDOW \
         block and the aterm topic's Fabric-menu pointer against menu.rs's MENU_MODEL (the \
         Fabric rows in order, File ▸ Driving, Window ▸ Set Role…, View ▸ Presence \
         Band/Rim), native_menu_action_enabled's halt pair and the [presence] band/rim keys; \
         2026-09-20 re-read of the `aterm help permissions` GOTCHAS against \
         control_privacy.rs's folder_state/folder_source and covers_split: the per-folder \
         bullet no longer says the state is `unknown` by construction full stop — it names \
         the TWO things that move a row off it (an access aterm observed, rendered \
         allowed/denied/asking/error, and `covered-by-fda` for a service a held grant is \
         measured to cover), says an observation outranks the coverage rule, and warns that \
         `documents=unknown` under a held grant means unmeasured rather than denied, which \
         is the reading an agent would otherwise get wrong. A second bullet records the \
         session-scoped `data from other apps` grant (\u{a7}1.6a): macOS keys that answer to \
         one process instance and ships no Settings switch for the class, so it returns on \
         every process replacement and retrying is the wrong advice — the text says to tell \
         the operator instead, which is the same division of labour the rest of the page \
         keeps. Both claims are measured (\u{a7}7 S4, 2026-09-19), not documented; \
         2026-09-20 (feat/agent-self-update-intercept merged onto origin/main 55323c26b): \
         the merged page — upstream's macOS consent/privacy and updater-park prose beside \
         this branch's SELF-UPDATE VERBS bullet and Windows caveat — re-read against the pkg \
         GOTCHAS handler and the Security page rows; nothing contradicted; 2026-09-20 (round \
         21) read of the FABRIC page's two receipt sentences against the code that decides \
         them. The RECEIPTS paragraph and the `[fabric]` block's `receipts = true` comment \
         said the value was `the default aterm fabric on writes`, which was true of that \
         command and false of every other way a bridge starts: read against aterm-link \
         fabric.rs receipts_from_config (ON for an absent key, an absent file and an \
         unreadable one), cli.rs's Config default and its serve arm, and enable.rs, which \
         still writes the key explicitly and still leaves an operator's `false` standing. \
         The page now says the default is the same either way and what off costs the SENDER. \
         No claim contradicted; 2026-09-20 (round 21 review) read of the FABRIC page's \
         heartbeat sentence against the code that now makes it false. The page said the \
         bridge reports its link on change and after a 2x move, 'never on a timer, there is \
         no heartbeat': read against aterm-link bridge.rs's ghost sweep, whose `Last` walk \
         goes through `observe` -> `note_ack` -> `ack_wants_report`, so ANY answered broker \
         read is an ack and a periodic read is a periodic `link up` — measured by a reviewer \
         at one per 60 s per attached bridge, for ever. The sweep is now gated on having a \
         candidate, and the page states the bounded exception instead of denying it; the pin \
         at the foot of this file was widened to the new sentence so it cannot silently go \
         back. No claim contradicted; 2026-09-22 read of the ONE addition this change makes \
         — the new `harness` topic page and its index line — against \
         crates/aterm-agent/src/harness/cli.rs, the handler it teaches. Checked claim by \
         claim: the eight KEY USAGE lines are exactly `parse`'s subcommands and flag \
         spellings (`--json`, `--since`, `--settings`, `--state`, `--dry-run`, `--purge`); \
         ‘exit 0 always’ is `run_hook`/`run_statusline`, which return `ExitCode::SUCCESS` on \
         every path including an unparsable payload, and `main_entry`, which answers 0 \
         rather than 2 for a usage error on a hook-shaped argv; ‘one JSON object for a \
         decision, one line for the footer’ is `allow_json` (a single compact object, no \
         newline) and `run_statusline` (exactly one `writeln!`); the key-order/whitespace \
         sentence is the honest statement of `merge_ours` over `aterm_json`'s sorted-key \
         writer, and the backup/restore rule it describes is `Env::settings_backup` plus \
         `run_uninstall`'s equality check, pinned by \
         `install_then_uninstall_leaves_the_settings_file_byte_identical` and \
         `uninstall_keeps_an_owner_edit_rather_than_restoring_over_it`; ‘there is no deny’ \
         is `RmDecision`, which has two variants; `ATERM_NO_HARNESS=1` is the first guard in \
         `bridge_sh`. The `--bare` and adopted-session sentences are the design's MEASURED \
         facts (docs/DESIGN-aterm-wrapper-2026-09-17.md §4.5 and the live-session capture in \
         harness/observe_tests.rs), quoted, not re-measured in this read. No claim \
         contradictedound-22 REVIEW re-read of `aterm help fabric` and `aterm help drive` \
         against the round-22 installer and `drive_cli`: the fabric help said four hooks, a \
         PreToolUse that blocks and a Stop that keeps the turn alive, and described a report \
         read from the transcript — all deleted in d6e9524a0; it now says three hooks, names \
         `--gate-tools`/`--keep-alive` as opt-in, and describes the screen report and its ` \
         busy=1`. `drive task`'s advice said a hooked worker WANTS `--no-nudge`, which \
         against a round-22 default install posts a task nothing wakes for; `drive_cli`'s \
         `no_nudge` defaults to false (it nudges) so the CODE was right and the advice was \
         wrong, and the advice now names `--keep-alive` as the precondition. No claim \
         contradicted; 2026-09-22 (2) re-read after a reviewer found the fabric page \
         CONTRADICTING ITSELF — it said `merges four hooks` in one sentence and `THREE \
         hooks, not four` in the next, because the round-22 correction was appended instead \
         of replacing the claim it corrected. One true sentence now: it merges THREE, and \
         the other two are opt-in. No claim contradicted; 2026-09-22 read, rebased onto \
         round 22: the agents page's hooks paragraph (FIVE default entries with PreToolUse \
         opt-in; the guard allows only when every value the variables take on the line \
         resolves outside the critical classes; every other box is left to the human and \
         escalated by the notification hook; the window's pass installs from an installed \
         aterm only, on macOS and Linux, and leaves a complete block another installed aterm \
         wrote alone) and the fabric page's BEING WOKEN Claude row (FIVE hooks, \
         PermissionRequest answers or leaves alone, Notification escalates, \
         --gate-tools/--keep-alive opt-in), against aterm-link hook.rs \
         claude_settings_for/DEFAULT_EVENTS/permission_request/notification/escalate, \
         permission.rs decide, and aterm-primer HookLane::install(force)/installed_exe; ; \
         2026-09-22 (the safety-and-honesty audit fixes)  re-read of the `harness` topic — \
         its tagline, its `source` sentence and its KEY  USAGE block — against \
         `harness::cli::parse`, `harness::cli::RINGS`,  `harness::cli::Readout::new` and \
         `harness::source::Source::as_str`. THREE  CLAIMS WERE WRONG AND ARE FIXED. (1) The \
         page said each read verb's `source`  field reads `spine`, `hook` or `statusline`; \
         `Source::as_str` has no `spine` —  commit 7d0348bfe collapsed it into `grid`, \
         `Source::parse` of the word `spine`  is an explicit negative control in \
         `harness/source.rs` and in  `harness/cli_tests.rs`, and `Readout::new`'s ladder is \
         statusline then hook  then grid. The page now names those three and says in one \
         clause that `spine`  was retired, so a reader of an older page is not left \
         guessing. (2) The tagline  and the KEY USAGE block described the hook bridge and \
         its read verbs and listed  8 of the 21 subcommands `parse` accepts, omitting EVERY \
         acting verb; KEY USAGE  is now two blocks, and the acting block's five entries \
         (`switch`, `watch`,  `recover`, `nudge`, `disk --apply`) were each checked against \
         their `Cmd` arm  and run function — `run_switch_now` and `run_watch` open the \
         control socket,  `run_recover`/`run_nudge` print and withhold an unwitnessed L3 \
         act's commands,  `run_disk` acts only with a named class AND `disk.apply`. (3) The \
         `ledger` line  named three rings where `RINGS` has six; it now lists all six and \
         says that  `recovery` and `actuation` are where `Watcher::pump` writes, which is  \
         `watch::RING_RECOVERY`/`RING_ACTUATION` and the `journal`/`verdict_row` calls  in \
         `pump` and `execute`. The page's central-law paragraph and its GOTCHAS were  \
         re-read unchanged and still hold. Earlier reads in this row still hold; 2026-09-22 \
         (round-23 review) read of the FABRIC_PAGE's new BROADCAST block against \
         `fabric::cmd_topic`, `bridge::resolve_to`/`on_say_record` and \
         `subscribe::parse_mail_params`: the three verbs, the empty-by-default set (the \
         sender included), `since=head|@<off>`, the topic grammar, the demotion, \
         `mail:topic=`, `dropped=` on the recipient, the 2 s roster latency and the `fabric \
         tail --filter`/TOPICS pointers each match the handler; `topic`/`say`/`broadcast` \
         are aliases of this page in `render` (pinned by \
         fabric_topic_is_reachable_and_honest_about_wake_paths). No claim contradicted; \
         2026-09-22 merge read (round 23 onto main): upstream's five-hook contract \
         (SessionStart, UserPromptSubmit, PermissionRequest, Notification, Stop; PreToolUse \
         and keep-alive still opt-in) read against aterm-link hook.rs and the agents pass, \
         with round 23's topic/say help — consistent; 2026-09-22 read of the one line `aterm help rust` gained since the last read - the sentence telling a reader to use the tools' own names in replies, tippy rather than clippy - against atpkg reroute.rs's reroute table and the primer's Rust note, which now say the same thing. The rest of this surface is this row's earlier reads.; 2026-09-22 (round 24, item 4) read of the `fabric` tagline, the `agents` topic's skill list and Codex sentence, the introspection page's MAIL paragraph and FABRIC_PAGE's BEING WOKEN rows against aterm-primer (`CODEX_ADDENDUM`, `skills_for`) and aterm-link `cli::dispatch`: THE FILE MIRROR section is gone with the code, and the Codex row states the owner's principle (the socket is refused; driven from outside; no part in messaging; nothing to configure). Pinned by fabric_topic_is_reachable_and_honest_about_wake_paths; 2026-09-23 read of the vendor-direct rewrite (design 2026-09-22 §1.7): the pkg page's `update`, `install` NOTE, `which` and `rollback` lines, the landing example lines, the new VENDOR PROGRAMS bullet and the SELF-UPDATE bullet, read against atpkg cli.rs cmd_update_one → cmd_update_vendor, install_inner's vendor fork, managed_state_of/vendor_state_of, cmd_rollback → cmd_rollback_vendor (vendor_direct::lane::roll_back_by_hand refuses a yanked build, no index resolve), landing.rs build_words (a vendor build is its version), selfupdate.rs announce/declined lines and the vendor lane's verdict lines; 2026-09-23 re-read after review: the VENDOR PROGRAMS cadence sentence now says what this build does — every update pass checks both vendors (the window's launch and 6h loop, cmd_update_all_code → run_update_pass → vendor_pass) and `claude update`/`codex update` check now (selfupdate → cmd_update_vendor); design §1.6's 5-minute head watch is not on this build and the page no longer claims it — and the SELF-UPDATE bullet's announce/decline sentences (aterm updates the program from its vendor) read against selfupdate.rs announce_line/declined_line; 2026-09-22 (round-24 review fix, commit C) read of `aterm help fabric`'s status line and table against fabric.rs (five `fabric=` states; the `stale` row added, worded as the fabric skill's) and of `aterm help rust`'s stock-cargo sentence against reroute.rs (announced, then refused with exit 2 under ATERM_REROUTE_STRICT=1). No claim contradicted; 2026-09-23 read of the VENDOR PROGRAMS cadence sentence restored with the head watch: the window checks both vendors about every 5 minutes and shortly after a wake — read against atpkg vendor_direct::watch (CADENCE 5 min on the wall clock, AFTER_WAKE 20 s, conditional GETs whose 304 is a zero-byte answer) and aterm-gui's package-loop park that runs it; every update pass still checks both vendors (run_update_pass → vendor_pass) and `claude update`/`codex update` check now (selfupdate → cmd_update_vendor). The merge also carries upstream's fabric=stale and reroute-strict lines, read by their own rows' note above; 2026-09-23 read of the VENDOR PROGRAMS bullet's new clause — a row names why the latest is not known when the vendor's release channel was not reached for a day — against atpkg cli.rs record_vendor_status (an Unreachable verdict past ProgramStamp::checked_recently, CHECK_VOUCHES_FOR_SECS = 24 h, writes `managed <v> — <Vendor>'s release channel not reached since <date>`) and managed_current_entries (leaves such a program out); and of no_page_carries_the_retired_vendor_wording's doc, now naming the window's 5-minute head watch and the update pass as the cadence, against the assertions under it (`about every 5 minutes`).; 2026-09-23 read of the VENDOR PROGRAMS bullet's closing sentences, reflowed to the page's width (a 145-character line; no word changed, shown by `gate help-surfaces --diff`): `The ALab index can only yank a version, never supply one` against atpkg vendor_direct::policy (the index lends a vendor program its yanks, nothing else) and `DISABLE_AUTOUPDATER=1` against vendor_direct::table's claude shim_env.; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of `help agents` (the pass writes no hooks and takes an earlier aterm's entries out once, with a .bak-<unix>), `help drive` (task: the typed nudge is the verb; watch --mail: a report the worker posts itself) and `help fabric` (the one sentence where BEING WOKEN was) against aterm-primer `remove_aterm_hooks`/`auto_prime`, aterm-agent `supervise::mail::task` and the fabric skill. Pinned by fabric_topic_is_reachable_and_honest_about_wake_paths. No claim contradicted; 2026-09-22 (round 25, item 2 — the watcher is the single supervisor) read of `help drive`'s watch paragraph against the same code. No claim contradicted; 2026-09-23 merge read: the page is the union of main's decision-B edits (the agents/hooks paragraphs and the `aterm agents` rows, read by main's own clauses above) and this branch's vendor-direct paragraphs, which are unchanged since their read above — no sentence of one contradicts the other; 2026-09-22 latency read: index_probe checks the four next public signed-index tags on bounded one- and five-minute cadences, with a first-page listing as a five-minute large-skip hint; package_park_tick_wakes only wakes the normal signed pass and preserves failure backoff. Checked against index_probe and the package worker; 2026-09-22 merge read: harness help's source list, account-switch limits and operation order checked against the handlers. No claim contradicted; 2026-09-23 vendor latency read: the pkg page now says the vendor head check is about every minute and shared across processes, matched to vendor_direct::watch CADENCE and the store-scoped watch lock/stamp. The GUI package park remains off the render thread; `claude update` and `codex update` still take the direct vendor pass. No claim contradicted; 2026-09-22 (Phase 2, silent by default) read of the pkg page's four changed passages (shown by `xtask gate help-surfaces --diff`) against the code under each: SAME TAB's no-wait sentence against atpkg cli::cmd_landing (the current bin/<program> exec'd at once, nothing printed; the usage, tool-name and could-not-run lines only on misuse), landing::sweep called from cli::mutator_store_lock once the lock is held (every mutating verb), platform::twin_prelude (the self-update block alone; no marker test) and activate::reconcile_agents's byte compare that re-lays an older twin once; the Windows sentence against cmd_selfupdate_prelude (empty on every input), twin_prelude's Windows arm (empty, so cmd_shim_content_twin == cmd_shim_content_env) and CMD_FRAME_HEAD/CMD_FORWARD_TAIL; the self-update bullet's `an install` for the pass that holds the lock; AUTOMATIC updates' never-checked sentence against doctor::run_with section (8) (the two warn lines naming the ALab index's programs and `claude and codex update from their vendors without it`), the deleted warn_if_never_checked and its four callers, and crates/aterm/src/main.rs's session lane (no never-checked line, no staged nudge, spawn failures to aterm_log); PROVENANCE's parenthetical against lay::allowed_note (aterm_update_core::log_pkg_note, never stderr) and main.rs's reroute-lay thread (lay_executables from a session), the staging lane's own note in install.rs being a pass's and unchanged. No claim contradicted; 2026-09-22 (Phase 2 review fixes) read of the two passages changed since (`gate help-surfaces --diff`): AUTOMATIC updates' never-checked sentence against doctor::vendor_update_clause (VENDORS minus `[packages] exclude`, each kept by vendor_direct::watch::follows_vendor — installed, not removed, pinned or dev-linked — and only while Probes::automatic: manager_enabled, `enabled` and `auto_update`; no clause when none is left); PROVENANCE's parenthetical against lay::allowed_note → notice::say (stderr in atpkg's own CLI — a typed verb, or a window pass whose stderr the window keeps in aterm.log — and aterm_update_core::log_pkg_note once a host has called notice::to_host_log: the session lane in crates/aterm main.rs and aterm_gui::main_entry, each with the aterm.log file logger installed). This read supersedes the previous clause's `never stderr` for allowed_note. No claim contradicted; 2026-09-23 (p2/gui review) read of the two reworded sentences against their handlers: the network-install line's `(on a first run the toolchain bar shows the sizes; later passes stay off the glass)` against aterm-gui `pass_is_first_run` and `StatusBars::toolchain_announced` (a routine announcement raises no row); the machine-settings line (recorded in `aterm ctl appstatus` and the log, named on Settings ▸ Security as the Last change) against `Wake::PkgMachineSettings` → `StatusBars::toolchain_machine_settings` (ledger entries, no row) and `PackagesService::note_machine_change` (the card's Last change); 2026-09-23 Phase-2 merge read: the union of p2/shell's re-read (no landing wait, no never-checked or staged-update line in a shell; a host's unasked notices go to aterm.log; doctor's truthful vendor clause) and p2/gui's (routine package rows go to the ledger and log; failures light the Settings Packages badge; appnotice says recorded for a marker) — disjoint behaviours, and no sentence of one contradicts the other; 2026-09-23 (Phase 3 pass work) read of the two paragraphs this change rewrote against their handlers: MACHINE SETTINGS (the edge gate `machine_table_changed`, the window's post-seed and each session's `machine apply`, the apply's own prints and refusals unchanged) and the update-pass paragraph's additions — write-only-when-bytes-differ (`reroute::lay`, `stub::pending_stub_executable_at`, `hooks::write_hooks`, the vendor stamp), the roll-forward (`cli::roll_forward_interrupted_activations` at the seed/update/install edge), the single durable write (`status::hold`, `write_durably`), exit 69 (`aterm_update_core::pkg_check::PASS_OFFLINE_EXIT` from `run_update_pass`/`update_pass_stopped`, classified `Offline` by the window) and the `status.toml.corrupt-<unix>` heal (`status::seed_for_rewrite`) — consistent; 2026-09-23 read of the Phase 3 review fixes: the `noindex apply` and `machine apply` rows now name what applies the [machine] settings — `aterm pkg machine apply` as the window opens (`spawn_machine_settings_once`, now beside the launch seed) and as every interactive session starts (`spawn_detached_machine_apply`), a pass only after an edit or an unfinished apply (`machine_table_changed`, `machine::forget_applied`), queued on `machine.lock` (`machine_apply_queue`); the GOTCHAS paragraph's roll-forward gates (pinned, yanked, grouped — `roll_forward_interrupted_activations`), the write-through record (`status::write`, `only_stamps_differ`), the offline definition (no host answered — `Fetcher::index_link_down`; a refused listing exits 1; every lost member lost at a fetch) and the corrupt-record heal in one rename (`status::heal`) — consistent; 2026-09-23 p3/reconciled merge read (p3/pass onto main): the pkg page is the union of main's AUTOMATIC-updates sentences (the next-index probe's one- and five-minute HEADs, the shared listing, the six-hour full check) and p3/pass's MACHINE SETTINGS, noindex/machine-apply rows and the update-pass GOTCHAS additions (write only what differs, roll-forward, durable status.toml, offline exit 69, corrupt-record heal), each read above against its handler; the two sets touch disjoint sentences and no sentence of one contradicts the other (the window's Offline verdict is a failure-ladder retry in main's loop, which is the `retries it quietly` the GOTCHAS line promises); 2026-09-23 p3/reconciled read (p3/sched's schedule onto main's probe, shown by `xtask gate help-surfaces --diff`): the AUTOMATIC-updates paragraph against aterm-gui spawn_pkg_update_check — the six-hour walk is `pkg_check::FULL_PASS_INTERVAL_SECS` with no knob (update_interval_secs and the once-pass mode deleted), machine-wide through `scheduled_pass_gate` over `pass_stamps` (full_pass_due_in on the record with the lane's own success folded in; a sibling's pass in flight via `atpkg::progress::pass_running` or attempted within PASS_SPACING_SECS holds it; an index the verified floor covers is not re-run), wall-clock parks with `park_deadline_after_slice` moving a slept-through deadline to watch::AFTER_WAKE (20 s), `index_probe::classify` admitting only a 302/307 to RELEASE_ASSET_HOSTS, `BumpWatch::unlanded` plus the failure ladder for an index a pass did not land, Backoff::FAILURE 10 min doubling to 2 h, `PassEnding::Settled` for an exit 2 that reached the index; the session sentence against aterm main.rs session_pass_due (full_pass_owed with in_flight, then pkg_check::claim on session-pass.stamp) and session_pass_args (pass_flags: --wait-lock, --progress-file); the `noindex apply` / `machine apply` rows and the MACHINE SETTINGS GOTCHA against main.rs machine_apply_due (the daily claim on machine-apply.stamp) and the window's unchanged spawn_machine_settings_once — consistent; 2026-09-23 Phase 4 p4/settings read: the AUTOMATIC-updates bullet's new sentences (`[packages] enabled` is Settings ▸ Packages' \"Automatic updates\", read LIVE by the window: aterm-gui `LiveSwitch` polled before every pass and at each 5 s `PARK_SLICE`, `stand_by` inside `PkgLane::park` with the deadline kept; Update Now is the Settings worker's own pass and ignores the switch) and the new PACKAGE LOG bullet read against `atpkg::packages_log` (`aterm_types::dirs::logs_dir`, one `O_APPEND` write per line, `sanitize`'s URL/token/control/bidi stripping, `ROTATE_AT_BYTES` 1 MiB and `KEEP_ROTATED` 5 only under `lock::held_by_this_process`, `ACTIVITY_EVENTS` 50) and `cli::pass_lane`'s lanes (head watch, session, agent update, window, typed; `PassRecord::refused` for a verb the lock turned away) — consistent; 2026-09-23 (Phase 4, one path) re-read of every sentence that taught an environment knob, against the code that replaced it: the Rust note and `aterm help rust`'s FLAGS line name `[reroute] announce = false` (atpkg `reroute::run` -> `config::cached_reroute().announce()`) and `aterm --no-reroute` (the front door's `take_no_reroute` -> the internal `ATERM_REROUTE_PASSTHROUGH` marker); the reroute topic's ESCAPES AND CONTROLS table drops the four reroute variables (the ORACLE refusal now names the upstream path, `oracle_message`); the harness bullet names `harness.enabled`; the atpkg page names `[packages] enabled` for the session lane (`packages.enabled()` in the front door), `[packages] tracked_install = \"refuse\"` as the one refusal (`lay::tracked_policy_of`), `auto_install` as the one install consent (`PackagesConfig::auto_install`, `cli::should_complete_set`, `set_completion_consent`) and the retired/development keys (`config::admit_packages`); the update page adds the `[update] enabled` reason (`aterm_update_core::settings`) and the token-lane rung ids `keychain`/`file`/`gh-cli` (`token::RUNGS`). Pinned by `atpkg_topic_states_the_compiled_anchor_and_the_window_scoped_update_loop` and `rust_topic_measures_and_leads_with_the_trust_default`, which now also refuse every retired name.; 2026-09-23 (Phase 4, one path, second read) of the three further edits: the CONFIG page's starter-file count is 155 keys (`aterm-gui` `cli::tests::starter_config_key_count_matches_the_manual` counts the template: the retired `auto_update`, `seed_install`, `account`, `channel`, `include` and the owner/repo link example left, `[reroute] announce` arrived), THE KEY ROSTER says `[update] enabled` is on Settings ▸ Terminal ▸ Updates (`prefs::section_of` routes every `update.` leaf to Terminal, group \"Updates\") and `auto_apply` on Software Update (`update_automatic_card`), and the update page's off-switch bullet says the switch applies next launch (`settings::update_enabled` is read once per process).; 2026-09-23 (Phase 4 review fixes) read of the two changed passages (`gate help-surfaces --diff`): the `aterm` topic's WHAT IT IS no longer teaches `ATERM_SESSION_MODEL=1` — a development seam a shipped binary never reads (`aterm_cli::session_model_seam` through `aterm_types::dev_seam!`) — and says only that the in-process VT model is off and not readable from outside; the update page's third reason now says `[update] enabled = false` stops the background checks only (`aterm_update::automatic` gates `spawn_background_check_with_source`), that `aterm update status` says so (`print_update_status` prints `automatic checks: off`), and that `aterm update check` / Check for Updates still check and a staged build still applies (`check_now` and the launch apply read `aterm_update::enabled`, the platform alone). No claim contradicted; 2026-09-23 merge read (p4/integrate: p4/settings merged with p4/onepath): the merged ATERM PACKAGES page read as one — settings' GOTCHAS sentence (`[packages] enabled` is Settings ▸ Packages' \"Automatic updates\" switch, read LIVE by the window, Update Now either way) and its PACKAGE LOG bullet beside onepath's ONE PATH bullet and no-kill-switch sentences, against the merged lane: `LiveSwitch` in aterm-gui `spawn_pkg_update_check` gates on atpkg's own `PackagesConfig::enabled` (the retired `auto_update` folded in; `LoopGate` has two states, On and Off), so the page's `enabled` sentence and its `the retired auto_update still reads as enabled` sentence describe the one reader — consistent. No claim contradicted; 2026-09-23 read after review of the merge (`gate help-surfaces --diff`): the ONE PATH bullet no longer says the retired keys read as their new ones `until renamed` — the blanket rename onepath removed from doctor and the config editor. It now says `auto_update = false` keeps updates off whatever `enabled` says and `seed_install` reads as `auto_install` while that is unset (atpkg `PackagesConfig::enabled`/`auto_install`), that a retired key is renamed only when its new key is not written — beside it a rename writes the key twice, a file that no longer parses, which atpkg reads as automatic updates ON — else removed, with `enabled = false` said when `auto_update = false` is what keeps updates off (`config::auto_update_note`, the sentence doctor, the config editor and Settings ▸ Packages show), and that turning Automatic updates on removes it (aterm-gui `prefs::apply_prefs_edits`, `RETIRED_PACKAGES_SPELLINGS`). Pinned by pkg_page_never_advises_a_rename_that_writes_enabled_twice. No claim contradicted; 2026-09-23 read of the SELF-UPDATE VERBS bullet's added clause — the announce line names the version you have (`you have 2.1.278`; `build N` for an index build) and does not echo the typed words — against `atpkg::selfupdate::announce_line` (pinned byte for byte by `every_line_is_exact`; the words come from `vendor_direct::build_words` of the active build) and `cli::selfupdate_check`, which reads the active build before the child; the words `no_environment_knob_reaches_the_intercept` requires are all still there; no claim contradicted; 2026-09-23 Phase-4 merge read with main's 8ea92e191: the SELF-UPDATE bullet's announce line now names the version you have (read against cli.rs selfupdate_check: ops::active_builds then vendor_direct::have_words into selfupdate::announce_line, the typed words no longer echoed) — consistent with Phase 4's one-switch and one-path text on the same page, which is unchanged by it; 2026-09-23 (Phase 5, the sealed-seed client lane deleted) read of the `aterm pkg seed` entry against atpkg's cmd_seed as it now stands: it records adoption, lays a pending stub per default-set name, consults the signed index (report_seedless_posture) and installs nothing itself, the update pass behind it installing; `[packages].auto_install=false` makes it print one line and return before adoption, stubs, reroutes or the seam — the pre-v0.63 seeded-bundle sentence is gone with the lane; no claim contradicted; 2026-09-23 (fix/claude-at-exec-time, re-read at its merge of origin/main 2c256a83e) read of the four hunks `xtask gate help-surfaces --diff` shows on top of main's Phase-4 text — the pkg page's SAME TAB bullet (the `aterm --no-reroute` sentence, now naming that its marker is not read by the claude/codex stubs; the WHICH COPY RUNS IS DECIDED WHEN IT RUNS paragraph with its `rehash`/`hash -r` step; the narrowed SHADOWED sentence) and the reroute page's THE AGENT PROGRAMS section (three markers since main removed ATPKG_AGENTS_EVERYWHERE; the --no-reroute marker), its ESCAPES AND CONTROLS `--no-reroute` row and its WHERE THE STUBS LIVE sentence — against atpkg reroute.rs agents_stub_body_sh (markers only, never PASSTHROUGH_ENV; the pass-through past every reroute dir, agents/ and the stub's own links, reaching bin/ as before; the 127 line), agents_routes/lay, hooks.rs AGENTS_MARKERS (the three), cli.rs agent_stub_on_path/shadow_in_shell/agent_routed_row/agent_passed_row/which_line_in, doctor.rs (3b)/(10d), state.rs agent_routed_in_shell/agent_passed_through, aterm-cli lib.rs reroute_dir_from_values and aterm main.rs (a --no-reroute launch lays and hands no reroute dir), and the zsh/bash command cache the 2026-09-23 review measured — consistent; 2026-09-23 (fix/claude-at-exec-time, item 2c) read of DRIVE_PAGE's watch box sentence (own first row ≤64 cells, one kind=ask per review point, asked again after the worker worked; supervise escalates nothing) against aterm-agent run.rs escalate_box/approval_line and the review.unattended() guard — consistent; 2026-09-23 Phase-5 merge read with main through d8d9c7bad: the union of main's exec-time agent routing text (4516e581a: the claude/codex reroute stubs, THE AGENT PROGRAMS, the rehash/hash -r step — read by main's own clauses above) and Phase 5's removal of the sealed-seed wording (`atpkg seed` adopts, lays stubs and reports; nothing installs from a bundle) — disjoint paragraphs, no sentence of one contradicts the other; 2026-09-23 read of the prose 2aa8e689d moved (shown by `xtask gate help-surfaces --diff`, matched at 2c256a83e: 1 hunk, 4 lines removed / 7 added) — the introspection page's headless paragraph, now `A headless instance never takes the keyboard: on macOS it is a background process with no Dock tile that cannot become the active app, so a harness may boot one while you type elsewhere`, and the unchanged ATERM_AI_HINT sentence reflowed around it — read against aterm-gui lib.rs's `launch_posture` (:6568: `headless` → `policy: Some(AppActivation::Prohibited)`, `activate_on_launch: false`), `apply_launch_posture` (:6585, mapping Prohibited to winit's `ActivationPolicy::Prohibited` and passing `with_activate_ignoring_other_apps(false)`), its application to the app's own event-loop builder (:34082 `apply_launch_posture(&mut builder, launch_posture(headless))`), the AppActivation::Prohibited doc (:6475: NSApplicationActivationPolicyProhibited, no Dock tile) and crates/aterm/tests/headless_activation.rs, which boots a real headless instance and reads NSRunningApplication.activationPolicy and NSWorkspace.frontmostApplication; the ATERM_AI_HINT sentence is byte-identical apart from the reflow; no claim contradicted; 2026-09-24 read of the harness page's new `upgrade` synopsis line and paragraph against crates/aterm-agent/src/harness/cli.rs (`parse`'s `upgrade` arm, `run_upgrade`) and harness/upgrade_drive.rs: notice first, READY answer and every shell under the agent waited for (never killed), SIGTERM, the atpkg hook sourced before the relaunch (when the hook file exists), the same flags plus `--resume <session>`, the continuation line, `--dry-run` printing each session's next step, state and ledger under `<state>/upgrade/`. Consistent; 2026-09-24 (2) read of the added sentence (the window sweeps every minute on its own tabs by default; `aterm harness config set cap.upgrade.enabled false` turns it off) against `upgrade_drive::host`/`host_enabled`, the `cap.upgrade.enabled` key in harness/config.rs's KEYS, and aterm-gui's `spawn_agent_live_upgrade` (skipped headless). Consistent; 2026-09-24 (3) the `upgrade` paragraph's first line re-read after rewording it to name Claude Code's `Update installed` banner without quoting its restart phrase (B12 / `no_update_lane_source_prompts_a_restart`: aterm never prompts a restart, and the quote read as one); the claim is unchanged — `upgrade` restarts the agent in place and resumes the conversation. Consistent; 2026-09-24 read of the AUTOMATIC-updates paragraph's added sentences and the session sentence's new clause (fin/model: the full-pass rule's derived model and the rate-limit hold; `gate help-surfaces --diff`: 2 hunks, the rest of the paragraph reflowed to the page width with no word changed) against their handlers: a pass whose GitHub API listing was refused as rate-limited records the reset GitHub named — atpkg net.rs `GithubFetcher::note_metered_hold` over the full pass's header dump (`resolve_pass_fetcher`, `Layout::listing_headers`) and aterm-update-core `RateLimitHeaders::resume_at` (`retry-after`, else `x-ratelimit-reset` with `x-ratelimit-remaining: 0`), recorded by cli.rs `run_recorded_update_pass` → status.rs `stamp_pass_end` as `metered_hold_until`; `at most an hour ahead` against the parser's clamp and `pkg_check::METERED_HOLD_MAX_SECS` (`Stamps::metered_hold_in` ignores a later one); `no aterm schedules a full check before it` against aterm-gui `scheduled_pass_gate` (every PassWhy but Bumped parks for the hold), the loop's failure park (the hold instead of a ladder rung) and `pkg_check::full_pass_due_in` (the session lane's rule); `a pending program's own request, and one you run, still go` against the gate's Bumped arm, the Settings worker's own pass and a typed `aterm pkg update` (neither goes through the gate); `the index probes and the claude/codex head check keep their own cadence` against `PkgLane::park` (the probe off only in a failing lane's backoff, as before; the head watch on every park) and index_probe's own cooldowns; `Each full check records how it ended in status.toml, and that is what these rules read` against `stamp_pass_end` (`last_pass`/`last_pass_at`) and `pkg_check::Stamps::parse`/`last_failed` (no rule reads `updated_at`); the session clause `or before a rate-limit reset a pass recorded` against aterm main.rs `session_pass_due` → `full_pass_owed(&atpkg::status::pass_stamps(..))`. No claim contradicted; 2026-09-24 re-read after review of fin/model (`gate help-surfaces --diff`: one hunk, the pkg gotcha paragraph): `the index probes keep their own cadence (a window parked for the reset parks them too), and so does the claude/codex head check` against aterm-gui spawn_pkg_update_check's gate park (the probe off while `metered_hold_in` > 0), the failure park after the window's own rate-limited pass (probe off while failing) and `PkgLane::park` (the head watch on every park); `one with nothing to check too` against atpkg cmd_update_all_code's EMPTY_UPDATE arm, which now stamps `stamp_pass_end(Ok, Kept)`, and `pkg_check::Stamps::parse` (an ok pass end with no success is the walk's last success); the launch sentence (`waits for it and never reports the wait as a failure; when that install was a full check it is not repeated back to back — the waiting pass takes its ending (up to date, offline, or its failure, retried on the window's ladder) — and otherwise the waiting pass then runs`) against `pass_ended_since_contended` (a full pass's end strictly after the first contended second), `stood_down_after_wait` (0 / 69 / 1 with the holder's sentence on stderr) and the window's `pass_ending`/failure ladder for those exits; the session clause, now `never while a pass is installing, within five minutes of one, or within 6h of one that failed` with the rate-limit clause dropped, against `full_pass_due_in` (the hold term removed: a hold is recorded only with a pass end, which holds that rule an interval — the model `AtpkgFullPassRule` proves the session lane never spawns inside one). No claim contradicted; 2026-09-24 merge-read (fin/integrate, while merging fin/gui): the fin/model merge 84cd5b82f kept both sides' reads in this row but recorded main's hash (53efeb67c3d58c45, d0a4c2930's text), so the gate refused it. `gate help-surfaces --diff` against that read shows ONE hunk — fin/model's rewrite of the pkg page's AUTOMATIC-updates paragraph, read above against its handlers — and nothing else: main's harness `upgrade` synopsis and paragraph and the update page's LINUX section are byte-identical to main's read. Read together, the merged page does not contradict itself: the pkg paragraph's `the app's OWN self-update check runs from the window and from every terminal session` agrees with the update page's `Checks run in windows and interactive sessions`, and the LINUX section's `Set [update] enabled = false to disable automatic checks from the next launch; explicit `check` and `apply` remain available` holds against aterm-update: `linux::spawn_background_check_with_settings` starts its checker only under `aterm_update::automatic()` (the once-per-process `update_core::settings::update_enabled`), while `aterm update check` goes through `check_now_with_settings` -> `linux::check_with_settings(.., force = true)` and `apply` through `linux::apply`, neither of which reads the key. No claim contradicted; 2026-09-24 (audit1) read of the `aterm pkg run` row and the three SELF-UPDATE VERBS / Windows gotcha edits against atpkg's `cmd_run` (exports `ops::exec_env`, the build's declared env; `runs_self_update_verb` routes a first-word `update|upgrade|install` to the `selfupdate_plan` settled by `settle_selfupdate`, help run as the store binary with that env). Consistent; 2026-09-24 merge-read (fin/integrate over origin/main 9533aed18; both sides' reads kept above): the two sides recorded their own pages (fin/integrate 9b63e8729edc76d2, main 009c245023b03137). `gate help-surfaces --diff` against fin/integrate's read shows only main's four audit1 hunks (the `aterm pkg run` row, the Windows gotcha's `aterm claude update` sentence, the SELF-UPDATE VERBS `or after aterm` clause and the TARGET line's `aterm claude update` does), read in main's (audit1) note; against main's read it shows only fin/model's AUTOMATIC-updates paragraph, read in this row's fin/model notes — disjoint prose. Read together the merged page does not contradict itself: `aterm claude update` goes atpkg cli.rs `cmd_run` -> `runs_self_update_verb` -> `selfupdate_plan` -> `settle_selfupdate` -> `selfupdate_check`, whose child is `update <program> --wait-lock N` (selfupdate `child_args`, `cmd_update_one`) — a program's own typed request, never a full check — so fin/model's `a pending program's own request, and one you run, still go` and `a later write by the head check or a typed verb is no check` hold for it: `stamp_pass_end` is called only from `cmd_update_all_code` (its EMPTY_UPDATE arm) and `run_recorded_update_pass`, which only `cmd_update_all_code` calls. No claim contradicted; 2026-09-24 read (origin/main merged into ux/status-reporting, shown by `gate help-surfaces --diff`): the harness page's `upgrade` synopsis line and paragraph are main's, read by main's clauses recorded below. `aterm help update` is the merge's, read against crates/aterm/src/main.rs update_verb/update_summary: the branch's plain lines kept; `check` now says it exits nonzero when the copy cannot update or its checks fail (update_verb returns FAILURE for a check on !enabled, !installable, channel_unreadable or failing_checks > 0); `identity` (binary_identity_json of running_binary_identity) and `--help` (the concise usage, exit 0) are listed because update_verb now answers them, and the `--help is a usage error` sentence is gone because it no longer is; main's LINUX section is carried verbatim — its enable/apply/rollback/install and identity arms read against update_verb's arms, `every 30 minutes` against linux.rs's `next_check_unix = started + 1800`, the trial and rollback-floor sentences main's and not re-derived here (the module is Linux-only and does not compile on this host); HOW aterm UPDATES opens `On a Mac` (the 10-minute cadence is cadence::WEB_INTERVAL_SECS, the macOS lane); main's platform paragraph and `WHEN A MACOS COPY` heading replace the branch's `macOS ONLY` paragraph, whose quoted `Nothing to report` sentence update_verb no longer prints (it says `this copy of aterm can't update itself here`); no claim contradicted; re-read on 2026-09-23 (the review of the plain status wording): `aterm help update`'s staged line and HOW aterm UPDATES now say an update installs within a minute \"while an aterm window is open\" — read against crates/aterm/src/main.rs update_summary and the window-only apply lane (the session lane checks and stages, never applies) — and `check` says it is checking (update_verb: `Checking for updates…` on stderr at a terminal); the trust page's INSTALL gotcha names the renamed Settings ▸ Packages button, Install ALab Tools (native_settings.rs packages_page). Earlier: re-read on 2026-09-23 (the plain status wording): `aterm help update` rewritten in plain words — the verb's one line and `-v`, how aterm updates, the three reasons it cannot, and a LOG section naming Settings ▸ Log and the three files with their caps — read against crates/aterm/src/main.rs update_verb/update_summary (one stdout line, a stderr trouble line, the updater's records to aterm.log through the session file logger, `-v` the ledger detail, any other argument a usage error exiting 2), aterm_update::cadence::WEB_INTERVAL_SECS (30 min), aterm_log::MAX_LOG_BYTES (4 MiB) and the history/packages log budgets. Earlier: re-read on 2026-09-23 (the provenance lane; merged the same day with main's Phase 5 deletions — the `seed` and legacy-bundle lines — which left this reading's paragraph byte-identical, re-measured over the merged page): `aterm help pkg`'s PROVENANCE gotcha said the lane copies the helper when \"the binary itself is tagged or quarantined\" and records a tagged install only \"when even that lane cannot run\". Read against atpkg::stage_helper::plan_for_exe, which now measures the binary, every `.app` above it in any case, and its `Contents/MacOS/` wrapper (provenance::taint_candidates — a clean binary inside a stamped bundle runs tracked, measured with a launchd job), and against the staging lane's witness path, which keeps and records a build whose lane ran but laid tagged files (the eight `<build>.tracked-install` records the owner's Mac held on 2026-09-22 were that case, by inference from their dates). Merged the same day with main's removal of `ATPKG_REFUSE_TRACKED_INSTALL`: the paragraph keeps this reading of the lane and main's `[packages] tracked_install = \"refuse\"` as the one refusal spelling, read against the merged lay.rs tracked_policy. Earlier: re-read on 2026-09-18 for the v0.88.0 assembly: the kitty-commands merge (2a06b13c2) added the `aterm list-kitty-commands` index line and the `help kitty` page (KITTY_PAGE_HEAD plus the generated vocabulary); read against the topic router (`pet | cat | tricks | kitty-commands | list-kitty-commands => kitty`, so every alias the page names resolves), `kitty_page` (the vocabulary printed IS the compiled TrickLexicon, as the page claims) and aterm-gui's `[sparkle_words] tricks` config (SparkleTricksConfig carries the `enabled` key the page documents); the page's behavioural sentences describe aterm_effects::typed_tricks and PetBrain::note_trick and are the feature author's, not re-derived in this read; the earlier reads this row carried still hold; 2026-09-19 (round 20) read of DRIVE_PAGE's watch limit clause — cleared at the first busy read after the auto-continue notice (`continuing automatically at 1:50pm` its time the reset, `continuing shortly` a minute; the row stays on the screen while the worker resumes), when it answers after a notice naming a reset, and the probe a minute past an auto-continue time — against aterm-agent run.rs limit_on_busy / look / arm_probe_at_reset and limit.rs parse_reset + SHORTLY, the same claims DRIVE_HELP makes at length; no claim contradicted; 2026-09-19 (round 20 review fixes) read of the added clause — the wall again before the worker answers is the same episode, mailed once — against run.rs open_episode/escalate(again); no claim contradicted; 2026-09-19 read of DRIVE_PAGE's one changed hunk at the rebase onto main 3d584667d (`xtask gate help-surfaces --diff`, matched at 5a48e1bf7: watch's limit clause, 3 lines out, 11 in) — the attention cleared when the worker WORKS again: at the first busy read after the auto-continue notice (its time the reset, `continuing shortly` a minute, the row still on the screen; the wall again before an answer the same episode, mailed once) and when it answers after a notice naming a reset; the probe a minute past an auto-continue time — against aterm-agent run.rs await_turn_from (a busy read is shown to limit_on_busy when review.unattended(); a soft busy is not busy there, worker_phase's order), limit_on_busy (a resumes_itself episode only; close_episode at the read's seq, Retry kept, rebrief_due), look's `worked || Prompt` close for a notice naming a reset and its retry reset on any non-limited point, open_episode/escalate(again: `meta set attention`, no post, `mail=skipped: the retry hit the wall again, the episode of seq=<m>`), arm_probe_at_reset (SHORTLY grace for resumes_itself, a failed>0 backoff kept), limit.rs parse_reset (`continuing automatically at <time>` a clock time, `shortly`/`continuing shortly` SHORTLY = 60 s) and resumes_by_itself, aterm-phase worker_phase (a hard busy wins before limit_notice) and reset_of (`1:50pm` / `shortly` handed over as printed); no claim contradicted, nothing fixed; the rest of the page is main's 2026-09-18 read; 2026-09-18 round-19 read of the IN THE WINDOW block added to FABRIC_PAGE and the `aterm` topic's window sentence against crates/aterm-gui/src/menu.rs FABRIC_MENU / DRIVING_MENU / VIEW_MENU / WINDOW_MENU (the row labels and their order), app_fabric_menu.rs (Inbox… is `inbox --peek --meta`, metadata only; the halt pair is cmd_hold with HoldIssuer::Owner, LOCAL origin, a fleet hold greying both rows with hold_row_reason; Fabric Status… and Turn Fabric On…/Off… run `aterm fabric` / `on` / `off` as a child, on/off behind confirmation(); the toggles write PRESENCE_BAND_KEY = presence.band / PRESENCE_RIM_KEY = presence.rim), menu_chrome_lines (`aterm ctl chrome` lists the tree, submenu lines included), from_invoke_name (every row an `invoke <Name>`) and canonical_invoke_name with every_pre_round_19_invoke_name_still_resolves (every pre-round-19 name still works). No claim contradicted; 2026-09-19 read of the fabric page's IN THE WINDOW block and the aterm topic's Fabric-menu pointer against menu.rs's MENU_MODEL (the Fabric rows in order, File ▸ Driving, Window ▸ Set Role…, View ▸ Presence Band/Rim), native_menu_action_enabled's halt pair and the [presence] band/rim keys; 2026-09-20 re-read of the `aterm help permissions` GOTCHAS against control_privacy.rs's folder_state/folder_source and covers_split: the per-folder bullet no longer says the state is `unknown` by construction full stop — it names the TWO things that move a row off it (an access aterm observed, rendered allowed/denied/asking/error, and `covered-by-fda` for a service a held grant is measured to cover), says an observation outranks the coverage rule, and warns that `documents=unknown` under a held grant means unmeasured rather than denied, which is the reading an agent would otherwise get wrong. A second bullet records the session-scoped `data from other apps` grant (\u{a7}1.6a): macOS keys that answer to one process instance and ships no Settings switch for the class, so it returns on every process replacement and retrying is the wrong advice — the text says to tell the operator instead, which is the same division of labour the rest of the page keeps. Both claims are measured (\u{a7}7 S4, 2026-09-19), not documented; 2026-09-20 (feat/agent-self-update-intercept merged onto origin/main 55323c26b): the merged page — upstream's macOS consent/privacy and updater-park prose beside this branch's SELF-UPDATE VERBS bullet and Windows caveat — re-read against the pkg GOTCHAS handler and the Security page rows; nothing contradicted; 2026-09-20 (round 21) read of the FABRIC page's two receipt sentences against the code that decides them. The RECEIPTS paragraph and the `[fabric]` block's `receipts = true` comment said the value was `the default aterm fabric on writes`, which was true of that command and false of every other way a bridge starts: read against aterm-link fabric.rs receipts_from_config (ON for an absent key, an absent file and an unreadable one), cli.rs's Config default and its serve arm, and enable.rs, which still writes the key explicitly and still leaves an operator's `false` standing. The page now says the default is the same either way and what off costs the SENDER. No claim contradicted; 2026-09-20 (round 21 review) read of the FABRIC page's heartbeat sentence against the code that now makes it false. The page said the bridge reports its link on change and after a 2x move, 'never on a timer, there is no heartbeat': read against aterm-link bridge.rs's ghost sweep, whose `Last` walk goes through `observe` -> `note_ack` -> `ack_wants_report`, so ANY answered broker read is an ack and a periodic read is a periodic `link up` — measured by a reviewer at one per 60 s per attached bridge, for ever. The sweep is now gated on having a candidate, and the page states the bounded exception instead of denying it; the pin at the foot of this file was widened to the new sentence so it cannot silently go back. No claim contradicted; 2026-09-22 read of the ONE addition this change makes — the new `harness` topic page and its index line — against crates/aterm-agent/src/harness/cli.rs, the handler it teaches. Checked claim by claim: the eight KEY USAGE lines are exactly `parse`'s subcommands and flag spellings (`--json`, `--since`, `--settings`, `--state`, `--dry-run`, `--purge`); ‘exit 0 always’ is `run_hook`/`run_statusline`, which return `ExitCode::SUCCESS` on every path including an unparsable payload, and `main_entry`, which answers 0 rather than 2 for a usage error on a hook-shaped argv; ‘one JSON object for a decision, one line for the footer’ is `allow_json` (a single compact object, no newline) and `run_statusline` (exactly one `writeln!`); the key-order/whitespace sentence is the honest statement of `merge_ours` over `aterm_json`'s sorted-key writer, and the backup/restore rule it describes is `Env::settings_backup` plus `run_uninstall`'s equality check, pinned by `install_then_uninstall_leaves_the_settings_file_byte_identical` and `uninstall_keeps_an_owner_edit_rather_than_restoring_over_it`; ‘there is no deny’ is `RmDecision`, which has two variants; `ATERM_NO_HARNESS=1` is the first guard in `bridge_sh`. The `--bare` and adopted-session sentences are the design's MEASURED facts (docs/DESIGN-aterm-wrapper-2026-09-17.md §4.5 and the live-session capture in harness/observe_tests.rs), quoted, not re-measured in this read. No claim contradictedound-22 REVIEW re-read of `aterm help fabric` and `aterm help drive` against the round-22 installer and `drive_cli`: the fabric help said four hooks, a PreToolUse that blocks and a Stop that keeps the turn alive, and described a report read from the transcript — all deleted in d6e9524a0; it now says three hooks, names `--gate-tools`/`--keep-alive` as opt-in, and describes the screen report and its ` busy=1`. `drive task`'s advice said a hooked worker WANTS `--no-nudge`, which against a round-22 default install posts a task nothing wakes for; `drive_cli`'s `no_nudge` defaults to false (it nudges) so the CODE was right and the advice was wrong, and the advice now names `--keep-alive` as the precondition. No claim contradicted; 2026-09-22 (2) re-read after a reviewer found the fabric page CONTRADICTING ITSELF — it said `merges four hooks` in one sentence and `THREE hooks, not four` in the next, because the round-22 correction was appended instead of replacing the claim it corrected. One true sentence now: it merges THREE, and the other two are opt-in. No claim contradicted; 2026-09-22 read, rebased onto round 22: the agents page's hooks paragraph (FIVE default entries with PreToolUse opt-in; the guard allows only when every value the variables take on the line resolves outside the critical classes; every other box is left to the human and escalated by the notification hook; the window's pass installs from an installed aterm only, on macOS and Linux, and leaves a complete block another installed aterm wrote alone) and the fabric page's BEING WOKEN Claude row (FIVE hooks, PermissionRequest answers or leaves alone, Notification escalates, --gate-tools/--keep-alive opt-in), against aterm-link hook.rs claude_settings_for/DEFAULT_EVENTS/permission_request/notification/escalate, permission.rs decide, and aterm-primer HookLane::install(force)/installed_exe; ; 2026-09-22 (the safety-and-honesty audit fixes)  re-read of the `harness` topic — its tagline, its `source` sentence and its KEY  USAGE block — against `harness::cli::parse`, `harness::cli::RINGS`,  `harness::cli::Readout::new` and `harness::source::Source::as_str`. THREE  CLAIMS WERE WRONG AND ARE FIXED. (1) The page said each read verb's `source`  field reads `spine`, `hook` or `statusline`; `Source::as_str` has no `spine` —  commit 7d0348bfe collapsed it into `grid`, `Source::parse` of the word `spine`  is an explicit negative control in `harness/source.rs` and in  `harness/cli_tests.rs`, and `Readout::new`'s ladder is statusline then hook  then grid. The page now names those three and says in one clause that `spine`  was retired, so a reader of an older page is not left guessing. (2) The tagline  and the KEY USAGE block described the hook bridge and its read verbs and listed  8 of the 21 subcommands `parse` accepts, omitting EVERY acting verb; KEY USAGE  is now two blocks, and the acting block's five entries (`switch`, `watch`,  `recover`, `nudge`, `disk --apply`) were each checked against their `Cmd` arm  and run function — `run_switch_now` and `run_watch` open the control socket,  `run_recover`/`run_nudge` print and withhold an unwitnessed L3 act's commands,  `run_disk` acts only with a named class AND `disk.apply`. (3) The `ledger` line  named three rings where `RINGS` has six; it now lists all six and says that  `recovery` and `actuation` are where `Watcher::pump` writes, which is  `watch::RING_RECOVERY`/`RING_ACTUATION` and the `journal`/`verdict_row` calls  in `pump` and `execute`. The page's central-law paragraph and its GOTCHAS were  re-read unchanged and still hold. Earlier reads in this row still hold; 2026-09-22 (round-23 review) read of the FABRIC_PAGE's new BROADCAST block against `fabric::cmd_topic`, `bridge::resolve_to`/`on_say_record` and `subscribe::parse_mail_params`: the three verbs, the empty-by-default set (the sender included), `since=head|@<off>`, the topic grammar, the demotion, `mail:topic=`, `dropped=` on the recipient, the 2 s roster latency and the `fabric tail --filter`/TOPICS pointers each match the handler; `topic`/`say`/`broadcast` are aliases of this page in `render` (pinned by fabric_topic_is_reachable_and_honest_about_wake_paths). No claim contradicted; 2026-09-22 merge read (round 23 onto main): upstream's five-hook contract (SessionStart, UserPromptSubmit, PermissionRequest, Notification, Stop; PreToolUse and keep-alive still opt-in) read against aterm-link hook.rs and the agents pass, with round 23's topic/say help — consistent; 2026-09-22 read of the one line `aterm help rust` gained since the last read - the sentence telling a reader to use the tools' own names in replies, tippy rather than clippy - against atpkg reroute.rs's reroute table and the primer's Rust note, which now say the same thing. The rest of this surface is this row's earlier reads.; 2026-09-22 (round 24, item 4) read of the `fabric` tagline, the `agents` topic's skill list and Codex sentence, the introspection page's MAIL paragraph and FABRIC_PAGE's BEING WOKEN rows against aterm-primer (`CODEX_ADDENDUM`, `skills_for`) and aterm-link `cli::dispatch`: THE FILE MIRROR section is gone with the code, and the Codex row states the owner's principle (the socket is refused; driven from outside; no part in messaging; nothing to configure). Pinned by fabric_topic_is_reachable_and_honest_about_wake_paths; 2026-09-23 read of the vendor-direct rewrite (design 2026-09-22 §1.7): the pkg page's `update`, `install` NOTE, `which` and `rollback` lines, the landing example lines, the new VENDOR PROGRAMS bullet and the SELF-UPDATE bullet, read against atpkg cli.rs cmd_update_one → cmd_update_vendor, install_inner's vendor fork, managed_state_of/vendor_state_of, cmd_rollback → cmd_rollback_vendor (vendor_direct::lane::roll_back_by_hand refuses a yanked build, no index resolve), landing.rs build_words (a vendor build is its version), selfupdate.rs announce/declined lines and the vendor lane's verdict lines; 2026-09-23 re-read after review: the VENDOR PROGRAMS cadence sentence now says what this build does — every update pass checks both vendors (the window's launch and 6h loop, cmd_update_all_code → run_update_pass → vendor_pass) and `claude update`/`codex update` check now (selfupdate → cmd_update_vendor); design §1.6's 5-minute head watch is not on this build and the page no longer claims it — and the SELF-UPDATE bullet's announce/decline sentences (aterm updates the program from its vendor) read against selfupdate.rs announce_line/declined_line; 2026-09-22 (round-24 review fix, commit C) read of `aterm help fabric`'s status line and table against fabric.rs (five `fabric=` states; the `stale` row added, worded as the fabric skill's) and of `aterm help rust`'s stock-cargo sentence against reroute.rs (announced, then refused with exit 2 under ATERM_REROUTE_STRICT=1). No claim contradicted; 2026-09-23 read of the VENDOR PROGRAMS cadence sentence restored with the head watch: the window checks both vendors about every 5 minutes and shortly after a wake — read against atpkg vendor_direct::watch (CADENCE 5 min on the wall clock, AFTER_WAKE 20 s, conditional GETs whose 304 is a zero-byte answer) and aterm-gui's package-loop park that runs it; every update pass still checks both vendors (run_update_pass → vendor_pass) and `claude update`/`codex update` check now (selfupdate → cmd_update_vendor). The merge also carries upstream's fabric=stale and reroute-strict lines, read by their own rows' note above; 2026-09-23 read of the VENDOR PROGRAMS bullet's new clause — a row names why the latest is not known when the vendor's release channel was not reached for a day — against atpkg cli.rs record_vendor_status (an Unreachable verdict past ProgramStamp::checked_recently, CHECK_VOUCHES_FOR_SECS = 24 h, writes `managed <v> — <Vendor>'s release channel not reached since <date>`) and managed_current_entries (leaves such a program out); and of no_page_carries_the_retired_vendor_wording's doc, now naming the window's 5-minute head watch and the update pass as the cadence, against the assertions under it (`about every 5 minutes`).; 2026-09-23 read of the VENDOR PROGRAMS bullet's closing sentences, reflowed to the page's width (a 145-character line; no word changed, shown by `gate help-surfaces --diff`): `The ALab index can only yank a version, never supply one` against atpkg vendor_direct::policy (the index lends a vendor program its yanks, nothing else) and `DISABLE_AUTOUPDATER=1` against vendor_direct::table's claude shim_env.; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of `help agents` (the pass writes no hooks and takes an earlier aterm's entries out once, with a .bak-<unix>), `help drive` (task: the typed nudge is the verb; watch --mail: a report the worker posts itself) and `help fabric` (the one sentence where BEING WOKEN was) against aterm-primer `remove_aterm_hooks`/`auto_prime`, aterm-agent `supervise::mail::task` and the fabric skill. Pinned by fabric_topic_is_reachable_and_honest_about_wake_paths. No claim contradicted; 2026-09-22 (round 25, item 2 — the watcher is the single supervisor) read of `help drive`'s watch paragraph against the same code. No claim contradicted; 2026-09-23 merge read: the page is the union of main's decision-B edits (the agents/hooks paragraphs and the `aterm agents` rows, read by main's own clauses above) and this branch's vendor-direct paragraphs, which are unchanged since their read above — no sentence of one contradicts the other; 2026-09-22 latency read: index_probe checks the four next public signed-index tags on bounded one- and five-minute cadences, with a first-page listing as a five-minute large-skip hint; package_park_tick_wakes only wakes the normal signed pass and preserves failure backoff. Checked against index_probe and the package worker; 2026-09-22 merge read: harness help's source list, account-switch limits and operation order checked against the handlers. No claim contradicted; 2026-09-23 vendor latency read: the pkg page now says the vendor head check is about every minute and shared across processes, matched to vendor_direct::watch CADENCE and the store-scoped watch lock/stamp. The GUI package park remains off the render thread; `claude update` and `codex update` still take the direct vendor pass. No claim contradicted; 2026-09-22 (Phase 2, silent by default) read of the pkg page's four changed passages (shown by `xtask gate help-surfaces --diff`) against the code under each: SAME TAB's no-wait sentence against atpkg cli::cmd_landing (the current bin/<program> exec'd at once, nothing printed; the usage, tool-name and could-not-run lines only on misuse), landing::sweep called from cli::mutator_store_lock once the lock is held (every mutating verb), platform::twin_prelude (the self-update block alone; no marker test) and activate::reconcile_agents's byte compare that re-lays an older twin once; the Windows sentence against cmd_selfupdate_prelude (empty on every input), twin_prelude's Windows arm (empty, so cmd_shim_content_twin == cmd_shim_content_env) and CMD_FRAME_HEAD/CMD_FORWARD_TAIL; the self-update bullet's `an install` for the pass that holds the lock; AUTOMATIC updates' never-checked sentence against doctor::run_with section (8) (the two warn lines naming the ALab index's programs and `claude and codex update from their vendors without it`), the deleted warn_if_never_checked and its four callers, and crates/aterm/src/main.rs's session lane (no never-checked line, no staged nudge, spawn failures to aterm_log); PROVENANCE's parenthetical against lay::allowed_note (aterm_update_core::log_pkg_note, never stderr) and main.rs's reroute-lay thread (lay_executables from a session), the staging lane's own note in install.rs being a pass's and unchanged. No claim contradicted; 2026-09-22 (Phase 2 review fixes) read of the two passages changed since (`gate help-surfaces --diff`): AUTOMATIC updates' never-checked sentence against doctor::vendor_update_clause (VENDORS minus `[packages] exclude`, each kept by vendor_direct::watch::follows_vendor — installed, not removed, pinned or dev-linked — and only while Probes::automatic: manager_enabled, `enabled` and `auto_update`; no clause when none is left); PROVENANCE's parenthetical against lay::allowed_note → notice::say (stderr in atpkg's own CLI — a typed verb, or a window pass whose stderr the window keeps in aterm.log — and aterm_update_core::log_pkg_note once a host has called notice::to_host_log: the session lane in crates/aterm main.rs and aterm_gui::main_entry, each with the aterm.log file logger installed). This read supersedes the previous clause's `never stderr` for allowed_note. No claim contradicted; 2026-09-23 (p2/gui review) read of the two reworded sentences against their handlers: the network-install line's `(on a first run the toolchain bar shows the sizes; later passes stay off the glass)` against aterm-gui `pass_is_first_run` and `StatusBars::toolchain_announced` (a routine announcement raises no row); the machine-settings line (recorded in `aterm ctl appstatus` and the log, named on Settings ▸ Security as the Last change) against `Wake::PkgMachineSettings` → `StatusBars::toolchain_machine_settings` (ledger entries, no row) and `PackagesService::note_machine_change` (the card's Last change); 2026-09-23 Phase-2 merge read: the union of p2/shell's re-read (no landing wait, no never-checked or staged-update line in a shell; a host's unasked notices go to aterm.log; doctor's truthful vendor clause) and p2/gui's (routine package rows go to the ledger and log; failures light the Settings Packages badge; appnotice says recorded for a marker) — disjoint behaviours, and no sentence of one contradicts the other; 2026-09-23 (Phase 3 pass work) read of the two paragraphs this change rewrote against their handlers: MACHINE SETTINGS (the edge gate `machine_table_changed`, the window's post-seed and each session's `machine apply`, the apply's own prints and refusals unchanged) and the update-pass paragraph's additions — write-only-when-bytes-differ (`reroute::lay`, `stub::pending_stub_executable_at`, `hooks::write_hooks`, the vendor stamp), the roll-forward (`cli::roll_forward_interrupted_activations` at the seed/update/install edge), the single durable write (`status::hold`, `write_durably`), exit 69 (`aterm_update_core::pkg_check::PASS_OFFLINE_EXIT` from `run_update_pass`/`update_pass_stopped`, classified `Offline` by the window) and the `status.toml.corrupt-<unix>` heal (`status::seed_for_rewrite`) — consistent; 2026-09-23 read of the Phase 3 review fixes: the `noindex apply` and `machine apply` rows now name what applies the [machine] settings — `aterm pkg machine apply` as the window opens (`spawn_machine_settings_once`, now beside the launch seed) and as every interactive session starts (`spawn_detached_machine_apply`), a pass only after an edit or an unfinished apply (`machine_table_changed`, `machine::forget_applied`), queued on `machine.lock` (`machine_apply_queue`); the GOTCHAS paragraph's roll-forward gates (pinned, yanked, grouped — `roll_forward_interrupted_activations`), the write-through record (`status::write`, `only_stamps_differ`), the offline definition (no host answered — `Fetcher::index_link_down`; a refused listing exits 1; every lost member lost at a fetch) and the corrupt-record heal in one rename (`status::heal`) — consistent; 2026-09-23 p3/reconciled merge read (p3/pass onto main): the pkg page is the union of main's AUTOMATIC-updates sentences (the next-index probe's one- and five-minute HEADs, the shared listing, the six-hour full check) and p3/pass's MACHINE SETTINGS, noindex/machine-apply rows and the update-pass GOTCHAS additions (write only what differs, roll-forward, durable status.toml, offline exit 69, corrupt-record heal), each read above against its handler; the two sets touch disjoint sentences and no sentence of one contradicts the other (the window's Offline verdict is a failure-ladder retry in main's loop, which is the `retries it quietly` the GOTCHAS line promises); 2026-09-23 p3/reconciled read (p3/sched's schedule onto main's probe, shown by `xtask gate help-surfaces --diff`): the AUTOMATIC-updates paragraph against aterm-gui spawn_pkg_update_check — the six-hour walk is `pkg_check::FULL_PASS_INTERVAL_SECS` with no knob (update_interval_secs and the once-pass mode deleted), machine-wide through `scheduled_pass_gate` over `pass_stamps` (full_pass_due_in on the record with the lane's own success folded in; a sibling's pass in flight via `atpkg::progress::pass_running` or attempted within PASS_SPACING_SECS holds it; an index the verified floor covers is not re-run), wall-clock parks with `park_deadline_after_slice` moving a slept-through deadline to watch::AFTER_WAKE (20 s), `index_probe::classify` admitting only a 302/307 to RELEASE_ASSET_HOSTS, `BumpWatch::unlanded` plus the failure ladder for an index a pass did not land, Backoff::FAILURE 10 min doubling to 2 h, `PassEnding::Settled` for an exit 2 that reached the index; the session sentence against aterm main.rs session_pass_due (full_pass_owed with in_flight, then pkg_check::claim on session-pass.stamp) and session_pass_args (pass_flags: --wait-lock, --progress-file); the `noindex apply` / `machine apply` rows and the MACHINE SETTINGS GOTCHA against main.rs machine_apply_due (the daily claim on machine-apply.stamp) and the window's unchanged spawn_machine_settings_once — consistent; 2026-09-23 Phase 4 p4/settings read: the AUTOMATIC-updates bullet's new sentences (`[packages] enabled` is Settings ▸ Packages' \"Automatic updates\", read LIVE by the window: aterm-gui `LiveSwitch` polled before every pass and at each 5 s `PARK_SLICE`, `stand_by` inside `PkgLane::park` with the deadline kept; Update Now is the Settings worker's own pass and ignores the switch) and the new PACKAGE LOG bullet read against `atpkg::packages_log` (`aterm_types::dirs::logs_dir`, one `O_APPEND` write per line, `sanitize`'s URL/token/control/bidi stripping, `ROTATE_AT_BYTES` 1 MiB and `KEEP_ROTATED` 5 only under `lock::held_by_this_process`, `ACTIVITY_EVENTS` 50) and `cli::pass_lane`'s lanes (head watch, session, agent update, window, typed; `PassRecord::refused` for a verb the lock turned away) — consistent; 2026-09-23 (Phase 4, one path) re-read of every sentence that taught an environment knob, against the code that replaced it: the Rust note and `aterm help rust`'s FLAGS line name `[reroute] announce = false` (atpkg `reroute::run` -> `config::cached_reroute().announce()`) and `aterm --no-reroute` (the front door's `take_no_reroute` -> the internal `ATERM_REROUTE_PASSTHROUGH` marker); the reroute topic's ESCAPES AND CONTROLS table drops the four reroute variables (the ORACLE refusal now names the upstream path, `oracle_message`); the harness bullet names `harness.enabled`; the atpkg page names `[packages] enabled` for the session lane (`packages.enabled()` in the front door), `[packages] tracked_install = \"refuse\"` as the one refusal (`lay::tracked_policy_of`), `auto_install` as the one install consent (`PackagesConfig::auto_install`, `cli::should_complete_set`, `set_completion_consent`) and the retired/development keys (`config::admit_packages`); the update page adds the `[update] enabled` reason (`aterm_update_core::settings`) and the token-lane rung ids `keychain`/`file`/`gh-cli` (`token::RUNGS`). Pinned by `atpkg_topic_states_the_compiled_anchor_and_the_window_scoped_update_loop` and `rust_topic_measures_and_leads_with_the_trust_default`, which now also refuse every retired name.; 2026-09-23 (Phase 4, one path, second read) of the three further edits: the CONFIG page's starter-file count is 155 keys (`aterm-gui` `cli::tests::starter_config_key_count_matches_the_manual` counts the template: the retired `auto_update`, `seed_install`, `account`, `channel`, `include` and the owner/repo link example left, `[reroute] announce` arrived), THE KEY ROSTER says `[update] enabled` is on Settings ▸ Terminal ▸ Updates (`prefs::section_of` routes every `update.` leaf to Terminal, group \"Updates\") and `auto_apply` on Software Update (`update_automatic_card`), and the update page's off-switch bullet says the switch applies next launch (`settings::update_enabled` is read once per process).; 2026-09-23 (Phase 4 review fixes) read of the two changed passages (`gate help-surfaces --diff`): the `aterm` topic's WHAT IT IS no longer teaches `ATERM_SESSION_MODEL=1` — a development seam a shipped binary never reads (`aterm_cli::session_model_seam` through `aterm_types::dev_seam!`) — and says only that the in-process VT model is off and not readable from outside; the update page's third reason now says `[update] enabled = false` stops the background checks only (`aterm_update::automatic` gates `spawn_background_check_with_source`), that `aterm update status` says so (`print_update_status` prints `automatic checks: off`), and that `aterm update check` / Check for Updates still check and a staged build still applies (`check_now` and the launch apply read `aterm_update::enabled`, the platform alone). No claim contradicted; 2026-09-23 merge read (p4/integrate: p4/settings merged with p4/onepath): the merged ATERM PACKAGES page read as one — settings' GOTCHAS sentence (`[packages] enabled` is Settings ▸ Packages' \"Automatic updates\" switch, read LIVE by the window, Update Now either way) and its PACKAGE LOG bullet beside onepath's ONE PATH bullet and no-kill-switch sentences, against the merged lane: `LiveSwitch` in aterm-gui `spawn_pkg_update_check` gates on atpkg's own `PackagesConfig::enabled` (the retired `auto_update` folded in; `LoopGate` has two states, On and Off), so the page's `enabled` sentence and its `the retired auto_update still reads as enabled` sentence describe the one reader — consistent. No claim contradicted; 2026-09-23 read after review of the merge (`gate help-surfaces --diff`): the ONE PATH bullet no longer says the retired keys read as their new ones `until renamed` — the blanket rename onepath removed from doctor and the config editor. It now says `auto_update = false` keeps updates off whatever `enabled` says and `seed_install` reads as `auto_install` while that is unset (atpkg `PackagesConfig::enabled`/`auto_install`), that a retired key is renamed only when its new key is not written — beside it a rename writes the key twice, a file that no longer parses, which atpkg reads as automatic updates ON — else removed, with `enabled = false` said when `auto_update = false` is what keeps updates off (`config::auto_update_note`, the sentence doctor, the config editor and Settings ▸ Packages show), and that turning Automatic updates on removes it (aterm-gui `prefs::apply_prefs_edits`, `RETIRED_PACKAGES_SPELLINGS`). Pinned by pkg_page_never_advises_a_rename_that_writes_enabled_twice. No claim contradicted; 2026-09-23 read of the SELF-UPDATE VERBS bullet's added clause — the announce line names the version you have (`you have 2.1.278`; `build N` for an index build) and does not echo the typed words — against `atpkg::selfupdate::announce_line` (pinned byte for byte by `every_line_is_exact`; the words come from `vendor_direct::build_words` of the active build) and `cli::selfupdate_check`, which reads the active build before the child; the words `no_environment_knob_reaches_the_intercept` requires are all still there; no claim contradicted; 2026-09-23 Phase-4 merge read with main's 8ea92e191: the SELF-UPDATE bullet's announce line now names the version you have (read against cli.rs selfupdate_check: ops::active_builds then vendor_direct::have_words into selfupdate::announce_line, the typed words no longer echoed) — consistent with Phase 4's one-switch and one-path text on the same page, which is unchanged by it; 2026-09-23 (Phase 5, the sealed-seed client lane deleted) read of the `aterm pkg seed` entry against atpkg's cmd_seed as it now stands: it records adoption, lays a pending stub per default-set name, consults the signed index (report_seedless_posture) and installs nothing itself, the update pass behind it installing; `[packages].auto_install=false` makes it print one line and return before adoption, stubs, reroutes or the seam — the pre-v0.63 seeded-bundle sentence is gone with the lane; no claim contradicted; 2026-09-23 (fix/claude-at-exec-time, re-read at its merge of origin/main 2c256a83e) read of the four hunks `xtask gate help-surfaces --diff` shows on top of main's Phase-4 text — the pkg page's SAME TAB bullet (the `aterm --no-reroute` sentence, now naming that its marker is not read by the claude/codex stubs; the WHICH COPY RUNS IS DECIDED WHEN IT RUNS paragraph with its `rehash`/`hash -r` step; the narrowed SHADOWED sentence) and the reroute page's THE AGENT PROGRAMS section (three markers since main removed ATPKG_AGENTS_EVERYWHERE; the --no-reroute marker), its ESCAPES AND CONTROLS `--no-reroute` row and its WHERE THE STUBS LIVE sentence — against atpkg reroute.rs agents_stub_body_sh (markers only, never PASSTHROUGH_ENV; the pass-through past every reroute dir, agents/ and the stub's own links, reaching bin/ as before; the 127 line), agents_routes/lay, hooks.rs AGENTS_MARKERS (the three), cli.rs agent_stub_on_path/shadow_in_shell/agent_routed_row/agent_passed_row/which_line_in, doctor.rs (3b)/(10d), state.rs agent_routed_in_shell/agent_passed_through, aterm-cli lib.rs reroute_dir_from_values and aterm main.rs (a --no-reroute launch lays and hands no reroute dir), and the zsh/bash command cache the 2026-09-23 review measured — consistent; 2026-09-23 read of the provenance heal (ux/wp1): the PROVENANCE bullet is now three sentences, read against atpkg — the tag is cleared after every install (`provenance::heal_store` ends the update pass, the default-set pass, the single-program install door, the grouped update, rollback and the launch seed, and `install::verify_and_stage_with` clears a staged tree before its swap), `doctor` 5b prints one line counting the installed files `store_roots` still finds tagged, and `repair` runs the same heal and exits 1 only while files stay tagged; the removed sentences (the stdout record marker, the re-seed advice) describe code this commit deletes; 2026-09-23 merge read (ux/status-reporting-2026-09-23 with origin/main's Phase 4; the one hunk `gate help-surfaces --diff` shows over main's text): the ATERM PACKAGES GOTCHAS keep Phase 4's LIVE `[packages] enabled` sentence and THE PACKAGE LOG bullet beside ux/wp1's three-sentence PROVENANCE bullet, which replaces main's long one — main's re-spelled `[packages] tracked_install = \"refuse\"` sentence, the in-process record and the re-seed advice describe code ux/wp1 deletes (atpkg `provenance::heal_store`, `repair_store`, doctor 5b); no environment knob is taught (`atpkg_topic_states_the_compiled_anchor_and_the_window_scoped_update_loop`) — consistent; 2026-09-23 (fix/claude-at-exec-time, re-read at its merge of origin/main 2c256a83e) read of the four hunks `xtask gate help-surfaces --diff` shows on top of main's Phase-4 text — the pkg page's SAME TAB bullet (the `aterm --no-reroute` sentence, now naming that its marker is not read by the claude/codex stubs; the WHICH COPY RUNS IS DECIDED WHEN IT RUNS paragraph with its `rehash`/`hash -r` step; the narrowed SHADOWED sentence) and the reroute page's THE AGENT PROGRAMS section (three markers since main removed ATPKG_AGENTS_EVERYWHERE; the --no-reroute marker), its ESCAPES AND CONTROLS `--no-reroute` row and its WHERE THE STUBS LIVE sentence — against atpkg reroute.rs agents_stub_body_sh (markers only, never PASSTHROUGH_ENV; the pass-through past every reroute dir, agents/ and the stub's own links, reaching bin/ as before; the 127 line), agents_routes/lay, hooks.rs AGENTS_MARKERS (the three), cli.rs agent_stub_on_path/shadow_in_shell/agent_routed_row/agent_passed_row/which_line_in, doctor.rs (3b)/(10d), state.rs agent_routed_in_shell/agent_passed_through, aterm-cli lib.rs reroute_dir_from_values and aterm main.rs (a --no-reroute launch lays and hands no reroute dir), and the zsh/bash command cache the 2026-09-23 review measured — consistent; 2026-09-23 (fix/claude-at-exec-time, item 2c) read of DRIVE_PAGE's watch box sentence (own first row ≤64 cells, one kind=ask per review point, asked again after the worker worked; supervise escalates nothing) against aterm-agent run.rs escalate_box/approval_line and the review.unattended() guard — consistent; 2026-09-23 merge read (ux/status-reporting-2026-09-23 onto origin/main 9545a1131): `gate help-surfaces --diff` shows only the DRIVE_PAGE watch-box hunk the previous clause read, over ux/wp1's recorded text — consistent; 2026-09-23 (Settings ▸ Log) read of the two added hunks: the package-log paragraph's `Settings ▸ Log shows it together with aterm's own record` against aterm-gui history_screen::read_log (packages.log and its newest older copy merged with history.log); UPDATE_PAGE's WHERE THE RECORD IS against native_settings log_page (Copy = history_screen::LogProjection::copy_text after about::provenance_text, Open Log Folder = AppEffect::OpenLogFolder on history_log::dir), history_log (ROTATE_AT_BYTES 256 KiB, one `.1` copy through logging::RotatingFile), atpkg packages_log (ROTATE_AT_BYTES 1 MiB, KEEP_ROTATED 5) and aterm-log MAX_LOG_BYTES (4 MiB, one `.1`), and the events it names against App::flush_history, the Wake::UpdateProgress Staged arm, Wake::UpdateHealth and logging::CrashEvidence; no claim contradicted; 2026-09-23 read of ux/wp6's doctor prose (`--diff` shows only the four doctor sentences) against atpkg's doctor_detail and doctor::present: the verdict leads, the default lists only warn/FAIL/PROBLEM rows at most 160 characters with the row's own next step, `--verbose` prints every check and is the one flag, and the exit code is still run_with's; `doctor --verbose` is what names the seams — consistent; 2026-09-23 ux/wp6 review read of the `aterm pkg machine apply` row's one changed line: `aterm pkg machine` prints the Universal Control revert line (cmd_machine prints machine::doctor_line), where the default doctor no longer shows ok rows — consistent; the same review's second read: the two passages that quoted an ok row as something the default doctor prints — the paper-master anchor (doctor's `ok — paper master pinned (fingerprint …)` row) and the agent route (`routed at exec time by …`, an ok row) — now name `aterm pkg doctor --verbose`, which prints every row (doctor::present's Detail::Everything) — consistent; 2026-09-23 merge read (ux/wp6 onto ux/wp5's Settings ▸ Log text): `gate help-surfaces --diff` shows only wp6's doctor hunks, read in the clause before — consistent; 2026-09-23 Phase-5 merge read with main through d8d9c7bad: the union of main's exec-time agent routing text (4516e581a: the claude/codex reroute stubs, THE AGENT PROGRAMS, the rehash/hash -r step — read by main's own clauses above) and Phase 5's removal of the sealed-seed wording (`atpkg seed` adopts, lays stubs and reports; nothing installs from a bundle) — disjoint paragraphs, no sentence of one contradicts the other; 2026-09-23 read of the prose 2aa8e689d moved (shown by `xtask gate help-surfaces --diff`, matched at 2c256a83e: 1 hunk, 4 lines removed / 7 added) — the introspection page's headless paragraph, now `A headless instance never takes the keyboard: on macOS it is a background process with no Dock tile that cannot become the active app, so a harness may boot one while you type elsewhere`, and the unchanged ATERM_AI_HINT sentence reflowed around it — read against aterm-gui lib.rs's `launch_posture` (:6568: `headless` → `policy: Some(AppActivation::Prohibited)`, `activate_on_launch: false`), `apply_launch_posture` (:6585, mapping Prohibited to winit's `ActivationPolicy::Prohibited` and passing `with_activate_ignoring_other_apps(false)`), its application to the app's own event-loop builder (:34082 `apply_launch_posture(&mut builder, launch_posture(headless))`), the AppActivation::Prohibited doc (:6475: NSApplicationActivationPolicyProhibited, no Dock tile) and crates/aterm/tests/headless_activation.rs, which boots a real headless instance and reads NSRunningApplication.activationPolicy and NSWorkspace.frontmostApplication; the ATERM_AI_HINT sentence is byte-identical apart from the reflow; no claim contradicted; 2026-09-23 merge read (ux/status-reporting-2026-09-23 with origin/main through 0467ca2ea): `gate help-surfaces --diff` from this branch's recorded read shows two hunks, both main's and both read by main's clauses above — the `aterm pkg seed` entry (Phase 5: adopts, lays stubs, consults the index, installs nothing; auto_install=false lays nothing) against cmd_seed as merged, which keeps main's body behind ux/wp1's silent launch heal, and the headless paragraph against launch_posture/apply_launch_posture; the PROVENANCE gotcha stays ux/wp1's three-sentence bullet — main's longer one described the lane's copy plan (now main's taint_candidates walk in plan_for_exe, which the merge keeps) together with the in-process record and the doctor re-seed listing that ux/wp1's heal replaced, so it is not restored; no claim contradicted; 2026-09-24 read of the one changed line: HOW aterm UPDATES says \"about every 10 minutes\", read against aterm-update cadence::WEB_INTERVAL_SECS (10 * 60) — consistent; 2026-09-24 read (the ux/status-reporting merge's log port, ba7e63129), shown by `--diff` (2 hunks, 7 removed, 9 added): the LOG section names Settings ▸ Messages (everything aterm told you, newest first, records included; a row expands to all of it), Copy All with the build information and Open Log Folder (native_settings MESSAGES_COPY_ALL / MESSAGES_OPEN_FOLDER and AppEffect::OpenLogFolder), package updates on Settings ▸ Packages ▸ Activity, and the files' caps — aterm.log 4 MiB + aterm.log.1 (aterm_log::MAX_LOG_BYTES, logging::RotationBudget::ATERM_LOG), messages.log 1 MiB + messages.log.1 (messages_store::BUDGET.rotate_at = TAIL_BYTES through logging::RotatingFile), the page's newest 512 (aterm_messages::LOG_CAP), packages.log 1 MiB × 5; the atpkg paragraph drops its Settings ▸ Log clause. No claim contradicted; 2026-09-24 read of the harness page's new `upgrade` synopsis line and paragraph against crates/aterm-agent/src/harness/cli.rs (`parse`'s `upgrade` arm, `run_upgrade`) and harness/upgrade_drive.rs: notice first, READY answer and every shell under the agent waited for (never killed), SIGTERM, the atpkg hook sourced before the relaunch (when the hook file exists), the same flags plus `--resume <session>`, the continuation line, `--dry-run` printing each session's next step, state and ledger under `<state>/upgrade/`. Consistent; 2026-09-24 (2) read of the added sentence (the window sweeps every minute on its own tabs by default; `aterm harness config set cap.upgrade.enabled false` turns it off) against `upgrade_drive::host`/`host_enabled`, the `cap.upgrade.enabled` key in harness/config.rs's KEYS, and aterm-gui's `spawn_agent_live_upgrade` (skipped headless). Consistent; 2026-09-24 (3) the `upgrade` paragraph's first line re-read after rewording it to name Claude Code's `Update installed` banner without quoting its restart phrase (B12 / `no_update_lane_source_prompts_a_restart`: aterm never prompts a restart, and the quote read as one); the claim is unchanged — `upgrade` restarts the agent in place and resumes the conversation. Consistent; 2026-09-24 merge read (origin/main 798075024 onto ux/status-reporting): `gate help-surfaces --diff` over main's recorded read shows only this branch's own pkg-doctor, PROVENANCE, update-page and Settings ▸ Messages hunks, each read against its handler in the clauses above — consistent; 2026-09-24 merge-read (fin/integrate over origin/main dd2581889; both sides' reads kept above): the two sides recorded their own pages (fin/integrate 0168d5333e58e410; main 7f75718867735eb3, which no committed tree hashes to, so the gate's `--diff` fell back to 7765ddc32, the commit that last touched main's row). `gate help-surfaces --diff` against fin/integrate's read shows only main's 13 hunks — `aterm pkg doctor --verbose` (the anchor sentence, the usage row, the one-flag parenthesis, the seams sentence, the doctor gotcha and the reroute route), the `aterm pkg machine` revert pointer, the PROVENANCE gotcha rewritten to the tag the package manager clears, `Install ALab Tools`, and `aterm help update` rewritten (status/check/-v/identity, HOW aterm UPDATES, the automatic-checks reason, LOG) — read in main's notes above; against main's fallback it shows only fin/model's AUTOMATIC-updates paragraph, read in fin's note above. Read together over the merged tree: the update page's `\"Check for updates automatically\" is OFF (Settings ▸ Terminal ▸ Updates) … Turning it back on takes effect the next time aterm opens` holds against the two-switch Software Update card (the same `[update] enabled` row, which prefs.rs places on the card, on Terminal ▸ Updates and in Search; `aterm_update::automatic` reads it once per process) and against the update_screen detail this merge settled (`Automatic checks start next launch — until then, Update to Latest Now still checks now`); `turn that off in Settings ▸ Software Update to install only when you press the button` is the card's Install switch (`[update] auto_apply`). No claim contradicted ; 2026-09-24 (origin/main merged into harness/integrate): the merged page is the union of the two sides — the harness and drive pages as read in the first two clauses, and main's package-pass paragraph (the rate-limited listing's recorded reset, status.toml's check ending) as main's own clause read it; the merge added no prose of its own",
    ),
    (
        "crates/aterm-cli/src/windowing.rs",
        "fd8be119f5c354f8",
        "2026-09-14",
        "read against its parser/dispatch by the 2026-09-10 sweep; findings fixed in 412cf3acd; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code",
    ),
    (
        "crates/aterm-control/src/selection.rs",
        "7269abf2d2722432",
        "2026-09-10",
        "read against its parser/dispatch by the 2026-09-10 sweep (aterm-B-window-and-clients); findings fixed in the commit that added this row",
    ),
    (
        "crates/aterm-ctl/src/conn.rs",
        "8ce0726820ccb121",
        "2026-09-18",
        "read against its parser/dispatch by the 2026-09-10 sweep (aterm-B-window-and-clients); findings fixed in the commit that added this row; 2026-09-14 drift sweep (lane ctl, 61 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code re-read on 2026-09-18 after the dead-`latest`-alias fallback landed: the one prose item it adds here — the doc on `ConnWire`'s new `origin` field — was read against `conn_real_main`'s `--sock`/`--pid` parsing and its `resolve_target` call, `ConnWire::request`'s `connect_stream(&path, self.origin)`, and aterm-ctl's `resolve_target`/`connect_error`/`running_elsewhere_hint`: every flag and an explicit `$ATERM_CONTROL_SOCK` resolve to `TargetOrigin::Pinned`, whose dead socket is told only how many other instances are live and is handed no instance to drive. `CONN_USAGE` still carries both flags. No claim contradicted.",
    ),
    (
        "crates/aterm-ctl/src/lib.rs",
        "e5fae0d332715c77",
        "2026-09-24",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane small-1); the cell attribute roster omitted `hidden` and is fixed here; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at 550ace022) — `offscreen` joining `inbox` as the verbs whose `OK <n> …` header goes to stderr with stdout kept as rows, and the two `offscreen` cases in streams_payload_gates_by_verb_and_image_read — against exchange's header branch, stderr_line's `aterm-ctl: ` prefix, streams_payload → control_verbs::framing_of (offscreen's catalog row is Lines) and aterm-agent report.rs parse_offscreen, which reads that stderr header (CtlClient shells out to this client); no claim contradicted; 2026-09-14 drift sweep (lane ctl, 61 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 round-12: two public entries added for aterm-link's hooks, resolve_sock_for and read_token_beside; their docs read against resolve_path (an explicit or disabled $ATERM_CONTROL_SOCK directive first, then self_instance_sock's graph entry with its liveness probe, then default_sock_path's latest alias) and read_token_at (the per-socket token, then for a non-instance socket name the legacy aterm.token; an InvalidData/InvalidInput miss stops the search); nothing else in the file moved; 2026-09-14 read of the two public entries added since (`local_instances` and `instance_token`, and their docs) against inspect_fleet/enumerate_instances (the readdir plus graph-entry walk `instances`/`ls`/`windows` share, sorted by pid, `0` for a graph entry naming none, no dial), discovery_report's early arms (Unresolvable/Missing/Unreadable carry the same text and EXIT_UNREACHABLE `ls` exits with; Empty and Found are answers, returned as Ok) and read_token_at (the per-socket token through the `latest` alias, the legacy shared file for an explicit socket only, a malformed per-socket file refused without reaching past it), by feat/round-11-fabric-ledger for crates/aterm-link/src/fabric.rs, their one caller; no claim contradicted; 2026-09-14 read again on the merge branch: local_instances' and instance_token's docs against inspect_fleet (it answers Unresolvable, Missing or Unreadable only for a directory it could not look in, and Empty/Found for one it could, which is exactly the Ok/Err split the doc claims) and enumerate_instances (the readdir pass and the graph-entry pass deduped by the CANONICAL socket path, `out.sort_unstable()` so the pairs come back pid-ordered, graph_entry_pid's `unwrap_or(0)` behind the `0` placeholder sentence) and read_token_at (per-socket file through `latest`, the legacy shared file for an explicit socket only, a malformed per-socket file refused without reaching past it); no claim contradicted; 2026-09-14 merge read of feat/round-12-hooks over main: the merged prose is exactly this branch's resolve_sock_for and read_token_beside docs (one hunk after self_instance_sock) plus main's local_instances and instance_token docs (one hunk after fleet_sessions), checked against resolve_path's argument order (SOCK_ENV, NO_SOCK_ENV, then self_sid), inspect_fleet's DirOutcome arms in local_instances (Found, Empty and Scoped are Ok; every other outcome goes through discovery_report into FleetListError) and read_token_at, which both read_token_beside and instance_token wrap with the same `{}: {e}` map — the one string each side's --diff shows as the other's addition; neither side's hunk touches the other's anchors, and no sentence is wrong; the two identical bodies are a duplication for the owner to fold, recorded here rather than changed at the merge; 2026-09-15 read of the prose this change adds (duplicate_sids' doc, the two run_discovery comments — every answered row kept for the ambiguity sweep, and one mapping deciding what `ls` shows, what the bridge federates and what the sweep reads — and the new `WARNING: session id … is served by N live instances …` line) against duplicate_sids itself (pids deduped per sid before the >1 count, so one instance listing a sid twice is not two claimants; listing order kept by the `order` vec; a row with no sid column skipped exactly as the printer skips it), against run_discovery's loop (rows are still written as each instance answers — the sweep is the only thing that waits for all of them — and instance_sessions is now called once per answered instance for all three verbs, the `ls` arm iterating it by reference), and against stderr_line's `aterm-ctl: ` prefix; the doc's claim that `graph/<sid>` records exactly one host per id read against aterm-gui proxy::write_graph_entry (one file per sid, last writer wins); no claim contradicted; 2026-09-17 read of the `ls` shape (identity=<name|-> last, before the self mark) against control_session::sessions_lines and roster_tail's by-key walk re-read on 2026-09-18 against the handler, in full, because the fallback commit moved most of this surface's prose: the module doc's and SOCKET RESOLUTION's dead-alias sentences and the docs of `resolve_path`, `TargetOrigin`, `resolve_target`, `flagless_target`, `newest_live_instance_in`, `newest`, `instance_socks_in`, `connect_error`, `connect_error_naming`, `live_instances_besides`, `running_elsewhere_hint`/`_count`/`_flags`, `reached_by_pid`, `connect_stream`, `exchange`, `feed_bin_exchange` and `front_door_instance` were read against the code they describe (the Pinned arms; the PerInstance arm's `flagless_target`; the `NotFound`/`ConnectionRefused`-only fallback over `instance_socks_in`, filtered by an accepted connect and ordered mtime-then-pid; `connect_error`'s dir+fleet look-up and `connect_error_naming`'s empty-fleet split; `reached_by_pid`'s filename-then-component-then-`same_socket_path` rule; and `run_discovery` dispatching before `resolve_target`, so `ls`/`instances`/`windows` never enter the fallback) and against the code outside it (aterm-gui's `cleanup_socket` with `symlink_targets_pid`, `sweep_stale_instances` via `stale_instance_files`/`instance_pid`, which can never match the fixed alias name, `publish_latest_link`'s forward-only publish, `write_graph_entry`'s per-entry hosting pid, `discovery_targets`' dead-`--pid` message, and aterm-cli's `new-tab` grammar, which takes no `--pid`/`--sock`). Every claim held. THREE SLIPS FIXED in the commit that records this row: `resolve_sock_for`'s order stopped at the `latest` alias though it now inherits the fallback, `resolve_path`'s doc named only the front door though the public `resolve_sock_for` also calls it, and `live_instances_besides` said `aterm ctl instances` \"would list\" a set that is wider than what that verb prints (it counts an accepted connect; the verb prints those that also answer `sessions` inside `PROBE_DEADLINE`, so a wedged instance is counted and not listed). LEFT FOR THE OWNER, recorded rather than changed: the aterm-link hook.rs mirrors of that stale order (a rostered surface whose own hash did not move), and the fallback's two probes carrying no deadline where discovery's connector has one, so a wedged peer can stall a flagless call at resolution time.; 2026-09-22 (round-23 review) read of `drain_refusal_tail` and its call site: a non-OK status line is printed with the `aterm-ctl: ` prefix as before, then every continuation line the server wrote is printed verbatim and unprefixed until EOF, a read error or REFUSAL_TAIL_MAX_LINES; the server's two-line `subscribe` refusal was measured on the raw socket and the old client printed one. Pinned by a_multi_line_err_is_drained_to_the_end_and_bounded. No claim contradicted ; 2026-09-22 read of the ONE doc comment this change rewrites, `drain_refusal_tail`, against its own body and its call site in `exchange`. The claim the old doc made — \"until EOF\" — was the defect: a control connection is NOT closed after a refusal, so the read waited for an EOF that never came and every `aterm ctl` invocation a live instance refused blocked in `recvfrom` until the 900 s per-op deadline (MEASURED 2026-09-22 with `aterm ctl <unknown verb>`, which printed its ERR and then hung; `crates/aterm/tests/front_door_verbs.rs` caught it as a 60 s timeout, and only because a live instance was there to refuse — with nothing listening the dial fails first and that suite is green, which is how this reached main). The rewritten doc states what the body now does and no more: it issues NO read, takes complete lines from `BufReader::buffer()` alone, leaves a partial final line where it is, and says in its own words that a continuation sent in a LATER write is not waited for and not printed — a deliberate trade, named rather than hidden. Checked against the two tests beside it, including the new `the_drain_never_reads_from_the_socket`, whose reader panics if read. Nothing else in this file's prose changed; 2026-09-22 (round 24, item 1) read of the `subscribe --help` fabric-kinds list, which now names `topic`, against `crate::fabric::FABRIC_EVENT_KINDS` in aterm-gui (the one table the digest's filter reads). No claim contradicted; 2026-09-22 (round 24, item 4) read of `dominant_hint`'s Denied arm and the `Probe` doc against `discovery_report`: a denied connect is named as the sandbox's refusal with nothing to configure — no directory, no `--allow-unix-socket`, no escalation; the per-socket cause line is unchanged. Pinned by a_denied_connect_is_named_and_not_worked_around and the two EPERM/EACCES report rows; 2026-09-22 (round-24 review fix, commit A) read of PUSH FRAMES' `sessions` and `GAP` lines against subscribe.rs: `fabric-retire` joins the sessions kinds, `events-dropped=<n>` the GAP tokens. No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the `resolve_sock_for` and `resolve_path` docs against their callers: aterm-link's hooks are gone; the harness (`aterm harness`) is the caller that resolves through them. No claim contradicted; 2026-09-22 read of the `path=<frozen|live>` column added to the `ls` grammar and its CLIENT_VERBS entry against control_session.rs's sessions_lines (the token is emitted last, after identity=, from SessionStore::has_frozen_path under the snapshot's guard) and against roster_tail here (reads the tail by key back to meta=, so ls/windows need no change); pinned by help_for_a_client_verb_is_answered_from_the_client_block and the ls-columns assertion; merged twice with origin/main's own 2026-09-22 reads of this file (drain_refusal_tail), whose prose this round did not touch; 2026-09-22 merge read: refusal-tail text checked against drain_refusal_tail and the no-read regression; continuation bytes are bounded by the direct MAX_LINE_BYTES check. PUSH FRAMES' sessions and GAP lines checked against subscribe.rs: fabric-retire is a session event, and events-dropped=<n> is a GAP token. No claim contradicted; 2026-09-23 read of the prose this commit moves (1 hunk, 4 removed / 5 added, shown by `xtask gate help-surfaces --diff`): the `latest` alias doc said the server maintains it in the FORWARD direction only and that nothing ever hands it back to a survivor. False, and read against the handler that makes it false: aterm-gui's app_update_handoff.rs `HandoffWorkerCleanup::complete` (:654, `Complete all filesystem repair before the UI is told rollback is safe`) calls `control_auth::publish_latest_link(latest_link, socket_path)` for the parent's socket, so an update-handoff ROLLBACK republishes the surviving parent. The doc now names that one exception and keeps the rest (a newer instance repoints the alias at itself; the two ways a dead alias fails are unchanged); no other claim contradicted; 2026-09-24 read of the prose fin/deflake's review fixes move (8 hunks, 3 removed / 28 added, shown by `xtask gate help-surfaces --diff`): `armed`'s doc against ProbeIo's read/write/flush and the platform — macOS answers setsockopt SO_RCVTIMEO with EINVAL once the peer has closed while its sent bytes stay readable (probed with a socketpair: EINVAL, then the buffered bytes, then ENOTCONN on shutdown), and `armed` passes only an OS-raised InvalidInput on an apple target, so std's own zero-duration refusal (never reached: ProbeDeadline::remaining is never zero) and every other platform keep the error; `abandon_socket`'s doc against its body (bind, drop, then a 10 s bounded poll until connect is ECONNREFUSED; off unix it returns at once, as the dead-alias test's unix-only check did); the new hang-up test's doc and literals against the test (the peer writes `ERR auth` and is joined before the read); the mock's dropped `finish mock reply` literal (its half-close is now best-effort). No usage or help text a user reads changed; no claim contradicted",
    ),
    (
        "crates/aterm-dev/src/main.rs",
        "2eb70f54c29b309b",
        "2026-09-18",
        "read against its parser/dispatch by the 2026-09-10 sweep; findings fixed in 412cf3acd; AND 2026-09-18 re-read of the `ship` passthrough's help and doc text (the driver ladder `$CARGO` → `aterm pkg which targo` → the store's current targo → a prefix-bound PATH targo → cargo, the `--version` lane sniff, `targo --unverified ship …`) against ship_driver/which_line_store_path/cargo_lane_args/run_ship in the same file, by the rust-free host-lane round; no slip found",
    ),
    (
        "crates/aterm-forge/src/cli.rs",
        "082ffd0b7dab9c05",
        "2026-09-14",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-forge); no claim contradicted the moved code; 2026-09-14 drift sweep (lane forge, 46 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code",
    ),
    (
        "crates/aterm-forge/src/lib.rs",
        "ac6752148aef0c5f",
        "2026-09-14",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-forge); the astream-classifier figures and the inclusion claim were false and are fixed here; 2026-09-14 drift sweep (lane forge, 46 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code",
    ),
    (
        "crates/aterm-gui/src/agent_identity.rs",
        "6d35ebeb3db0a6c2",
        "2026-09-22",
        "read in full against its handlers on 2026-09-17 by the round-18 identities surfaces pass: the module doc, parse_name's refusals, the ensure/ensure_in/restorable/carry_claude_hooks docs, and the `identities` verb's reply table (IDENTITIES_USAGE, cmd_identities/identities_reply and the list/one/forget arms) against the code beneath each and control_verbs' `identities` row — every reply the doc names is produced by exactly one arm and pinned by the four identities_* tests; `identities <name>` answers `OK <1+agents>` because aterm-ctl streams exactly n lines; 2026-09-17 read of the review-fix prose (--diff matched at 74d637316): parse_name's `forget` refusal (after the fold, so FORGET and Forget too; forget2 and forgetful pass) against its body, ensure's create=false clause against ensure_in's identity_dir early return (symlink_metadata + is_dir, the verb's own predicate), the `existing` twin's doc against restore_terminal_leaf's bootstrap_wears_leaf_identity check and main_entry's first_leaf_identity read (lib.rs), restorable's stderr line, and the two REVIEW test docs against the shapes they pin; no claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the module doc and `ensure_in` against the code: no hook block is carried into an identity (the carry, HOOK_MARK and its writer are gone). No claim contradicted",
    ),
    (
        "crates/aterm-gui/src/cli.rs",
        "a911a126e7937f87",
        "2026-09-23",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-gui-2); no claim contradicted the moved code; 2026-09-13 leftovers lane (89e3292d1, shown by `xtask gate help-surfaces --diff`: 1 hunk, 2 template lines rewritten as 5): the STARTER_CONFIG comments on `allow_notifications` (OSC 9/99/777; macOS delivers through terminal-notifier if installed, else osascript, a subprocess under aterm's identity) read against notify.rs deliver (Command terminal-notifier .status(), Err → the osascript `display notification` fallback; both subprocesses of aterm) and `allow_osc52_query` (a program's OSC 52 READ, answered only when on; on macOS 26 that read raises the system's \"aterm would like to paste from …\" alert) read against spawn.rs's ClipboardOperation::Query arm (reached only through a minted ClipboardQueryCapability, answers control::pbpaste — an in-process NSPasteboard read, clipboard.rs pbpaste); no claim contradicted; 2026-09-13 release-candidate read of the ONE prose line 47d9a1560 moved (the rainbow-gap merge, arriving on origin/main): the sample config's `cursor_trail_intensity` comment now reads `1.0` where it read `0.7`, and the comment states a DEFAULT, so it was checked against the code that supplies one — app_config.rs `cursor_trail_intensity_or_default` is `self.cursor_trail_intensity.unwrap_or(1.0)`, clamped to 0.0..=1.0 with a non-finite value failing OFF to 0.0, so the new literal is the default an unset key really gets and the old one had rotted; the remaining `intensity: 0.7` in this crate is a `resolve_cursor_glow` test fixture, not a default; the stated range `0.0..=1.0` still matches the clamp; 2026-09-13 read of the ONE line e61ee4b2b moved (the September 13 integration with the comet and the vivid rail): the `cursor_trail_style` roster in the sample config gained `rainbow kitty flat (the A/B control: the flat body of 2026-09-13, before the comet body and its vivid rail; aliases \"rainbow flat\"/\"flat rainbow\"/\"nyan flat\")`, checked against prefs.rs — the canonical name is in the style roster, the three aliases map to it in the alias table, and it resolves in the style match — so every spelling the line offers is one a config can actually set; no other style row changed; 2026-09-14 the prose moved by a TEST added here, not by a help claim — `aterm help config` states how many keys `--write-config` ships and that number lives in aterm-cli, which cannot read this private const, so `starter_config_key_count_matches_the_manual` counts STARTER_CONFIG's distinct commented `key =` names (table-scoped included, the same thing a reader counts in the written file) and reds when they move, naming the manual line to move with it; the count was measured at 159 against the const and the manual's `158` was the stale copy; 2026-09-15: the commented `# [machine]` starter block read against app_config.rs MachineConfig (two keys, both defaults act; [key_sequences] stays last); 2026-09-14 v0.86 candidate: the starter config's own prose is unchanged — what moved here is its two TEST pins, the key count (159 -> 161, together with the manual's line) and the `aterm pkg machine apply` disclosure check, which now reads the command the way the comment's wrap actually carries it because reflowing the block to keep the command on one line adds a Settings category (measured: `category_layout_matches_grouping_table` goes red). No claim about what the file tells a user changed; AND UPSTREAM'S READ OF THE SAME PROSE, kept: re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-gui-2); no claim contradicted the moved code; 2026-09-13 leftovers lane (89e3292d1, shown by `xtask gate help-surfaces --diff`: 1 hunk, 2 template lines rewritten as 5): the STARTER_CONFIG comments on `allow_notifications` (OSC 9/99/777; macOS delivers through terminal-notifier if installed, else osascript, a subprocess under aterm's identity) read against notify.rs deliver (Command terminal-notifier .status(), Err → the osascript `display notification` fallback; both subprocesses of aterm) and `allow_osc52_query` (a program's OSC 52 READ, answered only when on; on macOS 26 that read raises the system's \"aterm would like to paste from …\" alert) read against spawn.rs's ClipboardOperation::Query arm (reached only through a minted ClipboardQueryCapability, answers control::pbpaste — an in-process NSPasteboard read, clipboard.rs pbpaste); no claim contradicted; 2026-09-13 release-candidate read of the ONE prose line 47d9a1560 moved (the rainbow-gap merge, arriving on origin/main): the sample config's `cursor_trail_intensity` comment now reads `1.0` where it read `0.7`, and the comment states a DEFAULT, so it was checked against the code that supplies one — app_config.rs `cursor_trail_intensity_or_default` is `self.cursor_trail_intensity.unwrap_or(1.0)`, clamped to 0.0..=1.0 with a non-finite value failing OFF to 0.0, so the new literal is the default an unset key really gets and the old one had rotted; the remaining `intensity: 0.7` in this crate is a `resolve_cursor_glow` test fixture, not a default; the stated range `0.0..=1.0` still matches the clamp; 2026-09-13 read of the ONE line e61ee4b2b moved (the September 13 integration with the comet and the vivid rail): the `cursor_trail_style` roster in the sample config gained `rainbow kitty flat (the A/B control: the flat body of 2026-09-13, before the comet body and its vivid rail; aliases \"rainbow flat\"/\"flat rainbow\"/\"nyan flat\")`, checked against prefs.rs — the canonical name is in the style roster, the three aliases map to it in the alias table, and it resolves in the style match — so every spelling the line offers is one a config can actually set; no other style row changed; 2026-09-14 the prose moved by a TEST added here, not by a help claim — `aterm help config` states how many keys `--write-config` ships and that number lives in aterm-cli, which cannot read this private const, so `starter_config_key_count_matches_the_manual` counts STARTER_CONFIG's distinct commented `key =` names (table-scoped included, the same thing a reader counts in the written file) and reds when they move, naming the manual line to move with it; the count was measured at 159 against the const and the manual's `158` was the stale copy; 2026-09-15: the commented `# [machine]` starter block read against app_config.rs MachineConfig (two keys, both defaults act; [key_sequences] stays last); 2026-09-15 integration read: the starter contains 161 distinct commented keys after adding both machine settings. CONFIG_PAGE and starter_config_key_count_matches_the_manual now name that measured count. The complete `aterm pkg machine apply` command stays on one starter-comment line, matching the CLI handler and its disclosure regression; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 read at the merge of feat/round-15-receipts over main of the prose f3a5467ed moved with no row (shown by `xtask gate help-surfaces --diff`, matched at 595e13613): HELP_TITLE split out of HELP_HEAD, which now opens on its blank line, and the --help format `{HELP_TITLE}{}\\n{HELP_HEAD}{}{HELP_TAIL}` — against parse_cli's `-h | --help` arm (the title, aterm_types::identity::ORIGIN_LINE, then the body with keys_help and HELP_TAIL, then WINDOWS_HELP_TAIL on Windows only); no claim contradicted; landing of the erase pending-wrap fix, over f3a5467ed: read the split-out HELP_TITLE, HELP_HEAD's new leading newline, the --help format string and its origin-line comment against the -h/--help print arm, aterm_types::identity::ORIGIN_LINE and its assembly test, keys_help, the man page NAME line, and the 1200-byte window of the Windows help-arm scan (now at 941 bytes), and no changed claim contradicted the code; the unchanged doc comment the commit moved onto HELP_TITLE still called the help text a single concat when it is five pieces joined at print time (HELP_TITLE, ORIGIN_LINE, HELP_HEAD, keys_help, HELP_TAIL), and is fixed in the same commit that records this read to say the text is kept in constants printed only by the --help arm, so a no-arg / Finder launch never touches them; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged file is byte-identical to origin/main's, whose recorded read (0bd2d71bd) already hashes to the merged prose; 2026-09-16 re-read of STARTER_CONFIG's cursor block against the keys it documents, after `cursor_trail_wake_ms` was retired: the commented line for it is deleted (the key is no longer a `Config` field and `native_config_language::RETIRED_CONFIG_KEYS` carries it, so a starter that still advertised it would be teaching a dead key), every surviving `cursor_trail*` line still names a live resolver (`cursor_trail_ms`/`_length`/`_intensity`/`_radius`/`_ring` -> the `*_or_default` methods on `app_config::Config`), and `starter_config_key_count_matches_the_manual` re-counts the file at 160 distinct keys, moved here and in `aterm help config` together; 2026-09-17 read of CHILD-SHELL ENV HYGIENE's one exception against agent_identity::env and aterm_pty::build_child_env's env_add-after-deny order; 2026-09-22 read of the ONE line this surface gained since the last read (shown by `xtask gate help-surfaces --diff`): the `load_adaptive_motion` comment now says decorative effects are shed under sustained render overload and that SMOOTH SCROLLING IS NEVER SHED. Checked against aterm-gui app_render.rs `motion_policy` (whose doc states functional motion must not consume the fold) and `effect_policy` (the exemption itself: an effect that is `load_shed_exempt` resolves without the latch), plus `load_shed_active_with` in app_config.rs. The claim holds; the rest of this surface is the earlier read's.; \
         2026-09-22 index-latency read: the STARTER_CONFIG auto_update comment matches packages_update_loop_enabled and spawn_pkg_update_check: a default six-hour full pass, with a published public index able to wake a successful park sooner; both config gates are sampled at launch; \
         2026-09-22 merge read: starter load_adaptive_motion line checked against app_render::load_shed_active, flip_shed_latch and the effect policy; smooth scrolling remains functional motion during a decorative shed; 2026-09-23 (Phase 3 pass work) read of the starter config's `[machine]` header against the new schedule: `machine apply` runs as the window opens (after its launch seed) and as each interactive session starts, and a package pass carries the table only after an edit (atpkg `machine_table_changed`); the `aterm pkg machine apply` applies-now line the test pins is unchanged; 2026-09-23 p3/reconciled merge read (p3/pass onto main): STARTER_CONFIG carries main's auto_update line (a six-hour full signed check a published public index can wake sooner) and p3/pass's `[machine]` header (applied as the window and each session start, and by the next pass after an edit); disjoint lines, each read above against its handler, neither contradicts the other; 2026-09-23 p3/reconciled read (shown by `xtask gate help-surfaces --diff`): the starter config's `[machine]` header now says the settings apply as the window opens, by a terminal session once a day, and by the next pass after an edit — read against spawn_machine_settings_once, aterm main.rs machine_apply_due and atpkg machine_table_changed; the auto_update line (a full signed check every 6 hours, a published index waking it sooner) is unchanged and still true of the machine-wide walk — consistent; 2026-09-23 Phase 4 p4/settings read: STARTER_CONFIG's `enabled`/`auto_update` comments now say the window reads both LIVE (`LiveSwitch` in `spawn_pkg_update_check`, re-read on an mtime/length change each `PARK_SLICE`) and name packages.log beside aterm.log (`atpkg::packages_log::log_path` over `aterm_types::dirs::logs_dir`) — consistent; 2026-09-23 (Phase 4, one path) re-read of the starter file's `[packages]` block against atpkg `config.rs`: `enabled` (Automatic updates, `PackagesConfig::enabled`), `auto_install` (the one install consent, default true, `PackagesConfig::auto_install`), `exclude`, a checkout-path `[packages.links]` dev link (the owner/repo form is a development setting `admit_packages` drops), and the new `[reroute] announce` (`RerouteConfig::announce`); the retired `auto_update`/`seed_install`/`channel`/`include` and the environment-override sentence are gone.; 2026-09-23 (Phase 4, one path, second read): the starter-file key-count pin now says 155 — the template read above, counted by `starter_config_key_count_matches_the_manual`, and the same number the manual's CONFIG page states.; 2026-09-23 merge read (p4/integrate: p4/settings merged with p4/onepath): STARTER_CONFIG's `[packages]` block carries onepath's keys (enabled, auto_install, exclude, the checkout-path-only `[packages.links]`, `[reroute] announce`) with settings' LIVE wording on `enabled` — read against the merged `spawn_pkg_update_check`, whose `LiveSwitch` polls `aterm.toml` per `PARK_SLICE` and gates on `atpkg::config::PackagesConfig::enabled` (the retired `auto_update` folded in), and `atpkg::packages_log::log_path` beside `aterm.log` — and the template's retired `auto_update` line is gone, as onepath had it — consistent; 2026-09-23 read of the prose 2aa8e689d moved (shown by `xtask gate help-surfaces --diff`, matched at 1dd854025: 1 hunk, 2 lines removed / 5 added) — the Headless variant's doc, now `on macOS the process cannot be activated either (no Dock tile, never the front app; launch_posture in lib.rs), so a harness may boot one beside a human who is typing` — read against lib.rs's `launch_posture` (EXISTS, :6568; headless → AppActivation::Prohibited, activate_on_launch false), `apply_launch_posture` (:6585) applied to the app's builder at :34082, and headless_activation.rs's live proof (activationPolicy and frontmostApplication read off the real process); the named cross-reference resolves; no claim contradicted",
    ),
    (
        // Linux rebase read, 2026-09-23: cmd_update retains the owner-only check
        // and apply fence. Linux applies synchronously under the updater lock;
        // macOS posts its handoff request. Native status fields match State,
        // and check_settings_provider admits the compiled-channel/settings policy.
        "crates/aterm-gui/src/control.rs",
        "e82a1ad703ba1cc7",
        "2026-09-24",
        "2026-09-23 (lane E fix): read the supervisor-claim test's new strings (`unset supervisor holder=` and `holder=a b` as usage errors; `unset supervisor holder=sup-a` answering `OK` and leaving sup-b's claim, `holder=sup-b` clearing it, the bare unset clearing whichever holder's) against cmd_meta_supervisor's unset arm and session_timeline::release_supervisor_held_by. No claim contradicted; 2026-09-24: lane-C review-fix read — the control_query re-export gaining ScreenGen/screen_gen, the fenced-write dispatch comments renamed to if-gen= (the licence exemption keyed on if=/if-gen=/if-fp=, the `send`/`key` arm naming the screen generation), the InputFence literal's guard_at_cursor=false for key/send, and the new tests' doc prose (the re-entry swap with the per-grid seq repeat as its negative control, the transcript-caret guarded submit with the any-row scan as its negative control, `pressed=1` on the verdict) against control_input::InputFence::check, control_session::guarded_submit and the stamp the dispatch appends; built and green at record time (targo --unverified test -p aterm-gui --lib: 5232 passed). No claim contradicted; 2026-09-24: read the lane-C stage-2 prose against its handlers — the fenced-write dispatch comments (the licence exemption now keyed on any of if=/if-seq=/if-fp=, the `send`/`key` arm that routes every armed InputFence to cmd_key_fenced/cmd_send_fenced under one terminal lock hold), the `meta` arm's supervisor hand-off (cmd_meta_supervisor answered first, with serving_connection()), SERVING_CONNECTION/CONNECTION_CLAIMED/ConnectionScope/release_connection_claims against serve_borrowed (entered at its top, so it covers the bridge lane and ends when the lane stops serving requests, a subscribe hand-off included) and release_supervisor; the new test strings (OK skipped reason=changed, ERR busy supervisor=, the `OK 0 turn skipped reason=guard` verdict, attention_owner=/attention_owners=/supervisor= on the meta line) against the code that prints them; built and green at record time (targo --unverified test -p aterm-gui --lib). No claim contradicted; 2026-09-21 read of the one added string (shown by `gate help-surfaces --diff`): ` apply_phase={phase}\\n` appended to the update-status OK line — read against `update_control::Snapshot::apply_phase` (Some only while `apply_posture` answers automatic or automatic-idle, from `App::automatic_apply_phase` for the staged build; None for every other posture, so no phase is promised for a lane that will not apply by itself) and the append site in `update_status`, which trims the newline and adds the token before `apply_policy_reason=`; the verb's long help in control_verbs.rs names the same four values; no claim contradicted; re-read on 2026-09-18 for the v0.88.0 assembly: the change since 2026-09-17 is 668119edd (a dead-peer verdict rests on a peer no inherited descriptor can revive) — `kill_peer_in_kernel`, the socketpair/dup2 subjects and the stranger-holds-the-read-end negative control, plus its doc; every changed literal sits inside `mod tests` (from line 9666), so no control-verb help text moved; read by diffing the surface (`gate help-surfaces --diff`) and locating each changed string; the earlier reads this row carried still hold; 2026-09-17 round-19 read: dispatch_story_verb's doc and its three refusal strings (`ERR denied`, audited, for an edge token; `ERR no such session`; `ERR story: no session (name one: @<sid> story …)`), the two `story` hand-off comments in dispatch_before_session and handle, and the WriteInput-set comment — against the function (is_owner_class first; Local / Sid / self_session resolution; control_query::cmd_story) and both call sites (before the self-only gate, like hold; the early dispatch hands over only a selector-named session and returns None for the bare form, which handle resolves). One slip fixed before this row: the doc said the bare form is refused on the terminal-less path, where it falls through to handle. The function sits after FABRIC_USAGE so the hold audit span still counts three log_denial calls (the_hold_refusals_are_audited_from_both_dispatch_entries); 2026-09-19 read of the prose the round-19 second fix added (shown by `xtask gate help-surfaces --diff`): the cfg(test) re-export's doc (`cmd_turn` and `TurnIo` for app_presence's driver-attribution tests) and `turn_driver`'s doc — the edge-scoped caller is the source its presented token was granted to in the RESOLVED target's table, the lookup `caller_actor` makes, `None` for Owner-class scopes — read against `turn_driver` (Scope::Edge → ctx.edges.src_of(&presented).cloned(); Owner | Bridge → None) and its one call in the `turn` arm before the three routes; no claim contradicted; 2026-09-19 read of the story verb's two dispatch sites (a selector-named session before the self-only gate, the bare form through handle with the connection's session), dispatch_story_verb's owner-class refusal and its None arm, and turn_driver (Edge: src_of on the resolved table; Owner/Bridge: None); 2026-09-20 (round 21 review) read of the `lease` rationale block against what survives. It justified the printable-ASCII holder rule partly by the bridge's §6.6 row-5 mirror and `Bridge::local_lease_holder`, both cut in round 21: read against the LOCAL half that is untouched — who `lease` lets a caller claim to be, and what `who`/`lease status` then show — which is what the rule is actually for. The bus half is now marked as history. No claim contradicted; 2026-09-21 read of the prose 367d85b9e added and of the mouse-arm fix landing with this row and its pin test (seven hunks, 100 lines, shown by `xtask gate help-surfaces --diff`, matched at 2524398f6): the two refusal tests and their docs — an_unknown_key_mods_name_is_refused_not_dropped (seven bad `mods=` spellings must answer None from take_mods and parse_key; a fourteen-entry control list — the ten known names, both separators, the bare `mods=` and no token — must still parse) and an_unknown_mouse_mods_name_is_refused_not_dropped (`mods=CTRL|cmd|super|hyper|shfit` must be Err from parse_mouse; shift+ctrl+alt, meta, option, control, the empty list and no token must be Ok) — plus the re-worded expect strings in take_mods_is_additive and take_mods_parses_meta_and_hyper_distinctly, read against control_input's take_mods (now Option: the ten known names, `\"\"` → empty, `_ => return None`, `+` and `,` both split), parse_key (`take_mods(rest)?`), take_inline_mods (`_ => break` on `CTRL`, so `key CTRL+a` keeps a six-char body that neither named_key_from_token nor the single-char arm accepts, and cmd_key answers KEY_USAGE), take_event_type / take_base_layout (each `_ => return None`), parse_mouse (shift / alt|option|meta / ctrl|control / `\"\"`, else `ERR bad modifier … shift|alt|meta|ctrl only`) and aterm-types' SHIFT_MASK/ALT_MASK/CTRL_MASK, with the before-claims (`OK` and a bare key, an unmodified press) matching the `_ => {}` arms the commit removed; the mouse doc's `refuse and say what it can carry` held at 367d85b9e for cmd_mouse (`Err(e) => e`) and cross_mouse (cross_mouse_apply's `?`) but not for the front-routed cross arm (`mouse` if is_cross && targets_front), which `.ok()`'d the Result and answered MOUSE_USAGE — closed in this same commit: that arm now matches the Result (`Err(refusal) => refusal`), and the_front_routed_cross_mouse_arm_forwards_the_parsers_refusal pins it on the dispatch source beside cross_session_key_arms_answer_the_key_usage_line (its strings and doc are the two later hunks), read against the arm itself, front_routed_input's `&EventLoopProxy<Wake>` parameter, the single production occurrence of each split anchor, and parse_mouse's `press left 4 4 mods=cmd` → `ERR bad modifier` ≠ MOUSE_USAGE; built and green at record time (`targo test -p aterm-gui --lib`: the two arm pins and the two refusal tests, 4 passed); no claim contradicted; 2026-09-22 round-22 read of the `subscribe` surfaces against `run_subscribe_with_peer_probe` and `Requested::parse`: USAGE now spells the stream vocabulary and the two `mail:` parameters on its own line, and the one-line menu entry names `mail` — it had to be FOLDED back to a single line, because the short catalog is bounded by SHORT_CATALOG_MAX_BYTES and had two bytes of headroom (`help_short_form_is_bounded_and_is_the_summary_catalog` measured 10243/10240). No claim contradicted — and the `ERR usage: streams are a subset of …` refusal, which enumerates the vocabulary and had not been updated; a temp-instance run of the new verb is what caught it, which is what a live read is for; 2026-09-22 round-22 REVIEW re-read: the `subscribe` refusal is no longer typed out beside the vocabulary it enumerates — it is BUILT from `aterm_types::control_verbs::SUBSCRIBE_STREAMS` and pinned by `the_subscribe_refusal_names_every_stream`, after spending a round telling operators that `mail` did not exist in the build that had it with no test to notice. The `⊆ {…}` doc comment names the same constant. The stale `writes a single ERR line` remark is corrected (the usage reply is two lines). No claim contradicted; 2026-09-22 (2) re-read after a reviewer found a SECOND hand-typed stream list — a `const USAGE` served on four of the five refusal paths, with the names in a different order — while the derived one reached only the fifth. There is now ONE `subscribe_usage_bytes`, built from `SUBSCRIBE_STREAMS` and served on all five, and the test asserts the printed list IS the vocabulary byte for byte plus a LITERAL roster of the streams this build serves (iterating the vocabulary cannot catch a deletion from it — removing `mail` removed the assertion that would have caught removing `mail`, which is why the first version of this test stayed green). The `⊆ {…}` doc comment names the constant. No claim contradicted; 2026-09-22 (round 23, broadcast) read of the three strings this round changed against their handlers. `dispatch_topic_verb`'s doc and its audited refusal were read against the function itself and its TWO dispatch entries — `dispatch_before_session`'s selector arm (a `@<sid>` or `@<local>` resolves with no terminal; the bare form returns None for `handle` to resolve) and `handle`'s owner-only block, both BEFORE the self-only gate, exactly as `hold` and `story` are — so `@<sid> topic add` is not refused as a redirected self-only verb; the Owner-class claim is `is_owner_only(\"topic\")` reading the table's `Access::OwnerOnly`, and `Scope::is_owner_class` admits Bridge, which is what lets the bridge poll `topic ls` on its roster round. The `subscribe` usage line now names `mail:topic=<t>` beside the other two `mail:` parameters, read against `subscribe::parse_mail_params`; `the_subscribe_refusal_names_every_stream` now asserts a LITERAL roster of the three parameters as well as the stream vocabulary, for the reason the 2026-09-22 (2) entry above gives — iterating what the refusal prints cannot catch a deletion from what it prints. `topic` was added to the pinned None (OpClass::Owner) set of `required_op_classifies_each_verb`. No claim contradicted; 2026-09-22 (round-23 review) re-read of the `topic` dispatch tests after the add serial: `ls` rows carry `serial=<n>`, a re-add mints a fresh serial, `since=@+5` is a usage error, and an adopted session answers its carried set through `dispatch_topic_verb` (an_adopted_session_answers_the_carried_topics). No claim contradicted; 2026-09-22 (round 24, item 1) re-read of the `topic` dispatch tests after the serial's removal: `ls` rows are `topic <t> since=<s>`, every add-that-added and drop-that-dropped is asserted as a `topic` timeline record in order with its `since=`, and the adopted session's carried set is two-field rows. No prose beyond the tests changed. No claim contradicted; 2026-09-22 (round 24, item 2) read of `FABRIC_USAGE`'s new `fabric retire <sid>...` form and `cmd_fabric_retire`'s doc against the function, `dispatch_fabric_verb`'s Owner-only gate (a `Scope::Bridge` or edge caller and any selector are refused before it), `SessionStore::request_retire` (a hosted sid answers `false`, so `ERR hosted <sid>` is answered before anything is requested) and `drain_session_events`' `EVENT * fabric-retire <sid>` frame: `OK requested=<n>` counts the rows that were journaled. No claim contradicted; merged with upstream's own read of the same day: 2026-09-22 SOUND lane: the only strings that moved sit inside `mod tests` - the three new `tone` row assertions and the `sound_seam: true` field added to the existing `TrailStatus` fixture. No control-verb help text and no refusal string changed; checked with `gate help-surfaces --diff`. PRIOR READS, ALL STILL STANDING: 2026-09-21 read of the one added string (shown by `gate help-surfaces --diff`): ` apply_phase={phase}\\n` appended to the update-status OK line — read against `update_control::Snapshot::apply_phase` (Some only while `apply_posture` answers automatic or automatic-idle, from `App::automatic_apply_phase` for the staged build; None for every other posture, so no phase is promised for a lane that will not apply by itself) and the append site in `update_status`, which trims the newline and adds the token before `apply_policy_reason=`; the verb's long help in control_verbs.rs names the same four values; no claim contradicted; re-read on 2026-09-18 for the v0.88.0 assembly: the change since 2026-09-17 is 668119edd (a dead-peer verdict rests on a peer no inherited descriptor can revive) — `kill_peer_in_kernel`, the socketpair/dup2 subjects and the stranger-holds-the-read-end negative control, plus its doc; every changed literal sits inside `mod tests` (from line 9666), so no control-verb help text moved; read by diffing the surface (`gate help-surfaces --diff`) and locating each changed string; the earlier reads this row carried still hold; 2026-09-17 round-19 read: dispatch_story_verb's doc and its three refusal strings (`ERR denied`, audited, for an edge token; `ERR no such session`; `ERR story: no session (name one: @<sid> story …)`), the two `story` hand-off comments in dispatch_before_session and handle, and the WriteInput-set comment — against the function (is_owner_class first; Local / Sid / self_session resolution; control_query::cmd_story) and both call sites (before the self-only gate, like hold; the early dispatch hands over only a selector-named session and returns None for the bare form, which handle resolves). One slip fixed before this row: the doc said the bare form is refused on the terminal-less path, where it falls through to handle. The function sits after FABRIC_USAGE so the hold audit span still counts three log_denial calls (the_hold_refusals_are_audited_from_both_dispatch_entries); 2026-09-19 read of the prose the round-19 second fix added (shown by `xtask gate help-surfaces --diff`): the cfg(test) re-export's doc (`cmd_turn` and `TurnIo` for app_presence's driver-attribution tests) and `turn_driver`'s doc — the edge-scoped caller is the source its presented token was granted to in the RESOLVED target's table, the lookup `caller_actor` makes, `None` for Owner-class scopes — read against `turn_driver` (Scope::Edge → ctx.edges.src_of(&presented).cloned(); Owner | Bridge → None) and its one call in the `turn` arm before the three routes; no claim contradicted; 2026-09-19 read of the story verb's two dispatch sites (a selector-named session before the self-only gate, the bare form through handle with the connection's session), dispatch_story_verb's owner-class refusal and its None arm, and turn_driver (Edge: src_of on the resolved table; Owner/Bridge: None); 2026-09-20 (round 21 review) read of the `lease` rationale block against what survives. It justified the printable-ASCII holder rule partly by the bridge's §6.6 row-5 mirror and `Bridge::local_lease_holder`, both cut in round 21: read against the LOCAL half that is untouched — who `lease` lets a caller claim to be, and what `who`/`lease status` then show — which is what the rule is actually for. The bus half is now marked as history. No claim contradicted; 2026-09-21 read of the prose 367d85b9e added and of the mouse-arm fix landing with this row and its pin test (seven hunks, 100 lines, shown by `xtask gate help-surfaces --diff`, matched at 2524398f6): the two refusal tests and their docs — an_unknown_key_mods_name_is_refused_not_dropped (seven bad `mods=` spellings must answer None from take_mods and parse_key; a fourteen-entry control list — the ten known names, both separators, the bare `mods=` and no token — must still parse) and an_unknown_mouse_mods_name_is_refused_not_dropped (`mods=CTRL|cmd|super|hyper|shfit` must be Err from parse_mouse; shift+ctrl+alt, meta, option, control, the empty list and no token must be Ok) — plus the re-worded expect strings in take_mods_is_additive and take_mods_parses_meta_and_hyper_distinctly, read against control_input's take_mods (now Option: the ten known names, `\"\"` → empty, `_ => return None`, `+` and `,` both split), parse_key (`take_mods(rest)?`), take_inline_mods (`_ => break` on `CTRL`, so `key CTRL+a` keeps a six-char body that neither named_key_from_token nor the single-char arm accepts, and cmd_key answers KEY_USAGE), take_event_type / take_base_layout (each `_ => return None`), parse_mouse (shift / alt|option|meta / ctrl|control / `\"\"`, else `ERR bad modifier … shift|alt|meta|ctrl only`) and aterm-types' SHIFT_MASK/ALT_MASK/CTRL_MASK, with the before-claims (`OK` and a bare key, an unmodified press) matching the `_ => {}` arms the commit removed; the mouse doc's `refuse and say what it can carry` held at 367d85b9e for cmd_mouse (`Err(e) => e`) and cross_mouse (cross_mouse_apply's `?`) but not for the front-routed cross arm (`mouse` if is_cross && targets_front), which `.ok()`'d the Result and answered MOUSE_USAGE — closed in this same commit: that arm now matches the Result (`Err(refusal) => refusal`), and the_front_routed_cross_mouse_arm_forwards_the_parsers_refusal pins it on the dispatch source beside cross_session_key_arms_answer_the_key_usage_line (its strings and doc are the two later hunks), read against the arm itself, front_routed_input's `&EventLoopProxy<Wake>` parameter, the single production occurrence of each split anchor, and parse_mouse's `press left 4 4 mods=cmd` → `ERR bad modifier` ≠ MOUSE_USAGE; built and green at record time (`targo test -p aterm-gui --lib`: the two arm pins and the two refusal tests, 4 passed); no claim contradicted; 2026-09-22 round-22 read of the `subscribe` surfaces against `run_subscribe_with_peer_probe` and `Requested::parse`: USAGE now spells the stream vocabulary and the two `mail:` parameters on its own line, and the one-line menu entry names `mail` — it had to be FOLDED back to a single line, because the short catalog is bounded by SHORT_CATALOG_MAX_BYTES and had two bytes of headroom (`help_short_form_is_bounded_and_is_the_summary_catalog` measured 10243/10240). No claim contradicted — and the `ERR usage: streams are a subset of …` refusal, which enumerates the vocabulary and had not been updated; a temp-instance run of the new verb is what caught it, which is what a live read is for; 2026-09-22 round-22 REVIEW re-read: the `subscribe` refusal is no longer typed out beside the vocabulary it enumerates — it is BUILT from `aterm_types::control_verbs::SUBSCRIBE_STREAMS` and pinned by `the_subscribe_refusal_names_every_stream`, after spending a round telling operators that `mail` did not exist in the build that had it with no test to notice. The `⊆ {…}` doc comment names the same constant. The stale `writes a single ERR line` remark is corrected (the usage reply is two lines). No claim contradicted; 2026-09-22 (2) re-read after a reviewer found a SECOND hand-typed stream list — a `const USAGE` served on four of the five refusal paths, with the names in a different order — while the derived one reached only the fifth. There is now ONE `subscribe_usage_bytes`, built from `SUBSCRIBE_STREAMS` and served on all five, and the test asserts the printed list IS the vocabulary byte for byte plus a LITERAL roster of the streams this build serves (iterating the vocabulary cannot catch a deletion from it — removing `mail` removed the assertion that would have caught removing `mail`, which is why the first version of this test stayed green). The `⊆ {…}` doc comment names the constant. No claim contradicted; 2026-09-22 (round 23, broadcast) read of the three strings this round changed against their handlers. `dispatch_topic_verb`'s doc and its audited refusal were read against the function itself and its TWO dispatch entries — `dispatch_before_session`'s selector arm (a `@<sid>` or `@<local>` resolves with no terminal; the bare form returns None for `handle` to resolve) and `handle`'s owner-only block, both BEFORE the self-only gate, exactly as `hold` and `story` are — so `@<sid> topic add` is not refused as a redirected self-only verb; the Owner-class claim is `is_owner_only(\"topic\")` reading the table's `Access::OwnerOnly`, and `Scope::is_owner_class` admits Bridge, which is what lets the bridge poll `topic ls` on its roster round. The `subscribe` usage line now names `mail:topic=<t>` beside the other two `mail:` parameters, read against `subscribe::parse_mail_params`; `the_subscribe_refusal_names_every_stream` now asserts a LITERAL roster of the three parameters as well as the stream vocabulary, for the reason the 2026-09-22 (2) entry above gives — iterating what the refusal prints cannot catch a deletion from what it prints. `topic` was added to the pinned None (OpClass::Owner) set of `required_op_classifies_each_verb`. No claim contradicted; 2026-09-22 (round-23 review) re-read of the `topic` dispatch tests after the add serial: `ls` rows carry `serial=<n>`, a re-add mints a fresh serial, `since=@+5` is a usage error, and an adopted session answers its carried set through `dispatch_topic_verb` (an_adopted_session_answers_the_carried_topics). No claim contradicted; 2026-09-22 merge read (round 24 + v0.91.0): `fabric retire <sid>...` usage, ERR hosted / OK requested= replies, and the topic ls fixtures without the serial read against cmd_fabric_retire and cmd_topic — consistent; 2026-09-24 final-integration merge read (harness/integrate over origin/main at 423f461e1): the hunks `--diff` shows are all main's Linux-update prose (the `update` arm's `linux` replies, the linux_* status fields, the Owner-only predicate's and the cached-facts docs' macOS/Linux qualifiers), read against the update arm and update_is_owner_only_subcmd in the merged file; lane C's fenced-write and supervisor prose is unchanged by the merge; no claim contradicted; 2026-09-24 final-integration merge read (lane E over main and B2): the only hunks `--diff` shows are lane E's supervisor-claim test strings (`unset supervisor holder=`, the sup-a/sup-b/sup-c exchange), read above by E's own clause against cmd_meta_supervisor and release_supervisor_held_by; main's Linux-update prose and lane C's fence prose are as the earlier reads left them; no claim contradicted; 2026-09-24 read of the prose eeb49f84f (a post wakes its session's watchers) adds, merged with upstream on land/quality-round-2: the `post` doc and the framed form's line read against post_waking_watchers (control.rs), which hands crate::fabric::cmd_post_waking a wake that notifies the session's subscribers when any exist, and against both call sites — the framed form (Some(body)) and the inline form (None) — so `events` watchers drain the post record at once, the meta/topic rule; 2026-09-24 read (origin/main merged into ux/status-reporting, shown by `gate help-surfaces --diff`: 3 hunks, 10 lines removed, 20 added) of main's Linux-delivery prose (d0a4c2930; main's own read is the plain comment above this row), against cmd_update: `check` is check_now_with_settings through update_control::check_settings_provider (the `ERR cannot read current update source` refusal left with the source read); Linux `apply` answers `OK {message}` / `ERR {error}` from aterm_update::linux::apply synchronously and posts no Wake, every other platform still posts Wake::ApplyStagedUpdate and acknowledges the request; the status line gains ` linux_installed_build= linux_staged_build= linux_trial= linux_trial_starts= linux_trial_healthy=` (`-` with no stage, `none` with no trial) only when `st.linux` is Some; update_is_owner_only_subcmd still fences `check`/`apply`; installed_update_facts is `None` off macOS, so the codesign/spctl/PlistBuddy probe spawns only on macOS as the TTL doc now says. One PLAIN comment inside cmd_update still says `apply` re-execs the process (not prose, not hashed). No claim contradicted; 2026-09-23 (resource hygiene) read of the prose this change adds (2 hunks, 14 doc lines and one string, shown by `gate help-surfaces --diff`): `sweep_dead_instance_files`' doc against its body (control_auth::sweep_stale_instances, then proxy::sweep_stale_graph) and its two callers (bind_control_listener's startup block, still gated on `plan.latest_link.is_some()`, and sweep_after_handoff); `HANDOFF_SWEEP_AFTER`'s doc against seamless::commit_and_exit (one write of the commit wire, then `_exit(0)` with no graceful cleanup); `sweep_after_handoff`'s doc against its body (a plan with no latest link returns; otherwise one `aterm-handoff-sweep` thread — the one added string — sleeps HANDOFF_SWEEP_AFTER, then sweeps the plan's directory and seamless::sweep_dead_handoff_leftovers) and its one call, lib.rs's `Wake::ActivateCommittedHandoff` arm. No control-verb help text or refusal string changed; 2026-09-24 merge read (ux/status-reporting onto origin/main 9533aed18): `gate help-surfaces --diff` over main's recorded read shows only this branch's own hunks, each read in the clauses above — consistent; 2026-09-24 merge read (origin/main dd2581889 into harness/integrate, shown by `gate help-surfaces --diff`: 4 hunks) — main's handoff sweep (`sweep_after_handoff`, `HANDOFF_SWEEP_AFTER`, the `aterm-handoff-sweep` thread) and the post that wakes its session's watchers (`post_waking_watchers` over `crate::fabric::cmd_post_waking`) are main's text, read by main's eeb49f84f clause in this row; this branch's supervisor-claim strings are unchanged; consistent; 2026-09-24 read of the new who test nonce expectation against control_session::cmd_who and sessions_lines: both format h.nonce.to_hex() from the registered SessionHandle snapshot, and who follows that nonce with fgpgid=<pid|->, the owner-only PTY foreground-group read that control_session::cmd_who makes; the unreadable test row ends fgpgid=-. No claim contradicted; 2026-09-24 merge read (origin/main 182e44a03 into harness/integrate): main's `who` presence test (nonce= then fgpgid=- as the last token) and this branch's supervisor-claim and fenced-press strings are disjoint hunks; both clauses above were re-read against the merged cmd_who and cmd_meta_supervisor. Consistent; 2026-09-24 merged Fabric batch read: sessions status is Owner-class only, reaches cmd_sessions_status in both production and generic dispatch, and other sessions arguments retain their roster or refusal behavior; checked against both match arms and control_session. Consistent",
    ),
    (
        "crates/aterm-gui/src/control_input.rs",
        "1e1e2f3ce4bb8dbc",
        "2026-09-24",
        "2026-09-24: lane-C review-fix read — KEY_USAGE's if-gen=<epoch>.<seq>, LeadingInputOptions::if_gen and take_leading_options' if-gen= arm (ScreenGen::parse; malformed and given-twice carried as refusals) and its if-seq= arm (recognized, taken off the tail and REFUSED, so a `send if-seq=` is never typed), InputFence's generation/guard_at_cursor docs against check() (screen_gen compared, then the fp, then the guard over RowRange::Span of the cursor row when guard_at_cursor), GuardedInput::Changed's doc, and input_if_row_matches' check-and-press doc moved onto input_if_fenced (the only caller of the removed wrapper was guarded_submit); the cfg(test) wrappers' doc links repointed. No claim contradicted; 2026-09-24: read the lane-C stage-2 prose against its handlers — KEY_USAGE's two new options, LeadingInputOptions::if_seq/if_fp and take_leading_options' two new arms (a value that does not parse carried as a refusal like a pattern-less if=, `given twice` refused), parse_fp (1..=16 hex digits), InputFence's doc and check() (seq, then fp over crate::control::screen_text hashed with turn_ledger::fnv1a_64 — the pair screen_stamp and `status hash=` print — then the guard), GuardedInput::Changed and guarded_input_reply's `OK skipped reason=changed`, input_if_fenced/cmd_key_fenced/cmd_send_fenced and the cfg(test) guard-only wrappers; operator_input_if_epoch is the shape named, not called. No claim contradicted; re-read against control.rs's cross-session `key` arms, post_input_reply and lib.rs's Wake::Input arm, input_if_row_matches, pty_idem KEYED_VERBS, cmd_scroll and parse_tab by the 2026-09-10 round-3 reader after the round-2 merge; the cross-session `key` usage (code, control.rs, pinned by a test), the `mouse` fire-and-forget claim, the `sole encoder caller` claim, GUARDED_VERBS' `no id=` claim, the `hello id=1` count, and the `scroll`/`tab` grammar lines fixed in the commit that updated this row; parse_tab's doc sentence still omits `close`/`move`; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim); 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — ONE slip fixed before this row: `key_arms_own_license`'s doc said anything MODIFIED (Ctrl/Alt/Super) answers false and keeps the fence, and a79895918 added a second arm returning true for a bare Ctrl-V (the delivered-insert class beside the gesture class). The sibling help in control_media.rs had been updated for that change; this doc had not, so a bare `key ctrl+v` skipped the licence pre-clear the doc promised; 2026-09-16 read of the one prose line a29417119 added — the ERR why reply in cmd_tab — against cmd_tab's Ok(Err(why)) arm and against the claim in the comment beside it that the aimed twin and the sibling @sid close verb already use this vocabulary: cmd_tab_aimed EXISTS (crates/aterm-gui/src/control_media.rs:2597, dispatched by control.rs's tab-if-is_cross arm through Wake::TabCmdAimed) and cmd_close (control_media.rs:2741) answers ERR for its own refusals, so the vocabulary claim holds; the reasons the arm forwards come from apply_tab_cmd_in's bounds checks and close_active_native_tab's Err arms; no claim contradicted; 2026-09-21 read of the prose 367d85b9e moved (shown by `xtask gate help-surfaces --diff`: 4 hunks, 1 removed / 13 added, all accounted for) — take_mods's refusal paragraph and its `\"\"` arm, parse_key's `mods=` added to its Returns-`None` line, parse_mouse's `\"\"` arm and the `ERR bad modifier` string — against take_mods (:122 `\"\" => Modifiers::empty()`, :123 `_ => return None`, its one non-test caller parse_key :319 and the control.rs tests all taking the Option), parse_key's three `?` (:319-321) and its residual-character `_ => None` (:344), take_inline_mods' case-sensitive table (:374 `_ => break`, so `key CTRL+a` still falls through named_key_from_token's `_ => return None` (:304) to KEY_USAGE via cmd_key :469 / cmd_key_guarded :709 / the cross arms control.rs:9260-9263), take_event_type :148 and take_base_layout :170's own `_ => return None`, hwkey::take_tokens' quoted rule (hwkey.rs:261-262, exact modulo the capital) and its `\"\" => 0` / `other => return Err` (hwkey.rs:277-278), the parent 3164e20c0's infallible `(Modifiers, String)` take_mods with its `_ => {}` arm for the `key a mods=CTRL`-answered-OK history, and parse_mouse's mods arm (:935-944) with the replies that carry its string — cmd_mouse :1114 `Err(e) => e` and cross_mouse_apply's `parse_mouse(rest)?` (control.rs:8306); pinned by an_unknown_key_mods_name_is_refused_not_dropped and an_unknown_mouse_mods_name_is_refused_not_dropped; no help surface advertises a `mods=` vocabulary for `mouse` beyond shift/alt (control_verbs.rs:1185 and ctl_help_full.txt:92 say `inject a mouse event`; HIERARCHICAL_SESSIONS.md:1270-1271 drive `mods=shift` / `mods=alt`, both still accepted), so the refusal contradicts none; `modifier convergence` is this paragraph's own name for the bytes_human_eq_controller seam (input.rs:886) and occurs nowhere else; ONE GAP found beside the mouse doc's `refuse and say what it can carry`: at 67462387c the `@<sid>`-targets-front `mouse` arm (control.rs:9307-9312) `.ok()`'d the Result and answered MOUSE_USAGE for every parse error, so the new string reached the wire on the flagless and background arms only — closed in the commit that records this row, which makes that arm match the Result (`Err(refusal) => refusal`) and pins it on the dispatch source by the_front_routed_cross_mouse_arm_forwards_the_parsers_refusal, so all three `mouse` arms answer the refusal by name; line numbers are 67462387c's, and that fix shifts control.rs by +6 after :9312; no claim contradicted",
    ),
    (
        "crates/aterm-gui/src/control_media.rs",
        "c7f0bfc1dafc8e8a",
        "2026-09-24",
        "re-read on 2026-09-13 by drift sweep of 2026-09-13 (aterm:aterm-gui); the Screen Recording contrast, the ERR enumeration and two trail rosters were stale and are fixed in that commit; 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-15 read of the new cmd_kitty doc against its body and the main-thread pair: the `Lines` framing claim against the catalog entry (v(\"kitty\", Write, Lines, App, ..)) and against the wear arm's `OK 1\n{row}\n`, the list arm's `OK {n}` + one row each; the usage refusals against the two `ERR usage:` arms; `a main-thread hop like trail/tone` against the two call_main sites and Wake::KittyCollection / Wake::KittyWear in lib.rs, whose handlers are App::kitty_collection_rows and App::wear_kitty_on_front; the `pins the cat that would ride ANYWAY` and `monotone, needs no unpin` claims against App::promotable_kitty and KittyLog::favourite_look/max_ts respectively. No claim contradicted; 2026-09-17 read of SPAWN_USAGE (`[identity=<name>|-]`), IdentitySpec/resolve_spawn_identity and the parse_spawn_args identity= arm's docs against the parser and the inherit/opt-out rule of 79ce005c3; 2026-09-17 read of the raise= prose (--diff matched at 49aefbcc7): SPAWN_USAGE's `[raise=<t|f>]`, the parse doc's `true|false` too, as `split=` takes `vertical`, and the REVIEW test doc against the parser's t|true and f|false arms beside v|vertical and h|horizontal, and the catalog's spawn summary (control_verbs.rs) saying the same letters; no claim contradicted; 2026-09-23 read of the Log route's four aliases (`logs`, `history`, `activity log`, `history log`) against parse_settings_section_route's arm and native_settings::SettingsRoute::Log (label `Log`, path `/log`, so `log` resolves through ALL), and the alias test's four new rows; `kitty log` and `diagnostics` still resolve to nothing; no claim contradicted; 2026-09-24 read (the ux/status-reporting merge, bdbd8be16), shown by `--diff` (2 hunks, 1 line removed, 5 added): parse_settings_section_route's log aliases — `log`, `logs`, `history`, `history log`, `activity log`, `message log`, `messages log` — now answer SettingsRoute::Messages, the one review page (the retired SettingsRoute::Log is gone with history_screen.rs), read against the arm and the video_parse_tests alias table, which pins (`log`, `logs`, `history`, `activity log`, `messages`) → Messages; `messages` itself resolves through the route's own label. No claim contradicted",
    ),
    (
        "crates/aterm-gui/src/control_privacy.rs",
        "66c8dd3372d66c59",
        "2026-09-24",
        "the row hashed a checkout behind 0a8275760/acc20c5fd/da5d75e33; re-read against parse_consent_timeout, covers_split and PrivacySnapshot::lines by the 2026-09-10 round-3 reader, the SERVICES doc (`never uncovered` vs NEVER_COVERED) fixed in 33311eda0; re-read against observer_fda_value/inert_fda_probe, ConsentState::fda -> ConsentCache::get_or_probe, app_settings.rs macos_access_projection and consent_policy by the round-3 fixer; the `live` field's `observer row's third value` claim (the row renders the inert labels as `off`, pinned at the fda=off/responsible=off asserts), the consent_panel_facts/session_consent `no syscall` claims (the cached probe's one open(TCC.db) on a miss), ConsentPanelFacts' `no covers= list` claim (the panel takes covers_split directly) and the consent_policy doc line displaced onto consent_probe_interval fixed in the commit that updated this row; the catalog's `covers=`/`uncovered=` split in control_verbs.rs still omits `unmeasured=` — that surface's own row; NOTE's doc now says its `covers is empty` clause is written for the UNMEASURED evidence read_privacy hard-codes and must turn evidence-conditional when §7 S4 lands (the measured arm of lines, tests-only today, renders a covers= list above it), the one-line fix for the round-3 reader's note finding; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim); 2026-09-13 read of the one prose change since (instance_attribution's doc, shown by `xtask gate help-surfaces --diff`) against instance_attribution (Attribution::Adopted iff App::handoff_successor, else Live; ConsentPolicy::adoption maps it to Unknown only when the policy is disabled), main_entry's `handoff_successor: adopting` (true whenever the boot re-adopted handed-off shells) and app_restore.rs take_session0_shell (None, so a fresh session 0, when window 0's layout has no terminal leaf, which leaves handoff_successor true); no claim contradicted; 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 read of the three literals f4988d1ae added (the target_os = macos cfg pair and the non-macOS `not-a-bundle` arm, shown by `xtask gate help-surfaces --diff`) against install_posture_rows, which_copy.rs's macOS-only `posture_from` re-export, bundle.rs InstallPosture::NotABundle (`nothing is wrong; there is simply no bundle`, the sentence the arm's comment quotes) and the PrivacyStatus.install doc that already lists `not-a-bundle` among its tokens; no slip found; 2026-09-14 read of the one sentence 6c6a65787 added to install_posture_rows's doc (canonicalized on unix only, shown by `xtask gate help-surfaces --diff`) against the cfg(unix) canonicalize in install_posture_rows, which_copy.rs observe's own cfg(unix) canonicalize and its comment on the verbatim spelling Windows answers, and the `install={} running={}` row that prints the path for a human; no slip found; 2026-09-14 (third audit lane, last merge): origin's 47198639f ('stabilize kitty motion and redraw trails') moved this file's prose (51 line(s)) without a row and this lane never touched the file; the union's hash is recorded at the merge, the read owed to that commit's author; 2026-09-15 read of 47198639f (12 hunks, 30 removed / 77 added, shown by `xtask gate help-surfaces --diff`) against covers_split, observed_fda_scope, app_data_covered, finish_privacy_read, PrivacyRead, session_consent and NOTE: app-data enters `covers=` iff fda=Granted and the scope is ThisProcess|NewProcesses, a Granted probe with scope Unknown is promoted to ThisProcess and never to NewProcesses, every other service stays `unmeasured` until fda_coverage_measured and NEVER_COVERED stays `uncovered` either way — the docs' new posture to the letter; the worker's wait has one deadline, Pending cannot extend it and a failed re-read keeps the last Pending snapshot (pinned by privacy_read_has_one_deadline_and_keeps_stuck_work_pending); PrivacyRead is `lines` plus a `pending` bool and the public rows are unchanged; session_consent still joins with SpikeEvidence::UNMEASURED and observed_eperm=false, so an adopted session inherits nothing (the derive model's NoInheritedGrant invariant); the one surface this change contradicted was the CATALOG's privacy row in control_verbs.rs (`so fda_scope=unknown`), fixed in the commit that updated this row; no claim in this file contradicted; 2026-09-15: install_posture_rows doc re-read after the Linux fix — off macOS the token is `not-a-bundle` by construction, and the unix-only canonicalize note from upstream is kept beside it 2026-09-15: re-read folder_names() against consent::Folder::ALL after the roster gained `app-data`; the `folder` row now carries a fourth name before the two volume classes, the format is unchanged, and the doc says what the new name is.; 2026-09-20 read of this round's changes against their handlers — the SERVICES roster gaining media-library/photos/app-bundles (all three also in NEVER_COVERED, because each is its own consent service with its own Settings pane that a Full Disk Access grant does not reach, so `uncovered` and never `unmeasured`), the `folder` row ceasing to be a string literal, the new folder_state/folder_source joins, and PrivacySnapshot's `folders`/`folder_source` fields — checked against covers_split, folder_names, PrivacySnapshot::lines and cmd_privacy_json. The row now renders what was observed: a warm-up row the owner asked for outranks the coverage rule (a warmed folder that answered EPERM reads `denied` while the grant is held), `covered-by-fda` appears only for the ONE service \u{a7}7 S4 measured on 2026-09-19 (app-data) and the four it could not isolate stay `unknown`, and `source=` names the provenance (none|warmup|fda|warmup,fda). This closes this row's own standing note that the NOTE doc's `covers is empty` clause must turn evidence-conditional when \u{a7}7 S4 lands — for app-data only: SpikeEvidence::fda_coverage_measured stays false on purpose, because it is one scalar gating five classes and only one was measured, so prompt_possible and fs_consent are unchanged. The pre-S4 tripwire no_folder_state_is_ever_inferred_from_the_grant fired on this commit and was NARROWED to the still-unmeasured five rather than deleted; same day, second pass: the `anchor=` token on the install row and its conditional note, read against aterm_containment::classify_image_anchor/ImageAnchor and PrivacySnapshot::lines — `anchor` is read FRESH on every report and never memoized, because the whole point is that it changes under a running process (an apply renames the bundle out from under it, then boot-health deletes it); Displaced/Deleted are the only two that set grant_unverifiable and the only two that add the second note row, while NotBundled (a dev run) and Unknown (a failed read) add nothing, which is the same refusal-to-over-claim the rest of this file keeps. The note is deliberately NOT folded into full_disk_access=, because the probe can legitimately answer `granted` in the broken state; 2026-09-22 read of this round's changes against their handlers — the claimant census (`claimants=`/`claimant `/the conflicting note in PrivacySnapshot::claimant_lines, the `census` object in cmd_privacy_json, ConsentState::claimants and ConsentProbes::census) checked against aterm_containment::consent's classify_claimants/Claimants::conflicting/sole_claimant: the summary carries the true totals and truncation cannot shrink them (MAX_CLAIMANT_ROWS with its own `truncated=` row), only CONFLICTING copies earn a line because a copy sharing the running requirement cannot cause the destructive resolution, `claimants=-` renders for BOTH unanswered states with `census=pending|off` distinguishing them, and `sole=` is sole_claimant() which only a COMPLETE enumeration may assert. The census takes the FDA probe's admission discipline exactly (one worker, retry_after floor, epoch-checked publication, invalidate() retires it) and runs codesign only on the worker — pinned by the_census_never_runs_on_the_reading_thread_and_is_cached_until_invalidated. Same pass: the grant_unverifiable note's closing sentence was CORRECTED — it claimed a process launched fresh from the installed bundle is unaffected, which the measured tccd behaviour refutes, and it now names that mechanism and points at the claimants= row; the identity readers now come from aterm_containment::consent; 2026-09-22 Linux merge read: only PrivacySnapshot.install documentation changes. install_posture_rows uses which_copy::posture_from on macOS, not-a-bundle for a resolved non-macOS executable, and unknown if executable resolution fails. All current main privacy observations, coverage and consent text retained.; 2026-09-23 read of the census rework against its handlers: the summary row gains `age_ms=` (claimant_lines renders the served answer's age), the census is served from its own slot until CENSUS_TTL and refreshed on a worker while the last answer is shown, ConsentState::invalidate no longer retires it, `census=off` covers a disabled privacy posture as well as an inert instance or no bundle id (claimants() returns (None,false) for all three), the JSON census carries the same seven keys in every state and caps `conflicts` at consent::MAX_CLAIMANT_ROWS with a `truncated` count, the claimant note is now pct-encoded free text, and the grant_unverifiable comment names the exec-time path rather than the refuted `follows the vnode`; no claim contradicted; 2026-09-24 re-read after the lint pass (doc-comment code spans, the census slot's doc once invalidate_census was dropped as uncalled): unchanged claims, no claim contradicted; 2026-09-24 read of the Trash-gesture round against its handlers: the claimant note's new clause (Settings > Security lists the copy and can move an ad-hoc or unsigned one to the Trash) matches native_settings' MacosAccess::retire_plan (conflicting copies Claimants::retirable offers, only with /usr/bin/trash present) and the MACOS_ACCESS_TRASH button drawn exactly when that plan is non-empty; ConsentState::invalidate_census's doc (a worker already out publishes nothing, the next read dispatches without the retry floor) matches the CensusSlot generation check in complete_census and the cleared retry_after, pinned by a_census_out_when_the_disk_changes_publishes_nothing; running_bundle_id_if_warm reads signing_identity_if_warm and never spawns codesign; no claim contradicted",
    ),
    (
        "crates/aterm-gui/src/control_query.rs",
        "d7b71d7aec37f3dd",
        "2026-09-24",
        "2026-09-24 second merge read (claude/unified-messages with origin/main after 5d4598a77): cmd_appnotice's doc takes main's sentence on whose the harness lane is — an agent harness outside the window, the window's own supervisor (`harness_host`) posting none — and keeps the branch's every-lane-a-record semantics, read against cmd_appnotice (the only reply arm is `OK recorded`); no other claim moved; 2026-09-24 merge read (claude/unified-messages with origin/main 6afab83df, the words stage): cmd_appnotice's doc is the one prose change — every lane is a record replying `OK recorded` (design ruling 147), the harness lane an `appstatus` entry of kind=harness — read against cmd_appnotice (USAGE names toolchain|update|harness; the only reply arm is `AppNoticeTaken::Recorded` → `OK recorded`) and lib.rs take_app_notice (all three lanes record, `harness` through record_harness_note); no other claim moved. Main's read of the same row, merged: the row predates ffda97813 (`text tail=`/`rows=`), which the round-2 merge carried; re-read against cmd_modes, cmd_cell, cmd_metrics/cmd_metrics_json by the 2026-09-10 round-3 reader and against text_args/TextShape::select/frame_rows_reply/cmd_text_opt by the round-3 fixer (that delta matches its code); the `modes` frame (`OK <n>` and twelve keys, not `OK` and seven — pinned by modes_frames_its_count_and_twelve_keys), the `cell` attrs vocabulary (`wide`/`wide_cont`, pinned by cell_attrs_carry_the_width_markers) and the JSON `percentiles` doc (it now names the reflow quartet the text form carries and the JSON body omits; the code gap stands) fixed in the commit that updated this row; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim); 2026-09-13 read of the `offscreen` prose feat/round-7-offscreen added (shown by `xtask gate help-surfaces --diff`, matched at a94e1bc9b) — OFFSCREEN_USAGE/OFFSCREEN_BAD_SINCE/OFFSCREEN_DEFAULT_MAX, OffscreenSince/OffscreenArgs, offscreen_number, offscreen_args, cmd_offscreen and format_offscreen_reply, the header format strings, and the offscreen_tests/offscreen_wire_tests docs — against the parser (each key once, ASCII digits only, tail/max > 0, screen=1 only), cmd_offscreen's other-origin/bad-since arms and its one-lock clone-out, format_offscreen_reply's field order (back_at/pin only when back > 0, enabled=0 only when off, last= the page's own last row), aterm-core alt_archive.rs AltArchive::read/page_last/set_budget/set_enabled/wipe and AltArchiveRead's fields, and env_opted_out; one slip fixed before this row (OFFSCREEN_DEFAULT_MAX said a bare poll never ships a whole archive, false for one under 2000 rows — it now says at most that many rows); 2026-09-14 read of the offscreen doc's since= bullet round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at 547991d5b) — a mark from a restarted process, or from one whose self-update handoff could not carry the archive, reads from the start, and one that did keeps its origin — against cmd_offscreen's other-origin arm (a since= origin that is not archive.origin() reads from 0), aterm-core alt_archive.rs AltArchive::import (a non-zero carried origin replaces this process's; Refused leaves it) and spawn.rs alt_archive_origin/new_live_terminal; no claim contradicted; 2026-09-13 release-candidate merge of the perf port: the ONE prose change since that read is the per-site UI-thread terminal-mutex wait fragment the port added (`text_term_wait_fields` and its JSON twin `json_term_wait_fields`, plus the two `metrics` format strings that carry them), read here against crates/aterm-gui/src/metrics.rs TermWaitSite (a THREE-member enum RedrawA/RedrawB/Press with `ALL` a const array of all three and `label()` a const match to `redraw_a`/`redraw_b`/`press` — so the doc's `empty never (the sites are static)` holds and the report order is the doc's order), term_wait_distribution/term_wait_max_ns, and the two builders themselves: the text form writes a LEADING-SPACE run ` n_term_wait_<site>=<count> term_wait_<site>_p50_ms= _p95_ms= _p99_ms= max_term_wait_<site>_ms=` per site and the JSON twin the same fields LEADING-COMMA and quoted, each from the same histogram and the same `term_wait_max_ns`, appended at the matching `{}` of the text and JSON percentile lines; no claim contradicted; 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen since= bullet (a mark from a restarted process, or from one whose self-update handoff could not carry the archive, reads from the start; one that did keeps its origin) plus main's perf-port term-wait fragment (the text_term_wait_fields and json_term_wait_fields docs and strings, and the extra `{}` in both metrics format strings) — `xtask gate help-surfaces --diff` against main's read (matched at 23f4c32fe) shows round 10's one hunk and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged cmd_offscreen other-origin arm, aterm-core AltArchive::import and spawn.rs new_live_terminal/alt_archive_origin (main's spawn.rs hunks do not reach them), and metrics.rs TermWaitSite (ALL = RedrawA, RedrawB, Press; label() redraw_a/redraw_b/press) with the two builders' leading-space and leading-comma fragments appended ahead of the echo_rtt fields in cmd_metrics and cmd_metrics_json; no claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the prose main's seven metrics/watchdog commits moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at a4c4d3eaa) — the `percentiles` text and JSON format strings' new key_queue and acquire_queue fields, the module doc's WHEN, AND WHOSE / THE PARK OUTSIDE THE REDRAW / US OR THEM, ON THE SAME LINE blocks, the reworded `input_*` bullet and the new `present_glass` sentence, and the docs of the five new or rewritten tests — each against the code beside it: cmd_metrics's percentiles arm (n_key_queue + key_queue_p50/p95/p99_ms + last_/max_key_queue_ms spliced BESIDE the n_key_write it splits, n_acquire_queue beside n_acquire, then text_term_wait_fields, echo_rtt::percentile_fields_text and metrics::present_glass_fields_text) and its summary arm (metrics::lateness_fields_text after stale_arm_heals, then echo_rtt::percentile_fields_text and watchdog::turn_census_fields_text, in that order, after first_visible_ms); metrics.rs note_key_arrival_queued (the NSEvent backdate also recorded into H_KEY_QUEUE/LAST_/MAX_KEY_QUEUE_NS), INPUT_STAMP_NS's PER-WINDOW ATTRIBUTION note and book_input_present (a key routed to a window is closed only by that window's content present; a session-routed stamp still by any window's), lateness_fields_text/_json (max_wake_late_ms/_owner/_at_ms, max_deadline_late_*, max_present_latency_at_ms, max_present_latency_gap_ms, max_input_present_at_ms and metrics_now_ms, every stamp on the now_ns process clock), the wake arms (a Timer wake stores now - due into LAST_WAKE_LATE_NS and the next WaitCancelled stores 0 over it, while MAX_WAKE_LATE_NS fetch_maxes and stamps _AT_NS), should_log_slow_present (SLOW_PRESENT_LOG_THRESHOLD_NS = 3 x SLOW_FRAME_THRESHOLD_NS = 100 ms, SLOW_PRESENT_LOG_MIN_GAP_NS = 10 s, ungated in every build beside the $ATERM_TRACE_LATENCY stderr line), and present_glass_fields_text against aterm-gpu present_glass.rs + metal/swapchain.rs (presentDrawable: registration -> the drawable's presentedTime); watchdog.rs turn_census_fields_text/_json (max_turn_ms, max_turn_owner, max_turn_at_ms, last_turn_ms, turns, long_turns, long_turn_threshold_ms) with LONG_TURN_THRESHOLD_NS = metrics::SLOW_FRAME_THRESHOLD_NS (33.33 ms, one 30 fps frame), Breadcrumb::metric_name's user_event and RELEASE_STALL_THRESHOLD = 5 s; and renderer.rs await_frame_slot / drain_pending (both unbounded waitUntilCompleted, the ring drain booked through add_gpu_park before the present's work timer starts). No claim in this file contradicted — but the ctl catalog's copy of the same summary line was stale and is fixed in this commit (see the crates/aterm-types/src/control_verbs.rs row); 2026-09-16 read at the merge of 48695d2d6 (the find highlight and a width reflow), which landed without re-recording: the doc of FullHistorySearch::history_renumber_epoch (shown by `xtask gate help-surfaces --diff`) — that it is `Grid::history_renumber_epoch()` the rows were keyed against, travels with the results as absolute_row_revision does, and covers a width reflow that renumbers every retained row while moving no footer revision — against aterm-grid pin_methods.rs history_renumber_epoch (a monotonic epoch of renumberings invisible to the (content_gen, absolute_row_revision) pair; a WIDTH reflow advances it), the key taken beside the results (`t.grid().history_renumber_epoch()` at the snapshot) and the torn check that compares it with the revision and content_seq; no claim contradicted the code; 2026-09-17 round-19 read: the story_usage / parse_story / cmd_story docs and their literals (`ERR usage: story <…> [<text>]` spelled from StoryVerb::TOLD_WORDS, `ERR story: text is longer than 96 bytes`, `ERR story: text carries a control character`, `OK story=<n>`) against parse_story (split at the first whitespace, the closed-set verb, trim, the byte cap, char::is_control) and cmd_story's Wake::Story round trip; the story_tests module doc against its assertions (every junk form is the usage line; the cap is bytes, not chars). No claim contradicted; 2026-09-19 read of parse_story and cmd_story against their docs (the closed set from StoryVerb::TOLD_WORDS, the 96-byte cap, the control-byte refusal, `OK story=<seq>` from Wake::Story) and the grammar tests; 2026-09-21 read of the prose db57acddb moved (shown by `xtask gate help-surfaces --diff`, matched at 2f6cfae0c): the three ledger fields appended to the `metrics` text surface (`offscreen_rasters= last_offscreen_raster_ms= max_offscreen_raster_ms=`, :1015-1016) and to its JSON twin (:1589-1590), the test metrics_surfaces_publish_the_offscreen_raster_ledger and its doc (**THE OFFSCREEN RASTER LEDGER REACHES A SURFACE**) — read against both `format!`s' argument lists (:1114-1116 and :1682-1684 pull `m.offscreen_rasters`, `ms(m.last_offscreen_raster_ns)`, `ms(m.max_offscreen_raster_ns)` in the same order, so the twins stay field-for-field), metrics.rs's record_offscreen_raster (:2206: `OFFSCREEN_RASTERS.fetch_add(1)`, `LAST_OFFSCREEN_RASTER_NS.store`, `MAX_OFFSCREEN_RASTER_NS.fetch_max`) and the snapshot that loads all three (:3647-3649), and the parent db57acddb^ — where the ledger and record_offscreen_raster already existed and control_query.rs contained no `offscreen_raster` at all, so the doc's `read by NOTHING: not the text summary, not the JSON twin, not a ctl verb` holds (the `metrics` verb IS those two surfaces); the test asserts all six field names on both surfaces before, then records one raster and requires the text count to move, which is the `a value that MOVES` claim; the 2208x1788 `aterm ctl image` / 160-fields measurement is the commit's own, consistent with a parent whose surfaces read no ledger field; no claim contradicted; 2026-09-21 round-22 read of the new `screen_stamp` and `screen_text` against `cmd_text_opt`, `cmd_session_status` and `control_session`'s turn verdict: the stamp hashes exactly the bytes a bare `text` serves (pinned in-crate by `screen_stamp_matches_the_text_verbs_rows` and across the wire by `the_status_stamp_hashes_the_rows_the_text_verb_serves`), `cmd_text_opt` keeps its own span loop because it serves `tail=`/`rows=` and the doc now says so, and the measured cost is recorded on the function (61.6 us a call on a 60x200 grid, 55.3 us of it `visible_row` rendering the grid). No claim contradicted; 2026-09-22 round-22 REVIEW: re-read unchanged in substance — this row moved only because the merge with origin/main brought its own 2026-09-21 read of the same file. No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the `screen_stamp` docs against the code: the stamp's reader is any reporter, no hook. No claim contradicted; 2026-09-23 (p2/gui review) read of cmd_appnotice's doc and replies: `OK recorded` for toolchain text shaped like `managed-current:` / `machine-settings:` against `App::take_app_notice` (AppNoticeTaken::Recorded: the marker's ledger entry, no row), `OK posted` for any other text (a `StatusBars::notice` row); 2026-09-23 (wp4 bounded aterm.log) read of the WHEN, AND WHOSE block's aterm.log sentence against aterm-gui metrics.rs note_slow_present / slow_episode_step / note_slow_episode_quiet / log_slow_episode_end: a sample over SLOW_PRESENT_LOG_THRESHOLD_NS (100 ms) opens an episode with one line, later samples under SLOW_PRESENT_EPISODE_QUIET_NS (60 s) apart join it silently, and the summary line is written when it goes quiet (or at the first present after `metrics reset`) only when it held more than one sample; ungated by build, beside the $ATERM_TRACE_LATENCY stderr line and the 5 s release stall watchdog. No claim contradicted; 2026-09-24 read of the `harness` lane prose (shown by `xtask gate help-surfaces --diff`: 2 hunks, 7 removed / 9 added, all accounted for) — cmd_appnotice's usage and lane check now name `toolchain|update|harness`, and its doc says a harness note is a record and never a row — against cmd_appnotice (the `matches!` lane gate) and lib.rs App::take_app_notice's `\"harness\"` arm (StatusBars::notice(Lane::Harness) → AppNoticeTaken::Recorded → `OK recorded`, pinned by appnotice_says_recorded_for_a_marker_and_posted_for_a_row) and status_bars.rs Lane::Harness (ledger entry at once, no row, pinned by a_harness_note_is_recorded_and_never_takes_a_row); no claim contradicted; the branch's reads of the same row: the row predates ffda97813 (`text tail=`/`rows=`), which the round-2 merge carried; re-read against cmd_modes, cmd_cell, cmd_metrics/cmd_metrics_json by the 2026-09-10 round-3 reader and against text_args/TextShape::select/frame_rows_reply/cmd_text_opt by the round-3 fixer (that delta matches its code); the `modes` frame (`OK <n>` and twelve keys, not `OK` and seven — pinned by modes_frames_its_count_and_twelve_keys), the `cell` attrs vocabulary (`wide`/`wide_cont`, pinned by cell_attrs_carry_the_width_markers) and the JSON `percentiles` doc (it now names the reflow quartet the text form carries and the JSON body omits; the code gap stands) fixed in the commit that updated this row; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim); 2026-09-13 read of the `offscreen` prose feat/round-7-offscreen added (shown by `xtask gate help-surfaces --diff`, matched at a94e1bc9b) — OFFSCREEN_USAGE/OFFSCREEN_BAD_SINCE/OFFSCREEN_DEFAULT_MAX, OffscreenSince/OffscreenArgs, offscreen_number, offscreen_args, cmd_offscreen and format_offscreen_reply, the header format strings, and the offscreen_tests/offscreen_wire_tests docs — against the parser (each key once, ASCII digits only, tail/max > 0, screen=1 only), cmd_offscreen's other-origin/bad-since arms and its one-lock clone-out, format_offscreen_reply's field order (back_at/pin only when back > 0, enabled=0 only when off, last= the page's own last row), aterm-core alt_archive.rs AltArchive::read/page_last/set_budget/set_enabled/wipe and AltArchiveRead's fields, and env_opted_out; one slip fixed before this row (OFFSCREEN_DEFAULT_MAX said a bare poll never ships a whole archive, false for one under 2000 rows — it now says at most that many rows); 2026-09-14 read of the offscreen doc's since= bullet round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at 547991d5b) — a mark from a restarted process, or from one whose self-update handoff could not carry the archive, reads from the start, and one that did keeps its origin — against cmd_offscreen's other-origin arm (a since= origin that is not archive.origin() reads from 0), aterm-core alt_archive.rs AltArchive::import (a non-zero carried origin replaces this process's; Refused leaves it) and spawn.rs alt_archive_origin/new_live_terminal; no claim contradicted; 2026-09-13 release-candidate merge of the perf port: the ONE prose change since that read is the per-site UI-thread terminal-mutex wait fragment the port added (`text_term_wait_fields` and its JSON twin `json_term_wait_fields`, plus the two `metrics` format strings that carry them), read here against crates/aterm-gui/src/metrics.rs TermWaitSite (a THREE-member enum RedrawA/RedrawB/Press with `ALL` a const array of all three and `label()` a const match to `redraw_a`/`redraw_b`/`press` — so the doc's `empty never (the sites are static)` holds and the report order is the doc's order), term_wait_distribution/term_wait_max_ns, and the two builders themselves: the text form writes a LEADING-SPACE run ` n_term_wait_<site>=<count> term_wait_<site>_p50_ms= _p95_ms= _p99_ms= max_term_wait_<site>_ms=` per site and the JSON twin the same fields LEADING-COMMA and quoted, each from the same histogram and the same `term_wait_max_ns`, appended at the matching `{}` of the text and JSON percentile lines; no claim contradicted; 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen since= bullet (a mark from a restarted process, or from one whose self-update handoff could not carry the archive, reads from the start; one that did keeps its origin) plus main's perf-port term-wait fragment (the text_term_wait_fields and json_term_wait_fields docs and strings, and the extra `{}` in both metrics format strings) — `xtask gate help-surfaces --diff` against main's read (matched at 23f4c32fe) shows round 10's one hunk and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged cmd_offscreen other-origin arm, aterm-core AltArchive::import and spawn.rs new_live_terminal/alt_archive_origin (main's spawn.rs hunks do not reach them), and metrics.rs TermWaitSite (ALL = RedrawA, RedrawB, Press; label() redraw_a/redraw_b/press) with the two builders' leading-space and leading-comma fragments appended ahead of the echo_rtt fields in cmd_metrics and cmd_metrics_json; no claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the prose main's seven metrics/watchdog commits moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at a4c4d3eaa) — the `percentiles` text and JSON format strings' new key_queue and acquire_queue fields, the module doc's WHEN, AND WHOSE / THE PARK OUTSIDE THE REDRAW / US OR THEM, ON THE SAME LINE blocks, the reworded `input_*` bullet and the new `present_glass` sentence, and the docs of the five new or rewritten tests — each against the code beside it: cmd_metrics's percentiles arm (n_key_queue + key_queue_p50/p95/p99_ms + last_/max_key_queue_ms spliced BESIDE the n_key_write it splits, n_acquire_queue beside n_acquire, then text_term_wait_fields, echo_rtt::percentile_fields_text and metrics::present_glass_fields_text) and its summary arm (metrics::lateness_fields_text after stale_arm_heals, then echo_rtt::percentile_fields_text and watchdog::turn_census_fields_text, in that order, after first_visible_ms); metrics.rs note_key_arrival_queued (the NSEvent backdate also recorded into H_KEY_QUEUE/LAST_/MAX_KEY_QUEUE_NS), INPUT_STAMP_NS's PER-WINDOW ATTRIBUTION note and book_input_present (a key routed to a window is closed only by that window's content present; a session-routed stamp still by any window's), lateness_fields_text/_json (max_wake_late_ms/_owner/_at_ms, max_deadline_late_*, max_present_latency_at_ms, max_present_latency_gap_ms, max_input_present_at_ms and metrics_now_ms, every stamp on the now_ns process clock), the wake arms (a Timer wake stores now - due into LAST_WAKE_LATE_NS and the next WaitCancelled stores 0 over it, while MAX_WAKE_LATE_NS fetch_maxes and stamps _AT_NS), should_log_slow_present (SLOW_PRESENT_LOG_THRESHOLD_NS = 3 x SLOW_FRAME_THRESHOLD_NS = 100 ms, SLOW_PRESENT_LOG_MIN_GAP_NS = 10 s, ungated in every build beside the $ATERM_TRACE_LATENCY stderr line), and present_glass_fields_text against aterm-gpu present_glass.rs + metal/swapchain.rs (presentDrawable: registration -> the drawable's presentedTime); watchdog.rs turn_census_fields_text/_json (max_turn_ms, max_turn_owner, max_turn_at_ms, last_turn_ms, turns, long_turns, long_turn_threshold_ms) with LONG_TURN_THRESHOLD_NS = metrics::SLOW_FRAME_THRESHOLD_NS (33.33 ms, one 30 fps frame), Breadcrumb::metric_name's user_event and RELEASE_STALL_THRESHOLD = 5 s; and renderer.rs await_frame_slot / drain_pending (both unbounded waitUntilCompleted, the ring drain booked through add_gpu_park before the present's work timer starts). No claim in this file contradicted — but the ctl catalog's copy of the same summary line was stale and is fixed in this commit (see the crates/aterm-types/src/control_verbs.rs row); 2026-09-16 read at the merge of 48695d2d6 (the find highlight and a width reflow), which landed without re-recording: the doc of FullHistorySearch::history_renumber_epoch (shown by `xtask gate help-surfaces --diff`) — that it is `Grid::history_renumber_epoch()` the rows were keyed against, travels with the results as absolute_row_revision does, and covers a width reflow that renumbers every retained row while moving no footer revision — against aterm-grid pin_methods.rs history_renumber_epoch (a monotonic epoch of renumberings invisible to the (content_gen, absolute_row_revision) pair; a WIDTH reflow advances it), the key taken beside the results (`t.grid().history_renumber_epoch()` at the snapshot) and the torn check that compares it with the revision and content_seq; no claim contradicted the code; 2026-09-17 round-19 read: the story_usage / parse_story / cmd_story docs and their literals (`ERR usage: story <…> [<text>]` spelled from StoryVerb::TOLD_WORDS, `ERR story: text is longer than 96 bytes`, `ERR story: text carries a control character`, `OK story=<n>`) against parse_story (split at the first whitespace, the closed-set verb, trim, the byte cap, char::is_control) and cmd_story's Wake::Story round trip; the story_tests module doc against its assertions (every junk form is the usage line; the cap is bytes, not chars). No claim contradicted; 2026-09-19 read of parse_story and cmd_story against their docs (the closed set from StoryVerb::TOLD_WORDS, the 96-byte cap, the control-byte refusal, `OK story=<seq>` from Wake::Story) and the grammar tests; 2026-09-21 read of the prose db57acddb moved (shown by `xtask gate help-surfaces --diff`, matched at 2f6cfae0c): the three ledger fields appended to the `metrics` text surface (`offscreen_rasters= last_offscreen_raster_ms= max_offscreen_raster_ms=`, :1015-1016) and to its JSON twin (:1589-1590), the test metrics_surfaces_publish_the_offscreen_raster_ledger and its doc (**THE OFFSCREEN RASTER LEDGER REACHES A SURFACE**) — read against both `format!`s' argument lists (:1114-1116 and :1682-1684 pull `m.offscreen_rasters`, `ms(m.last_offscreen_raster_ns)`, `ms(m.max_offscreen_raster_ns)` in the same order, so the twins stay field-for-field), metrics.rs's record_offscreen_raster (:2206: `OFFSCREEN_RASTERS.fetch_add(1)`, `LAST_OFFSCREEN_RASTER_NS.store`, `MAX_OFFSCREEN_RASTER_NS.fetch_max`) and the snapshot that loads all three (:3647-3649), and the parent db57acddb^ — where the ledger and record_offscreen_raster already existed and control_query.rs contained no `offscreen_raster` at all, so the doc's `read by NOTHING: not the text summary, not the JSON twin, not a ctl verb` holds (the `metrics` verb IS those two surfaces); the test asserts all six field names on both surfaces before, then records one raster and requires the text count to move, which is the `a value that MOVES` claim; the 2208x1788 `aterm ctl image` / 160-fields measurement is the commit's own, consistent with a parent whose surfaces read no ledger field; no claim contradicted; 2026-09-21 round-22 read of the new `screen_stamp` and `screen_text` against `cmd_text_opt`, `cmd_session_status` and `control_session`'s turn verdict: the stamp hashes exactly the bytes a bare `text` serves (pinned in-crate by `screen_stamp_matches_the_text_verbs_rows` and across the wire by `the_status_stamp_hashes_the_rows_the_text_verb_serves`), `cmd_text_opt` keeps its own span loop because it serves `tail=`/`rows=` and the doc now says so, and the measured cost is recorded on the function (61.6 us a call on a 60x200 grid, 55.3 us of it `visible_row` rendering the grid). No claim contradicted; 2026-09-22 round-22 REVIEW: re-read unchanged in substance — this row moved only because the merge with origin/main brought its own 2026-09-21 read of the same file. No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the `screen_stamp` docs against the code: the stamp's reader is any reporter, no hook. No claim contradicted; 2026-09-23 (p2/gui review) read of cmd_appnotice's doc and replies: `OK recorded` for toolchain text shaped like `managed-current:` / `machine-settings:` against `App::take_app_notice` (AppNoticeTaken::Recorded: the marker's ledger entry, no row), `OK posted` for any other text (a `StatusBars::notice` row); 2026-09-23 read of the prose design §10.5 H7 changed (the attention-and-motion work, `--diff`: cmd_appnotice's doc and its reply arms) — against take_app_notice (lib.rs), whose every arm answers `AppNoticeTaken::Recorded` through `record_message` / `post_managed_current` / `post_machine_settings` (each a `Hold::LogOnly` record), and against the one-variant `AppNoticeTaken` enum: `OK posted` is gone from the handler and from the doc, `OK recorded` is the only success reply, and the `ERR usage` / `ERR <error>` arms are unchanged; pinned by appnotice_text_is_recorded; no claim contradicted",
    ),
    (
        "crates/aterm-gui/src/control_session.rs",
        "0f8f2fcb78ad0d83",
        "2026-09-24",
        "2026-09-23 (lane E fix): read cmd_meta_supervisor's doc and USAGE (`meta unset supervisor [holder=<holder>]`, a bare unset clearing any holder's claim, holder= only while that holder holds it, `OK` either way) against its unset arm and release_supervisor_held_by; AgentWord's doc (`limited` kept as a DEPRECATED alias for the limit walls, presence::wall_kind_is_limit: usage-session, usage-weekly, model-bucket, spend) against parse_agent_words/AgentWord::Limited::matches, and the await-agent test's prose. No claim contradicted; 2026-09-24 (lane E): read AGENT_WORDS' doc (the verdict words less the walls, `unknown` added, `limited` retired) and AgentWord's (the eight kinds named from aterm_phase::WallKind::name, a well-formed unknown kind parking to its timeout) against parse_agent_words/AgentWord::matches and presence::wall_word; the await-agent test's doc. No claim contradicted; 2026-09-24: lane-C review-fix read — cmd_turn's THE GUARDED SUBMIT paragraph and the USAGE-adjacent comment (the CURSOR row, `pressed=<n>`), GuardedSubmit's docs, guarded_submit's doc (the measured transcript `❯` match, input_if_fenced with guard_at_cursor, the ORDERING paragraph naming the echo cap as the one gap) against the loop and the verdict's `pressed=` write; AgentWord/parse_agent_words' docs (wall / wall:<kind>, kind 1..=32 of [a-z0-9_-]) against await_agent's matches(); the roster test's AGENT_COLUMNS gaining agent_gen=/agent_fp=. No claim contradicted; 2026-09-24 (stage 2): read the guarded-submit prose (cmd_turn's doc paragraph, the USAGE string's submit=<key|none|guarded:<re>>, GuardedSubmit and guarded_submit: input_if_row_matches with parse_key(\"enter\"), zero-byte BusyZero/ConflictZero retried every 5 ms until the submit window, the first-press miss answering `OK 0 turn skipped reason=guard submitted=0 seq=<n> id=<n>`) against cmd_turn_guarded's press loop; the keyed-attention refusals (leading_attention_owner's owner= usage line, attention_owned_reply's four arms against AttentionWriteError) and cmd_meta_supervisor's USAGE/ERR denied/ERR busy supervisor= against session_timeline::claim_supervisor/release_supervisor; meta_status's attention_owner=/attention_owners=/supervisor= tail and sessions_lines' last `supervisor=` column against SessionMeta::live_supervisor. No claim contradicted; 2026-09-24: read the lane-C additions against their handlers — sessions_lines' doc and the five published columns after path= (program= agent= agent_detail= agent_rev= agent_since_ms=, AgentPublication::wire_fields read from ctx.timeline, no lock, no hop), cmd_await's USAGE and the `await agent` branch (parse_agent_words against AGENT_WORDS, await_agent latched on the timeline word, OK agent <word> rev=<n> / OK timeout / ERR exited), and the test fixtures that pin them; no claim contradicted the code; 2026-09-22: re-read the 14 new string literals in history_and_timeline_limits_match_retained_wire_rows against cmd_history/cmd_timeline; they are regression fixtures and assertions for unchanged limit/since framing, retention boundaries and record order. No user-facing help or response format changed; re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-gui-1); no claim contradicted the moved code; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at c811b7c5a) — the history record's `arch={}` field and cmd_history's doc — against cmd_history's format (arch= before text=), turn_ledger.rs ArchMark (Display `<origin>:<last>`, ArchMark::of from the term's archive) stamped in cmd_turn after preflight and before the yield, control_query::offscreen_args's since=<origin>:<i>, and the readers that cut a row at ` text=` (aterm-link hook.rs strip_body, aterm-agent report.rs parse_turn_line); no claim contradicted; 2026-09-14 read of the prose round 10 added (shown by `xtask gate help-surfaces --diff`, matched at 151dc6e1b) — history's ` carried=1` format piece and cmd_history's doc, NEXT_TURN_ID's handoff sentences, turn_ids_minted and raise_turn_ids — against cmd_history's format (carried=1 between arch= and text=), turn_ledger.rs TurnRecord::carried (started_ms on the old process's epoch, seq its engine's content_seq), cmd_turn's mint (fetch_add then + 1), raise_turn_ids' fetch_max clamped to handoff_carry::MAX_TURN_ID, seamless.rs take_incoming's raise from the manifest's next_turn_id and each carry's high_turn_id while single-threaded, and app_update_handoff.rs's manifest_turn_id(turn_ids_minted()); one slip fixed before this row (turn_ids_minted said the last id this process minted, 0 before the first, where after an adoption it is the count continued from the old process before this one mints any); 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's read (history's ` carried=1` piece and cmd_history's doc, NEXT_TURN_ID's handoff sentences, turn_ids_minted and raise_turn_ids) plus no prose of main's: main's one change to this file is code (the two test SessionCtx literals gain `modes` and `ui_waiting`), so the hash is round 10's, and `xtask gate help-surfaces --diff` against main's drift-sweep read (matched at 23f4c32fe) shows round 10's three hunks and nothing else; checked against the merged cmd_history (carried=1 between arch= and text=), cmd_turn's mint, raise_turn_ids' fetch_max clamped to handoff_carry::MAX_TURN_ID (i64::MAX) and seamless.rs take_incoming, none of which main touched; no claim contradicted; 2026-09-17 read of the `sessions` doc's identity= column (last, after detail=, `-` for none) against sessions_lines and the identity pair test; 2026-09-17 read of round 19's five lease-wake comments (commit 3facc6c65, unrecorded until now) against the calls they sit on: lease acquire and release post post_lease_changed(sid, false); the turn guard's drop posts it when the guard's own turn still holds the lease (`the rim lifts`); the turn's acquire posts it (`goes teal`); and the verified submit posts it ONCE with submitted = true after the verdict, inside `if submitted`, never on the press. No claim contradicted; 2026-09-19 read of the prose the round-19 second fix added (shown by `--diff`): `TurnIo::driver`'s doc (the session the connection's edge token was granted to, `None` for Owner-class callers and the embedded operator, recorded in `Lease::Turn` for the turn's length, resolved by the dispatch) and `LeaseGuard::driver`'s (the driver's own band is woken with the driven session) — read against `cmd_turn_guarded`'s lease acquire (`Lease::Turn { id, driver: io.driver.clone() }`), the two `post_lease_changed` sites after it (self_id, then the driver) and the guard's Drop (the same pair), `paste_only()` (driver: None) and control.rs's `turn_driver`; no claim contradicted; 2026-09-19 read of TurnIo::driver and the LeaseGuard field docs against cmd_turn (Lease::Turn{id, driver}, the driver's own band woken on acquire and on release); 2026-09-22 read of the `path=<frozen|live>` paragraph added to cmd_sessions's doc and sessions_lines's doc against the code under them: cmd_sessions_store collects the frozen ids under the SAME read guard as the snapshot and sessions_lines prints `path=frozen|live` as the LAST token on both the placed and the failed-hop arms (sessions_lines_name_the_frozen_path_tab_as_the_last_column pins both arms and the verb over a real store with mark_frozen_path); the mark's upper-bound caveat is SessionStore::frozen_path_tabs's own; 2026-09-24 read of the who wire line and doc against cmd_who, SessionStore::snapshot, sessions_lines, and both owner-class dispatch arms in control.rs: one snapshot provides sid and h.nonce.to_hex(), each row carries nonce=<hex32> (LaunchNonce is 16 bytes), then fgpgid=<pid|-> from quit_safety::foreground_pgrp over the cloned PTY master outside the registry and subscriber locks; an unreadable group is -, and cmd_who still has no session_window_rows or call_main hop while sessions does. No claim contradicted; 2026-09-24 merge read (origin/main 182e44a03 into harness/integrate): main's cmd_who doc and wire line (nonce=, fgpgid=) and this branch's cmd_meta_supervisor / AgentWord / guarded-submit prose are disjoint hunks, each read by its own clause above. The who doc's `a harness can refresh its generation from this guard read` named main's watch loop, which this branch deleted; the column is still read by the upgrade sweep's host roster (fgpgid) and the doc was reworded to say so. Consistent; 2026-09-24 merged Fabric batch read: cmd_sessions_status sends one ReadSessionStatuses wake and returns its Lines frame; the producer emits sid, revision, hold, detail and agent beside local id, stable sid and nonce; the bridge checks the incarnation and retries missing or malformed rows singly. Re-read against session_statuses_record, fabric_status_fields and parse_batch_rows. Consistent",
    ),
    (
        "crates/aterm-gui/src/fabric.rs",
        "782a58ff1edc18f7",
        "2026-09-24",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-gui-2); the note_bridge_supervised caller claim was falsified by `fabric attach` and is fixed here; 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 read of the round-13 prose (FABRIC_STALLED, LINK_STARTING / LINK_BRIDGE_LOST, LinkReport, LANE_GENERATION and its two accessors, fabric_link_facts / fabric_status_tail, bridge_attached's attach-is-stalled paragraph, link_report, cmd_link / LINK_USAGE, link_reason_token, wake_parked, bridge_lost's link stamp, cmd_post's stalled note, fabric_wait_refusal's stalled paragraph, the `link` HALT_EXEMPT entry and the three new test docs) against the code beside each: link_report drops a report whose generation is not the owner's, one arriving before any attach, and one arriving once the state is DISCONNECTED, stores CONNECTED or STALLED under the generation lock and wakes every registered session only when an up link went down; bridge_attached stores STALLED with reason=starting and clears rtt/acked_at; bridge_lost keeps the numbers and stamps reason=bridge-lost; cmd_post's entry and per-wake checks read fabric_state() != connected so a stalled fabric answers fabric_wait_refusal at once, and bridge_reachable is true once a bridge has attached so the token is queued=1; no claim contradicted; 2026-09-14 read of the round-13 review's prose (LinkReport.reported's doc, bridge_attached's amended doc, link_evidence's doc with its measured claim, link_report's first-report clause, wait_is_futile's doc, the `apply_fleet_halts` renames and the four new test docs) against link_evidence (the owner generation only, under FABRIC_STALLED only, returns once `reported` or off `starting`, rtt None and acked_at now, state -> CONNECTED), link_report (`reported = true`; `(was_up || was_starting) && !up` wakes the parked waiters), wait_is_futile (connected false; stalled -> `reported || reason != starting`; else true), cmd_deliver and cmd_outbox_sent calling link_evidence(lane_generation()), and aterm-link bridge.rs apply_fleet_halts/converge_hold under those names; no claim contradicted; 2026-09-15 read of round 15's key prose (shown by `xtask gate help-surfaces --diff`, matched at c1fc82257): POST_USAGE's key= and OUTBOX_USAGE's dup=1 against cmd_post/cmd_outbox_sent, the outbox line's `key=` and the in-flight post row's against cmd_outbox/cmd_inbox, PostRow's key and dup docs against retire_post (the key cleared with the body; dup sticky across a retried retirement and refused on off=-) and wait_landing's `OK <id> off=<n> dup=1`, KEY_MAX/valid_key's charset, the `a post without key=` clause in fabric_wait_refusal's doc and the wait-refusal test's, and the post-key test's doc and strings against that test; no claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset prose (shown by `xtask gate help-surfaces --diff`): the usage strings (DELIVER_USAGE's fetched= and receipt= forms, INBOX_USAGE's `inbox get @<off>`, POST_USAGE's --wait-ack, await inbox re=) against the parsers they name (cmd_deliver -> deliver_fetched/deliver_receipt/parse_deliver_fields, cmd_inbox_get -> inbox_get_at, cmd_post, cmd_await_inbox); the reply formats (Fetched::render, cmd_inbox's oldest_on_bus header, wait_receipt's ack=/ERR expired/ERR timeout … off=); the constants' docs (VERDICTS, FETCH_SLOTS/FETCH_WAIT_MS against aterm-link bridge.rs IDLE_TICK 250 ms and the roster backstop, RECEIPTS_OWED_MAX, EXPIRY_GRACE_MS against the bound cmd_post computes); OwedReceipt's history against the bridge's reconnect back-off (every mailbox item but Closed(Aterm) is dropped) and send_receipt; deliver_fetched's chunk rule (at= must equal the partial body's length, at=0 restarts, len= above BODY_MAX accepted, a refusal fails the parked read); and the test docs against their tests. ONE doc INCOMPLETE and FIXED here: FetchSlot::Failed listed the bridge's reasons without `refused` (answer_fetch's follow-up to a chunk the endpoint refused) and did not say `malformed` is written by refuse_fetched itself. No claim contradicted; 2026-09-14 v0.86 candidate: the file carries both halves of prose — upstream's and this candidate's — each read by its author against the same handlers, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-gui-2); the note_bridge_supervised caller claim was falsified by `fabric attach` and is fixed here; 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 read of the round-13 prose (FABRIC_STALLED, LINK_STARTING / LINK_BRIDGE_LOST, LinkReport, LANE_GENERATION and its two accessors, fabric_link_facts / fabric_status_tail, bridge_attached's attach-is-stalled paragraph, link_report, cmd_link / LINK_USAGE, link_reason_token, wake_parked, bridge_lost's link stamp, cmd_post's stalled note, fabric_wait_refusal's stalled paragraph, the `link` HALT_EXEMPT entry and the three new test docs) against the code beside each: link_report drops a report whose generation is not the owner's, one arriving before any attach, and one arriving once the state is DISCONNECTED, stores CONNECTED or STALLED under the generation lock and wakes every registered session only when an up link went down; bridge_attached stores STALLED with reason=starting and clears rtt/acked_at; bridge_lost keeps the numbers and stamps reason=bridge-lost; cmd_post's entry and per-wake checks read fabric_state() != connected so a stalled fabric answers fabric_wait_refusal at once, and bridge_reachable is true once a bridge has attached so the token is queued=1; no claim contradicted; 2026-09-14 read of the round-13 review's prose (LinkReport.reported's doc, bridge_attached's amended doc, link_evidence's doc with its measured claim, link_report's first-report clause, wait_is_futile's doc, the `apply_fleet_halts` renames and the four new test docs) against link_evidence (the owner generation only, under FABRIC_STALLED only, returns once `reported` or off `starting`, rtt None and acked_at now, state -> CONNECTED), link_report (`reported = true`; `(was_up || was_starting) && !up` wakes the parked waiters), wait_is_futile (connected false; stalled -> `reported || reason != starting`; else true), cmd_deliver and cmd_outbox_sent calling link_evidence(lane_generation()), and aterm-link bridge.rs apply_fleet_halts/converge_hold under those names; no claim contradicted; 2026-09-15 recovery audit: read the explicit-incarnation additions against link_evidence and link_report, which require Some of the current nonzero generation and preserve lost-bridge precedence under the existing generation lock; read the scoped fixture, None/stale/current admission matrix and historical negative control against serve_bridge TLS setup and the two real writers. Corrected the old Ok(bool) return description to its actual bool API; no claim contradicted.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the key, receipts and fetch-by-offset prose: POST_USAGE, OUTBOX_USAGE, DELIVER_USAGE and INBOX_USAGE, the outbox and inbox row formats, the PostRow, OwedReceipt, FetchSlot and Fetched docs, VERDICTS, FETCH_SLOTS, FETCH_WAIT_MS, RECEIPTS_OWED_MAX and EXPIRY_GRACE_MS, the cmd_inbox_get, inbox_get_at, deliver_fetched, deliver_receipt and wait_receipt docs, and the round-15 test docs and strings, shown by `xtask gate help-surfaces --diff` against main's read at 595e13613) plus upstream's change (the explicit-incarnation sentences of link_evidence and link_report and link_report's bool return, the LINK_SECTION and link_write_is_foreign docs, on_lane's doc, and the unscoped-delivery and admission-model test strings, shown against round 15's read at 030ba1788) plus ONE tense fixed at this merge: LINK_SECTION's doc said a None lane generation `is exactly the shape link_evidence's own gate accepts`, where since the recovery audit that gate returns on None; it now says the gate accepted it until it came to require the explicit incarnation that owns the link, checked against link_evidence and link_report (each returns before any store unless the generation is Some of the nonzero owner; link_report also on a DISCONNECTED state), link_write_is_foreign under with_link_reset's Section, cmd_deliver's fetched= and receipt= forms (deliver_fetched, deliver_receipt), cmd_outbox's receipt and fetch trailer lines, and aterm-link bridge.rs drain_outbox, which answers the parked fetches first and reads the roster only before receipts or posts, so the fetch docs' `next drain (the idle tick, 250 ms; the roster backstop, 2 s, under load)` still holds; 2026-09-15 read of the new `kitty` HALT_EXEMPT entry against what the verb does: `puts no bytes on a PTY, retires no session and drives no program` read against control_media::cmd_kitty (two call_main hops only), App::wear_kitty_on_front and App::wear_kitty in app_input.rs (KittyLogHost::wear plus cursor_cat.on_collect and a redraw — no sink, no session retirement), so it is the `rain`/`fx` class it claims; `writes the machine-owned toy ledger` read against KittyLogHost::wear's delta.favourite_collectible + maybe_flush onto the kitty-collectibles path, which is also the argument menu.rs already records for FavouriteKitty's WriteInput class. The doc block's `SIX MEMBERS ARE NOT Target::Session ROWS` count is untouched: `kitty` joins the EXEMPT list, not the halt set, and both the_halt_set_is_derived_from_the_verb_table and the_hold_help_enumerates_exactly_the_halt_set pass. No claim contradicted; 2026-09-17 read of the `identities` HALT_EXEMPT argument against is_pty_reaching and agent_identity::forget_reply (a directory read/removal, refused while a live session carries the identity); 2026-09-17 read of round 19's prose (commit 3facc6c65's mail_facts doc and post_fabric_changed comments, and this commit's `story` HALT_EXEMPT entry) against SessionFabric::mail_facts (unread = rows above `seen`, pending = those already `listed`, dropped = the monotone counter, queued = posts with no `off` and not dead, last = the newest unhandled row's kind / from / trust, head = the newest row id, one leaf lock) and is_pty_reaching (story absent, so the halt never refuses it; the exemption's reason — chrome, not the grid; Owner-only besides; a held worker's window is the one whose `exit` / `timeout` must still arrive). No claim contradicted; 2026-09-19 read of the round-19 fix's prose (128b0f4a8, shown by `xtask gate help-surfaces --diff`): `wake_presence`'s doc (`a presence wake for every live session, and nothing else — the link's recovery moves the band's fabric slot but answers no parked waiter`) against its body (live_handles' self_ids collected under one read guard, then presence::post_fabric_changed per session; no wake_parked) and against link_report's new `went_up` arm (`(!was_up || was_starting) && up` calls wake_presence, `went_down` still calls wake_parked, the two arms exclusive); the `task`/`ask` kind strings in mail_facts' `last` (the newest unread task or ask above the watermark, else the newest unread row) against presence.rs's wait law, which lights the amber rim for exactly an unread task or ask. No claim contradicted; 2026-09-19 read of the prose the round-19 second fix added (shown by `--diff`): `LinkReport::stalled_at`'s doc and `fabric_stalled_ms`'s — the stall is dated from the down transition (from up, or the dial refused), `None` while up, before any attach and for an attached bridge that never reported — read against `bridge_attached` (stalled_at = None beside acked_at = None), `link_report` (up clears it; down stamps it when `was_up || stalled_at.is_none()`, so a repeated down keeps its date), `link_evidence` (clears it on the first delivery of a non-reporting bridge), `bridge_lost` (clears it: the slot reads `✕ lost`) and `fabric_stalled_ms` (`None` when `link.up`, else the stamp's elapsed ms); no claim contradicted; 2026-09-19 read of mail_facts (its doc corrected: the row the mail slot names is an unread task or ask first, else the newest unread — the code already did so), LinkReport::stalled_at's transitions (dated on a down after up or after starting; cleared on up, a fresh attach, bridge lost and the test reset), fabric_stalled_ms, and the `story` hold-exempt row; 2026-09-20 (round 21) read of the module header and the new link prose against the code. The header's LINK bullet now lists five states and says which four are STORED and which one is DERIVED: read against fabric_state, where `stale` is computed at call time from LinkReport::owed_since and the other four are loads of LINK.state. STALE_AFTER's doc states why the threshold is measured from the WORK and not from the ack's age, and quotes the live measurement it was checked against: read against fabric_link_facts (the age is Instant::elapsed at read time and grows on a quiet link by construction), note_work_owed (armed where cmd_post records the post the push lane turns into `EVENT <sid> post`), clear_work_owed and its three callers (link_report's up arm, link_evidence, cmd_outbox_sent), bridge_attached (a fresh incarnation inherits no debt), wait_is_futile (the new CONNECTED-and-stale arm answers queued=1 for the same reason the stalled arm does) and wait_landing (the condvar park is capped at stale_in so a waiter is not left to its full --wait). No claim contradicted; 2026-09-20 (round 21 review) read of the owed-clock prose against the reconciled code. Three reviewers found `stale` never armed for a post queued BEHIND one that landed, because the clock was one flag any evidence cleared: read against `posts_outstanding` (derived from the session rings, not counted, because a post can leave a ring without passing `outbox sent`), `settle_work_owed` (clears only when nothing is un-landed, otherwise re-stamps), its callers in `cmd_outbox_sent` after `retire_post` and in `deliver landed=`, `link_report`'s pre-lock read (the one lock order is ctx.fabric -> LINK.link), `note_work_owed`'s new FABRIC_CONNECTED gate, and `wait_receipt`'s park cap, which round 21 claimed and did not ship. No claim contradicted; 2026-09-22 round-22 read of the new mail surface against the ring it reads: `sender_class` (the `h-`/`s-`/`a-` prefixes the subject's `<src>` segment carries, `other` for anything else), `MailFilter::wants`, `MailLine` (no body field, which is HOW the push stream cannot carry one) and `SessionFabric::mail_since` — a leaf read that writes nothing, so watching a mailbox never changes what a drain the agent runs itself would show. `KINDS` is now `pub(crate)` because the subscribe grammar validates against it. Also read `note_work_owed` against its new `link_write_is_foreign` guard. No claim contradicted; 2026-09-22 round-22 REVIEW re-read of the mail surface against the ring: `MailSince` now reports `present` (rows above the cursor the ring still holds, unfiltered) instead of `oldest`, because eviction is BY CLASS and a human row pinned at the front held `oldest` still while agent rows behind it vanished — 612 rows into a 512 ring lost 100 and the old GAP reported 0. `next_msg_id` renamed `last_msg_id`: it is pre-incremented and held the LAST id, and the name cost a `- 1` that double-pushed every row's first sighting. `MailFilter::wants` reads `contains`. No claim contradicted — and the gap test now records the OLD formula's answer (0 on a fixture that lost 100) beside the new one, so the defect stays written down; 2026-09-22 (round 23, broadcast) read of the added doc blocks and the one usage line against the code they describe. `TOPIC_USAGE` and `cmd_topic`'s doc were read against `cmd_topic`: every refusal path answers the usage line (`add` with no topic, a topic that is not `[a-z0-9][a-z0-9._-]{0,31}`, a `since=` that is neither `head` nor `@<n>`, a trailing word, an unknown sub-keyword), and `topic_add`'s doc claim that a REPEATED add does not rewind is the `entry().or_insert_with` it now uses — `insert` replaced the value while answering `added=0`, which would have rewound a running topic to the start of the log; `topic_is_owner_class_and_names_its_session` pins it. `InboxRow::topic`, `MailFilter::topic` and `MailLine::topic`'s docs were read against `parse_deliver_fields` (the `topic=` arm is closed to the same grammar, so the bridge cannot invent a second field on the `msg` row), `render_row`, `MailFilter::wants` and `mail_since`. `SessionFabric::topics`' doc — receiver-side opt-in, empty by default, its own lock — was read against the field and its four accessors. `parse_topics`' doc claim that a malformed carry row is DROPPED rather than repaired is the `filter_map` it is, pinned by `the_carry_round_trips_and_a_malformed_row_invents_nothing`. `topic`'s HALT_EXEMPT entry is a new NAMED exemption argued in the list itself: it arranges future deliveries, and `deliver` and `post` are already exempt, so refusing it would only stop an operator arming a held session to hear the all-clear. No claim contradicted; 2026-09-22 (round-23 review) read of `TopicEntry`, `topic_add`/`set_topics`, `valid_since`, `parse_topics` and the `dropped` doc against the code: the serial is stamped under the map's lock, a carry re-seeds the counter above the carried maximum, `@+5` is refused by digits-only, and a broadcast the per-sender quota refuses is counted in `dropped=` at the refusal site. `gen` is a reserved word on this crate's edition, hence `serial` everywhere. No claim contradicted; 2026-09-22 (round 24, item 1) read of `FABRIC_EVENT_KINDS`' doc (six kinds; `topic` is the endpoint telling its bridge an opt-in moved), `TopicEntry` (the `since=` token only), the `topics` field doc (every change pushed as a `topic` event, so drop-then-add is two events in order) and `parse_topics`' note that a 0.90.x manifest's third field is read past — each against `cmd_topic`'s two `record(\"topic\", …)` sites and `render_topics`/`parse_topics`. No claim contradicted; 2026-09-22 (round 24, item 4) read of the `POSTABLE`, `inbox seen` and an_ack_releases_the_senders_quota docs, which named the file mirror as their reader, against the code: the invariants stand alone (a sender may not claim a bridge's verdict; an ack releases the sender's quota, so a `--peek`-only reader stays reachable). No claim contradicted; 2026-09-22 (round-24 review fix, commit A) CORRECTION of the 2026-09-22 item-4 clause above, which said the `topics` field doc contradicted nothing: it still described a per-roster-round read and an add SERIAL after both were removed (review lens 3, finding 2). Re-read now against `topic_add`/`topic_drop` (the record is written under the topics lock), `cmd_topic` (returns whether the set changed) and bridge.rs `sample_unsampled_topics` (once per session, and after a GAP): the doc says exactly that; 2026-09-23: re-read the fetch event and two-second outbox backstop prose against inbox_get_at, FABRIC_EVENT_KINDS, cmd_outbox, Bridge::on_event and Bridge::roster_backstop; a queued fetch now wakes the bridge through the push digest and a lost event is recovered by the roster round; 2026-09-23 read (fix/bridge-sampled-before-watched), by `gate help-surfaces --diff`: the `cmd_post_waking` doc's `wake` paragraph read against the function — every `OK` return and the `fabric_wait_refusal` return sit after `wake()`, which follows the `post` timeline record, and every refusal (usage, outbox full, denied) returns before it — and pinned by `an_accepted_post_wakes_before_its_wait_parks_and_a_refused_one_never` (whose new strings are the rest of this change); the test-only `cmd_post` wrapper doc read against its `cfg(test)`; the two intra-doc links retargeted from `cmd_post` to `cmd_post_waking` name the same bounded-`to` and admission code; no claim contradicted; 2026-09-24 read of the one `appnotice` line the harness lane changed (shown by `xtask gate help-surfaces --diff`: 1 hunk, 1 removed / 1 added) — \"posts a text row on the pull-down status bars, or a note to their record\" — against control_query::cmd_appnotice and App::take_app_notice: the `harness` lane records an appstatus entry and posts no row, and no arm writes PTY bytes or retires a session; no claim contradicted; 2026-09-24 merge read (ux/status-reporting onto origin/main 9533aed18): `gate help-surfaces --diff` over main's recorded read shows only this branch's own hunks, each read in the clauses above — consistent",
    ),
    (
        "crates/aterm-gui/src/hwkey.rs",
        "b2436705411d74a4",
        "2026-09-14",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-gui-2); the main-thread-park claim was falsified by the off-thread drawable and is fixed here; 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code",
    ),
    (
        "crates/aterm-gui/src/menu.rs",
        "c959ed40304e0dad",
        "2026-09-24",
        "2026-09-24 read (origin/main merged into ux/status-reporting, shown by `gate help-surfaces --diff`: 3 hunks, 22 lines added, none removed): every added line is main's — ba2feb22f's headless OS-UI refusal (`aterm-gui: {what} needs a window; a headless instance presents no OS UI`, printed by `os_ui_refused` only while PROCESS_HEADLESS is set), the `opening {s}` `what` the two NSWorkspace open helpers hand it, and its source-scan test's strings — read against menu.rs `os_ui_refused` and its callers and against main's own read recorded below; the branch changed no menu prose; no claim contradicted; read against its parser/dispatch by the 2026-09-10 sweep (aterm-B-window-and-clients); findings fixed in the commit that added this row; 2026-09-15 read of the new NextKitty action doc and its `Next Kitty` View row: `FavouriteKitty pins the cat that would ride ANYWAY (the focused window's tenured program cat, else the launch kitty)` read against App::promotable_kitty (kitty_tenure.worn().map(|i| i.look).unwrap_or(self.launch_kitty)) and App::favourite_kitty, which promotes exactly that; `walks the collection, one cat per press, wrapping at the end` read against App::wear_next_kitty ((i + 1) % rows.len() over KittyLogHost::wearable's roster, starting at 0 when nothing is worn); `kitty wear <key> is the addressed form` read against the shipped verb row v(\"kitty\", Write, Lines, App, ..) and control_media::cmd_kitty. The WriteInput comment beside it read against the same toy-ledger argument the FavouriteKitty arm above already carries. No claim contradicted 2026-09-16: the one sentence b690b30403 moved (FavouriteKitty's doc, which now names `App::favourite_kitty_checked` instead of the discarding wrapper it deleted) read against the code it points at, by the macOS-doctor audit lane, which found the row red and owed: app_input.rs's MenuAction::FavouriteKitty arm calls favourite_kitty_checked(wid, now) and puts its Err in `pending_action_refusal`, so the doc's claim that it answers on glass is now literally what happens rather than a discarded verdict; favourite_kitty_checked exists at app_input.rs:3928 and gates on ensure_sparkle + the effects master and feline sub-gates; wire id 46 matches menu.rs's to-id (394) and from-id (456); the legacy `FavouriteSessionKitty` spelling is still mapped by canonical_invoke_name (481); and the action is absent from requires_terminal_tab (490) and present in the WriteInput class (664), which is what the process-wide, never-terminal-only claim says. No slip found; the rest of the page is unchanged since the 2026-09-15 read.; 2026-09-18 round-19 read (feat/round-19-presence, commit 3: the menu reworked to align, SPEC19 §9) of the prose this commit added or moved — the Fleet/Inbox/LedgerForSession/HoldSession/LiftHold/FabricStatus/FabricOn/FabricOff/SetRole/TogglePresenceBand/TogglePresenceRim variant docs, `help()`'s sentences, canonical_invoke_name's three aliases, the FrontHold/set_front_hold/native_menu_checked/hold_row_reason docs, MenuEntry::Submenu and `items`, the DRIVING_MENU/FABRIC_MENU/MENU_MODEL docs, menu_chrome_lines' submenu rule, build_section's doc, and the golden test's WHY — against app_input.rs's dispatch arms (Fleet → open_connection_map; Inbox → open_session_inbox_tab; LedgerForSession → open_session_ledger; Hold/Lift → menu_hold(on); the three fabric rows → run_fabric_cli; SetRole → begin_active_session_meta_edit(Role); the toggles → user_toggle_presence), app_fabric_menu.rs (cmd_inbox `--peek --meta` written 0600 and opened as a Markdown tab; cmd_hold with HoldIssuer::Owner, HOLD_DENIED shown on the notice; the child run with ATERM_CONTROL_SOCK = self_sock_path; confirmation() only for on/off; queue_presence_write of presence.band / presence.rim; headless composes only), palette.rs resolve (Hold enabled iff terminal_front and no hold, Lift iff a local hold, a fleet hold greys both with hold_row_reason), native_menu_action_enabled (HoldSession iff FrontHold::None, LiftHold iff FrontHold::Local), the macOS reader in app_introspect.rs (`<label> ▸` in the parent line, then `menu \"<parent> ▸ <label>\"`), install's loop over MENU_MODEL skipping Version, and command_registry.rs's specs. No claim contradicted; the identity rows are omitted, as the two comments say, because this base has no `identities`; 2026-09-19 read of the round-19 fix's prose (128b0f4a8, shown by `xtask gate help-surfaces --diff`): FRONT_HOLD_REASON's doc and `set_front_hold`'s (`the reason … empty with no hold`) against its body (the reason cleared, then pushed only when the kind is not None) and its one caller app_fabric_menu.rs publish_front_hold (front_hold_fact's reason, the empty string with no fact); `front_hold_reason`'s doc against the clone under the lock; `native_item_tip`'s doc against its body (the halt pair under FrontHold::Fleet answers hold_row_reason(front_hold_reason()), every other case action.help()); `hold_row_reason`'s amended sentence (`a human reads the decoded words`) against the format over aterm_control::wire::pct_decode; `stamp_native_tip`'s doc against validate_menu_item (called after the checkmark stamp, before the enabled bit, on every tagged item) and its early return for every action but the halt pair, so `add_item_mods`' help tool tip (setToolTip: of action.help(), once) stands on every other row; the macOS test's WHY against the test itself (the real FABRIC_MENU built through build_section with the probe target, set_front_hold(Fleet, `main%20broken`), stamp_native_tip called as AppKit would, the tip read back containing `main broken` and `cannot be lifted here`, then the tips returning to help() once the hold lifts). No claim contradicted; 2026-09-19 read of the prose the round-19 second fix added (shown by `--diff`): `hold_row_reason`'s doc now says the decode runs through `presence::hold_reason_words`, which sanitizes what the decode lets out (a bridge-supplied `%e2%80%ae` would otherwise reverse the native tool tip, the palette row and the a11y description), and the macOS tip test's new assertion strings — read against `hold_row_reason` (format over `crate::presence::hold_reason_words(reason)`), `hold_reason_words` (`sanitize_token(&pct_decode(reason), 32)`: C0, bidi overrides and the band's glyphs stripped, whitespace collapsed) and the test's expected `fleet hold: mainbroken [31m, cannot be lifted here`; no claim contradicted; 2026-09-19 read of the round-19 model after the rebase onto main: every action's help sentence, the Fabric/Driving/Presence entry docs against MENU_MODEL and the tree golden, native_item_tip, native_menu_action_enabled and hold_row_reason for the halt pair, native_menu_checked for the two checkables, macos::build_section, and the identity-row notes reworded (round 18 landed on main after the design; the rows are a follow-up, omitted not disabled); 2026-09-23 Phase 4 p4/settings read: the new `open_file_in_workspace` doc (NSWorkspace `openURL:` over `fileURLWithPath:`, main thread only, no shell, the link allowlist untouched) read against `open_in_workspace(s, true)` and its one caller, `AppEffect::OpenPackagesLog`, which opens the path `atpkg::packages_log::log_path` resolves; the non-macOS stub's doc (spawns nothing, `false`; Settings names the path instead) against the open-log reducer arm, which emits no effect off macOS — consistent; 2026-09-23 read (the origin/main merge of claude/unified-messages) of the prose c68511b96 added (Phase 2 of docs/DESIGN-unified-messages-2026-09-21.md), shown by `xtask gate help-surfaces --diff` (5 hunks, 1 line removed, 9 added): the `Messages` variant doc, its `help()` sentence, its `from_invoke_name` spelling, the app menu's `Messages…` label and the MENU_MODEL golden's `aterm` line. `Open Settings AT the Messages route` read against app_input.rs's MenuAction::Messages arm (open_settings_tab(SettingsRoute::Messages), the Packages arm's twin: the frontmost window's Settings tab, at the route) and command_registry.rs's `app.settings.messages` spec (S::App, A::ConfigMutate, C::Any, the Packages class; invoke_authority puts it in ConfigWrite beside Packages and ToggleSettings, as its comment says); the §4 it cites is the design's `settings_page — SettingsRoute::Messages` section. `everything aterm told the person, newest first` read against App::messages_state, which walks the log ring with records().rev() (pinned by the_projection_is_newest_first_in_the_wires_words_with_live_actionability) and is the page's own subtitle; `beneath Packages…` against MENU_MODEL (Packages…, Messages…, then Open aterm.toml, which the golden line spells the same way); `the page the band's Details › opens` against perform_intent's Intent::Details arm, which calls open_messages_entry and so open_settings_tab_in_window(wid, SettingsRoute::Messages): the same route, in the pressed window rather than the frontmost one. The help sentence `Open Settings at Messages: everything aterm told you.` is the item's tool tip (native_item_tip answers action.help() for every action but the halt pair) and the palette row's accessibility description (palette.rs appends action.help()), one line ending in a period as every_action_has_a_one_line_help_sentence requires of ALL_ACTIONS, which lists it. Wire id 69 is the next number after TogglePresenceRim's 68 in both `tag` and `from_tag`, pinned by tags_round_trip_and_are_unique; `Messages` round-trips through from_invoke_name, and it is rightly absent from PRE_ROUND_19_INVOKE_NAMES, which lists only names that resolved before round 19. No claim contradicted; 2026-09-23 read (the second origin/main merge of claude/unified-messages, 9545a1131), shown by `xtask gate help-surfaces --diff` from c68511b96 (3 hunks, 0 prose lines removed, 8 added): exactly the Phase 4 prose the p4/settings read above covers, and nothing of either side's lost — `open_file_in_workspace`'s doc, the non-macOS stub's doc and the stub's `target_os` literal — re-read in the merged tree against macos::open_file_in_workspace (`open_in_workspace(s, true)`: NSURL fileURLWithPath:, NSWorkspace sharedWorkspace, openURL:, `false` on a path that is not UTF-8 or a null object), its one caller AppEffect::OpenPackagesLog in app_native.rs (the path from atpkg::packages_log::log_path, never the view's; a warn when refused), and the `packages/activity/open-log` reducer arm in native_settings.rs, which pushes that effect only under `cfg!(target_os = \"macos\")` and otherwise sets the feedback `The package log is <path>`, so the stub's `false` is never asked for by a shipping path. The message band's own `Open log` capsule (Intent::OpenPath, messages_host::open_path_external: `/usr/bin/open -t` under the log dir) does not pass through this function, and the doc claims nothing about it. No claim contradicted; 2026-09-23 WP3 one-update-row read (shown by `--diff`): the Version menu's ApplyUpdate row now reads `↑ Install update now` in the model and `{arrow} Install aterm v{version} now` / `{arrow} Install build {build} now` live (staged_apply_label), the update row's own verb, read against MenuAction::ApplyUpdate → App::apply_update_or_details, which applies a strictly-newer staged build immediately through the one-shot apply and otherwise opens Software Update, so `now` is what the press does; the troubled form `{arrow} Install {what} — {row_tail}` read against ApplyTrouble::row_tail (`will try again` / `try again now` / `install the signed release`, by ApplyRetry); the realized row `Updated to aterm v{} just now` against build_version_menu's realized arm; the moved docs (ApplyUpdate's, staged_apply_label's `# The words are the update row's`, update_version_menu's) and the two test assertions against the same code. No claim contradicted; 2026-09-24 read of the merged file (bdbd8be16, the ux/status-reporting merge of origin/main 3808bc40f), shown by `--diff` from abb0ee204 (5 hunks): every changed line is the WP3 one-update-row wording read above — `↑ Install update now`, `build {build}` / `aterm v{version}` and `{arrow} Install {what} now` / `{arrow} Install {what} — {}` from staged_apply_label, `Updated to aterm v{} just now` in build_version_menu's realized arm, and the tests' `Install aterm v9.9.9 now` / `will try again` — read against the same code in the merged tree (MenuAction::ApplyUpdate → App::apply_update_or_details; ApplyTrouble::row_tail); upstream's Messages action and open_file_in_workspace reads are unchanged by the merge. No claim contradicted; 2026-09-24 read of the headless OS-UI refusal: the `PROCESS_HEADLESS`, `mark_process_headless` and `os_ui_refused` docs and the refusal's stderr line, against main_entry (marks the process right after `cli::headless_arming`), `launch_posture` (headless -> Prohibited) and the five helpers that ask it first: `choose_local_file`, `confirm` (proceeds, as `confirm_destructive_close`'s headless arm does), `notify`, and `open_in_workspace` with its delayed twin, which carry `open_file_in_workspace` (Open Log), `open_help_url` and `open_privacy_settings`. The per-caller guards the doc names (app_palette's picker rows, app_input's document-open path) are present. Pinned by every_modal_and_workspace_open_asks_the_headless_refusal_first; 2026-09-24 re-read after the pin's `openURL:` needle was re-split before its `!(` (the crate's selector census read the old split as a selector); no claim changed; 2026-09-24 read of the confirm split against its code: `confirm` is ask(..).unwrap_or(true) (every could-not-ask case still proceeds, the ⌘Q contract unchanged), `confirm_owner` is ask(..) == Some(true) (fails closed), and `ask` returns None exactly at the four former fail-open points (os_ui_refused/off the main thread, a refused NSString, a nil NSAlert); the Wake sentence matches winit queuing user events during a handler and the alert receiving only NSEvents; no claim contradicted",
    ),
    (
        "crates/aterm-gui/src/operator_host.rs",
        "17a4ea0696e54b49",
        "2026-09-23",
        "2026-09-23 (lane E fix): read the new measure_looks_like_approval doc and its strings (the per-screen p50/p99/max line, the run-by-name command) against the test body and looks_like_approval/generic_choice_box; test-only prose, no user-facing help changed. No claim contradicted; read against its parser/dispatch by the 2026-09-10 sweep (aterm-A-ctl-verb-usages); re-read 2026-09-24 by lane E (harness/e-host): looks_like_approval's doc against its body (aterm_phase::parse_prompt, aterm_phase::read's prompt/phase, then generic_choice_box) and the actuator refusals in control.rs that call it",
    ),
    (
        "crates/aterm-gui/src/pty_idem.rs",
        "1cbe648448ee72c3",
        "2026-09-20",
        "read pty_idem.rs (module/Realm/PRODUCER_CAP/dup_reply/guarded/record_in_doubt docs, USAGE, KEYED_VERBS, test docs) against control.rs dispatch + run_feed_bin_routed, control_input.rs guarded press/take_leading_options, control_session.rs cmd_turn_guarded, control_verbs.rs catalog + framing_of, aterm-ctl stream_count/malformed header, aterm-link bridge feed key by workflow aterm-help-surfaces-read on 2026-09-10; 4 low findings left; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim); 2026-09-14 drift sweep (lane gui, 147 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-20 (round 21 review) read of the module header against aterm-link. It described §6.5's `bus -> PTY (term/in)` hop in the present tense and said the bridge loses a keystroke 'by subscribing the drive face from the HEAD on every attach': read against aterm-link bridge.rs, which has no `term` subscription, no `feed` and no function that writes to a PTY after round 21. The header now says the hop is gone and that what is documented below is the LOCAL seam (`aterm-ctl feed-bin`, `send --stdin`), which is unaffected — it is `Realm::Bridge` that lost its writer. No claim contradicted",
    ),
    (
        "crates/aterm-link/src/cli.rs",
        "4d9cedbd7371480e",
        "2026-09-24",
        "re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane aterm-link); no claim contradicted the moved code; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-14 drift sweep (lane link, 26 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — ONE slip fixed before this row, and it inverted a SECURITY boundary: `broker`'s doc said the socket 'is same-uid only'. Nothing chmods it and no peer uid is checked — the USAGE string ten lines below already says it is created world-connectable and that the boundary is the 0700 DIRECTORY you put it in, so the doc contradicted the help it introduces; 2026-09-14 read of the prose added since (USAGE's `aterm-link fabric` line and parse's doc on why it is now pub(crate)) against dispatch's new `fabric` arm (crate::fabric::main, the same entry crates/aterm/src/main.rs routes `aterm fabric` to) and crates/aterm-link/src/fabric.rs's bridge_config, which reads `[fabric] command` through this parse, by feat/round-11-fabric-ledger; no claim contradicted; 2026-09-14 read again on the merge branch: USAGE's `aterm-link fabric` line and parse's pub(crate) doc against dispatch's `fabric` arm (crate::fabric::main, the entry crates/aterm/src/main.rs also routes `aterm fabric` to) and fabric.rs bridge_config, which reads the `[fabric] command`'s fleet, broker, cap files and state dir through THIS parse; no claim contradicted; 2026-09-14 round-12: the `hook` synopsis line gained --merge, --dry-run and --check, read against hook::parse's flag table; nothing else in the file moved; 2026-09-14 read of the prose added since (USAGE's `aterm-link fabric` line and parse's doc on why it is now pub(crate)) against dispatch's new `fabric` arm (crate::fabric::main, the same entry crates/aterm/src/main.rs routes `aterm fabric` to) and crates/aterm-link/src/fabric.rs's bridge_config, which reads `[fabric] command` through this parse, by feat/round-11-fabric-ledger; no claim contradicted; 2026-09-14 read again on the merge branch: USAGE's `aterm-link fabric` line and parse's pub(crate) doc against dispatch's `fabric` arm (crate::fabric::main, the entry crates/aterm/src/main.rs also routes `aterm fabric` to) and fabric.rs bridge_config, which reads the `[fabric] command`'s fleet, broker, cap files and state dir through THIS parse; no claim contradicted; 2026-09-14 merge read of feat/round-12-hooks over main: the merged prose is exactly this branch's `hook` synopsis line (--merge, --dry-run and --check, against hook.rs parse's flag table) plus main's `aterm-link fabric` USAGE line, the `fabric` word in the verb list and parse's pub(crate) doc (checked against dispatch's `fabric` arm calling crate::fabric::main, fabric.rs USAGE and parse_args — `[status] [--json]`, `tail [--bodies] [--from <offset>]`, `help` — and bridge_config's crate::cli::parse(&flags)); ONE SLIP FIXED at the merge, in this branch's prose: the rewritten synopsis had silently dropped `[--rewake]`, which hook.rs parse and hook's own USAGE line still take — restored between [--dry-run] and `| run`; 2026-09-14 read of the USAGE prose added (the `fabric` line's `on|off|doctor` forms, the paragraph on the read-side verbs defaulting their flags from the rendezvous file, and the broker usage's and mint doc's `aterm fabric on` in place of the script) against dispatch (ls, glance and tui through enable::with_rendezvous_defaults, mirror through enable::with_default_sock, serve deliberately not — the comment on the arm says why) and crates/aterm-link/src/enable.rs fill_defaults (only an absent flag is prepended, so a flag on the command line wins), with_default_sock (`<dir>/aterm.sock` only when the rendezvous file and that socket exist) and Rendezvous::read (no file leaves argv alone; an unreadable one is said on stderr and leaves it alone), by feat/round-13-fabric-on; no claim contradicted; 2026-09-14 read of the round-13 C prose (feat/round-13-fabric-on: the module doc's writer roster — `driving=` alone with no writer, `role=`/`detail=`/`phase=`/`context=`/`title=` written by a meta-mode bridge, the six columns appended to §7's — USAGE's `--presence` entry and the `ls` paragraph, `column`'s two comments naming presence::token, Parsed::presence_given, and the column test's rewritten docs) against parse (`--presence meta|minimal` through presence::Mode::parse, presence_given set only by the flag), run's serve arm (cfg.presence = fabric::presence_from_config() when the flag was absent; `ls` never reads the file), row's format string (phase= context= title= after epoch=, every column through `column`, so the bridge's `context=12%` prints `12%25`), and presence.rs Fields::tokens; no claim contradicted; 2026-09-14 read of the rewritten `--presence` entry (phase= read off the last TAIL_ROWS = 40 rows only on a `status revision=` move, a session running anything but Claude Code reads idle, title= is `meta set title` alone, minimal reads no screen, else the file's `[fabric] presence`, else meta) against presence.rs read_meta (`user_title=` alone, `-` when unset), phase_word over aterm_phase::worker_phase (Idle when no prompt box, busy signal, limit notice or trailing `?` is on the rows), bridge.rs sample_presence with Slot::needs_screen (the tail read only when the revision moved) and fabric::presence_from_config, and of the merged USAGE (the round-12 `hook` synopsis beside the round-13 `fabric` line) at the rebase onto main's round-12 merge; no claim contradicted; 2026-09-14 audit fixes read against dispatch: the USAGE's backslash-n line continuations that lost the broker/mint rows' indent are plain newlines, the `asb` clause names `aterm link broker`; dispatch gained `-h|--help|help` (stdout, exit 0 — the contract `aterm --help` and the `broker -h`/`mint -h` children already kept) and an unknown-subcommand arm refused BY NAME (the header's own rule, which `wake|pin|lash` got and a typo did not); `broker` refuses a surplus argument and removes a log THIS call created when the bind is refused; `mint` refuses a repeated --secret-file (the empty key used to win last, exit 0); 2026-09-14 merge read of main over 28508563a: the merged prose is exactly round 13's `--presence` entry, the ls row's phase=/context=/title= columns and the module doc's SIX ADDED COLUMNS / THREE-without-a-writer paragraphs plus main's audit additions (the reflowed USAGE rows, `aterm link broker` in the identity clause, dispatch's `-h | --help | help` and unknown-subcommand strings, broker's unexpected-argument and mint's --secret-file-twice refusals), checked against dispatch (the help arm prints USAGE on stdout and exits 0; an unknown word is refused by name, exit 2), broker (a surplus word is `unexpected argument`), mint (`--secret-file given twice`, SECRET_MIN = 32), parse's --presence arm and crate::presence Fields::tokens. ONE SLIP IN ROUND 13'S OWN PROSE FIXED at the merge, and the hash recorded here is the corrected file: the module doc said title= is the session's user title `else its terminal title`, where presence.rs takes `meta user_title=` and NOTHING ELSE and publishes `-` without one — the doc now says so; 2026-09-15 read of the `--receipts`/`--no-receipts` usage entry, the flag names in the parser's strings and the receipts_given doc against parse_args (both flags set cfg.receipts and receipts_given; accepted on every verb and read only by `serve`, as `--presence` is), main's `serve` arm (receipts_from_config only when neither flag was given), fabric.rs receipts_from_config (off when the key or the file is absent; off with a stderr line when unreadable or not a boolean), enable.rs (writes `receipts = true` once when the table lacks the key; an operator's `false` stands) and bridge.rs send_receipt (acks only an ask/task with one of the three verdicts, onto the sender's lane per receipt_lane; with receipts off every owed receipt is retired `off=-`); no claim contradicted; 2026-09-15 read of the top-level `hook` synopsis line's new `[--report-to @sid]` against hook.rs parse (the flag is accepted by `install claude` and `run`, acted on by `stop`) and hook.rs USAGE; no claim contradicted; 2026-09-14 (2026-09-15 UTC) re-read of the `hook` synopsis line's `[--report-to @sid]` against hook.rs parse (the flag is accepted on every hook argv; the installer writes it on the Stop command only, claude_settings_for) and hook.rs USAGE; no claim contradicted; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `--receipts`/`--no-receipts` usage entry, the two flag names among parse_args' strings and the receipts_given doc: 3 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 6d66e6858) plus upstream's change (the `hook` synopsis line's `[--report-to @sid]`: 1 hunk, shown against round 15's read at 030ba1788), checked against parse_args (--receipts/--no-receipts set cfg.receipts and receipts_given), the serve arm (receipts_from_config only when neither flag was given), fabric.rs receipts_from_config (off when the key or the file is absent), and hook.rs parse's `--report-to` arm and claude_settings_for (written on the Stop command only); 2026-09-15 round-16 read (feat/round-16-crosshost) of the new broker usage and doc, the USAGE broker/fabric lines and the build line: broker()'s parse (--tcp/--key-file/--secret-file once each, --allow-remote, the positional log required with --tcp, surplus words refused), the default-build refusal before any other check (transport::SEALED_UNAVAILABLE), the always-guarded TCP rule, is_loopback_endpoint's refusal text, read_private_key_file/read_secret (0600, 32+ bytes) before the log is opened, serve_sealed, the `listening <bound addr>` line, and build_line!/SEALED_BUILD_MARK against enable.rs binary_has_sealed; the fabric synopsis against fabric.rs parse_args; no claim contradicted; 2026-09-15 read of the broker prose the round-16 review's twin added (the doc's two-listeners paragraph, USAGE's `[--unix <socket>]` synopsis, its entry and the second `listening` line, the `--unix needs --tcp` refusal, `--unix given twice`, and the mode line's `and the unix socket`) against broker(): --unix parsed once, refused without --tcp before the log is opened, served by broker.serve(sock) on the SAME Broker after serve_sealed succeeds (one log, one guard, two acceptors; a refused socket drops the TCP handle and removes a log this call created empty), `listening <tcp>` then `listening <sock>` printed only after both are bound, both handles kept for the process; and astream-broker serve_tcp_with's MAX_PREAUTH_CONNS = 64 / PREAUTH_TIMEOUT = 5 s, which gate the TCP accept path only (serve() has no pre-auth wrap); no claim contradicted; 2026-09-15 read of every prose line the round-16 branch changed since main, by the round-16 merge pass: the rendezvous-defaults paragraph against enable.rs fill_defaults (--tcp --key-file defaulted only beside a --broker defaulted from a rendezvous file that names key_file) and finish (key_file written only for Wire::Sealed with no bind, i.e. by join) and dispatch (ls/glance/tui through with_rendezvous_defaults, mirror through with_default_sock): one gap, the paragraph named `on` alone and not the sealed pair a joined host defaults, fixed before this row; the broker USAGE and doc against broker() (each flag once, --key-file/--allow-remote/--unix refused without --tcp, the SEALED refusal first, --secret-file required and the log positional required with --tcp, is_loopback_endpoint before the key is read, listening lines only after both binds) and build_line!; no other claim contradicted; 2026-09-17 re-read after rebasing onto main e6f88a94b (the bytes unchanged by the rebase): USAGE's broker lines and the rendezvous paragraph against enable.rs fill_defaults (--tcp --key-file added only when the rendezvous names key_file and no --broker was given) and broker()'s parsing (each flag once; --key-file, --allow-remote and --unix refused without --tcp; the SEALED refusal before any other word; --secret-file and the log required with --tcp; is_loopback_endpoint then read_private_key_file before the log is opened; read_secret's check_private and SECRET_LEN; the `listening` lines only after both binds) and build_line!; no claim contradicted; 2026-09-17 read again by the pass that folded feat/round-16-crosshost into its two commits: the gate run with the roster as it stood before this row's 2026-09-17 entry named exactly these five surfaces, and each `--diff` above was re-read against the handlers named, hook.rs's and this row's claims included; no claim contradicted; 2026-09-20 (round 21, narrow the fabric) read of every prose line this round moved, against the handlers below. USAGE lost the `--screen <sid|all>` entry, the `aterm-link glance` and `aterm-link tui` synopsis lines and the `Later rungs own wake, pin and lash` line, and the rendezvous-defaults paragraph now names `ls` and `mirror` alone: read against dispatch, which no longer has a `glance`, `tui` or `wake|pin|lash` arm and routes an unknown word to the refuse-by-name arm at exit 2, and against parse, which no longer has a `--screen` arm or a `Config::screen` to fill. The `--receipts` entry is rewritten because the DEFAULT MOVED: read against parse_args (both flags still set cfg.receipts and receipts_given), the serve arm (receipts_from_config only when neither flag was given) and fabric.rs receipts_from_config, which now answers ON for an absent key, an absent file and an unreadable one. The `ls` column paragraph gained a clause saying `holder=` has no writer on a row THIS node writes and is printed as a pass-through of an older peer's token: read against bridge.rs publish_session_presence, whose body no longer carries `holder=`, and against `column`/`row`, which render whatever the body holds. The module header's refuse-by-name paragraph is rewritten in the same read. No claim contradicted; 2026-09-22 (round 24, item 4) read of USAGE and the rendezvous-defaults paragraph against `dispatch` and `enable::with_rendezvous_defaults`: `mirror` is gone from both and `ls` alone defaults its flags. Pinned by the_usage_offers_no_file_mirror; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of USAGE against `dispatch`: `hook` is gone; an unknown word is refused by name. No claim contradicted; 2026-09-23 (fix/claude-at-exec-time, item 2a) read of the four hunks `xtask gate help-surfaces --diff` shows — the `hook` dispatch arm, RETIRED_HOOK_LINE and its use, the module doc's exception paragraph and the retired_hook/RETIRED_HOOK_LINE docs — against dispatch (the `hook` arm ahead of the unknown-word refusal, which still answers every other word exit 2) and retired_hook (stdin read to its end unless a terminal, the one eprintln, ExitCode::SUCCESS, nothing on stdout), and aterm-primer's CLAUDE_SETTINGS_FILE (home-relative: a project settings.local.json is never opened) — consistent; 2026-09-24 merge read (harness/integrate: lane D's harness/d-decisionb over main): lane D's parallel `hook_tombstone` (its TOMBSTONE_STDIN_MAX bound, doc, dispatch arm and unit test) was dropped at the merge and this file is main's bytes — `retired_hook` is the one implementation, so the hash above is unchanged; lane D's extra argv shapes (0.91.0's `stop --wake-budget 6/1 --timeout 15`, notification, `permission-request --gate-tools`, `install --dry-run`, the `hooks` neighbour) moved into tests/r25_retired_hook.rs and the one-binary routes (`aterm hook run`, `aterm link hook run`, the `aterm-link` argv0 symlink) into crates/aterm/tests/retired_hook_shape.rs, both held to RETIRED_HOOK_LINE; 2026-09-24 (lane D2) read of the --presence entry and the ls paragraph against bridge.rs after the presence re-grounding: phase= is set only by Fields::set_agent from status agent= (status_into_presence) and the EVENT agent push (on_event's `agent` arm), no screen is read anywhere in the crate (screen_tail and presence::read_screen deleted, aterm-phase gone from Cargo.toml), a non-agent session reads phase=- (measured in r13_presence), context= has no writer (Fields has no context field) while ls still prints the column through field(\"context\"), and minimal writes attention= alone (Fields::tokens)",
    ),
    (
        "crates/aterm-link/src/fabric.rs",
        "202e755efd208d3e",
        "2026-09-24",
        "written and read by feat/round-11-fabric-ledger on 2026-09-14 against its own handlers and the code it describes: USAGE and the module doc against parse_args (no argument is `status`; `--json` is status-only, `--bodies`/`--from` tail-only, each refused by name elsewhere), resolve_command/command_in_toml (ATERM_FABRIC_COMMAND first, then `[fabric] command` through aterm_toml, blank as absent — aterm-gui fabric_launch::configured_command's order), bridge_config over crate::cli::parse, probe_broker (connect with IO_TIMEOUT, `hello`, every cap attached, `Fetch max=0`; broker_pid from the process table, launchd_label by pid), gather's walk over aterm_ctl::local_instances with read_instance (`fabric status`, the child running `link serve`) and read_session (`meta`, `inbox 0 --peek --meta`, `inbox --peek --meta`, and `timeline` only when held — every read `--peek`, every inbox read `--meta`), last_records/traffic_of (TRAFFIC_ROWS = 10, metadata only; the text reaches stdout only through tail_line with `--bodies`, after the trust label), warnings() (each warning checked by every_condition_that_makes_connected_a_lie_or_loses_mail_is_a_warning; the shared-state-dir sentence against Bridge::on_inbox_record's not-hosted `undeliverable` and the broker's SubscribeGroup resuming every member from the one committed cursor; the `connected` sentence against the 2026-09-12 measurement FABRIC_PAGE records), status_main/exit_of (0, 1 with any warning, 2 from Off) and tail (exit 1 when the broker is unreachable or closes the stream); the live run on this machine matched the text: fleet local, the launchd broker's pid and label, instance 66439's supervised bridge, both sessions' inbox numbers, the bus records by offset; 2026-09-14 RE-READ of the three module-doc bullets the adversarial review's fixes moved (the BROKER bullet's head-query sentence, the WARNINGS bullet's node-gone sentence, and the fallback-source sentence) against the handlers they now describe: BrokerView::read_ok (reachable && attached && head.is_some()) as probe_broker leaves it — attach answered, `Fetch max=0` refused, so `view.attached` is true with no head — against warnings()'s second arm and render_text's `reachable` line, which now read the same predicate and print `yes, but the bus cannot be read: <the broker's own error>` and exit 1 (a_head_query_that_failed_is_not_a_broker_that_answered, and the end-to-end a_broker_that_refuses_the_read_is_not_reachable_yes against a broker that answers Hello and Attach and errors the Fetch); warnings()'s new node arm against join_sessions, which fills report.nodes from the roster rows whose owner is `node` (so it is empty whenever the head query failed and nothing is claimed about a bus nobody read) and against bridge.rs's will (`state=gone fabric=disconnected`) — a_node_the_bus_calls_gone_is_a_warning_while_its_instance_says_connected also pins that a REMOTE node's `gone` row is not this machine's warning; and the fallback sentence against resolve_command's Source::Instance arm, which learns the command from a running instance and cannot tell `aterm ctl fabric attach` from a launch with $ATERM_FABRIC_COMMAND set, so neither the doc nor the warning claims which it was; USAGE itself is unchanged, and the live run on this machine matched again: the same broker pid and label, and the node-gone warning now printed for n-1b631315bf5cae35 under instance 66439's `connected` bridge, exit 1; 2026-09-14 read again on the merge branch (the round rebased onto main at d0b30e46a): USAGE's six-section synopsis, its flag ownership and its exit paragraph against parse_args (no argument AND a leading flag are both `status`; `help`/`-h`/`--help` answer Cmd::Help; `--bodies`/`--from` under `status` and `--json` under `tail` are refused by name; any other word is a usage error), main/status_main/tail_main (exit 2 for a usage error and for every Off arm, exit_of's 0-or-1 straight off the warning list) and render_text, whose six section heads are the six USAGE names (CONFIG, BROKER, BRIDGES, SESSIONS, TRAFFIC, WARNINGS) and whose `reachable` line has the three arms read_ok/reachable/neither; TRAFFIC_ROWS = 10 behind `the last 10 bus records`; tail()'s Err on a broker that will not connect and on a closed subscription, both ExitCode::FAILURE; warnings()'s node arm reading r.nodes only for an instance whose own fabric is `connected`, so a remote node's row is still not this machine's warning; no claim contradicted; 2026-09-14 read of the prose added for `on|off|doctor` (USAGE's three synopsis lines and their entries, the module doc's grammar block and its rendezvous-file sentence, Source::Rendezvous's describe and token, Off::message naming `aterm fabric on`, the two warnings that name it, and the both-spellings doc naming `on` as the writer of the `aterm link serve` form) against parse_args (on's --dry-run/--service/--fleet/--tcp/--key-file and off's --dry-run/--service, each refused by name on the other verbs), main's three new arms, rendezvous_command in gather and tail_main (env, then `[fabric] command`, then the rendezvous file's command, then a running instance), and crates/aterm-link/src/enable.rs (on's steps in USAGE's order, Service::parse's three words, the --tcp/--key-file refusal text and its reason, off's kept identity and bus-log line, doctor's fix_for and its rendezvous check, Paths::resolve's ATERM_FABRIC_FLEET default), by feat/round-13-fabric-on; no claim contradicted; 2026-09-14 read of the round-13 prose (the module doc's BROKER, BRIDGES and WARNINGS bullets, InstanceView's reason / rtt_ms / link_age_ms docs, fabric_cell's doc, the stalled warning and the reworded connected-but-unanswered warning, the test docs) against read_instance (kv of reason / rtt_ms / link_age_ms off `fabric status`, a `-` reason dropped), fabric_cell's three arms, warnings()'s `stalled` arm and its `connected` arm's new wording, and enable.rs fix_for's `broker link is down` and `does not answer this report's own probe` arms with every_warning_shape_has_a_fix pinning both; no claim contradicted; 2026-09-14 read of the round-13 C prose (feat/round-13-fabric-on: the SESSIONS bullet of the module doc, USAGE's SESSIONS clause, the presence_in_toml and presence_from_config docs, SessionRow's bus_role/bus_title/detail/phase/context docs, bus_field, session_role and session_title, Report::presence, the CONFIG `presence` line and the two new test docs) against join_sessions (bus_field on both arms, `-` and empty read as absent), render_text (columns ROLE DETAIL PHASE CTX TITLE; the local instance's meta first for role and title, the bus row's for a remote session), render_json (detail, phase, context as a number off `<n>%`, config.presence) and gather (a `--presence` on the command wins, else presence_from_config, which is meta on a missing file, on a bad value with a word on stderr, and on an absent key); no claim contradicted; 2026-09-14 read of the `starting` warning (attached, no link report yet: a bridge older than the report shows connected at its first delivery and `post --wait` parks; a newer one reports within its dial) against aterm-gui fabric.rs bridge_attached (reason=starting, reported=false), link_evidence (a `deliver` or `outbox sent` from a never-reporting incarnation moves it to connected) and wait_is_futile (no refusal under starting), and against enable.rs fix_for's `has not reported its broker link` arm; no claim contradicted; 2026-09-15 read of round 15's deadline prose (shown by `xtask gate help-surfaces --diff`, matched at c1fc82257): the Overdue struct and the OVERDUE_SCAN_SPAN/overdue_work/overdue_of/OverdueScan docs against the fold (an ask/task with dl= whose t+dl is past, settled by an answer|report|ack carrying that re=, an `expired` reported and not settling, a note's dl= ignored, one page of bodies held at a time), the WARNINGS rule sentence and the two tails of the overdue warning against warnings(), and the overdue test fixtures against their assertions. The doc says settled by a reply `anywhere on the fleet's in lanes`, which is what the fold does — broader than the bridge's asker's-lane rule, so a reply delivered to another session hides the warning but not the `expired`; accurate as written, left for a follow-up. No claim contradicted; 2026-09-15 read of round 15's receipts prose: receipts_in_toml/receipts_from_config and their error strings against the code, and the report's `receipts on|off` line against the command's serve flags else the config file (the bridge's own precedence); no claim contradicted; 2026-09-15 round-16 read (feat/round-16-crosshost) of USAGE's on --tcp/--allow-remote, mint-for, --out, join and its flags, the seven-section status and the guarded-broker sentence, plus the module doc's NODES and guarded-read paragraphs, against parse_args (each flag on its own verb, refused by name elsewhere; mint-for's one positional), enable.rs wire_for_on/key_step/finish, join.rs mint_for/join (probe before any write, node refusal, install, local_broker_step, --service none touches no supervisor), probe_broker's head fallback on `unauthorized`, readable_faces/in_filters/last_records/overdue_work/tail_faces, render_nodes/node_where/live_sessions_on and BRIDGES keeping to local instances, and last_records' one shared window over every granted face (merged by offset, a record two faces match kept once); no claim contradicted; 2026-09-15 read of the prose the round-16 review moved (USAGE's --dry-run, --tcp/--key-file, mint-for, --out and join entries) against parse_args (join's --tcp a bare flag and --key-file required by join::check_inputs; on's --tcp <h:p>), join::mint_for (the SEALED refusal first, exit 2; the joined-root refusal when <root>/node.cap exists and minted_here is false; symlink_metadata refusing a non-regular --out; private_mode making an identical cap 0600 before the `(0600)` line) and join::join/probe_remote (the node's Last{/f/<F>/pub/<node>/node/presence} read after the probe, a live row on a root whose link-state/node is not the cap's node refused exit 2), and the dry-run caveat against astream-broker's stage_bind (the first attach of a bound rw grant appends one hidden /a/bind record, a repeat appends nothing); no claim contradicted; 2026-09-15 read of the twin's prose (BrokerView::serves_tcp, serves_socket's doc, the BROKER section's `serves` line and USAGE's `--tcp` entry: the port as well as the socket, this host's own bridges on the socket, the `wire` step, serves_tcp in the rendezvous) against status() (serves_tcp copied from the rendezvous file only when its broker is this command's, never probed), render_text's `serves` kv and render_json's `serves_tcp`, broker_pid's `--unix <sock>` match, and enable.rs wire_step/finish (the rendezvous's serves_tcp/serves_key_file on Wire::Sealed{bind: Some}); no claim contradicted; 2026-09-15 read of every prose line the round-16 branch changed since main, by the round-16 merge pass: USAGE's verbs and flags against parse_args (--fleet on and mint-for only, --dry-run/--service on/off/join, --tcp/--key-file on or join with join's --tcp bare, --allow-remote on, --broker/--cap-file/--node/--accept-from join only, --out mint-for only, one mint-for positional), the mint-for and join entries against join.rs mint_for (SEALED refusal, malformed and own id, the joined-root refusal through minted_here, the symlink refusal, private_mode on an identical cap) and join (a different node id refused before the probe, probe_remote before any write, the live-node refusal exit 2, install 0600, local_broker_step leaving --service none alone), and the module doc's NODES/guarded-read/serves prose against probe_broker (the whole fleet first, the first granted face on unauthorized), readable_faces, render_nodes, broker_pid's --unix match and broker_pid_tcp; no claim contradicted; 2026-09-17 read after rebasing onto main e6f88a94b: the status USAGE's SESSIONS clause against render_text's sessions table (the NODE column only when the fleet has more than one node, many_nodes — one slip fixed before this row: it said each session's node), the seven sections' order against render_text (CONFIG, BROKER, NODES, BRIDGES, SESSIONS, TRAFFIC, WARNINGS), and the mint-for/join entries and the module doc's NODES and guarded-read prose again against join.rs mint_for/join, render_nodes/node_where/live_sessions_on, readable_faces, probe_broker's refused arm and tail_faces; no other claim contradicted; 2026-09-17 read again by the pass that folded feat/round-16-crosshost into its two commits: the gate run with the roster as it stood before this row's 2026-09-17 entry named exactly these five surfaces, and each `--diff` above was re-read against the handlers named, hook.rs's and this row's claims included; no claim contradicted; 2026-09-20 (round 21) read of the report's changed prose against the code that produces it. The module header's `fabric=` paragraph gained `stale` and the BRIDGES cell a `stale (owed, last ack <age> ago)` arm: read against fabric_cell's match and against aterm-gui's fabric_state, which is the only writer of the token this report parses out of `fabric status`'s `state=`. A `stale` WARNING is added naming the fix (kill the bridge pid, the instance relaunches it): read against the warnings match, where it sits between the `disconnected` and `connected`-but-broker-unreachable arms and cannot fire for either. SESSIONS now LISTS a retired row under a local node as `gone` instead of folding it into the `exited` count: read against join_sessions, whose bus-only arm gained the local-node and GONE_SHOW_MS recency test, and against bridge.rs retire_ghost_presence, which writes the `state=exited` row that arm renders. CONFIG's `receipts` line is rewritten both ways round because the default moved to ON: read against receipts_from_config. No claim contradicted; 2026-09-20 (round 21 review) read of the ghost WARNING against `join_sessions`. Round 21 started LISTING a retired row as `gone`, and the warning selected on `pid.is_none()` alone — a test written by a reviewer showed it then fired for every session that ended normally in the last hour, saying 'the bus advertises @<sid> live' about rows whose `state=` is `exited`. Read against the bus-only arm of `join_sessions` that produces those rows and against `SessionRow::bus`; the warning now also requires `bus == \"live\"`. No claim contradicted; 2026-09-20 (round 21 review, second pass) read of one doc line and one test. `tail_line`'s doc said the `--bodies` ordering is 'the ordering `tui` keeps' — a module round 21 deleted; read against the renderer itself, which still puts the trust label before the text for the reason the sentence gives, and the claim is now in the past tense with the surviving reason kept. And the test over the `gone`-row warning fix was not exercising it: its `s-gone` row carried `t=1`, an hour past `GONE_SHOW_MS`, so `join_sessions` counted it into `exited` and it never reached the warning filter — the assertion passed with the filter present or absent. It now carries a fresh `crate::now_ms()`, asserts the row is LISTED as `gone`, asserts `exited` drops to 0, and asserts no `@s-gone` warning; with the filter reverted it fails, quoting the sentence. No claim contradicted; 2026-09-22 round-22 read of the `doctor` help against `enable::doctor`/`retire_ghosts_action` and of the new `ghost_rows`/`local_nodes`/`publish_exited`/`retired_body` against the bridge's own sweep: the help now names `--retire-ghosts [--yes]`, says DRY by default, and says it is refused while a local bridge runs (that refusal is real — `StateDir::reserve_sequence` is an unlocked file write, so a CLI racing the bridge for a sequence would be deduped into a retire that reports success and appends nothing). The `advertises @<sid> live` warning now names the flag as its fix, and its predicate and the doctor's are ONE function. No claim contradicted; 2026-09-22 round-22 REVIEW re-read of the `doctor` help and the ghost helpers: `--yes` without `--retire-ghosts` is now a usage error rather than a silent no-op; `every_instance_answered`/`unanswered` are shared with the `status` warning so the doctor cannot retire a row an unreachable instance may host; and the `the bus advertises` fix text names `--retire-ghosts` and no longer claims a relaunch clears a row whose incarnation died without a will — it cannot, which is the whole reason the flag exists. No claim contradicted; 2026-09-22 (round 23, broadcast) read of USAGE's new `--filter` entry, the SESSIONS clause and `is_subject_filter`'s doc against `parse_args`, `tail` and `read_session`. `--filter` is a `tail` flag: `parse_args` takes it only under `tail`, refuses it by name under `status` and under every other verb, and refuses a value that is not a subject filter BEFORE any broker is dialled (`is_subject_filter`: anchored at `/f/`, no empty segment, `*` and `>` the only wildcards, nothing outside a subject segment's characters — the charset half matters because the filter is echoed into the operator's terminal beside every record it matches, so a string that can carry an escape is one that can paint one). The doc's SHAPE-ONLY claim is exact: the fleet segment is checked in `tail`, against `fleet_root`'s prefix, where the config has been read; and the entry's promise that a guarded broker's refusal is REPORTED rather than silently followed by the granted faces is the `refused(&e) && want.is_some()` arm ahead of the faces fallback. The SESSIONS clause now names the TOPICS column, read against `read_session` (one `topic ls` per session per report; an `ERR` from an older instance leaves the list empty rather than marking the whole session `error=`), `render_text`'s column and `render_json`'s `topics` array, with `sessions_show_the_broadcast_topics_and_a_dash_for_none` pinning both and the `-` for a session that hears no broadcast. No claim contradicted; 2026-09-22 (round-23 review) read of `traffic_of`'s broadcast arm against the subject the bridge publishes (`/f/<F>/pub/<node>/<sid>/say/<topic>`, eight segments): `from=<sid>@<node>`, `to=say:<topic>`, kind from the body (`note` when absent); the seven-segment arm it replaced matched nothing that was ever published. `render_json` answers `topics:null` for a row no local instance hosts. Both pinned. No claim contradicted; 2026-09-22 (round 24, item 2) read of USAGE's rewritten `--retire-ghosts` entry against `enable::retire_ghosts_action` and `retire_through_bridge`: with `running_bridge` Some the CLI asks that instance's `fabric retire`, reads the bus back for up to RETIRE_WAIT (5 s) with `presence_states`, and reports per row; with none it calls `publish_exited` as before; an `ERR usage` answer (an instance older than 0.91) is reported with the quit-and-rerun remedy. The dry run's note names the bridge that would do it. No claim contradicted; 2026-09-22 (round-24 review fix, commit B) read of the doctor entry's last sentence against enable.rs `retire_through_bridge`: an instance from 0.91.0 or earlier answers `ERR usage`, which the doctor reports with the quit-and-rerun remedy; `node_hosted`/`retire_refusal` docs read against the bridge's `retire_on_request`. No claim contradicted; 2026-09-24 (lane D2) read of the SESSIONS paragraph and the two presence-mode words in the status report against presence.rs/bridge.rs: detail= and phase= are the bus row's (phase= the server's agent verdict the bridge relays), context= is dropped from the meta-mode description because no bridge writes it any more (the SessionRow.context reader stays for rows an older bridge wrote), and minimal no longer claims a screen it never read",
    ),
    (
        "crates/aterm-link/src/notify.rs",
        "9bb353d9b8be4c3e",
        "2026-09-22",
        "read against its parser/dispatch by the 2026-09-10 sweep (aterm-C-link-forge-verify); findings fixed in the commit that added this row; 2026-09-20 (round 21 review) read of three stale references. The header and the presence-row comment both named `glance.json` as a live PULL surface, and round 21 cut that file's writer — read against `glance::Row::parse`, which survives and is what the comment is actually about, and the sentences now name `aterm fabric`. The same four-copies claim as hook.rs is corrected the same way. Also fixed a reference that predates round 21: `column()` was cited as `main.rs`'s and lives in `cli.rs`, `main.rs` being a 13-line shim. No claim contradicted; 2026-09-20 (round 21 review, second pass) read of the two remaining `main.rs` citations in this file — the state-dir rule's doc and the assertion message it prints — against `cli.rs`, where `column()` and the rule actually live, `main.rs` being a 13-line shim. The copy count in the same sentence was already corrected to three in the first pass; this is the spelling of WHERE, which that pass missed. No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the state-dir rule's pin doc against cli.rs and notify.rs: the rule exists twice (hook.rs's copy went with the file). No claim contradicted",
    ),
    (
        "crates/aterm-messages/src/wire.rs",
        "145c4ef334a9035f",
        "2026-09-24",
        "2026-09-24 merge read (claude/unified-messages with origin/main 6afab83df): the prose is the branch's and main's joined by the engine stage — activity_rows_compat's three lanes (toolchain, update, harness; a harness note only ever `phase=done`, design ruling 61 renumbered, ruling 147) and ruling 116's current words on a `phase=live` line — read against activity_rows_compat; no claim contradicted. Main's read of the same row, merged: 2026-09-24 read (origin/main merged into ux/status-reporting, shown by `gate help-surfaces --diff`: 2 hunks, 2 prose lines): the two `design ruling 60` references are now `design ruling 61` — the branch's rulings 55–73 were renumbered 56–74 when main's ruling 55 (the full-width meter) took that number — and ruling 61 is still `tags::HARNESS`, a harness note a record (`Hold::LogOnly`, only ever `phase=done`), read against activity_rows_compat; no claim changed; 2026-09-23 read of decd72d86 (shown by `gate help-surfaces --diff`: 1 hunk, 4 prose lines moved, none reworded): the `Invariant 20` doc comment moved back onto the test it names (activity_rows_compat's byte-identity pin), below the busy/harness doc that now heads the busy-and-harness wire test; the sentences are unchanged and were read against those two tests and activity_rows_compat. Earlier: read on 2026-09-23 (the origin/main merge review of claude/unified-messages), the first read of this file, which b162eb2ca (Phase 0) added. THE TWO VERBS ARE NOT WIRED YET: READ_USAGE, POST_USAGE, parse_read_args, ReadQuery, message_row and PostRequest have no caller outside this file (Phase 5 of docs/DESIGN-unified-messages-2026-09-21.md adds `cmd_messages` / `cmd_notice` and their control_verbs.rs rows; neither verb is in the catalog today), so the two usage lines reach no reader yet, and this read is of each grammar against the parser beside it. READ_USAGE `messages [<n>] [since=<id>] [tag=<tag>] [sev=<sev>] [live]` read against parse_read_args: exactly five word shapes, in any order — `live`, a bare count (0 refused, clamped to LOG_CAP), `since=` (a u64 through MessageId::from_raw, so 0 is refused), `tag=` (Tag::try_new) and `sev=` (Severity::parse: success|info|warn|error) — and every other word is Err(READ_USAGE), pinned by the seven junk words in message_rows_carry_state_glass_and_progress. ReadQuery's field docs read against select: the newest n, still ascending (it skips len − n); since strictly greater; the severity a floor over the derived order Success < Info < Warn < Error, so `sev=warn` is warn + error; `live` is LogRecord::is_live (LogState::Posted). POST_USAGE `notice post <tag> [sev=<sev>] [key=<key>] [hold=<1..3600>] <title>[ -- <detail>]` read against PostRequest::parse: the tag is the first word and required; the three options are taken in any order until the first word that is none of them; a `hold=` outside 1..=MAX_WIRE_HOLD_SECS, or not a number, is Err(POST_USAGE); the title is what remains before ` -- `, clipped to TITLE_CAP, and an empty one is refused. The field docs hold: severity defaults to Info; key is clipped to KEY_CAP (an empty `key=` is no key, not an error); the detail is cut to WIRE_DETAIL_BYTES on a char boundary, split on a LITERAL backslash-n, trimmed, blank lines dropped, at most DETAIL_LINES_CAP lines; `the severity's hold when absent` is Hold::Default, which center.rs's hold_span resolves through Severity::default_hold; into_message's `NO actions ever` is true, as it adds none (pinned by notice_post_parses_the_grammar_and_never_authors_an_action). message_row's grammar line read against its format! field by field (message, at, ago_ms, tag, sev, state, glass, rep, key, title, detail, actions; then progress only for a live meter's fill, and since_ms only when retired_at is set, which log.rs documents as this process only), against design §5.1, and against its pinned row; `actions=` joins Intent::label, the FULL capsule label (Intent::short is the narrow form). state_word's doc read against its match and Hold::is_held (Default, For and Ask are held; Live and Standing read `live`), and its ONE-table claim against messages_host.rs's message_view, which calls wire::state_word for the page. activity_rows_compat's doc read against its body and its two host callers (lib.rs's Wake::ReadAppStatus arm and a messages_host test helper, both passing crate::control::pct_encode, which is aterm_control::wire::pct_encode as the module doc says): the toolchain lane then the update lane, each (posted_at, id) ascending, then the retired records of those tags that carry a retired_at, Superseded and Withdrawn excluded, (retired_at, id) ascending; `outcome=warn` iff severity >= Warn. The silent lane's Hold::LogOnly records (ruling 34) reach this face as `phase=done` rows because MessageCenter::post retires them Folded at ingress with retired_at = now — which is what upstream's record_toolchain ledger rows printed, so `per record those tags retired IN THIS PROCESS` covers them. The `status_bars.rs:971-999`, `:975` and `:4443` citations are PROVENANCE into the module Phase 1 deleted, checked against b162eb2ca's copy (activity_rows at 971-999, the progress rounding at 975, activity_rows_put_the_live_bars_above_the_finished_ones at 4443) and left as the crate's convention for ported rules (lib.rs, text.rs and glass.rs cite it the same way). The test's `aterm-control/src/wire.rs:28` is pct_encode's line, and the copy is its table byte for byte. ONE DOC LINK FIXED during this read: state_word's [`Retired::as_word`] named a type not in scope in this module, and `targo --unverified doc -p aterm-messages` warned it was unresolved; it now names crate::log::Retired::as_word and the doc build is clean. No other claim contradicted; 2026-09-24 read (the ux/status-reporting merge, bdbd8be16 + bc9449ab8), shown by `--diff` (4 hunks, 3 removed, 18 added): activity_rows_compat's doc now says `kind=<toolchain|update|harness>` and that a harness note is only ever `phase=done` (design ruling 61) — read against its body (`lanes = [toolchain, update, harness]`; a `Hold::LogOnly` record retires at post, so it never reaches the live loop) — and that appstatus reads a meter's FILL only (`progress_word(meter.fill_permille)`, so a busy row is `progress=-`); the new strings are activity_rows_compat_ignores_busy_and_lists_the_harness's fixtures, which pin both lines. No claim contradicted; the branch's reads of the same row: re-read on 2026-09-24 (the final gates of visual review round 3): the only prose \
         that moved (866daf7f2) is activity_rows_compat's first line, which now says the \
         `appstatus` body keeps the bars' byte-identical GRAMMAR while a `phase=live` line \
         carries the row's CURRENT words (design ruling 116). Read against its body and \
         b162eb2ca's status_bars.rs (activity_rows at 971-999, the two format! lines at \
         :982/:991): the same keys in the same order, `phase=live|done`, `outcome=-` on a \
         live line and ok|warn (Outcome::as_str) on a done one, `progress=<n>/100|-` with \
         the same half-up rounding (progress_word's (p + 5)/10 over permille), and \
         percent-encoding through `enc`, which all three callers pass as \
         crate::control::pct_encode (lib.rs's Wake::ReadAppStatus arm, messages_host's \
         appstatus test helper, and band_motion_tests' appstatus_carries_no_motion_words, \
         the third added since the last read); the live line's title, first detail line \
         and stats are the Live row's current Message, and ruling 116 (design line 3951) \
         says exactly that, pinned by appstatus_keeps_the_bars_grammar_in_the_rows_\
         current_words and a_routine_pass_raises_no_row_and_records_every_outcome. No \
         claim contradicted. \
         Earlier: read on 2026-09-23 (the origin/main merge review of claude/unified-messages), the first read of this file, which b162eb2ca (Phase 0) added. THE TWO VERBS ARE NOT WIRED YET: READ_USAGE, POST_USAGE, parse_read_args, ReadQuery, message_row and PostRequest have no caller outside this file (Phase 5 of docs/DESIGN-unified-messages-2026-09-21.md adds `cmd_messages` / `cmd_notice` and their control_verbs.rs rows; neither verb is in the catalog today), so the two usage lines reach no reader yet, and this read is of each grammar against the parser beside it. READ_USAGE `messages [<n>] [since=<id>] [tag=<tag>] [sev=<sev>] [live]` read against parse_read_args: exactly five word shapes, in any order — `live`, a bare count (0 refused, clamped to LOG_CAP), `since=` (a u64 through MessageId::from_raw, so 0 is refused), `tag=` (Tag::try_new) and `sev=` (Severity::parse: success|info|warn|error) — and every other word is Err(READ_USAGE), pinned by the seven junk words in message_rows_carry_state_glass_and_progress. ReadQuery's field docs read against select: the newest n, still ascending (it skips len − n); since strictly greater; the severity a floor over the derived order Success < Info < Warn < Error, so `sev=warn` is warn + error; `live` is LogRecord::is_live (LogState::Posted). POST_USAGE `notice post <tag> [sev=<sev>] [key=<key>] [hold=<1..3600>] <title>[ -- <detail>]` read against PostRequest::parse: the tag is the first word and required; the three options are taken in any order until the first word that is none of them; a `hold=` outside 1..=MAX_WIRE_HOLD_SECS, or not a number, is Err(POST_USAGE); the title is what remains before ` -- `, clipped to TITLE_CAP, and an empty one is refused. The field docs hold: severity defaults to Info; key is clipped to KEY_CAP (an empty `key=` is no key, not an error); the detail is cut to WIRE_DETAIL_BYTES on a char boundary, split on a LITERAL backslash-n, trimmed, blank lines dropped, at most DETAIL_LINES_CAP lines; `the severity's hold when absent` is Hold::Default, which center.rs's hold_span resolves through Severity::default_hold; into_message's `NO actions ever` is true, as it adds none (pinned by notice_post_parses_the_grammar_and_never_authors_an_action). message_row's grammar line read against its format! field by field (message, at, ago_ms, tag, sev, state, glass, rep, key, title, detail, actions; then progress only for a live meter's fill, and since_ms only when retired_at is set, which log.rs documents as this process only), against design §5.1, and against its pinned row; `actions=` joins Intent::label, the FULL capsule label (Intent::short is the narrow form). state_word's doc read against its match and Hold::is_held (Default, For and Ask are held; Live and Standing read `live`), and its ONE-table claim against messages_host.rs's message_view, which calls wire::state_word for the page. activity_rows_compat's doc read against its body and its two host callers (lib.rs's Wake::ReadAppStatus arm and a messages_host test helper, both passing crate::control::pct_encode, which is aterm_control::wire::pct_encode as the module doc says): the toolchain lane then the update lane, each (posted_at, id) ascending, then the retired records of those tags that carry a retired_at, Superseded and Withdrawn excluded, (retired_at, id) ascending; `outcome=warn` iff severity >= Warn. The silent lane's Hold::LogOnly records (ruling 34) reach this face as `phase=done` rows because MessageCenter::post retires them Folded at ingress with retired_at = now — which is what upstream's record_toolchain ledger rows printed, so `per record those tags retired IN THIS PROCESS` covers them. The `status_bars.rs:971-999`, `:975` and `:4443` citations are PROVENANCE into the module Phase 1 deleted, checked against b162eb2ca's copy (activity_rows at 971-999, the progress rounding at 975, activity_rows_put_the_live_bars_above_the_finished_ones at 4443) and left as the crate's convention for ported rules (lib.rs, text.rs and glass.rs cite it the same way). The test's `aterm-control/src/wire.rs:28` is pct_encode's line, and the copy is its table byte for byte. ONE DOC LINK FIXED during this read: state_word's [`Retired::as_word`] named a type not in scope in this module, and `targo --unverified doc -p aterm-messages` warned it was unresolved; it now names crate::log::Retired::as_word and the doc build is clean. No other claim contradicted",
    ),
    (
        "crates/aterm-nest/src/main.rs",
        "50a498270b151279",
        "2026-09-10",
        "read against its parser/dispatch by the 2026-09-10 sweep (aterm-B-window-and-clients); findings fixed in the commit that added this row",
    ),
    (
        "crates/aterm-primer/src/lib.rs",
        "9964087ba138659c",
        "2026-09-24",
        "2026-09-24 (harness fix wave, laws review minor): HARNESS_MARK's doc no longer names the deleted aterm_agent OWN_MARK; no help text changed ; 2026-09-24 (harness fix wave, E2E D4): read the new test's doc (the 0.91.0 app bundle's five `aterm link hook run` hooks are the start-up sweep's) against is_aterm_hook_command (LINK_HOOK_FORMS, bare_path_hook) and the owner's measured settings shapes; no help text changed ; re-read on 2026-09-13; FABRIC_NOTE and its doc carried the same false `post refuses` claim as the assets and are fixed in that commit; 2026-09-13 read of the prose 9758cd022 moved since that read (shown by `xtask gate help-surfaces --diff`, matched at 2493a60a7) — FABRIC_NOTE's rewritten `fabric=absent` sentence and the whole-block budget test's re-measured doc comment — against aterm-gui fabric.rs cmd_post (the row pushed into `posts` before any wait and `OK <id>` returned when `wait` is None, `--wait` ON by default only for `ask`/`task` via `matches!(kind, ask|task)`, and the option tokens to/kind/re/dl/via/len/--wait, none of them a caller-supplied dedup key, so the no-idempotency-key clause holds), fabric_wait_refusal/bridge_reachable/fabric_state, the wait loop's `ERR timeout id={id}` return, and Inbox::trim_retired_posts (evicts only rows with `off=` or `dead`, so a timed-out post is still queued), cross-checked against control_verbs.rs's `post` row and manual.rs's fabric page (all three not-landed answers mean queued; `aterm link mirror` is the file mirror the note points at); the budget doc's figures were MEASURED by building `primer_block` out of tree rather than recalled — codex 4354 and the other three 3564 against the test's `widest <= 4_400`, and 4297/3507 before the change — which confirms 4354/3564 but contradicts the doc's Fifty-four bytes on every agent's every turn: 54 is the overshoot past the retired 4_300 cap, while the recurring price is 57 (FABRIC_NOTE 1119 -> 1176 bytes, every agent's block +57), and the earlier raise from a guessed 4200 appears nowhere in this file's history (the cap was born 4_300 at 273a1d56d), both left for the orchestrator; the agent-facing sentence itself is sound except that `fabric=absent` alone does not imply `no-bridge=1` (fabric_wait_refusal answers `queued=1` while a supervisor is armed and the first bridge is still attaching) and an explicit `--wait` takes the same refusal on any kind, not only `ask`/`task` — both narrowings inherited from the sentence it replaced THE CONTRADICTION THAT READ FOUND WAS FIXED IN THIS COMMIT: the budget test's doc priced the addition at `Fifty-four bytes on every agent's every turn`, and the measured per-turn price is FIFTY-SEVEN (FABRIC_NOTE 1119 -> 1176, codex 4297 -> 4354, the other three 3507 -> 3564); 54 is the new widest block's overshoot past the RETIRED 4300 cap, a different quantity. The doc now states the measurement and says which slip it was. The unverifiable clause about `a guessed 4200` (no such cap literal exists in this file's history — the test was born at 4_300 and has been raised once, to 4_400) was removed with it. The hash recorded here is the CORRECTED doc; 2026-09-14 drift sweep (lane primer, 46 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 audit fixes read against the code beside them: FABRIC_NOTE's doc comment no longer claims a `fabric=` gate sentence the paragraph never had (PRIMER_BODY's aterm-detection gate is the only one it needs) and names `ERR timeout id=<n>` as the third queued outcome (read against fabric.rs cmd_post's wait deadline arm); the fabric doc is ONE body (assets/aterm-fabric-body.md) under four per-agent headers — Claude's frontmatter, Codex's none, OpenCode's description-only, Gemini's TOML — pinned by every_agent_gets_the_body_under_its_own_header; join_with_xdg's doc says a `.config/` row follows $XDG_CONFIG_HOME as OpenCode does (read against home_join's call sites and display_path); no claim contradicted; 2026-09-15 read of FABRIC_NOTE's `never re-post, unless under the same key=` against aterm-gui fabric.rs cmd_post (key= accepted on every kind and carried on the queued row) and aterm-link bridge.rs drain_outbox (a re-post under a key reuses the sequence the key reserved, the broker dedups it and the post answers the original offset with dup=1), and of the whole-block budget doc's round-15 note, whose sizes were DERIVED: MEASURED here with a throwaway test over primer_block — codex 4352, the other three 3562, FABRIC_NOTE 1174 — matching the derivation; the note now says measured. No claim contradicted; 2026-09-15 updater audit integration: read the changed path-context docs against auto_prime, status_line, agents_report and their with_xdg implementations. Each public boundary captures XDG once; detection, file operations and display use that same value. Scratch tests pass both roots explicitly, including relocated OpenCode install, idempotence, status and removal with an untouched inactive-default sentinel. Agent-report status/install/remove and skill rows now use display_path too; exact relocated paths and the original default column formatting are regression-tested. Primer/skill assets are unchanged.; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (FABRIC_NOTE's `never re-post, unless under the same key=` and the budget doc's round-15 note: 2 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 02285ecc6) plus upstream's change (the status, install and remove report lines printed through display_path as `{:<9} {:<30}` where they were `{:<9} ~/{:<28}`, the status test's new pin that every agent's default `~/{:<28}` spelling and padding stay byte-identical, agents_report's XDG_CONFIG_HOME doc, and the relocated-OpenCode test strings: 5 hunks, shown against round 15's read at 478963013), checked against display_path (`~/<rel>` unless $XDG_CONFIG_HOME moved a .config/ entry, so the default spelling and its 30-column padding are byte-identical), agents_report/agents_report_with_xdg, and primer_block, which upstream did not touch — the budget doc's measured sizes (codex 4352, the other three 3562, the cap 4400) stand, as the crate's own block-size test re-measures; 2026-09-17 read of the AGENT_FILES `var` column (CLAUDE_CONFIG_DIR, CODEX_HOME; gemini/opencode None until measured) and the AgentHome/agent_homes docs against agent_identity::env and the 79ce005c3 tests; 2026-09-17 read of the auto_prime_identity prose and the two test docs (--diff matched at 49aefbcc7) against auto_prime_identity's auto_prime_with_xdg(dir, None) beside auto_prime's xdg_config_home() read and join_with_xdg's `.config/` redirection; no claim contradicted re-read on 2026-09-18 for the first-launch disclosure: `AUTO_PRIME_OFF_SWITCH`, `AgentOutcome::wrote`, `AutoPrime::changed`, `auto_prime`'s summary paragraph and its four new tests were read against `auto_prime_with_xdg`'s write-result fold (`PrimerWrite::Created|Appended|Replaced`, `SkillWrite::Installed|Updated`), `AUTO_PRIME_NOTE` (which carries the switch verbatim, printed by the `status` and `remove` arms), `spawn.rs`'s `run_agent_prime` (which logs the summary only when `changed()`), and `MAX_RECORD_BYTES`. No claim contradicted.; 2026-09-22 round-22 REVIEW re-read of the installed skills against the round-22 defaults: `DRIVE_SKILL_BODY`'s stream list was the fourth copy of the vocabulary and lacked `mail`, so the surface agents READ never learned the stream exists — now fixed and pinned by `the_drive_skill_names_every_subscribe_stream`; the supervise skill's `drive task --no-nudge` recipes assumed a Stop hook that waits, which is opt-in since round 22, and now nudge by default. No claim contradicted — the skill test reads the `Streams ⊆ …` LINE, not the whole document: the body says `--mail` and `mail:kinds=` elsewhere, so a document-wide search passed while the list agents read omitted the stream, which is the defect itself; 2026-09-22 (2) re-read after a reviewer found two more `--no-nudge` recipes in `drive-aterm-skill.md` (the command table and the `task` paragraph) that assume a `Stop` hook which waits — opt-in since round 22 — so against a default install they post a task nothing wakes for. Both now nudge, and both name `--keep-alive` as the precondition for dropping it. The skill's stream-list test gained the same literal roster as the refusal's. No claim contradicted; 2026-09-22 read, rebased onto round 22: HookState::InstalledBy and its word, HookLane::install(force), from_current_exe (unix only, canonicalized), installed_exe, agents_report_with_lane's out-of-session sentence, and the hooks rows of status/install/remove, against the code beside them and aterm-link hook.rs's status verdicts; 56 tests; 2026-09-22 alignment read: HookLane::install no longer forces --rewake (round 22 made the Stop wait opt-in), withdraw_own/withdraw_hooks_with_lane remove only our own Installed block when `[harness] enabled = false`, and the docs of both read against the code and aterm-gui spawn.rs's hook plan; 57 tests; 2026-09-22 read of the change since the last read: the primer's Rust note now names the tools BY NAME in replies too (`tippy` not clippy, `targo` not cargo, `trustc` not rustc, `trustfmt` not rustfmt). Checked against atpkg reroute.rs `CARGO_VERB_REROUTES` (bare `clippy` to `tippy`, `cargo clippy` to `targo tippy`) and the same wording in `aterm help rust` (manual.rs). The rest of this surface is this row's earlier reads, including the hook-lane one recorded above.; 2026-09-22 (round 24, item 4) read of `MARK_BEGIN` (v8), `FABRIC_NOTE` and `CODEX_ADDENDUM` with their docs against `primer_block`/`auto_prime` and aterm-ctl's Denied hint: the note points only at `aterm help fabric`; the addendum is the one sentence (the socket is refused by this sandbox; driven from outside; no part in messaging; nothing to configure) and teaches no allowance. Pinned by only_codex_carries_the_sandbox_addendum and auto_prime_upgrades_a_v1_block_in_place (and the v3-upgrade test's doc, which names the sandbox sentence); 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of `remove_aterm_hooks`, `HookRemoval`, `strip_marked_hooks`, `write_backup`, the agents footer and the AgentHome doc against the pass and `aterm agents remove`: the hook lane, HookLane and installed_exe are gone; the pass removes marked entries once with a backup. Pinned by the_pass_removes_the_hook_entries_aterm_wrote_and_leaves_the_foreign_ones and the_installed_primer_and_skills_offer_no_hook; 2026-09-22 (round 25, item 3) the rewritten settings file ends with a newline, as the vendor's writer leaves it; `remove_aterm_hooks`'s doc read against it. No claim contradicted; 2026-09-22 merge read: RUST_NOTE's tool-name sentence checked against its budget/content test, the rust skill asset and aterm help rust; Trust spellings, lanes and PATH guidance agree. No claim contradicted; 2026-09-23 (fix/claude-at-exec-time, item 2b) read of the three hunks `xtask gate help-surfaces --diff` shows — write_backup's doc and the new backup-mode test's doc and strings — against write_backup (create_private_temp: create_new at 0600, then write_all, sync_all and set_permissions to the target's own metadata, which no umask masks), write_atomically's widen-after-write and remove_aterm_hooks (one backup per removal) — consistent; 2026-09-23 (lane D M1) read of the v9 inbox guidance against the code it describes: `aterm drive task` types `Inbox: task @<off>` through `turn idle=600` when the worker is idle (aterm-agent supervise/mail.rs NUDGE), `fabric=connected` is the status word aterm-gui fabric.rs answers for a live bridge link, and with `fabric=absent` no bridge drains the outbox, so nothing can arrive; the per-turn poll is gone; the verb pointer names `aterm ctl help` / `aterm ctl help <verb>` (measured: both answered by the control server, 10314 and 2860 bytes for `help` and `help key`, against 116677 for `aterm help introspection`); the rm sentence is guidance only, nothing enforces it; `remove_aterm_hooks` doc read against `is_aterm_hook_command` and `restore_harness_statusline`, pinned by four tests Re-read 2026-09-23 (review fixes): PRIMER_BODY names `aterm ctl --help` as the spelling that needs no socket (measured: rc 0, 23568 bytes with no socket, where `aterm ctl help` cannot connect); the `is_aterm_hook_command` doc read against `bare_path_hook` and `ATERM_EXE_NAMES` (now with `aterm-cli`); the `restore_harness_statusline` doc against the code (`<state>/statusline.user` from the state the harness's own command names, the `.aterm-harness.orig` copy alone only when that state is gone); the `remove_aterm_hooks` doc against its callers (`auto_prime` and `aterm agents remove`; aterm-gui calls it only through the pass).; 2026-09-24 merge read (harness/integrate: lane D's harness/d-decisionb over main at 9f67ca2ae): the merged prose is exactly lane D's sweep, v9 primer and statusLine-restore prose (read above) plus main's write_backup doc and backup-mode test (5f5ee37f9, read above); write_backup now serves lane D's removal unchanged (one backup per removal, born 0600 via create_private_temp and widened to the target's mode), and the_settings_backup_keeps_the_settings_files_mode runs over lane D's SETTINGS_WITH_ATERM_HOOKS fixture; no claim contradicted",
    ),
    (
        "crates/aterm-release/src/cli.rs",
        "40823d62f6c37191",
        "2026-09-22",
        "read against its parser/dispatch by the 2026-09-10 sweep; findings fixed in 412cf3acd; 2026-09-13 leftovers lane (8035c0c56, shown by `xtask gate help-surfaces --diff`: 4 hunks, 1 line removed, 17 added): the provision usage's `[--cert-dir <folder>]` and its paragraph (the request is copied there for the upload dialog, the .cer looked for there and in ~/.aterm/apple; without it every read stays inside ~/.aterm/apple; naming one is the consent and a note says so before the errand) read against apple.rs Watch::new/dirs (named first, then ~/.aterm/apple), find_matching_cert (reads only Watch::dirs), surface_csr (called only with the named folder, await_then_install's `.and_then(|named| surface_csr(…))`) and Watch::consent_note (printed before errand_lines when the named folder is under a protected root); the four parse refusals (`--cert-dir given twice`, `needs a folder`, `not an empty string`, the flag itself) read against parse's \"--cert-dir\" arm; the Cmd::Provision cert_dir field doc read against the same; no claim contradicted; AND 2026-09-18 re-read of the `ship provision` help's audited-items sentence (the front door is the store's targo + trustc answering --version, rustup's link reported informationally) against provision.rs front_door_check/front_door_verdict and rustup_note, by the rust-free host-lane round; no slip found; 2026-09-22 Linux merge read against parse/parse_cut/dispatch, linux::run_worker/check_identity/Handoff::new/import and publish run_cut: linux-build requires its four named operands and validates native target, positive build, full clean claim identity, private existing output directory and sealed Trust provenance; it builds a handoff locally without signing or publication. --linux-artifacts freezes the directory and required target set, defaults to both architectures, and missing handoffs pause before signing for resume. --linux-target is repeatable only for distinct supported targets and requires --linux-artifacts; incompatible resume/abandon combinations are refused. Current main cutter options and release policy retained.",
    ),
    (
        // Linux rebase read, 2026-09-23: the update help matches cmd_update,
        // linux::apply/status and the unchanged macOS automatic-apply ladder;
        // both catalog fixtures retain upstream's current wire rows.
        "crates/aterm-types/src/control_verbs.rs",
        "e9d7155478952bff",
        "2026-09-24",
        "2026-09-24 read of the timeline event vocabulary after the merged Fabric fetch wake: inbox_get_at records `fetch` with `off=` when a read is parked, FABRIC_EVENT_KINDS and subscribe.rs forward that timeline kind, and both help fixtures carry the same added token; no claim contradicted; 2026-09-24 read (origin/main merged into ux/status-reporting, shown by `gate help-surfaces --diff`: 3 hunks, 10 lines removed, 26 added): the `update` row is main's platform-specific text (d0a4c2930) with the branch's ladder bound kept — `no later than a minute after it was armed` — read against control.rs cmd_update (the Linux apply arm, the linux_* status tokens with `-`/`none` for an absent stage/trial) and native_update_auto_intent::LANDS_WITHIN (3 + 7 + 45 s) plus SWITCH_ALLOWANCE (5 s), const-asserted at most 60 s; the `privacy` row's rewritten claimants paragraph and its Settings ▸ Security sentence are main's (423f461e1), carried verbatim and read by main's own clause recorded below; the added test strings are main's `update` help pins, green on the merged row; both goldens regenerated by their ignored regen tests; no claim contradicted; 2026-09-23 read (the gates-and-documents review of the ux/status-reporting merge, shown by `gate help-surfaces --diff`) of `appstatus` and `appnotice` as the merge left them: `appstatus` said \"as the two status bars show it\", \"the harness never takes a bar\" and \"a live bar\" of a surface the unified message system deleted, and now says the message band, a harness note is a record, never a row, and a live row — read against aterm_messages::wire::activity_rows_compat (the three lanes toolchain/update/harness; live rows first, sorted by post; progress= from the fill alone, so a busy row with no fill reads `-`; done rows from the log ring's retired records, Superseded and Withdrawn excluded, oldest first by retirement; outcome=warn for Warn/Error) and message_reporters::harness_note (Hold::LogOnly, retired at its post, so always phase=done); `appnotice` (the merge's wording) read against App::take_app_notice: `managed-current:`/`machine-settings:` on the toolchain lane and every harness note reply OK recorded and post no row (a harness note repeated word for word is not written again, still OK recorded), any other toolchain/update text is toolchain_words::appnotice, an Info message (HOLD_INFO 30 s) replying OK posted, and any other lane is refused; the ctl/help goldens edited in the same commit. Earlier: 2026-09-23 read of the one reworded sentence (shown by `gate help-surfaces --diff`): the ladder now ends with the build applied no later than a minute after arming — read against `native_update_auto_intent::LANDS_WITHIN` (3 + 7 + 45 s = 55 s, when the ladder stops waiting) plus `SWITCH_ALLOWANCE` (5 s for the launch, park and freeze that follow), the two const-asserted to be at most 60 s and `apply_phase` (the same four phases and `ApplyPhase::as_str` spellings, unchanged), with the ctl/help goldens regenerated in the same commit; no claim contradicted. Earlier: 2026-09-21 read of the one added sentence in the `update` verb's long help (shown by `gate help-surfaces --diff`): while the posture is automatic, `apply_phase` names the ladder phase — prefer-idle, prefer-output-gap, keys-only, land — and the ladder ends with the build applied no later than 15 minutes after arming;2026-09-24 (harness fix wave, E2E D2): read META_ATTENTION_KEYED_MAX's doc against aterm-gui session_timeline's keyed attention write (value.len() after trim > the cap → AttentionWriteError, rendered `ERR attention too long (max 200 bytes)` by control_session.rs) and supervise's ATTENTION_BYTES; the `meta` help's `text 200B` agrees; no help text changed ; 2026-09-23 (lane E fix): read the `status` row's program= shim clause (a shell interpreter running atpkg's own shim for claude/codex — the script directly in the managed prefix's agents/ or bin/ — reads as that agent; a script of that name elsewhere stays the shell) against session_program::program_from_argv/atpkg_shim/script_in. No claim contradicted; 2026-09-23 (lane E fix): read the `meta` row's SUPERVISOR paragraph (`meta unset supervisor [holder=<holder>]`; prompts, questions and the limit walls raising no row under a live claim while overloaded/api-error/auth/context still raise theirs; the holder-conditional unset) against cmd_meta_supervisor, release_supervisor_held_by and status_item::escalation's supervisor_takes_over; the `await` row's deprecated `limited` alias against parse_agent_words. No claim contradicted; 2026-09-24 (lane E): read the `status` row's agent= vocabulary (wall:<kind> with the eight kinds, unknown for a reading without evidence), the agent_detail wall-reset wording, the level= `limited` = an agent at a wall gloss, and the program= shim clause against presence::{AgentPhase::word, wall_word, classify, AgentVerdict::detail}, PresenceSlot::level and session_program::program_from_argv; the `await` row's agent words against control_session::AGENT_WORDS/parse_agent_words. No claim contradicted; 2026-09-24: lane-C review-fix read — the `status` row's agent_gen=/agent_fp= and gen= fields and their prose against session_status_record and AgentPublication::wire_fields, the read-only verdict sentence against presence::bash_readings, the frame-only identification sentence against presence::agent_verdict; the `send` row's back-compat sentence (an older aterm types a leading if-gen= as text; `key` answers ERR usage) against main's take_leading_options; the `key` row's FENCES paragraph (if-gen=, if-seq= refused) against control_input; the `turn` row's cursor-row guard and `pressed=` against guarded_submit; the `await` row's wall words against parse_agent_words; the `subscribe` and `sessions` rows' stamped EVENT agent and agent_gen/agent_fp columns against publish_agent and wire_fields; the `meta` row's ttl lapse sentence against lapse_supervisor and App::presence_herald_tick. Goldens regenerated as a pair. No claim contradicted; 2026-09-24 (stage 2): read the new catalog prose against the handlers — `key`'s FENCES sentence (if-seq=/if-fp= against InputFence::check and the dispatch's fenced arm; `OK skipped reason=changed seq=<n>` is guarded_input_reply stamped by stamp_input_seq; the id= sequence given back is pty_idem::skipped_by_guard's `OK skipped` prefix), `send`'s pointer to it, `turn`'s submit=guarded:<re> (cmd_turn_guarded + guarded_submit, the Lines-framed skip verdict), `meta`'s KEYED ATTENTION and SUPERVISOR clauses (write_attention_owned, the 7-keyed-plus-bare cap in AttentionOwners::put, the 200 B keyed cap, sanitized() carrying only the bare entry, cmd_meta_supervisor's Owner-only gate and the connection/ttl lifetimes in ConnectionScope/claim_supervisor), and `status`/`sessions`' supervisor= field. The meta and key summaries were kept at their old text because summaries_fit_the_short_catalog_budget caps a row at 96 chars; both help goldens regenerated. No claim contradicted; 2026-09-24: read the lane-C rows against their handlers — status (why=, program=, agent=, agent_detail=, agent_rev=, agent_since_ms=, integration= against session_status_record, Slot::why, AgentPublication, agent_verdict and ShellIntegrationPosture; path= now after why=), await (the agent form against await_agent), subscribe (EVENT <local> agent <word> rev=<n> against timeline_wire_kind), sessions (the columns after path= against sessions_lines) and timeline (agent-change); no claim contradicted the code; 2026-09-21 read of the one added sentence in the `update` verb's long help (shown by `gate help-surfaces --diff`): while the posture is automatic, `apply_phase` names the ladder phase — prefer-idle, prefer-output-gap, keys-only, land — and the ladder ends with the build applied no later than 15 minutes after arming; read against `update_control::Snapshot::apply_phase` (the four `ApplyPhase::as_str` spellings, None for every non-automatic posture), `native_update_auto_intent::apply_phase` and `LANDS_WITHIN` (120 + 180 + 600 s = 15 min), and the ctl/help goldens regenerated in the same commit; no claim contradicted; catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 read of round 15's key and deadline catalog prose (shown by `xtask gate help-surfaces --diff`, matched at 135cf7646): `post`'s key= (aterm-gui fabric.rs valid_key; aterm-link state.rs key_seq/set_key_seq and KEYS_KEEP; bridge.rs drain_outbox's reserved sequence, and `dup=1` only for a key-chosen deduped publish) and dl= (note_deadline, settle_deadline_for, settled_on_bus, expire_deadlines); `outbox`'s `[key=<token>]` and `outbox sent … dup=1` against cmd_outbox/cmd_outbox_sent/retire_post; and the needle table's new rows against the rows they pin. THREE defects FIXED here: the `inbox` post-row grammar omitted the `key=` cmd_inbox prints on it; `post`'s futile-wait answer named `<absent|disconnected>` although fabric_wait_refusal answers `stalled` too; and `late=1` was unqualified although the bridge's `expired` map is in memory only, so a relaunched bridge delivers a late reply unflagged. Both help goldens regenerated as a pair. No other claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset catalog prose: `await inbox re=` against aterm-gui fabric.rs cmd_await_inbox (re= accepts every kind, since= defaults to 0); `inbox`'s oldest_on_bus header and verdict= against cmd_inbox/render_row; `inbox get @<off>` against inbox_get_at, deliver_fetched and Fetched::render (the ring answers only a whole row, FETCH_SLOTS 8, FETCH_WAIT_MS 10 s, `ERR fabric <state> off=` at once, chunked answers up to 256 KiB, one `ERR no such record`); `inbox seen`'s receipt against cmd_inbox_seen/owe_receipt and aterm-link bridge.rs send_receipt/retire_receipt; `post`'s --wait-ack bound (explicit ms, else dl= plus EXPIRY_GRACE_MS, else 30 s); `deliver`'s fetched= and receipt= forms against deliver_fetched/deliver_receipt and fetched_lines; `outbox`'s receipt and fetch lines against cmd_outbox; and SHORT_CATALOG_MAX_BYTES' round-15 raise, re-measured by printing cmd_help(\"\")'s length in help_short_form_is_bounded_and_is_the_summary_catalog: 9 863 B under 9 984. ONE defect FIXED here: a line continuation rendered `deliver`'s `verdict=<handled| refused|deferred>` with a stray space. Both help goldens regenerated as a pair. No other claim contradicted; AND THE v0.86 CANDIDATE'S READS THAT MAIN'S ROW CARRIED AHEAD OF THE READ ABOVE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally. 2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `await`, `inbox`, `inbox get`, `inbox seen`, `post`, `deliver`, `outbox` and `outbox sent` rows, the needle table's new rows, SHORT_CATALOG_MAX_BYTES' 9984 note and the `inbox get` history's round-15 aside: 8 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 595e13613) plus upstream's change (the `update` row's apply-posture sentences, the `privacy` row's this_process, app-data and unmeasured sentences and needles and the `trail status` row's ribbon_drawn=/ribbon_curtain_ms= keys and gloss, all read by main — and f3a5467ed's `version` row, which main recorded no row for: flavor=t|r, trust= as Trust's own version (`none` on an upstream build), rust_compat=, and trustc=/trustc_commit=/trustc_host= carrying the rustc-shaped token, shown against round 15's read at 030ba1788) plus ONE sentence added at this merge to `post`'s key= text — its ADDRESS is still resolved first, against the live roster, so a re-post to one that no longer routes is retired `ERR unroutable` (or `ambiguous`), appends nothing and leaves the key naming its record (both help goldens regenerated as a pair from the merged catalog), checked against aterm-gui build_info.rs control_line (trust= is `none` off flavor t and `unreported` on a Trust build predating the marker, a value the row does not list but does not deny; rust_compat= and trustc= are both compiler_release()), update_control.rs apply_posture and apply_policy_reason (the twelve words the row lists, none other), control_privacy.rs covers_split and observed_fda_scope, aterm-effects cursor_glow.rs TrailStatus::line, and aterm-link bridge.rs drain_outbox (resolve_to before StateDir::key_seq; the non-routing arm retires `off=- reason=` and clears only the post's pin) with the r15_receipts test a_keyed_re_post_to_a_session_whose_exit_line_never_arrives_is_unroutable_and_the_key_holds;  2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; AND UPSTREAM'S OWN READ OF THE SAME PROSE, recorded in parallel and kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 read at the landing of the erase pending-wrap fix, over f3a5467ed: read the version verb's new key detail (flavor=t|r, trust= as Trust's own version and none on upstream, rust_compat= as the compatible Rust release, trustc=/trustc_commit=/trustc_host= as release token, commit and host, the v0.10.0 rename, and trustc= equal to rust_compat=) against build_info control_line, compiler_release, compiler_commit_short, compiler_probe parse_rustc_vv and detect_flavor, build.rs, both version dispatch sites in aterm-gui control.rs, the CHANGELOG 0.10.0 entry, the two help goldens and this box's live rustc -vV, and no claim contradicted the code.; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged prose is round 15's read change plus main's, and it hashes to this row's recorded value, so no prose moved at the merge; 2026-09-15 read of the new `kitty` verb entry against its handlers: the summary `kitty [wear <key>]` and every field of the two documented row shapes read against App::kitty_collection_rows (`cat key={} coat={} iris={} age={} seen={} worn={worn}`, `seen=` being the roster row's `count`) and App::wear_kitty_on_front (`worn key={key} coat={} iris={}`) in crates/aterm-gui/src/app_input.rs; the `worn=1` claim against KittyLogHost::wearable, which derives the worn key from the cached `favourite` the companion reads rather than a second roster election (two pins inside one RFC3339 second tie, and the key breaks the tie the other way — found and fixed while writing the test); `the key is the row's own key=` against KittyLog::wear's `item.key == key`; the Favourite-This-Kitty claim against App::promotable_kitty (kitty_tenure.worn().map(|i| i.look).unwrap_or(self.launch_kitty)); `elects by GREATEST pin stamp` against KittyLog::favourite_look's max_by over `favourite`, and `survives a merge` against max_ts in favourite_collectible/merge_collectible; the three refusal cases against App::wear_kitty's sparkle gate, cfg.feline gate and None arm. `Write` rather than ConfigWrite is pinned by control.rs's required_op census. No claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the `metrics` row's echo paragraph, added by fe8ec4d58 (shown by `xtask gate help-surfaces --diff`, matched at b3a62ce1d), against crates/aterm-gui/src/control_query.rs cmd_metrics and crates/aterm-gui/src/echo_rtt.rs percentile_fields_text — the echo fragment does ride the PLAIN summary line, and the who-owes-the-time reading matches the episode the code itself records (n_echo=499 echo_p95_ms=75.69 echo_p99_ms=150.04 against aterm's own key_write_p99_ms=6.29). ONE SLIP FIXED in the commit that updated this row: the row said the line `ends ... then the CHILD's own round trip`, but the summary's LAST fragment is watchdog::turn_census_fields_text (e50cbbff4), spliced after echo_rtt's — the row now names that turn census and its seven fields as the end of the line, and lists echo_total= with the rest of the ledger percentile_fields_text actually prints; 2026-09-16 read of the tab entry's rewritten reply sentence a29417119 changed (OK active count only when the action HAPPENED, ERR reason when it did not: an index no tab holds, or a tab host that declined the close — a native document waiting on a durable checkpoint, or a close deferred behind a pending update handoff) against apply_tab_cmd_in's no_such bounds refusals for Select, Close(N) and both ends of Move; close_active_native_tab's Err arms (tab close deferred until update child rollback, returned behind defer_pending_update_handoff_teardown, which queues the mutation onto the pending handoff's teardown and applies it after the cancellation lands; native document close is waiting for a durable checkpoint; terminal close requires confirmation); and the regression test an_out_of_range_tab_action_refuses_instead_of_reporting_ok, which pins each refusal beside an in-range positive control; the headless sentence is unchanged and still holds; no claim contradicted; 2026-09-17 read of the round-18 identities rows: the recut `spawn` summary and its identity= sentences against control_media::parse_spawn_args/resolve_spawn_identity and control::escalated_op's fence; the `sessions` summary/detail `identity=` against control_session::sessions_lines; the `status` reply list against session_status::session_status_record; the new `identities` row's reply table against agent_identity::identities_reply arm by arm (list; one, `OK <n>` counting the agent lines since the client streams exactly n; forget unconfirmed, in use, removed, unknown, usage); the SHORT_CATALOG_MAX_BYTES paragraph against the short help measured on the live headless instance; 2026-09-17 read of the accounting paragraph's recount (--diff matched at 74d637316): 105 entries (help_catalog_full.txt carries 105 rows) and `OK 108` against control.rs's VERBS.len() + 3 status line; no claim contradicted; 2026-09-17 round-19 read: the new `story` row against control_query::parse_story / cmd_story and control.rs dispatch_story_verb (the closed set, the 96-byte cap, no control byte, `OK story=<n>`, Owner-only, `@<sid>` or bare), its phase-slot claims against presence::words (TOLD_FLASH, `✓ approved`, the a11y `approved by watcher`, Level::Story and the summary's `approvals` count), and its watcher claim against aterm-agent's story_of (an approval told bare); the `status` row's `hand=` / `level=` / `story=` grammar against presence::Hand::wire, Level::wire and App::presence_status_tail; the `chrome` row against App::presence_chrome_line (the rim tokens, the quote escaping, empty quotes when the row is folded); SHORT_CATALOG_MAX_BYTES' new paragraph MEASURED, not recalled (10 102 B against the raised 10 240; VERBS is 103 rows); the two goldens regenerated as a pair from this catalog. Three slips fixed before this row: the story summary ran to 129 chars over SUMMARY_MAX_CHARS and now reads `tell the window`; the chrome detail quoted the band grammar with its two-space gaps, which the table forbids; the story detail said the watcher sends `seq=` where it tells an approval bare; 2026-09-19 read of the `status` row's `hand=` clause the round-19 second fix reworded (shown by `--diff`): `:<holder>` names the driver when the turn came over an edge — the edge's source — and an Owner-token turn names nobody whatever edges stand; `driving:<sid>` is this session's own open turn into another — read against aterm-gui's `presence_facts` (`Lease::Turn { id, driver }` → `Hand::DrivenTurn { holder: driver.map(name_of_sid) }`, role or short sid), `driving_target` (a peer's lease naming this session as driver, never a standing edge) and `Hand::wire` (`turn:<id>[:<holder>]`, `driving:<sid>`); both goldens (`help_catalog_full.txt`, `ctl_help_full.txt`) carry the same clause; no claim contradicted; 2026-09-19 read of the merged `status` summary and detail (identity= after the fabric tail, then hand= level= story=) against session_status_record and presence_status_tail, the `story` row against dispatch_story_verb/parse_story/cmd_story, the `chrome` row against presence_chrome_line, Rim::wire, Level::wire, Hand::wire and the ReadChrome arm, and the SHORT_CATALOG_MAX_BYTES note re-measured through aterm-ctl on the headless live try at 10 238 B (10 199 B of content after the `OK 109 …` line, 106 rows), two bytes under 10 240; 2026-09-20 read of the privacy row's rewrite against control_privacy.rs's folder_state, folder_source, covers_split and NEVER_COVERED: the per-folder paragraph documents the `covered-by-fda` value and `source=` for the first time (nothing in the tree could emit either before this commit), states that an observation outranks the coverage rule, and keeps `unknown` as the default for a service with no measurement of its own; the coverage paragraph records §7 S4's 2026-09-19 measurement (four other-app data locations read with zero dialogs and no tccd traffic, the grant held) and why Full Disk Access is the ONLY durable answer to that prompt — macOS records an app-data allow against one process instance and ships no Settings switch for the class; and the never-covered class is named in full (cloud-storage File Provider domains, the media and photo libraries, App Management) to match the widened roster. Goldens regenerated on purpose with regen_help_catalog_golden; same day, second pass: the privacy row gained the `anchor=` paragraph, read against control_privacy.rs's install row and note arm and aterm_containment's ImageAnchor — the five tokens, which two mean no grant can be validated, that they earn their own note row, and WHY (running= follows the vnode and reads healthy in exactly that state). Golden regenerated with regen_help_catalog_golden; 2026-09-20 (round 21) read of the two changed verb entries against their handlers in aterm-gui. `status`'s `fabric=` paragraph gained `stale` as a fifth value, with what it names (a bridge attached, not reporting down, owing an answer for three link refreshes) and the fix: read against aterm-gui fabric.rs fabric_state, the only writer of this token, and against the sentence already in this entry saying a large age on `connected` means a quiet link — which is why the new value is NOT read off that number, and the entry now says so. `inbox seen` and `post`'s receipt prose no longer says `--receipts (which aterm fabric on writes)` but that receipts are the default whichever way the bridge was set up: read against aterm-link cli.rs parse_args and fabric.rs receipts_from_config. No claim contradicted; 2026-09-20 (round 21 review) read of `status`'s NO HEARTBEAT sentence against the ghost sweep. Any answered broker request is an ack and an ack becomes a `link up`, so the round-21 sweep on a free-running 60 s timer made every attached bridge report for ever and capped `fabric_link_age_ms=` at a minute — which would also have made the `stale` wording two paragraphs down unreachable in practice. Read against bridge.rs's gated sweep (candidates discovered once per attach, timer only while the set is non-empty) and against `ack_wants_report`; the entry now states the bounded exception. No claim contradicted; 2026-09-21 round-22 re-read of the `status` row against `session_status.rs`'s record builder, which gained `seq=`/`hash=`: both fields are appended last on both the observed and the never-classified branch, `seq=` is the terminal's `content_seq` and `hash=` is FNV-1a-64 of the untrimmed visible screen through `control_query::screen_stamp`, and `-`/`-` is what a contended `try_lock` answers — the row states all three. Its claim that this is the pair `turn` returns and `history` keeps per turn id is now true BY CONSTRUCTION rather than by coincidence: the turn verdict and the stamp both build their bytes with `control_query::screen_text`. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 read of the `subscribe` row against `Requested::parse`, `fold_mail_params`, `parse_mail_params` and `drain_mail_events`: `mail` added to the streams list with the `MAIL`/`GAP mail-dropped=` line shapes, the metadata-only rule, the live-stream seeding, the `kinds=`/`from=` grammar and its fail-closed cases, and the authority note (`inbox` and `subscribe` are the same `OpClass::Read` -> `ReadScreen`, so the stream is a push face on a read the subscriber can already make). The SUMMARY line is deliberately unchanged — it never enumerated every frame kind, and the short catalog's byte budget has no room. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 REVIEW: `SUBSCRIBE_STREAMS` added as the ONE stream vocabulary, after four hand-written copies of it were found (this catalog row, the `ERR usage` refusal, the parser's fold table and the installed drive skill) and the refusal was wrong for a whole round. The refusal and the fold table now derive from it; the skill is tested against it. No claim contradicted; 2026-09-22 (round 23, broadcast) read of every string this round changed against the handler each one describes. The NEW `topic` row (Owner, Lines, Session, OwnerOnly) was read against `fabric::cmd_topic` and `control::dispatch_topic_verb`: `ls`/the bare form answer `OK <n>` then one `topic <t> since=<head|@<off>>` row each, `add` answers `OK <t> since=<..> added=<0|1>` and `drop` `OK <t> dropped=<0|1>` — and because those two are STATUS lines while the listing is Lines, `framing_of` gained the `topic add|drop` flip beside `inbox seen`'s, the hazard `identities forget` measured on 2026-09-17 (a client that reads the TOPIC as a row count reports a malformed header). The claim that the set is EMPTY by default and that an empty set receives nothing is `SessionFabric::topics` plus `bridge::on_say_record`, whose recipient filter is the topic map and nothing else; the `since=` sentences are `bridge::sample_topics` (resolved once, at first sight) and `catch_up_topic`. The `post` row's summary now spells `say[:<topic>]` (read against `bridge::resolve_to` and `fabric::cmd_post`'s `valid_topic` check, which refuses a malformed topic at the post rather than retiring a record `unroutable` a publish later) and its broadcast paragraph was CORRECTED as well as extended: it said `task`/`ask` are demoted, and `bridge::DEMOTE_UNLESS_ACCEPTED` is `[\"task\", \"control\"]` — `ask` is not in it and never was, so the sentence now names task/control, read against `classify_kind`. 2026-09-22 (round 23 review) CORRECTION OF THE PREVIOUS CLAUSE: no committed `post` row ever said `task`/`ask` are demoted — `git show origin/main:crates/aterm-types/src/control_verbs.rs` and the item-1 commit both name `ask` only under kinds, `--wait` and `dl=`. The sentence that was corrected existed only in this round's uncommitted draft, so the clause above recorded a read of a defect that shipped help never had; what IS true is that the row now says task/control, read against `DEMOTE_UNLESS_ACCEPTED`. The `subscribe` row gained `mail:topic=<t>`, read against `subscribe::parse_mail_params` and `MailFilter::wants`. SHORT_CATALOG_MAX_BYTES was RAISED from 10 240 to 10 496 with the measurement in its doc (10 358 measured by `help_short_form_is_bounded_and_is_the_summary_catalog`, 118 over: 109 for the new row plus 9 for the `post` summary), which is the raise the previous paragraph predicted. Golden pair regenerated in order (types, then gui). No claim contradicted; 2026-09-22 (round-23 review) read of the `topic` row's `serial=<n>` clause against `cmd_topic` and `bridge::sample_topics`, and of the SHORT_CATALOG_MAX_BYTES doc against the measurement: rows sum to 9 986 (the types-side test's number), the `topic` row is 110 and the `post` row grew 10, and only aterm-gui's wire test sees the 10 358. No claim contradicted; 2026-09-22 merge read (round 23 onto main): upstream's `privacy` claimants=/census= rows read against control_query's privacy handler, with round 23's topic/say/subscribe rows — consistent; 2026-09-22 read of the one reworded sentence in the `trail` verb's long help (shown by `gate help-surfaces --diff`): the row now ends `ribbon_retired= ribbon_followed=`, read against `TrailStatus::line_v2` (cursor_glow.rs, the two fields last, in that order); `ribbon_followed=` is `rk::Status::followed`, the cumulative count `Engine::follow_rows` adds and `Engine::reset` keeps; the follow search is `Witness::follow_runs` (dr in -1, +1, -2, +2 — \"one or two rows\", same glyph at the same column, as a block); a run carried off the caret row is set flowing by `Ribbon::flow_cohort` and `Cohort::swoosh_total` is `FLOW_TOTAL_S` = 1.10 + 0.24 = 1.34 s from `Cohort::clock`; the `ribbon_retired=` melt is `RETIRE_MELT_S` = 0.12 s; merged with origin/main's round-22 reads of this file on 2026-09-22 by the rainbow-ribbon integration (both sides' prose kept; the hash is the merged file's); 2026-09-22 (round 24, item 1) read of the `topic` row's rewritten sentence and the `timeline` row's kind list against `fabric::cmd_topic` (an add that added and a drop that dropped each `record(\"topic\", …)` on the session timeline), `subscribe::timeline_wire_kind` via `FABRIC_EVENT_KINDS` (now six, `topic` last) and `bridge::on_event`'s `topic` arm → `topic_change`: the `ls` row carries no serial any more, and an add or drop reaches the bridge as `EVENT <local> topic add|drop …` at once. No claim contradicted; 2026-09-22 (round 24, item 2) read of the `fabric` row's summary (now `status|attach|retire`, trimmed to the summary bound) and its new `fabric retire` sentence against `cmd_fabric`/`cmd_fabric_retire` (control.rs), the roster journal's `RetireRequested` row and its `EVENT * fabric-retire <sid>` push (session_store.rs, subscribe.rs) and the bridge's `fabric-retire` arm → `retire_on_request` (hosting re-checked, the row published `exited` with its own fields through `publish` and the bridge's own sequence). No claim contradicted; merged with upstream's own read of the same day: 2026-09-21 read of the one added sentence in the `update` verb's long help (shown by `gate help-surfaces --diff`): while the posture is automatic, `apply_phase` names the ladder phase — prefer-idle, prefer-output-gap, keys-only, land — and the ladder ends with the build applied no later than 15 minutes after arming; read against `update_control::Snapshot::apply_phase` (the four `ApplyPhase::as_str` spellings, None for every non-automatic posture), `native_update_auto_intent::apply_phase` and `LANDS_WITHIN` (120 + 180 + 600 s = 15 min), and the ctl/help goldens regenerated in the same commit; no claim contradicted; catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 read of round 15's key and deadline catalog prose (shown by `xtask gate help-surfaces --diff`, matched at 135cf7646): `post`'s key= (aterm-gui fabric.rs valid_key; aterm-link state.rs key_seq/set_key_seq and KEYS_KEEP; bridge.rs drain_outbox's reserved sequence, and `dup=1` only for a key-chosen deduped publish) and dl= (note_deadline, settle_deadline_for, settled_on_bus, expire_deadlines); `outbox`'s `[key=<token>]` and `outbox sent … dup=1` against cmd_outbox/cmd_outbox_sent/retire_post; and the needle table's new rows against the rows they pin. THREE defects FIXED here: the `inbox` post-row grammar omitted the `key=` cmd_inbox prints on it; `post`'s futile-wait answer named `<absent|disconnected>` although fabric_wait_refusal answers `stalled` too; and `late=1` was unqualified although the bridge's `expired` map is in memory only, so a relaunched bridge delivers a late reply unflagged. Both help goldens regenerated as a pair. No other claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset catalog prose: `await inbox re=` against aterm-gui fabric.rs cmd_await_inbox (re= accepts every kind, since= defaults to 0); `inbox`'s oldest_on_bus header and verdict= against cmd_inbox/render_row; `inbox get @<off>` against inbox_get_at, deliver_fetched and Fetched::render (the ring answers only a whole row, FETCH_SLOTS 8, FETCH_WAIT_MS 10 s, `ERR fabric <state> off=` at once, chunked answers up to 256 KiB, one `ERR no such record`); `inbox seen`'s receipt against cmd_inbox_seen/owe_receipt and aterm-link bridge.rs send_receipt/retire_receipt; `post`'s --wait-ack bound (explicit ms, else dl= plus EXPIRY_GRACE_MS, else 30 s); `deliver`'s fetched= and receipt= forms against deliver_fetched/deliver_receipt and fetched_lines; `outbox`'s receipt and fetch lines against cmd_outbox; and SHORT_CATALOG_MAX_BYTES' round-15 raise, re-measured by printing cmd_help(\"\")'s length in help_short_form_is_bounded_and_is_the_summary_catalog: 9 863 B under 9 984. ONE defect FIXED here: a line continuation rendered `deliver`'s `verdict=<handled| refused|deferred>` with a stray space. Both help goldens regenerated as a pair. No other claim contradicted; AND THE v0.86 CANDIDATE'S READS THAT MAIN'S ROW CARRIED AHEAD OF THE READ ABOVE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally. 2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `await`, `inbox`, `inbox get`, `inbox seen`, `post`, `deliver`, `outbox` and `outbox sent` rows, the needle table's new rows, SHORT_CATALOG_MAX_BYTES' 9984 note and the `inbox get` history's round-15 aside: 8 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 595e13613) plus upstream's change (the `update` row's apply-posture sentences, the `privacy` row's this_process, app-data and unmeasured sentences and needles and the `trail status` row's ribbon_drawn=/ribbon_curtain_ms= keys and gloss, all read by main — and f3a5467ed's `version` row, which main recorded no row for: flavor=t|r, trust= as Trust's own version (`none` on an upstream build), rust_compat=, and trustc=/trustc_commit=/trustc_host= carrying the rustc-shaped token, shown against round 15's read at 030ba1788) plus ONE sentence added at this merge to `post`'s key= text — its ADDRESS is still resolved first, against the live roster, so a re-post to one that no longer routes is retired `ERR unroutable` (or `ambiguous`), appends nothing and leaves the key naming its record (both help goldens regenerated as a pair from the merged catalog), checked against aterm-gui build_info.rs control_line (trust= is `none` off flavor t and `unreported` on a Trust build predating the marker, a value the row does not list but does not deny; rust_compat= and trustc= are both compiler_release()), update_control.rs apply_posture and apply_policy_reason (the twelve words the row lists, none other), control_privacy.rs covers_split and observed_fda_scope, aterm-effects cursor_glow.rs TrailStatus::line, and aterm-link bridge.rs drain_outbox (resolve_to before StateDir::key_seq; the non-routing arm retires `off=- reason=` and clears only the post's pin) with the r15_receipts test a_keyed_re_post_to_a_session_whose_exit_line_never_arrives_is_unroutable_and_the_key_holds;  2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; AND UPSTREAM'S OWN READ OF THE SAME PROSE, recorded in parallel and kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 read at the landing of the erase pending-wrap fix, over f3a5467ed: read the version verb's new key detail (flavor=t|r, trust= as Trust's own version and none on upstream, rust_compat= as the compatible Rust release, trustc=/trustc_commit=/trustc_host= as release token, commit and host, the v0.10.0 rename, and trustc= equal to rust_compat=) against build_info control_line, compiler_release, compiler_commit_short, compiler_probe parse_rustc_vv and detect_flavor, build.rs, both version dispatch sites in aterm-gui control.rs, the CHANGELOG 0.10.0 entry, the two help goldens and this box's live rustc -vV, and no claim contradicted the code.; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged prose is round 15's read change plus main's, and it hashes to this row's recorded value, so no prose moved at the merge; 2026-09-15 read of the new `kitty` verb entry against its handlers: the summary `kitty [wear <key>]` and every field of the two documented row shapes read against App::kitty_collection_rows (`cat key={} coat={} iris={} age={} seen={} worn={worn}`, `seen=` being the roster row's `count`) and App::wear_kitty_on_front (`worn key={key} coat={} iris={}`) in crates/aterm-gui/src/app_input.rs; the `worn=1` claim against KittyLogHost::wearable, which derives the worn key from the cached `favourite` the companion reads rather than a second roster election (two pins inside one RFC3339 second tie, and the key breaks the tie the other way — found and fixed while writing the test); `the key is the row's own key=` against KittyLog::wear's `item.key == key`; the Favourite-This-Kitty claim against App::promotable_kitty (kitty_tenure.worn().map(|i| i.look).unwrap_or(self.launch_kitty)); `elects by GREATEST pin stamp` against KittyLog::favourite_look's max_by over `favourite`, and `survives a merge` against max_ts in favourite_collectible/merge_collectible; the three refusal cases against App::wear_kitty's sparkle gate, cfg.feline gate and None arm. `Write` rather than ConfigWrite is pinned by control.rs's required_op census. No claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the `metrics` row's echo paragraph, added by fe8ec4d58 (shown by `xtask gate help-surfaces --diff`, matched at b3a62ce1d), against crates/aterm-gui/src/control_query.rs cmd_metrics and crates/aterm-gui/src/echo_rtt.rs percentile_fields_text — the echo fragment does ride the PLAIN summary line, and the who-owes-the-time reading matches the episode the code itself records (n_echo=499 echo_p95_ms=75.69 echo_p99_ms=150.04 against aterm's own key_write_p99_ms=6.29). ONE SLIP FIXED in the commit that updated this row: the row said the line `ends ... then the CHILD's own round trip`, but the summary's LAST fragment is watchdog::turn_census_fields_text (e50cbbff4), spliced after echo_rtt's — the row now names that turn census and its seven fields as the end of the line, and lists echo_total= with the rest of the ledger percentile_fields_text actually prints; 2026-09-16 read of the tab entry's rewritten reply sentence a29417119 changed (OK active count only when the action HAPPENED, ERR reason when it did not: an index no tab holds, or a tab host that declined the close — a native document waiting on a durable checkpoint, or a close deferred behind a pending update handoff) against apply_tab_cmd_in's no_such bounds refusals for Select, Close(N) and both ends of Move; close_active_native_tab's Err arms (tab close deferred until update child rollback, returned behind defer_pending_update_handoff_teardown, which queues the mutation onto the pending handoff's teardown and applies it after the cancellation lands; native document close is waiting for a durable checkpoint; terminal close requires confirmation); and the regression test an_out_of_range_tab_action_refuses_instead_of_reporting_ok, which pins each refusal beside an in-range positive control; the headless sentence is unchanged and still holds; no claim contradicted; 2026-09-17 read of the round-18 identities rows: the recut `spawn` summary and its identity= sentences against control_media::parse_spawn_args/resolve_spawn_identity and control::escalated_op's fence; the `sessions` summary/detail `identity=` against control_session::sessions_lines; the `status` reply list against session_status::session_status_record; the new `identities` row's reply table against agent_identity::identities_reply arm by arm (list; one, `OK <n>` counting the agent lines since the client streams exactly n; forget unconfirmed, in use, removed, unknown, usage); the SHORT_CATALOG_MAX_BYTES paragraph against the short help measured on the live headless instance; 2026-09-17 read of the accounting paragraph's recount (--diff matched at 74d637316): 105 entries (help_catalog_full.txt carries 105 rows) and `OK 108` against control.rs's VERBS.len() + 3 status line; no claim contradicted; 2026-09-17 round-19 read: the new `story` row against control_query::parse_story / cmd_story and control.rs dispatch_story_verb (the closed set, the 96-byte cap, no control byte, `OK story=<n>`, Owner-only, `@<sid>` or bare), its phase-slot claims against presence::words (TOLD_FLASH, `✓ approved`, the a11y `approved by watcher`, Level::Story and the summary's `approvals` count), and its watcher claim against aterm-agent's story_of (an approval told bare); the `status` row's `hand=` / `level=` / `story=` grammar against presence::Hand::wire, Level::wire and App::presence_status_tail; the `chrome` row against App::presence_chrome_line (the rim tokens, the quote escaping, empty quotes when the row is folded); SHORT_CATALOG_MAX_BYTES' new paragraph MEASURED, not recalled (10 102 B against the raised 10 240; VERBS is 103 rows); the two goldens regenerated as a pair from this catalog. Three slips fixed before this row: the story summary ran to 129 chars over SUMMARY_MAX_CHARS and now reads `tell the window`; the chrome detail quoted the band grammar with its two-space gaps, which the table forbids; the story detail said the watcher sends `seq=` where it tells an approval bare; 2026-09-19 read of the `status` row's `hand=` clause the round-19 second fix reworded (shown by `--diff`): `:<holder>` names the driver when the turn came over an edge — the edge's source — and an Owner-token turn names nobody whatever edges stand; `driving:<sid>` is this session's own open turn into another — read against aterm-gui's `presence_facts` (`Lease::Turn { id, driver }` → `Hand::DrivenTurn { holder: driver.map(name_of_sid) }`, role or short sid), `driving_target` (a peer's lease naming this session as driver, never a standing edge) and `Hand::wire` (`turn:<id>[:<holder>]`, `driving:<sid>`); both goldens (`help_catalog_full.txt`, `ctl_help_full.txt`) carry the same clause; no claim contradicted; 2026-09-19 read of the merged `status` summary and detail (identity= after the fabric tail, then hand= level= story=) against session_status_record and presence_status_tail, the `story` row against dispatch_story_verb/parse_story/cmd_story, the `chrome` row against presence_chrome_line, Rim::wire, Level::wire, Hand::wire and the ReadChrome arm, and the SHORT_CATALOG_MAX_BYTES note re-measured through aterm-ctl on the headless live try at 10 238 B (10 199 B of content after the `OK 109 …` line, 106 rows), two bytes under 10 240; 2026-09-20 read of the privacy row's rewrite against control_privacy.rs's folder_state, folder_source, covers_split and NEVER_COVERED: the per-folder paragraph documents the `covered-by-fda` value and `source=` for the first time (nothing in the tree could emit either before this commit), states that an observation outranks the coverage rule, and keeps `unknown` as the default for a service with no measurement of its own; the coverage paragraph records §7 S4's 2026-09-19 measurement (four other-app data locations read with zero dialogs and no tccd traffic, the grant held) and why Full Disk Access is the ONLY durable answer to that prompt — macOS records an app-data allow against one process instance and ships no Settings switch for the class; and the never-covered class is named in full (cloud-storage File Provider domains, the media and photo libraries, App Management) to match the widened roster. Goldens regenerated on purpose with regen_help_catalog_golden; same day, second pass: the privacy row gained the `anchor=` paragraph, read against control_privacy.rs's install row and note arm and aterm_containment's ImageAnchor — the five tokens, which two mean no grant can be validated, that they earn their own note row, and WHY (running= follows the vnode and reads healthy in exactly that state). Golden regenerated with regen_help_catalog_golden; 2026-09-20 (round 21) read of the two changed verb entries against their handlers in aterm-gui. `status`'s `fabric=` paragraph gained `stale` as a fifth value, with what it names (a bridge attached, not reporting down, owing an answer for three link refreshes) and the fix: read against aterm-gui fabric.rs fabric_state, the only writer of this token, and against the sentence already in this entry saying a large age on `connected` means a quiet link — which is why the new value is NOT read off that number, and the entry now says so. `inbox seen` and `post`'s receipt prose no longer says `--receipts (which aterm fabric on writes)` but that receipts are the default whichever way the bridge was set up: read against aterm-link cli.rs parse_args and fabric.rs receipts_from_config. No claim contradicted; 2026-09-20 (round 21 review) read of `status`'s NO HEARTBEAT sentence against the ghost sweep. Any answered broker request is an ack and an ack becomes a `link up`, so the round-21 sweep on a free-running 60 s timer made every attached bridge report for ever and capped `fabric_link_age_ms=` at a minute — which would also have made the `stale` wording two paragraphs down unreachable in practice. Read against bridge.rs's gated sweep (candidates discovered once per attach, timer only while the set is non-empty) and against `ack_wants_report`; the entry now states the bounded exception. No claim contradicted; 2026-09-21 round-22 re-read of the `status` row against `session_status.rs`'s record builder, which gained `seq=`/`hash=`: both fields are appended last on both the observed and the never-classified branch, `seq=` is the terminal's `content_seq` and `hash=` is FNV-1a-64 of the untrimmed visible screen through `control_query::screen_stamp`, and `-`/`-` is what a contended `try_lock` answers — the row states all three. Its claim that this is the pair `turn` returns and `history` keeps per turn id is now true BY CONSTRUCTION rather than by coincidence: the turn verdict and the stamp both build their bytes with `control_query::screen_text`. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 read of the `subscribe` row against `Requested::parse`, `fold_mail_params`, `parse_mail_params` and `drain_mail_events`: `mail` added to the streams list with the `MAIL`/`GAP mail-dropped=` line shapes, the metadata-only rule, the live-stream seeding, the `kinds=`/`from=` grammar and its fail-closed cases, and the authority note (`inbox` and `subscribe` are the same `OpClass::Read` -> `ReadScreen`, so the stream is a push face on a read the subscriber can already make). The SUMMARY line is deliberately unchanged — it never enumerated every frame kind, and the short catalog's byte budget has no room. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 REVIEW: `SUBSCRIBE_STREAMS` added as the ONE stream vocabulary, after four hand-written copies of it were found (this catalog row, the `ERR usage` refusal, the parser's fold table and the installed drive skill) and the refusal was wrong for a whole round. The refusal and the fold table now derive from it; the skill is tested against it. No claim contradicted; 2026-09-22 (round 23, broadcast) read of every string this round changed against the handler each one describes. The NEW `topic` row (Owner, Lines, Session, OwnerOnly) was read against `fabric::cmd_topic` and `control::dispatch_topic_verb`: `ls`/the bare form answer `OK <n>` then one `topic <t> since=<head|@<off>>` row each, `add` answers `OK <t> since=<..> added=<0|1>` and `drop` `OK <t> dropped=<0|1>` — and because those two are STATUS lines while the listing is Lines, `framing_of` gained the `topic add|drop` flip beside `inbox seen`'s, the hazard `identities forget` measured on 2026-09-17 (a client that reads the TOPIC as a row count reports a malformed header). The claim that the set is EMPTY by default and that an empty set receives nothing is `SessionFabric::topics` plus `bridge::on_say_record`, whose recipient filter is the topic map and nothing else; the `since=` sentences are `bridge::sample_topics` (resolved once, at first sight) and `catch_up_topic`. The `post` row's summary now spells `say[:<topic>]` (read against `bridge::resolve_to` and `fabric::cmd_post`'s `valid_topic` check, which refuses a malformed topic at the post rather than retiring a record `unroutable` a publish later) and its broadcast paragraph was CORRECTED as well as extended: it said `task`/`ask` are demoted, and `bridge::DEMOTE_UNLESS_ACCEPTED` is `[\"task\", \"control\"]` — `ask` is not in it and never was, so the sentence now names task/control, read against `classify_kind`. 2026-09-22 (round 23 review) CORRECTION OF THE PREVIOUS CLAUSE: no committed `post` row ever said `task`/`ask` are demoted — `git show origin/main:crates/aterm-types/src/control_verbs.rs` and the item-1 commit both name `ask` only under kinds, `--wait` and `dl=`. The sentence that was corrected existed only in this round's uncommitted draft, so the clause above recorded a read of a defect that shipped help never had; what IS true is that the row now says task/control, read against `DEMOTE_UNLESS_ACCEPTED`. The `subscribe` row gained `mail:topic=<t>`, read against `subscribe::parse_mail_params` and `MailFilter::wants`. SHORT_CATALOG_MAX_BYTES was RAISED from 10 240 to 10 496 with the measurement in its doc (10 358 measured by `help_short_form_is_bounded_and_is_the_summary_catalog`, 118 over: 109 for the new row plus 9 for the `post` summary), which is the raise the previous paragraph predicted. Golden pair regenerated in order (types, then gui). No claim contradicted; 2026-09-22 (round-23 review) read of the `topic` row's `serial=<n>` clause against `cmd_topic` and `bridge::sample_topics`, and of the SHORT_CATALOG_MAX_BYTES doc against the measurement: rows sum to 9 986 (the types-side test's number), the `topic` row is 110 and the `post` row grew 10, and only aterm-gui's wire test sees the 10 358. No claim contradicted; 2026-09-22 merge read (round 23 onto main): upstream's `privacy` claimants=/census= rows read against control_query's privacy handler, with round 23's topic/say/subscribe rows — consistent; 2026-09-22 read of the one reworded sentence in the `trail` verb's long help (shown by `gate help-surfaces --diff`): the row now ends `ribbon_retired= ribbon_followed=`, read against `TrailStatus::line_v2` (cursor_glow.rs, the two fields last, in that order); `ribbon_followed=` is `rk::Status::followed`, the cumulative count `Engine::follow_rows` adds and `Engine::reset` keeps; the follow search is `Witness::follow_runs` (dr in -1, +1, -2, +2 — \"one or two rows\", same glyph at the same column, as a block); a run carried off the caret row is set flowing by `Ribbon::flow_cohort` and `Cohort::swoosh_total` is `FLOW_TOTAL_S` = 1.10 + 0.24 = 1.34 s from `Cohort::clock`; the `ribbon_retired=` melt is `RETIRE_MELT_S` = 0.12 s; merged with origin/main's round-22 reads of this file on 2026-09-22 by the rainbow-ribbon integration (both sides' prose kept; the hash is the merged file's); 2026-09-22 read of the tone and trail rows' new audibility prose — the seam= verdict and its seven witnesses, audio= as the audio worker's state word, reopens_left=, and trail status's sound_seam= — against sound_seam.rs's SeamReason table (the nine reasons the help names are exactly the nine it spells), tone_infer::AudioHost::label over trail_audio::HostState (eight states; `reopening` was printed on the wire but missing from the help's enumeration, fixed in the commit that re-recorded this row) and control.rs's tone_status_help_enumerates_exactly_the_keys_the_row_emits (set equality both ways between the help and the emitted row); merged with origin/main's round-23 read of this file, upstream's row kept and this read appended; 2026-09-22 merge read (round 24 + v0.91.0): the topic row without serial and the `fabric retire` row read against the handlers — consistent; 2026-09-22 (round-24 review fix, commit A) read of the `subscribe` row's events and sessions clauses against subscribe.rs `drain_timeline_events` (the `GAP <local> events-dropped=<n>` it emits when the watermark is below the timeline's retained low) and `drain_instance_events` (`EVENT * fabric-retire <sid>` for `RosterChange::RetireRequested`). No claim contradicted; 2026-09-22 (round-24 review fix, commit C) read of the `status` synopsis against fabric.rs (five `fabric=` states, `stale` derived) and of the `subscribe` summary against control.rs's grammar (`@*` accepted). No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the `status` (stamp) and `spawn` (identity=) help against control_query.rs `screen_stamp` and agent_identity.rs `ensure_in`: no hook carries the stamp, no hook block is carried into an identity. No claim contradicted; 2026-09-22 read of the `path=<frozen|live>` sentences added to the `sessions` summary/detail and to the `status` Reply grammar against control_session.rs::sessions_lines (last token, after identity=) and session_status.rs's two status format strings (after story=, before the seq=/hash= stamp, which assert_stamp_rides_last keeps last); the sessions summary lost its `rows:` word to stay under SUMMARY_MAX_CHARS (summaries_fit_the_short_catalog_budget); both help goldens regenerated as a pair; 2026-09-23 (p2/gui review) read of the `appnotice` long help: marker-shaped toolchain text is taken as the marker is (an `appstatus` ledger entry, no row) and replies `OK recorded`, against `App::take_app_notice` and `control_query::cmd_appnotice`; any other text an Info row held 30 s (HOLD_NOTICE) then kept in the ledger, replying `OK posted`; the summary line is unchanged; 2026-09-24 read of the `appstatus`/`appnotice` prose the harness lane changed (shown by `xtask gate help-surfaces --diff`; appnotice summary, detail and `appstatus` kind list) against StatusBars::activity_rows (a ledger row prints `kind=<Lane::as_str>`, and Lane::Harness is never in `bars()`, so its rows are always phase=done), control_query::cmd_appnotice's lane gate and App::take_app_notice's `harness` arm (`OK recorded`, pinned by appnotice_says_recorded_for_a_marker_and_posted_for_a_row and a_harness_note_is_recorded_and_never_takes_a_row), and history_log::Topic::Harness (the history line the detail names); both goldens regenerated by name; 2026-09-24 (harness/integrate, the fence switch) read of the `text` entry's added sentence (`--json` always carries \"gen\":\"<epoch>.<seq>\" after seq, read under the same lock, what a fenced `key if-gen=` names) against aterm-gui control_query.rs cmd_text_json_opt and control_input.rs's if-gen= arm; the two help goldens regenerated by their ignored regen tests; no claim contradicted; 2026-09-24 read (the ux/status-reporting merge's log port, ba7e63129), shown by `--diff` (1 hunk, 4 removed, 4 added): appnotice's one line `post a message (harness: a record, never a row)` and its long help — a marker-shaped toolchain text and every harness note are records on Settings ▸ Messages and appstatus, `OK recorded`; any other text an Info row held 30 s, `OK posted` — read against lib.rs take_app_notice (post_managed_current / post_machine_settings / record_harness_note → App::record_message, Hold::LogOnly; toolchain_words::appnotice → an Info message at the default 30 s hold) and control_query::cmd_appnotice's owner gate; both help goldens regenerated as a pair. No claim contradicted; 2026-09-23 read of the privacy row's rewritten `claimants=` paragraph and corrected `running=` sentence against PrivacySnapshot::claimant_lines, ConsentState::claimants and consent::claimants_for: `age_ms=`, the eight-row cap with true counts, the candidate sources (the running bundle's folder, /Applications, ~/Applications, LaunchServices), the four `off` causes, and `running=` as the frozen exec-time path (install_posture_rows reads current_exe once); both goldens regenerated as a pair; no claim contradicted; 2026-09-24 read of the privacy row's two changes against consent::classify_claimants and the Security panel: a registered copy under a .Trash/.Trashes component is filtered before it is read (in_trash), and Settings ▸ Security lists conflicting copies and offers MACOS_ACCESS_TRASH only for Claimants::retirable copies, which app_control::act_app refuses under MACOS_ACCESS_GESTURE_PREFIX (the_security_panels_owner_gestures_are_refused_on_the_control_surface); both goldens regenerated as a pair; no claim contradicted; 2026-09-24 re-read of the privacy row's Trash sentence after the owner-confirm fix: the press is confirmed through ConsentGestures::confirm (menu::confirm_owner, fail-closed) before MACOS_ACCESS_TRASH records a plan, and app act still refuses it; both goldens regenerated as a pair; no claim contradicted; 2026-09-24 final-integration merge read (harness/integrate over origin/main at 423f461e1): the hunks `--diff` shows are main's (the `update` row's macOS/Linux split and the privacy row's claimants rewrite, both read by main's own clauses above) and their test literals; lane C's status/key/send/turn/await/meta prose is unchanged by the merge; both goldens are regenerated as a pair after the lane merges; 2026-09-24 final-integration merge read (lane E over main and B2): the hunks `--diff` shows are lane E's (meta's `unset supervisor [holder=]` and the supervised-walls sentence, status's shim/program and reader/wall:<kind>/unknown clauses, level's limited-is-a-wall note, await agent's word list with the deprecated `limited` alias), read above by E's own clause; main's update/privacy rows sit beside them unchanged; both goldens are regenerated from this merged catalog; 2026-09-24 final-integration glue read: the `meta` row's supervised-walls sentence now says every `wall:<kind>` raises no menu row or notification while a claim is live (the supervisor answers or escalates it), read against aterm-gui status_item::supervisor_escalates_wall (true for every wall kind) and supervisor_takes_over, pinned by a_supervised_wall_is_the_supervisors_and_an_unsupervised_one_raises_its_row, and against aterm-agent policy::turn_end's wall arm (each kind retried, typed or escalated); both goldens regenerated as a pair; 2026-09-24 merge read (origin/main dd2581889 into harness/integrate, shown by `gate help-surfaces --diff`: 2 hunks) — the `update` entry's one-minute ladder bound and the `appstatus`/`appnotice` harness-lane rows are main's text, read by main's clauses in this row. ONE CLAIM the merge made stale, corrected in the commit after it: `the harness lane (what aterm harness posts)` names main's `harness watch`, which this branch deleted — `aterm harness` posts nothing; 2026-09-24 (the fix after the origin/main merge) read of the `appnotice` entry's harness-lane parenthesis against the tree: `what aterm harness posts` named main's `harness watch`, deleted on this branch, and `aterm harness` (harness/cli.rs) writes no `appnotice`; it now says an agent harness's notes are posted from outside the window and that aterm's own supervisor (in-window, harness_host) posts none — no `record_harness_note` caller but the appnotice handler; both help goldens regenerated by their ignored regen tests (the one line); consistent; 2026-09-24 read of the who long help nonce clause against control_session::cmd_who and cmd_sessions_store: cmd_who appends h.nonce.to_hex() as the final token from the same registry snapshot, and unlike sessions it never asks session_window_rows for a main-thread placement hop; the OwnerOnly spec agrees with control.rs owner-class gates. No claim contradicted; 2026-09-24 read of the who row's fgpgid clause against control_session::cmd_who and upgrade_drive::host_candidates: who appends the PTY master's foreground group (or - on unreadable), the host requires a unique group plus matching process controlling-terminal foreground group, and a readable conflicting tab env vetoes the match. Both goldens regenerated as a pair; no claim contradicted; 2026-09-24 merge read (origin/main 182e44a03 into harness/integrate): the `timeline` entry's kind list is the union of this branch's `agent-change` and main's `fetch`, read against session_timeline's recorded kinds and FABRIC_EVENT_KINDS; the `who` entry is main's text (nonce=, fgpgid=), read by main's two clauses above; both goldens regenerated as a pair by their ignored regen tests. Consistent; 2026-09-24 merged Fabric batch read: the sessions status help names the same five status fields including agent, the sid and nonce fence, and the single-session fallback; both catalog goldens match the generated text and the GUI producer and bridge parser implement it. Consistent; 2026-09-24 second merge read (claude/unified-messages with origin/main after 5d4598a77): the `appnotice` detail takes main's parenthesis on the harness lane (an agent harness's notes, posted from outside the window; aterm's own supervisor runs inside it and posts none) and keeps every lane a record replying `OK recorded`, read against control_query::cmd_appnotice; both goldens regenerated by their ignored tests; 2026-09-24 merge build-stage read: the `appnotice` summary is `record a text in the message log` — the `(a record, never a row)` tail put it at 101 chars, over the 96-char short-catalog cap (summaries_fit_the_short_catalog_budget); the detail still says every text on every lane is recorded and never raises a row, replying `OK recorded`, read against control_query::cmd_appnotice; both goldens carry the same text; 2026-09-24 merge read (claude/unified-messages with origin/main 6afab83df): the `appnotice` row now says every lane is a record replying `OK recorded`, the harness lane an `appstatus` entry of kind=harness (design ruling 147), read against control_query::cmd_appnotice and lib.rs take_app_notice, and both help goldens (ctl_help_full.txt, help_catalog_full.txt) carry the same text; the `appstatus` row is main's (kind=harness, `the message band`). Main's read of the same row, merged: 2026-09-24 read (origin/main merged into ux/status-reporting, shown by `gate help-surfaces --diff`: 3 hunks, 10 lines removed, 26 added): the `update` row is main's platform-specific text (d0a4c2930) with the branch's ladder bound kept — `no later than a minute after it was armed` — read against control.rs cmd_update (the Linux apply arm, the linux_* status tokens with `-`/`none` for an absent stage/trial) and native_update_auto_intent::LANDS_WITHIN (3 + 7 + 45 s) plus SWITCH_ALLOWANCE (5 s), const-asserted at most 60 s; the `privacy` row's rewritten claimants paragraph and its Settings ▸ Security sentence are main's (423f461e1), carried verbatim and read by main's own clause recorded below; the added test strings are main's `update` help pins, green on the merged row; both goldens regenerated by their ignored regen tests; no claim contradicted; 2026-09-23 read (the gates-and-documents review of the ux/status-reporting merge, shown by `gate help-surfaces --diff`) of `appstatus` and `appnotice` as the merge left them: `appstatus` said \"as the two status bars show it\", \"the harness never takes a bar\" and \"a live bar\" of a surface the unified message system deleted, and now says the message band, a harness note is a record, never a row, and a live row — read against aterm_messages::wire::activity_rows_compat (the three lanes toolchain/update/harness; live rows first, sorted by post; progress= from the fill alone, so a busy row with no fill reads `-`; done rows from the log ring's retired records, Superseded and Withdrawn excluded, oldest first by retirement; outcome=warn for Warn/Error) and message_reporters::harness_note (Hold::LogOnly, retired at its post, so always phase=done); `appnotice` (the merge's wording) read against App::take_app_notice: `managed-current:`/`machine-settings:` on the toolchain lane and every harness note reply OK recorded and post no row (a harness note repeated word for word is not written again, still OK recorded), any other toolchain/update text is toolchain_words::appnotice, an Info message (HOLD_INFO 30 s) replying OK posted, and any other lane is refused; the ctl/help goldens edited in the same commit. Earlier: 2026-09-23 read of the one reworded sentence (shown by `gate help-surfaces --diff`): the ladder now ends with the build applied no later than a minute after arming — read against `native_update_auto_intent::LANDS_WITHIN` (3 + 7 + 45 s = 55 s, when the ladder stops waiting) plus `SWITCH_ALLOWANCE` (5 s for the launch, park and freeze that follow), the two const-asserted to be at most 60 s and `apply_phase` (the same four phases and `ApplyPhase::as_str` spellings, unchanged), with the ctl/help goldens regenerated in the same commit; no claim contradicted. Earlier: 2026-09-21 read of the one added sentence in the `update` verb's long help (shown by `gate help-surfaces --diff`): while the posture is automatic, `apply_phase` names the ladder phase — prefer-idle, prefer-output-gap, keys-only, land — and the ladder ends with the build applied no later than 15 minutes after arming; read against `update_control::Snapshot::apply_phase` (the four `ApplyPhase::as_str` spellings, None for every non-automatic posture), `native_update_auto_intent::apply_phase` and `LANDS_WITHIN` (120 + 180 + 600 s = 15 min), and the ctl/help goldens regenerated in the same commit; no claim contradicted; catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 read of round 15's key and deadline catalog prose (shown by `xtask gate help-surfaces --diff`, matched at 135cf7646): `post`'s key= (aterm-gui fabric.rs valid_key; aterm-link state.rs key_seq/set_key_seq and KEYS_KEEP; bridge.rs drain_outbox's reserved sequence, and `dup=1` only for a key-chosen deduped publish) and dl= (note_deadline, settle_deadline_for, settled_on_bus, expire_deadlines); `outbox`'s `[key=<token>]` and `outbox sent … dup=1` against cmd_outbox/cmd_outbox_sent/retire_post; and the needle table's new rows against the rows they pin. THREE defects FIXED here: the `inbox` post-row grammar omitted the `key=` cmd_inbox prints on it; `post`'s futile-wait answer named `<absent|disconnected>` although fabric_wait_refusal answers `stalled` too; and `late=1` was unqualified although the bridge's `expired` map is in memory only, so a relaunched bridge delivers a late reply unflagged. Both help goldens regenerated as a pair. No other claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset catalog prose: `await inbox re=` against aterm-gui fabric.rs cmd_await_inbox (re= accepts every kind, since= defaults to 0); `inbox`'s oldest_on_bus header and verdict= against cmd_inbox/render_row; `inbox get @<off>` against inbox_get_at, deliver_fetched and Fetched::render (the ring answers only a whole row, FETCH_SLOTS 8, FETCH_WAIT_MS 10 s, `ERR fabric <state> off=` at once, chunked answers up to 256 KiB, one `ERR no such record`); `inbox seen`'s receipt against cmd_inbox_seen/owe_receipt and aterm-link bridge.rs send_receipt/retire_receipt; `post`'s --wait-ack bound (explicit ms, else dl= plus EXPIRY_GRACE_MS, else 30 s); `deliver`'s fetched= and receipt= forms against deliver_fetched/deliver_receipt and fetched_lines; `outbox`'s receipt and fetch lines against cmd_outbox; and SHORT_CATALOG_MAX_BYTES' round-15 raise, re-measured by printing cmd_help(\"\")'s length in help_short_form_is_bounded_and_is_the_summary_catalog: 9 863 B under 9 984. ONE defect FIXED here: a line continuation rendered `deliver`'s `verdict=<handled| refused|deferred>` with a stray space. Both help goldens regenerated as a pair. No other claim contradicted; AND THE v0.86 CANDIDATE'S READS THAT MAIN'S ROW CARRIED AHEAD OF THE READ ABOVE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally. 2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `await`, `inbox`, `inbox get`, `inbox seen`, `post`, `deliver`, `outbox` and `outbox sent` rows, the needle table's new rows, SHORT_CATALOG_MAX_BYTES' 9984 note and the `inbox get` history's round-15 aside: 8 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 595e13613) plus upstream's change (the `update` row's apply-posture sentences, the `privacy` row's this_process, app-data and unmeasured sentences and needles and the `trail status` row's ribbon_drawn=/ribbon_curtain_ms= keys and gloss, all read by main — and f3a5467ed's `version` row, which main recorded no row for: flavor=t|r, trust= as Trust's own version (`none` on an upstream build), rust_compat=, and trustc=/trustc_commit=/trustc_host= carrying the rustc-shaped token, shown against round 15's read at 030ba1788) plus ONE sentence added at this merge to `post`'s key= text — its ADDRESS is still resolved first, against the live roster, so a re-post to one that no longer routes is retired `ERR unroutable` (or `ambiguous`), appends nothing and leaves the key naming its record (both help goldens regenerated as a pair from the merged catalog), checked against aterm-gui build_info.rs control_line (trust= is `none` off flavor t and `unreported` on a Trust build predating the marker, a value the row does not list but does not deny; rust_compat= and trustc= are both compiler_release()), update_control.rs apply_posture and apply_policy_reason (the twelve words the row lists, none other), control_privacy.rs covers_split and observed_fda_scope, aterm-effects cursor_glow.rs TrailStatus::line, and aterm-link bridge.rs drain_outbox (resolve_to before StateDir::key_seq; the non-routing arm retires `off=- reason=` and clears only the post's pin) with the r15_receipts test a_keyed_re_post_to_a_session_whose_exit_line_never_arrives_is_unroutable_and_the_key_holds;  2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; AND UPSTREAM'S OWN READ OF THE SAME PROSE, recorded in parallel and kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 read at the landing of the erase pending-wrap fix, over f3a5467ed: read the version verb's new key detail (flavor=t|r, trust= as Trust's own version and none on upstream, rust_compat= as the compatible Rust release, trustc=/trustc_commit=/trustc_host= as release token, commit and host, the v0.10.0 rename, and trustc= equal to rust_compat=) against build_info control_line, compiler_release, compiler_commit_short, compiler_probe parse_rustc_vv and detect_flavor, build.rs, both version dispatch sites in aterm-gui control.rs, the CHANGELOG 0.10.0 entry, the two help goldens and this box's live rustc -vV, and no claim contradicted the code.; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged prose is round 15's read change plus main's, and it hashes to this row's recorded value, so no prose moved at the merge; 2026-09-15 read of the new `kitty` verb entry against its handlers: the summary `kitty [wear <key>]` and every field of the two documented row shapes read against App::kitty_collection_rows (`cat key={} coat={} iris={} age={} seen={} worn={worn}`, `seen=` being the roster row's `count`) and App::wear_kitty_on_front (`worn key={key} coat={} iris={}`) in crates/aterm-gui/src/app_input.rs; the `worn=1` claim against KittyLogHost::wearable, which derives the worn key from the cached `favourite` the companion reads rather than a second roster election (two pins inside one RFC3339 second tie, and the key breaks the tie the other way — found and fixed while writing the test); `the key is the row's own key=` against KittyLog::wear's `item.key == key`; the Favourite-This-Kitty claim against App::promotable_kitty (kitty_tenure.worn().map(|i| i.look).unwrap_or(self.launch_kitty)); `elects by GREATEST pin stamp` against KittyLog::favourite_look's max_by over `favourite`, and `survives a merge` against max_ts in favourite_collectible/merge_collectible; the three refusal cases against App::wear_kitty's sparkle gate, cfg.feline gate and None arm. `Write` rather than ConfigWrite is pinned by control.rs's required_op census. No claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the `metrics` row's echo paragraph, added by fe8ec4d58 (shown by `xtask gate help-surfaces --diff`, matched at b3a62ce1d), against crates/aterm-gui/src/control_query.rs cmd_metrics and crates/aterm-gui/src/echo_rtt.rs percentile_fields_text — the echo fragment does ride the PLAIN summary line, and the who-owes-the-time reading matches the episode the code itself records (n_echo=499 echo_p95_ms=75.69 echo_p99_ms=150.04 against aterm's own key_write_p99_ms=6.29). ONE SLIP FIXED in the commit that updated this row: the row said the line `ends ... then the CHILD's own round trip`, but the summary's LAST fragment is watchdog::turn_census_fields_text (e50cbbff4), spliced after echo_rtt's — the row now names that turn census and its seven fields as the end of the line, and lists echo_total= with the rest of the ledger percentile_fields_text actually prints; 2026-09-16 read of the tab entry's rewritten reply sentence a29417119 changed (OK active count only when the action HAPPENED, ERR reason when it did not: an index no tab holds, or a tab host that declined the close — a native document waiting on a durable checkpoint, or a close deferred behind a pending update handoff) against apply_tab_cmd_in's no_such bounds refusals for Select, Close(N) and both ends of Move; close_active_native_tab's Err arms (tab close deferred until update child rollback, returned behind defer_pending_update_handoff_teardown, which queues the mutation onto the pending handoff's teardown and applies it after the cancellation lands; native document close is waiting for a durable checkpoint; terminal close requires confirmation); and the regression test an_out_of_range_tab_action_refuses_instead_of_reporting_ok, which pins each refusal beside an in-range positive control; the headless sentence is unchanged and still holds; no claim contradicted; 2026-09-17 read of the round-18 identities rows: the recut `spawn` summary and its identity= sentences against control_media::parse_spawn_args/resolve_spawn_identity and control::escalated_op's fence; the `sessions` summary/detail `identity=` against control_session::sessions_lines; the `status` reply list against session_status::session_status_record; the new `identities` row's reply table against agent_identity::identities_reply arm by arm (list; one, `OK <n>` counting the agent lines since the client streams exactly n; forget unconfirmed, in use, removed, unknown, usage); the SHORT_CATALOG_MAX_BYTES paragraph against the short help measured on the live headless instance; 2026-09-17 read of the accounting paragraph's recount (--diff matched at 74d637316): 105 entries (help_catalog_full.txt carries 105 rows) and `OK 108` against control.rs's VERBS.len() + 3 status line; no claim contradicted; 2026-09-17 round-19 read: the new `story` row against control_query::parse_story / cmd_story and control.rs dispatch_story_verb (the closed set, the 96-byte cap, no control byte, `OK story=<n>`, Owner-only, `@<sid>` or bare), its phase-slot claims against presence::words (TOLD_FLASH, `✓ approved`, the a11y `approved by watcher`, Level::Story and the summary's `approvals` count), and its watcher claim against aterm-agent's story_of (an approval told bare); the `status` row's `hand=` / `level=` / `story=` grammar against presence::Hand::wire, Level::wire and App::presence_status_tail; the `chrome` row against App::presence_chrome_line (the rim tokens, the quote escaping, empty quotes when the row is folded); SHORT_CATALOG_MAX_BYTES' new paragraph MEASURED, not recalled (10 102 B against the raised 10 240; VERBS is 103 rows); the two goldens regenerated as a pair from this catalog. Three slips fixed before this row: the story summary ran to 129 chars over SUMMARY_MAX_CHARS and now reads `tell the window`; the chrome detail quoted the band grammar with its two-space gaps, which the table forbids; the story detail said the watcher sends `seq=` where it tells an approval bare; 2026-09-19 read of the `status` row's `hand=` clause the round-19 second fix reworded (shown by `--diff`): `:<holder>` names the driver when the turn came over an edge — the edge's source — and an Owner-token turn names nobody whatever edges stand; `driving:<sid>` is this session's own open turn into another — read against aterm-gui's `presence_facts` (`Lease::Turn { id, driver }` → `Hand::DrivenTurn { holder: driver.map(name_of_sid) }`, role or short sid), `driving_target` (a peer's lease naming this session as driver, never a standing edge) and `Hand::wire` (`turn:<id>[:<holder>]`, `driving:<sid>`); both goldens (`help_catalog_full.txt`, `ctl_help_full.txt`) carry the same clause; no claim contradicted; 2026-09-19 read of the merged `status` summary and detail (identity= after the fabric tail, then hand= level= story=) against session_status_record and presence_status_tail, the `story` row against dispatch_story_verb/parse_story/cmd_story, the `chrome` row against presence_chrome_line, Rim::wire, Level::wire, Hand::wire and the ReadChrome arm, and the SHORT_CATALOG_MAX_BYTES note re-measured through aterm-ctl on the headless live try at 10 238 B (10 199 B of content after the `OK 109 …` line, 106 rows), two bytes under 10 240; 2026-09-20 read of the privacy row's rewrite against control_privacy.rs's folder_state, folder_source, covers_split and NEVER_COVERED: the per-folder paragraph documents the `covered-by-fda` value and `source=` for the first time (nothing in the tree could emit either before this commit), states that an observation outranks the coverage rule, and keeps `unknown` as the default for a service with no measurement of its own; the coverage paragraph records \u{a7}7 S4's 2026-09-19 measurement (four other-app data locations read with zero dialogs and no tccd traffic, the grant held) and why Full Disk Access is the ONLY durable answer to that prompt — macOS records an app-data allow against one process instance and ships no Settings switch for the class; and the never-covered class is named in full (cloud-storage File Provider domains, the media and photo libraries, App Management) to match the widened roster. Goldens regenerated on purpose with regen_help_catalog_golden; same day, second pass: the privacy row gained the `anchor=` paragraph, read against control_privacy.rs's install row and note arm and aterm_containment's ImageAnchor — the five tokens, which two mean no grant can be validated, that they earn their own note row, and WHY (running= follows the vnode and reads healthy in exactly that state). Golden regenerated with regen_help_catalog_golden; 2026-09-20 (round 21) read of the two changed verb entries against their handlers in aterm-gui. `status`'s `fabric=` paragraph gained `stale` as a fifth value, with what it names (a bridge attached, not reporting down, owing an answer for three link refreshes) and the fix: read against aterm-gui fabric.rs fabric_state, the only writer of this token, and against the sentence already in this entry saying a large age on `connected` means a quiet link — which is why the new value is NOT read off that number, and the entry now says so. `inbox seen` and `post`'s receipt prose no longer says `--receipts (which aterm fabric on writes)` but that receipts are the default whichever way the bridge was set up: read against aterm-link cli.rs parse_args and fabric.rs receipts_from_config. No claim contradicted; 2026-09-20 (round 21 review) read of `status`'s NO HEARTBEAT sentence against the ghost sweep. Any answered broker request is an ack and an ack becomes a `link up`, so the round-21 sweep on a free-running 60 s timer made every attached bridge report for ever and capped `fabric_link_age_ms=` at a minute — which would also have made the `stale` wording two paragraphs down unreachable in practice. Read against bridge.rs's gated sweep (candidates discovered once per attach, timer only while the set is non-empty) and against `ack_wants_report`; the entry now states the bounded exception. No claim contradicted; 2026-09-21 round-22 re-read of the `status` row against `session_status.rs`'s record builder, which gained `seq=`/`hash=`: both fields are appended last on both the observed and the never-classified branch, `seq=` is the terminal's `content_seq` and `hash=` is FNV-1a-64 of the untrimmed visible screen through `control_query::screen_stamp`, and `-`/`-` is what a contended `try_lock` answers — the row states all three. Its claim that this is the pair `turn` returns and `history` keeps per turn id is now true BY CONSTRUCTION rather than by coincidence: the turn verdict and the stamp both build their bytes with `control_query::screen_text`. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 read of the `subscribe` row against `Requested::parse`, `fold_mail_params`, `parse_mail_params` and `drain_mail_events`: `mail` added to the streams list with the `MAIL`/`GAP mail-dropped=` line shapes, the metadata-only rule, the live-stream seeding, the `kinds=`/`from=` grammar and its fail-closed cases, and the authority note (`inbox` and `subscribe` are the same `OpClass::Read` -> `ReadScreen`, so the stream is a push face on a read the subscriber can already make). The SUMMARY line is deliberately unchanged — it never enumerated every frame kind, and the short catalog's byte budget has no room. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 REVIEW: `SUBSCRIBE_STREAMS` added as the ONE stream vocabulary, after four hand-written copies of it were found (this catalog row, the `ERR usage` refusal, the parser's fold table and the installed drive skill) and the refusal was wrong for a whole round. The refusal and the fold table now derive from it; the skill is tested against it. No claim contradicted; 2026-09-22 (round 23, broadcast) read of every string this round changed against the handler each one describes. The NEW `topic` row (Owner, Lines, Session, OwnerOnly) was read against `fabric::cmd_topic` and `control::dispatch_topic_verb`: `ls`/the bare form answer `OK <n>` then one `topic <t> since=<head|@<off>>` row each, `add` answers `OK <t> since=<..> added=<0|1>` and `drop` `OK <t> dropped=<0|1>` — and because those two are STATUS lines while the listing is Lines, `framing_of` gained the `topic add|drop` flip beside `inbox seen`'s, the hazard `identities forget` measured on 2026-09-17 (a client that reads the TOPIC as a row count reports a malformed header). The claim that the set is EMPTY by default and that an empty set receives nothing is `SessionFabric::topics` plus `bridge::on_say_record`, whose recipient filter is the topic map and nothing else; the `since=` sentences are `bridge::sample_topics` (resolved once, at first sight) and `catch_up_topic`. The `post` row's summary now spells `say[:<topic>]` (read against `bridge::resolve_to` and `fabric::cmd_post`'s `valid_topic` check, which refuses a malformed topic at the post rather than retiring a record `unroutable` a publish later) and its broadcast paragraph was CORRECTED as well as extended: it said `task`/`ask` are demoted, and `bridge::DEMOTE_UNLESS_ACCEPTED` is `[\"task\", \"control\"]` — `ask` is not in it and never was, so the sentence now names task/control, read against `classify_kind`. 2026-09-22 (round 23 review) CORRECTION OF THE PREVIOUS CLAUSE: no committed `post` row ever said `task`/`ask` are demoted — `git show origin/main:crates/aterm-types/src/control_verbs.rs` and the item-1 commit both name `ask` only under kinds, `--wait` and `dl=`. The sentence that was corrected existed only in this round's uncommitted draft, so the clause above recorded a read of a defect that shipped help never had; what IS true is that the row now says task/control, read against `DEMOTE_UNLESS_ACCEPTED`. The `subscribe` row gained `mail:topic=<t>`, read against `subscribe::parse_mail_params` and `MailFilter::wants`. SHORT_CATALOG_MAX_BYTES was RAISED from 10 240 to 10 496 with the measurement in its doc (10 358 measured by `help_short_form_is_bounded_and_is_the_summary_catalog`, 118 over: 109 for the new row plus 9 for the `post` summary), which is the raise the previous paragraph predicted. Golden pair regenerated in order (types, then gui). No claim contradicted; 2026-09-22 (round-23 review) read of the `topic` row's `serial=<n>` clause against `cmd_topic` and `bridge::sample_topics`, and of the SHORT_CATALOG_MAX_BYTES doc against the measurement: rows sum to 9 986 (the types-side test's number), the `topic` row is 110 and the `post` row grew 10, and only aterm-gui's wire test sees the 10 358. No claim contradicted; 2026-09-22 merge read (round 23 onto main): upstream's `privacy` claimants=/census= rows read against control_query's privacy handler, with round 23's topic/say/subscribe rows — consistent; 2026-09-22 read of the one reworded sentence in the `trail` verb's long help (shown by `gate help-surfaces --diff`): the row now ends `ribbon_retired= ribbon_followed=`, read against `TrailStatus::line_v2` (cursor_glow.rs, the two fields last, in that order); `ribbon_followed=` is `rk::Status::followed`, the cumulative count `Engine::follow_rows` adds and `Engine::reset` keeps; the follow search is `Witness::follow_runs` (dr in -1, +1, -2, +2 — \"one or two rows\", same glyph at the same column, as a block); a run carried off the caret row is set flowing by `Ribbon::flow_cohort` and `Cohort::swoosh_total` is `FLOW_TOTAL_S` = 1.10 + 0.24 = 1.34 s from `Cohort::clock`; the `ribbon_retired=` melt is `RETIRE_MELT_S` = 0.12 s; merged with origin/main's round-22 reads of this file on 2026-09-22 by the rainbow-ribbon integration (both sides' prose kept; the hash is the merged file's); 2026-09-22 (round 24, item 1) read of the `topic` row's rewritten sentence and the `timeline` row's kind list against `fabric::cmd_topic` (an add that added and a drop that dropped each `record(\"topic\", …)` on the session timeline), `subscribe::timeline_wire_kind` via `FABRIC_EVENT_KINDS` (now six, `topic` last) and `bridge::on_event`'s `topic` arm → `topic_change`: the `ls` row carries no serial any more, and an add or drop reaches the bridge as `EVENT <local> topic add|drop …` at once. No claim contradicted; 2026-09-22 (round 24, item 2) read of the `fabric` row's summary (now `status|attach|retire`, trimmed to the summary bound) and its new `fabric retire` sentence against `cmd_fabric`/`cmd_fabric_retire` (control.rs), the roster journal's `RetireRequested` row and its `EVENT * fabric-retire <sid>` push (session_store.rs, subscribe.rs) and the bridge's `fabric-retire` arm → `retire_on_request` (hosting re-checked, the row published `exited` with its own fields through `publish` and the bridge's own sequence). No claim contradicted; merged with upstream's own read of the same day: 2026-09-21 read of the one added sentence in the `update` verb's long help (shown by `gate help-surfaces --diff`): while the posture is automatic, `apply_phase` names the ladder phase — prefer-idle, prefer-output-gap, keys-only, land — and the ladder ends with the build applied no later than 15 minutes after arming; read against `update_control::Snapshot::apply_phase` (the four `ApplyPhase::as_str` spellings, None for every non-automatic posture), `native_update_auto_intent::apply_phase` and `LANDS_WITHIN` (120 + 180 + 600 s = 15 min), and the ctl/help goldens regenerated in the same commit; no claim contradicted; catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 read of round 15's key and deadline catalog prose (shown by `xtask gate help-surfaces --diff`, matched at 135cf7646): `post`'s key= (aterm-gui fabric.rs valid_key; aterm-link state.rs key_seq/set_key_seq and KEYS_KEEP; bridge.rs drain_outbox's reserved sequence, and `dup=1` only for a key-chosen deduped publish) and dl= (note_deadline, settle_deadline_for, settled_on_bus, expire_deadlines); `outbox`'s `[key=<token>]` and `outbox sent … dup=1` against cmd_outbox/cmd_outbox_sent/retire_post; and the needle table's new rows against the rows they pin. THREE defects FIXED here: the `inbox` post-row grammar omitted the `key=` cmd_inbox prints on it; `post`'s futile-wait answer named `<absent|disconnected>` although fabric_wait_refusal answers `stalled` too; and `late=1` was unqualified although the bridge's `expired` map is in memory only, so a relaunched bridge delivers a late reply unflagged. Both help goldens regenerated as a pair. No other claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset catalog prose: `await inbox re=` against aterm-gui fabric.rs cmd_await_inbox (re= accepts every kind, since= defaults to 0); `inbox`'s oldest_on_bus header and verdict= against cmd_inbox/render_row; `inbox get @<off>` against inbox_get_at, deliver_fetched and Fetched::render (the ring answers only a whole row, FETCH_SLOTS 8, FETCH_WAIT_MS 10 s, `ERR fabric <state> off=` at once, chunked answers up to 256 KiB, one `ERR no such record`); `inbox seen`'s receipt against cmd_inbox_seen/owe_receipt and aterm-link bridge.rs send_receipt/retire_receipt; `post`'s --wait-ack bound (explicit ms, else dl= plus EXPIRY_GRACE_MS, else 30 s); `deliver`'s fetched= and receipt= forms against deliver_fetched/deliver_receipt and fetched_lines; `outbox`'s receipt and fetch lines against cmd_outbox; and SHORT_CATALOG_MAX_BYTES' round-15 raise, re-measured by printing cmd_help(\"\")'s length in help_short_form_is_bounded_and_is_the_summary_catalog: 9 863 B under 9 984. ONE defect FIXED here: a line continuation rendered `deliver`'s `verdict=<handled| refused|deferred>` with a stray space. Both help goldens regenerated as a pair. No other claim contradicted; AND THE v0.86 CANDIDATE'S READS THAT MAIN'S ROW CARRIED AHEAD OF THE READ ABOVE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally. 2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `await`, `inbox`, `inbox get`, `inbox seen`, `post`, `deliver`, `outbox` and `outbox sent` rows, the needle table's new rows, SHORT_CATALOG_MAX_BYTES' 9984 note and the `inbox get` history's round-15 aside: 8 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 595e13613) plus upstream's change (the `update` row's apply-posture sentences, the `privacy` row's this_process, app-data and unmeasured sentences and needles and the `trail status` row's ribbon_drawn=/ribbon_curtain_ms= keys and gloss, all read by main — and f3a5467ed's `version` row, which main recorded no row for: flavor=t|r, trust= as Trust's own version (`none` on an upstream build), rust_compat=, and trustc=/trustc_commit=/trustc_host= carrying the rustc-shaped token, shown against round 15's read at 030ba1788) plus ONE sentence added at this merge to `post`'s key= text — its ADDRESS is still resolved first, against the live roster, so a re-post to one that no longer routes is retired `ERR unroutable` (or `ambiguous`), appends nothing and leaves the key naming its record (both help goldens regenerated as a pair from the merged catalog), checked against aterm-gui build_info.rs control_line (trust= is `none` off flavor t and `unreported` on a Trust build predating the marker, a value the row does not list but does not deny; rust_compat= and trustc= are both compiler_release()), update_control.rs apply_posture and apply_policy_reason (the twelve words the row lists, none other), control_privacy.rs covers_split and observed_fda_scope, aterm-effects cursor_glow.rs TrailStatus::line, and aterm-link bridge.rs drain_outbox (resolve_to before StateDir::key_seq; the non-routing arm retires `off=- reason=` and clears only the post's pin) with the r15_receipts test a_keyed_re_post_to_a_session_whose_exit_line_never_arrives_is_unroutable_and_the_key_holds;  2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; AND UPSTREAM'S OWN READ OF THE SAME PROSE, recorded in parallel and kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 read at the landing of the erase pending-wrap fix, over f3a5467ed: read the version verb's new key detail (flavor=t|r, trust= as Trust's own version and none on upstream, rust_compat= as the compatible Rust release, trustc=/trustc_commit=/trustc_host= as release token, commit and host, the v0.10.0 rename, and trustc= equal to rust_compat=) against build_info control_line, compiler_release, compiler_commit_short, compiler_probe parse_rustc_vv and detect_flavor, build.rs, both version dispatch sites in aterm-gui control.rs, the CHANGELOG 0.10.0 entry, the two help goldens and this box's live rustc -vV, and no claim contradicted the code.; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged prose is round 15's read change plus main's, and it hashes to this row's recorded value, so no prose moved at the merge; 2026-09-15 read of the new `kitty` verb entry against its handlers: the summary `kitty [wear <key>]` and every field of the two documented row shapes read against App::kitty_collection_rows (`cat key={} coat={} iris={} age={} seen={} worn={worn}`, `seen=` being the roster row's `count`) and App::wear_kitty_on_front (`worn key={key} coat={} iris={}`) in crates/aterm-gui/src/app_input.rs; the `worn=1` claim against KittyLogHost::wearable, which derives the worn key from the cached `favourite` the companion reads rather than a second roster election (two pins inside one RFC3339 second tie, and the key breaks the tie the other way — found and fixed while writing the test); `the key is the row's own key=` against KittyLog::wear's `item.key == key`; the Favourite-This-Kitty claim against App::promotable_kitty (kitty_tenure.worn().map(|i| i.look).unwrap_or(self.launch_kitty)); `elects by GREATEST pin stamp` against KittyLog::favourite_look's max_by over `favourite`, and `survives a merge` against max_ts in favourite_collectible/merge_collectible; the three refusal cases against App::wear_kitty's sparkle gate, cfg.feline gate and None arm. `Write` rather than ConfigWrite is pinned by control.rs's required_op census. No claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the `metrics` row's echo paragraph, added by fe8ec4d58 (shown by `xtask gate help-surfaces --diff`, matched at b3a62ce1d), against crates/aterm-gui/src/control_query.rs cmd_metrics and crates/aterm-gui/src/echo_rtt.rs percentile_fields_text — the echo fragment does ride the PLAIN summary line, and the who-owes-the-time reading matches the episode the code itself records (n_echo=499 echo_p95_ms=75.69 echo_p99_ms=150.04 against aterm's own key_write_p99_ms=6.29). ONE SLIP FIXED in the commit that updated this row: the row said the line `ends ... then the CHILD's own round trip`, but the summary's LAST fragment is watchdog::turn_census_fields_text (e50cbbff4), spliced after echo_rtt's — the row now names that turn census and its seven fields as the end of the line, and lists echo_total= with the rest of the ledger percentile_fields_text actually prints; 2026-09-16 read of the tab entry's rewritten reply sentence a29417119 changed (OK active count only when the action HAPPENED, ERR reason when it did not: an index no tab holds, or a tab host that declined the close — a native document waiting on a durable checkpoint, or a close deferred behind a pending update handoff) against apply_tab_cmd_in's no_such bounds refusals for Select, Close(N) and both ends of Move; close_active_native_tab's Err arms (tab close deferred until update child rollback, returned behind defer_pending_update_handoff_teardown, which queues the mutation onto the pending handoff's teardown and applies it after the cancellation lands; native document close is waiting for a durable checkpoint; terminal close requires confirmation); and the regression test an_out_of_range_tab_action_refuses_instead_of_reporting_ok, which pins each refusal beside an in-range positive control; the headless sentence is unchanged and still holds; no claim contradicted; 2026-09-17 read of the round-18 identities rows: the recut `spawn` summary and its identity= sentences against control_media::parse_spawn_args/resolve_spawn_identity and control::escalated_op's fence; the `sessions` summary/detail `identity=` against control_session::sessions_lines; the `status` reply list against session_status::session_status_record; the new `identities` row's reply table against agent_identity::identities_reply arm by arm (list; one, `OK <n>` counting the agent lines since the client streams exactly n; forget unconfirmed, in use, removed, unknown, usage); the SHORT_CATALOG_MAX_BYTES paragraph against the short help measured on the live headless instance; 2026-09-17 read of the accounting paragraph's recount (--diff matched at 74d637316): 105 entries (help_catalog_full.txt carries 105 rows) and `OK 108` against control.rs's VERBS.len() + 3 status line; no claim contradicted; 2026-09-17 round-19 read: the new `story` row against control_query::parse_story / cmd_story and control.rs dispatch_story_verb (the closed set, the 96-byte cap, no control byte, `OK story=<n>`, Owner-only, `@<sid>` or bare), its phase-slot claims against presence::words (TOLD_FLASH, `✓ approved`, the a11y `approved by watcher`, Level::Story and the summary's `approvals` count), and its watcher claim against aterm-agent's story_of (an approval told bare); the `status` row's `hand=` / `level=` / `story=` grammar against presence::Hand::wire, Level::wire and App::presence_status_tail; the `chrome` row against App::presence_chrome_line (the rim tokens, the quote escaping, empty quotes when the row is folded); SHORT_CATALOG_MAX_BYTES' new paragraph MEASURED, not recalled (10 102 B against the raised 10 240; VERBS is 103 rows); the two goldens regenerated as a pair from this catalog. Three slips fixed before this row: the story summary ran to 129 chars over SUMMARY_MAX_CHARS and now reads `tell the window`; the chrome detail quoted the band grammar with its two-space gaps, which the table forbids; the story detail said the watcher sends `seq=` where it tells an approval bare; 2026-09-19 read of the `status` row's `hand=` clause the round-19 second fix reworded (shown by `--diff`): `:<holder>` names the driver when the turn came over an edge — the edge's source — and an Owner-token turn names nobody whatever edges stand; `driving:<sid>` is this session's own open turn into another — read against aterm-gui's `presence_facts` (`Lease::Turn { id, driver }` → `Hand::DrivenTurn { holder: driver.map(name_of_sid) }`, role or short sid), `driving_target` (a peer's lease naming this session as driver, never a standing edge) and `Hand::wire` (`turn:<id>[:<holder>]`, `driving:<sid>`); both goldens (`help_catalog_full.txt`, `ctl_help_full.txt`) carry the same clause; no claim contradicted; 2026-09-19 read of the merged `status` summary and detail (identity= after the fabric tail, then hand= level= story=) against session_status_record and presence_status_tail, the `story` row against dispatch_story_verb/parse_story/cmd_story, the `chrome` row against presence_chrome_line, Rim::wire, Level::wire, Hand::wire and the ReadChrome arm, and the SHORT_CATALOG_MAX_BYTES note re-measured through aterm-ctl on the headless live try at 10 238 B (10 199 B of content after the `OK 109 …` line, 106 rows), two bytes under 10 240; 2026-09-20 read of the privacy row's rewrite against control_privacy.rs's folder_state, folder_source, covers_split and NEVER_COVERED: the per-folder paragraph documents the `covered-by-fda` value and `source=` for the first time (nothing in the tree could emit either before this commit), states that an observation outranks the coverage rule, and keeps `unknown` as the default for a service with no measurement of its own; the coverage paragraph records \u{a7}7 S4's 2026-09-19 measurement (four other-app data locations read with zero dialogs and no tccd traffic, the grant held) and why Full Disk Access is the ONLY durable answer to that prompt — macOS records an app-data allow against one process instance and ships no Settings switch for the class; and the never-covered class is named in full (cloud-storage File Provider domains, the media and photo libraries, App Management) to match the widened roster. Goldens regenerated on purpose with regen_help_catalog_golden; same day, second pass: the privacy row gained the `anchor=` paragraph, read against control_privacy.rs's install row and note arm and aterm_containment's ImageAnchor — the five tokens, which two mean no grant can be validated, that they earn their own note row, and WHY (running= follows the vnode and reads healthy in exactly that state). Golden regenerated with regen_help_catalog_golden; 2026-09-20 (round 21) read of the two changed verb entries against their handlers in aterm-gui. `status`'s `fabric=` paragraph gained `stale` as a fifth value, with what it names (a bridge attached, not reporting down, owing an answer for three link refreshes) and the fix: read against aterm-gui fabric.rs fabric_state, the only writer of this token, and against the sentence already in this entry saying a large age on `connected` means a quiet link — which is why the new value is NOT read off that number, and the entry now says so. `inbox seen` and `post`'s receipt prose no longer says `--receipts (which aterm fabric on writes)` but that receipts are the default whichever way the bridge was set up: read against aterm-link cli.rs parse_args and fabric.rs receipts_from_config. No claim contradicted; 2026-09-20 (round 21 review) read of `status`'s NO HEARTBEAT sentence against the ghost sweep. Any answered broker request is an ack and an ack becomes a `link up`, so the round-21 sweep on a free-running 60 s timer made every attached bridge report for ever and capped `fabric_link_age_ms=` at a minute — which would also have made the `stale` wording two paragraphs down unreachable in practice. Read against bridge.rs's gated sweep (candidates discovered once per attach, timer only while the set is non-empty) and against `ack_wants_report`; the entry now states the bounded exception. No claim contradicted; 2026-09-21 round-22 re-read of the `status` row against `session_status.rs`'s record builder, which gained `seq=`/`hash=`: both fields are appended last on both the observed and the never-classified branch, `seq=` is the terminal's `content_seq` and `hash=` is FNV-1a-64 of the untrimmed visible screen through `control_query::screen_stamp`, and `-`/`-` is what a contended `try_lock` answers — the row states all three. Its claim that this is the pair `turn` returns and `history` keeps per turn id is now true BY CONSTRUCTION rather than by coincidence: the turn verdict and the stamp both build their bytes with `control_query::screen_text`. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 read of the `subscribe` row against `Requested::parse`, `fold_mail_params`, `parse_mail_params` and `drain_mail_events`: `mail` added to the streams list with the `MAIL`/`GAP mail-dropped=` line shapes, the metadata-only rule, the live-stream seeding, the `kinds=`/`from=` grammar and its fail-closed cases, and the authority note (`inbox` and `subscribe` are the same `OpClass::Read` -> `ReadScreen`, so the stream is a push face on a read the subscriber can already make). The SUMMARY line is deliberately unchanged — it never enumerated every frame kind, and the short catalog's byte budget has no room. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 REVIEW: `SUBSCRIBE_STREAMS` added as the ONE stream vocabulary, after four hand-written copies of it were found (this catalog row, the `ERR usage` refusal, the parser's fold table and the installed drive skill) and the refusal was wrong for a whole round. The refusal and the fold table now derive from it; the skill is tested against it. No claim contradicted; 2026-09-22 (round 23, broadcast) read of every string this round changed against the handler each one describes. The NEW `topic` row (Owner, Lines, Session, OwnerOnly) was read against `fabric::cmd_topic` and `control::dispatch_topic_verb`: `ls`/the bare form answer `OK <n>` then one `topic <t> since=<head|@<off>>` row each, `add` answers `OK <t> since=<..> added=<0|1>` and `drop` `OK <t> dropped=<0|1>` — and because those two are STATUS lines while the listing is Lines, `framing_of` gained the `topic add|drop` flip beside `inbox seen`'s, the hazard `identities forget` measured on 2026-09-17 (a client that reads the TOPIC as a row count reports a malformed header). The claim that the set is EMPTY by default and that an empty set receives nothing is `SessionFabric::topics` plus `bridge::on_say_record`, whose recipient filter is the topic map and nothing else; the `since=` sentences are `bridge::sample_topics` (resolved once, at first sight) and `catch_up_topic`. The `post` row's summary now spells `say[:<topic>]` (read against `bridge::resolve_to` and `fabric::cmd_post`'s `valid_topic` check, which refuses a malformed topic at the post rather than retiring a record `unroutable` a publish later) and its broadcast paragraph was CORRECTED as well as extended: it said `task`/`ask` are demoted, and `bridge::DEMOTE_UNLESS_ACCEPTED` is `[\"task\", \"control\"]` — `ask` is not in it and never was, so the sentence now names task/control, read against `classify_kind`. 2026-09-22 (round 23 review) CORRECTION OF THE PREVIOUS CLAUSE: no committed `post` row ever said `task`/`ask` are demoted — `git show origin/main:crates/aterm-types/src/control_verbs.rs` and the item-1 commit both name `ask` only under kinds, `--wait` and `dl=`. The sentence that was corrected existed only in this round's uncommitted draft, so the clause above recorded a read of a defect that shipped help never had; what IS true is that the row now says task/control, read against `DEMOTE_UNLESS_ACCEPTED`. The `subscribe` row gained `mail:topic=<t>`, read against `subscribe::parse_mail_params` and `MailFilter::wants`. SHORT_CATALOG_MAX_BYTES was RAISED from 10 240 to 10 496 with the measurement in its doc (10 358 measured by `help_short_form_is_bounded_and_is_the_summary_catalog`, 118 over: 109 for the new row plus 9 for the `post` summary), which is the raise the previous paragraph predicted. Golden pair regenerated in order (types, then gui). No claim contradicted; 2026-09-22 (round-23 review) read of the `topic` row's `serial=<n>` clause against `cmd_topic` and `bridge::sample_topics`, and of the SHORT_CATALOG_MAX_BYTES doc against the measurement: rows sum to 9 986 (the types-side test's number), the `topic` row is 110 and the `post` row grew 10, and only aterm-gui's wire test sees the 10 358. No claim contradicted; 2026-09-22 merge read (round 23 onto main): upstream's `privacy` claimants=/census= rows read against control_query's privacy handler, with round 23's topic/say/subscribe rows — consistent; 2026-09-22 read of the one reworded sentence in the `trail` verb's long help (shown by `gate help-surfaces --diff`): the row now ends `ribbon_retired= ribbon_followed=`, read against `TrailStatus::line_v2` (cursor_glow.rs, the two fields last, in that order); `ribbon_followed=` is `rk::Status::followed`, the cumulative count `Engine::follow_rows` adds and `Engine::reset` keeps; the follow search is `Witness::follow_runs` (dr in -1, +1, -2, +2 — \"one or two rows\", same glyph at the same column, as a block); a run carried off the caret row is set flowing by `Ribbon::flow_cohort` and `Cohort::swoosh_total` is `FLOW_TOTAL_S` = 1.10 + 0.24 = 1.34 s from `Cohort::clock`; the `ribbon_retired=` melt is `RETIRE_MELT_S` = 0.12 s; merged with origin/main's round-22 reads of this file on 2026-09-22 by the rainbow-ribbon integration (both sides' prose kept; the hash is the merged file's); 2026-09-22 read of the tone and trail rows' new audibility prose — the seam= verdict and its seven witnesses, audio= as the audio worker's state word, reopens_left=, and trail status's sound_seam= — against sound_seam.rs's SeamReason table (the nine reasons the help names are exactly the nine it spells), tone_infer::AudioHost::label over trail_audio::HostState (eight states; `reopening` was printed on the wire but missing from the help's enumeration, fixed in the commit that re-recorded this row) and control.rs's tone_status_help_enumerates_exactly_the_keys_the_row_emits (set equality both ways between the help and the emitted row); merged with origin/main's round-23 read of this file, upstream's row kept and this read appended; 2026-09-22 merge read (round 24 + v0.91.0): the topic row without serial and the `fabric retire` row read against the handlers — consistent; 2026-09-22 (round-24 review fix, commit A) read of the `subscribe` row's events and sessions clauses against subscribe.rs `drain_timeline_events` (the `GAP <local> events-dropped=<n>` it emits when the watermark is below the timeline's retained low) and `drain_instance_events` (`EVENT * fabric-retire <sid>` for `RosterChange::RetireRequested`). No claim contradicted; 2026-09-22 (round-24 review fix, commit C) read of the `status` synopsis against fabric.rs (five `fabric=` states, `stale` derived) and of the `subscribe` summary against control.rs's grammar (`@*` accepted). No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the `status` (stamp) and `spawn` (identity=) help against control_query.rs `screen_stamp` and agent_identity.rs `ensure_in`: no hook carries the stamp, no hook block is carried into an identity. No claim contradicted; 2026-09-22 read of the `path=<frozen|live>` sentences added to the `sessions` summary/detail and to the `status` Reply grammar against control_session.rs::sessions_lines (last token, after identity=) and session_status.rs's two status format strings (after story=, before the seq=/hash= stamp, which assert_stamp_rides_last keeps last); the sessions summary lost its `rows:` word to stay under SUMMARY_MAX_CHARS (summaries_fit_the_short_catalog_budget); both help goldens regenerated as a pair; 2026-09-23 (p2/gui review) read of the `appnotice` long help: marker-shaped toolchain text is taken as the marker is (an `appstatus` ledger entry, no row) and replies `OK recorded`, against `App::take_app_notice` and `control_query::cmd_appnotice`; any other text an Info row held 30 s (HOLD_NOTICE) then kept in the ledger, replying `OK posted`; the summary line is unchanged; 2026-09-24 read of the `appstatus`/`appnotice` prose the harness lane changed (shown by `xtask gate help-surfaces --diff`; appnotice summary, detail and `appstatus` kind list) against StatusBars::activity_rows (a ledger row prints `kind=<Lane::as_str>`, and Lane::Harness is never in `bars()`, so its rows are always phase=done), control_query::cmd_appnotice's lane gate and App::take_app_notice's `harness` arm (`OK recorded`, pinned by appnotice_says_recorded_for_a_marker_and_posted_for_a_row and a_harness_note_is_recorded_and_never_takes_a_row), and history_log::Topic::Harness (the history line the detail names); both goldens regenerated by name; no claim contradicted; 2026-09-24 read (the ux/status-reporting merge's log port, ba7e63129), shown by `--diff` (1 hunk, 4 removed, 4 added): appnotice's one line `post a message (harness: a record, never a row)` and its long help — a marker-shaped toolchain text and every harness note are records on Settings ▸ Messages and appstatus, `OK recorded`; any other text an Info row held 30 s, `OK posted` — read against lib.rs take_app_notice (post_managed_current / post_machine_settings / record_harness_note → App::record_message, Hold::LogOnly; toolchain_words::appnotice → an Info message at the default 30 s hold) and control_query::cmd_appnotice's owner gate; both help goldens regenerated as a pair. No claim contradicted; 2026-09-23 read of the privacy row's rewritten `claimants=` paragraph and corrected `running=` sentence against PrivacySnapshot::claimant_lines, ConsentState::claimants and consent::claimants_for: `age_ms=`, the eight-row cap with true counts, the candidate sources (the running bundle's folder, /Applications, ~/Applications, LaunchServices), the four `off` causes, and `running=` as the frozen exec-time path (install_posture_rows reads current_exe once); both goldens regenerated as a pair; no claim contradicted; 2026-09-24 read of the privacy row's two changes against consent::classify_claimants and the Security panel: a registered copy under a .Trash/.Trashes component is filtered before it is read (in_trash), and Settings ▸ Security lists conflicting copies and offers MACOS_ACCESS_TRASH only for Claimants::retirable copies, which app_control::act_app refuses under MACOS_ACCESS_GESTURE_PREFIX (the_security_panels_owner_gestures_are_refused_on_the_control_surface); both goldens regenerated as a pair; no claim contradicted; 2026-09-24 re-read of the privacy row's Trash sentence after the owner-confirm fix: the press is confirmed through ConsentGestures::confirm (menu::confirm_owner, fail-closed) before MACOS_ACCESS_TRASH records a plan, and app act still refuses it; both goldens regenerated as a pair; no claim contradicted; the branch's reads of the same row: 2026-09-21 read of the one added sentence in the `update` verb's long help (shown by `gate help-surfaces --diff`): while the posture is automatic, `apply_phase` names the ladder phase — prefer-idle, prefer-output-gap, keys-only, land — and the ladder ends with the build applied no later than 15 minutes after arming; read against `update_control::Snapshot::apply_phase` (the four `ApplyPhase::as_str` spellings, None for every non-automatic posture), `native_update_auto_intent::apply_phase` and `LANDS_WITHIN` (120 + 180 + 600 s = 15 min), and the ctl/help goldens regenerated in the same commit; no claim contradicted; catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 read of round 15's key and deadline catalog prose (shown by `xtask gate help-surfaces --diff`, matched at 135cf7646): `post`'s key= (aterm-gui fabric.rs valid_key; aterm-link state.rs key_seq/set_key_seq and KEYS_KEEP; bridge.rs drain_outbox's reserved sequence, and `dup=1` only for a key-chosen deduped publish) and dl= (note_deadline, settle_deadline_for, settled_on_bus, expire_deadlines); `outbox`'s `[key=<token>]` and `outbox sent … dup=1` against cmd_outbox/cmd_outbox_sent/retire_post; and the needle table's new rows against the rows they pin. THREE defects FIXED here: the `inbox` post-row grammar omitted the `key=` cmd_inbox prints on it; `post`'s futile-wait answer named `<absent|disconnected>` although fabric_wait_refusal answers `stalled` too; and `late=1` was unqualified although the bridge's `expired` map is in memory only, so a relaunched bridge delivers a late reply unflagged. Both help goldens regenerated as a pair. No other claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset catalog prose: `await inbox re=` against aterm-gui fabric.rs cmd_await_inbox (re= accepts every kind, since= defaults to 0); `inbox`'s oldest_on_bus header and verdict= against cmd_inbox/render_row; `inbox get @<off>` against inbox_get_at, deliver_fetched and Fetched::render (the ring answers only a whole row, FETCH_SLOTS 8, FETCH_WAIT_MS 10 s, `ERR fabric <state> off=` at once, chunked answers up to 256 KiB, one `ERR no such record`); `inbox seen`'s receipt against cmd_inbox_seen/owe_receipt and aterm-link bridge.rs send_receipt/retire_receipt; `post`'s --wait-ack bound (explicit ms, else dl= plus EXPIRY_GRACE_MS, else 30 s); `deliver`'s fetched= and receipt= forms against deliver_fetched/deliver_receipt and fetched_lines; `outbox`'s receipt and fetch lines against cmd_outbox; and SHORT_CATALOG_MAX_BYTES' round-15 raise, re-measured by printing cmd_help(\"\")'s length in help_short_form_is_bounded_and_is_the_summary_catalog: 9 863 B under 9 984. ONE defect FIXED here: a line continuation rendered `deliver`'s `verdict=<handled| refused|deferred>` with a stray space. Both help goldens regenerated as a pair. No other claim contradicted; AND THE v0.86 CANDIDATE'S READS THAT MAIN'S ROW CARRIED AHEAD OF THE READ ABOVE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally. 2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `await`, `inbox`, `inbox get`, `inbox seen`, `post`, `deliver`, `outbox` and `outbox sent` rows, the needle table's new rows, SHORT_CATALOG_MAX_BYTES' 9984 note and the `inbox get` history's round-15 aside: 8 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 595e13613) plus upstream's change (the `update` row's apply-posture sentences, the `privacy` row's this_process, app-data and unmeasured sentences and needles and the `trail status` row's ribbon_drawn=/ribbon_curtain_ms= keys and gloss, all read by main — and f3a5467ed's `version` row, which main recorded no row for: flavor=t|r, trust= as Trust's own version (`none` on an upstream build), rust_compat=, and trustc=/trustc_commit=/trustc_host= carrying the rustc-shaped token, shown against round 15's read at 030ba1788) plus ONE sentence added at this merge to `post`'s key= text — its ADDRESS is still resolved first, against the live roster, so a re-post to one that no longer routes is retired `ERR unroutable` (or `ambiguous`), appends nothing and leaves the key naming its record (both help goldens regenerated as a pair from the merged catalog), checked against aterm-gui build_info.rs control_line (trust= is `none` off flavor t and `unreported` on a Trust build predating the marker, a value the row does not list but does not deny; rust_compat= and trustc= are both compiler_release()), update_control.rs apply_posture and apply_policy_reason (the twelve words the row lists, none other), control_privacy.rs covers_split and observed_fda_scope, aterm-effects cursor_glow.rs TrailStatus::line, and aterm-link bridge.rs drain_outbox (resolve_to before StateDir::key_seq; the non-routing arm retires `off=- reason=` and clears only the post's pin) with the r15_receipts test a_keyed_re_post_to_a_session_whose_exit_line_never_arrives_is_unroutable_and_the_key_holds;  2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; AND UPSTREAM'S OWN READ OF THE SAME PROSE, recorded in parallel and kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 read at the landing of the erase pending-wrap fix, over f3a5467ed: read the version verb's new key detail (flavor=t|r, trust= as Trust's own version and none on upstream, rust_compat= as the compatible Rust release, trustc=/trustc_commit=/trustc_host= as release token, commit and host, the v0.10.0 rename, and trustc= equal to rust_compat=) against build_info control_line, compiler_release, compiler_commit_short, compiler_probe parse_rustc_vv and detect_flavor, build.rs, both version dispatch sites in aterm-gui control.rs, the CHANGELOG 0.10.0 entry, the two help goldens and this box's live rustc -vV, and no claim contradicted the code.; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged prose is round 15's read change plus main's, and it hashes to this row's recorded value, so no prose moved at the merge; 2026-09-15 read of the new `kitty` verb entry against its handlers: the summary `kitty [wear <key>]` and every field of the two documented row shapes read against App::kitty_collection_rows (`cat key={} coat={} iris={} age={} seen={} worn={worn}`, `seen=` being the roster row's `count`) and App::wear_kitty_on_front (`worn key={key} coat={} iris={}`) in crates/aterm-gui/src/app_input.rs; the `worn=1` claim against KittyLogHost::wearable, which derives the worn key from the cached `favourite` the companion reads rather than a second roster election (two pins inside one RFC3339 second tie, and the key breaks the tie the other way — found and fixed while writing the test); `the key is the row's own key=` against KittyLog::wear's `item.key == key`; the Favourite-This-Kitty claim against App::promotable_kitty (kitty_tenure.worn().map(|i| i.look).unwrap_or(self.launch_kitty)); `elects by GREATEST pin stamp` against KittyLog::favourite_look's max_by over `favourite`, and `survives a merge` against max_ts in favourite_collectible/merge_collectible; the three refusal cases against App::wear_kitty's sparkle gate, cfg.feline gate and None arm. `Write` rather than ConfigWrite is pinned by control.rs's required_op census. No claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the `metrics` row's echo paragraph, added by fe8ec4d58 (shown by `xtask gate help-surfaces --diff`, matched at b3a62ce1d), against crates/aterm-gui/src/control_query.rs cmd_metrics and crates/aterm-gui/src/echo_rtt.rs percentile_fields_text — the echo fragment does ride the PLAIN summary line, and the who-owes-the-time reading matches the episode the code itself records (n_echo=499 echo_p95_ms=75.69 echo_p99_ms=150.04 against aterm's own key_write_p99_ms=6.29). ONE SLIP FIXED in the commit that updated this row: the row said the line `ends ... then the CHILD's own round trip`, but the summary's LAST fragment is watchdog::turn_census_fields_text (e50cbbff4), spliced after echo_rtt's — the row now names that turn census and its seven fields as the end of the line, and lists echo_total= with the rest of the ledger percentile_fields_text actually prints; 2026-09-16 read of the tab entry's rewritten reply sentence a29417119 changed (OK active count only when the action HAPPENED, ERR reason when it did not: an index no tab holds, or a tab host that declined the close — a native document waiting on a durable checkpoint, or a close deferred behind a pending update handoff) against apply_tab_cmd_in's no_such bounds refusals for Select, Close(N) and both ends of Move; close_active_native_tab's Err arms (tab close deferred until update child rollback, returned behind defer_pending_update_handoff_teardown, which queues the mutation onto the pending handoff's teardown and applies it after the cancellation lands; native document close is waiting for a durable checkpoint; terminal close requires confirmation); and the regression test an_out_of_range_tab_action_refuses_instead_of_reporting_ok, which pins each refusal beside an in-range positive control; the headless sentence is unchanged and still holds; no claim contradicted; 2026-09-17 read of the round-18 identities rows: the recut `spawn` summary and its identity= sentences against control_media::parse_spawn_args/resolve_spawn_identity and control::escalated_op's fence; the `sessions` summary/detail `identity=` against control_session::sessions_lines; the `status` reply list against session_status::session_status_record; the new `identities` row's reply table against agent_identity::identities_reply arm by arm (list; one, `OK <n>` counting the agent lines since the client streams exactly n; forget unconfirmed, in use, removed, unknown, usage); the SHORT_CATALOG_MAX_BYTES paragraph against the short help measured on the live headless instance; 2026-09-17 read of the accounting paragraph's recount (--diff matched at 74d637316): 105 entries (help_catalog_full.txt carries 105 rows) and `OK 108` against control.rs's VERBS.len() + 3 status line; no claim contradicted; 2026-09-17 round-19 read: the new `story` row against control_query::parse_story / cmd_story and control.rs dispatch_story_verb (the closed set, the 96-byte cap, no control byte, `OK story=<n>`, Owner-only, `@<sid>` or bare), its phase-slot claims against presence::words (TOLD_FLASH, `✓ approved`, the a11y `approved by watcher`, Level::Story and the summary's `approvals` count), and its watcher claim against aterm-agent's story_of (an approval told bare); the `status` row's `hand=` / `level=` / `story=` grammar against presence::Hand::wire, Level::wire and App::presence_status_tail; the `chrome` row against App::presence_chrome_line (the rim tokens, the quote escaping, empty quotes when the row is folded); SHORT_CATALOG_MAX_BYTES' new paragraph MEASURED, not recalled (10 102 B against the raised 10 240; VERBS is 103 rows); the two goldens regenerated as a pair from this catalog. Three slips fixed before this row: the story summary ran to 129 chars over SUMMARY_MAX_CHARS and now reads `tell the window`; the chrome detail quoted the band grammar with its two-space gaps, which the table forbids; the story detail said the watcher sends `seq=` where it tells an approval bare; 2026-09-19 read of the `status` row's `hand=` clause the round-19 second fix reworded (shown by `--diff`): `:<holder>` names the driver when the turn came over an edge — the edge's source — and an Owner-token turn names nobody whatever edges stand; `driving:<sid>` is this session's own open turn into another — read against aterm-gui's `presence_facts` (`Lease::Turn { id, driver }` → `Hand::DrivenTurn { holder: driver.map(name_of_sid) }`, role or short sid), `driving_target` (a peer's lease naming this session as driver, never a standing edge) and `Hand::wire` (`turn:<id>[:<holder>]`, `driving:<sid>`); both goldens (`help_catalog_full.txt`, `ctl_help_full.txt`) carry the same clause; no claim contradicted; 2026-09-19 read of the merged `status` summary and detail (identity= after the fabric tail, then hand= level= story=) against session_status_record and presence_status_tail, the `story` row against dispatch_story_verb/parse_story/cmd_story, the `chrome` row against presence_chrome_line, Rim::wire, Level::wire, Hand::wire and the ReadChrome arm, and the SHORT_CATALOG_MAX_BYTES note re-measured through aterm-ctl on the headless live try at 10 238 B (10 199 B of content after the `OK 109 …` line, 106 rows), two bytes under 10 240; 2026-09-20 read of the privacy row's rewrite against control_privacy.rs's folder_state, folder_source, covers_split and NEVER_COVERED: the per-folder paragraph documents the `covered-by-fda` value and `source=` for the first time (nothing in the tree could emit either before this commit), states that an observation outranks the coverage rule, and keeps `unknown` as the default for a service with no measurement of its own; the coverage paragraph records \u{a7}7 S4's 2026-09-19 measurement (four other-app data locations read with zero dialogs and no tccd traffic, the grant held) and why Full Disk Access is the ONLY durable answer to that prompt — macOS records an app-data allow against one process instance and ships no Settings switch for the class; and the never-covered class is named in full (cloud-storage File Provider domains, the media and photo libraries, App Management) to match the widened roster. Goldens regenerated on purpose with regen_help_catalog_golden; same day, second pass: the privacy row gained the `anchor=` paragraph, read against control_privacy.rs's install row and note arm and aterm_containment's ImageAnchor — the five tokens, which two mean no grant can be validated, that they earn their own note row, and WHY (running= follows the vnode and reads healthy in exactly that state). Golden regenerated with regen_help_catalog_golden; 2026-09-20 (round 21) read of the two changed verb entries against their handlers in aterm-gui. `status`'s `fabric=` paragraph gained `stale` as a fifth value, with what it names (a bridge attached, not reporting down, owing an answer for three link refreshes) and the fix: read against aterm-gui fabric.rs fabric_state, the only writer of this token, and against the sentence already in this entry saying a large age on `connected` means a quiet link — which is why the new value is NOT read off that number, and the entry now says so. `inbox seen` and `post`'s receipt prose no longer says `--receipts (which aterm fabric on writes)` but that receipts are the default whichever way the bridge was set up: read against aterm-link cli.rs parse_args and fabric.rs receipts_from_config. No claim contradicted; 2026-09-20 (round 21 review) read of `status`'s NO HEARTBEAT sentence against the ghost sweep. Any answered broker request is an ack and an ack becomes a `link up`, so the round-21 sweep on a free-running 60 s timer made every attached bridge report for ever and capped `fabric_link_age_ms=` at a minute — which would also have made the `stale` wording two paragraphs down unreachable in practice. Read against bridge.rs's gated sweep (candidates discovered once per attach, timer only while the set is non-empty) and against `ack_wants_report`; the entry now states the bounded exception. No claim contradicted; 2026-09-21 round-22 re-read of the `status` row against `session_status.rs`'s record builder, which gained `seq=`/`hash=`: both fields are appended last on both the observed and the never-classified branch, `seq=` is the terminal's `content_seq` and `hash=` is FNV-1a-64 of the untrimmed visible screen through `control_query::screen_stamp`, and `-`/`-` is what a contended `try_lock` answers — the row states all three. Its claim that this is the pair `turn` returns and `history` keeps per turn id is now true BY CONSTRUCTION rather than by coincidence: the turn verdict and the stamp both build their bytes with `control_query::screen_text`. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 read of the `subscribe` row against `Requested::parse`, `fold_mail_params`, `parse_mail_params` and `drain_mail_events`: `mail` added to the streams list with the `MAIL`/`GAP mail-dropped=` line shapes, the metadata-only rule, the live-stream seeding, the `kinds=`/`from=` grammar and its fail-closed cases, and the authority note (`inbox` and `subscribe` are the same `OpClass::Read` -> `ReadScreen`, so the stream is a push face on a read the subscriber can already make). The SUMMARY line is deliberately unchanged — it never enumerated every frame kind, and the short catalog's byte budget has no room. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 REVIEW: `SUBSCRIBE_STREAMS` added as the ONE stream vocabulary, after four hand-written copies of it were found (this catalog row, the `ERR usage` refusal, the parser's fold table and the installed drive skill) and the refusal was wrong for a whole round. The refusal and the fold table now derive from it; the skill is tested against it. No claim contradicted; 2026-09-22 (round 23, broadcast) read of every string this round changed against the handler each one describes. The NEW `topic` row (Owner, Lines, Session, OwnerOnly) was read against `fabric::cmd_topic` and `control::dispatch_topic_verb`: `ls`/the bare form answer `OK <n>` then one `topic <t> since=<head|@<off>>` row each, `add` answers `OK <t> since=<..> added=<0|1>` and `drop` `OK <t> dropped=<0|1>` — and because those two are STATUS lines while the listing is Lines, `framing_of` gained the `topic add|drop` flip beside `inbox seen`'s, the hazard `identities forget` measured on 2026-09-17 (a client that reads the TOPIC as a row count reports a malformed header). The claim that the set is EMPTY by default and that an empty set receives nothing is `SessionFabric::topics` plus `bridge::on_say_record`, whose recipient filter is the topic map and nothing else; the `since=` sentences are `bridge::sample_topics` (resolved once, at first sight) and `catch_up_topic`. The `post` row's summary now spells `say[:<topic>]` (read against `bridge::resolve_to` and `fabric::cmd_post`'s `valid_topic` check, which refuses a malformed topic at the post rather than retiring a record `unroutable` a publish later) and its broadcast paragraph was CORRECTED as well as extended: it said `task`/`ask` are demoted, and `bridge::DEMOTE_UNLESS_ACCEPTED` is `[\"task\", \"control\"]` — `ask` is not in it and never was, so the sentence now names task/control, read against `classify_kind`. 2026-09-22 (round 23 review) CORRECTION OF THE PREVIOUS CLAUSE: no committed `post` row ever said `task`/`ask` are demoted — `git show origin/main:crates/aterm-types/src/control_verbs.rs` and the item-1 commit both name `ask` only under kinds, `--wait` and `dl=`. The sentence that was corrected existed only in this round's uncommitted draft, so the clause above recorded a read of a defect that shipped help never had; what IS true is that the row now says task/control, read against `DEMOTE_UNLESS_ACCEPTED`. The `subscribe` row gained `mail:topic=<t>`, read against `subscribe::parse_mail_params` and `MailFilter::wants`. SHORT_CATALOG_MAX_BYTES was RAISED from 10 240 to 10 496 with the measurement in its doc (10 358 measured by `help_short_form_is_bounded_and_is_the_summary_catalog`, 118 over: 109 for the new row plus 9 for the `post` summary), which is the raise the previous paragraph predicted. Golden pair regenerated in order (types, then gui). No claim contradicted; 2026-09-22 (round-23 review) read of the `topic` row's `serial=<n>` clause against `cmd_topic` and `bridge::sample_topics`, and of the SHORT_CATALOG_MAX_BYTES doc against the measurement: rows sum to 9 986 (the types-side test's number), the `topic` row is 110 and the `post` row grew 10, and only aterm-gui's wire test sees the 10 358. No claim contradicted; 2026-09-22 merge read (round 23 onto main): upstream's `privacy` claimants=/census= rows read against control_query's privacy handler, with round 23's topic/say/subscribe rows — consistent; 2026-09-22 read of the one reworded sentence in the `trail` verb's long help (shown by `gate help-surfaces --diff`): the row now ends `ribbon_retired= ribbon_followed=`, read against `TrailStatus::line_v2` (cursor_glow.rs, the two fields last, in that order); `ribbon_followed=` is `rk::Status::followed`, the cumulative count `Engine::follow_rows` adds and `Engine::reset` keeps; the follow search is `Witness::follow_runs` (dr in -1, +1, -2, +2 — \"one or two rows\", same glyph at the same column, as a block); a run carried off the caret row is set flowing by `Ribbon::flow_cohort` and `Cohort::swoosh_total` is `FLOW_TOTAL_S` = 1.10 + 0.24 = 1.34 s from `Cohort::clock`; the `ribbon_retired=` melt is `RETIRE_MELT_S` = 0.12 s; merged with origin/main's round-22 reads of this file on 2026-09-22 by the rainbow-ribbon integration (both sides' prose kept; the hash is the merged file's); 2026-09-22 (round 24, item 1) read of the `topic` row's rewritten sentence and the `timeline` row's kind list against `fabric::cmd_topic` (an add that added and a drop that dropped each `record(\"topic\", …)` on the session timeline), `subscribe::timeline_wire_kind` via `FABRIC_EVENT_KINDS` (now six, `topic` last) and `bridge::on_event`'s `topic` arm → `topic_change`: the `ls` row carries no serial any more, and an add or drop reaches the bridge as `EVENT <local> topic add|drop …` at once. No claim contradicted; 2026-09-22 (round 24, item 2) read of the `fabric` row's summary (now `status|attach|retire`, trimmed to the summary bound) and its new `fabric retire` sentence against `cmd_fabric`/`cmd_fabric_retire` (control.rs), the roster journal's `RetireRequested` row and its `EVENT * fabric-retire <sid>` push (session_store.rs, subscribe.rs) and the bridge's `fabric-retire` arm → `retire_on_request` (hosting re-checked, the row published `exited` with its own fields through `publish` and the bridge's own sequence). No claim contradicted; merged with upstream's own read of the same day: 2026-09-21 read of the one added sentence in the `update` verb's long help (shown by `gate help-surfaces --diff`): while the posture is automatic, `apply_phase` names the ladder phase — prefer-idle, prefer-output-gap, keys-only, land — and the ladder ends with the build applied no later than 15 minutes after arming; read against `update_control::Snapshot::apply_phase` (the four `ApplyPhase::as_str` spellings, None for every non-automatic posture), `native_update_auto_intent::apply_phase` and `LANDS_WITHIN` (120 + 180 + 600 s = 15 min), and the ctl/help goldens regenerated in the same commit; no claim contradicted; catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 read of round 15's key and deadline catalog prose (shown by `xtask gate help-surfaces --diff`, matched at 135cf7646): `post`'s key= (aterm-gui fabric.rs valid_key; aterm-link state.rs key_seq/set_key_seq and KEYS_KEEP; bridge.rs drain_outbox's reserved sequence, and `dup=1` only for a key-chosen deduped publish) and dl= (note_deadline, settle_deadline_for, settled_on_bus, expire_deadlines); `outbox`'s `[key=<token>]` and `outbox sent … dup=1` against cmd_outbox/cmd_outbox_sent/retire_post; and the needle table's new rows against the rows they pin. THREE defects FIXED here: the `inbox` post-row grammar omitted the `key=` cmd_inbox prints on it; `post`'s futile-wait answer named `<absent|disconnected>` although fabric_wait_refusal answers `stalled` too; and `late=1` was unqualified although the bridge's `expired` map is in memory only, so a relaunched bridge delivers a late reply unflagged. Both help goldens regenerated as a pair. No other claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset catalog prose: `await inbox re=` against aterm-gui fabric.rs cmd_await_inbox (re= accepts every kind, since= defaults to 0); `inbox`'s oldest_on_bus header and verdict= against cmd_inbox/render_row; `inbox get @<off>` against inbox_get_at, deliver_fetched and Fetched::render (the ring answers only a whole row, FETCH_SLOTS 8, FETCH_WAIT_MS 10 s, `ERR fabric <state> off=` at once, chunked answers up to 256 KiB, one `ERR no such record`); `inbox seen`'s receipt against cmd_inbox_seen/owe_receipt and aterm-link bridge.rs send_receipt/retire_receipt; `post`'s --wait-ack bound (explicit ms, else dl= plus EXPIRY_GRACE_MS, else 30 s); `deliver`'s fetched= and receipt= forms against deliver_fetched/deliver_receipt and fetched_lines; `outbox`'s receipt and fetch lines against cmd_outbox; and SHORT_CATALOG_MAX_BYTES' round-15 raise, re-measured by printing cmd_help(\"\")'s length in help_short_form_is_bounded_and_is_the_summary_catalog: 9 863 B under 9 984. ONE defect FIXED here: a line continuation rendered `deliver`'s `verdict=<handled| refused|deferred>` with a stray space. Both help goldens regenerated as a pair. No other claim contradicted; AND THE v0.86 CANDIDATE'S READS THAT MAIN'S ROW CARRIED AHEAD OF THE READ ABOVE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally. 2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `await`, `inbox`, `inbox get`, `inbox seen`, `post`, `deliver`, `outbox` and `outbox sent` rows, the needle table's new rows, SHORT_CATALOG_MAX_BYTES' 9984 note and the `inbox get` history's round-15 aside: 8 hunks, shown by `xtask gate help-surfaces --diff` against main's read at 595e13613) plus upstream's change (the `update` row's apply-posture sentences, the `privacy` row's this_process, app-data and unmeasured sentences and needles and the `trail status` row's ribbon_drawn=/ribbon_curtain_ms= keys and gloss, all read by main — and f3a5467ed's `version` row, which main recorded no row for: flavor=t|r, trust= as Trust's own version (`none` on an upstream build), rust_compat=, and trustc=/trustc_commit=/trustc_host= carrying the rustc-shaped token, shown against round 15's read at 030ba1788) plus ONE sentence added at this merge to `post`'s key= text — its ADDRESS is still resolved first, against the live roster, so a re-post to one that no longer routes is retired `ERR unroutable` (or `ambiguous`), appends nothing and leaves the key naming its record (both help goldens regenerated as a pair from the merged catalog), checked against aterm-gui build_info.rs control_line (trust= is `none` off flavor t and `unreported` on a Trust build predating the marker, a value the row does not list but does not deny; rust_compat= and trustc= are both compiler_release()), update_control.rs apply_posture and apply_policy_reason (the twelve words the row lists, none other), control_privacy.rs covers_split and observed_fda_scope, aterm-effects cursor_glow.rs TrailStatus::line, and aterm-link bridge.rs drain_outbox (resolve_to before StateDir::key_seq; the non-routing arm retires `off=- reason=` and clears only the post's pin) with the r15_receipts test a_keyed_re_post_to_a_session_whose_exit_line_never_arrives_is_unroutable_and_the_key_holds;  2026-09-14 (the Rainbow Path v3 curtain merge): the ONE prose change `--diff` shows is the `trail status` key list gaining `ribbon_drawn= ribbon_curtain_ms=` between `ribbon_hue_bands=` and `field=`, READ against TrailStatus::line in aterm-effects cursor_glow.rs, whose format string emits them in exactly that position and prints `ribbon_curtain_ms=none` through `Option::map_or_else` when no curtain is falling; against CursorGlow::ribbon_drawn and CursorGlow::curtain_left_ms, both gated on `self.v2.engaged()` and answering 0 / None for every other style; against Engine::ribbon_drawn = Ribbon::ink_quad_count (the LAST FRAME'S own quads whose composite `reads_as_ink`) and Engine::curtain_left_ms = Ribbon::curtain_left_ms; and against App::trail_status in app_render.rs, which fills both. One COMPLETENESS slip fixed in the same commit rather than recorded around: the merge added the two keys to the list and glossed neither, and they are the pair a reader cannot use unglossed — `ribbon_segments=` is floored once for the arc's DIMMEST stop (`STATUS_LIT_COV`) and is a bound on what may be CLAIMED, which is why it read 0 for the last half of every curtain while a thousand quads were still compositing, and `ribbon_drawn=`/`ribbon_curtain_ms=` are the fact and the reason beside that claim. The row now says so, in the same words `TrailStatus::ribbon_drawn`'s and `CursorGlow::ribbon_drawn`'s own doc comments use, and both goldens were regenerated as a pair; the hash recorded here is the GLOSSED row. No claim contradicted.; AND UPSTREAM'S OWN READ OF THE SAME PROSE, recorded in parallel and kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-14 v0.86 release-candidate merge: this file now carries BOTH halves of prose — upstream's and this candidate's — each read in full by its own author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept rather than dropped: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 the privacy row re-read against control_privacy.rs after 47198639f: its `which services it covers is not measured here, so fda_scope=unknown` clause contradicted covers_split/observed_fda_scope, which render `fda_scope=this_process` and `covers=app-data` for a Granted probe; reworded to the two things a grant now establishes and the three it still does not (other services unmeasured, adopted sessions inherit nothing, folder rows unknown), the scope-ruling sentence kept, the test's needles moved with it, both goldens regenerated; no other claim contradicted; 2026-09-14 v0.86 candidate merge: the file carries BOTH halves of prose — upstream's and this candidate's — each read in full by its author against the same handlers, neither contradicting the other, and this row records the UNION's hash; AND UPSTREAM'S READ OF THE SAME PROSE, kept: catalog read in full against the handlers by aterm-help-surfaces-read on 2026-09-10; the fabric row and the reworded hold row read against dispatch_fabric_verb / dispatch_hold_verb and the access-set pins by verify:F1 and verify:F2 of aterm-fabric-attach-round-3 on 2026-09-10; goldens regenerated as a pair from the merged catalog on 2026-09-11; merged with origin/main's own 2026-09-11 re-read of this file, conflicts resolved by the orchestrator (upstream wording kept where both sides fixed the same claim). 2026-09-12: the ONE prose change since that read is SHORT_CATALOG_MAX_BYTES' raise paragraph, read in full and MEASURED rather than recalled — aterm-gui's bare cmd_help is 9 610 B against the new 9 728 ceiling, VERBS is 101 rows, the summary rows alone are 9 238 B; the fabric, fx and hold rows and the OpClass/Access/Framing/Target docs re-read against dispatch_fabric_verb, cmd_fabric/cmd_fabric_attach/fabric_status_line, dispatch_hold_verb/fabric::cmd_hold and the access-set pin, the other 98 catalog rows sampled. One contradicted claim fixed in the commit that updated this row — Access::OwnerOnly called `hold` the ONE member whose handler tells the two owner-class scopes apart, and `fabric`'s handler is a second (Scope::Owner exactly, the bridge refused), as aterm-gui's own is_owner_class doc already records; 2026-09-13 (UTC; 2026-09-12 local) re-read of the trail row's paste-sweep additions (the licence= classes, program-row, the inserts_* status keys, which inputs stamp an insert and which stay dark, and the `inserts_delivered>0 inserts_lit=0` reading after a drop) against aterm-effects cursor_glow.rs AdmissionRecord::line/LICENCE_*, move_licensed, insert_echo/insert_rewrite/INSERT_HINT_FRESH, note_insert_delivered/lay_insert/retract_insert/insert_tally and TrailStatus::line, app_render.rs tick_cursor_fx's Rainbow-Kitty-gated delivery prelude, app_input.rs input_paste and insert_gesture_armed, lib.rs drop_file/deliver_paste, and control.rs's flagless, front-routed, background, guarded and run_feed_bin_routed paste-bin routes, fixing the contradictions in the row (paste-bin into the tab on screen stamps an insert, not nothing; the insert class and counters are Rainbow Kitty only; `A move paints only if a keypress LICENSED it` now admits the delivered insert; the key class gains the composer newline and ⌃V's gesture; inserts_delivered counts a bare Tab and ⌃V too; last_insert_cells is the 32-cell bound when unpriced; the drop reading covers an echo never seen inside the 2 s window) and regenerating both goldens as a pair; the 0.84 train's release-candidate merge (this machine's provenance/repair/TCC work over the peers' 2026-09-12 re-read at the tip) moved the bytes once more — both halves were read by their authors as recorded, the merged file is their union, and this row records the union's hash; 2026-09-13 read of the prose ca54aaadd moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0f7667e8a) — the trail row's `trail status` key list gaining `park_returns= park_flushed=` and its new HELD PARKS sentence — against aterm-effects cursor_glow.rs TrailStatus::line (the two keys last, after swallowed_no_echo=), InFlightTally's park_returns/park_flushed, HeldPark, park_candidate (Rainbow Kitty, same-row backward, a fresh stamp or presses in flight), spawn's RETURN arm (a forward move from the landing past the origin within TYPE_HINT_FRESH = 0.25 s, park_return_paid, judged origin -> target) and flush_park, and the flushes at the stale-park tick, note_backspace, note_scroll and the next other move; no claim contradicted; 2026-09-13 read of the prose feat/round-7-offscreen moved (shown by `xtask gate help-surfaces --diff`, matched at ca54aaadd) — the new `offscreen` row (Read, Lines, Session; the summary, and the detail sentence by sentence) and the `history` row's arch= clause — against aterm-gui control_query.rs offscreen_args/cmd_offscreen/format_offscreen_reply/OFFSCREEN_DEFAULT_MAX, aterm-core alt_archive.rs (ALT_ARCHIVE_DEFAULT_BUDGET, env_opted_out's 0/off/false/no, AltArchive::read and wipe, the gap kinds, the ESU commit and the 16 ms epilogue fallback), control.rs json_unsupported and control_session.rs cmd_history's arch= field; two slips fixed before this row (a committed frame was only `a DEC 2026 close`, where an app that never sends one is committed at the batch epilogue at most once per 16 ms; lost= counted evicted rows only, where a reset or turning the archive off wipes rows into it too) and both goldens regenerated as a pair; 2026-09-13 merge of main into feat/round-7-offscreen: main's drift-sweep re-read on 2026-09-13 (lane small-b) found the trail licence= row saying an unpaid press logs `key` and fixed it; `gate help-surfaces --diff` at the merge shows the merged prose is exactly round 7's read offscreen row and history arch= clause plus main's trail licence=/decline-reasons correction and its rainbow-kitty v2_ sentence (added 2026-09-12, before main's recorded read); recorded at the merge; 2026-09-14 read of the prose round 10 moved (shown by `xtask gate help-surfaces --diff`, matched at bd8f70ca9) — the offscreen row's `in memory` (no longer `only`), its self-update sentence (the origin kept; the rows after the last 8 submitted turns' marks and at least the running app's last 8 screens, the newest without a turn, up to 1 MiB with older ones in lost=; a new origin when not carried; the resize breaks= gap) and breaks='s scroll-back clause, and the history row's carried=1 sentence — against aterm-gui handoff_carry.rs export/tail_from/TAIL_TURNS/CARRY_ARCHIVE_BYTES/encode_within/decode, aterm-core alt_archive.rs carry_head, carry_reach (REANCHOR_SCREENS screens, never below the app run's floor, and the re-shown run), carry_rows, import and shift_down's Jump gap on a scroll-back past the oldest retained row, control_session.rs cmd_history (` carried=1` before text=) and raise_turn_ids, turn_ledger.rs TurnRecord::carried, and seamless.rs take_incoming's next_turn_id raise; one slip fixed before this row (the carry was said to reach at least the last 8 screens, where carry_reach stops at the running app's floor — now the running app's last 8 screens) and both goldens regenerated as a pair; 2026-09-13 release-candidate merge of the leftovers lane over the peers' round-7/round-8 reads: the merged prose is the UNION of the two halves — main's `offscreen` row and `history` arch= clause (read by round 7's author, recorded above) and the leftovers lane's `momentum_glow=` key and the absorbed rainbow-kitty v2_ sentence (read by that lane, recorded next) — each half read in full by its author against the code it describes, neither contradicted by the other, and this row records the UNION's hash: re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane small-b); the trail licence= roster said an unpaid press logs `key` and is fixed in that commit; that read recorded 212628b6b90c5780 (834cd5f9f), and main 2f15705bc was ALREADY red on this row: the wrapped-row band merge (169dafa7b) had added a sentence to the `trail status` row that nobody read — `While rainbow kitty owns the frame the row ends with v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` and its glosses; 2026-09-13 leftovers lane: TWO prose changes since that read, both read here: (1) the `momentum_glow=` key in the backticked run and its sentence (MomentumGlow::value beside the cat's `momentum=`), against TrailStatus::line in cursor_glow.rs and App::trail_status in app_render.rs (`ws.momentum_glow.value(now, MOMENTUM_GLOW_TAU_S)`), pinned by control.rs trail_status_help_enumerates_exactly_the_keys_the_row_emits; (2) the absorbed v2 sentence, against TrailStatus::line_v2 in cursor_glow.rs (appends exactly ` v2_quads= v2_halos= v2_stars= v2_meteors= v2_bridged= ribbon_retired=` from rk::Status, and only when CursorGlow::v2_status is Some — `self.v2.engaged()`, so only while rainbow kitty owns the frame, at the tail), rainbow_kitty/mod.rs Status (quads/halos written this frame, stars and meteors live; `bridged` = cells the echo ledger relit for a late echo the admission ring scored declined; `retired` = ribbon cells retired by CONTENT over the engine's life — Engine::witness_rows when the glyph under a cell changed or went, Engine::retire_row when the caret was seen on another row through a declined move — cumulative across Engine::reset) and app_render.rs (per-window `ws.cursor_glow`, so `the window's cumulative count`); no claim contradicted; 2026-09-13 (2026-09-14 UTC) read of the prose 9758cd022 moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at 0333f4a441d2) — the `post` row's rewritten `no-bridge=1` paragraph (NARROWER rather than the opposite, not a verdict on the message, `fabric attach <command...>` arms a supervisor and that same outbox drains, further posts only until `ERR outbox full`) and its new THIRD OUTCOME sentence (`ERR timeout id=<n>`, the `--wait` expiring with no landing reported, queued exactly like the other two), plus the two `contains` assertions in the_fabric_rows_state_the_bounds_they_are_held_to that pin `none is coming ON ITS OWN` and `ERR timeout id=<n>` — against aterm-gui fabric.rs cmd_post (its option tokens are to/kind/re/dl/via/--wait/len only, so there is no idempotency key that could collapse a re-post; the refusal at the door answers `ERR outbox full queued= bytes=` at OUTBOX_CAP = 128 or OUTBOX_BYTES_MAX = 4 MiB measured by queued_load + caller_sized_bytes; the PostRow is pushed BEFORE the wait loop), that loop's deadline arm returning `ERR timeout id={id}` with the row untouched (only trim_retired_posts removes rows and only ones carrying `off` or `dead`, so a timed-out post is still queued and still drained), fabric_wait_refusal and bridge_reachable (`no-bridge=1` is exactly `!supervised && state == absent`), note_bridge_supervised, fabric_launch.rs spawn_supervisor/arm/preflight and control.rs cmd_fabric_attach (`fabric attach <command...>` is the one seam that starts a supervise thread at RUNTIME and flips the latch), and aterm-link bridge.rs drain_outbox, whose bare `outbox` peek takes exactly the `off.is_none() && !dead` rows and runs on the reconcile path a freshly attached bridge takes; no claim contradicted — the retired `Nothing will publish it, no answer can arrive` WAS false, and control.rs fabric_attach_arms_the_supervisor_of_a_running_instance_once_and_for_owner_only shows the flip (`no-bridge=1` at id=1 and id=2, `queued=1` at id=3 after the attach, the two earlier posts still queued); one narrowness left standing rather than fixed here: the row gives an instance with no `[fabric] command` as the cause, where the predicate is bridge_reachable, so a CONFIGURED command whose program fails arm's preflight answers `no-bridge=1` too (that same test's `/nonexistent/aterm-link`, id=2), and the remedy the row prints is the one spawn_supervisor's own warn line names for that case as well AND the same completeness slip the manual.rs read found was fixed here in this commit: the row enumerated three outcomes and `cmd_post` has a fourth — `ERR <reason> id=<n>` for a post the bridge retired (`unroutable`/`ambiguous`/`undeliverable`), which `outbox` then omits, so it is the one outcome that does NOT mean queued; the row now says so and `the_fabric_rows_state_the_...` pins `ERR <reason> id=<n>` and `unroutable` beside the other three. The hash recorded here is the CORRECTED row, and both goldens were regenerated as a pair; 2026-09-14 drift sweep (lane cli-and-types, 25 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code The 2026-09-14 merge of the audit2 lane over the cli-and-types drift sweep moved the bytes once more; `--diff` shows exactly two prose lines, both the audit lane's own and read here against the code: the `no-fresh-hint` gloss's Rainbow Kitty clause (seam_licensed gates a fresh typed stamp on typed_credits_within >= 1 under GlowStyle::RainbowKitty, logged DECLINE_NO_FRESH_HINT; pinned by a_caret_that_advances_with_no_unpaid_press_still_buys_nothing), and the `inflight_forgotten=` edge list (forget_typed_credits at note_kill, at the keyless backward/cross-row refusal, and at the licensed move when the licence is not Typed, the hop is cross-row and unhinted, credit_starved or typed_over_cap — a same-row `no-fresh-hint` refusal is none of those and keeps the pool; a glyph's echo with Enter fresh takes the Typed licence, pinned by a_glyph_echo_does_not_spend_the_enter_pressed_behind_it); no claim contradicted; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's offscreen and history rows (`in memory`, the self-update carry sentence, breaks='s scroll-back clause, carried=1) plus main's reads recorded above (the trail rows' paid-press no-fresh-hint gloss, the `momentum_glow=` key and sentence and the inflight_forgotten edge list; the post row's NARROWER no-bridge=1, its THIRD and FOURTH outcomes and their test needles) — `xtask gate help-surfaces --diff` against main's read (matched at d68552079) shows round 10's two hunks and against round 10's read (matched at a4a023d4a) main's three, nothing else — checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, TAIL_TURNS), alt_archive.rs (REANCHOR_SCREENS, carry_reach, import) and control_session.rs cmd_history, untouched by main, and against cursor_glow.rs seam_licensed (a Rainbow Kitty typed stamp licenses only while the press ring owes a cell), TrailStatus::line (momentum_glow= after momentum_display=), forget_typed_credits' edges and fabric.rs cmd_post's four wait outcomes, untouched by round 10; no claim contradicted; both goldens regenerated as a pair at the merge and unchanged 2026-09-14 (merge of the new-line fade round): the ONE prose change is the `ribbon_retired=` sentence, which now says the count includes cells RELEASED to the swoosh when their text went, as well as cells retired on the fast melt when it was replaced — read against Engine::witness_rows and Status::retired (rainbow_kitty/mod.rs), which count both on the same tally.; 2026-09-14 read of the round-13 additions (the `status` row's fabric=<connected|stalled|disconnected|absent>, fabric_rtt_ms= and fabric_link_age_ms= sentences, the `fabric` row's state/reason/rtt_ms/link_age_ms reply shape, and the new bridge-only `link` row) against aterm-gui fabric.rs fabric_state, fabric_link_facts, fabric_status_tail, bridge_attached (an attach stores STALLED with reason=starting and no numbers), link_report (accepted only for the owning generation, dropped once DISCONNECTED, CONNECTED/STALLED stored under the generation lock, every registered session woken on the up-to-down transition) and cmd_link (the grammar, `OK stale=1` for a ghost lane), control.rs fabric_status_line and dispatch_bridge_verb's `link` arm, session_status.rs's record tail, and aterm-link bridge.rs LinkReport::ack_wants_report / down_wants_report (change, more-than-2x past LINK_MOVE_GAP = 250 ms, or LINK_REFRESH = 2 s), link_reason's tokens, ACK_DEADLINE = 5 s and RECONNECT_MIN = 100 ms / RECONNECT_MAX = 5 s; goldens regenerated as a pair; SHORT_CATALOG_MAX_BYTES raised to 9856 for the row with the same accounting as the three raises before it; no claim contradicted; 2026-09-15 recovery audit: read the update synopsis and full help against cmd_update and update_control::Snapshot. Apply acknowledges a request before asynchronous validation; checks use the current GUI source and notify on every completion. relaunch_ready indicates stage existence, the enumerated apply_posture values match the exact-artifact projection including unknown/unreconciled, and apply_policy_reason is emitted only for a current policy block. No readiness or timing guarantee is asserted; both generated help fixtures are regenerated together. The subsequent privacy-help merge was read against observed_fda_scope, covers_split and per-session privacy observations: a host grant establishes this_process and app-data coverage, other services remain unmeasured/uncovered, and adopted sessions and folder rows retain unknown access. The row records both changes.; 2026-09-14 v0.86 candidate merge: this file carries BOTH halves of prose — upstream's and the candidate's — each read by its own author against the same handlers, and the row records the UNION's hash; 2026-09-15 read at the landing of the erase pending-wrap fix, over f3a5467ed: read the version verb's new key detail (flavor=t|r, trust= as Trust's own version and none on upstream, rust_compat= as the compatible Rust release, trustc=/trustc_commit=/trustc_host= as release token, commit and host, the v0.10.0 rename, and trustc= equal to rust_compat=) against build_info control_line, compiler_release, compiler_commit_short, compiler_probe parse_rustc_vv and detect_flavor, build.rs, both version dispatch sites in aterm-gui control.rs, the CHANGELOG 0.10.0 entry, the two help goldens and this box's live rustc -vV, and no claim contradicted the code.; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged prose is round 15's read change plus main's, and it hashes to this row's recorded value, so no prose moved at the merge; 2026-09-15 read of the new `kitty` verb entry against its handlers: the summary `kitty [wear <key>]` and every field of the two documented row shapes read against App::kitty_collection_rows (`cat key={} coat={} iris={} age={} seen={} worn={worn}`, `seen=` being the roster row's `count`) and App::wear_kitty_on_front (`worn key={key} coat={} iris={}`) in crates/aterm-gui/src/app_input.rs; the `worn=1` claim against KittyLogHost::wearable, which derives the worn key from the cached `favourite` the companion reads rather than a second roster election (two pins inside one RFC3339 second tie, and the key breaks the tie the other way — found and fixed while writing the test); `the key is the row's own key=` against KittyLog::wear's `item.key == key`; the Favourite-This-Kitty claim against App::promotable_kitty (kitty_tenure.worn().map(|i| i.look).unwrap_or(self.launch_kitty)); `elects by GREATEST pin stamp` against KittyLog::favourite_look's max_by over `favourite`, and `survives a merge` against max_ts in favourite_collectible/merge_collectible; the three refusal cases against App::wear_kitty's sparkle gate, cfg.feline gate and None arm. `Write` rather than ConfigWrite is pinned by control.rs's required_op census. No claim contradicted; 2026-09-15 (2026-09-16 UTC) read of the `metrics` row's echo paragraph, added by fe8ec4d58 (shown by `xtask gate help-surfaces --diff`, matched at b3a62ce1d), against crates/aterm-gui/src/control_query.rs cmd_metrics and crates/aterm-gui/src/echo_rtt.rs percentile_fields_text — the echo fragment does ride the PLAIN summary line, and the who-owes-the-time reading matches the episode the code itself records (n_echo=499 echo_p95_ms=75.69 echo_p99_ms=150.04 against aterm's own key_write_p99_ms=6.29). ONE SLIP FIXED in the commit that updated this row: the row said the line `ends ... then the CHILD's own round trip`, but the summary's LAST fragment is watchdog::turn_census_fields_text (e50cbbff4), spliced after echo_rtt's — the row now names that turn census and its seven fields as the end of the line, and lists echo_total= with the rest of the ledger percentile_fields_text actually prints; 2026-09-16 read of the tab entry's rewritten reply sentence a29417119 changed (OK active count only when the action HAPPENED, ERR reason when it did not: an index no tab holds, or a tab host that declined the close — a native document waiting on a durable checkpoint, or a close deferred behind a pending update handoff) against apply_tab_cmd_in's no_such bounds refusals for Select, Close(N) and both ends of Move; close_active_native_tab's Err arms (tab close deferred until update child rollback, returned behind defer_pending_update_handoff_teardown, which queues the mutation onto the pending handoff's teardown and applies it after the cancellation lands; native document close is waiting for a durable checkpoint; terminal close requires confirmation); and the regression test an_out_of_range_tab_action_refuses_instead_of_reporting_ok, which pins each refusal beside an in-range positive control; the headless sentence is unchanged and still holds; no claim contradicted; 2026-09-17 read of the round-18 identities rows: the recut `spawn` summary and its identity= sentences against control_media::parse_spawn_args/resolve_spawn_identity and control::escalated_op's fence; the `sessions` summary/detail `identity=` against control_session::sessions_lines; the `status` reply list against session_status::session_status_record; the new `identities` row's reply table against agent_identity::identities_reply arm by arm (list; one, `OK <n>` counting the agent lines since the client streams exactly n; forget unconfirmed, in use, removed, unknown, usage); the SHORT_CATALOG_MAX_BYTES paragraph against the short help measured on the live headless instance; 2026-09-17 read of the accounting paragraph's recount (--diff matched at 74d637316): 105 entries (help_catalog_full.txt carries 105 rows) and `OK 108` against control.rs's VERBS.len() + 3 status line; no claim contradicted; 2026-09-17 round-19 read: the new `story` row against control_query::parse_story / cmd_story and control.rs dispatch_story_verb (the closed set, the 96-byte cap, no control byte, `OK story=<n>`, Owner-only, `@<sid>` or bare), its phase-slot claims against presence::words (TOLD_FLASH, `✓ approved`, the a11y `approved by watcher`, Level::Story and the summary's `approvals` count), and its watcher claim against aterm-agent's story_of (an approval told bare); the `status` row's `hand=` / `level=` / `story=` grammar against presence::Hand::wire, Level::wire and App::presence_status_tail; the `chrome` row against App::presence_chrome_line (the rim tokens, the quote escaping, empty quotes when the row is folded); SHORT_CATALOG_MAX_BYTES' new paragraph MEASURED, not recalled (10 102 B against the raised 10 240; VERBS is 103 rows); the two goldens regenerated as a pair from this catalog. Three slips fixed before this row: the story summary ran to 129 chars over SUMMARY_MAX_CHARS and now reads `tell the window`; the chrome detail quoted the band grammar with its two-space gaps, which the table forbids; the story detail said the watcher sends `seq=` where it tells an approval bare; 2026-09-19 read of the `status` row's `hand=` clause the round-19 second fix reworded (shown by `--diff`): `:<holder>` names the driver when the turn came over an edge — the edge's source — and an Owner-token turn names nobody whatever edges stand; `driving:<sid>` is this session's own open turn into another — read against aterm-gui's `presence_facts` (`Lease::Turn { id, driver }` → `Hand::DrivenTurn { holder: driver.map(name_of_sid) }`, role or short sid), `driving_target` (a peer's lease naming this session as driver, never a standing edge) and `Hand::wire` (`turn:<id>[:<holder>]`, `driving:<sid>`); both goldens (`help_catalog_full.txt`, `ctl_help_full.txt`) carry the same clause; no claim contradicted; 2026-09-19 read of the merged `status` summary and detail (identity= after the fabric tail, then hand= level= story=) against session_status_record and presence_status_tail, the `story` row against dispatch_story_verb/parse_story/cmd_story, the `chrome` row against presence_chrome_line, Rim::wire, Level::wire, Hand::wire and the ReadChrome arm, and the SHORT_CATALOG_MAX_BYTES note re-measured through aterm-ctl on the headless live try at 10 238 B (10 199 B of content after the `OK 109 …` line, 106 rows), two bytes under 10 240; 2026-09-20 read of the privacy row's rewrite against control_privacy.rs's folder_state, folder_source, covers_split and NEVER_COVERED: the per-folder paragraph documents the `covered-by-fda` value and `source=` for the first time (nothing in the tree could emit either before this commit), states that an observation outranks the coverage rule, and keeps `unknown` as the default for a service with no measurement of its own; the coverage paragraph records \u{a7}7 S4's 2026-09-19 measurement (four other-app data locations read with zero dialogs and no tccd traffic, the grant held) and why Full Disk Access is the ONLY durable answer to that prompt — macOS records an app-data allow against one process instance and ships no Settings switch for the class; and the never-covered class is named in full (cloud-storage File Provider domains, the media and photo libraries, App Management) to match the widened roster. Goldens regenerated on purpose with regen_help_catalog_golden; same day, second pass: the privacy row gained the `anchor=` paragraph, read against control_privacy.rs's install row and note arm and aterm_containment's ImageAnchor — the five tokens, which two mean no grant can be validated, that they earn their own note row, and WHY (running= follows the vnode and reads healthy in exactly that state). Golden regenerated with regen_help_catalog_golden; 2026-09-20 (round 21) read of the two changed verb entries against their handlers in aterm-gui. `status`'s `fabric=` paragraph gained `stale` as a fifth value, with what it names (a bridge attached, not reporting down, owing an answer for three link refreshes) and the fix: read against aterm-gui fabric.rs fabric_state, the only writer of this token, and against the sentence already in this entry saying a large age on `connected` means a quiet link — which is why the new value is NOT read off that number, and the entry now says so. `inbox seen` and `post`'s receipt prose no longer says `--receipts (which aterm fabric on writes)` but that receipts are the default whichever way the bridge was set up: read against aterm-link cli.rs parse_args and fabric.rs receipts_from_config. No claim contradicted; 2026-09-20 (round 21 review) read of `status`'s NO HEARTBEAT sentence against the ghost sweep. Any answered broker request is an ack and an ack becomes a `link up`, so the round-21 sweep on a free-running 60 s timer made every attached bridge report for ever and capped `fabric_link_age_ms=` at a minute — which would also have made the `stale` wording two paragraphs down unreachable in practice. Read against bridge.rs's gated sweep (candidates discovered once per attach, timer only while the set is non-empty) and against `ack_wants_report`; the entry now states the bounded exception. No claim contradicted; 2026-09-21 round-22 re-read of the `status` row against `session_status.rs`'s record builder, which gained `seq=`/`hash=`: both fields are appended last on both the observed and the never-classified branch, `seq=` is the terminal's `content_seq` and `hash=` is FNV-1a-64 of the untrimmed visible screen through `control_query::screen_stamp`, and `-`/`-` is what a contended `try_lock` answers — the row states all three. Its claim that this is the pair `turn` returns and `history` keeps per turn id is now true BY CONSTRUCTION rather than by coincidence: the turn verdict and the stamp both build their bytes with `control_query::screen_text`. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 read of the `subscribe` row against `Requested::parse`, `fold_mail_params`, `parse_mail_params` and `drain_mail_events`: `mail` added to the streams list with the `MAIL`/`GAP mail-dropped=` line shapes, the metadata-only rule, the live-stream seeding, the `kinds=`/`from=` grammar and its fail-closed cases, and the authority note (`inbox` and `subscribe` are the same `OpClass::Read` -> `ReadScreen`, so the stream is a push face on a read the subscriber can already make). The SUMMARY line is deliberately unchanged — it never enumerated every frame kind, and the short catalog's byte budget has no room. Goldens regenerated as the pair. No claim contradicted; 2026-09-22 round-22 REVIEW: `SUBSCRIBE_STREAMS` added as the ONE stream vocabulary, after four hand-written copies of it were found (this catalog row, the `ERR usage` refusal, the parser's fold table and the installed drive skill) and the refusal was wrong for a whole round. The refusal and the fold table now derive from it; the skill is tested against it. No claim contradicted; 2026-09-22 (round 23, broadcast) read of every string this round changed against the handler each one describes. The NEW `topic` row (Owner, Lines, Session, OwnerOnly) was read against `fabric::cmd_topic` and `control::dispatch_topic_verb`: `ls`/the bare form answer `OK <n>` then one `topic <t> since=<head|@<off>>` row each, `add` answers `OK <t> since=<..> added=<0|1>` and `drop` `OK <t> dropped=<0|1>` — and because those two are STATUS lines while the listing is Lines, `framing_of` gained the `topic add|drop` flip beside `inbox seen`'s, the hazard `identities forget` measured on 2026-09-17 (a client that reads the TOPIC as a row count reports a malformed header). The claim that the set is EMPTY by default and that an empty set receives nothing is `SessionFabric::topics` plus `bridge::on_say_record`, whose recipient filter is the topic map and nothing else; the `since=` sentences are `bridge::sample_topics` (resolved once, at first sight) and `catch_up_topic`. The `post` row's summary now spells `say[:<topic>]` (read against `bridge::resolve_to` and `fabric::cmd_post`'s `valid_topic` check, which refuses a malformed topic at the post rather than retiring a record `unroutable` a publish later) and its broadcast paragraph was CORRECTED as well as extended: it said `task`/`ask` are demoted, and `bridge::DEMOTE_UNLESS_ACCEPTED` is `[\"task\", \"control\"]` — `ask` is not in it and never was, so the sentence now names task/control, read against `classify_kind`. 2026-09-22 (round 23 review) CORRECTION OF THE PREVIOUS CLAUSE: no committed `post` row ever said `task`/`ask` are demoted — `git show origin/main:crates/aterm-types/src/control_verbs.rs` and the item-1 commit both name `ask` only under kinds, `--wait` and `dl=`. The sentence that was corrected existed only in this round's uncommitted draft, so the clause above recorded a read of a defect that shipped help never had; what IS true is that the row now says task/control, read against `DEMOTE_UNLESS_ACCEPTED`. The `subscribe` row gained `mail:topic=<t>`, read against `subscribe::parse_mail_params` and `MailFilter::wants`. SHORT_CATALOG_MAX_BYTES was RAISED from 10 240 to 10 496 with the measurement in its doc (10 358 measured by `help_short_form_is_bounded_and_is_the_summary_catalog`, 118 over: 109 for the new row plus 9 for the `post` summary), which is the raise the previous paragraph predicted. Golden pair regenerated in order (types, then gui). No claim contradicted; 2026-09-22 (round-23 review) read of the `topic` row's `serial=<n>` clause against `cmd_topic` and `bridge::sample_topics`, and of the SHORT_CATALOG_MAX_BYTES doc against the measurement: rows sum to 9 986 (the types-side test's number), the `topic` row is 110 and the `post` row grew 10, and only aterm-gui's wire test sees the 10 358. No claim contradicted; 2026-09-22 merge read (round 23 onto main): upstream's `privacy` claimants=/census= rows read against control_query's privacy handler, with round 23's topic/say/subscribe rows — consistent; 2026-09-22 read of the one reworded sentence in the `trail` verb's long help (shown by `gate help-surfaces --diff`): the row now ends `ribbon_retired= ribbon_followed=`, read against `TrailStatus::line_v2` (cursor_glow.rs, the two fields last, in that order); `ribbon_followed=` is `rk::Status::followed`, the cumulative count `Engine::follow_rows` adds and `Engine::reset` keeps; the follow search is `Witness::follow_runs` (dr in -1, +1, -2, +2 — \"one or two rows\", same glyph at the same column, as a block); a run carried off the caret row is set flowing by `Ribbon::flow_cohort` and `Cohort::swoosh_total` is `FLOW_TOTAL_S` = 1.10 + 0.24 = 1.34 s from `Cohort::clock`; the `ribbon_retired=` melt is `RETIRE_MELT_S` = 0.12 s; merged with origin/main's round-22 reads of this file on 2026-09-22 by the rainbow-ribbon integration (both sides' prose kept; the hash is the merged file's); 2026-09-22 read of the tone and trail rows' new audibility prose — the seam= verdict and its seven witnesses, audio= as the audio worker's state word, reopens_left=, and trail status's sound_seam= — against sound_seam.rs's SeamReason table (the nine reasons the help names are exactly the nine it spells), tone_infer::AudioHost::label over trail_audio::HostState (eight states; `reopening` was printed on the wire but missing from the help's enumeration, fixed in the commit that re-recorded this row) and control.rs's tone_status_help_enumerates_exactly_the_keys_the_row_emits (set equality both ways between the help and the emitted row); merged with origin/main's round-23 read of this file, upstream's row kept and this read appended; 2026-09-22 merge read (round 24 + v0.91.0): the topic row without serial and the `fabric retire` row read against the handlers — consistent; 2026-09-22 (round-24 review fix, commit A) read of the `subscribe` row's events and sessions clauses against subscribe.rs `drain_timeline_events` (the `GAP <local> events-dropped=<n>` it emits when the watermark is below the timeline's retained low) and `drain_instance_events` (`EVENT * fabric-retire <sid>` for `RosterChange::RetireRequested`). No claim contradicted; 2026-09-22 (round-24 review fix, commit C) read of the `status` synopsis against fabric.rs (five `fabric=` states, `stale` derived) and of the `subscribe` summary against control.rs's grammar (`@*` accepted). No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the `status` (stamp) and `spawn` (identity=) help against control_query.rs `screen_stamp` and agent_identity.rs `ensure_in`: no hook carries the stamp, no hook block is carried into an identity. No claim contradicted; 2026-09-22 read of the `path=<frozen|live>` sentences added to the `sessions` summary/detail and to the `status` Reply grammar against control_session.rs::sessions_lines (last token, after identity=) and session_status.rs's two status format strings (after story=, before the seq=/hash= stamp, which assert_stamp_rides_last keeps last); the sessions summary lost its `rows:` word to stay under SUMMARY_MAX_CHARS (summaries_fit_the_short_catalog_budget); both help goldens regenerated as a pair; 2026-09-23 (p2/gui review) read of the `appnotice` long help: marker-shaped toolchain text is taken as the marker is (an `appstatus` ledger entry, no row) and replies `OK recorded`, against `App::take_app_notice` and `control_query::cmd_appnotice`; any other text an Info row held 30 s (HOLD_NOTICE) then kept in the ledger, replying `OK posted`; the summary line is unchanged; 2026-09-23 read of the `appnotice` row design §10.5 H7 re-worded (`--diff`: the short help, the long help and the comment above the row) — against cmd_appnotice (control_query.rs: only the `OK recorded` arm) and take_app_notice (lib.rs: the marker-shaped toolchain texts taken as their markers, every other text a `Hold::LogOnly` record on the named lane, never a row), and against `appstatus`, which lists those records as finished activities; the regenerated goldens help_catalog_full.txt and ctl_help_full.txt carry the same words; no claim contradicted; 2026-09-24 integration read: the combined control help retains the Fabric sessions status and who fields from this branch and the remote message and appnotice rows; the merged GUI handlers and both full-catalog fixtures were checked together. Consistent",
    ),
    (
        "crates/aterm-verify/src/cli.rs",
        "077c4e797c0568cc",
        "2026-09-23",
        "re-read on 2026-09-23: the new `--disk-floor <GiB>` entry in the usage text and the \
         `disk_floor_gib` / `DiskFloorNeedsGib` docs, read against cli::parse (both spellings; \
         a missing, empty, signed, fractional, suffixed or overflowing value is the usage \
         error, pinned by the_disk_floor_is_whole_gib_or_a_usage_error), main.rs (the value \
         reaches Ctx::with_disk_floor after the pinned child facts) and lib.rs run() (the \
         `verify: disk …` line prints the floor in force, so a moved floor is on the record). \
         Its claim that lowering the floor cannot make a receipt lie was read against \
         exec::Run::environment_failure and ladder::Report::child_could_not_run: a run that \
         does run out of space is COULD NOT RUN, which writes no passing receipt. \
         Earlier: re-read on 2026-09-12 against the code that moved under it since the roster was minted, by the 2026-09-12 drift sweep (lane small-2); the 45-minute ceiling was two raises stale and is now DERIVED from DEFAULT_CHILD_CEILING; 2026-09-13 read (2026-09-14 UTC) of the prose b734f1069 added (shown by `xtask gate help-surfaces --diff`, matched at 56f7c5de5) — the --in-place flag, its usage example and help paragraph, the module doc, Args.in_place and the new test's strings — against parse's --in-place arm (a bare flag; --in-place=yes is Unknown), main.rs choose_source (--in-place and --selftest run in place, and so does a root that is not a git checkout, with a header note; otherwise snapshot::prepare at ATERM_VERIFY_SNAPSHOT or default_root's <root>-verify.noindex), snapshot::sync (HEAD, git diff --binary HEAD, untracked non-ignored files, flag-hidden edits), identity::Tripwire::check (source only for a git tree, toolchain always) and exec::Timings (a side channel; the ladder bytes are the same with or without it); ONE claim was false and was fixed in the commit before this row: the help said every run except --selftest verifies a snapshot and that a moved source tree always makes the run COULD NOT RUN, while a root that is not a git checkout runs in place with no source tripwire — the paragraph now scopes both to a git checkout; 2026-09-14 re-read of the module doc and the --in-place help paragraph against main.rs choose_source, identity::Tripwire::check, lib.rs run's tripped-check arm, verdict::verdict's failure-before-could-not-run order and exec::Timings::create; the mid-run move, snapshot scope and timings claims were corrected on 2026-09-14: the module doc said a run verifies a snapshot by default with no git-checkout scope (now scoped, with --selftest and a non-git root running in place); the help said a moved compiler or source tree makes the run COULD NOT RUN unconditionally, where it stops every stage not yet started and adds a source identity COULD NOT RUN row, so a run with nothing failed ends COULD NOT RUN (exit 3) and one with a FAILED stage keeps FAIL (exit 1); and it said the timings file gets per-child rows, where Timings::create truncates it each run and writes a row per child plus one (stage) row per stage; 2026-09-17 read of the `--changed` entry's pre-push clause (the only prose that moved) against `.githooks/pre-push` as it now stands and against crates/aterm-verify/src/receipt.rs: the hook runs no tier at all and refuses a push whose commit carries no passing receipt, which is what the clause now says; the four flag entries, the exit codes and the env list were re-read against cli::parse and Args and no claim contradicted the code, by lane a-gatetruth",
    ),
    (
        // Linux rebase read, 2026-09-23: with_pinned_child_facts supplies the
        // full commit beside the display stamp and dev count, all-or-none.
        // The clean/dirty Git fixture tests that identity; no receipt rule moves.
        "crates/aterm-verify/src/lib.rs",
        "96b7a5c4fdbf52d9",
        "2026-09-23",
        "re-read on 2026-09-23: the only prose that moved is `Ctx::disk_floor` and \
         `with_disk_floor`, which now say a run takes disk::FLOOR_BYTES unless \
         `--disk-floor` moved it and that the gate's fixture tests use 0 — read against \
         main.rs (the one caller outside tests), gate_contract.rs's ctx_with and \
         environment_contract.rs's ctx (both now `.with_disk_floor(0)`, after 23 fixture \
         tests refused at the preflight inside a merge-contract run at 17.6 GiB free) and \
         the three preflight laws, which set u64::MAX or 0 themselves. \
         Earlier: re-read on 2026-09-22 after the conformance-release lane converged on ONE implementation. This row used to describe `Ctx::conformance_bin`, a `OnceLock` one stage set and another read; it is DELETED, and so are `Scope::includes_conformance_artifact` and the three `*_BIN` environment variables that carried the path. The saving is unchanged and now belongs entirely to `StageId::ConformanceRelease`, which builds the release `aterm` at t0 in `target/conformance-release` — the same lane directory and the same argv `crates/aterm-conformance/tests/support/mod.rs` uses, so the suites' own call is a cargo fingerprint no-op and nothing has to be handed anywhere. Two implementations of one idea met in a rebase and the simpler one was kept; the duplicate was deleted rather than renamed. Nothing else in this file's prose changed. Earlier: re-read on 2026-09-21 (third read of the day, after the adversarial review corrected two claims here): `Ctx::child_env_add`'s doc now names the third thing every child is given — the [`CHILD_ENV`] constants seeded at construction, beside the git stamp and test concurrency that `with_pinned_child_facts` appends — and says builders APPEND, read against `Ctx::new` (which seeds the vector from CHILD_ENV) and `with_pinned_child_facts` (which pushes onto it); the two docs on the same value said `two facts` and `Three of them today` one file apart, and the wider sentence was the wrong one. `CHILD_ENV`'s reason no longer says `a snapshot nothing edits between runs`, which was false for `--changed` (snapshot.rs re-syncs an edited tree on every run of that tier) — it now states the pin as a trade, scoped to the merge-contract tiers, read against `snapshot::prepare`. Its measurements drop the stage name and the `~50 min` figure: no ladder from 2026-09-20 survives to say which stage died, and the only receipt of that day (`0a45a7446`) was written 32 min after its commit, so `~50 min` was not measured anywhere — what remains is the 36 GB -> 55 GB growth and the 23 GB snapshot, both re-measured. Also in this file and read the same way: `with_pinned_child_facts` (resolves the git stamp from `self.root` and the test-thread count, pushes both onto `child_env_add` and one note per fact; its `both or neither` sentence matches the `if let (Some, Some)` guard, and the defect it names is the one `crates/aterm-gui/build.rs` records at `git_watch_paths`); HOOK_CLAIM, which says the hook REFUSES when it cannot judge rather than admitting, read against .githooks/pre-push's `cannot_judge` arms and measured by tests/push_gate.rs (23 laws — the newest pins that git's chatter on a SUCCEEDING command, under any GIT_TRACE variable, is not the repository root, which the hook read as one for a day); `Ctx::disk_floor`/`with_disk_floor`; run()'s `verify: disk …` line and `disk preflight` COULD NOT RUN row; and write_receipt's paragraph on what a COULD-NOT-RUN run leaves — read against exec::ExecEnv::add_env, disk.rs (read_free/parse_df anchored on the Capacity column/decide/remedy) and tests/environment_contract.rs's three preflight laws. Earlier: re-read on 2026-09-18: HOOK_CLAIM (the sentence the gate prints to every clone) now names the three pushes the receipt hook admits without a receipt of their own — a tag, the release cutter's claim over origin's tip touching only CHANGELOG.md and RELEASES.ledger, and a clean automatic merge of a receipted commit onto origin's tip whose tree is byte-equal to git's own merge of its parents — beside the ATERM_PUSH_NO_GATE=1 exception; read against .githooks/pre-push (`release_claim`, `gated_merge`, `receipt_passes`, the refs/tags/* arm) and measured by tests/push_gate.rs, which runs the shipped hook for each admission and each refusal the sentence implies",
    ),
    (
        "crates/aterm-winsign/src/lib.rs",
        "c64c32cb488903df",
        "2026-09-16",
        "NEW SURFACE, never rostered: `cargo winsign` landed at b83bb56b0 and grew its Trusted Signing lane at a7ad1ad0b. USAGE read in full against parse_flags and run_inner on 2026-09-15: the four advertised verbs (sign/verify/doctor/help — plus `status`, an undocumented alias of verify that the usage block does not claim), `--credentials <file>` against values_from_profile's `winsign_<key> = \"…\"` grammar, the other nine option spellings against KEYS one for one, and the stated precedence flag > ATERM_WINSIGN_<KEY> > profile against resolve_config's first-wins source order; `--lane` against the inference arms (both configured and no --lane is refused by name), `--thumbprint` against the 40-hex-digit gate and sign_args's `/sha1` (the module doc's `never /f <pfx> /p <password>` holds — no lane takes a password), `--timestamp` against default_timestamp (acs.microsoft.com for Trusted Signing, digicert for a store cert), `--signtool` against find_signtool (PATH, then Windows Kits 10 bin newest SDK first) and `--dlib` against find_dlib (newest microsoft.trusted.signing.client under the NuGet cache); the tier prose against tier_from/pins::anchor_active, judge (signed, verifies under `/pa` — verify_args passes it — leaf `Issued to:` equal to the anchor, timestamp required) and report_file's readback after every sign; doctor's `Exit 0 when a sign would run` against its ok-tracking (0 or 2). ONE DEFECT FIXED: the Inactive bullet claimed `Verification never fails a build in this state`, but report_file returns false for an unsigned or untrusted-root exe whatever the tier, so run exits 1 — pinned by verify_reports_an_untrusted_chain_with_exit_one_even_when_inactive and thrown on by apps/aterm-win/build.ps1 -Sign; the bullet now says the TIER refuses nothing while the exit code still reports. ONE DEFECT REPORTED, NOT FIXED HERE (code, not doc, and aterm-winsign is another lane's file this hour): the EXIT block promises `1 … or signing failed`, but a signtool `sign` that runs and exits non-zero leaves run_inner as Err and run() maps every Err to 2, so a real signing failure exits 2 — the code should return 1 there. THAT DEFECT IS NOW FIXED in the same round: a signtool that runs and exits non-zero returns `Ok(1)` after printing its output, so exit 1 means the signature failed and exit 2 stays with \"nothing to sign with\" — pinned both ways by `a_signtool_that_ran_and_refused_is_exit_one_not_two`, whose twin (return `Err` there again) reddens exactly that test. Re-read 2026-09-16 after that change and after the lane that routed every Windows-layout path through `win_join`: USAGE is unchanged by both, the module doc's Inactive bullet is the one edited above, and the EXIT block now matches its handler; SECOND ROW FOR THIS PATH, MERGED HERE VERBATIM 2026-09-16 (it was the R6 DUPLICATE): first read of the crate, for the help-surfaces gate round of 2026-09-15 (2026-09-16 UTC): USAGE and the module doc read against the parser and the verbs — the four synopsis lines against run_inner's verb match (sign, verify, doctor, help; `status` an undocumented alias of verify; a bare argv and --help both reach help), the OPTIONS header against resolve_config(&[flags, env, profile]) (first source wins, so flag > ATERM_WINSIGN_<KEY> > the profile's winsign_<key>) with KEYS, values_from_env and values_from_profile (an unknown winsign_ key is refused, a non-winsign_ key skipped), --lane against resolve_config (a lane is inferred when only one is configured and refused by name when both are), --thumbprint against its 40-hex-digit SHA-1 check, --timestamp against default_timestamp (acs.microsoft.com for Trusted Signing, digicert for a store certificate), --signtool against find_signtool (configured, then PATH, then the newest Windows Kits 10 bin/<version>/<arch>), --dlib against find_dlib (configured, else the newest microsoft.trusted.signing.client under NUGET_PACKAGES or the home .nuget cache), and the two tier paragraphs against tier_from/pins::anchor_active and judge (Active demands signed + verified under /pa + Issued to exactly the anchored publisher + timestamped; Inactive judges every report Ok). THREE SLIPS FIXED in the commit that added this row: (1) the EXIT block said `1 a signature is wrong for the tier, or signing failed`, where a non-zero `signtool sign` returns Err from run_inner and run maps EVERY Err to 2 — exit 1 is only report_file's bad verdict — so EXIT now reads 1 = a verdict was read and it is bad, 2 = no verdict (nothing to sign with, a missing tool, or signtool itself failing); (2) the Tier::Inactive bullet said `Verification never fails a build in this state`, where report_file returns false for an unsigned or untrusted-chain exe in EVERY tier and the crate's own verify_reports_an_untrusted_chain_with_exit_one_even_when_inactive pins that exit 1 — the bullet now says the tier refuses nothing while the Code-Integrity verdict still exits 1; and (3) the `only RealHost touches the machine` sentence was made precise in the same commit: `sign` unlinks its own Trusted Signing metadata file through std::fs, outside the Host seam. 2026-09-16 re-read after the review of that commit, which found slip (2) only half fixed: the SAME contradicted claim still stood in two other pieces of this file's prose — the `Tier::Inactive` variant doc (`No anchor: signing is optional and verification is advisory`) and, user-visibly, the `impl Display for Tier` string that doctor, sign and report_file's UNSIGNED line all print (`signing optional, verification advisory`) — both read again against judge (Inactive is Ok for every report), report_file (false whenever !verified, in EVERY tier) and verify_reports_an_untrusted_chain_with_exit_one_even_when_inactive, and both now say the tier itself refuses nothing while an unsigned or untrusted chain is still reported and exits 1; verification in this tier is not advisory and no prose in the crate says it is any more. The EXIT block was read in the same pass against parse_flags, run_inner, run, resolve_config and doctor: `run` maps EVERY Err to 2, so the 2 line now names the argument and usage refusals it had left out (an unknown verb or option, a flag with no value, no exe named, a path that is not a file, a credentials profile that will not read, and a lane that does not resolve — resolve_config's both-lanes-configured, incomplete-triple, unknown-lane and not-40-hex-digits arms) alongside the missing tool and signtool's own non-zero exit; and the 0 line, which said only `signed and judged good`, now covers what actually returns 0 — sign/verify on a verdict the tier accepts, doctor's READY (`if ok { 0 } else { 2 }`, so NOT READY is a 2) and help (run_inner returns Ok(0) after printing USAGE); 2026-09-15 re-read after the same branch's follow-up commit edited the very prose the first row recorded (the row had been taken from an uncommitted tree): the reworded Tier::Inactive Display string and variant doc, and the rewritten EXIT block, read against run (every Err from run_inner becomes 2), report_file (judge accepts under Inactive, yet an unverified report still answers false, so verify/sign exit 1 in that tier — pinned by verify_reports_an_untrusted_chain_with_exit_one_even_when_inactive), doctor's READY/NOT READY arms, and help's Ok(0); no claim contradicted the code; 2026-09-16 (this row): the path carried TWO rows — R6 DUPLICATE — because two lanes read this new crate in the same round; they are MERGED here, both notes kept verbatim above, and the prose re-read whole against the handlers. Read the USAGE block and the module doc against parse_flags, run_inner, run, report_file, judge, doctor, find_signtool, find_dlib, find_dotnet_runtime and win_join, and the fake-host tests against the code they pin. THREE FALSE CLAIMS FOUND AND FIXED. (1) THE EXIT BLOCK, and it was a live contradiction between the two merged rows: the second row's rewrite put `signtool itself failing` under exit 2 on the reasoning that `run` maps every `Err` to 2, but the first row's own lane had already changed run_inner so a `sign` whose signtool RAN and exited non-zero returns `Ok(1)` after printing its output — pinned by a_signtool_that_ran_and_refused_is_exit_one_not_two, which asserts 1 for a refused credential and 2 for an absent SDK. EXIT now reads 1 = a bad verdict OR a signtool that ran and refused, 2 = nothing signed and no verdict read (parse_flags' unknown verb/option and missing value, the empty file list, `not a file`, an unreadable credentials profile, resolve_config's unresolvable lane, find_signtool/find_dlib/find_dotnet_runtime misses, a Host::run that cannot launch the tool, and doctor's `if ok { 0 } else { 2 }`); the test doc and the in-code comment that QUOTE those two USAGE lines were re-quoted to match. (2) win_path's doc claimed `Production builds these paths with PathBuf::join ... and must stay PathBuf::join`, which b29cf13ad falsified after 8e2487739 wrote it: every Windows tool layout goes through win_join now (Windows' `\\` on every build host) and Path::join survives only in find_on_path's PATH entries and the temp metadata file, both host-native on purpose — the doc now says that. (3) USAGE's closing line still sourced the identity from `Azure Trusted Signing` after 734f9caaa renamed the service everywhere else in the crate (module doc, the `sign` lane line, doctor's lane line) and in apps/aterm-win/SIGNING.md: it now reads Azure Artifact Signing (formerly Trusted Signing), the spelling the rest of the file prints. Everything else re-read and TRUE: Tier::Inactive's `the tier refuses nothing` against judge's `Tier::Inactive => Ok(())` and its `still reported and exits 1` against report_file (false whenever `!verified`, in every tier); DLIB_PACKAGES `newest lineage first` (microsoft.artifactsigning.client before microsoft.trusted.signing.client) and find_dlib's `newest version of the newest lineage` against the package loop with versions_newest_first, pinned by the_renamed_package_lineage_wins_over_the_unlisted_one; the `one exception` to the RealHost seam against the literal `std::fs::remove_file(m)` on the metadata file; win_path's separator/collapse/trailing rules and its NOT-case-folded note against its body",
    ),
    (
        "crates/atpkg-keys/src/main.rs",
        "a491ce402d28ccc7",
        "2026-09-10",
        "read against its parser/dispatch by the 2026-09-10 sweep (aterm-D-keys-xtask-misc); findings fixed in the commit that added this row",
    ),
    (
        // Linux rebase read, 2026-09-23: list/doctor distinguish validated local
        // links from store builds (including vendor-direct builds), with no
        // invented registry identity. run_update_pass skips linked programs but
        // records broken links as faults; only a reached index earns its success
        // stamp. VERB_USAGE, dispatch and link/update grammar are unchanged.
        // Final rebase keeps repair's signed shim_env sidecar restoration.
        // relay_shims now preserves explicit links over retained store builds;
        // checked directory/marker failures name the refusal and leave shims
        // alone. Read its new messages against these branches and the actual
        // repair_store regressions (executable/live, malformed, oversized and
        // symlinked); the valid fixture remains in live_dev_links after repair.
        "crates/atpkg/src/cli.rs",
        "139442af429366ef",
        "2026-09-24",
        "2026-09-24 read of the two added macOS activation-notice clauses: after acquiring the store lock, main_entry snapshots the live Claude twin and prepares its dedicated notice directory; after run_verb returns under that lock, note_if_changed compares the final live twin and atomically publishes only a changed-target wake, while a failed hint leaves the minute host sweep authoritative (activation_notice.rs and upgrade_wake.rs). No CLI help string changed; re-read on 2026-09-13 by drift sweep of 2026-09-13 (aterm:atpkg); `repair`'s tracked-install sentence covered one of the record's two causes and is fixed in that commit; 2026-09-14 drift sweep (lane atpkg-and-verify, 38 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 read of the arm-1c prose 622a87275 added (index_program_shadowed_by_unrelated_line's doc and format strings, its test's doc and literals; the other commit since, 4fe5ed5e7, moved only code — the channel_for lookups) against which_line (arms 1 and 1b return on a managed shim or a live bundle, so 1c is the not-installed case; it also needs no pending stub, an index program with no system= and no extra that is not an agent program, and system_binary_on_path), state::UNAVAILABLE_PREFIX/BLOCKED_PREFIX/NEEDS_ADMIN_PREFIX (the three reasons the doc names, quoted as-is), not_installed_fix's `aterm pkg install` spelling and state::system; the new test runs green; one slip fixed: the test doc quoted the old arm-2 answer as `/usr/bin/trust — system — not managed by aterm`, but state::system renders `system: <path> — not managed by aterm`, so the quote now reads `trust → /usr/bin/trust — system: /usr/bin/trust — not managed by aterm` as the fixing commit itself recorded it; 2026-09-14 read of the `machine` prose 2e3fa303d added (carried by the merge of origin/main; `--diff` against this row's hash shows exactly that commit's lines, and against origin/main's recorded row shows them plus the arm-1c `which` prose this side read at 985cd1c72): VERB_USAGE's machine entry, the strings cmd_machine and apply_machine_settings print, apply_machine_settings' rewritten doc, machine_home_mismatch/_between's docs, cmd_machine's doc, and the docs of machine_settings_apply_before_anything_can_fail_in_every_pass, a_redirected_home_is_a_synthetic_machine and machine_grammar_is_exact — against the handlers themselves, the three call sites at the top of cmd_update_all_code/cmd_install_default_set_code/cmd_seed (and the pre-2e3fa303d file, where update's call sat inside `if failures == 0` at the pass's end, as the test doc says), doctor.rs's Spotlight ok/warn sentences and its doctor_line call, machine.rs apply_universal_control, noindex.rs SUPPORTED/Scan::counts/apply_under, platform/unix.rs account_home (getpwuid_r), and aterm-gui's spawn_pkg_update_check + PackagesRequest::InstallDefaultSet (the three passes the window runs); two slips fixed before this row: cmd_machine's doc said the bare read prints `the same two lines` doctor prints, where only the Universal Control line is doctor_line's and the Spotlight line is a count over the doctor's scan, not its remedy sentence; machine_home_mismatch's doc called the function pure where it reads $HOME and the passwd record — only machine_home_mismatch_between is; 2026-09-14 read of the prose the merge 516d330df carried (shown by `xtask gate help-surfaces --diff`: the dev-linked `which` arm 1b' and its doc, the repair tracked-install and re-seed lines, the linked-from-the-start `list` rows and their doc, the default_set_adopts doc, the NET_FAILED whitespace, and the which/list/adoption test docs and the codex-to-emacs fixture rename) against which_line's arm order and dev_linked_program_line, linkmode is_linked/linked_checkout/linked_bins, stub::is_agent_program, list_porcelain_lines/list_human_lines (the `-` build column, the linked_only entry counted as a program and a dev-link), default_set_adopts and its caller in the --default-set pass (activated, failures, !before.is_empty(), skipped_narrowed, remaining_missing), uninstall's `Re-adopt with aterm pkg install --default-set` line and clear_adoption, provenance::REMEDY, install.rs decide_tracked_stage (KeepRecorded and InProcessRecorded both write the record), lay.rs tracked_policy_of (unset is Allow) and stage_helper.rs plan_helper; two slips fixed before this row: the repair line said `the default, ATPKG_REFUSE_TRACKED_INSTALL=1 refuses instead`, which reads as if =1 were the default where tracked_policy_of makes unset Allow — it now says that is the default and =1 refuses instead; and the re-seed line said the launchd job runs `a clean copy of itself`, where plan_helper runs an untagged binary in place and copies only a tagged one — it now says so; 2026-09-14 (third audit lane, final merge): origin re-read this file after this lane's last read; the only prose since is this lane's `lock-acquired: ` marker (6d95caa50), already read against mutator_store_lock and read_seed_markers above; the union's hash is recorded at the merge; 2026-09-15: `machine` verb usage + cmd_machine/apply_machine_settings/machine_home* docs and the lock-refusal hint re-read against the code by a reader prompted to refute (an earlier cut claimed a window reader that did not yet exist; it exists now — aterm-gui parses `machine-state:`); MACHINE_VERDICT_PREFIX/MACHINE_NOT_APPLIED_PREFIX are the GUI's contract literals; 2026-09-15 audit integration read: MACHINE_APPLY_FAILED_PREFIX and record_universal_control_outcome preserve partial Spotlight changes while reporting a failed Universal Control write independently of exit status. Read against apply_machine_settings, the producer regression, and GUI machine_apply_stdout/machine_apply_completion, which deliver change events before a Failed command outcome. The existing success/revert and no-op messages retain their behavior; 2026-09-15 read at the merge of feat/round-15-receipts over main of the prose f3a5467ed moved with no row (shown by `xtask gate help-surfaces --diff`, matched at 0504383fa): cmd_help's `{}` line — against cmd_help (the title line, then aterm_types::identity::ORIGIN_LINE, then the usage line); no claim contradicted; landing of the erase pending-wrap fix, over f3a5467ed: read the new format literal in cmd_help, which prints the origin line under the atpkg title, against aterm_types::identity::ORIGIN_LINE and its parts test, the help, -h and --help dispatch in main_entry, the aterm pkg forwarding in crates/aterm/src/main.rs, the unchanged static and HOME-free doc comment, the identity module doc and CHANGELOG entry that name atpkg help, and the help tests in cli.rs and settings_action_completion.rs, which pin exit codes only; no claim contradicted the code; 2026-09-15 merge read of main (round 15) over origin/main 58bb2fc63: the merged file is byte-identical to origin/main's; 58bb2fc63 moved its prose after main's recorded read, so read here: THE FAULT LIFT doc and its test strings (the aborted:/error: lift, the three fault fixtures, the dropped 'error: x' other-arm fixture) against up_to_date_row_needs_rewrite, doctor::is_problem_state's aborted:/error: prefixes, report_channel_apply's Aborted arm and the coherence-group abort format at cli.rs:7703; no slip found; 2026-09-15 read of the exec-root wiring on fix/tippy-view-shims (the only prose since 58bb2fc63 is this working tree's, shown by `xtask gate help-surfaces --diff`): reconcile_exec_roots' doc and its four format strings against compat::reconcile/Report (built lines only for Ensured::Built, which needs_root gates; routed = shims whose bytes differed from the render, routed or back to plain; swept = sweep's dot debris, non-directories at numeric names and roots whose build is gone or needs none; errors to eprintln) and its five call sites (cmd_update_all_code after print_gc_abstentions, install_default_set_inner after reconcile_aliases, cmd_seed after reassert_rustup_seam, cmd_rollback's trust branch, repair_store at Deep); the GUI claim against aterm-gui run_pass (stdout through read_seed_markers, which acts only on marker lines; stderr kept whole by read_pass_stderr); run_repair's steps 3/4 and exit-1 sentence and repair_store's doc and its new repair line against repair_store (reconcile after relay_shims, exec_roots_ok folded into the SUCCESS condition); remove_exec_roots' doc and warn line against compat::remove_all, ops::uninstall's compat::remove_program and gc's sweep (which walks only compat/trust, hence `any trust exec root left in it`); print_gc_sweeps' two new lines against GcReport.swept_exec_roots/exec_root_errors; the new test docs and literals against seed_affected_trust, repair_store and the call sites they pin. Four slips fixed before this row: the built line said a `separate copy` where needs_root also answers for a non-copy file; both removed/swept lines said `its build is gone or no longer needs one` over sweep's debris and non-directory arms; the repair line said a root `could not be laid` where any reconcile error (a listing, a removal, a render, a lay) reaches it; the remove_exec_roots line said `aterm pkg gc` retries, where gc sweeps only compat/trust roots, not compat/ itself; and the doc's first cut claimed the GUI discards a pass's stdout, where run_pass pipes it for markers; 2026-09-15 read of the review fixes on fix/tippy-view-shims (shown by `xtask gate help-surfaces --diff`, matched at 0e70d4b94; only this working tree's edits since): the built line's format literal became `{}` over compat::laid_line, whose text is the removed literal byte for byte, printed for each Report.built as before and now also by activate::install_tools_env and flow::rollback_member on Ensured::Built; the_pass_reconcile_routes_an_up_to_date_affected_install's rewritten doc and its new literals against the five call sites: cmd_update_all_code's call sits after print_gc_abstentions and before `if failures == 0 {` (the pass's earlier returns are the disabled manager, no layout, the empty-update line and an index that fails to resolve — a whole-pass failure; a failed program only adds to `failures`, so `a pass with one failed program still heals` holds), install_default_set_inner's after reconcile_aliases and before reconcile_shadowed, cmd_seed's after reassert_rustup_seam and before `let Some(seed_dir) = crate::bundled_seed_dir() else {` whose else-arm returns after report_seedless_posture, cmd_rollback's inside the SEAM_PROGRAM branch before its ExitCode::SUCCESS, repair_store's after relay_shims and before `repair: done`; `write the account's real defaults or rc wiring from inside a test process` against apply_machine_settings at the top of cmd_update_all_code and cmd_seed and hooks::refresh in cmd_update_all_code and run_repair; the mutation claim against two probes run on this branch (the seed call moved below the seedless return, the pass's call moved inside `if failures == 0`), each of which fails the test now (exit 101). One slip fixed before this row: the first cut said most launches return at the seedless return, where cmd_seed's own comment says most launches return below it without installing — only a Mac with no bundled seed returns there; 2026-09-15 read after rebasing fix/tippy-view-shims onto d348f3f51, whose prose moved the hash: settle_net_lane_index's doc (on an EMPTY store the apply lane never runs, against `if !installed.is_empty()`; the row it writes against the apply lane's Err arm, same `error: {e}` state and `update failed: {e}` outcome; the retire against clear_status_row; the apply-resolved arm that neither un-resolves nor re-opens), the `index_resolved` comment (the apply lane's `return 1` on Err; the set-completion lane returns an outcome), the failed-pass exit's `if index_resolved` comment, DefaultSetOutcome.index_error's doc (install_default_set_inner's two early exits, resolve_verified_index Err and channel_for None as FlowError::NoChannel, are the only constructions carrying Some; the pass-end one carries None; the wrappers construct none) and the new test's comments; 19019c7aa's `current_triple(),` moved only code; the exec-root claims above re-checked on the rebased tree (reconcile_exec_roots still after print_gc_abstentions and before `if failures == 0`). One slip fixed on this branch: record_success's doc said a stamping pass finished without a failure and was called only at success exits, against the failed-pass exit's stamp (since 2026-09-14, now guarded by index_resolved); it now names update's clean exit, its resolved failed-pass exit and install --default-set's zero-failure exits; 2026-09-15 read, on rebasing fix/tippy-view-shims over 52469ede9, b7bd41d61 and 616fc8011 (shown by `xtask gate help-surfaces --diff` on origin/main f53f94ad0, whose own gate refused this row: the three commits recorded no read): refuse_seed_for_disk's doc against its body (the `*toolset*` `blocked: insufficient disk space` row with `needs {} free`, `have = None` is no refusal through is_none_or, clear_adoption only when adopted_by_this_run, the marker left to the caller) and cmd_seed's order (the `!manager_enabled()` return with both `lane skipped (fail-closed)` lines sits before `adopted_by_this_run = !adopted(&layout)` and record_adoption, and the disk gate calls refuse_seed_for_disk with that flag); seedless_serves_us's doc against its body and report_seedless_posture (the `!crate::active_builds(layout).is_empty()` argument answered before the deferred closure builds resolve_fetcher and runs resolve_verified_index and seed_serviceable) and the three new test docs and literals against the code they pin; this branch's reconcile_exec_roots call in cmd_seed now sits behind the disabled-manager return, so a disabled manager lays no exec root either, which is the kill switch's rule; no claim contradicted the code; 2026-09-15 (2026-09-16 UTC) read of the prose four commits moved since the recorded read (shown by `xtask gate help-surfaces --diff`, matched at ca6a3a6a3) — cmd_verify_pkg's `FAIL: machine {} signed this manifest, but no client can read it` line and the five reasons pkg_post_verify_refusal spells, uninstall_and_retire's THE INTENT IS WRITTEN BEFORE THE DELETE block and retirement_name_gate's ROW DELETION IS A NEW CAPABILITY block, the reusable_index / recover_missing_roots / install_default_set(_with_path) / cmd_update_all_code one-verify-select docs, and the new tests' docs and literals — against manifest.rs parse_pkg (only schema > SUPPORTED_SCHEMA = 2 is Reject::Schema, so `newer than this build reads` holds), validate_rows (kind = vendor-fetch is RetiredKind; a github-release or https row without asset/sha256, and a pkg row without url/sha256, are Malformed) and parse_toml (not UTF-8, not TOML, a missing or duplicated field is Malformed) — the malformed sentence names each of those; cli.rs uninstall_and_retire (retirement_name_gate BEFORE record_removed/clear_optin/clear_retired and crate::uninstall, so a refused name mints no marker) and ops.rs uninstall_name_shape (empty, `.`, `..`, a separator or NUL — exactly the set the doc says it rejects); and reusable_index's flow::index_is_fresh re-gate. ONE SLIP FIXED in the commit that updated this row: pkg_post_verify_refusal's doc said Reject::ShimEnv and Reject::RetiredKind `each promise in their own doc that this verb names the fix`, where RetiredKind's doc promises the authoring machine's own `atpkg install` names it; the doc now states each promise as it is actually written; 2026-09-16 read of the prose the merge b3def0952 carried in (shown by `xtask gate help-surfaces --diff`, matched at 8ff8f99f4): `__pending`'s `[args...]` usage line and pending_passthrough's doc against cmd_pending's dispatch (args.get(2..)), stub::is_agent_program + vendor::system_binary_on_path and run_pending's exec arm (one stderr line, argv verbatim, 126 if the exec fails, 127 off Unix); managed_state_of's doc against its arms with state::managed/managed_pin; record_index_freshness against flow::last_resolve and status::stamp_index_freshness (the `(index from cache - why)` suffix, added once); GROUP_ABORTED_MARKER/TRACKED_INSTALL_MARKER and clip_cause against report_channel_apply's Aborted arm, install.rs's stage record and the 240-char first-line clip; apply_machine_settings' parent_is_gone early return; print_provenance_carriers against provenance::tagged_files_in/_under over active builds (macOS only, one line per pass); the group-abort `- {cause}` strings and the `N program(s) failed` aggregate; and the two new test docs against clip_cause and pending_passthrough. THREE FALSE CLAIMS FOUND AND FIXED, all three of them doc blocks the merge ORPHANED by inserting a new item between a doc comment and the item it documents. (a) run_pending's whole doc block sat on managed_state_of, so a state-string helper was documented as `the pending verb's body ... ALWAYS exits 127` and run_pending had no doc at all; the block is back on run_pending, and its `ALWAYS exits 127` — false since the passthrough arm landed — now names the exception (an agent program's foreign copy is exec'ed in this process's place; 126 if that exec fails; 127 off Unix). (b) The `STABLE stdout markers` group header sat on GROUP_ABORTED_MARKER, and its enumeration of the markers the ledger leaves unclassified named LOCK_WAITING/LOCK_ACQUIRED/SHADOWED/MANAGED_CURRENT/MACHINE_SETTINGS but not the two new ones — neither of which is in announcement::MARKERS, so `note` classifies neither; the header is back on SEED_STARTING_MARKER and names them, and GROUP_ABORTED's own `A TERMINAL` now says which kind (a terminal ROW on the window's toolchain lane via toolchain_failed, not an answer in the ledger). (c) print_managed_current's doc block sat on print_provenance_carriers, leaving print_managed_current undocumented; it is back. Nothing else contradicted the code; 2026-09-16 read of what 56242acc4 added: the fourth input `config_unreadable` and its precedence, against the gate itself — `!declined && !config_unreadable && (auto_install || adopted)` (cli.rs) — so declined still outranks everything, an unreadable `[packages]` outranks BOTH adopted and auto_install, and the doc's reason (the lane is narrowed by `[packages].exclude`, which an unreadable table cannot supply) matches `config::PackagesConfig::unreadable_table`'s `unreadable: true` default-everything-else shape; the two new test-name strings describe exactly those two arms 2026-09-15 depth read: cmd_machine's new `not walked:` line re-read against noindex::skipped_names (built from SKIP_DIRS, dot-names filtered) and noindex::DOCTOR_DEPTH, which the same commit raised from 3 to 5 with Budget::DOCTOR raised to 60 000 / 3 s; measured on the developer's home, where depth 3 saw 1 exposed target dir and depth 5 sees 5, and doctor.rs's depth rationale now defers to the constant; the count sentence above it is unchanged and still says what an apply can hide. 2026-09-15 lock-ordering read: the edge's new apply_machine_settings_once() call and the reworded lock-refusal hint re-read against main_entry (the call sits above `let _store_lock = ...`, gated by verb_applies_machine_settings = install|seed|update), apply_machine_settings_once's doc (a OnceLock, so the bodies replay what the edge did rather than walking $HOME twice) and cmd_machine, which calls the RAW apply so an explicit gesture always re-reads; the hint now says the settings were applied above the refusal and still names `aterm pkg machine apply` as the re-apply door, which crates/atpkg/tests/store_lock_wait.rs pins on stderr while its new no_progress_marker helper pins that stdout carries only host-work lines, never a progress marker. 2026-09-16 audit-3 read: the machine read's new no-home line, the `nothing to apply from what was seen` verdict (scan_complete now starts false and is set only when a home was walked), machine_refusal's unreadable-config sentence, and the Spotlight `machine settings failed —` line re-read against noindex::Applied::Failed (the new variant the rollback/plan/link sites return and Applied::failure() reports), render_applied's FAILED row and its `, N failed` summary tail, config::parse_machine's unreadable flag, machine::MachineState::config_unreadable and machine_state_line's appended `config=unreadable` key; every other sentence in the verb is unchanged.; 2026-09-16 read of the prose that moved after this row was recorded, in two changes. (1) The peer repair-arity fix 5d9cdd94e, which landed without re-recording: its dispatch-arm comment (a store MUTATOR that took any argument and ignored it), the paragraph added to zero_arity's doc, and the prose of the assertions its test gained — read against the arm itself (repair now calls zero_arity(&args[1..], repair) and reaches run_repair only when no stray argument was given), against zero_arity (one stray argument prints `atpkg repair: unknown argument ... this verb takes none` plus usage_of(repair) and exits 2) and against run_repair (hooks::refresh_rewiring_rc, the `repair: shell integration rewritten (~/.aterm/shell.d + rc wiring)` line, lay_reroute_stubs, reassert_rustup_seam, then repair_store's relay_shims) — every claim held. (2) This change's own prose: cmd_install_argv's doc (the elevation flag belongs to the ONE-PROGRAM form alone), the new parse_install_argv doc, its whole-set refusal comment, the three lines that refusal prints, and the new test's doc — read against cmd_install_elevated, whose --default-set branch returns into cmd_install_default_set() ABOVE the single set_elevation(door_elevation(explicit, stdin_is_tty())) call, against cmd_install_default_set_code (which reads no elevation at all), against crate::elevate (Deferred is the thread-local default, so the whole-set pass never elevates) and against the manual's aterm pkg install block, which has only ever documented --elevate on the one-program spelling; 2026-09-16 read at the landing of the winit notice fix, over f37f01ae8 and 5fdea3bfd: the __pending SKIPPED lines read against the Phase::Skipped arm (Sink::finished retires a skipped program from the queue and advances programs_done, and the bump file only reorders what is still queued, so nothing later in the pass installs it; the reason is the recorded row error, sanitized), and the uninstall --all lines against foreign_copy_note (system_path, installed_via_path), print_left_alone, and the removed.is_empty() branch that prints the removed-nothing summary only when no removal failed and still names what was left alone; no claim contradicted the code; 2026-09-16 read of the pass's own `machine-state:` record (b8fab35f6 and this tree, shown by `xtask gate help-surfaces --diff`): the `atpkg: {MACHINE_STATE_MARKER}{}` literal print_machine_state_after_apply prints, the SpotlightCensus field docs, spotlight_census's and spotlight_after_apply's docs, print_machine_state_after_apply's doc, and the two new tests' docs and literals — against apply_machine_settings (census = Some(spotlight_after_apply(..)) inside the Spotlight half, print_machine_state_after_apply called after `machine_settings_line(&entries)` under `if crate::noindex::SUPPORTED && let Some(home)`, with census.or_else(|| (!entries.is_empty()).then(|| spotlight_census(home))) — so a pass whose Spotlight half is off walks only when its entries are non-empty, which with that half off can only be the Universal Control change), spotlight_after_apply (a Migrated or Failed outcome re-walks through spotlight_census; else scan.counts() with would_migrate 0), noindex::apply_one (the dry run's refusals — no Cargo.toml beside, a live build, a name in the way — are the real apply's Skipped arms, so an exposed dir a real apply left is one a dry run does not plan), noindex::apply_under_scan (the scan handed back beside the outcomes), machine::universal_control_state + platform::unix::universal_control_state (the two keys, one `defaults read` each) and machine::machine_state_line (the read's own builder, MACHINE_STATE_MARKER); aterm-gui's read_seed_markers/parse_seed_line take the record as Wake::PkgMachineState and the card's expectation is opened on the change line, which is why the order is pinned. ONE SLIP FIXED before this row: the printer's doc said `one defaults read` where the state is two keys, one read each; 2026-09-16 read of the four test literals 0dc0914a9 added to the_apply_prints_its_state_record_after_its_change_line (shown by `xtask gate help-surfaces --diff`): `cfg.universal_control()` counted once in apply_machine_settings' body — against the body, where the one call sits at `let policy = cfg.universal_control();` above apply_universal_control and print_machine_state_after_apply takes `policy` as a parameter (the record printer contains no `universal_control()` call) — and machine_refusal/cmd_machine, which read the policy on their own paths outside that body; no claim contradicted the code; 2026-09-16 re-read of the repair_store, uninstall_and_retire, remove_exec_roots and gc-report comments changed by the clone construction against compat::reconcile/ensure_root_in_process (Deep: clones plus the stock names' bytes, a hard-link root rebuilt), compat::remove_all, seam::detach and gc::run's closing compat::sweep; no user-facing string of the verb changed; 2026-09-16 read of the prose the Intel-Mac hold and its follow-up added (the union re-read at the merge with the 2026-09-15 rows): the set-completion Unpublished arm's `no artifact for <triple> — coherence group skipped whole (§6 clean skip)` line against the prescan's identical words and its note_finished(Skipped) loop; the UpToDate arm's `(no longer held)` against was_held and the `held:` row prefix the hold writes; the update lane's `NOT updated — <member>'s pinned build N is not published for <triple>; staying on the current builds` line and its per-installed-member `held:` rows against apply_group_txn's TxnOutcome::Unpublished { member, build, triple } and the installed.get(prog) guard; the loud arm's Aborted `why` (`its current build was recalled and the group's new pin (<member> build N) is not published for <triple>, so its commands are disabled until it is`) against disable_revoked_currents and the `aborted: <phase> — <cause>` row the Aborted arm records; the set-completion prescan's disabled-program clause — `program_disabled_here`'s doc (a `tombstoned:`/`aborted:` row, or a tombstone shim among the program's exposed tools, falling back to the program's own name when the build is no longer counted installed, exactly `repair`'s K2 measurement) against `is_tombstone_shim` and `installed_exposes`, and the `no build for this machine (<triple>) — and its current build is disabled here (see `aterm pkg doctor`); the row stays` line against the `continue` that skips `record_status` while `wanted.remove` still runs; the docs of an_installed_group_whose_new_pin_is_unpublished_here_is_held_not_failed and an_unpublished_pin_never_quietly_holds_a_revoked_current_build against the tests themselves, which run green on x86_64 macOS; re-read on 2026-09-16 after the rebase onto dd3808bc9 (the row upstream recorded there): `--diff` against that read shows exactly the prose the two carried commits added and this side had read at 40f502671e2b4c20 — the `tombstoned:`/`aborted:` row markers, the no-build-and-disabled row, the coherence-group clean-skip line, the `blocked`/`held`/`(no longer {lifted})` wording, the held-group NOT-updated row and their tests — and no line the merge itself wrote; nothing new to read against a handler; 2026-09-17 read of the hidden landing verb's prose (cmd_landing's doc and the dispatch comment above it) against the code beside it: the verb is dispatched before the verb match and BEFORE the store lock, alongside __pending and the reroute verb — which is precisely what lets it answer while the pass that is landing the build HOLDS that lock for the whole wait; HIDDEN_VERB = __landing; the optional PREFIX operand is taken only when it is not `--` and Path::is_absolute, so a run with no HOME still finds the store instead of exiting 1 with the tool never run; WAIT_SECS_ENV, DEFAULT_WAIT_SECS = 45 and REFRESH_SECS = 2 as landing.rs defines them, both pinned by its own tests; and SIGINT stops the wait without killing the command (the io.sleep contract at landing.rs:459 answers false when a SIGINT arrived, and every ending still runs the tool). No claim contradicted; 2026-09-17 merge read of main (round 16) over origin/main: the merged prose hashes to this row's recorded value, so no prose moved at the merge re-read on 2026-09-17 against the handler after 9613620b2 and the merge d0b23f22f, which changed this prose without re-recording: `cmd_landing`'s Windows hand-over and Ctrl-C sentences and `landing_ctrl`/`landing_wait_in_process` were read against `landing::HandOver::parse`, `landing::shim_command`, `platform::exec_or_run`, `platform::add_ctrl_handler`/`remove_ctrl_handler`, `platform::cmd_landing_prelude` with `CMD_FORWARD_TAIL`, and `activate::reconcile_agents`/`twin_is_rendered`; the string-set delta was read too (the new `system` literal is the `extern \"system\"` ABI of `landing_ctrl`, the two `--` literals moved verbatim into `HandOver::parse`, and the removed duplicate is the collapsed `#[cfg(unix)]` pair — no usage text lost its `--`). Six findings were FIXED in the commit that records this row rather than left as drift: the unconditional Windows Ctrl-C promise (arming the console handler is best-effort, so that one ending may not run the tool), the usage header that omitted `[<prefix>]`, the `LANDING_INTERRUPTED` doc that named only the SIGINT handler, an ending enumeration that missed the not-landing case, the manual's doubled `Terminate batch job` caveat that had dropped its landing-path scope, and its \"every later re-lay is safe\" and \"carries the same wait\" sentences, which now admit the residual byte-offset gap the code records and the fail-closed empty prelude that leaves a twin with no wait at all. re-read on 2026-09-18 for the first-launch disclosure, against `net_announcement`, `should_complete_set`, `clear_adoption` (which drops the who-adopted record with the marker), `Index::channel_for` (the per-target pin view this lane's `seed_install_bytes` must keep), `verb_mutates_store` with `mutator_store_lock` and `StoreLockError::Contended`'s retry sentence, and `aterm-gui`'s `toolchain_announced` (text after the LAST `(`, clipped at 40 by `sanitize_for_tty`). ONE SLIP FIXED: the log-cap check named the line `atpkg seed starting: {detail}`, but one `Wake::PkgSeedStarted` serves the seed and wire lanes and the GUI logs `atpkg install pass starting: {detail}`, so the sentence and the prefix the check measures with were corrected — the first-launch line is 426 bytes as logged, not the 418/420 the lane claimed. 2026-09-18 read of the rc-report prose (`repair_hook_lines`' doc and every line it prints, `run_repair`'s shell-integration comment, and `repair_names_the_rc_it_relaid_over_an_opt_out`'s doc and assert messages) against atpkg hooks.rs — `RC_BEGIN`/`RC_END`, the four-row `RC_FILES` roster, `RC_LEDGER` (`~/.aterm/rc-wired`, one CANONICAL path per line), `refresh` versus `refresh_rewiring_rc`, `refresh_at`/`pass_at`, `HookPass`'s three variants and each of `RcOutcome`'s seven, and `ensure_rc_sources_hooks`' exits in order (absent, unresolvable, the `protected::under_protected_root` consent fence, not a regular file, not UTF-8, marker present, ledger opt-out, temp+rename) — plus `aterm-update-core`'s `ensure_private_dir` (symlink, foreign owner and group/other-writable all refused) and `crate::protected`'s HOME_FOLDERS/LIBRARY_DOMAINS/ABSOLUTE_ROOTS for the folder list the consent-fence line quotes, `platform::our_uid` for the test's non-root guard, and `run_repair` as the ONE caller of `hooks::refresh_rewiring_rc` (`git grep` over crates/). THREE SLIPS FIXED before recording: the ported line said an rc over 4 MiB is left alone, but main has no size bound on the rc read (no `RC_MAX_BYTES`), so that clause is gone; \"nothing was touched\" on the not-hardened arm overclaimed, since `ensure_private_dir` may have created `~/.aterm` before refusing `~/.aterm/shell.d`, so it now says \"no rc was read or written\", which is what the arm actually guarantees; and the hooks-not-written arm no longer says the rc step \"still ran\" — on main `refresh_at` gates the rc wiring on the hook write, so that list is always empty. The historical line at the `repair` dispatch arm (\"printed \\\"repair: shell integration rewritten\\\" and exited 0\") is past tense about the pre-`zero_arity` verb and is left as history; 2026-09-18 read of the waited-launch stand-down prose added for the owner's \"Another aterm is installing\" report (the WAITED_FOR_LOCK / PROCESS_START_UNIX / FIRST_CONTENDED_UNIX docs, pass_finished_since_start and pass_finished_while_we_waited, stood_down_after_wait_line's `atpkg: update: up to date — a pass on this store succeeded N s ago, while this one waited for it; nothing to redo`, the LOCK_WAITING/LOCK_ACQUIRED marker docs now saying the GUI logs the line and paints no row, and parent_is_gone's orphan-watch sentence) against cmd_update_all_code's early return after `layout()`, the still_wanted closure in mutator_store_lock (contended polls only), and the once-pass exemption on ATPKG_UPDATE_INTERVAL_SECS=0; the stand-down keys on last_success_at strictly after the first contended second, so `up to date` is only ever said on a success stamp — no claim contradicts the code.; 2026-09-19 read of the `bin/`-listing-count prose 0a72bc5a1 added (the_alias_pass_lists_bin_once_and_per_program_only_where_it_deletes's and the_shadow_pass_lists_bin_once_for_the_whole_machine's docs and assert strings) and the pid-scoped fixture names 58f006bb6 gave the_same_directory_under_another_spelling_is_the_real_home, against cli::reconcile_aliases (ONE `ops::BinScan::capture` at the top, `active_builds_in`/`active_tools_in` off it, `activate::reconcile_aliases` per non-linked program ending in `prune_stale_shims`, which lists `bin/` itself through `ops::read_bin_dir` — the one door the test-only `BIN_SCANS` counter measures — then `reconcile_agents` once after the loop), cli::reconcile_shadowed (one capture, then reads, prints and `record_status` only), `activate::reconcile_agents` (no `bin/` listing of its own; `sweep_agents_dir` returns on a missing `agents/` before its `active_builds`, and `landing::sweep_stale` reads markers only), the old code at 6d20e089c (`active_builds` once, then `active_tools` per program in both passes, plus the prune per program in the alias pass — 1+2N and 1+N, the commit's measured 13 and 7 on six programs) and `std::process::id()` in the two format calls; the three tests run green; TWO SLIPS FIXED: the alias-pass doc said the old pass opened `bin/` `three times per active program` (3N) where the old code, the commit and the doc's own `1 + 2N` say once plus twice per program, and the shadow-pass doc said it `walked the same two listings per program as the alias pass`, naming `active_builds` as per-program, where it was one listing plus `active_tools` per program — both now say so.; 2026-09-19 (feat/agent-self-update-intercept merged onto origin/main): the `__selfupdate` usage line and `cmd_selfupdate` / `selfupdate_plan` / `selfupdate_check` / `agent_twin_intercepts` docs and the `which` sentence comment read against their bodies and `tests/selfupdate_intercept.rs`; the merged upstream prose (the bin/ listing, the parked-tree guard, the dev-link restore) was read by its own rows' authors; nothing contradicted; 2026-09-19 (no env knobs): re-read after the escape/wait knobs were removed — `cmd_selfupdate`'s steps 5–7 and `selfupdate_plan` / `selfupdate_check`'s docs read against their bodies (a declined shape is refused with no bypass named and no environment read; the child runs as `update <program> --wait-lock 1800`, `selfupdate::WAIT_LOCK_SECS`, the binary and the bound now `selfupdate_check`'s parameters), the `SEED_BUSY_MARKER` doc's self-update sentence against where the 75 lane is pinned (in-process, §8 of the test, and the edge file's hold-then-release lane), the in-process test's §8 strings against the lane that runs the real dev `atpkg` through a confining wrapper; the `__selfupdate` usage line unchanged; nothing contradicted; 2026-09-19 (landing knob retired, the same direction): `cmd_landing`'s doc now names `crate::landing::WAIT_SECS` (a constant, 45) as the expired bound where it named the env const — read against its body, which passes that constant to `landing_wait_in_process` with no `var_os` read (the earlier `WAIT_SECS_ENV` / `DEFAULT_WAIT_SECS` claim in this row is history: both are gone from landing.rs, `wait_secs_from_env` with them, and `no_environment_knob_reaches_the_landing` pins the fence over this verb); the `__landing` usage line unchanged; nothing contradicted; 2026-09-20: re-read after main's bde181c1e condensed the `__landing` verb doc and the repair shell-integration doc — both against `cmd_landing` (exec of the current bin shim, the Windows child, every ending runs the tool, the unarmed-handler exception) and the repair rc lines; nothing contradicted; 2026-09-21 read of the narration drop bde181c1e (294 prose lines removed, 142 added over 14 hunks) against its handlers, one reader per cluster, each asked to REFUTE: __landing (landing.rs's wait and its endings, platform's add_ctrl_handler), repair's rc report (hooks.rs refresh_rewiring_rc and the RcOutcome arms), seed adoption and consent (cmd_seed, set_completion_consent, should_complete_set, Consent, store.rs adopted_by_seed), machine/noindex (noindex.rs apply_one's dry run and its three refusals, apply_machine_settings' order of prints), the size lanes (seed_install_bytes, size_parenthetical, cost.rs human_bytes) and the bin/-scan and coherence-hold test docs (ops.rs BinScan, activate.rs prune_stale_shims, flow.rs group_missing_triple). An omission was not counted a defect. THREE surviving claims were contradicted by their handlers, each because the qualifier carrying it was dropped, and each is fixed in this commit: spotlight_after_apply's 'only a pass that moved something, or failed to, walks again' missed apply_machine_settings' second walk, the census.or_else taken when the Spotlight half is off and the Universal Control half changed something; size_parenthetical's 'every arm is built to fit' is false of the two-figure arm, whose inner text is 21 + |D| + |K| and so renders 41 against toolchain_announced's 40-character cap whenever both figures fall in the 1000.0-1023.9 MiB band human_bytes renders in 10; and Consent::Explicit's 'every update pass the seed pass's adoption does not stand behind' reads as !adopted_by_seed, while set_completion_consent's else-branch also catches a seed-adopted machine overridden by auto_install or by a seed_install=false set after adoption. One caveat recorded, nothing changed for it: the bin/-scan cost is 2 + N rather than 1 + N once an agent program is installed, because sweep_agents_dir then reaches its own active_builds; the fixture installs none, so what the test asserts is right; 2026-09-23 read of the vendor-surfaces change (design 2026-09-22 §1.7): which_line/managed_state_of/vendor_state_of, reconcile_shadowed's managed_row, the gc lines (gc_builds_words), the managed-current marker (managed_current_line/managed_current_entries read the vendor rows offline), cmd_rollback's vendor fork and vendor_rolled_back_line, the verify BuildMismatch line and shim_env_fix, each read against the code it describes; the tests' new docs read against the assertions under them; 2026-09-23 read of the review fixes: gc_sweep_lines/scratch_words (a stage-scratch sweep names its build by version, pid and shape, against store::stage_scratch_of), rollback_in and cmd_rollback_vendor (a vendor rollback builds no fetcher and may land on the retained legacy index build, against lane::roll_back_by_hand), print_managed_current/managed_current_entries (said at every vendor-lane exit, less the programs whose vendor this run could not reach), recorded_root_of and the shim_env_fix doc (the env alone, against shim_env::fix_line), each read against the code; the new tests' docs against their assertions; 2026-09-23 read of the head-watch flag's doc text: `--head-watch` is stripped at the dispatch edge (take_head_watch_flag) and only selects the unattended vendor door of `update <program>` (head_watch_pass → VendorDoor::Window); it appears in no usage line and is not user vocabulary, as its doc says; 2026-09-23 read of 57da3dd15's test literals (the managed-current source test reads `fn update_vendor(`, the body both doors run) and of the final-review fixes, each against the code it describes: VendorDoor::Person's doc (forgets refusals, installs what is missing, fails on an unreached vendor — update_vendor's `person` arms); VendorPass, settle_vendor_pass and update_pass_stopped (the gc after a vendor move is gc::run_for_programs over `pass.ran`, sparing `resolved_assets`, then the shell hooks); vendor_folded_outcome/failed_programs_outcome and report_channel_apply's `also_failed` aggregate (run_update_pass records a vendor failure's line, never `up to date (index build N)`); run_update_pass's and install_toolset's resolve-first comments (resolve_verified_index before vendor_pass, whose fresh verification refreshes the latch in flow::verify_select_fresh; an Err only stops the index lane, via update_pass_stopped); vendor_pass's policy comment (Policy::current with the fresh index, else the latch); legacy_index_for (skipped once the stamp keeps the legacy version); record_vendor_status's doc and unreached_since (an Unreachable verdict leaves the row only while ProgramStamp::checked_recently, else writes vendor_kept `<Vendor>'s release channel not reached since <date>`); managed_current_entries/print_managed_current (leave out a program whose stamp is older than CHECK_VOUCHES_FOR_SECS); do_install/do_install_with and install_vendor_door (flow::vendor_install_outcome, recorded by record_vendor_status, settled by settle_vendor_pass); install_default_set_inner's agent-rows comment (an `error:`/`tombstoned:` vendor row is skipped); the new tests' docs against their assertions.; 2026-09-23 read of the test prose the legacy-ceiling and door-refresh change added (no help text changed, shown by `gate help-surfaces --diff`): the head-watch, first-launch-install and failing-resolve door tests and the window-door legacy test's above-ceiling half, against update_vendor and install_vendor_door → flow::door_vendor_policy (a yank latch absent or over an hour old is refreshed by a live index verification first; a failed one leaves the latch and never fails the door) and vendor_direct::decide's LegacyUnread (an unread legacy build above VendorSpec::legacy_ceiling is kept unattended, replaced when a person asks). 2026-09-22 index-latency read: report_seedless_posture runs in the launch seed after adoption; cmd_update_all_code completes an adopted default set, and published_index_wakes starts that signed update after a public HEAD hint. The hint itself authorizes no install; 2026-09-22 (Phase 2, silent by default) read of the prose this change moved (shown by `xtask gate help-surfaces --diff`): the dispatch comment and cmd_landing's doc against its body (HandOver::parse, the prefix operand or layout(), shim_command, exec_or_run — no wait, no marker read, no line on the normal path; the usage, `is not a tool name` and `could not run` lines unchanged); mutator_store_lock's sweep comment against landing::sweep (landing/ removed, a link unlinked, never followed) under the held lock; the deleted landing_wait_in_process/landing_sigint/landing_ctrl/warn_if_never_checked and their docs; the rewritten test docs (never_checked_is_data_until_a_pass_has_succeeded over status::never_checked, the hand-over predicate over today's and older twins, the retired-wording test without the Marker words) against their assertions. No claim contradicted; 2026-09-23 (p2/gui review) read of the changed text: MACHINE_SETTINGS_MARKER's doc (ledger entries carrying UNIVERSAL_CONTROL_REVERT, Settings ▸ Security's Last change) against aterm-gui `toolchain_machine_settings` / `machine_setting_words`; print_managed_current's doc and run_update_pass's R6 comment (the window's `appstatus` ledger) against `toolchain_managed_current`; the new UNSERVED_ARCHITECTURE_CLAUSE / REMOVED_ON_PURPOSE_CLAUSE / seed_unusable_is_informational docs against the three `seed-unusable:` emitters, which now spell the clause through the constants (the printed lines are byte-identical), and aterm-gui's PkgSeedUnusable arm; 2026-09-23 Phase-2 merge read: the union of p2/shell's re-read (no landing wait, no never-checked or staged-update line in a shell; a host's unasked notices go to aterm.log; doctor's truthful vendor clause) and p2/gui's (routine package rows go to the ledger and log; failures light the Settings Packages badge; appnotice says recorded for a marker) — disjoint behaviours, and no sentence of one contradicts the other; 2026-09-23 (Phase 3 pass work) read of every string and doc this change touched against its code: the edge's changed-table gate and the refusal note beside the lock, `repair`'s `hooks in place` line (`write_hooks` now skips identical hooks) and its tagged-stub re-lay lines (`reroute::relay_tagged`, `stub::relay_tagged`), the unreadable-record line (printed only when `status::seed_for_rewrite` could not keep the file aside), the pass-log SHADOWED lines now saying `on this pass's PATH` (nothing recorded; `status::with_shadows` computes it on read), the roll-forward lines, and the offline exit (`PASS_OFFLINE_EXIT`) — consistent; 2026-09-23 read of the Phase 3 review fixes (p3/pass) against their handlers: the unreadable-record line now prints only for a read that failed for a reason that is not corruption (`status::seed_for_rewrite` heals InvalidData and a non-regular path, and leaves anything else alone — `status_seed_for_rewrite`); the roll-forward's new not-finished line is the sysroot resolve check it names (`flow::bundle_resolve_check` before `install_tools_env`), and pinned/yanked/grouped builds are skipped silently as the doc says; the offline exit is `PASS_OFFLINE_EXIT` only when no host answered (`Fetcher::index_link_down`, `ResolveProvenance::answered`, a vendor `Unreachable` with a status counting as answered) and every counted failure was a fetch (`flow::note_fetch_fault`), a refused listing exiting 1; `machine.lock` is `machine_apply_queue`'s file; the machine-settings `retried next pass` skip is true again (`machine::forget_applied` on an unfinished apply) — consistent; 2026-09-23 p3/reconciled merge read (p3/pass onto main): the union of main's seedless line (the windowed app runs the pass automatically, including when a new public index appears — aterm-gui's package park and atpkg::index_probe) and p3/pass's strings read above; disjoint strings, no contradiction; 2026-09-23 p3/reconciled read (shown by `xtask gate help-surfaces --diff`): SPAWNER_DETACHED and spawner_from's doc and test strings against is_orphaned (a detached pass names no spawner and waits out its bound); the `*toolset*` index-unreachable state now spelled through state::TOOLSET_INDEX_UNREACHABLE (the printed string is byte-identical); pass_finished_while_we_waited's doc without the deleted once-pass knob; the seedless line kept as main's (the windowed app runs the pass automatically, including when a new public index appears) — consistent; 2026-09-23 Phase 4 p4/settings read: the dispatch edge's package-log pass record (`PassRecord::begin` once the store lock is held, `end` before it is released, `refused` for a verb the lock turned away) and `pass_lane`'s rule read against the `--head-watch` flag, `SPAWNER_DETACHED`, `selfupdate::child_args` (`--wait-lock`, no `--progress-file`) and the window's `run_atpkg_pass` argv; `exit_code_u8`'s comparison-only doc against the exit-code census in lock.rs; `latest_known_pins`' offline read of the §14 cache (`select_index` over the effective anchor and floor, vendor programs left out) — consistent; no usage or verb text changed; 2026-09-23 p4/settings review read (shown by `xtask gate help-surfaces --diff`): `PassRecord`'s `outcome_before`/`outcomes_before` docs and `PassRecord::outcome` against its two checks (the thread's counter moved since `begin`, and the record's `(updated_at, outcome)` moved), `OUTCOMES_WRITTEN`/`note_outcome_written`/`outcomes_written` against the thread-local counter and its writers (`record_status` through `write_program_row(Some)`, `record_outcome`, `clear_seed_status`, `reconcile_seed_status`'s offer rewrite, `record_index_freshness`'s cache amendment; `record_row_keeping_outcome` through `write_program_row(None)` counts nothing), `record_status`'s and `write_program_row`'s docs, and the new test's doc and literals against the test — consistent; no usage or verb text changed; 2026-09-23 (Phase 4, one path) re-read of the changed message and doc text against the handlers: `print_unreachable_followup` no longer offers an environment token (the rate-limit line says only when it resets); `repair_store`'s tracked-install line names `[packages] tracked_install = \"refuse\"` (`lay::tracked_policy_of`, config only); the seed lane's skip line and the network announcement name `[packages].auto_install = false` (`cmd_seed`, `Consent::announced`); the which-lines of the reroute rows say `aterm --no-reroute` and, for z3, that its own path reaches it (`reroute::policy_summary`, `escape_clause`); the `do_install` doc names the one channel (`config::CHANNEL`).; 2026-09-23 (Phase 4 review fixes) read of the four added passages (`gate help-surfaces --diff`): the seed lane's new line (`[packages] prefix names a store this build does not use …`) against cmd_seed's branch on `PackagesConfig::ignored_prefix`, which returns before adoption, stubs and the rustup seam; `should_complete_set`'s doc — the consent input is `PackagesConfig::installs_unattended` (the consent and no ignored prefix) and the new `asked` input (a WRITTEN `auto_install = true` stands in for adoption) — against its body `consent && (adopted || asked) && enabled && !declined && !config_unreadable` and its caller in cmd_update_all_code (`cfg.auto_install == Some(true)`); and the table test's new message. No claim contradicted; 2026-09-23 merge read (p4/integrate: p4/settings merged with p4/onepath): `latest_known_pins`' doc now names the one channel (`config::CHANNEL`), which its body reads — settings wrote it against `[packages].channel`, which onepath retired — and settings' package-log pass records (`PassRecord`, `pass_lane`) sit beside onepath's once-per-process notes, `should_complete_set` and the retired-key doctor lines with neither side's text changed by the merge; no usage or verb text changed — consistent; 2026-09-23 read of `selfupdate_check`'s announce comment and `selfupdate_plan`'s note — the active build is read before the child for the announce and again after it for the epilogues, and the typed words are classification evidence the line no longer prints (`SelfUpdatePlan::Check` lost `words`); 2026-09-23 Phase-4 merge read with main's 8ea92e191: the SELF-UPDATE bullet's announce line now names the version you have (read against cli.rs selfupdate_check: ops::active_builds then vendor_direct::have_words into selfupdate::announce_line, the typed words no longer echoed) — consistent with Phase 4's one-switch and one-path text on the same page, which is unchanged by it; 2026-09-23 (Phase 5, the sealed-seed client lane deleted) read of every prose change against the code left: cmd_seed's doc (adopts, stubs, reroutes, seam, exec roots, the index verdict; installs nothing; auto_install=false and a disabled manager each return before adoption) against its body; the four writerless SEED_* markers' doc against their emitters (none; pinned by the_seed_marker_prefixes_are_the_ones_the_gui_parses); UNSERVED_ARCHITECTURE_CLAUSE and seed_unusable_is_informational against report_seedless_posture, the one `seed-unusable:` writer left; NET_PASS, net_announcement and install_default_set_with_path against their bodies (one lane, no lane parameter); retire_seed_row against its two callers (a clean update pass, the explicit toolset verb); the seedless sentence against report_seedless_posture; the decline-record failure line against cmd_seed (a next launch adopts, the update pass reinstalls); NET_STARTING_MARKER's doc against its one emitter, net_announcement, which the default-set pass and the vendor lane's fresh landings both call; no claim contradicted; 2026-09-23 (Phase 5 review fixes) read of every prose change against the code: Narrowing::verdict's two sentences and two states against index_serves and its two callers (report_seedless_posture's `seed-unusable:` marker and `*toolset*` row; the update pass's empty-store arm, `nothing is installed and nothing was installable — {why}`), each pinned verbatim by an_unserved_architecture_and_a_removal_are_facts_an_exclusion_is_not and behaviourally by an_empty_store_blames_the_architecture_only_when_nothing_is_published_for_it; REMOVED_ON_PURPOSE_CLAUSE and seed_unusable_is_informational against both emitters (a removal reads as a fact, an exclusion as a failure); retire_seed_row's doc against its three callers (cmd_seed before any consent return, a clean set-completing pass, install_toolset) and cmd_seed's consent doc and auto_install=false comment against that one sweep; the docs of wanted_programs, seed_serviceable, report_seedless_posture and seedless_serves_us against their callers; the stale install_default_set, ProvisionLane, disk-refusal and seed-lane-cleared references renamed or put in the past; no claim contradicted; 2026-09-23 (fix/claude-at-exec-time, re-read at its merge of origin/main 2c256a83e) read of the hunks `xtask gate help-surfaces --diff` shows on top of main's Phase-4 text — which_line's routed arm and format strings (`{tool} → {runs} → …`, the passed-through line), the which_line/which_line_in doc bullets, and the doc comments on AgentStub, agent_stub_on_path, shadow_in_shell, shadow_through, agent_passed_row and agent_routed_row plus the which-routed test's strings and doc — against reroute.rs agents_stub_body_sh, StubEnv (markers only), agents_stub_takes_twin, agents_stub_first_on_path, pass_through_path and without_reroute_dirs, doctor.rs (10d)/(10f), and state.rs agent_routed_in_shell/agent_passed_through — consistent; 2026-09-23 read of the one added string, repair_hook_lines' HookPass::NotReleaseBundle arm, against hooks.rs: the variant is returned only by pass_as for RcWiring::HonorOptOut from a non-release bundle executable, while repair calls refresh_rewiring_rc (RcWiring::Rewire), so repair never produces it; the arm exists so the match stays exhaustive and no report is silent, and its sentence states exactly the gate pass_as applies (an_unattended_hooks_pass_from_a_dev_bundle_writes_nothing pins it); 2026-09-23 Phase-5 merge read with main through d8d9c7bad: the union of main's exec-time agent routing text (4516e581a: the claude/codex reroute stubs, THE AGENT PROGRAMS, the rehash/hash -r step — read by main's own clauses above) and Phase 5's removal of the sealed-seed wording (`atpkg seed` adopts, lays stubs and reports; nothing installs from a bundle) — disjoint paragraphs, no sentence of one contradicts the other; 2026-09-24 read of the text this change adds — one plain comment in `relay_shims` and the doc of the new test `repair_relays_a_shim_with_the_environment_its_build_declared` — against `relay_shims` (now `activate::install_shims_env` with `shim_env::read_sidecar(&build_dir)`), `activate::install_shims_env` and the test itself; no user-facing usage/help line changed, and `repair`'s printed lines are unchanged. Consistent; 2026-09-24 read of repair's one new line against hooks::move_aside_copied_commands: it prints only after a regular file at ~/.local/bin/aterm or atpkg was renamed to <name>.moved-aside-<secs> AND the release bundle was linked in its place (a failed link renames it back and prints nothing), and only when this process runs from the release aterm.app; no claim contradicted; 2026-09-24 read of repair's changed lines against hooks.rs: the help line's move of a copied aterm/atpkg/aterm-ctl matches move_aside_copied_commands (COMMAND_LINKS get the release bundle linked in their place, RETIRED_COMMANDS only moved, all only when command_link_source accepts this executable), each printed line follows a completed rename_file_aside (exclusive hard_link, inode check, atomic temp-link rename, aside dropped on failure), and the macOS-only `still a file` line names repair_from_app_hint, the app's own atpkg when this is it; no claim contradicted; 2026-09-24 read of the prose fin/model added (`gate help-surfaces --diff`: 5 hunks, 29 lines added, none removed — doc comments and test strings only, no printed or usage text): `resolve_pass_fetcher`'s doc against its body (a header sink under the store for the full pass alone, which holds the store lock), `run_recorded_update_pass`'s against its body (`PassOutcome::of_pass` over `successes_stamped` and the exit; `MeteredHold::Until` from `Fetcher::metered_hold_until`, `Lifted` when `flow::last_resolve` reached the listing, else `Kept`), `successes_stamped`'s against `record_success`, and the new test's doc against its assertions. No claim contradicted; 2026-09-24 re-read after review of fin/model (`gate help-surfaces --diff`: 6 hunks, 42 prose lines removed, 74 added): the three lines a stood-down full pass prints (`up to date … nothing to redo`, exit 0; `offline … not run again back to back`, exit 69; `a pass on this store failed … ({outcome}); not run again back to back — the next scheduled pass retries it`, exit 1 on stderr) against `stood_down_after_wait` and its one caller in cmd_update_all_code, which exits by the holder's ending (stdout for 0 and 69, stderr for 1, each exit a `return` the exit-code census reads — the offline one by its name — the holder's `status.outcome` as the sentence, nothing recorded); the rewritten docs of `pass_ended_since_contended` (any recorded full-pass end strictly after FIRST_CONTENDED_UNIX, the ending read by `Stamps::last_failed`/`last_outcome`), `pass_ended_while_we_waited`, FIRST_CONTENDED_UNIX, WAITED_FOR_LOCK and LOCK_ACQUIRED_MARKER against their bodies; the test strings and the Tier-1 queue test's doc against its assertions (TakeLockBehind over both arms, the success-only legacy rule as the negative control). No claim contradicted; 2026-09-24 read of the legacy-rollback attestation prose fin/deflake added (`--diff` against this row's hash shows only the working tree's lines, all docs and test literals): legacy_root_of's doc against its body (the recorded row's root when that row is for the build, else ProgramStamp::legacy_root_of — no fetcher and no index anywhere on the path) and against the stamp's two writers it names (keep_replaced_legacy_root, from the row a vendor install replaces; the lane's legacy_facts, from the same signed-manifest read that yields the version); keep_replaced_legacy_root's doc against its body (a non-vendor build with a non-empty root only, skipped when the stamp already keeps that root) and against retained_legacy_below (gc keeps the replaced legacy build as the one below live); the new test's doc and literals against cmd_rollback_vendor, record_vendor_status's changed-row rule (which now compares tree_root) and verify::verify_program's kept-root arm — the refusal it quotes is cmd_verify's `no signed tree_root recorded` line; no usage or help text a user reads changed; the same day, one more literal: the passthrough test's fixture directory is named for its process (`atpkg-foreign-bin-<pid>`, was `foreign-bin`), a test-only path no surface prints; 2026-09-24 read of the prose fin/deflake's review fixes move (3 hunks, 3 removed / 23 added): legacy_index_for's doc against its body (the cached index is read when an active vendor program is a legacy build with no kept version, OR vendor_direct::lane::legacy_root_owed names a legacy build — the active one, or the newest retained below the active vendor build — whose signed root neither the stamp nor that build's recorded row holds) and against plan_one, which reads the root with the version from one signed manifest and does not ask again that pass; the rollback test's extended doc and literals against its new segment (the stamp stripped of `legacy_root_build`/`legacy_tree_root` as an older client rewrote it, a pass with a verified index, then cmd_rollback_vendor and verify_program). No usage or help text a user reads changed; 2026-09-23 (2026-09-24 UTC, fin/gui) read of the prose `xtask gate help-surfaces --diff` shows moved: the marker constants' doc (one announcement, `NET_STARTING`; the sealed-payload lane's four deleted with the window's readers of them), `NET_STARTING_MARKER`'s and `SEED_FOLLOW_ON`'s docs, the announcement ledger's doc and `MARKERS` (the `seed-pending:` paragraph gone), and the two marker tests' strings — against `announcement::MARKERS` (NET_STARTING the one opener; SEED_UNUSABLE/FAILED/DONE/BUSY and NET_INSTALLED/FAILED answers), every `emit_marker_line` / `emit_marker_line_lossy` call and `net_announcement` (each of the seven emitted), `cmd_seed` (announces nothing) and aterm-gui's `parse_seed_line`/`read_seed_markers`, which read those seven and no retired four; no claim contradicted; 2026-09-24 merge-read (fin/integrate: fin/gui merged over origin/main + fin/model + fin/deflake; both sides' read notes kept above; the row's recorded d845d133d0c26a1b was origin/main's text at 423f461e1 — the fin/model and fin/deflake merges kept their reads here but not their hash — and `gate help-surfaces --diff` against it shows exactly fin/model's stood-down-pass hunks, fin/deflake's legacy-root and `atpkg-foreign-bin-<pid>` hunks and fin/gui's marker hunks, each read in the notes above): the two sides moved disjoint prose — fin/gui the marker contract (the constants' doc, `NET_STARTING_MARKER`'s and `SEED_FOLLOW_ON`'s docs, the announcement ledger's doc and `MARKERS`, the two marker tests' strings), main/fin-model/fin-deflake the stood-down full pass, `relay_shims`, repair's move-aside lines and the legacy-root attestation — and read together in the merged file: no merged sentence names `seed-starting:`/`seed-installed:`/`seed-pending:`/`seed-partial:` as printed or parsed, the stood-down pass's three lines are plain `println!`/`eprintln!` with no marker, `announcement::MARKERS` holds only the seven `the_seed_marker_prefixes_are_the_ones_the_gui_parses` finds emitted, and aterm-gui's `parse_seed_line`/`read_seed_markers` read those seven and no retired four; no claim contradicted; 2026-09-24 (audit1) read of the new/changed doc prose — `cmd_run` (IT RUNS WHAT THE MANAGED NAME RUNS), `runs_self_update_verb`, `settle_selfupdate`, `SelfUpdatePlan::Forward`, the pass `reconcile_aliases` doc (a primary rewritten only when its env differs from its build's declaration, `activate::reassert_shim_env`) and the new test's doc — against those handlers and `activate::reassert_shim_env`; no USAGE line changed. Consistent; 2026-09-24 merged read on land/quality-round-2 over upstream's 423f461e1 and audit1 reads: the added prose is this branch's repair-own-name rule (repair_exposes' doc, its test doc and stated limit — read against repair_exposes and the fixture) and the doctor lane's `already current (build N, as index M pins it)` line, read against already_current_line and its test fixture; no claim contradicted; 2026-09-24 merge-read (fin/integrate over origin/main 9533aed18; both sides' reads kept above): the two sides recorded their own texts (fin/integrate 20587b9e976d2304, main b43992d862ee5711). `gate help-surfaces --diff` against fin/integrate's read shows only main's hunks — audit1's `cmd_run` (IT RUNS WHAT THE MANAGED NAME RUNS, `runs_self_update_verb`, `settle_selfupdate`, the `{note}` line, `SelfUpdatePlan::Forward`'s doc), the pass alias reconcile's `reassert_shim_env` doc and pass-shim-env test, round 2's repair-own-name rule and test, and the doctor lane's `already current (build N, as index M pins it)` (`already_current_line`) — each read in main's notes; against main's read it shows only fin/model's stood-down full pass, fin/gui's retired seed markers and fin/deflake's legacy-root tests and `atpkg-foreign-bin-<pid>`, each read in this row's notes — disjoint prose. Read together: the stood-down lines are `cmd_update_all_code`'s alone (`pass_ended_while_we_waited` -> `stood_down_after_wait`), and main's self-update child is `update <program>` (`cmd_update_one`), so audit1's contended/incomplete epilogues are not shadowed by them; `already_current_line` is the index-program no-op, not the vendor rows fin/deflake's `record_vendor_status` tree_root rule writes; main's reconcile/repair emit no announcement marker, so `announcement::MARKERS` (fin/gui's seven) still matches every emitter. No claim contradicted; 2026-09-24 read (origin/main merged into ux/status-reporting, shown by `gate help-surfaces --diff`): every added line is main's — 0c5dfc902's `repair` usage clause and move-aside lines, the development-link guard's repair lines and `cannot list development links`, the `list` rows (`{live_count} store builds live`, `pinned` after the dev-link row), the `*links*` pass rows and the pass end's `package pass completed; N healthy development-linked program(s) preserved` — each read by main's own clauses recorded below. What the MERGE decided, read against run_update_pass and relay_shims: the pass end keeps main's dev-link line and not main's `print_provenance_carriers`, which stays deleted with the branch's silent heal (`heal_store_provenance` at the end of every store-changing door, pinned by every_store_changing_door_ends_in_the_silent_heal); relay_shims returns the branch's four slots (no tracked-install slot) and main's development-link guard returns early as `(relaid, missing, revoked, failed)`; repair_relays_a_shim_with_the_environment_its_build_declared destructures the four; no claim contradicted; re-read on 2026-09-13 by drift sweep of 2026-09-13 (aterm:atpkg); `repair`'s tracked-install sentence covered one of the record's two causes and is fixed in that commit; 2026-09-23 read of the provenance heal (ux/wp1): `repair`'s tracked-install paragraphs and the reroute/pending tag re-lay lines are gone, replaced by `repair: cleared a macOS tag from N installed file(s)` / `repair: N installed file(s) still carry a macOS tag … it could not be cleared (why)`, read against `repair_store` (heal_store after the Deep exec-root reconcile; exit 1 only while `HealOutcome::is_clean` is false); `TRACKED_INSTALL_MARKER` and `print_provenance_carriers` are deleted and `heal_store_provenance`'s two lines go only to the log (`provenance::log_line`: a host's log or a piped stderr, never a terminal); the run_repair and relay_shims docs were re-read against their bodies (step 4 is the heal; the fourth tuple slot is the failed re-lays); `default_set_adopts`' doc and its test comment now put doctor's re-seed remedy in the past tense — doctor names `aterm pkg repair` now; 2026-09-23 review read (ux/wp1): the still-tagged `repair` line now says `carries` for one file and `carry` for more, read against `repair_store`'s `HealOutcome::left()`; the new source-scan test `every_store_changing_door_ends_in_the_silent_heal` pins the six doors' `heal_store_provenance` calls, repair's `heal_store`, and no print in the heal; 2026-09-23 merge read (ux/status-reporting-2026-09-23 with origin/main's Phase 4; the ten hunks `gate help-surfaces --diff` shows over main's text are ux/wp1's, read above): `repair_store`'s two tracked-install paragraphs, which main re-spelled to name `[packages] tracked_install = \"refuse\"`, stay deleted with ux/wp1's heal — repair says only `cleared a macOS tag from N installed file(s)` or that N still carry one; main's package-log pass records (`PassRecord`, `pass_lane`) and ux/wp1's `heal_store_provenance` at the end of every store-changing door sit side by side with neither's text changed — consistent; 2026-09-23 read of ux/wp6's terminal-output prose (`--diff` against this row's hash shows only that branch's lines) against the code each sentence describes: VERB_USAGE's `doctor`/`status [--verbose]` against doctor_detail (no argument, or exactly `--verbose`/`-v`; any other refused with exit 2 and the usage) and doctor::present; cmd_help's `aterm pkg` banner against the dispatch roster it prints; the pending stub's new lines against pending_state (a terminal person gets `is on its way … (Ctrl-C to stop)` and wait_then_run only when io.wait, a queued row bumped first; piped, the plain `is being installed (N%, …) — try again in a minute` / `is next in line` / `is waiting its turn` lines and exit 127) and wait_then_run's two failure lines against its loop (a Failed/Skipped row, a pass that ended with no shim); the lock-wait sentences against mutator_store_lock (a person is stdin+stderr terminals and no --wait-lock; TYPED_LOCK_WAIT is 600 s; holder_is_person picks the another-aterm-pkg-command wording) — the piped refusal text and exit 75 unchanged; human_marker's sentences against the markers they replace, only when stdout is a person's terminal, the piped bytes unchanged; channel_apply_summary, installed_line, the pin-hold, withdrawn-build and could-not-update lines against report_channel_apply's arms; the seedless line now said only on an empty store — consistent; 2026-09-23 second ux/wp6 read (`--diff` shows only that commit's lines): the pin verb's two lines against cmd_pin (pinned: stays on this version until unpinned; unpinned: the next update moves it), report_channel_apply's pin-hold line and row against flow's Pinned outcome (it carries the pinned members only; the whole tuple holds, so the line names the group and the pinned ones), and the pending wait's decision (pending_wait_step: run when the shim resolves, fail on a recorded failure or a pass read as ended, else wait) against wait_then_run, which reads the pass before the shim as the derived AtpkgPendingWait model requires, bound in the new Tier-1 test — consistent; 2026-09-23 third ux/wp6 read, doc comments only: emit_marker_line's and emit_marker_line_lossy's docs against mutator_store_lock (the lossy printer carries the waiter's lock-waiting:, lock-acquired: and seed-busy: lines), SEED_BUSY_MARKER's doc against the waiter's spinner and human_marker's lock-waiting sentence, cmd_pending's doc against run_pending's wait, and report_channel_apply's doc against channel_apply_summary — consistent; 2026-09-23 fourth ux/wp6 read: human_marker's doc and its shadowed arm against reconcile_shadowed, which prints one line per shadowed program naming the copy that runs first and the fix, so on a terminal the shadowed: marker adds no sentence of its own — consistent; 2026-09-23 ux/wp6 review read: human_marker's net-starting sentence keeps the way back (`To remove them later: aterm pkg uninstall --all`) when the marker's consent clause names it (NET_OFF_SWITCH, on every wire announcement), report_channel_apply's withdrawn line against flow's Tombstoned outcome (one member or several: build/builds, its/their), and channel_apply_summary's words (one program named, else `N ALab tools`) — consistent; the same review's second read: the default-set pass's per-program lines (bootstrap_group: a tuple left out by [packages] exclude, a member with no build for this triple, a member the person pinned — each `<members> not installed — they install together, and …`; the installed lines of bootstrap_group and bootstrap_singleton, now `installed <program> <build>`) against the arms that print them, which skip and record exactly as before — consistent; the same review's third read: human_marker's machine-settings arm and its doc against apply_machine_settings, which prints the Universal Control change on its own line with its revert (record_universal_control_outcome) before the marker, so the terminal sentence names only the Spotlight change and none when Universal Control was the only one — consistent; 2026-09-23 merge read (ux/status-reporting-2026-09-23 with origin/main through 0467ca2ea): `gate help-surfaces --diff` from this branch's recorded read shows main's Phase 5 hunks (the sealed-seed lane and its reclaim, the disk refusal and the offer row gone; index_serves and Narrowing::verdict; retire_seed_row; the four writerless SEED_* markers) and the hooks NotReleaseBundle repair line, each read by main's clauses above; the merge's own prose, read against the merged code: cmd_seed keeps main's body with ux/wp1's `heal_store_provenance` after the exec-root reconcile (silent, logged only); report_seedless_posture keeps main's Unserved and Narrowed arms and prints ux/wp6's `the ALab toolset is not installed yet` sentence only in the served-or-unresolved arm while `active_builds` is empty; install_default_set_with_path begins its \"net\" pass on pass_progress_path (the window's file, else the typed pass's own) with no lane parameter; human_marker keeps its sentences for the four writerless SEED_* markers so every marker the ledger lists still reads as one, while no production path prints them (the_seed_marker_prefixes_are_the_ones_the_gui_parses) — consistent; 2026-09-24 read of the prose the seed verb's window-pass silence added (shown by `xtask gate help-surfaces --diff`: 3 hunks, 0 removed / 28 added — says_not_installed_yet's doc, its test's doc and strings; the printed sentence itself is byte-identical) against report_seedless_posture (the `Some(IndexServes::Yes) | None` arm now guarded by says_not_installed_yet(active_builds empty, wait_lock().is_some())), wait_lock/take_wait_lock_flag (the window spawns `seed --wait-lock 1800`, lib.rs PassVerb::Seed words) and the window's first-run row (aterm-gui StatusBars::toolchain_announced, raised for a pass whose store held no program); pinned by the_not_installed_sentence_is_for_whoever_ran_the_verb; no claim contradicted; 2026-09-24 read: the only added lines are assertion messages of cli.rs's own test a_door_under_test_ends_in_the_scan_only_heal (provenance::test_bind), no help prose — consistent; 2026-09-24 merge read (origin/main 798075024 onto ux/status-reporting): `gate help-surfaces --diff` over main's recorded read shows only this branch's own terminal wording — doctor/status `[--verbose]`, the `aterm pkg` help banner, the meter phases and the pending-tool wait lines — read against cli.rs cmd_doctor/doctor::present, meter.rs and run_pending in the clauses above — consistent; 2026-09-24 read of wait_then_run's exec hand-off: its own `PATH` export and `failed to exec` line are gone because the landed tool now runs through cmd_run (which keeps both, plus ops::exec_env), and the one new literal is the `--` separator cmd_run drops — consistent; 2026-09-24 merge read (ux/status-reporting onto origin/main 9533aed18): `gate help-surfaces --diff` over main's recorded read shows only this branch's own hunks, each read in the clauses above — consistent; 2026-09-24 merge-read (fin/integrate over origin/main dd2581889; both sides' reads kept above): the two sides recorded their own texts (fin/integrate aec81dad42879d88; main 832fdc4bfcb02b03, which no committed tree hashes to, so `--diff` fell back to dd2581889). `gate help-surfaces --diff` against fin/integrate's read shows only main's hunks — `doctor [--verbose]` and its one-flag refusal, the `aterm pkg` title and usage, the pending stub's waiting lines, the typed lock-wait lines and meter phases, the tag-clearing repair and pass lines that replaced the tracked-install record, the plain install/update/pin/group sentences and the pass summary, and `human_marker`'s sentences — read in main's notes above; against main's fallback it shows fin's hunks (the `pass_ended_while_we_waited` stand-down lines and docs, the four writerless seed markers leaving the ledger and its tests, the legacy-root keep docs and fixtures, `resolve_pass_fetcher`'s doc), read in fin's notes above, and ONE hunk that is this merge's own: `human_marker`'s `Installing {n} ALab {} from aterm's own copy…` sentence and its `seed-installed:`/`seed-partial:`/`seed-pending:` arms are gone, because main wrote them against the four constants fin/integrate retired as writerless (the merged build failed on them); the `net-starting:`/`net-installed:` sentences it keeps are for lines the pass prints, and `a_marker_reads_as_a_sentence_on_a_terminal`'s every-ledger-marker loop still covers the ledger. No claim contradicted; 2026-09-24 re-read the 10 prose hunks since 8986a9f18, including the hidden published-index hint, queued-child decision literals, attempted-index docs and the empty-store retry regression, against main_entry and take_published_index_hint_flag, pass_ended_since_contended and Stamps::completed_older_index_pass, run_recorded_update_pass and run_update_pass, install_default_set_inner, and status::stamp_pass_end_with_index. The read found that an empty-store default-set retry could verify build 46 after a first failure but leave a requested-45 end witness; DefaultSetOutcome now returns its verified build and the full pass folds it before stamping. The regression fails the first index candidate read, succeeds on the signed second, requires the default-set retry to retire the *index* row, and asserts a durable target of 46 with same-target spacing. The later empty-store diagnostic currently resolves once more; the test does not pin that redundant work. No CLI usage text changed; 2026-09-24 follow-up read: the same empty-store retry also left run_update_pass using the first resolve's index_answered/listing_refused flags after the signed retry succeeded. DefaultSetOutcome now captures its own resolve provenance before member installs can replace it, and the pass uses the retry's refusal verdict while retaining whether either attempt got an answer. Two regressions leave ay installable and require exit 0, last_pass ok, a success stamp, and no *index* row after first-attempt refusal or link failure. These are hidden scheduling and status semantics; no user-facing CLI grammar changed; 2026-09-24 regression-fixture follow-up: the two success cases originally shared label+PID scratch/store paths and libtest ran them concurrently, so one deleted the other's staging directory and caused a spurious stage IO failure. The fixtures now use per-variant labels. The receipt hash is recomputed from this exact worktree with the source's own prose_of_rust/prose_hash functions (the existing xtask binary was compiled in a different worktree and reads that tree via CARGO_MANIFEST_DIR). No handler or public help text changed; 2026-09-24 read of the one-operand guard's prose against the dispatch edge: `ONE_OPERAND_VERBS`' doc (each listed arm reads `args.get(1)` and nothing after it — the nine arms in `main_entry`'s match; `install` refuses a second program in `parse_install_argv`, `link` and `refresh` take `&args[1..]`), `at_most_one_operand`'s doc (it runs after the `--progress-file`/`--head-watch`/`--wait-lock` strip and before `_store_lock` and the machine-settings apply, so nothing of the command runs — pinned by `one_operand_verbs_refuse_a_second_name_instead_of_dropping_it`, which also ties the list to the arms both ways) and its refusal text (`one operand at a time (got N: …) — nothing was done; run `aterm pkg <verb>` once per name` plus the verb's own usage line, read against tests/one_operand_verbs.rs, whose control shows the same fixture does uninstall one name); no USAGE line changed; 2026-09-24 read of the lost-shim prose (branch fix/pkg-run-truthful-when-shim-lost) against its code: `live_without_shim`'s doc, claim by claim — own non-alias name via `ToolName::new`/`is_alias`, a bundle's same-named binary never answering because `store_build_of` must name `tool` as owner, `bin/<tool>` proved absent by `symlink_metadata` NotFound, a COMPLETE build via `list_installed`, `bin/<tool>` `is_file`, and the removed/linked/`program_disabled_here`/shipped-name tombstone guards, each an early `None`; its no-`current` comment against the new `program_current(..).parent().is_dir()` stop before `active_builds` (`a_lost_shim_without_a_current_link_answers_from_the_surviving_shims`); `run_target`'s doc against its body (exec path, then pending stub, then the lost shim — an agent program answers `LostNotRun` and is not exec'd, every other one execs `route_for_shim` or the binary with `read_sidecar` of the build, which IS `exec_env`'s first source in ops.rs); `lost_shim_twin_runs`' doc against its walk (agent program, twin resolving, `agents/` reached on PATH before the foreign copy's directory; the reroute stub not consulted, as it says) and which arm 1a's three lines against `which_names_the_agents_twin_that_still_runs_over_a_lost_bin_shim` and `run_and_which_answer_a_live_build_whose_own_shim_is_lost`. The lane's commit said no help prose changed; the gate hashes doc prose too, so this row is its read. No USAGE line changed",
    ),
    (
        "crates/xtask/src/gate.rs",
        "bc6e86d40ea6df73",
        "2026-09-22",
        "re-read on 2026-09-18 for the v0.88.0 assembly after merging main at 5b13575de: the surface gained the cells gate's cache-dir literals (ae614355d — `cell_target_dir` resolves ATERM_CELL_TARGET_DIR, else $XDG_CACHE_HOME/aterm/cells, else $HOME/.cache/aterm/cells, and refuses the temp dir, read against that function and its fixture paths) and the `[patch.crates-io]` manifest-reader strings (fa57c6f97, read against the reader that names the manifest it could not read); the fmt-editions and roster reads recorded before this row still hold, and `the_header_roster_sentence_names_exactly_the_roster` still holds the header; 2026-09-19 read of the one hunk main moved after this row (53c986e40, pushed under ATERM_PUSH_NO_GATE=1 and never recorded: `xtask gate help-surfaces --diff`, matched at e98f6e9a9) — the two fixture literals of cell_cache_root_tests::an_override_inside_the_workspace_is_still_refused, its fixture roots renamed to `/home/me/repo` so the public export's content guard does not refuse them — the OLD spelling is not repeated here, because naming it is exactly what the guard refuses and this note is itself exported — against cell_cache_root_from: test strings, not a help surface; the law pinned (an override inside the workspace is refused) unchanged; nothing else in the file moved; 2026-09-20 read of the prose this commit adds — the paragraph on FLEET_HOST_TRIPLES recording that `x86_64-apple-darwin` joined it, and why — against that const and its two readers. Verified rather than asserted: `fa57c6f97` really does add the three cells the paragraph names (`mac-x64`, `linux-arm`, `win-arm` — its diff of aterm-forge's resolve.rs), so the fleet's Intel Mac began hosting a cell's triple on that commit and not before; `foreign_cells` filters `default_cells` by this const, so the subset the paragraph claims is exactly `win`, `wasm-cpu`, `wasm-gpu`, `linux-arm`, `win-arm` — derived from the eight cells in resolve.rs, non-empty, and still carrying the x86_64-pc-windows-msvc and wasm32-unknown-unknown cells that `no_cell_this_box_runs_natively_is_in_the_always_on_subset` asserts by name; and the measurement is that test's own failure in the merge gate's test stage on that box (macOS 13.7.8, x86_64), which named this const and the triple to add. That test passes here now, and `gate_cells_foreign`'s empty-selection refusal stays unreachable. No claim contradicted its handler; 2026-09-20 read of the prose the fleet-native libc oracle adds (`xtask gate help-surfaces --diff`, matched at 10b89a4679: 2 hunks, 0 removed, 28 added) — the doc comment and assertion strings of `the_libc_oracle_cell_table_agrees_with_the_fleet` — against the test itself and the two files it parses: libc-oracle/run.sh does decide one cell per box and carries `cell_table='…'` as `triple cell host` rows with `none` for an unhosted cell, libc-oracle/gen/symgate.py carries the same six rows as `CELLS` with None for a zero-surface cell and negative_controls.py imports it, and the const the strings name is FLEET_HOST_TRIPLES, read by that test through `super::`. Test strings, not a help surface; nothing else in the file moved; 2026-09-22 re-read after correcting two claims in run_repo_guards' provenance comment: the tagged-file count was measured again (1,980 of 3,858 tracked files, so the `every tracked file` wording was wrong and now reads `the files THAT SHELL wrote`), and the interpreter sentence no longer asserts /usr/bin/env resolves a clean system bash; the lane's own `exists()` check is named as what still catches a missing guard. No behaviour changed and no other claim in the file was touched; 2026-09-22 (round 24, item 3) read of the `help-surfaces` bullet, which now names the markdown assets and how each kind of surface is hashed, against `help_surfaces::surface_hash` and `embedded_files`. No claim contradicted",
    ),
    (
        "crates/xtask/src/main.rs",
        "da97db5a4f0e6a5a",
        "2026-09-22",
        "re-read on 2026-09-13 by drift sweep of 2026-09-13 (lane xtask); its opt-in list now derives from gate::opt_in_names(); 2026-09-14 drift sweep (lane xtask, 17 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-22 (round 24, item 3) read of the `gate help-surfaces --diff` help line, which now says which hash covers what (a .rs file's string literals and doc comments; a .md in full), against `help_surfaces::surface_hash`. No claim contradicted",
    ),
    (
        "crates/aterm-census/src/main.rs",
        "15f0c30a18130a47",
        "2026-09-14",
        "usage text written 2026-09-10 exact to its parser (root default `.`, five selection flags and aliases, last-wins, exit 0/1/2) in the program-entry pass; read against main() the same day; 2026-09-14 drift sweep (lane atpkg-and-verify, 38 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code",
    ),
    (
        "crates/aterm-primer/assets/aterm-fabric-body.md",
        "cd5fffdeda8eac59",
        "2026-09-24",
        "2026-09-24 (harness docs pass): read the new paragraph (a Claude Code session in a window is supervised by default and needs no fabric; the host answers what it proves safe, continues, escalates to keyed attention owner=supervisor -> menu-bar row + notification; posts no mail since nothing calls Session::set_manager in the host; drive watch --mail posts kind=ask only while fabric=connected per escalate.rs; [harness] enabled=false / Settings Harness) against harness_host.rs, escalate.rs and supervise/config.rs; Gemini TOML constraints (no backslash, no delimiter) kept; no other prose changed ; read in full against the code it teaches on 2026-09-13 by the primer-skill read of 2026-09-13, the first time any gate saw it — it is include_str!-ed into aterm-primer and installed into agents' own context files, so a wrong line here is TYPED; four false claim(s) found and fixed in that commit; 2026-09-13 read (2026-09-14 UTC) of the four prose changes 9758cd022 made here — the `fabric=connected` trap paragraph, the rewritten `no-bridge=1` bullet with its `queued, unpublishable until this instance has a bridge` / `not sent` wording (the only two lines the gate's --diff extracts), the new `ERR timeout id=<n>` bullet, and the `aterm-link` to `aterm link` respelling of `serve`, `mirror` and `hook install claude` — against fabric.rs (bridge_attached/fabric_state/bridge_lost, cmd_post pushing the PostRow into the outbox BEFORE any refusal path, fabric_wait_refusal and bridge_reachable = supervised || state != absent, the `ERR timeout id={id}` returned at the deadline, WAIT_DEFAULT_MS 30 s / WAIT_MAX_MS 600 s), control.rs serve_bridge (which stamps connected as the inherited Scope::Bridge lane starts being served, before the child has dialled any broker, and clears it only through BridgeLostGuard), aterm-link's Bridge::run reconnect loop (an unreachable broker is a back-off, never an exit — a bridge that exits on a broker hiccup lifts the fleet halt by dying) and cmd_outbox's peek-that-removes-nothing that lets a `fabric attach` drain the same queue, cli::dispatch's serve/ls/hook/mirror arms which `aterm link` (aterm/src/main.rs Verb::Link) and the `aterm-link` argv0 alias both reach, hook.rs's four-event `.claude/settings.json` template with its session-start/user-prompt-submit/pre-tool-use/stop arms, and fabric attach's own control_verbs.rs row; no claim contradicted, and the one gap worth naming is that the `third outcome` bullet does not reach a FOURTH — the bridge's retirement verdict `ERR <undeliverable|expired|ambiguous> id=<n>` from retire_post/DEAD_DEFAULT, which unlike the other three does NOT mean queued (`ctl help post` omits it too) and the shipped skill gained the FOURTH outcome in this commit, for the same reason the manual.rs and control_verbs.rs rows did: its list stopped at `ERR timeout id=<n>` as `the third outcome, and it means queued too`, where a post the bridge RETIRED answers `ERR unroutable|ambiguous|undeliverable id=<n>` and is the one outcome that is not queued. The hash recorded here is the CORRECTED asset; 2026-09-14 round-12: the Being woken Claude Code bullet rewritten (install with --merge and a backup, live loading, --check, exit 0 on a failure of the hook's own) and read against aterm-link hook.rs's install_claude/check/open; the gate's prose lexer sees no string literal or doc comment in this markdown, so its hash did not move — the read is recorded here regardless; 2026-09-14 read of the round-13 rewrite (the `status` synopsis, the four-row table with `stalled`, the no-heartbeat paragraph, and the queued=1 bullet naming stalled as answered at once) against the same handlers as the manual's fabric page — aterm-gui fabric.rs link_report / fabric_state / cmd_post / fabric_wait_refusal, control.rs fabric_status_line, aterm-link bridge.rs ACK_DEADLINE, LinkReport and link_reason; no claim contradicted; 2026-09-14 read of the new `Who is doing what, without reading a screen` section (feat/round-13-fabric-on, round 13 C) against crates/aterm-link/src/presence.rs Fields::tokens and phase_word (attention= role= detail= phase= [context=<n>%] title=; the six phase words; Mode::Minimal writes attention= alone), cli.rs row (the `aterm link ls` columns) and fabric.rs render_text (ROLE DETAIL PHASE CTX); the `busy worker gets the mail only` sentence is round 14's D2 as the spec states it, not a claim about this build's `post`, and reads as advice to the manager; no claim contradicted; 2026-09-14 read of the whole diff against main at the rebase onto main's round-12 merge (the `fabric=` table and paragraph, the `Who is doing what` section, the `absent|stalled|disconnected` bullet, and round 12's Being woken bullet kept beside them) with two sentences amended: a wait under `stalled reason=starting` parks (aterm-gui fabric.rs wait_is_futile) and `survey` is the presence word for `aterm drive phase`'s `survey 0` line (presence.rs phase_word over aterm_phase::survey_open; aterm-agent run.rs render_phase_and_survey prints the phase word and that line); `title=` read against presence.rs read_meta (`meta set title` alone); no other claim contradicted; 2026-09-14 SPLIT: this asset became aterm-fabric-body.md (the Markdown every agent gets) plus aterm-fabric-frontmatter.md (Claude's auto-discovery header alone) — an audit found Codex and OpenCode receiving byte-identical copies of the Claude SKILL.md, frontmatter included. The body was read against the verbs it teaches: `inbox`/`inbox get`/`inbox seen`/`post`/`await inbox` and the trust set trust=<human|agent|relayed|screen> in crates/aterm-types/src/control_verbs.rs, the two watermarks and dropped=/truncated= in aterm-gui fabric.rs cmd_inbox, the halt against `aterm ctl help hold` (the body keeps no second copy of the verb list on purpose), the three queued not-landed answers against fabric.rs cmd_post's wait loop, and the mirror's `--session <sid>` hazard against aterm-link mirror.rs USAGE (default is EVERY session; the mirror runs `inbox seen` on the agent's behalf); 2026-09-14 merge read of main over 28508563a (git carried round 13's edits onto the renamed aterm-fabric-body.md): the merged prose is exactly round 13's `stalled` row, no-heartbeat paragraph, `Who is doing what` section and `absent|stalled|disconnected` bullet plus main's split (the frontmatter moved to aterm-fabric-frontmatter.md), its trust=`screen` sentence with the `aterm ctl help inbox` pointer, and the mirror's `--session <sid>` line and paragraph with its `notice`-line sentence — checked against aterm-gui fabric.rs TRUSTS (human, agent, relayed, screen) and control_verbs.rs's inbox row (the same four), aterm-link mirror.rs Options::sessions (empty = EVERY session), run_inbox's `inbox seen` on the agent's behalf and dropped_notice (the `notice` line carrying the endpoint's eviction count), crates/aterm-primer/src/lib.rs FABRIC_BODY / FABRIC_SKILL_BODY (the body alone for every agent, frontmatter + body for Claude) and presence.rs (title= is the user title alone, else `-`); no claim contradicted; 2026-09-15 read of round 15's key and deadline paragraphs from `git diff` against the branch base (the gate's Rust lexer sees little of a Markdown file, so the hash did not move with them): the key= paragraph against aterm-gui fabric.rs cmd_post/valid_key and aterm-link bridge.rs drain_outbox/StateDir::key_seq (first wins, KEYS_KEEP 4096, dup=1 only for a key-chosen deduped publish), the queued-answer `without key=` clause, and the deadline paragraph against note_deadline/settle_deadline_for/expire_deadlines. ONE sentence FIXED here: `late=1` now says a restarted bridge delivers the late reply unflagged (the bridge's `expired` map is in memory only). No other claim contradicted; 2026-09-15 read of round 15's receipts and fetch-by-offset additions, the same way: the `inbox get @<off>` command line and the header's oldest_on_bus= against aterm-gui fabric.rs cmd_inbox/inbox_get_at; the dropped=/truncated=1 bullets and the Nothing-lost paragraph against inbox_get_at (the ring answers only a whole row), deliver_fetched and aterm-link bridge.rs fetched_lines (chunks, 256 KiB cut with len=), Fetched::render and fetch_failure; the Receipts paragraph against cmd_inbox_seen/owe_receipt, cmd_outbox's receipt lines, send_receipt/retire_receipt and enable.rs's `receipts = true`; `--wait-ack` against cmd_post/wait_receipt. ONE sentence FIXED here: `bounded by dl= plus a few seconds` now states the rule cmd_post computes (`--wait-ack=<ms>`, else dl= plus 5 s, else 30 s). No other claim contradicted; 2026-09-15 read of the Being woken Claude Code bullet's new --report-to sentences against aterm-link hook.rs stop/report/recipient_check (the post before the wait, re= the newest unhandled task, 4 KiB, once per message by the content key, --check's `report-to=<sid>` tail and the installer's refusal of a recipient the instance does not host); the gate's prose lexer sees no literal in this markdown, so its hash did not move — the read is recorded here regardless; no claim contradicted; 2026-09-14 (2026-09-15 UTC) read of the --report-to sentences against hook.rs report/report_task/recipient_check/fabric_cannot_carry and self_test, and of the wake bullet against may_wake/accepted: one sentence added in this commit, that `--accept-from` names who may wake you beside every human by OWNER sid, the bridge's node id being another list; the lexer sees no literal here, so the hash stands; no claim contradicted; 2026-09-15 merge read of feat/round-15-receipts over main: the merged prose is exactly round 15's change (the `inbox get @<off>` command line, the header's oldest_on_bus=, the dropped= and truncated=1 bullets, the Nothing-lost paragraph, the queued-answers `without key=` clause and the Exactly-once, Deadline and Receipts paragraphs, shown by `git diff` of main against the merged tree — the gate's Rust lexer sees no literal in this markdown, so neither side's hash moved) plus upstream's change (the Being-woken Claude Code bullet's --report-to and --accept-from sentences, shown by `git diff 88f04bba9 main`) plus ONE sentence added at this merge to the Exactly-once paragraph — the address is still resolved first, so a re-post to a session that has gone is retired `ERR unroutable` and puts nothing new on the bus; the Receipts paragraph's `inbox seen <id> handled` and the --report-to bullet's `a task you inbox seen <id> handled before you stop is still answered` agree, a verdict acking the manager's task while report_task still answers it, checked against aterm-link bridge.rs drain_outbox, resolve_to and refresh_sessions, send_receipt, answer_fetch and fetched_lines, hook.rs stop, report, report_task and recipient_check, and aterm-gui fabric.rs inbox_get_at, cmd_inbox_seen and owe_receipt; 2026-09-22 (round 23, broadcast) read and rewritten: the `to=say` paragraph now spells `to=say[:<topic>]`, says ONE record whatever the audience, and names the verb a recipient needs (`topic add`/`ls`/`drop`, `since=@<off>`), read against `bridge::resolve_to`, `bridge::on_say_record` and `fabric::cmd_topic`. Its old sentence — a recipient must be subscribed — was TRUE but named no way to subscribe, because there was none. RECORDED FOR THE NEXT READER, because this gate did NOT flag the change: `read_prose_hash` runs `prose_of_rust` on every rostered file, and for a MARKDOWN asset that extracts only its double-quoted spans, so a whole paragraph with no `\"` in it changes nothing the hash sees. The five `.md` rows do carry five distinct hashes, so the rule is not vacuous — it is NARROW, and narrower than the row's own claim to have read the file in full. Not fixed here: widening it re-flags every markdown surface and is a round of its own; 2026-09-22 THE FIRST READ THIS FILE'S OWN TEXT EVER HAD: until this commit a non-.rs surface was hashed with the Rust prose extractor, so of this document only its double-quoted fragments entered the hash and every edit outside them passed the gate unseen; the hash is now every line. Read `crates/aterm-primer/assets/aterm-fabric-body.md` in full against the handlers it teaches on 2026-09-22. The `fabric=` synopsis, table and no-heartbeat paragraph were read against `aterm-gui/src/fabric.rs`'s `fabric_state` (which returns a FIFTH token, `stale`, derived from `link_is_stale`/`STALE_AFTER` = `LINK_REFRESH * 3`), `wait_is_futile`/`fabric_wait_refusal` (a `stale` link is `queued=1`, answered at once), `control.rs`'s `fabric_status_line`, and `aterm-link/src/bridge.rs`'s `LinkReport::ack_wants_report` with `LINK_REFRESH` (2 s), `LINK_MOVE_GAP` (250 ms) and `ACK_DEADLINE` (5 s); the document's four-value enumeration and its two-clause report rule were both contradicted and were fixed in the synopsis, a new `stale` table row, the no-heartbeat paragraph and the `ERR fabric …queued=1` bullet. The today-edited Claude hook block was read against `aterm-link/src/hook.rs` — `DEFAULT_EVENTS` (five; `pre-tool-use` only under `--gate-tools`), `claude_settings_for`, `install_claude` with its `--check` self-test and exit 2, `permission_request`, `notification` with `WAITING_NOTIFICATIONS`, `escalate`/`set_attention`/`ask_manager`/`clear_attention`, `report`/`report_task`/`settled_screen`/`report_body`/`trim_report`, `accepted`/`may_wake` — and against `aterm-link/src/permission.rs`'s `decide` and `guard_removals` (`DECIDING_MODE` = `bypassPermissions`, Bash only, `rm` **and** `rmdir`); two claims were narrowed there, the guarded target set and the list of events that clear the attention. The posting, receipt, key, fetch and halt sentences were checked against `aterm-gui/src/fabric.rs` (`POSTABLE`, `TRUSTS`, `VERDICTS`, `TEXT_PREVIEW_MAX`, `BODY_MAX`, `WAIT_DEFAULT_MS`/`WAIT_MAX_MS`/`EXPIRY_GRACE_MS`, `valid_key`, `cmd_inbox`'s header, `is_pty_reaching`), `aterm-link/; 2026-09-22 (round 24, item 3: the file is hashed IN FULL from this row on) We read crates/aterm-primer/assets/aterm-fabric-body.md in full (291 lines, the body `include_str!`-ed as FABRIC_BODY at crates/aterm-primer/src/lib.rs:320 and wrapped for Claude, Codex, OpenCode and Gemini) against the code at e751e1002, sentence by sentence and with grep/sed only, no build or test: the endpoint in crates/aterm-gui/src/fabric.rs (fabric_state and the LINK/stale machinery, wait_is_futile and fabric_wait_refusal, cmd_post with wait_landing and wait_receipt, cmd_inbox and render_row, the fetch reply, cmd_await_inbox, cmd_topic, cmd_hold, is_pty_reaching and halt_refusal, POSTABLE/KINDS/TRUSTS/VERDICTS, KEY_MAX/valid_key, TEXT_PREVIEW_MAX, BODY_MAX, RING_CAP) and control.rs (cmd_fabric and fabric_status_line, dispatch_hold_verb, the fabric-lost drop guard) plus fabric_launch.rs; the bridge in crates/aterm-link/src/bridge.rs (ACK_DEADLINE, RECONNECT_MIN/MAX, LINK_REFRESH, LINK_MOVE_GAP, LinkReport::ack_wants_report, trust_of, expire_deadlines and EXPIRED_KEEP, the keyed producer-sequence reservation, the receipts config) with state.rs (KEYS_KEEP), presence.rs, hook.rs, mirror.rs, notify.rs, cli.rs, render.rs, enable.rs and the `aterm fabric` CLI in aterm-link/src/fabric.rs; the verb table in crates/aterm-types/src/control_verbs.rs (status, inbox, inbox get, inbox seen, post, link, hold, await, outbox, outbox sent); aterm-ctl/src/lib.rs (help <verb>), aterm-cli/src/lib.rs and manual.rs (agents, help fabric), aterm-agent/src/drive_cli.rs and supervise/mail.rs, aterm-phase/src/phase.rs, and the primer's marker, wrapper and auto-prime code. We found one substantive contradiction — the round-21 `stale` fabric state and its `ERR fabric stale id=<n> queued=1` refusal are missing from the asset's four-token `fabric=` grammar, its state table and its failure-token list — one understatement of the bridge's link-report rule (the 2 s post-ack refresh and the bounded ghost-sweep exception), and one cosmetic omission in the example `post` row (`len=`, `key=`); the 2026-09-12 measurement and the broker-side dedup rest on the verb help and the vendored astream crates respectively rather than on code we read; every other verb, flag, token, file, number, default, exit code and behaviour the asset names we found made true by the code at the anchors recorded. — three sentences the read contradicted are corrected in the same commit: the `fabric=` list now names `stale`, the link-report sentence says the bridge also re-sends on any ack once its last report is 2 s old, and the example `post` row carries `len=`; 2026-09-22 (round 24, item 4) read in full against aterm-link `cli::dispatch` and aterm-ctl's Denied hint: the section 'When you have no socket: the file mirror' is gone with `aterm link mirror`, and BEING WOKEN gains the sandbox row stating the owner's principle. No claim contradicted; 2026-09-22 (round-24 review fix, commit C) read of the link-report sentence against bridge.rs LINK_MOVE_GAP (a 2x move reports only 250 ms after the last report). No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read in full: the Being woken section is the one sentence (nothing wakes you for mail; you are typed to; you may run the verbs), the trust paragraph names no wake path. Read against supervise/mail.rs `task` and the manual. Pinned by the_installed_primer_and_skills_offer_no_hook; 2026-09-23 (lane D M1) read of the v9 inbox guidance against the code it describes: `aterm drive task` types `Inbox: task @<off>` through `turn idle=600` when the worker is idle (aterm-agent supervise/mail.rs NUDGE), `fabric=connected` is the status word aterm-gui fabric.rs answers for a live bridge link, and with `fabric=absent` no bridge drains the outbox, so nothing can arrive; the per-turn poll is gone (the Nothing wakes you for mail section); 2026-09-24 (lane D2 review fix) read of the `Who is doing what` paragraph against aterm-link presence.rs/bridge.rs: `phase=` is the session's own `status agent=` verdict relayed (six words or `-`), the bridge reads no screen, a session with no identified agent program reads `phase=-` (r13_presence's plain-shell negative control), no `context=` is written, and `aterm fabric` shows ROLE, DETAIL and PHASE; pinned by presence::tests::the_texts_that_teach_the_row_describe_this_row; 2026-09-24 final-integration glue read (lane D2's presence relay over lane E's verdict words): the presence row's `phase=` list is now the server's post-reader vocabulary — busy, idle, prompt, question, survey, unknown and `wall:<kind>` (a 529 is `wall:overloaded`; `unknown` is an agent whose screen could not be read, not idle) — read against aterm-link presence::PHASES, is_wall and Fields::set_agent (an older server's `limited` still carried, LEGACY_PHASES) and against aterm-gui presence::AgentPhase::word / control_session's AGENT_WORDS; pinned by the_texts_that_teach_the_row_describe_this_row (which now requires `unknown` and `wall:<kind>`) and phase_is_the_servers_word_from_the_closed_set_or_absent",
    ),
    (
        "crates/aterm-primer/assets/aterm-fabric-frontmatter.md",
        "5f77c2fe584215c9",
        "2026-09-22",
        "2026-09-14: the Claude Code skills frontmatter alone — `name: aterm-fabric` and the `description:` trigger text auto-discovery matches on — read against crates/aterm-primer/src/lib.rs skills_for (the `claude` arm is the only one that prepends it; FABRIC_SKILL_BODY = frontmatter + body) and every_agent_gets_the_body_under_its_own_header, which pins that Codex's prompt begins with the body and OpenCode's frontmatter carries description: and no name:; the description names the triggers (inbox, post to, ERR halted, fabric=absent, no-bridge) that the body's sections answer; no claim contradicted; 2026-09-22 THE FIRST READ THIS FILE'S OWN TEXT EVER HAD: until this commit a non-.rs surface was hashed with the Rust prose extractor, so of this document only its double-quoted fragments entered the hash and every edit outside them passed the gate unseen; the hash is now every line. I read `crates/aterm-primer/assets/aterm-fabric-frontmatter.md` in full, all four lines, against `crates/aterm-primer/src/lib.rs` (`FABRIC_SKILL_BODY`'s `concat!`, `skills_for`, `doc_label`, `skill_state` and the `bundled_skill_has_usable_frontmatter` test) and against the body it is prepended to, `crates/aterm-primer/assets/aterm-fabric-body.md`. I checked each clause of the `description:` against the implementing code rather than the body: the post kinds against `aterm-link/src/body.rs`'s closed `KINDS`, the `to=` address forms and the `await inbox` predicate against the generated catalog in `aterm-types/src/control_verbs.rs`, the `fabric=`/`ERR halted`/`no-bridge=1` trigger tokens against `aterm-link/src/{hook.rs,enable.rs,fabric.rs}`, and the wake-hook claim I added against `aterm-link/src/hook.rs`'s five-hook default event set. No claim in the file was contradicted; the `name:` matches the `.claude/skills/aterm-fabric/SKILL.md` install path and the file ends with `---` so the body's managed marker stays at line start. I widened the description by two clauses and one trigger token to cover the peer-presence and wake-hook sections the body teaches but the trigger text did not advertise, and re-ran `targo --unverified test -p aterm-primer`: 57 passed, 0 failed.; 2026-09-22 (round 24, item 3: the file is hashed IN FULL from this row on) We read crates/aterm-primer/assets/aterm-fabric-frontmatter.md in full (484 bytes: the `name: aterm-fabric` / `description:` YAML block that crates/aterm-primer/src/lib.rs:324-327 concat!s ahead of aterm-fabric-body.md for the Claude Code SKILL.md installed at .claude/skills/aterm-fabric/SKILL.md, lib.rs:444-445, and for no other agent, lib.rs:331-363 and the test at 3840-3877) against the code that makes each of its claims true: the `await`, `inbox`, `inbox get`, `inbox seen`, `post`, `hold` and `fabric` rows of the verb table in crates/aterm-types/src/control_verbs.rs (1728-1773, 1853-1901, 1952-2027, 2093-2135, 2221-2257) and the `status` row's `hold=`/`fabric=` semantics (966-975); the endpoint in crates/aterm-gui/src/fabric.rs — `KINDS`/`POSTABLE` (208-220), `TRUSTS` (253), `sender_class` (379-390), `halt_refusal` and its `ERR halted reason= origin=` string (2119-2128), `valid_principal` (2221-2235), the inbox header/row/`re-id=` printers (3746, 3766, 3772), `fabric_state` (1476-1487), `fabric_wait_refusal`'s `queued=1`/`no-bridge=1` fork (4538-4541), and `cmd_await_inbox` with its `OK inbox <id>`/`OK inbox hold=` answers (4569-4628, tests 6527-6596) — with the `await inbox` dispatch in control_session.rs:826-832 and the `status` record writer in session_status.rs:2088/2187; the bridge's routing of `@<sid>` and `h-*` addresses in crates/aterm-link/src/bridge.rs:4250-4256 and 2486 with the principal classes in subject.rs:27; the file mirror in crates/aterm-link/src/mirror.rs:4-16 and its `mirror` subcommand in cli.rs:98/207, with the `aterm help fabric` mirror section in crates/aterm-cli/src/manual.rs:3763-3785; and the primer's own pins on the frontmatter (lib.rs:3458-3467, 3855). Every verb, kind, token, trigger string and behaviour the description names was found emitted or accepted by code; nothing was contradicted; the only claim the repo cannot make true — that Claude Code discovers the skill on `description:` and names it by `name:` — is pinned for presence and path by the primer's tests and was observed live in this session's own skill roster, which lists `aterm-fabric` under this exact description. We built, ran and edited nothing. — nothing contradicted; 2026-09-22 (round 24, item 4) read of the description against aterm-fabric-body.md: the file-mirror trigger clause is gone with the section. No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the description against the body: no hook clause, no wake-me trigger. Pinned by the_installed_primer_and_skills_offer_no_hook",
    ),
    (
        "crates/aterm-primer/assets/drive-aterm-skill.md",
        "96a78f0825ac3870",
        "2026-09-24",
        "2026-09-24 (harness docs pass): read the ls line shape's new tail (path= program= agent= agent_detail= agent_rev= agent_since_ms= agent_gen= agent_fp= supervisor=) against control_verbs.rs `sessions` and control_session.rs; the field meanings (program= argv0 basename via session_program.rs, agent= words per presence::agent_verdict, supervisor= the live claim, aterm-harness@<pid> per harness_host::holder); the peer house rule's program/agent/supervisor sentence; the drive-wrapper paragraph (supervise presses only what policy/approval.rs decide proves safe, guarded on the judged row per policy/guard.rs) and the new default-host paragraph against harness_host.rs and claim.rs; no other prose changed ; read in full against the code it teaches on 2026-09-13 by the primer-skill read of 2026-09-13, the first time any gate saw it — it is include_str!-ed into aterm-primer and installed into agents' own context files, so a wrong line here is TYPED; six false claim(s) found and fixed in that commit; 2026-09-13 read of round 7's additions (the `aterm drive report` command line and paragraph, and `watch --report`) against drive_cli.rs's report arm, supervise/report.rs (the six reasons archive-gap, archive-reset, max-rows, marker-not-found, no-archive, main-screen; the header report complete= reason= marker= turn= rows= archived= screen= last=), run.rs reported() (idle, question, limited only) and reported_event_line (EVENT <phase> seq= complete= rows= then the summary), and control_session.rs's turn-start ArchMark with history printing arch= before text=; confirmed live on 2026-09-13 on a headless replay of a real Claude Code byte stream (offscreen returned every row the offline prototype recovered) and against the installed 0.84 server (report fell back to no-archive and found the start through marker=ledger); no slip found; recorded at the merge of main into feat/round-7-offscreen; 2026-09-13 read of round 8's paragraph (`Never rate a session for the human`: the survey's two rows, phase's and await-turn's `survey 0`, the guarded `key 'if=^●.How.is.Claude.doing' 0` command quoted for zsh, watch's EVENT survey line, --dismiss-surveys' DISMISSED line and its still-open hand-over, and the monitor grep with DISMISSED) against phase.rs survey_open, drive_cli.rs phase_reply, run.rs survey/dismiss_survey/survey_event_line and SURVEY_ROW, and the server's row_matcher guard; the gate's --diff shows only the quoted strings it extracts from a skill (the one new `@$SID`), so the paragraph was read from git diff; one slip fixed before this row (a copy of the survey elsewhere on the screen was said to match nothing, where only a quoted copy is sure not to); 2026-09-13 read (2026-09-14 UTC) of round 9's paragraph (`A worker running out of context is a decision point too`: the indicator's two spellings, phase's and await-turn's `context <n>%`, watch's `EVENT context` once a descent at or below --context-warn (default 10, 0 off) and `EVENT compacted` once the indicator has gone from above the composer or jumped 30 points, supervise's stderr lines for its run only and the `phase` check before the next run, the right-edge rule for a quoted copy) against phase.rs context_left, drive_cli.rs parse_sub/DEFAULT_CONTEXT_WARN/phase_reply and run.rs watch_context/drive/StopAtReview::say; the gate's --diff shows none of it (the paragraph quotes no string the gate extracts, so the hash is unchanged), and the paragraph was read from git diff; one slip fixed before this row (EVENT context was said to come once, where it comes once a descent and again after a compaction) and `gone` narrowed to gone from above the composer; 2026-09-14 read of round 10's sentences (an aterm self-update hands the archive and turn ledger over, `carried=1`, a report across one whole when its rows fit what it carries, a resize then `archive-gap` with nothing lost, and a carry that failed: `history` empty, marker=user-row or marker-not-found, `--since` an older mark archive-reset) against handoff_carry.rs export/tail_from/CARRY_ARCHIVE_BYTES/adopted_ledger, alt_archive.rs import, control_session.rs cmd_history, report.rs gather_report/assess and seamless_carry_tests' end-to-end, resize and dropped-carry tests; the gate's --diff shows none of it (the paragraph quotes no new string the gate extracts, so the hash is unchanged), and the paragraph was read from git diff main; one slip fixed before this row (whole across a self-update, where the carry keeps at most the newest 1 MiB — now whole when the rows since the turn fit what it carries); 2026-09-14 drift sweep (lane primer, 46 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 merge read of feat/round-10-carry over main: the merged prose is exactly round 10's paragraph (the self-update carry, `carried=1`, the 1 MiB bound, the resize `archive-gap`, and a failed carry's empty `history` and `archive-reset`) plus nothing of main's: main changed none of the file, only this row's note (the run id replaced by the read's date, and its drift sweep), and the hash is the same on both sides because round 10's paragraph quotes no string the gate extracts, so the paragraph was read from git diff; checked against the merged handoff_carry.rs (CARRY_ARCHIVE_BYTES, adopted_ledger), alt_archive.rs import, control_session.rs cmd_history and supervise/report.rs, none of which main touched; no claim contradicted; 2026-09-14 read of the round-11 additions (the three new cheat-sheet lines, the rewritten `Everything from classify down` sentence, and the `aterm drive ledger` paragraph) against aterm-agent drive_cli.rs (the verb list its unknown-command error prints, parse_sub's per-verb flags, ledger_verb), supervise/ledger.rs (the four sources, the clock placed by the control socket's birth time, `~` for what cannot be placed, no latency claimed then), supervise/journal.rs (one JSON object per line, append-only, 0600 when created, one warning that stops nothing) and supervise/blocks.rs view_rows; no claim contradicted; 2026-09-14 read of the paragraphs this round adds, taken from `git diff main` (the three new example command lines, the `Everything from classify down` sentence, and the `aterm drive ledger replays the loop` block), against drive_cli.rs ledger_verb and parse_sub, ledger.rs gather/render_ledger (the four sources, each named when unread; the `~` mark and the withheld latency when no clock anchor places a stamp), ledger_html.rs (one page, inline style and script, nothing fetched) and journal.rs (one object per printed line, append-only, 0600 on creation, one warning that stops nothing); the `--final` line against blocks.rs view_rows; no claim contradicted; 2026-09-14 (feat/round-14-mail, round 14 D1/D2/D4) read of the --mail and task prose — DRIVE_HELP's synopsis lines, the watch, task and --mail entries and the journal's mail kind and report field (lib.rs); DRIVE_PAGE's watch, task, --mail and --journal entries (manual.rs); run.rs's module doc, SuperviseOpts::mail, Fold, MailIn, NoLane, Sink, the Review, StopAtReview, Lines and folded_event_line docs, supervise_mail, supervise_with, run_loop, watch_mail, watch_with, look's fold gate, hold_for_report, event_line_as, render_result_mail and the six mail test docs; the two skills' loop, watch, task and ledger paragraphs — against mail.rs Lane::serve (an inbox 1 --peek --meta baseline; await inbox since=<newest> kinds=<all nine> timeout <step>, re-armed on OK timeout, the kinds list dropped on ERR usage; inbox since=<id> --peek --meta, then inbox get <id> for the watched worker's report only; the MAIL line per row through the loop's sink; lane_off said once on any other ERR or a lapsed reconnect window), Lane::call's retry of a lost reply (pause doubling to pause_max, within reconnect), task() (post to=@sid kind=task [dl=<ms>] <text> from --inbox or @self, the offset from the OK's off=, one text --json read and `turn idle=600 timeout=2500 Inbox: task @<off>` only on Phase::Idle with submitted=1 as nudged, the inbox 1 baseline read BEFORE the post, await inbox … kinds=answer,report,ack re-armed on rows without re=<off>, the TIMEOUT line and EXIT_TIMEOUT), run.rs hold_for_report (pending drained; a delivery within the window folds at once, else recv_timeout to min(now + grace, deadline); Disconnected gives None so the line is as without the flag), look's Phase::Idle gate with the brief skipped only on Fold::Report, run_loop's thread::scope join (the lane returns within one mail_step) and drive_cli.rs parse_sub (--mail, --report-window and --idle-grace watch's and supervise's only; --inbox needs a leading @; --deadline, --wait and --no-nudge task's only), mail_needs_sid and task_opts (--wait bounded by --deadline, else --timeout); measured on the live worker s-1e918c4662a1b7b8bd43 (busy for the whole 25 s budget: TIMEOUT, the process ending 40 s in as the lane's parked wait was joined within its 20 s step, no MAIL lane off) and by the mock tests; no claim contradicted; 2026-09-14 (2026-09-15 UTC) read of the round-14 paragraphs from `git diff main` (the three command lines, the reworded `Everything from classify down` sentence and the `With the fabric on` paragraph) against supervise/mail.rs Lane::serve/task() and supervise/run.rs hold_for_report/MailIn::is_this_turns/run_loop; one claim amended in this commit: `a worker with the wake hooks needs --no-nudge` now says the hooks must accept you (hook.rs may_wake/accepted: a human, or a listed OWNER sid); the gate's lexer hashes only the backticked variables of this markdown, so the hash above stands; no other claim contradicted; 2026-09-22 (round-23 review) the `subscribe` section gained the three `mail:` parameters and the `topic` opt-in, read against `parse_mail_params` and `cmd_topic`; NOT flagged by this gate for the markdown-hash reason the aterm-fabric-body.md row records; 2026-09-22 THE FIRST READ THIS FILE'S OWN TEXT EVER HAD: until this commit a non-.rs surface was hashed with the Rust prose extractor, so of this document only its double-quoted fragments entered the hash and every edit outside them passed the gate unseen; the hash is now every line. Read `crates/aterm-primer/assets/drive-aterm-skill.md` in full (783 lines, now 803) against the code that implements it, verb by verb. The catalog claims were checked against `crates/aterm-types/src/control_verbs.rs` (the `VERBS` table, `SUBSCRIBE_STREAMS`, `JSON_CAPABLE_VERBS`, `framing_of`) and against the handlers in `crates/aterm-gui/src/{control.rs,control_session.rs,control_query.rs,control_media.rs,subscribe.rs,session_status.rs,cli.rs,app_config.rs,cast.rs}`, `crates/aterm-control/src/selection.rs` and `crates/aterm-types/src/control_socket.rs`; the client-answered discovery verbs, exit codes, framing and stdin payloads against `crates/aterm-ctl/src/lib.rs`; `drive`/`supervise`/`watch`/`task`/`report`/`ledger` against `crates/aterm-agent/src/{lib.rs,drive_cli.rs,fleet_cli.rs,supervise/*}` and the screen reader in `crates/aterm-phase/src/phase.rs`; the Stop report and the wake against `crates/aterm-link/src/hook.rs`. Eight claims were contradicted and fixed in the document only: the `ls`/`sessions` row was missing `identity=`, `windows` was missing `active=-`, `detail=` was missing the one-wrapper unwrap, `search … incomplete` was narrowed to eviction alone, `rows <a> <b>` was attributed to `await match` alone, `subscribe`'s round-22 `mail:kinds=`/`mail:from=` narrowing and its `MAIL`/`mail-dropped=` frames were absent, and the `--mail` section never said the Stop report is now a SCREEN read carrying a `seq=`/`hash=[ busy=1]` stamp. `nice -n 19 env CARGO_BUILD_JOBS=6 targo --unverified test -p aterm-primer` passed after the edits: 57 passed, 0 failed, 0 ignored (plus 0 doc-tests), including `the_drive_skill_names_every_subscribe_stream`, which pins the document's `Streams ⊆` line against `SUBSCRIBE_STREAMS`. No other file was touched and nothing was committed.; 2026-09-22 (round 24, item 1) one sentence added to the `mail:` paragraph — each `topic add|drop` is pushed as `EVENT <local> topic add|drop …` — read against `cmd_topic`; not flagged by this gate (the markdown hash is widened in item 3 of the same round); 2026-09-22 (round 24, item 3: the file is hashed IN FULL from this row on) On 2026-09-22 we read crates/aterm-primer/assets/drive-aterm-skill.md (the asset include_str!-ed at crates/aterm-primer/src/lib.rs:293; at e751e1002) end to end, sentence by sentence, against the code that makes each claim true: the verb table, framing and socket rules in crates/aterm-types/src/{control_verbs,control_socket,dirs,env_sanitize}.rs; the server's dispatch, help, subscribe, input, query, media, session, status, fabric and handoff modules in crates/aterm-gui/src (control.rs, control_session.rs, control_query.rs, control_media.rs, control_input.rs, subscribe.rs, session_status.rs, session_store.rs, fabric.rs, cast.rs, cli.rs, app_config.rs, app_introspect.rs, app_window.rs, lib.rs, handoff_carry.rs, spawn.rs, turn_ledger.rs, session_timeline.rs, pty_idem.rs, control_auth_unix.rs); the wait/blocks verbs in crates/aterm-control/src/selection.rs; the seq watcher in crates/aterm-core/src/terminal/observe.rs; the client in crates/aterm-ctl/src/lib.rs; the argv0 aliases in crates/aterm/src/main.rs and the manual in crates/aterm-cli/src/manual.rs; the drive, fleet and supervise surface in crates/aterm-agent/src (lib.rs, drive_cli.rs, fleet_cli.rs, supervise/{mail,run,journal,ledger,report}.rs) with the phase reader in crates/aterm-phase/src/phase.rs; the link hook in crates/aterm-link/src/hook.rs; the PTY exec path in crates/aterm-pty/src/unix.rs; the rendezvous dir in crates/aterm-uds/src/lib.rs; the Codex primer in crates/aterm-primer/src/lib.rs; and the capture PNG encoder in crates/aterm-render-api/src/lib.rs. We used grep, sed and read only, ran no build or test, and edited nothing. We recorded two claims the code contradicts — the `ls`/`sessions` line shape omits the trailing `identity=` field the server emits, and `image` PNGs are encoded RGBA rather than 'RGB, no alpha' — one grammar line that omits the `@*` selector the server accepts, and left the measured byte/time figures and the 'older build' / 'fixed on <date>' history sentences unverified as not decidable from the current tree. — three sentences the read contradicted are corrected in the same commit: the `ls`/`sessions` line shape now ends `identity=<name|->`, the `image` PNG is RGBA, and the `subscribe` grammar names `@*`; 2026-09-22 (round 24, item 4) read of the exit-2 example, the `$XDG_RUNTIME_DIR` paragraph and gotcha 2 against aterm-ctl `dominant_hint`/`discovery_report`: the example's hint line is the binary's Denied hint verbatim and no `--allow-unix-socket` sentence remains. No claim contradicted; 2026-09-22 (round-24 review fix, commit A) read of the subscribe frame list against subscribe.rs: `events-dropped=<n>` and `EVENT * fabric-retire <sid>` added, as the code emits them. No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of the env table, the watch --mail paragraph and the task sentence against the code: `aterm link hook` is no reader of ATERM_CONTROL_TOKEN, a report is the worker's own, the nudge is unconditional over an idle screen. Pinned by the_installed_primer_and_skills_offer_no_hook",
    ),
    (
        "crates/aterm-primer/assets/rust-in-aterm-skill.md",
        "d54dd2a1a48ef3a3",
        "2026-09-23",
        "read in full against the code it teaches on 2026-09-13 by the primer-skill read of 2026-09-13, the first time any gate saw it — it is include_str!-ed into aterm-primer and installed into agents' own context files, so a wrong line here is TYPED; three false claim(s) found and fixed in that commit; 2026-09-15 (fix/help-rust-targo-line): the one paragraph describing what `aterm help rust` prints was rewritten against crates/aterm-cli/src/manual.rs rust_page (the gates' pick and refused candidates, `targo on PATH` followed through an atpkg shim, the PATH vs gates comparison, rustc's sysroot, the pin and the .cargo/config off-switch) — this row's hash did not move because the lexer the gate hashes .md assets with sees no string literal in that paragraph, so this clause is the only record of the read; the rest of the file was not re-read; 2026-09-22 THE FIRST READ THIS FILE'S OWN TEXT EVER HAD: until this commit a non-.rs surface was hashed with the Rust prose extractor, so of this document only its double-quoted fragments entered the hash and every edit outside them passed the gate unseen; the hash is now every line. Read `crates/aterm-primer/assets/rust-in-aterm-skill.md` in full on 2026-09-22 against `crates/atpkg/src/reroute.rs` (the TABLE, `CARGO_LANES`, `CARGO_VERB_REROUTES`, `signpost_message`, `QUIET_ENV`/`STRICT_ENV`), `crates/aterm-cli/src/manual.rs`'s `rust_page` and the `trust` help topic, `crates/atpkg/src/doctor.rs`'s rustup-seam lines (`seam_line`, the not-linked and no-`trust`-channel warns), `crates/atpkg/src/hooks.rs`'s rc block, `crates/aterm-verify/src/toolchain.rs`'s discovery order and `is_pinned_toolchain`, `crates/aterm-primer/src/lib.rs`'s `skill_state`/`SKILL_MARK_PREFIX`, `crates/xtask/src/driver.rs`, `crates/aterm-dev/src/main.rs` and `docs/measured/trust-gate-dead-2026-09-15.md`. Every lane claim was additionally MEASURED against the installed toolchain (`targo 1.99.0-dev (321aaeda7 2026-09-17)`, store build 9192) in a scratch crate: bare `build`/`check`/`test` refuse, `run`/`doc` have no verified lane and proceed with `nobody was asked`, `--unverified` on `fmt`/`metadata` is refused as non-compilation, and `-q` silences none of the UNVERIFIED warnings. Four claims were contradicted and fixed in the document only: the `cargo clippy` row now reads `targo tippy` / `tippy` as the reroute table maps it, `--allow-l0-gaps` is described as leaving verifier gaps as warnings rather than as a 'survey', the `--unverified` lane's 'one non-suppressible notice' is replaced by the measured warning-plus-`-q` behaviour, and the blanket 'targo will not choose a lane for you' is narrowed to builds with a new paragraph naming which verbs refuse, which default to unverified, and which refuse the flag. Everything else — the refusal strings quoted in 'Errors that are not what they look like', the `aterm help rust` contents, the `rustup which` caution, the `$ATPKG_BIN` sentence; 2026-09-22 (2) re-read after this file's own text became hashable (every line, not its quoted fragments) AND after b698f7a08 renamed the tools in it: the merged text carries both that commit's naming pass and this session's read of the same claims. Re-checked the tool table, the two lanes, the refusals and the `aterm pkg doctor`/`repair` remedies against atpkg reroute.rs, targo's own help output and aterm help rust; no claim contradicted.; 2026-09-22 (round 24, item 3: the file is hashed IN FULL from this row on) We read crates/aterm-primer/assets/rust-in-aterm-skill.md in full (100 lines, at e751e1002, read-only, no build or test run) against the code each sentence names: atpkg's reroute module (crates/atpkg/src/reroute.rs — TABLE, CARGO_LANES/RUSTC_LANES, CARGO_VERB_REROUTES, signpost_message and signpost_closing, signpost_action, QUIET_ENV/STRICT_ENV/NO_REROUTE_ENV, EXPLICIT_LANE_VERBS, and run's announce-then-exec_upstream path with its REFUSAL_EXIT branch), the `aterm help rust` renderer (crates/aterm-cli/src/manual.rs rust_page with its rust-toolchain, .cargo/config, gates'-toolchain, refused, atpkg-store, managed-tools, rustc-version/-vV/sysroot, targo --unverified --version, targo-on-PATH, rustup-trust and PATH-vs-gates rows, resolve_on_path, describe_on_path and compare_toolchains, the topic alias table, and the `trust`, `reroute`, `atpkg` and `agents` pages), aterm-verify's Toolchain::discover candidate order, `refused` field and pinned_channel (crates/aterm-verify/src/toolchain.rs), atpkg's doctor (the rustup `trust` seam checks, seam_line's \"NOT the managed store\" warn, the no-channel warn, rustup_trust_channel_resolves, the trustc -Vv probe), cli (the doctor/repair/update arms, run_repair → reassert_rustup_seam and its refuse-foreign rule), hooks (hook_files' ATPKG_BIN export and appended bin/, ensure_rc_sources_hooks' never-create rule and its shell coverage) and store (SENSITIVE_SHIMS carrying cargo/rustc/rustup), the primer crate's SKILL_MARK_PREFIX, skill_state, install_skill_file, SkillState::Foreign reporting in status and remove, skills_for's `.claude/skills/rust-in-aterm/SKILL.md` row, RUST_NOTE and the owner-quote docstring, aterm-gui spawn.rs's throttled auto-prime and aterm/src/main.rs's `pkg` forwarding and `--allow-l0-gaps` measurements; and, for the targo-side error strings and the Never rules, crates/xtask/src/driver.rs, crates/aterm-dev/src/main.rs, crates/aterm-release/src/gates.rs, .cargo/config.toml's --target corollary, docs/DESIGN-toolchain-philosophy-2026-08-29.md, docs/measured/trust-gate-dead-2026-09-15.md, docs/SESSION-linux-ux-2026-09-01.md, docs/SESSION-watchdog-noindex-2026-09-02.md and docs/DESIGN-agent-toolchain-guidance-2026-09-08.md. Thirty-nine claims anchored to file:line; two sentences contradicted in letter (\"never blocked\" — false under ATERM_REROUTE_STRICT=1, where a bare cargo exits 2, and silent about `cargo trust …` exec'ing targo; and the \"verbatim\" owner quote, whose in-tree record ends with \"!\" and continues); seven claims left unverifiable from this tree — `--report-dir` and the \"advisory survey\" gloss on `--allow-l0-gaps` (manual prose only, targo is external), the \"non-suppressible\" adjective on the --unverified notice, the world-writable-directory cause of `transport:missing-json` (no measured record; both in-tree occurrences have other causes), the cargo-in-disguise flag rejection, \"every shell\" (rc must pre-exist; sh/.profile excluded), and `aterm pkg repair` as the fix for a foreign stale link (it prints the ln -sfn fix rather than re-pointing). — two sentences the read contradicted are corrected in the same commit: the owner's instruction is quoted whole, and stock `cargo`/`rustc` is described as refused (exit 2) under `ATERM_REROUTE_STRICT=1` rather than never blocked; 2026-09-23 (Phase 4, one path) re-read of the two changed sentences against atpkg `reroute.rs`: stock `cargo`/`rustc` is announced and run, never refused (`run`'s SIGNPOST arm always execs upstream; the strict knob is gone), and `[reroute] announce = false` in aterm.toml silences the signpost (`config::cached_reroute`), replacing the environment variable that did.",
    ),
    (
        "crates/aterm-primer/assets/supervise-agent-skill.md",
        "8c3038b34501a9eb",
        "2026-09-24",
        "2026-09-24 (harness docs pass): read the new section `The window already supervises every Claude Code session` against harness_host.rs (default on, one loop per session, headless only with headless=true), supervise/config.rs defaults, policy/approval.rs rules (read-only both readings, no note, no bypass; Read outside secrets; rm breaker under a scratch root; trust dialog own folder under trust_roots; one-shot only), policy/turn_end.rs (min_work 2 min, OFFER_PHRASES continue, continue_per_hour 6, retry_backoff 1/5/15, reset_grace, model fallback and restore, /compact, /login, interrupt escalates), escalate.rs (keyed attention owner=supervisor, 200 B; the host posts no mail — no set_manager caller in aterm-gui; watch --mail asks only on fabric=connected), approvals.rs (ledger and journal paths) and claim.rs (Behind watches only; hosted yields when held); the box-escalation paragraph's new badge shape; supervise's paragraph (approval policy, row+generation guard, notes lines from approval_loop.rs, python allow-list empty by default per classify.rs DEFAULT_PYTHON_ALLOW, max-s 0 unbounded, no claim taken); watch's claim sentence; the limit bullet (mail only on fabric=connected; --resume CONTINUES with `keep going (standing rules: …)` and CONTINUED rule=usage-resume@v1, 10 then 30 min; the probe question, EVENT resumed/rebriefed/still-limited and the rules restatement are gone); the usage-limit list's rules-file and `continue at once` sentences; the classifier paragraph (shell-first pre-pass, heads-only program words, write forms anywhere, the open git-read gap) against classify.rs; the 2026-09-15 limit record's owner-typed one-line question no longer called a probe; no other prose changed ; read in full against the code it teaches on 2026-09-13 by the primer-skill read of 2026-09-13, the first time any gate saw it — it is include_str!-ed into aterm-primer and installed into agents' own context files, so a wrong line here is TYPED; four, one of them the --sandbox write-confinement claim false claim(s) found and fixed in that commit; 2026-09-13 read of round 7's additions (the `aterm drive report` command line and paragraph, and `watch --report`) against drive_cli.rs's report arm, supervise/report.rs (the six reasons archive-gap, archive-reset, max-rows, marker-not-found, no-archive, main-screen; the header report complete= reason= marker= turn= rows= archived= screen= last=), run.rs reported() (idle, question, limited only) and reported_event_line (EVENT <phase> seq= complete= rows= then the summary), and control_session.rs's turn-start ArchMark with history printing arch= before text=; confirmed live on 2026-09-13 on a headless replay of a real Claude Code byte stream (offscreen returned every row the offline prototype recovered) and against the installed 0.84 server (report fell back to no-archive and found the start through marker=ledger); no slip found; recorded at the merge of main into feat/round-7-offscreen; 2026-09-13 read (2026-09-14 UTC) of round 9's bullet (`EVENT context … means the worker is about to compact; EVENT compacted … means it has`: phase's and await-turn's `context <n>%`, watch's first reading at or below --context-warn (default 10, 0 off) once a descent even mid-turn, `EVENT compacted` on the indicator gone or up 30 points, supervise's stderr lines for its run only and the `aterm drive phase` check before the next supervise) against phase.rs context_left, drive_cli.rs parse_sub/DEFAULT_CONTEXT_WARN/phase_reply and run.rs watch_context/drive/StopAtReview::say; the gate's --diff shows none of it (the bullet quotes no string the gate extracts, so the hash is unchanged), and the bullet was read from git diff; two slips fixed before this row (`gone from the screen`, where a box covering the composer takes the indicator off the screen and is no compaction — now gone from above the composer; the warning said once, now once a descent); 2026-09-14 drift sweep (lane primer, 46 claims checked over the group): re-read against the code that moved under it since this row's anchor commit; every candidate went to a verifier prompted to REFUTE it — no claim contradicted the moved code; 2026-09-14 read of the round-11 additions (the `Keep a --journal too` paragraph under *Set up the worker*, the REVIEW step's `report --final` paragraph ahead of the whole-report one, the watch section's `--journal` bullet, and the new *See how the loop ran* section) against aterm-agent supervise/journal.rs (the record's fields read from the line, append-only, 0600 when created, the single warning), run.rs (what watch prints and what supervise journals for what it decides silently), supervise/blocks.rs view_rows (Final: the last message block and the done row that ended the turn; Messages: every message and `❯` block, no tool row, no `⎿` output) and report.rs render_view (the header kept, `view=`/`kept=` added), and supervise/ledger.rs gather/summary/items for the section's claims about the four sources, the latency numbers, the four lanes and the self-contained page; the two measured facts in it (the notes file holding none of the watcher's decisions; reports of 689 and 249 rows read to find a final message of about 70) are this round's own measurements, recorded in the spec; no claim contradicted; 2026-09-14 read of the paragraphs this round adds, taken from `git diff main` (`Keep a --journal too`, the `report --final` step, the `--journal FILE records every line it prints` bullet and the `See how the loop ran` section), against journal.rs (the kinds, the Unix stamp, the phase and seq read off the line), report.rs render_view with blocks.rs view_rows (the header kept, so `complete=` still answers), and ledger.rs gather/render_ledger with ledger_html.rs for the swimlanes, the SUMMARY counters and `--since`; no claim contradicted; 2026-09-14 (feat/round-14-mail, round 14 D1/D2/D4) read of the --mail and task prose — DRIVE_HELP's synopsis lines, the watch, task and --mail entries and the journal's mail kind and report field (lib.rs); DRIVE_PAGE's watch, task, --mail and --journal entries (manual.rs); run.rs's module doc, SuperviseOpts::mail, Fold, MailIn, NoLane, Sink, the Review, StopAtReview, Lines and folded_event_line docs, supervise_mail, supervise_with, run_loop, watch_mail, watch_with, look's fold gate, hold_for_report, event_line_as, render_result_mail and the six mail test docs; the two skills' loop, watch, task and ledger paragraphs — against mail.rs Lane::serve (an inbox 1 --peek --meta baseline; await inbox since=<newest> kinds=<all nine> timeout <step>, re-armed on OK timeout, the kinds list dropped on ERR usage; inbox since=<id> --peek --meta, then inbox get <id> for the watched worker's report only; the MAIL line per row through the loop's sink; lane_off said once on any other ERR or a lapsed reconnect window), Lane::call's retry of a lost reply (pause doubling to pause_max, within reconnect), task() (post to=@sid kind=task [dl=<ms>] <text> from --inbox or @self, the offset from the OK's off=, one text --json read and `turn idle=600 timeout=2500 Inbox: task @<off>` only on Phase::Idle with submitted=1 as nudged, the inbox 1 baseline read BEFORE the post, await inbox … kinds=answer,report,ack re-armed on rows without re=<off>, the TIMEOUT line and EXIT_TIMEOUT), run.rs hold_for_report (pending drained; a delivery within the window folds at once, else recv_timeout to min(now + grace, deadline); Disconnected gives None so the line is as without the flag), look's Phase::Idle gate with the brief skipped only on Fold::Report, run_loop's thread::scope join (the lane returns within one mail_step) and drive_cli.rs parse_sub (--mail, --report-window and --idle-grace watch's and supervise's only; --inbox needs a leading @; --deadline, --wait and --no-nudge task's only), mail_needs_sid and task_opts (--wait bounded by --deadline, else --timeout); measured on the live worker s-1e918c4662a1b7b8bd43 (busy for the whole 25 s budget: TIMEOUT, the process ending 40 s in as the lane's parked wait was joined within its 20 s step, no MAIL lane off) and by the mock tests; no claim contradicted; 2026-09-15: the loop paragraph's `--report-to \"@$ME\"` attribution corrected from `round 12` to `round 12's hooks, round 14's flag` (the four hooks are round 12's install_claude; the flag is hook.rs's round-14 --report-to) and re-read against hook.rs; the markdown hash does not move (the lexer sees no literal), so the read is recorded here; 2026-09-14 (2026-09-15 UTC) read of the round-14 sections from `git diff main` (the loop's four commands and its `watch --mail` paragraph, the watch section's --mail and --report bullets, the `Assign work by mail` section and the ledger's sentence) against supervise/mail.rs task()/Lane::serve, supervise/run.rs hold_for_report/MailIn::is_this_turns/run_loop, supervise/ledger.rs ended_on_words/MarkKind::of_record (turn and idle-no-report as a stop; a `mail` record listed as MarkKind::Other) and aterm-link hook.rs may_wake/accepted; two claims amended in this commit: the install line gains `--accept-from $ME` with why (the hook wakes for a human or a listed OWNER sid — measured on the owner's live worker, whose hooks list the node id and whose inbox holds the manager's tasks, kind=task, none woken for), and `A worker with round 12's hooks … wakes on its next Stop` now says hooks that accept you; no other claim contradicted; 2026-09-17 (feat/round-17-limits) read of the round-17 additions — the two launch lines' `--resume \"$RULES\"`, the rewritten `limited` EVENT bullet and the new `A usage limit — the worker's, and your own` section (the measured 2026-09-15 16:51 → 09-17 08:55 stall, the plain-process Monitor recipe, the rules file, `/login` in the worker's window as the owner's step) — against supervise/run.rs look/limit_after/escalate/extend/probe/settle_probe/rebrief and drive_cli.rs resume_opts (the escalation, EXTEND, the probe's cue on the screen leaving the notice, EVENT resumed/rebriefed/still-limited, the 10-then-30 backoff, no work invented); no claim contradicted; 2026-09-17 read of the round-17 changes (`git diff main` at the rebase onto ca8ab9aef: `--resume \"$RULES\"` in the three watch lines, the rewritten `limited` bullet and the new `A usage limit — the worker's, and your own` section) against supervise/run.rs escalate/close_episode/extend/probe/settle_probe/rebrief (the attention meta, `kind=control` mail, ESCALATED once, EXTEND 10 min past the reset once, ONE fixed PROBE turn, EVENT resumed/rebriefed/still-limited, the backoff 10 then 30 min, one_line's line breaks as spaces, the rules file read when sent), drive_cli.rs resume_opts (refused unreadable or empty at the launch), aterm-types control_verbs.rs's `history` verb and its `meta` help (`attention` is the escalation the menu-bar status item badges), and SPEC17 §C (a plain process under a Monitor, the rules file, `/login` in the worker's window as the owner's call); no claim contradicted, prose unchanged since the row's hash; 2026-09-17 read of the launch recipe (`spawn identity=worker`, the `detail=claude identity=worker` expectation, the identities/forget lines, the older-build fallback and the plain-session paragraph's identity clause) against control_media::parse_spawn_args, session_status_record's identity= tail and agent_identity::forget_reply; 2026-09-17 read of the merged text at the rebase onto main (git diff main: the identity recipe and its plain-session clause) beside main's round-17 usage-limit section, against control_media::parse_spawn_args, session_status's status line (detail= early, identity= last) and agent_identity::forget_reply; three sentences fixed before this row: the status comment implied detail= and identity= adjacent, the sign-in-once sentence claimed the human-run measurement the docs record as a TODO (now an expectation), and step 3 of the limit section still gave `/login` in the worker's window as the whole step where the identity recipe above is now what keeps the logins apart; the gate hashes a .md through the Rust prose lexer, so only its quoted spans count and these edits move no hash — read from the diff, not the hash; 2026-09-22 read of the paragraph `The harness answers one class of box itself` against aterm-link hook.rs and permission.rs: a bypass worker's variable removal is answered only when every value resolves outside the critical classes; any other box is left up and escalated by the notification hook about six seconds later (attention meta, --report-to ask); the clear at the next prompt or stop, or tool call where --gate-tools installed it; 2026-09-22 THE FIRST READ THIS FILE'S OWN TEXT EVER HAD: until this commit a non-.rs surface was hashed with the Rust prose extractor, so of this document only its double-quoted fragments entered the hash and every edit outside them passed the gate unseen; the hash is now every line. Read `crates/aterm-primer/assets/supervise-agent-skill.md` in full against the code that implements it, line by line rather than by its string literals. Checked the phase words, the live-zone busy rules, the limit notice, the survey, the context indicator and the placeholder rule against `crates/aterm-phase/src/{phase.rs,prompt.rs}`; the loop, its flags, defaults, notes, journal, ledger and classifier against `crates/aterm-agent/src/drive_cli.rs`, `crates/aterm-agent/src/lib.rs`'s drive help and `crates/aterm-agent/src/supervise/{run.rs,mail.rs,classify.rs,journal.rs,ledger.rs}`; the today-edited harness paragraph against `crates/aterm-link/src/{permission.rs,hook.rs}`, including which events clear the attention and which forms are escalated; and every quoted verb (`spawn`, `identities`, `status`, `meta`, `key if=`, `turn settle=gone:`, `await`, `inbox`, `story`, `lease`, `text`, `cursor`, `cell`) against the generated catalog in `crates/aterm-types/src/control_verbs.rs`, plus `ls`'s row shape in `crates/aterm-ctl/src/lib.rs`, the identity fields in `crates/aterm-gui/src/{session_status.rs,agent_identity.rs}`, the hooks row in `crates/aterm-primer/src/lib.rs`, and the containment profile in `crates/aterm-containment/src/sbpl.rs`. Five claims were corrected: `status`'s field order, `phase`'s word for a streaming build, the attention-clearing events in the new paragraph, the `Esc to cancel` rule behind `prompt`, and the classifier's `&` separator. `nice -n 19 env CARGO_BUILD_JOBS=6 targo --unverified test -p aterm-primer` after the edits: 57 passed, 0 failed, 0 ignored (plus 0 doc-tests). No other file was touched and nothing was committed.; 2026-09-22 (round 24, item 3: the file is hashed IN FULL from this row on) We read crates/aterm-primer/assets/supervise-agent-skill.md in full (591 lines, at e751e1002) against the code that makes each sentence true, without running any build or test: the drive CLI, its flags and defaults in crates/aterm-agent/src/drive_cli.rs and lib.rs; the loop, its constants and its line vocabulary in crates/aterm-agent/src/supervise/{run,mail,limit,report,blocks,ledger,ledger_html,journal,classify}.rs; the phase reader and prompt parser in crates/aterm-phase/src/{phase,prompt}.rs; the control verbs (turn settle=gone and its 240 s/2000 ms defaults, await, key if=, text tail=, cursor, cell, status, ls, lease, history, offscreen, spawn identity=, identities, meta, inbox, ERR halted) in crates/aterm-gui/src/{control,control_session,control_input,control_query,control_media,agent_identity,fabric,session_status}.rs and crates/aterm-ctl/src/lib.rs; the env stripping and observe/parent variables in crates/aterm-types/src/{env_sanitize,control_verbs}.rs; the hook install flags, the permission-request guard and the notification escalation in crates/aterm-link/src/{hook,permission,cli}.rs; the Seatbelt profile in crates/aterm-containment/src/{sbpl,actuator}.rs; the managed-skill marker, foreign state and hooks row in crates/aterm-primer/src/lib.rs; and the existence of docs/OPERATOR.md. Every verb, flag, default (180000 ms, 240 s, 2000 ms, 20 s, 28 rows, 120 s, 180 s, 1800 s, 10 %, 30 points, 10/30 min, 256 bytes, 600 s clamp, exit 124/1), line format and classifier rule the asset names matched, with two exceptions we record: the --notes paragraph's \"NOTHING else\" and \"stays empty without --auto-reads\" are contradicted by the survey, report-failure and lost-connection notes at run.rs 1928, 2075–2100, 2149 and 2219, and \"Off macOS nothing is enforced at all\" overstates actuator.rs:228, which still applies a process-cap gate. Seven sentences about older builds and human measurements had no code to read against and we left them unverified. — two sentences the read contradicted are corrected in the same commit: the notes file also records the lost-connection backspace and the survey lines, and off macOS the process-cap gate still applies; 2026-09-22 (round-24 review fix, commit C) read of the notes-file paragraph against supervise/run.rs's `append_note` sites (decision lines under --auto-reads, the backspace line, survey lines under --dismiss-surveys, `report failed:` under --report) and of the off-macOS sentence against containment actuator.rs (rlimits and the process-cap gate apply; no OS sandbox). No claim contradicted; 2026-09-22 (round 25, item 1 — decision B: no vendor hooks) read of The loop, the report fold, the box paragraph and Assign work by mail against supervise/mail.rs and run.rs: no hook install, no --no-nudge/--keep-alive, no box answered for the worker. Pinned by the_installed_primer_and_skills_offer_no_hook; 2026-09-22 (round 25, item 2 — the watcher is the single supervisor) read of the box paragraph against supervise/run.rs: the watcher's badge text, the one ask per box, the clear on the box leaving. No claim contradicted; 2026-09-23 (fix/claude-at-exec-time, item 2c) read of the No-box-is-answered paragraph's escalation sentence (own first row; one kind=ask per review point, asked again after the worker worked) against aterm-agent run.rs escalate_box/approval_line — consistent",
    ),
    // HELP_SURFACES_ROSTER_END
];

/// Files discovery finds that carry no user-facing help text.
const NOT_HELP: &[NotHelpRow<'static>] = &[
    // HELP_SURFACES_NOT_HELP_BEGIN
    (
        "crates/xtask/src/help_surfaces.rs",
        "this gate's own module: its strings are the refusal lines and the roster rows; a row for it would change its own prose and refuse itself forever — the module is reviewed with the roster it carries",
    ),
    (
        "crates/aterm-gpu/src/metal/ffi.rs",
        "Metal Objective-C FFI bindings for a library crate (no fn main, no println!/eprintln!, no usage/help string). The USAGE hits are the MTLTextureUsage bitmask constants TEXTURE_USAGE_SHADER_READ / TEXTURE_USAGE_RENDER_TARGET / TEXTURE_USAGE_PIXEL_FORMAT_VIEW (lines 615-621) and the `setUsage:` / `usage` selectors (1007, 1747).",
    ),
    (
        "crates/aterm-gui/src/accesskit_tree.rs",
        "Pure accessibility-tree projection (SettingsState/grid -> accesskit TreeUpdate). Its strings are screen-reader labels/descriptions and node-id docs, not CLI help or usage text; nothing here is printed to a terminal.",
    ),
    (
        "crates/aterm-http/src/verifier/apple.rs",
        "SecTrust TLS chain verifier for a library crate (no fn main, no printed text). The USAGE hit is the OSStatus constant ERR_SEC_INVALID_EXTENDED_KEY_USAGE = -67609 (line 107), mapped to an EKU rejection at line 534.",
    ),
    (
        "crates/aterm-http/src/verifier/windows.rs",
        "CryptoAPI/schannel TLS chain verifier for a library crate (no fn main, no printed text). The USAGE hits are the Win32 imports CERT_USAGE_MATCH / CTL_USAGE / USAGE_MATCH_TYPE_AND (lines 76-81, 228-230) and the HRESULT constant CERT_E_WRONG_USAGE (line 99); the module doc's mention of CERT_USAGE_MATCH (line 27) is an implementation note, not operator help.",
    ),
    (
        "crates/aterm-types/src/app_inspection.rs",
        "Wire-grammar parser for the `inspect app/v1` / `act app/v1` / `open app` control-socket verbs. Its ParseError::Usage strings are ERR-line payloads returned over the socket (surfaced via error.to_string() in aterm-gui/src/app_control.rs:114-139), not a --help surface. They were checked anyway and match parse_inspect/parse_act/parse_open_app exactly; no findings.",
    ),
    (
        "crates/aterm-agent/src/bin/aterm-drive.rs",
        "a one-line bin shim: its help is DRIVE_HELP in crates/aterm-agent/src/lib.rs (rostered)",
    ),
    (
        "crates/aterm-agent/src/bin/aterm-fleet.rs",
        "a bin shim: its help is crates/aterm-agent/src/fleet_cli.rs (rostered)",
    ),
    (
        "crates/aterm-ctl/src/main.rs",
        "a bin shim: its help is HELP_PROSE in crates/aterm-ctl/src/lib.rs (rostered)",
    ),
    (
        "crates/aterm-forge/src/main.rs",
        "a bin shim over crates/aterm-forge/src/cli.rs (rostered); its two own strings (--root unresolvable, no workspace Cargo.toml above cwd) read against canonical_root/discover_root 2026-09-10",
    ),
    (
        "crates/aterm-gui/src/bin/aterm-redraw-conformance.rs",
        "takes no arguments and reads no env; a conformance driver whose exit codes are owned by aterm_gui::control_redraw_conformance",
    ),
    (
        "crates/aterm-gui/src/main.rs",
        "a bin shim: the window's --help is HELP_HEAD in crates/aterm-gui/src/cli.rs (rostered)",
    ),
    (
        "crates/aterm-link/src/main.rs",
        "an argv0-symlink shim: its help is crates/aterm-link/src/cli.rs (rostered)",
    ),
    (
        "crates/aterm-release/src/main.rs",
        "a bin shim: its help is USAGE in crates/aterm-release/src/cli.rs (rostered)",
    ),
    (
        "crates/aterm-scrollback/fuzz/fuzz_targets/lz4_decompress.rs",
        "a libfuzzer target, not a program",
    ),
    (
        "crates/aterm-verify/src/main.rs",
        "a bin shim over crates/aterm-verify/src/cli.rs (rostered); its own root error names exactly the two files locate_root requires and the --root > ATERM_VERIFY_ROOT > walk precedence holds (read 2026-09-10)",
    ),
    (
        "crates/aterm-winsign/src/main.rs",
        "a bin shim: its help is USAGE in crates/aterm-winsign/src/lib.rs (rostered). Its own prose is the three-line module doc; it parses nothing, forwarding argv straight to aterm_winsign::run and exiting with the code that returns (read 2026-09-15)",
    ),
    (
        "crates/aterm/src/main.rs",
        "the front door delegates its --help to HELP_HEAD in crates/aterm-cli/src/lib.rs (rostered); its own printed strings (update status|check usage, the windowing warning, the reroute and staged-update notices, the never-checked line) were read against their code 2026-09-10 and one false platform claim fixed; 2026-09-22 (Phase 2, silent by default): the staged-update nudge and the never-checked line are deleted, and the reroute-lay thread's line and the two `could not start the background` lines are aterm_log records now (no logger is installed in the session lane, so they reach no terminal); the remaining printed strings unchanged; 2026-09-23 (plain status wording): `aterm update status|check [-v]` prints one plain summary line (update_summary) and, for trouble, one plain stderr line naming Settings ▸ Log and aterm.log; the updater's records go to aterm.log through the session file logger instead of a stderr logger, and `-v` prints the ledger detail; its review the same day: a staged build installs \"while an aterm window is open\" (the session lane never applies), a too-old copy says to reinstall, an install streak is not called a check failure, and `check` at a terminal first says it is checking",
    ),
    (
        "crates/atpkg/src/main.rs",
        "a bin shim: its help is VERB_USAGE and cmd_help in crates/atpkg/src/cli.rs (rostered)",
    ),
    (
        "crates/aterm-phase/src/wall.rs",
        "a screen reader's phrase table (read 2026-09-24 at the harness/integrate merge of lane A): the USAGE hit is `USAGE_VERBS`, the verbs a Claude Code usage/model-bucket/spend notice OPENS with (`reached`, `hit your`, `reset`, …), matched against the vendor's screen by `opens_with`; nothing in the file is printed to a terminal or offered as help — its strings are the vendor's notice phrases (`PHRASES`) and test fixtures",
    ),
    // HELP_SURFACES_NOT_HELP_END
];

/// A surface's subject is its OWN package by default — the nearest `Cargo.toml`
/// above it. These rows add the packages a surface ALSO describes, and they are
/// the whole point of [`drift_report`].
///
/// WHY. The 2026-09-12 drift sweep grouped moved code by crate and re-read that
/// crate's surfaces, so a surface describing ANOTHER crate's code was never
/// reached. Measured cost: three false `fabric` claims sat in `aterm-cli`'s
/// manual for days because they describe `aterm-gui`'s `fabric.rs`, and
/// `aterm-cli` had not moved. A whole-manual read on 2026-09-14 found 22 more of
/// the same shape. An edge here means the report names the surface when the
/// package it describes moves, whether or not its own package did.
///
/// Adding an edge is cheap and never wrong to add: this is a WORKLIST, not a
/// refusal, so a subject that turns out not to matter costs one line of report.
type DescribesRow<'a> = (&'a str, &'a [&'a str]);

const DESCRIBES: &[DescribesRow<'static>] = &[
    // `aterm help <topic>` — almost every page describes another crate.
    (
        "crates/aterm-cli/src/manual.rs",
        &[
            "crates/aterm",
            "crates/aterm-agent",
            "crates/aterm-gui",
            "crates/aterm-link",
            "crates/aterm-primer",
            "crates/aterm-release",
            "crates/aterm-types",
            "crates/aterm-verify",
            "crates/atpkg",
        ],
    ),
    // The primer block and the four skill assets are written INTO agents' context
    // files; they describe the control server, the fabric and the toolchain seams.
    (
        "crates/aterm-primer/src/lib.rs",
        &[
            "crates/aterm-agent",
            "crates/aterm-ctl",
            "crates/aterm-gui",
            "crates/atpkg",
        ],
    ),
    // `aterm help config` states how many keys the starter ships; the starter is
    // aterm-gui's.
    ("crates/aterm-gui/src/cli.rs", &["crates/aterm-cli"]),
    // The ctl client's help describes the verbs the GUI's control server serves.
    ("crates/aterm-ctl/src/lib.rs", &["crates/aterm-gui"]),
    // `aterm fabric` reports what the GUI's fabric verbs answer (`fabric status`,
    // the inbox header, the timeline's hold rows) over aterm-ctl's discovery.
    (
        "crates/aterm-link/src/fabric.rs",
        &["crates/aterm-ctl", "crates/aterm-gui"],
    ),
    // The message engine's wire grammar describes the `appstatus` face the GUI's
    // control server answers (from Phase 5 the `messages` and `notice` verbs too),
    // in aterm-control's percent-encoding.
    (
        "crates/aterm-messages/src/wire.rs",
        &["crates/aterm-control", "crates/aterm-gui"],
    ),
    (
        "crates/aterm-types/src/control_verbs.rs",
        &["crates/aterm-gui"],
    ),
    // xtask's gate prose describes what the verify stages run.
    ("crates/xtask/src/main.rs", &["crates/aterm-verify"]),
    ("crates/xtask/src/gate.rs", &["crates/aterm-verify"]),
];

/// What a drift SWEEP already answered, per surface: the commit it asked up to.
///
/// A sweep reads a surface against its subject's diff and often concludes the
/// change reaches NONE of its claims. That is a real answer, and it is narrower
/// than a read against the handler — so it must not move the row's date, which
/// is what [`MAX_AGE_DAYS`] measures. Without somewhere to put it the next
/// sweep re-derives the same negative, and the one after that too: on
/// 2026-09-14 nine of the thirty-six surfaces [`drift_report`] named were
/// exactly this case.
///
/// [`drift_report`] counts from whichever of the row's own commit and its sweep
/// bound is LATER, so the worklist converges. A bound naming a commit this
/// repository does not have is REFUSED by
/// `every_swept_row_names_a_rostered_surface_and_a_real_commit` rather than
/// ignored — an ignored bound would make the surface look swept for ever, which
/// is the one way this table can do harm.
type SweptRow<'a> = (&'a str, &'a str);

const SWEPT: &[SweptRow<'static>] = &[
    // The 2026-09-14 drift sweep (429 claims over nine groups) read these and
    // found the moved code reaches none of their claims.
    ("crates/aterm-gui/src/menu.rs", "f59b2b700"),
    ("crates/aterm-gui/src/operator_host.rs", "f59b2b700"),
    (
        "crates/aterm-primer/assets/rust-in-aterm-skill.md",
        "f59b2b700",
    ),
    ("crates/aterm-agent/src/fleet_cli.rs", "f59b2b700"),
    ("crates/aterm-cli/src/lib.rs", "f59b2b700"),
    ("crates/aterm-link/src/notify.rs", "f59b2b700"),
    ("crates/aterm-verify/src/lib.rs", "f59b2b700"),
];

/// `xtask gate help-surfaces`: the verdict, with the transcript on stderr.
pub(crate) fn gate_help_surfaces() -> bool {
    let (ok, log) = help_surfaces_report(&crate::workspace_root());
    eprint!("{log}");
    ok
}

/// The verb over an arbitrary root, returning the verdict plus the transcript.
pub(crate) fn help_surfaces_report(root: &Path) -> (bool, String) {
    let refusals = help_surfaces_check(
        root,
        SCAN_ROOTS,
        SURFACES,
        NOT_HELP,
        MAX_AGE_DAYS,
        today_days(),
    );
    let mut log = String::new();
    let _ = writeln!(
        log,
        "=== gate help-surfaces (every CLI help surface read against its handler) ==="
    );
    if refusals.is_empty() {
        let _ = writeln!(
            log,
            "gate help-surfaces: GREEN — {} surface(s) read-verified, {} classified not-help, \
             max age {MAX_AGE_DAYS} days",
            SURFACES.len(),
            NOT_HELP.len()
        );
        return (true, log);
    }
    for r in &refusals {
        let _ = writeln!(log, "gate help-surfaces: {r}");
    }
    let _ = writeln!(
        log,
        "gate help-surfaces: FAILED — {} refusal(s). Read the named text against its handler, \
         then record the row it prints in crates/xtask/src/help_surfaces.rs.",
        refusals.len()
    );
    (false, log)
}

/// The pure check: refusal lines (empty = green). `today` is days since the Unix
/// epoch, so a fixture can pin the calendar.
pub(crate) fn help_surfaces_check<'a>(
    root: &Path,
    scan_roots: &[&str],
    surfaces: &[SurfaceRow<'a>],
    not_help: &[NotHelpRow<'a>],
    max_age_days: i64,
    today: i64,
) -> Vec<String> {
    let mut out = Vec::new();
    let today_iso = civil_from_days(today);

    let mut seen: BTreeMap<&str, usize> = BTreeMap::new();
    for (p, ..) in surfaces {
        *seen.entry(p).or_insert(0) += 1;
    }
    for (p, _) in not_help {
        *seen.entry(p).or_insert(0) += 1;
    }
    for (p, k) in &seen {
        if *k > 1 {
            out.push(format!(
                "R6 DUPLICATE  {p} appears {k} times across SURFACES/NOT_HELP — keep one row"
            ));
        }
    }

    let discovered = discover(root, scan_roots);
    let rostered: BTreeMap<&str, &SurfaceRow<'a>> = surfaces.iter().map(|r| (r.0, r)).collect();
    let allowed: BTreeMap<&str, &str> = not_help.iter().copied().collect();

    for (rel, reasons) in &discovered {
        if rostered.contains_key(rel.as_str()) || allowed.contains_key(rel.as_str()) {
            continue;
        }
        let hash = read_prose_hash(&root.join(rel)).unwrap_or_default();
        out.push(format!(
            "R1 UNROSTERED {rel} carries help text ({}) and no row says it was read — read it \
             against its handler, then add: (\"{rel}\", \"{hash}\", \"{today_iso}\", \"<who read it against what>\"),",
            reasons.join(", ")
        ));
    }

    for (rel, hash, verified, method) in surfaces {
        let path = root.join(rel);
        if !path.is_file() {
            out.push(format!(
                "R3 GONE       {rel} is rostered but not on disk — drop the row or fix the path"
            ));
            continue;
        }
        let current = read_prose_hash(&path).unwrap_or_default();
        if current != *hash {
            out.push(format!(
                "R2 CHANGED    {rel}: help/doc text changed since it was read on {verified} — re-read \
                 it against its handler, then replace the row with: (\"{rel}\", \"{current}\", \
                 \"{today_iso}\", \"{method}\"), (see: xtask gate help-surfaces --diff {rel})"
            ));
        }
        match days_from_iso(verified) {
            None => out.push(format!(
                "R4 STALE      {rel}: verified date {verified:?} is not YYYY-MM-DD"
            )),
            Some(d) if today - d > max_age_days => out.push(format!(
                "R4 STALE      {rel}: read {} days ago (limit {max_age_days}) — re-read it and re-date the row",
                today - d
            )),
            Some(_) => {}
        }
    }

    let programs = program_entry_points(root, scan_roots);
    let embedded = embedded_files(root, surfaces);
    for (rel, _) in not_help {
        if !root.join(rel).is_file() {
            out.push(format!(
                "R5 DEAD_ALLOW {rel} is in NOT_HELP but not on disk — prune it"
            ));
        } else if !discovered.contains_key(*rel)
            && !programs.contains(*rel)
            && !embedded.contains_key(*rel)
        {
            out.push(format!(
                "R5 DEAD_ALLOW {rel} is in NOT_HELP but discovery no longer finds it — prune it"
            ));
        }
    }
    for (rel, via) in &embedded {
        if rostered.contains_key(rel.as_str()) || allowed.contains_key(rel.as_str()) {
            continue;
        }
        let hash = read_prose_hash(&root.join(rel)).unwrap_or_default();
        out.push(format!(
            "R8 EMBEDDED   {rel} is compiled into {via} by include_str!/include_bytes! and is in neither \
             list — it is shipped text a reader sees; read it against the code it describes and add: \
             (\"{rel}\", \"{hash}\", \"{today_iso}\", \"<who read it against what>\"), or record in \
             NOT_HELP why it is not help"
        ));
    }
    for rel in &programs {
        if rostered.contains_key(rel.as_str()) || allowed.contains_key(rel.as_str()) {
            continue;
        }
        let hash = read_prose_hash(&root.join(rel)).unwrap_or_default();
        out.push(format!(
            "R7 PROGRAM    {rel} is a binary entry point in neither list — read its help against \
             its parser and add: (\"{rel}\", \"{hash}\", \"{today_iso}\", \"<who read it against \
             what>\"), or record in NOT_HELP where its help lives (or that it takes no arguments)"
        ));
    }
    out
}

/// Every binary entry point under `scan_roots`: the conventional `src/main.rs`,
/// `src/bin/*.rs` and `src/bin/*/main.rs` of each Cargo package, plus any explicit
/// `[[bin]] path = "…"`. Test, example, bench and target trees are skipped as
/// everywhere else, and so is `vendor/`.
/// Files a rostered surface compiles in, as `{path: the surface that includes it}`.
///
/// `include_str!("../assets/x.md")` puts a whole file in the binary, and in this
/// workspace that is how shipped agent documentation travels. The lexer sees only
/// the PATH at the macro, so the CONTENT never reaches the including file's hash:
/// editing the asset moves nothing. Resolved relative to the including file, as
/// the macro resolves it. Comments are blanked first — strings are NOT, because
/// the path this looks for is one.
fn embedded_files(
    root: &Path,
    surfaces: &[SurfaceRow<'_>],
) -> std::collections::BTreeMap<String, String> {
    let mut out = std::collections::BTreeMap::new();
    for (rel, ..) in surfaces {
        let path = root.join(rel);
        if path.extension().is_none_or(|x| x != "rs") || !path.is_file() {
            continue;
        }
        let Ok(src) = std::fs::read_to_string(&path) else {
            continue;
        };
        let code = strip_comments_keep_strings(&src);
        for inc in include_paths(&code) {
            let Some(dir) = path.parent() else {
                continue;
            };
            let target = normalize(&dir.join(&inc));
            // A `.rs` target needs no rule of its own: the `*.rs` walk already
            // reaches it, and whether it carries help is R1's question.
            // Including it here would be a second, wrong answer to a question
            // another rule owns — measured 2026-09-13, when nine of R8's first
            // sixteen hits were `.rs` files a TEST include_str!s to scan its own
            // source. Test and fixture trees are excluded for the same reason
            // they are everywhere else: a golden is derived text, not a claim.
            if target.extension().is_some_and(|x| x == "rs") || !target.is_file() {
                continue;
            }
            if let Ok(r) = target.strip_prefix(root)
                && excluded_path(&r.to_string_lossy().replace('\\', "/"))
            {
                continue;
            }
            if let Ok(r) = target.strip_prefix(root) {
                out.entry(r.to_string_lossy().replace('\\', "/"))
                    .or_insert_with(|| (*rel).to_string());
            }
        }
    }
    out
}

/// Every `include_str!`/`include_bytes!` path literal in `code`.
fn include_paths(code: &str) -> Vec<String> {
    let mut out = Vec::new();
    for macro_name in ["include_str!", "include_bytes!"] {
        let mut from = 0;
        while let Some(i) = code[from..].find(macro_name) {
            let at = from + i + macro_name.len();
            from = at;
            let rest = code[at..].trim_start();
            let Some(open) = rest.strip_prefix('(') else {
                continue;
            };
            let open = open.trim_start();
            let Some(body) = open.strip_prefix('"') else {
                continue;
            };
            if let Some(end) = body.find('"') {
                out.push(body[..end].to_string());
            }
        }
    }
    out
}

/// Comments blanked, string literals INTACT — `..` and `.` resolved so a
/// relative include path becomes a real repo-relative one without touching the
/// filesystem's symlinks.
fn normalize(p: &Path) -> PathBuf {
    let mut out = PathBuf::new();
    for c in p.components() {
        match c {
            std::path::Component::ParentDir => {
                out.pop();
            }
            std::path::Component::CurDir => {}
            other => out.push(other),
        }
    }
    out
}

/// Comments blanked, string literals INTACT. The include path this looks for IS
/// a string literal, so a stripper that blanks literal bodies would erase it;
/// and a `//` inside a literal is not a comment, so literals must be skipped
/// rather than ignored.
fn strip_comments_keep_strings(src: &str) -> String {
    let b = src.as_bytes();
    let n = b.len();
    let mut out = String::with_capacity(n);
    let mut i = 0;
    while i < n {
        if b[i..].starts_with(b"//") {
            let j = src[i..].find('\n').map_or(n, |k| i + k);
            out.extend(std::iter::repeat_n(' ', j - i));
            i = j;
            continue;
        }
        if b[i..].starts_with(b"/*") {
            let mut depth = 1usize;
            let mut j = i + 2;
            while j < n && depth > 0 {
                if b[j..].starts_with(b"/*") {
                    depth += 1;
                    j += 2;
                } else if b[j..].starts_with(b"*/") {
                    depth -= 1;
                    j += 2;
                } else {
                    j += 1;
                }
            }
            out.extend(
                src[i..j]
                    .chars()
                    .map(|c| if c == '\n' { '\n' } else { ' ' }),
            );
            i = j;
            continue;
        }
        // A raw string: copy it whole, hashes and all, so nothing inside is read.
        if let Some((open_len, hashes)) = raw_string_open(b, i) {
            let start = i + open_len;
            let close = format!("\"{}", "#".repeat(hashes));
            let end = src[start..]
                .find(&close)
                .map_or(n, |k| start + k + close.len());
            out.push_str(&src[i..end]);
            i = end;
            continue;
        }
        if b[i] == b'"' {
            let mut j = i + 1;
            while j < n {
                if b[j] == b'\\' {
                    j += 2;
                    continue;
                }
                if b[j] == b'"' {
                    j += 1;
                    break;
                }
                j += 1;
            }
            out.push_str(&src[i..j.min(n)]);
            i = j;
            continue;
        }
        let len = utf8_len(b[i]);
        out.push_str(&src[i..(i + len).min(n)]);
        i += len;
    }
    out
}

fn program_entry_points(root: &Path, scan_roots: &[&str]) -> std::collections::BTreeSet<String> {
    let mut out = std::collections::BTreeSet::new();
    let mut manifests = Vec::new();
    for sr in scan_roots {
        collect_named(&root.join(sr), "Cargo.toml", &mut manifests);
    }
    manifests.sort();
    for toml in manifests {
        let Some(dir) = toml.parent() else {
            continue;
        };
        let Ok(rel_dir) = dir.strip_prefix(root) else {
            continue;
        };
        let rel_dir = rel_dir.to_string_lossy().replace('\\', "/");
        if excluded_path(&format!("{rel_dir}/x.rs")) || format!("/{rel_dir}/").contains("/vendor/")
        {
            continue;
        }
        let mut candidates = vec![dir.join("src/main.rs")];
        let mut bins = Vec::new();
        collect_rs(&dir.join("src/bin"), &mut bins);
        bins.sort();
        for b in bins {
            let depth = b
                .strip_prefix(dir.join("src/bin"))
                .map_or(0, |p| p.components().count());
            if depth == 1 || (depth == 2 && b.file_name().is_some_and(|n| n == "main.rs")) {
                candidates.push(b);
            }
        }
        if let Ok(text) = std::fs::read_to_string(&toml) {
            let mut in_bin = false;
            for line in text.lines() {
                let l = line.trim();
                if l.starts_with('[') {
                    in_bin = l == "[[bin]]";
                    continue;
                }
                if in_bin
                    && let Some(rest) = l.strip_prefix("path")
                    && let Some(v) = rest.trim_start().strip_prefix('=')
                {
                    candidates.push(dir.join(v.trim().trim_matches('"')));
                }
            }
        }
        for c in candidates {
            if !c.is_file() {
                continue;
            }
            let Ok(rel) = c.strip_prefix(root) else {
                continue;
            };
            let rel = rel.to_string_lossy().replace('\\', "/");
            if !excluded_path(&rel) {
                out.insert(rel);
            }
        }
    }
    out
}

fn collect_named(dir: &Path, name: &str, out: &mut Vec<PathBuf>) {
    let Ok(entries) = std::fs::read_dir(dir) else {
        return;
    };
    for e in entries.flatten() {
        let p = e.path();
        if p.is_dir() {
            if p.file_name().is_some_and(|n| n == "target") {
                continue;
            }
            collect_named(&p, name, out);
        } else if p.file_name().is_some_and(|n| n == name) {
            out.push(p);
        }
    }
}

// ------------------------------------------------------------------ discovery

/// Workspace-relative path -> why it was discovered, for every `.rs` file under
/// `scan_roots` that carries help text.
fn discover(root: &Path, scan_roots: &[&str]) -> BTreeMap<String, Vec<&'static str>> {
    let mut found = BTreeMap::new();
    for sr in scan_roots {
        let mut files = Vec::new();
        collect_rs(&root.join(sr), &mut files);
        files.sort();
        for f in files {
            let Ok(rel) = f.strip_prefix(root) else {
                continue;
            };
            let rel = rel.to_string_lossy().replace('\\', "/");
            if excluded_path(&rel) {
                continue;
            }
            let Ok(src) = std::fs::read_to_string(&f) else {
                continue;
            };
            let reasons = discovery_reasons(&src);
            if !reasons.is_empty() {
                found.insert(rel, reasons);
            }
        }
    }
    found
}

fn collect_rs(dir: &Path, out: &mut Vec<PathBuf>) {
    let Ok(entries) = std::fs::read_dir(dir) else {
        return;
    };
    for e in entries.flatten() {
        let p = e.path();
        if p.is_dir() {
            if p.file_name().is_some_and(|n| n == "target") {
                continue;
            }
            collect_rs(&p, out);
        } else if p.extension().is_some_and(|x| x == "rs") {
            out.push(p);
        }
    }
}

/// Test, example and bench trees are not shipped surfaces.
fn excluded_path(rel: &str) -> bool {
    let s = format!("/{rel}");
    ["/examples/", "/benches/", "/tests/", "/target/"]
        .iter()
        .any(|d| s.contains(d))
        || s.ends_with("_test.rs")
        || s.ends_with("_tests.rs")
        || s.ends_with("/tests.rs")
        || s.ends_with("/kani_proofs.rs")
}

fn discovery_reasons(src: &str) -> Vec<&'static str> {
    let (strings, _docs) = prose_of_rust(src);
    let mut reasons = Vec::new();
    if strings.iter().any(|s| has_usage_block_line(s)) {
        reasons.push("usage-string");
    }
    if let Some(r) = help_code_marker(src) {
        reasons.push(r);
    }
    reasons
}

/// A literal with a line that starts a usage block: `usage:`, `OPTIONS:`, ….
fn has_usage_block_line(literal: &str) -> bool {
    const KEYS: &[&str] = &[
        "usage",
        "options",
        "subcommands",
        "commands",
        "arguments",
        "verbs",
        "flags",
        "exit codes",
        "exit code",
    ];
    literal.lines().any(|line| {
        let l = line.trim_start().to_ascii_lowercase();
        KEYS.iter().any(|k| {
            l.strip_prefix(k)
                .is_some_and(|rest| rest.trim_start().starts_with(':'))
        })
    })
}

/// Code that exists only to print help.
fn help_code_marker(src: &str) -> Option<&'static str> {
    const FNS: &[&str] = &[
        "fn usage",
        "fn print_usage",
        "fn help_text",
        "fn usage_text",
        "fn print_help",
        "fn cmd_help",
        "fn verb_help",
        "fn usage_error",
    ];
    for f in FNS {
        for (i, _) in src.match_indices(f) {
            let rest = &src[i + f.len()..];
            if rest.starts_with('(') || rest.starts_with('<') {
                return Some("help-fn");
            }
        }
    }
    for kw in ["const ", "static "] {
        for (i, _) in src.match_indices(kw) {
            let ident: String = src[i + kw.len()..]
                .chars()
                .take_while(|c| c.is_ascii_alphanumeric() || *c == '_')
                .collect();
            if ident.split('_').any(|seg| seg == "USAGE" || seg == "HELP") {
                return Some("help-const");
            }
        }
    }
    if src.contains("#[command(")
        || src.contains("#[clap(")
        || src.contains("about = \"")
        || src.contains("long_about")
    {
        return Some("clap-help");
    }
    None
}

// ----------------------------------------------------------------- extraction

/// `(string literals, doc comments)` in source order; plain comments, char
/// literals and code are dropped.
pub(crate) fn prose_of_rust(src: &str) -> (Vec<String>, Vec<String>) {
    let b = src.as_bytes();
    let n = b.len();
    let mut strings = Vec::new();
    let mut docs = Vec::new();
    let mut i = 0;
    while i < n {
        let c = b[i];
        if b[i..].starts_with(b"//") {
            let j = src[i..].find('\n').map_or(n, |k| i + k);
            let line = &src[i..j];
            if line.starts_with("///") || line.starts_with("//!") {
                docs.push(line[3..].trim().to_string());
            }
            i = j + 1;
            continue;
        }
        if b[i..].starts_with(b"/*") {
            let doc = b[i..].starts_with(b"/**") || b[i..].starts_with(b"/*!");
            let mut depth = 1;
            let mut j = i + 2;
            while j < n && depth > 0 {
                if b[j..].starts_with(b"/*") {
                    depth += 1;
                    j += 2;
                } else if b[j..].starts_with(b"*/") {
                    depth -= 1;
                    j += 2;
                } else {
                    j += 1;
                }
            }
            if doc {
                let end = j.saturating_sub(2).max(i + 3);
                docs.push(src[i + 3..end].trim().to_string());
            }
            i = j;
            continue;
        }
        // raw strings: r"…", r#"…"#, br"…", cr"…"
        if let Some((open_len, hashes)) = raw_string_open(b, i) {
            let start = i + open_len;
            let close = format!("\"{}", "#".repeat(hashes));
            let end = src[start..].find(&close).map_or(n, |k| start + k);
            strings.push(src[start..end].to_string());
            i = end + close.len();
            continue;
        }
        if c == b'"' && !(i > 0 && i + 1 < n && b[i - 1] == b'\'' && b[i + 1] == b'\'') {
            let mut j = i + 1;
            let mut buf = String::new();
            while j < n {
                let ch = b[j];
                if ch == b'\\' {
                    let nxt = b.get(j + 1).copied();
                    if nxt == Some(b'\n') {
                        j += 2;
                        while j < n && matches!(b[j], b' ' | b'\t' | b'\r' | b'\n') {
                            j += 1;
                        }
                        continue;
                    }
                    buf.push('\\');
                    if let Some(x) = nxt {
                        buf.push(x as char);
                    }
                    j += 2;
                    continue;
                }
                if ch == b'"' {
                    break;
                }
                // Copy one UTF-8 scalar.
                let ch_len = utf8_len(ch);
                buf.push_str(&src[j..(j + ch_len).min(n)]);
                j += ch_len;
            }
            strings.push(buf);
            i = j + 1;
            continue;
        }
        if c == b'\'' {
            if i + 2 < n && b[i + 1] == b'\\' {
                let j = src[i + 2..].find('\'').map_or(n, |k| i + 2 + k);
                i = j + 1;
                continue;
            }
            // 'x' where x may be multi-byte
            if i + 1 < n {
                let l = utf8_len(b[i + 1]);
                if i + 1 + l < n && b[i + 1 + l] == b'\'' {
                    i += 2 + l;
                    continue;
                }
            }
            i += 1;
            continue;
        }
        i += utf8_len(c);
    }
    (strings, docs)
}

/// `Some((opener length, hash count))` when a raw string opens at `i`.
fn raw_string_open(b: &[u8], i: usize) -> Option<(usize, usize)> {
    let mut k = i;
    if k < b.len() && (b[k] == b'b' || b[k] == b'c') {
        k += 1;
    }
    if k >= b.len() || b[k] != b'r' {
        return None;
    }
    if i > 0 && (b[i - 1].is_ascii_alphanumeric() || b[i - 1] == b'_') {
        return None;
    }
    k += 1;
    let mut hashes = 0;
    while k < b.len() && b[k] == b'#' {
        hashes += 1;
        k += 1;
    }
    if k < b.len() && b[k] == b'"' {
        Some((k + 1 - i, hashes))
    } else {
        None
    }
}

fn utf8_len(first: u8) -> usize {
    match first {
        0x00..=0x7F => 1,
        0xC0..=0xDF => 2,
        0xE0..=0xEF => 3,
        0xF0..=0xF7 => 4,
        _ => 1,
    }
}

/// FNV-1a 64 over the prose, as 16 hex digits. Change detection, not a
/// certificate — the roster is reviewed text, not a trust boundary.
pub(crate) fn prose_hash(strings: &[String], docs: &[String]) -> String {
    let mut h: u64 = 0xcbf2_9ce4_8422_2325;
    let mut feed = |bytes: &[u8]| {
        for &x in bytes {
            h ^= u64::from(x);
            h = h.wrapping_mul(0x0000_0100_0000_01b3);
        }
    };
    for s in strings {
        feed(s.as_bytes());
        feed(b"\n\x00");
    }
    feed(b"\n\x01");
    for d in docs {
        feed(d.as_bytes());
        feed(b"\n\x00");
    }
    format!("{h:016x}")
}

fn read_prose_hash(path: &Path) -> Option<String> {
    let src = std::fs::read_to_string(path).ok()?;
    Some(if is_rust_source(path) {
        source_prose_hash(&src)
    } else {
        document_prose_hash(&src)
    })
}

/// Whether a surface is Rust source, whose user-facing text is its string
/// literals and doc comments — or a shipped DOCUMENT, which is user-facing
/// text end to end.
fn is_rust_source(path: &Path) -> bool {
    path.extension().is_some_and(|x| x == "rs")
}

/// [`prose_hash`] of a source text's [`prose_of_rust`] — the gate's hash.
fn source_prose_hash(src: &str) -> String {
    let (s, d) = prose_of_rust(src);
    prose_hash(&s, &d)
}

/// The gate's hash for a shipped DOCUMENT: every line of it.
///
/// A skill file aterm installs into an agent's own directory, or a primer
/// asset `include_str!`-ed into a rostered surface, is not source with text
/// inside it — it IS the text. Running [`prose_of_rust`] over one hashes the
/// double-quoted fragments and nothing else: MEASURED 2026-09-22 on
/// `crates/aterm-primer/assets/supervise-agent-skill.md`, 66 fragments out of
/// 591 lines, so a paragraph rewritten outside quotes changed no hash and the
/// gate passed a text that had moved. It was found by rewriting exactly such a
/// paragraph in that skill and watching the gate stay green.
fn document_prose_hash(src: &str) -> String {
    prose_hash(&document_lines(src), &[])
}

/// A document's lines, as the hash and the `--diff` view both read them:
/// trailing whitespace dropped, so the two can never disagree about what
/// changed.
fn document_lines(src: &str) -> Vec<String> {
    src.lines().map(|l| l.trim_end().to_string()).collect()
}

/// [`prose_lines`] for a surface, by what the surface IS ([`is_rust_source`]).
pub(crate) fn prose_lines_for(path: &Path, src: &str) -> Vec<String> {
    if is_rust_source(path) {
        prose_lines(src)
    } else {
        document_lines(src)
    }
}

// --------------------------------------------------------------------- --diff

/// Where the roster lives, workspace-relative: `--diff`'s fallback blames its row.
const ROSTER_FILE: &str = "crates/xtask/src/help_surfaces.rs";

const DIFF_USAGE: &str = "usage: xtask gate help-surfaces [--diff PATH]... | --drift\n  \
    no argument   the gate itself\n  \
    --diff PATH   not the gate: the prose PATH changed since the read its row records, \
    item by item as the gate hashes it (PATH as the row spells it; repeatable)\n  \
    --drift       not the gate: which read-verified surfaces describe code that has \
    MOVED since they were read — the maintenance worklist";

/// `xtask gate help-surfaces [--diff PATH]...`. With no argument, the gate (the
/// `all` roster calls [`gate_help_surfaces`] directly). Each `--diff PATH` prints
/// [`prose_diff_report`] to stdout instead — a reading aid, not a verdict: it
/// fails only when it cannot produce the diff.
pub(crate) fn gate_help_surfaces_args(rest: &[String]) -> bool {
    if rest.is_empty() {
        return gate_help_surfaces();
    }
    if rest.len() == 1 && rest[0] == "--drift" {
        let root = crate::workspace_root();
        let (produced, report) = drift_report(&root, SURFACES, DESCRIBES, SWEPT, ROSTER_FILE);
        print!("{report}");
        return produced;
    }
    let mut paths = Vec::new();
    let mut args = rest.iter();
    while let Some(arg) = args.next() {
        if arg != "--diff" {
            eprintln!("gate help-surfaces: unknown argument `{arg}`.\n{DIFF_USAGE}");
            return false;
        }
        let Some(path) = args.next() else {
            eprintln!("gate help-surfaces: `--diff` needs a path.\n{DIFF_USAGE}");
            return false;
        };
        paths.push(path.as_str());
    }
    let root = crate::workspace_root();
    let mut ok = true;
    for path in paths {
        let (produced, report) = prose_diff_report(&root, path, SURFACES, NOT_HELP, ROSTER_FILE);
        print!("{report}");
        ok &= produced;
    }
    ok
}

/// `--diff PATH` over an arbitrary root: `(produced, report)`.
///
/// It finds the version the row's read was of: walking `git log -- PATH` newest
/// to oldest, the first commit whose version hashes — by [`prose_of_rust`] and
/// [`prose_hash`], exactly as the gate hashes — to the row's recorded value. The
/// report is [`unified_diff`] of [`prose_lines`] from that version to the working
/// tree, headed by that commit's sha, date and subject. When no committed version
/// matches (the row was recorded from an uncommitted tree) it says so and diffs
/// from the version at the commit that last touched the row in `roster_file`
/// instead — which is NOT the text that was read, and the report says that too.
pub(crate) fn prose_diff_report(
    root: &Path,
    path: &str,
    surfaces: &[SurfaceRow<'_>],
    not_help: &[NotHelpRow<'_>],
    roster_file: &str,
) -> (bool, String) {
    let rel = workspace_rel(root, path);
    let mut log = String::new();
    let _ = writeln!(
        log,
        "=== gate help-surfaces --diff {rel} (the prose changed since the recorded read) ==="
    );
    let Some(&(_, recorded, verified, _)) = surfaces.iter().find(|r| r.0 == rel) else {
        let why = match not_help.iter().find(|r| r.0 == rel) {
            Some((_, reason)) => {
                format!("is in NOT_HELP ({reason}), so none of its prose is hashed")
            }
            None => "has no row in SURFACES, so no read is recorded to diff from".to_string(),
        };
        let _ = writeln!(
            log,
            "gate help-surfaces --diff: {rel} {why} — nothing to diff"
        );
        return (false, log);
    };
    let now_src = match std::fs::read_to_string(root.join(&rel)) {
        Ok(src) => src,
        Err(e) => {
            let _ = writeln!(
                log,
                "gate help-surfaces --diff: cannot read {rel}: {e} — a rostered file that is \
                 gone is R3, and there is nothing to diff"
            );
            return (false, log);
        }
    };
    let now_hash = source_prose_hash(&now_src);
    let _ = writeln!(log, "recorded: {recorded}, read on {verified}");
    if now_hash == recorded {
        let _ = writeln!(
            log,
            "now:      {now_hash} in the working tree — the same prose, so the row is green \
             and nothing changed since the read"
        );
        return (true, log);
    }
    let _ = writeln!(
        log,
        "now:      {now_hash} in the working tree — changed, so the gate refuses the row R2"
    );
    let (old_label, old_src) = match diff_base(root, &rel, recorded, roster_file, &mut log) {
        Ok(base) => base,
        Err(e) => {
            let _ = writeln!(log, "gate help-surfaces --diff: COULD NOT RUN — {e}");
            return (false, log);
        }
    };
    let diff = unified_diff(
        &old_label,
        &format!("{rel} (working tree)"),
        &prose_lines_for(Path::new(&rel), &old_src),
        &prose_lines_for(Path::new(&rel), &now_src),
        3,
    );
    if diff.hunks == 0 {
        let _ = writeln!(
            log,
            "gate help-surfaces --diff: the working tree's prose is that version's, line for \
             line — what the row recorded is in no committed version, so re-read the file whole"
        );
    } else {
        log.push_str(&diff.text);
        let _ = writeln!(
            log,
            "gate help-surfaces --diff: {} hunk(s), {} prose line(s) removed, {} added — read \
             them against their handlers, then record the row `xtask gate help-surfaces` prints",
            diff.hunks, diff.removed, diff.added
        );
    }
    (true, log)
}

/// The version `--diff` diffs from, as `(label, source)`, saying into `log` how
/// it was chosen.
fn diff_base(
    root: &Path,
    rel: &str,
    recorded: &str,
    roster_file: &str,
    log: &mut String,
) -> Result<(String, String), String> {
    let listing = git_stdout(root, &["log", "--format=%H", "--", rel])?;
    let shas: Vec<&str> = listing.lines().filter(|l| !l.is_empty()).collect();
    let mut blobs = Blobs::spawn(root)?;
    for (walked, sha) in shas.iter().enumerate() {
        let Some(src) = blobs.read(&format!("{sha}:{rel}"))? else {
            continue;
        };
        if source_prose_hash(&src) == recorded {
            let _ = writeln!(log, "matched:  {}", describe_commit(root, sha)?);
            let _ = writeln!(
                log,
                "          the newest commit whose version of {rel} hashes to the recorded value \
                 ({} of the {} commit(s) that touch it walked)",
                walked + 1,
                shas.len()
            );
            note_changes_since(root, rel, &shas[..walked], log)?;
            return Ok((
                format!("{rel} @ {} (the recorded read)", short_sha(sha)),
                src,
            ));
        }
    }
    let _ = writeln!(
        log,
        "matched:  none — no committed version of {rel} hashes to {recorded} (all {} commit(s) \
         that touch it walked): the row was recorded from an uncommitted tree",
        shas.len()
    );
    let (spec, label) = match row_commit(root, roster_file, rel)? {
        Some(sha) => {
            let _ = writeln!(log, "fallback: {}", describe_commit(root, &sha)?);
            let _ = writeln!(
                log,
                "          the commit that last touched {rel}'s row in {roster_file}. Its version \
                 is NOT the text that was read: prose edited between that read and this commit \
                 does not show below"
            );
            (
                format!("{sha}:{rel}"),
                format!(
                    "{rel} @ {} (the commit that last touched the row)",
                    short_sha(&sha)
                ),
            )
        }
        None => {
            let _ = writeln!(
                log,
                "fallback: HEAD — no line of {rel}'s row in {roster_file} is committed yet"
            );
            (format!("HEAD:{rel}"), format!("{rel} @ HEAD"))
        }
    };
    if let Some(src) = blobs.read(&spec)? {
        return Ok((label, src));
    }
    let _ = writeln!(
        log,
        "          {rel} does not exist in that version, so every prose line below is new"
    );
    Ok((format!("{label}, absent"), String::new()))
}

/// Who moved the prose since the match: the `newer` commits that touch `rel`
/// (newest first, the first [`SINCE_LISTED`]) and the working tree's own edits.
fn note_changes_since(
    root: &Path,
    rel: &str,
    newer: &[&str],
    log: &mut String,
) -> Result<(), String> {
    if !newer.is_empty() {
        let listed = &newer[..newer.len().min(SINCE_LISTED)];
        let shown = git_stdout(
            root,
            &[&["show", "-s", "--format=%h %cs %s"][..], listed].concat(),
        )?;
        let _ = writeln!(
            log,
            "since:    {} commit(s) touch {rel} after it, newest first:",
            newer.len()
        );
        for line in shown.lines().filter(|l| !l.is_empty()) {
            let _ = writeln!(log, "          {line}");
        }
        if newer.len() > listed.len() {
            let _ = writeln!(
                log,
                "          ... and {} older ones (git log -- {rel})",
                newer.len() - listed.len()
            );
        }
    }
    let dirty = !git_stdout(root, &["status", "--porcelain", "--", rel])?
        .trim()
        .is_empty();
    if dirty {
        let lead = if newer.is_empty() {
            "since:    only"
        } else {
            "          and"
        };
        let _ = writeln!(
            log,
            "{lead} the working tree's own uncommitted edits to {rel}"
        );
    }
    Ok(())
}

/// How many of the commits since the match `--diff` names.
/// `--drift`: which read-verified surfaces describe code that MOVED since they
/// were read. A REPORT, never a refusal.
///
/// Why not a refusal. These repos advance dozens of commits a day, so a gate
/// keyed on upstream motion would be permanently red and would teach
/// `--no-verify` — which costs more than the drift it names. The gate refuses an
/// unread or changed SURFACE; this verb hands the next sweep its worklist.
///
/// A row's read is anchored the way `--diff` anchors it: the newest commit whose
/// version of the file hashes to the row, falling back to the commit that last
/// touched the row in `roster_file` (and saying which). Its subject is its own
/// package — the nearest `Cargo.toml` above it — plus every package
/// [`DESCRIBES`] adds for it. A subject with commits in `read..HEAD` is drift.
fn drift_report(
    root: &Path,
    surfaces: &[SurfaceRow<'_>],
    describes: &[DescribesRow<'_>],
    swept: &[SweptRow<'_>],
    roster_file: &str,
) -> (bool, String) {
    let mut out = String::new();
    let mut blobs = match Blobs::spawn(root) {
        Ok(b) => b,
        Err(e) => {
            let _ = writeln!(out, "drift: {e}");
            return (false, out);
        }
    };
    let mut moved = 0usize;
    let mut cross = 0usize;
    for (rel, recorded, date, _) in surfaces {
        let anchor = match read_anchor(root, rel, recorded, roster_file, &mut blobs) {
            Ok(a) => a,
            Err(e) => {
                let _ = writeln!(out, "drift: {rel}: {e}");
                return (false, out);
            }
        };
        let anchor = match swept.iter().find(|(p, _)| p == rel) {
            // The later of the two bounds wins: a sweep that ran BEFORE the row
            // was last touched answers a question the row already supersedes.
            Some((_, bound)) => match anchor {
                Some((sha, exact)) => match is_ancestor(root, &sha, bound) {
                    Ok(true) => Some(((*bound).to_string(), true)),
                    Ok(false) => Some((sha, exact)),
                    Err(e) => {
                        let _ = writeln!(out, "drift: {rel}: {e}");
                        return (false, out);
                    }
                },
                None => Some(((*bound).to_string(), true)),
            },
            None => anchor,
        };
        let Some((sha, exact)) = anchor else {
            let _ = writeln!(
                out,
                "drift: {rel} (read {date}) — no commit anchors this read, so nothing can be \
                 diffed against it; re-read it",
            );
            moved += 1;
            continue;
        };
        let own = own_package(root, rel);
        let extra: Vec<&str> = describes
            .iter()
            .filter(|(p, _)| p == rel)
            .flat_map(|(_, subs)| subs.iter().copied())
            .filter(|s| *s != own)
            .collect();
        if !extra.is_empty() {
            cross += 1;
        }
        let mut hits: Vec<(String, usize)> = Vec::new();
        for subject in std::iter::once(own.as_str()).chain(extra.iter().copied()) {
            match commits_touching(root, &sha, subject) {
                Ok(0) => {}
                Ok(n) => hits.push((subject.to_string(), n)),
                Err(e) => {
                    let _ = writeln!(out, "drift: {rel}: {e}");
                    return (false, out);
                }
            }
        }
        if hits.is_empty() {
            continue;
        }
        moved += 1;
        let subjects = hits
            .iter()
            .map(|(s, n)| format!("{s} ({n} commit(s))"))
            .collect::<Vec<_>>()
            .join(", ");
        let how = if exact {
            "row recorded"
        } else {
            "prose last moved"
        };
        let _ = writeln!(
            out,
            "drift: {rel} ({how} {date} @ {}) — subject moved: {subjects}",
            short_sha(&sha),
        );
    }
    let _ = writeln!(
        out,
        "drift: {moved} of {} surface(s) describe code that moved since they were read \
         ({cross} row(s) declare a cross-crate subject beyond their own)",
        surfaces.len(),
    );
    (true, out)
}

/// The commit a row's read is anchored to: `(sha, exact)`.
///
/// FIRST the commit that last touched the row in `roster_file`: a row is a dated
/// assertion, and the commit that RECORDED it is when the read happened. Failing
/// that, the newest commit whose version of the file hashes to the row — the
/// text that was read, though it may have been committed long before the
/// reading, which is why it is the fallback and not the first choice. Measured
/// 2026-09-14: taking the hash match first left `drive-aterm-skill.md` anchored
/// three commits before the row that re-read it, so the sweep's own work showed
/// as drift.
fn read_anchor(
    root: &Path,
    rel: &str,
    recorded: &str,
    roster_file: &str,
    blobs: &mut Blobs,
) -> Result<Option<(String, bool)>, String> {
    if let Some(sha) = row_commit(root, roster_file, rel)? {
        return Ok(Some((sha, true)));
    }
    let listing = git_stdout(root, &["log", "--format=%H", "--", rel])?;
    for sha in listing.lines().filter(|l| !l.is_empty()) {
        if let Some(src) = blobs.read(&format!("{sha}:{rel}"))?
            && source_prose_hash(&src) == recorded
        {
            return Ok(Some((sha.to_string(), false)));
        }
    }
    Ok(None)
}

/// Is `a` an ancestor of (or equal to) `b`?
fn is_ancestor(root: &Path, a: &str, b: &str) -> Result<bool, String> {
    let out = std::process::Command::new("git")
        .arg("-C")
        .arg(root)
        .args(["merge-base", "--is-ancestor", a, b])
        .output()
        .map_err(|e| format!("could not run `git merge-base`: {e}"))?;
    Ok(out.status.success())
}

/// How many commits in `since..HEAD` touch `subject`.
fn commits_touching(root: &Path, since: &str, subject: &str) -> Result<usize, String> {
    let listing = git_stdout(
        root,
        &[
            "log",
            "--format=%H",
            &format!("{since}..HEAD"),
            "--",
            subject,
        ],
    )?;
    Ok(listing.lines().filter(|l| !l.is_empty()).count())
}

/// The package `rel` belongs to: the nearest ancestor directory holding a
/// `Cargo.toml`, workspace-relative. The workspace root itself when there is
/// none above it.
fn own_package(root: &Path, rel: &str) -> String {
    let mut dir = Path::new(rel).parent();
    while let Some(d) = dir {
        if d.as_os_str().is_empty() {
            break;
        }
        if root.join(d).join("Cargo.toml").is_file() {
            return d.to_string_lossy().replace('\\', "/");
        }
        dir = d.parent();
    }
    ".".to_string()
}

const SINCE_LISTED: usize = 10;

/// The commit that last touched `rel`'s row in `roster_file`: of the commits
/// `git blame` gives the row's lines (its path line through its closing `),`),
/// the newest by committer time. `None` when no line of the row is committed.
fn row_commit(root: &Path, roster_file: &str, rel: &str) -> Result<Option<String>, String> {
    let text = std::fs::read_to_string(root.join(roster_file))
        .map_err(|e| format!("cannot read {roster_file}: {e}"))?;
    let (first, last) =
        row_line_span(&text, rel).ok_or_else(|| format!("{roster_file} has no row for {rel}"))?;
    let porcelain = git_stdout(
        root,
        &[
            "blame",
            "--porcelain",
            "-L",
            &format!("{first},{last}"),
            "--",
            roster_file,
        ],
    )?;
    Ok(newest_blamed_commit(&porcelain))
}

/// The 1-based, inclusive line span of `rel`'s row in the roster text: from the
/// line that is exactly its quoted path to the next `),`, searched from the
/// roster's begin marker when there is one.
fn row_line_span(text: &str, rel: &str) -> Option<(usize, usize)> {
    let quoted = format!("\"{rel}\",");
    let lines: Vec<&str> = text.lines().collect();
    let from = lines
        .iter()
        .position(|l| l.trim() == "// HELP_SURFACES_ROSTER_BEGIN")
        .unwrap_or(0);
    let begin = from + lines[from..].iter().position(|l| l.trim() == quoted)?;
    let end = begin + lines[begin..].iter().position(|l| l.trim() == "),")?;
    Some((begin + 1, end + 1))
}

/// Of the commits a `git blame --porcelain` transcript gives lines to, the newest
/// by committer time — the all-zero id of a line not committed yet skipped.
fn newest_blamed_commit(porcelain: &str) -> Option<String> {
    let mut current: Option<&str> = None;
    let mut newest: Option<(i64, &str)> = None;
    for line in porcelain.lines() {
        if line.starts_with('\t') {
            continue;
        }
        let first = line.split(' ').next().unwrap_or("");
        if matches!(first.len(), 40 | 64)
            && first.bytes().all(|b| b.is_ascii_hexdigit())
            && line.len() > first.len()
        {
            current = Some(first);
        } else if let Some(t) = line.strip_prefix("committer-time ")
            && let (Some(sha), Ok(t)) = (current, t.trim().parse::<i64>())
            && sha.bytes().any(|b| b != b'0')
            && newest.is_none_or(|(seen, _)| t > seen)
        {
            newest = Some((t, sha));
        }
    }
    newest.map(|(_, sha)| sha.to_string())
}

/// `path` as the roster spells it: workspace-relative, `/`-separated, no `./`.
fn workspace_rel(root: &Path, path: &str) -> String {
    let p = Path::new(path);
    let rel = p
        .strip_prefix(root)
        .unwrap_or(p)
        .to_string_lossy()
        .replace('\\', "/");
    rel.strip_prefix("./").map_or(rel.clone(), str::to_string)
}

fn short_sha(sha: &str) -> &str {
    sha.get(..12).unwrap_or(sha)
}

/// `<sha> <committer date> <subject>`.
fn describe_commit(root: &Path, sha: &str) -> Result<String, String> {
    Ok(
        git_stdout(root, &["show", "-s", "--format=%H %ci %s", sha])?
            .trim_end()
            .to_string(),
    )
}

fn git_stdout(root: &Path, args: &[&str]) -> Result<String, String> {
    let out = std::process::Command::new("git")
        .arg("-C")
        .arg(root)
        .args(args)
        .output()
        .map_err(|e| format!("could not run `git {}`: {e}", args.join(" ")))?;
    if !out.status.success() {
        return Err(format!(
            "`git {}` exited {:?} in {}: {}",
            args.join(" "),
            out.status.code(),
            root.display(),
            String::from_utf8_lossy(&out.stderr).trim_end()
        ));
    }
    Ok(String::from_utf8_lossy(&out.stdout).into_owned())
}

/// One `git cat-file --batch` for the whole walk: a file with hundreds of
/// commits costs one spawn, not hundreds.
struct Blobs {
    child: std::process::Child,
    stdin: Option<std::process::ChildStdin>,
    out: std::io::BufReader<std::process::ChildStdout>,
}

impl Blobs {
    fn spawn(root: &Path) -> Result<Self, String> {
        use std::process::Stdio;
        let mut child = std::process::Command::new("git")
            .arg("-C")
            .arg(root)
            .args(["cat-file", "--batch"])
            .stdin(Stdio::piped())
            .stdout(Stdio::piped())
            .stderr(Stdio::null())
            .spawn()
            .map_err(|e| format!("could not run `git cat-file --batch`: {e}"))?;
        let stdin = child.stdin.take();
        let Some(out) = child.stdout.take() else {
            let _ = child.kill();
            let _ = child.wait();
            return Err("`git cat-file --batch` gave no stdout".to_string());
        };
        Ok(Self {
            child,
            stdin,
            out: std::io::BufReader::new(out),
        })
    }

    /// The text of `<rev>:<path>`, or `None` when that version has no such file.
    fn read(&mut self, spec: &str) -> Result<Option<String>, String> {
        use std::io::{BufRead as _, Read as _, Write as _};
        let broken = |e: std::io::Error| format!("`git cat-file --batch` broke on {spec}: {e}");
        let stdin = self
            .stdin
            .as_mut()
            .ok_or_else(|| "`git cat-file --batch` has no stdin".to_string())?;
        writeln!(stdin, "{spec}").map_err(broken)?;
        stdin.flush().map_err(broken)?;
        let mut header = String::new();
        if self.out.read_line(&mut header).map_err(broken)? == 0 {
            return Err(format!(
                "`git cat-file --batch` ended before answering {spec}"
            ));
        }
        // `<oid> <type> <size>`, or `<spec> missing` (and `ambiguous`) with no
        // body — read off the END, since a spec can hold a space.
        let header = header.trim_end();
        if header.ends_with(" missing") || header.ends_with(" ambiguous") {
            return Ok(None);
        }
        let fields: Vec<&str> = header.split(' ').collect();
        let [_, kind, size] = fields.as_slice() else {
            return Err(format!(
                "`git cat-file --batch` answered {header:?} for {spec}"
            ));
        };
        let size: usize = size
            .parse()
            .map_err(|e| format!("`git cat-file --batch` header {header:?}: {e}"))?;
        let mut body = vec![0u8; size + 1];
        self.out.read_exact(&mut body).map_err(broken)?;
        body.pop();
        if *kind != "blob" {
            return Ok(None);
        }
        Ok(Some(String::from_utf8_lossy(&body).into_owned()))
    }
}

impl Drop for Blobs {
    fn drop(&mut self) {
        drop(self.stdin.take());
        let _ = self.child.wait();
    }
}

/// The prose as [`prose_hash`] sees it, as lines to diff: every string literal,
/// then every doc comment — the order the hash feeds them — one item per line.
/// See [`prose_lines_of`] for an item whose text breaks.
pub(crate) fn prose_lines(src: &str) -> Vec<String> {
    let (strings, docs) = prose_of_rust(src);
    prose_lines_of(&strings, &docs)
}

/// [`prose_lines`] over items already extracted. `str>` opens a literal and
/// `doc>` a doc comment; an item whose text breaks — a newline in it, or a `\n`
/// escape in a literal — continues on `str|`/`doc|` lines, so a 200-line manual
/// page diffs by the line that changed rather than as one line. A literal breaks
/// AFTER its `\n` escape (the escape stays, visible), and one that ends in a `\n`
/// escape gets no empty last line. A piece longer than [`WRAP_AT`] bytes — a
/// catalog row continued over forty source lines is one piece of 5 KB — is also
/// soft-wrapped after each `. `, continuing on `str~`/`doc~` lines that rejoin
/// with one space, so an added sentence shows as that sentence. Every break is
/// recoverable from the lines, so two sources render the same lines exactly when
/// they hash the same, and a diff of these lines is empty exactly when the gate
/// sees no change.
pub(crate) fn prose_lines_of(strings: &[String], docs: &[String]) -> Vec<String> {
    let mut lines = Vec::new();
    for s in strings {
        push_prose_item(&mut lines, "str", s, true);
    }
    for d in docs {
        push_prose_item(&mut lines, "doc", d, false);
    }
    lines
}

/// A piece of an item longer than this many bytes is soft-wrapped at its
/// sentence ends (see [`prose_lines_of`]); a manual page's lines stay whole.
const WRAP_AT: usize = 100;

fn push_prose_item(lines: &mut Vec<String>, tag: &str, text: &str, escapes: bool) {
    let mut mark = '>';
    let mut emit = |piece: &str| {
        let sentences: Vec<&str> = if piece.len() > WRAP_AT {
            piece.split(". ").collect()
        } else {
            vec![piece]
        };
        for (k, sentence) in sentences.iter().enumerate() {
            let m = if k == 0 { mark } else { '~' };
            let dot = if k + 1 < sentences.len() { "." } else { "" };
            lines.push(if sentence.is_empty() && dot.is_empty() {
                format!("{tag}{m}")
            } else {
                format!("{tag}{m} {sentence}{dot}")
            });
        }
        mark = '|';
    };
    let mut start = 0;
    let mut after_escape = false;
    let mut chars = text.char_indices().peekable();
    while let Some((i, c)) = chars.next() {
        if c == '\n' {
            emit(&text[start..i]);
            start = i + 1;
            after_escape = false;
        } else if c == '\\' && escapes {
            match chars.peek() {
                Some(&(j, 'n')) => {
                    chars.next();
                    emit(&text[start..=j]);
                    start = j + 1;
                    after_escape = true;
                }
                // A backslash that ends a raw string's line escapes nothing: the
                // newline after it breaks on its own.
                Some(&(_, '\n')) | None => {}
                Some(_) => {
                    chars.next();
                }
            }
        }
    }
    if start < text.len() || !after_escape {
        emit(&text[start..]);
    }
}

/// What [`unified_diff`] found: the text (empty when the sides are equal) and
/// its tallies.
#[derive(Debug, Default)]
pub(crate) struct LineDiff {
    pub(crate) text: String,
    pub(crate) hunks: usize,
    pub(crate) removed: usize,
    pub(crate) added: usize,
}

/// A unified diff of `old` -> `new` with `context` lines around each change:
/// `---`/`+++` headed, `@@ -a,b +c,d @@` hunks (a count of 1 omitted, as git
/// does), a change's removals before its additions. The edit is a shortest one
/// ([`edit_script`]). Empty, with zero tallies, when the lines are equal.
pub(crate) fn unified_diff(
    old_label: &str,
    new_label: &str,
    old: &[String],
    new: &[String],
    context: usize,
) -> LineDiff {
    let mut ids = std::collections::HashMap::new();
    let a: Vec<u32> = old.iter().map(|s| intern(&mut ids, s)).collect();
    let b: Vec<u32> = new.iter().map(|s| intern(&mut ids, s)).collect();
    // Each edit with the old and new index it applies at.
    let mut at = Vec::with_capacity(a.len() + b.len());
    let (mut i, mut j) = (0, 0);
    for e in edit_script(&a, &b) {
        at.push((e, i, j));
        match e {
            Edit::Keep => (i, j) = (i + 1, j + 1),
            Edit::Del => i += 1,
            Edit::Ins => j += 1,
        }
    }
    let changed: Vec<usize> = (0..at.len()).filter(|&k| at[k].0 != Edit::Keep).collect();
    let mut diff = LineDiff::default();
    if changed.is_empty() {
        return diff;
    }
    let _ = writeln!(diff.text, "--- {old_label}\n+++ {new_label}");
    let mut g = 0;
    while g < changed.len() {
        let mut h = g;
        while h + 1 < changed.len() && changed[h + 1] - changed[h] - 1 <= 2 * context {
            h += 1;
        }
        let span =
            &at[changed[g].saturating_sub(context)..(changed[h] + context + 1).min(at.len())];
        let old_n = span.iter().filter(|x| x.0 != Edit::Ins).count();
        let new_n = span.iter().filter(|x| x.0 != Edit::Del).count();
        let _ = writeln!(
            diff.text,
            "@@ -{} +{} @@",
            hunk_range(span[0].1, old_n),
            hunk_range(span[0].2, new_n)
        );
        let mut k = 0;
        while k < span.len() {
            if span[k].0 == Edit::Keep {
                let _ = writeln!(diff.text, " {}", old[span[k].1]);
                k += 1;
                continue;
            }
            let run = &span[k..k + span[k..].iter().take_while(|x| x.0 != Edit::Keep).count()];
            for &(_, oi, _) in run.iter().filter(|x| x.0 == Edit::Del) {
                let _ = writeln!(diff.text, "-{}", old[oi]);
                diff.removed += 1;
            }
            for &(_, _, ni) in run.iter().filter(|x| x.0 == Edit::Ins) {
                let _ = writeln!(diff.text, "+{}", new[ni]);
                diff.added += 1;
            }
            k += run.len();
        }
        diff.hunks += 1;
        g = h + 1;
    }
    diff
}

fn intern<'a>(ids: &mut std::collections::HashMap<&'a str, u32>, s: &'a str) -> u32 {
    let next = u32::try_from(ids.len()).unwrap_or(u32::MAX);
    *ids.entry(s).or_insert(next)
}

/// A hunk side's `start,count`: 1-based, except that an empty side names the
/// line before it (`0,0` at the top), and a count of 1 is omitted.
fn hunk_range(index: usize, count: usize) -> String {
    match count {
        0 => format!("{index},0"),
        1 => format!("{}", index + 1),
        n => format!("{},{n}", index + 1),
    }
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum Edit {
    Keep,
    Del,
    Ins,
}

/// A shortest edit script from `a` to `b` — its `Keep`s are a longest common
/// subsequence — by Hirschberg's divide and conquer: linear space, and the
/// common prefix and suffix peeled at every level cost nothing.
fn edit_script(a: &[u32], b: &[u32]) -> Vec<Edit> {
    let mut out = Vec::with_capacity(a.len() + b.len());
    hirschberg(a, b, &mut out);
    out
}

fn hirschberg(a: &[u32], b: &[u32], out: &mut Vec<Edit>) {
    use std::iter::repeat_n;
    let pre = a.iter().zip(b).take_while(|(x, y)| x == y).count();
    out.extend(repeat_n(Edit::Keep, pre));
    let (a, b) = (&a[pre..], &b[pre..]);
    let suf = a
        .iter()
        .rev()
        .zip(b.iter().rev())
        .take_while(|(x, y)| x == y)
        .count();
    let (a, b) = (&a[..a.len() - suf], &b[..b.len() - suf]);
    if a.is_empty() || b.is_empty() {
        out.extend(repeat_n(Edit::Del, a.len()));
        out.extend(repeat_n(Edit::Ins, b.len()));
    } else if a.len() == 1 {
        if let Some(j) = b.iter().position(|y| *y == a[0]) {
            out.extend(repeat_n(Edit::Ins, j));
            out.push(Edit::Keep);
            out.extend(repeat_n(Edit::Ins, b.len() - j - 1));
        } else {
            out.push(Edit::Del);
            out.extend(repeat_n(Edit::Ins, b.len()));
        }
    } else {
        let mid = a.len() / 2;
        let fwd = lcs_prefix_row(&a[..mid], b);
        let bwd = lcs_suffix_row(&a[mid..], b);
        let split = (0..=b.len())
            .max_by_key(|&k| (fwd[k] + bwd[k], std::cmp::Reverse(k)))
            .unwrap_or(0);
        hirschberg(&a[..mid], &b[..split], out);
        hirschberg(&a[mid..], &b[split..], out);
    }
    out.extend(repeat_n(Edit::Keep, suf));
}

/// `row[k]` = the LCS length of `a` and `b[..k]`.
fn lcs_prefix_row(a: &[u32], b: &[u32]) -> Vec<usize> {
    let mut prev = vec![0; b.len() + 1];
    let mut cur = vec![0; b.len() + 1];
    for x in a {
        for (k, y) in b.iter().enumerate() {
            cur[k + 1] = if x == y {
                prev[k] + 1
            } else {
                prev[k + 1].max(cur[k])
            };
        }
        std::mem::swap(&mut prev, &mut cur);
    }
    prev
}

/// `row[k]` = the LCS length of `a` and `b[k..]`.
fn lcs_suffix_row(a: &[u32], b: &[u32]) -> Vec<usize> {
    let mut prev = vec![0; b.len() + 1];
    let mut cur = vec![0; b.len() + 1];
    for x in a.iter().rev() {
        for (k, y) in b.iter().enumerate().rev() {
            cur[k] = if x == y {
                prev[k + 1] + 1
            } else {
                prev[k].max(cur[k + 1])
            };
        }
        std::mem::swap(&mut prev, &mut cur);
    }
    prev
}

// ---------------------------------------------------------------------- dates

fn today_days() -> i64 {
    let secs = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map_or(0, |d| d.as_secs());
    i64::try_from(secs / 86_400).unwrap_or(0)
}

/// `YYYY-MM-DD` -> days since 1970-01-01 (proleptic Gregorian); `None` when
/// malformed.
fn days_from_iso(s: &str) -> Option<i64> {
    let b = s.as_bytes();
    if b.len() != 10 || b[4] != b'-' || b[7] != b'-' {
        return None;
    }
    let y: i64 = s[0..4].parse().ok()?;
    let m: i64 = s[5..7].parse().ok()?;
    let d: i64 = s[8..10].parse().ok()?;
    if !(1..=12).contains(&m) || !(1..=31).contains(&d) {
        return None;
    }
    // Howard Hinnant's days_from_civil.
    let y = if m <= 2 { y - 1 } else { y };
    let era = if y >= 0 { y } else { y - 399 } / 400;
    let yoe = y - era * 400;
    let mp = (m + 9) % 12;
    let doy = (153 * mp + 2) / 5 + d - 1;
    let doe = yoe * 365 + yoe / 4 - yoe / 100 + doy;
    Some(era * 146_097 + doe - 719_468)
}

/// Inverse of [`days_from_iso`], for the rows the refusal lines print.
fn civil_from_days(z: i64) -> String {
    let z = z + 719_468;
    let era = if z >= 0 { z } else { z - 146_096 } / 146_097;
    let doe = z - era * 146_097;
    let yoe = (doe - doe / 1460 + doe / 36_524 - doe / 146_096) / 365;
    let y = yoe + era * 400;
    let doy = doe - (365 * yoe + yoe / 4 - yoe / 100);
    let mp = (5 * doy + 2) / 153;
    let d = doy - (153 * mp + 2) / 5 + 1;
    let m = if mp < 10 { mp + 3 } else { mp - 9 };
    let y = if m <= 2 { y + 1 } else { y };
    format!("{y:04}-{m:02}-{d:02}")
}

#[cfg(test)]
mod tests {
    use super::*;

    const SRC: &str = "//! A tool.\nconst USAGE: &str = \"Usage: tool <file> [--fast]\\n\\nOptions:\\n  --fast  skip checks\";\nfn main() { let _c = '\"'; // not a \"string\"\n    /* plain comment \"not prose\" */ println!(\"{USAGE}\"); }\n";
    const OTHER: &str = "fn helper() -> u8 { 7 } // no help here\n";
    const TODAY: i64 = 20_706; // 2026-09-10

    fn tree(label: &str) -> PathBuf {
        let root = std::env::temp_dir().join(format!(
            "xtask-help-surfaces-{}-{label}",
            std::process::id()
        ));
        let _ = std::fs::remove_dir_all(&root);
        std::fs::create_dir_all(root.join("crates/t/src")).unwrap();
        std::fs::write(
            root.join("crates/t/Cargo.toml"),
            "[package]\nname = \"t\"\n",
        )
        .unwrap();
        std::fs::write(root.join("crates/t/src/main.rs"), SRC).unwrap();
        std::fs::write(root.join("crates/t/src/helper.rs"), OTHER).unwrap();
        root
    }

    fn tags(refusals: &[String]) -> String {
        refusals
            .iter()
            .map(|r| r.split_whitespace().next().unwrap_or("").to_string())
            .collect::<Vec<_>>()
            .join(" ")
    }

    /// The verb-level red fixture: the REAL check over a temp tree, GREEN on a
    /// read row and RED — with the rule named — on every way a roster can lie.
    #[test]
    fn a_changed_or_unrostered_help_surface_fails_the_help_surfaces_verb() {
        let root = tree("verb");
        let hash = read_prose_hash(&root.join("crates/t/src/main.rs")).unwrap();
        let row = (
            "crates/t/src/main.rs",
            hash.as_str(),
            "2026-09-10",
            "fixture",
        );
        let check = |surfaces: &[SurfaceRow<'_>], not_help: &[NotHelpRow<'_>]| {
            help_surfaces_check(&root, &["crates"], surfaces, not_help, 180, TODAY)
        };

        assert!(
            check(&[row], &[]).is_empty(),
            "control: a read row is green"
        );
        let unrostered = check(&[], &[]);
        assert!(!unrostered.is_empty(), "no row at all must be RED");
        assert!(tags(&unrostered).contains("R1"), "{unrostered:?}");
        assert!(
            tags(&check(&[row], &[("crates/t/src/helper.rs", "planted")])).contains("R5"),
            "NOT_HELP naming a file discovery does not find"
        );
        assert!(
            tags(&check(&[row, row], &[])).contains("R6"),
            "the same path rostered twice"
        );
        assert!(
            tags(&check(
                &[("crates/t/src/main.rs", &hash, "2025-01-01", "f")],
                &[]
            ))
            .contains("R4"),
            "a row older than the limit"
        );
        assert!(
            tags(&check(
                &[("crates/t/src/main.rs", &hash, "yesterday", "f")],
                &[]
            ))
            .contains("R4"),
            "a malformed date"
        );

        // A code-only edit leaves the row green; a help-literal edit turns it red.
        std::fs::write(
            root.join("crates/t/src/main.rs"),
            SRC.replace("let _c", "let _d"),
        )
        .unwrap();
        assert!(check(&[row], &[]).is_empty(), "code-only edit: still green");
        std::fs::write(
            root.join("crates/t/src/main.rs"),
            SRC.replace("skip checks", "skip nothing"),
        )
        .unwrap();
        let red = check(&[row], &[]);
        assert!(!red.is_empty(), "an edited help literal must be RED");
        assert!(tags(&red).contains("R2"), "edited help literal: {red:?}");
        assert!(
            red.iter().any(|r| r.contains("2026-09-10")),
            "the refusal carries today's row to paste: {red:?}"
        );
        assert!(
            red.iter().any(|r| r.starts_with("R2")
                && r.ends_with("(see: xtask gate help-surfaces --diff crates/t/src/main.rs)")),
            "the R2 line ends by naming the verb that shows what changed: {red:?}"
        );

        // A program with no help text and no row: R7, cleared by a NOT_HELP row
        // that R5 must not then refuse.
        std::fs::write(root.join("crates/t/src/main.rs"), SRC).unwrap();
        std::fs::create_dir_all(root.join("crates/t/src/bin")).unwrap();
        std::fs::write(
            root.join("crates/t/src/bin/quiet.rs"),
            "fn main() { let _ = std::env::args(); }\n",
        )
        .unwrap();
        let quiet = check(&[row], &[]);
        assert!(!quiet.is_empty(), "an unaccounted program must be RED");
        assert!(tags(&quiet).contains("R7"), "{quiet:?}");
        assert!(
            check(
                &[row],
                &[("crates/t/src/bin/quiet.rs", "takes no arguments")]
            )
            .is_empty(),
            "a NOT_HELP row accounts for a program"
        );
        std::fs::remove_file(root.join("crates/t/src/bin/quiet.rs")).unwrap();

        // A non-`.rs` file the surface COMPILES IN: R8, because the lexer sees
        // only the path at the macro, so the asset's text is in no hash. A
        // `.rs` include is NOT R8's question — the `*.rs` walk already reaches
        // it — and a NOT_HELP row clears the real one without R5 refusing it.
        std::fs::create_dir_all(root.join("crates/t/assets")).unwrap();
        std::fs::write(
            root.join("crates/t/assets/guide.md"),
            "Usage: tool <file>\n",
        )
        .unwrap();
        std::fs::write(root.join("crates/t/assets/scan.rs"), "fn scanned() {}\n").unwrap();
        let with_rs_include = SRC.replace(
            "fn main()",
            "const S: &str = include_str!(\"../assets/scan.rs\");\nfn main()",
        );
        std::fs::write(root.join("crates/t/src/main.rs"), &with_rs_include).unwrap();
        let rs_hash = read_prose_hash(&root.join("crates/t/src/main.rs")).unwrap();
        let rs_row = (
            "crates/t/src/main.rs",
            rs_hash.as_str(),
            "2026-09-13",
            "fixture",
        );
        assert!(
            check(&[rs_row], &[]).is_empty(),
            "a .rs include is R1's question, not R8's"
        );
        let both = with_rs_include.replace(
            "fn main()",
            "const G: &str = include_str!(\"../assets/guide.md\");\nfn main()",
        );
        std::fs::write(root.join("crates/t/src/main.rs"), &both).unwrap();
        let both_hash = read_prose_hash(&root.join("crates/t/src/main.rs")).unwrap();
        let both_row = (
            "crates/t/src/main.rs",
            both_hash.as_str(),
            "2026-09-13",
            "fixture",
        );
        let embedded = check(&[both_row], &[]);
        assert!(
            !embedded.is_empty(),
            "an unaccounted embedded file must be RED"
        );
        assert!(tags(&embedded).contains("R8"), "{embedded:?}");
        assert!(
            check(&[both_row], &[("crates/t/assets/guide.md", "planted")]).is_empty(),
            "a NOT_HELP row accounts for an embedded file"
        );
        std::fs::write(root.join("crates/t/src/main.rs"), SRC).unwrap();
        std::fs::remove_dir_all(root.join("crates/t/assets")).unwrap();

        std::fs::remove_file(root.join("crates/t/src/main.rs")).unwrap();
        assert!(
            tags(&check(&[row], &[])).contains("R3"),
            "rostered file removed"
        );
        let _ = std::fs::remove_dir_all(&root);
    }

    /// The gate itself, in the test lane: the workspace roster is complete and
    /// current. A red here names the surface and prints the row a fresh read
    /// should record.
    /// **A DOCUMENT IS HASHED BY ITS LINES, a Rust file by its prose.** The
    /// defect this pins: a skill file is text end to end, and the Rust
    /// extractor hashed only its double-quoted fragments — so a rewritten
    /// paragraph outside quotes changed no hash and the gate passed it
    /// (MEASURED 2026-09-22 on supervise-agent-skill.md: 66 fragments,
    /// 591 lines). Both directions are asserted, because a fix that hashed
    /// every file by its lines would stop ignoring Rust CODE.
    #[test]
    fn a_documents_prose_is_every_line_and_a_rust_files_is_not() {
        let root = tree("doc-hash");
        let doc = root.join("crates/t/assets/skill.md");
        std::fs::create_dir_all(doc.parent().unwrap()).unwrap();
        let md = "# A skill\n\nRun `aterm ctl ls` first.\n";
        std::fs::write(&doc, md).unwrap();
        let before = read_prose_hash(&doc).unwrap();
        // A paragraph edited OUTSIDE any quotes: the old extractor saw none of
        // this, and the row stayed green.
        std::fs::write(&doc, "# A skill\n\nRun `aterm ctl windows` first.\n").unwrap();
        let after = read_prose_hash(&doc).unwrap();
        assert_ne!(
            before, after,
            "a document's prose change must move its hash"
        );
        // Trailing whitespace is not a change, so the hash and the `--diff`
        // view cannot disagree about what moved.
        std::fs::write(&doc, "# A skill\n\nRun `aterm ctl windows` first.   \n").unwrap();
        assert_eq!(read_prose_hash(&doc).unwrap(), after);
        assert_eq!(
            prose_lines_for(&doc, md),
            vec!["# A skill", "", "Run `aterm ctl ls` first."],
            "a document diffs by its own lines"
        );

        // A Rust file keeps the prose extractor: its CODE is still ignored.
        let rs = root.join("crates/t/src/other.rs");
        std::fs::write(&rs, "/// Doc.\nfn main() { let x = \"lit\"; }\n").unwrap();
        let rs_before = read_prose_hash(&rs).unwrap();
        std::fs::write(&rs, "/// Doc.\nfn main() { let renamed = \"lit\"; }\n").unwrap();
        assert_eq!(
            read_prose_hash(&rs).unwrap(),
            rs_before,
            "a code-only edit must still leave a Rust row alone"
        );
        std::fs::write(
            &rs,
            "/// Doc, reworded.\nfn main() { let renamed = \"lit\"; }\n",
        )
        .unwrap();
        assert_ne!(read_prose_hash(&rs).unwrap(), rs_before);
        let _ = std::fs::remove_dir_all(&root);
    }

    #[test]
    fn the_workspace_help_surfaces_are_read_verified() {
        let (ok, log) = help_surfaces_report(&crate::workspace_root());
        assert!(ok, "{log}");
    }

    #[test]
    fn extraction_keeps_literals_and_doc_comments_and_drops_the_rest() {
        let (strings, docs) = prose_of_rust(SRC);
        assert_eq!(docs, vec!["A tool.".to_string()]);
        assert_eq!(strings.len(), 2, "{strings:?}");
        assert!(strings[0].starts_with("Usage: tool"));
        assert!(
            !strings
                .iter()
                .any(|s| s.contains("not prose") || s.contains("not a"))
        );
        let (raw, _) = prose_of_rust("let r = r#\"a \"quoted\" b\"#; let c = '\\'';");
        assert_eq!(raw, vec!["a \"quoted\" b".to_string()]);
    }

    #[test]
    fn iso_dates_round_trip() {
        for (iso, days) in [
            ("1970-01-01", 0),
            ("2026-09-10", TODAY),
            ("2000-02-29", 11_016),
        ] {
            assert_eq!(days_from_iso(iso), Some(days), "{iso}");
            assert_eq!(civil_from_days(days), iso);
        }
        assert_eq!(days_from_iso("2026-13-01"), None);
        assert_eq!(days_from_iso("26-09-10"), None);
    }

    /// Rebuilds the items [`prose_lines_of`] rendered — the inverse the doc
    /// promises, which is what makes "the lines differ exactly when the hash
    /// does" true rather than hoped.
    fn items_of_lines(lines: &[String]) -> (Vec<String>, Vec<String>) {
        fn ends_in_escaped_n(line: &str) -> bool {
            let mut chars = line.chars().peekable();
            let mut last_was_n_escape = false;
            while let Some(c) = chars.next() {
                last_was_n_escape = false;
                if c == '\\'
                    && let Some(&next) = chars.peek()
                {
                    chars.next();
                    last_was_n_escape = next == 'n';
                }
            }
            last_was_n_escape
        }
        let (mut strings, mut docs) = (Vec::new(), Vec::new());
        let mut open: Option<(bool, String, bool)> = None; // (doc, text, last line ended in `\n`)
        let close =
            |open: Option<(bool, String, bool)>, s: &mut Vec<String>, d: &mut Vec<String>| {
                if let Some((doc, text, _)) = open {
                    if doc { d.push(text) } else { s.push(text) }
                }
            };
        for line in lines {
            let (tag, rest) = line.split_at(3);
            let mark = rest.chars().next().expect("a mark after the tag");
            let body = rest.get(2..).unwrap_or("");
            let doc = tag == "doc";
            if mark == '>' {
                close(open.take(), &mut strings, &mut docs);
                open = Some((doc, body.to_string(), !doc && ends_in_escaped_n(body)));
            } else {
                let (_, text, escaped) = open.as_mut().expect("a continuation opens nothing");
                if mark == '~' {
                    text.push(' ');
                } else if !*escaped {
                    text.push('\n');
                }
                text.push_str(body);
                *escaped = !doc && ends_in_escaped_n(body);
            }
        }
        close(open, &mut strings, &mut docs);
        (strings, docs)
    }

    /// `--diff`'s split, on the shape a naive grep for quotes misses: a literal
    /// continued across source lines (`\` at the line end) whose middle line has
    /// no quote at all, and a sentence continued with no `\n` in it. Both diff,
    /// by the line that changed.
    #[test]
    fn a_continued_literal_diffs_on_its_quoteless_continuation_line() {
        let old = "const USAGE: &str = \"usage: t <file>\\n\\\n    --fast  skip checks\\n\\\n    \
                   --slow  run all\";\nconst NOTE: &str = \"one sentence that \\\n    continues here\";\n";
        let new = old
            .replace("skip checks", "skip nothing")
            .replace("continues here", "continues there");
        assert_eq!(
            prose_lines(old),
            [
                "str> usage: t <file>\\n",
                "str| --fast  skip checks\\n",
                "str| --slow  run all",
                "str> one sentence that continues here",
            ],
            "one line per `\\n`-ended piece, and a joined continuation is one line"
        );
        let d = unified_diff("old", "new", &prose_lines(old), &prose_lines(&new), 3);
        assert_eq!(
            d.text,
            "--- old\n+++ new\n@@ -1,4 +1,4 @@\n str> usage: t <file>\\n\n-str| --fast  skip checks\\n\n\
             +str| --fast  skip nothing\\n\n str| --slow  run all\n-str> one sentence that continues \
             here\n+str> one sentence that continues there\n"
        );
        assert_eq!((d.hunks, d.removed, d.added), (1, 2, 2));
    }

    #[test]
    fn an_unchanged_or_code_only_edited_file_diffs_empty() {
        let lines = prose_lines(SRC);
        for other in [SRC.to_string(), SRC.replace("let _c", "let _d")] {
            let d = unified_diff("a", "b", &lines, &prose_lines(&other), 3);
            assert!(d.text.is_empty(), "{}", d.text);
            assert_eq!((d.hunks, d.removed, d.added), (0, 0, 0));
        }
    }

    /// A catalog row continued over many source lines is ONE piece of prose; a
    /// sentence added to it shows as that sentence, not as the row twice.
    #[test]
    fn a_sentence_added_to_a_long_catalog_row_shows_as_that_sentence() {
        let row = |extra: &str| {
            format!(
                "const ROW: &str = \"trail [<n>]: the focused window's last verdicts, newest \\\n    \
                 last. `licence=` names the class that admitted the row. {extra}Every observed \\\n    \
                 move is counted exactly once.\";\n"
            )
        };
        let (old, new) = (row(""), row("A held park is judged as one echo. "));
        assert_eq!(
            prose_lines(&old),
            [
                "str> trail [<n>]: the focused window's last verdicts, newest last.",
                "str~ `licence=` names the class that admitted the row.",
                "str~ Every observed move is counted exactly once.",
            ]
        );
        let d = unified_diff("a", "b", &prose_lines(&old), &prose_lines(&new), 3);
        assert_eq!((d.hunks, d.removed, d.added), (1, 0, 1), "{}", d.text);
        assert!(
            d.text
                .contains("\n+str~ A held park is judged as one echo.\n"),
            "{}",
            d.text
        );
    }

    #[test]
    fn a_doc_comment_only_edit_shows_in_the_diff() {
        let old = "/// Frobs the widget.\n/// Twice.\npub fn frob() -> &'static str { \"frob\" }\n";
        let new = old.replace("the widget", "every widget");
        let d = unified_diff("a", "b", &prose_lines(old), &prose_lines(&new), 3);
        assert_eq!(
            d.text,
            "--- a\n+++ b\n@@ -1,3 +1,3 @@\n str> frob\n-doc> Frobs the widget.\n\
             +doc> Frobs every widget.\n doc> Twice.\n"
        );
    }

    /// Every break in an item is recoverable from its lines — tricky texts and
    /// the prose of every rostered surface in this tree alike — so two sources
    /// render the same lines only when the hasher sees the same items.
    #[test]
    fn prose_lines_render_every_item_recoverably() {
        let tricky: Vec<String> = [
            "",
            "\\n",
            "a\\nb",
            "a\\n\nb",
            "a\\n",
            "a\\n\n",
            "a\nb",
            "a\n",
            "\n",
            "a\\\\nb",
            "C:\\",
            "shell \\\n  --flag",
            "tab\\tthen\\n\\nblank",
            "ü\\n€\\n",
            "\\\"quoted\\\"\\n",
        ]
        .iter()
        .map(|s| (*s).to_string())
        .chain([
            format!("{}. {}. ", "a".repeat(60), "b".repeat(60)),
            format!("One. Two has an escape\\n{}. Tail. ", "c".repeat(120)),
            format!("{}\\. then more. {}", "d".repeat(80), "e".repeat(40)),
            format!("{}. . .\\n. x", "f".repeat(101)),
        ])
        .collect();
        for t in &tricky {
            let items = (
                vec![t.clone(), "x".to_string()],
                vec![t.replace("\\n", "n")],
            );
            assert_eq!(
                items_of_lines(&prose_lines_of(&items.0, &items.1)),
                items,
                "{t:?}"
            );
        }
        for (a, b) in [
            (vec!["a\\nb"], vec!["a\\n\nb"]),
            (vec!["ab"], vec!["a", "b"]),
            (vec!["a\\n", ""], vec!["a\\n"]),
        ] {
            let own = |v: Vec<&str>| v.into_iter().map(str::to_string).collect::<Vec<_>>();
            assert_ne!(
                prose_lines_of(&own(a.clone()), &[]),
                prose_lines_of(&own(b.clone()), &[]),
                "{a:?} vs {b:?}"
            );
        }
        let s = vec!["x".to_string()];
        assert_ne!(prose_lines_of(&s, &[]), prose_lines_of(&[], &s));

        let root = crate::workspace_root();
        for (rel, ..) in SURFACES {
            let src = std::fs::read_to_string(root.join(rel)).unwrap();
            let (strings, docs) = prose_of_rust(&src);
            let lines = prose_lines_of(&strings, &docs);
            assert!(
                lines.iter().all(|l| !l.contains('\n')),
                "{rel}: a line holds a newline"
            );
            assert_eq!(items_of_lines(&lines), (strings, docs), "{rel}");
        }
    }

    /// The hunks git would print, and a shortest edit on random pairs: the
    /// script rebuilds `new` from `old`, and its keeps are a longest common
    /// subsequence (checked against the full table).
    #[test]
    fn the_line_diff_is_a_shortest_edit_in_unified_hunks() {
        let old: Vec<String> = (1..=12).map(|n| n.to_string()).collect();
        let mut new = old.clone();
        new[0] = "one".into();
        new[11] = "twelve".into();
        assert_eq!(
            unified_diff("a", "b", &old, &new, 3).text,
            "--- a\n+++ b\n@@ -1,4 +1,4 @@\n-1\n+one\n 2\n 3\n 4\n@@ -9,4 +9,4 @@\n 9\n 10\n 11\n-12\n+twelve\n"
        );
        new[5] = "six".into();
        let one_hunk = unified_diff("a", "b", &old, &new, 3);
        assert_eq!(
            one_hunk.hunks, 1,
            "changes 4 lines apart share a hunk at context 3"
        );
        assert_eq!((one_hunk.removed, one_hunk.added), (3, 3));
        assert_eq!(
            unified_diff("a", "b", &[], &["x".to_string()], 3).text,
            "--- a\n+++ b\n@@ -0,0 +1 @@\n+x\n"
        );

        fn lcs_len(a: &[u32], b: &[u32]) -> usize {
            let mut t = vec![vec![0; b.len() + 1]; a.len() + 1];
            for i in 0..a.len() {
                for j in 0..b.len() {
                    t[i + 1][j + 1] = if a[i] == b[j] {
                        t[i][j] + 1
                    } else {
                        t[i][j + 1].max(t[i + 1][j])
                    };
                }
            }
            t[a.len()][b.len()]
        }
        let mut seed: u64 = 0x2026_0913;
        let mut next = |m: u64| {
            seed = seed
                .wrapping_mul(6_364_136_223_846_793_005)
                .wrapping_add(1_442_695_040_888_963_407);
            (seed >> 33) % m
        };
        for _ in 0..500 {
            let a: Vec<u32> = (0..next(10)).map(|_| next(4) as u32).collect();
            let b: Vec<u32> = (0..next(10)).map(|_| next(4) as u32).collect();
            let (mut i, mut j, mut keeps, mut rebuilt) = (0, 0, 0, Vec::new());
            for e in edit_script(&a, &b) {
                match e {
                    Edit::Keep => {
                        assert_eq!(a[i], b[j], "{a:?} -> {b:?}");
                        rebuilt.push(a[i]);
                        (i, j, keeps) = (i + 1, j + 1, keeps + 1);
                    }
                    Edit::Del => i += 1,
                    Edit::Ins => {
                        rebuilt.push(b[j]);
                        j += 1;
                    }
                }
            }
            assert_eq!((i, j), (a.len(), b.len()), "{a:?} -> {b:?}");
            assert_eq!(rebuilt, b, "{a:?} -> {b:?}");
            assert_eq!(
                keeps,
                lcs_len(&a, &b),
                "{a:?} -> {b:?}: not a shortest edit"
            );
        }
    }

    #[test]
    fn blame_porcelain_names_the_newest_committed_owner() {
        let z = "0".repeat(40);
        let (old, new) = ("a".repeat(40), "b".repeat(40));
        let porcelain = format!(
            "{old} 3 3 1\nauthor x\ncommitter-time 100\nsummary s\nfilename f\n\t\"p\",\n\
             {new} 4 4 1\ncommitter-time 300\nfilename f\n\t\"h\",\n\
             {z} 5 5 1\ncommitter-time 900\nfilename f\n\t\"d\",\n{old} 6 6\n\t),\n"
        );
        assert_eq!(newest_blamed_commit(&porcelain), Some(new));
        assert_eq!(
            newest_blamed_commit(&format!("{z} 1 1 1\ncommitter-time 9\n\tx\n")),
            None
        );
        let roster = "x\n    // HELP_SURFACES_ROSTER_BEGIN\n    (\n        \"a/b.rs\",\n        \"h\",\n    ),\n";
        assert_eq!(row_line_span(roster, "a/b.rs"), Some((4, 6)));
        assert_eq!(row_line_span(roster, "a/c.rs"), None);
    }

    #[test]
    fn diff_flags_other_than_diff_path_are_refused() {
        let args = |v: &[&str]| v.iter().map(|s| (*s).to_string()).collect::<Vec<_>>();
        assert!(!gate_help_surfaces_args(&args(&["--dif", "x"])));
        assert!(!gate_help_surfaces_args(&args(&["--diff"])));
        let root = crate::workspace_root();
        let (ok, out) =
            prose_diff_report(&root, "crates/no/such.rs", SURFACES, NOT_HELP, ROSTER_FILE);
        assert!(!ok && out.contains("has no row in SURFACES"), "{out}");
        let (ok, out) = prose_diff_report(&root, ROSTER_FILE, SURFACES, NOT_HELP, ROSTER_FILE);
        assert!(!ok && out.contains("is in NOT_HELP"), "{out}");
    }

    /// `--diff` over a real (scratch) history: it names the NEWEST commit whose
    /// version hashes to the row — a code-only commit on top of the read still
    /// matches — and diffs only the prose changed since; when no committed version
    /// matches, it says so and diffs from the commit that last touched the row.
    #[test]
    fn diff_walks_history_to_the_recorded_read_or_falls_back_to_the_rows_commit() {
        let root = std::env::temp_dir().join(format!(
            "xtask-help-surfaces-{}-diff-history",
            std::process::id()
        ));
        let _ = std::fs::remove_dir_all(&root);
        std::fs::create_dir_all(root.join("crates/t/src")).unwrap();
        let rel = "crates/t/src/main.rs";
        let mut day = 0;
        let mut commit = |paths: &[&str], msg: &str| -> String {
            day += 1;
            let date = format!("2026-09-{day:02}T12:00:00+0000");
            let git = |args: &[&str]| {
                let out = std::process::Command::new("git")
                    .arg("-C")
                    .arg(&root)
                    .args([
                        "-c",
                        "commit.gpgsign=false",
                        "-c",
                        "core.hooksPath=/dev/null",
                    ])
                    .args([
                        "-c",
                        "user.name=fixture",
                        "-c",
                        "user.email=fixture@example.invalid",
                    ])
                    .args(args)
                    .env("GIT_AUTHOR_DATE", &date)
                    .env("GIT_COMMITTER_DATE", &date)
                    .output()
                    .expect("git runs");
                assert!(out.status.success(), "git {args:?}: {out:?}");
                String::from_utf8_lossy(&out.stdout).trim().to_string()
            };
            if day == 1 {
                git(&["init", "-q"]);
            }
            git(&[&["add", "--"][..], paths].concat());
            git(&["commit", "-q", "--no-verify", "-m", msg]);
            git(&["rev-parse", "HEAD"])
        };
        let write = |name: &str, text: &str| std::fs::write(root.join(name), text).unwrap();
        let roster = |hash: &str| {
            format!(
                "    (\n        \"{rel}\",\n        \"{hash}\",\n        \"2026-09-10\",\n        \
                 \"fixture\",\n    ),\n"
            )
        };
        let v1 = "/// The tool.\nconst USAGE: &str = \"usage: t\\n\\\n    --fast  skip checks\";\n\
                  fn main() {}\n";
        let v2 = v1.replace("The tool.", "The tool, read.");
        let v3 = v2.replace("fn main() {}", "fn main() { let _ = 1; }");
        let v4 = v3.replace("skip checks", "skip nothing");
        write(rel, v1);
        commit(&[rel], "v1: first text");
        write(rel, &v2);
        commit(&[rel], "v2: the text that was read");
        write(rel, &v3);
        let c3 = commit(&[rel], "v3: a code-only edit");
        let read = source_prose_hash(&v2);
        write("roster.rs", &roster(&read));
        commit(&["roster.rs"], "the read's row");
        write(rel, &v4);

        let surfaces = [(rel, read.as_str(), "2026-09-10", "fixture")];
        let (ok, out) = prose_diff_report(&root, rel, &surfaces, &[], "roster.rs");
        assert!(ok, "{out}");
        assert!(
            out.contains(&format!(
                "matched:  {c3} 2026-09-03 12:00:00 +0000 v3: a code-only edit\n"
            )),
            "the newest matching commit, with its date and subject: {out}"
        );
        assert!(
            out.contains("(1 of the 3 commit(s) that touch it walked)"),
            "{out}"
        );
        assert!(
            out.contains("-str| --fast  skip checks\n+str| --fast  skip nothing\n"),
            "{out}"
        );
        assert!(
            !out.contains("-doc> ") && !out.contains("+doc> "),
            "the doc edit before the read is not a change since it: {out}"
        );
        assert!(
            out.contains(&format!(
                "since:    only the working tree's own uncommitted edits to {rel}\n"
            )),
            "{out}"
        );

        // A row recorded from a tree no commit holds: the fallback names the
        // commit that last touched the row, and says its text is not the read.
        let uncommitted = source_prose_hash(&v4.replace("usage", "Usage"));
        write("roster.rs", &roster(&uncommitted));
        let row_commit = commit(
            &["roster.rs"],
            "the row re-recorded from an uncommitted tree",
        );
        let surfaces = [(rel, uncommitted.as_str(), "2026-09-10", "fixture")];
        let (ok, out) = prose_diff_report(&root, rel, &surfaces, &[], "roster.rs");
        assert!(ok, "{out}");
        assert!(
            out.contains("matched:  none — no committed version"),
            "{out}"
        );
        assert!(out.contains(&format!("fallback: {row_commit} ")), "{out}");
        assert!(out.contains("is NOT the text that was read"), "{out}");
        assert!(out.contains("+str| --fast  skip nothing\n"), "{out}");

        // Once the edit is committed, it is named as the change since the read.
        let c6 = commit(&[rel], "v4: the edit since the read");
        let surfaces = [(rel, read.as_str(), "2026-09-10", "fixture")];
        let (ok, out) = prose_diff_report(&root, rel, &surfaces, &[], "roster.rs");
        assert!(ok, "{out}");
        assert!(out.contains(&format!("matched:  {c3} ")), "{out}");
        let since = format!("since:    1 commit(s) touch {rel} after it, newest first:\n");
        let named = out
            .split_once(since.as_str())
            .and_then(|(_, rest)| rest.lines().next())
            .and_then(|l| l.strip_prefix("          "))
            .and_then(|l| l.split_once(' '));
        assert!(
            named.is_some_and(|(abbrev, rest)| c6.starts_with(abbrev)
                && abbrev.len() >= 7
                && rest == "2026-09-06 v4: the edit since the read"),
            "{out}"
        );
        assert!(!out.contains("uncommitted edits"), "{out}");

        // The working tree IS the read: nothing to diff.
        let now = source_prose_hash(&v4);
        let surfaces = [(rel, now.as_str(), "2026-09-10", "fixture")];
        let (ok, out) = prose_diff_report(&root, rel, &surfaces, &[], "roster.rs");
        assert!(ok && out.contains("the row is green"), "{out}");
        let _ = std::fs::remove_dir_all(&root);
    }

    /// Every [`DESCRIBES`] row names a surface the roster actually carries and
    /// packages that actually exist. A cross-crate edge that has rotted is worse
    /// than none: `--drift` would silently stop naming the surface when the crate
    /// it describes moves, which is the exact failure the table exists to prevent.
    #[test]
    fn every_describes_row_names_a_rostered_surface_and_a_real_package() {
        let root = crate::workspace_root();
        let rostered: std::collections::BTreeSet<&str> =
            SURFACES.iter().map(|(p, ..)| *p).collect();
        for (path, subjects) in DESCRIBES {
            assert!(
                rostered.contains(path),
                "DESCRIBES names `{path}`, which is not a rostered surface — a row for a \
                 file the gate does not track is never consulted"
            );
            assert!(
                !subjects.is_empty(),
                "DESCRIBES row for `{path}` adds no subject; delete it instead"
            );
            for subject in *subjects {
                assert!(
                    root.join(subject).join("Cargo.toml").is_file(),
                    "DESCRIBES says `{path}` describes `{subject}`, which is not a package \
                     (no Cargo.toml) — a subject git cannot log is drift nobody is told about"
                );
            }
        }
    }

    /// Every [`SWEPT`] row names a surface the roster carries and a commit this
    /// repository has. A bound that resolves to nothing is IGNORED by
    /// [`drift_report`], so the surface would look swept for ever and never be
    /// named again — the one way this table can do harm, and the reason it needs
    /// a check rather than a comment.
    #[test]
    fn every_swept_row_names_a_rostered_surface_and_a_real_commit() {
        let root = crate::workspace_root();
        let rostered: std::collections::BTreeSet<&str> =
            SURFACES.iter().map(|(p, ..)| *p).collect();
        for (path, bound) in SWEPT {
            assert!(
                rostered.contains(path),
                "SWEPT names `{path}`, which is not a rostered surface — a bound for a file the \
                 gate does not track is never consulted"
            );
            let ok = std::process::Command::new("git")
                .arg("-C")
                .arg(&root)
                .args(["rev-parse", "--verify", &format!("{bound}^{{commit}}")])
                .output()
                .expect("git runs")
                .status
                .success();
            assert!(
                ok,
                "SWEPT says `{path}` was swept at `{bound}`, which this repository has no commit \
                 for — an unresolvable bound is silently ignored, so the surface would look swept \
                 for ever"
            );
        }
    }

    /// A surface's own subject is the package it lives in, not the workspace and
    /// not its own directory.
    #[test]
    fn own_package_walks_up_to_the_nearest_cargo_toml() {
        let root = std::env::temp_dir().join(format!(
            "xtask-help-surfaces-{}-own-package",
            std::process::id()
        ));
        let _ = std::fs::remove_dir_all(&root);
        std::fs::create_dir_all(root.join("crates/t/src/deep")).unwrap();
        std::fs::write(
            root.join("crates/t/Cargo.toml"),
            "[package]\nname = \"t\"\n",
        )
        .unwrap();
        assert_eq!(own_package(&root, "crates/t/src/deep/cli.rs"), "crates/t");
        assert_eq!(own_package(&root, "crates/t/src/cli.rs"), "crates/t");
        // Nothing above it: the walk ends rather than climbing out of the tree.
        assert_eq!(own_package(&root, "docs/notes.rs"), ".");
        let _ = std::fs::remove_dir_all(&root);
    }
}
