# aterm v0.92.0

2026-09-24 · 487 changes since v0.91.0

## Features

- **gui, link, types, ctl:** the endpoint pushes `topic add|drop` on the event lane; the serial, the 2 s sample and the drop/add race go
- **gui, link, types:** `aterm fabric doctor --retire-ghosts` retires through a LIVE bridge, over the push lane
- **agent, phase:** the watcher is the single supervisor — a box it does not approve sets the worker's attention and asks the manager once, cleared when the box goes
- **atpkg:** OpenPGP detached-signature verifier for vendor digest documents
- **atpkg:** the vendor\_direct pure core — table, version ids, decide, policy latch, records
- **agent, harness:** the live-upgrade core — a cooperative, in-place resume relaunch of a running Claude Code, planned purely
- **harness:** `aterm harness upgrade` restarts a live Claude Code onto a newer build and resumes the conversation
- **harness, gui:** live agent upgrade runs by default — the window sweeps its own tabs every minute
- **linux:** integrate native delivery and toolchain support on main
- **privacy:** Settings ▸ Security moves conflicting copies to the Trash, and `app act` cannot press its buttons
- **messages, gui, render, gpu:** the progress bar runs the full width of the window — 0 % empty, 50 % the middle, 100 % edge to edge
- **effects, docs:** shed\_ab — an ear bench proving a shed key now sounds byte-for-byte like a lit one, and the audit's outcome

## Fixes

- **atpkg:** the managed claude and codex lead inside aterm only — and the Trust toolchain keeps every terminal
- **sound:** a shed frame is still a heard key — the click seam no longer closes with the light
- preserve lint-clean merged harness and rainbow tests
- **sound:** a device that opens but never plays now spends its budget and stops — and the stale help golden that made main red
- **xtask, primer, docs:** the help-surfaces gate hashes a markdown asset IN FULL; the five primer assets re-read, eight sentences corrected; doc §12
- **containment:** the non-macOS codesign\_report stub carries its doc — the wasm site build denies missing\_docs
- **gui:** an update pass served from the index cache retries in minutes, not six hours
- **gui, link, types, ctl:** a topic change evicted from the timeline is a GAP the bridge acts on — every session's opt-ins re-read; the record drains at once, under the topics lock
- **link:** `fabric retire` is gated on the NODE's hosted set at publish time — a sibling instance's live session is refused; the doctor says queued or refused, not "did not"
- **types, cli, primer, xtask, docs:** the review's false prose made true — `fabric=stale` named, the STRICT refusal, `@*` in the subscribe grammar, what the notes file records; round 24's CHANGELOG entries re-homed under Unreleased; doc §12
- **spec:** update live report shape pin for four new models
- **atpkg, gui, ctl, lanes:** three silences from the 2026-09-22 "didn't install the latest claude" read — a missing freshness stamp, a latch that read as a clean no-op, and the tab nobody named
- **fabric:** resolve new head topics from broker head
- **atpkg:** vendor\_direct core — yank-aware high-water, legacy holds, row-checked records, locked stamps
- **atpkg:** a replayed older index never lifts a latched vendor yank, and cached bytes never vouch the latch current
- **atpkg:** share prompt vendor head checks and bound retries
- **atpkg:** `claude update` says what you have before it checks — the version, no echo of the words typed
- **atpkg, cli:** which copy of claude/codex runs is decided at exec time, never at shell start
- **link:** the retired `aterm link hook` is a silent no-op, never Claude Code's blocking exit 2
- **primer:** the settings backup is never wider than the settings file it copies
- **agent, phase:** the watcher's box escalation names the box's own row, asks once per review point, never waits
- **atpkg, gui:** a dev bundle's launch no longer rewrites the user's shared setup — the PATH links, the shell hooks and the agent primer are the release bundle's to write unattended
- **gui:** a --headless launch is a process that cannot take the keyboard — macOS made it a Regular app that activated itself
- **gui:** a live SDR-to-HDR switch on Windows reseeds the reference white at the next redraw
- **gui:** the reflow SMT probe steps aside on criterion's arguments instead of panicking
- **gui:** a first-launch capture stall times the font seal and its chrome re-sync apart
- **libc, update, release:** three self-contained fixes from the Linux delivery line
- **core, grid, render, gpu:** scrolling round 2 — the incoming row fills the strip, the rescue stops parking, the Metal compose copies its stale rect, and a scrolled viewport refills only what the wheel exposed
- **core:** the incoming-row apron's BiDi guard keys on the row's visual order, never on the mode
- **gui, pty:** main's two gate reds — a tippy lint in the message reporters, and a pty control that three descheduled runs could fail
- **update:** stop collecting the rollback on a failed trial, and make the ownership guard testable
- **atpkg:** repair re-lays each shim with the environment its build declared
- **atpkg:** gc keeps ANY program's superseded build while a live process runs from it
- **cli, gui:** the upgrade help names Claude Code's update banner without quoting its restart phrase
- **xtask:** the manual's re-read note names the banner in backticks, not raw quotes (they ended the row's string)
- **release:** the cut stops leaving a launchable copy of the app behind
- **gui, messages:** a broken aterm.toml is reported as broken, removed keys are "no effect", diagnostics keep their rows, and the band's seam meets the terminal
- **privacy:** the image anchor reads the kernel's path alone, and a failed lookup is not a deletion
- **gui:** the automatic-update card takes eight arguments, so it says so
- **privacy:** one bundle-identity reader, a census that finds LaunchServices' copies, and no model restating it
- **atpkg, agent:** no unattended pass runs a file it did not place; repair moves a copied command aside
- **gui:** a headless process presents no OS UI — no modal it cannot show, no other app brought forward
- **privacy, atpkg:** the Security gestures ask the owner, the Trash takes only what it proves, and repair can no longer lose a file
- **atpkg:** a legacy rollback is attested by its kept root; flocks release by LOCK\_UN, not the close
- **atpkg, aterm-ctl:** an owed legacy root is read again; the stamp lock releases by LOCK\_UN; a probe reads a reply the peer hung up after
- **sound:** a queue AudioToolbox stops under us is seen on the next key or tick, not after the 750 ms watchdog
- **census:** atpkg's machine-apply queue is the fourth real file-advisory lock — the pin moves with it, and main is green in aterm-census again
- **atpkg:** the index probe waits out a spawn window before deferring — one try\_lock read a descriptor copy as another prober
- **gui:** main's two gate reds after the quiet-driver posture — the toolbar drive activates before its clicks, and apply\_launch\_posture gets its other arm
- **atpkg:** a trust rollback no longer re-points ty, ay and clean at the trust bundle, and repair puts a lost own-name shim back
- **aterm-spec:** the store tier of find\_ty/find\_ay/find\_trust\_ir reads atpkg's exec-stub shims, and a rollback respects an older prefix's programs
- **link, gui:** an opt-in read before its session was watched is read again when the push lane adopts it
- **gui:** a post wakes its session's watchers, so the bridge drains the outbox now instead of on the next tick
- **lint:** main's two tippy -D warnings findings — a clone made only to build a one-element slice, and an import the sealed test stopped using
- **gui:** the push lane's test hold is read through dev\_seam!, the one reader env\_reads admits — the landing gate's last red
- main's five gate reds after the privacy and Linux-delivery merges — each read to its cause, none waived
- **harness:** the live-upgrade sweep lock waits out a spawn window before calling a sweep busy — one try\_lock read a descriptor copy as another sweep
- **atpkg:** `aterm <tool>` exports what its build declares and answers the twin's self-update verbs; the pass heals a shim that lost its env; doctor names one
- **atpkg:** `aterm claude update --help` runs the vendor's help with its build's environment; the manual says `aterm claude update` is answered everywhere
- **atpkg:** the rustup view is not re-laid under a build running from it, and the process-args reader keeps what the kernel recorded
- **atpkg:** a view left behind names `aterm pkg repair`, and the refusal record keeps following the disk
- **harness:** the master switch reads what Settings reads, and a hand-run upgrade obeys it
- **harness:** a misplaced kill switch still reads off, and the window says when it cannot sweep
- **harness:** the relaunch line fits the tty's canonical line, and the argv rewrite consumes values as Claude's parser does
- **harness:** plan the relaunch before the upgrade notice, and bound its line per kernel
- **harness:** the live upgrade drives only its own shell's foreground job, on the tab's own terminal
- **harness:** an unread parent never fails a relaunch; every job re-read is pinned; a held-back session says why
- **census, gui, update:** main's two lock-census reds — an unregistered guard helper and a state read named `read`
- **release, containment:** three of main's gate reds — a tippy lint, two dangling citations, a macOS-only clock the wasm guard could not see was macOS-only
- the sweep lock and the index probe's range locks release by LOCK\_UN, each pinned; await\_exec gets back its own cfg
- **gui:** define launch posture on non-macOS hosts
- **fabric:** keep broker reconnect backoff under local event load
- **core:** refresh apron caret on cursor-only frames
- **effects:** preserve wrapped rainbow prefix across 32-key batches
- **release:** keep mirror citations and lint checks current
- **census:** expose GUI guard and name Linux state load
- **gui:** pace direct queued-key echoes without blocking spill
- **pkg:** deliver ready vendor hints while index probe runs
- **effects:** keep exact wrapped rainbow prefixes through 128 keys
- **pkg:** retain vendor hints across higher-priority wakes
- **gui, effects:** preserve queued paste and later key glows
- **atpkg:** run, which and `aterm <tool>` tell a lost shim from an uninstalled program, and run executes the live build
- **atpkg:** which names the agents/ twin that still runs over a lost bin/ shim, and an unknown word costs the front door one stat
- prioritize hand follow rows in rainbow witness
- **agent, phase, gui, atpkg, tests:** fixtures the public export refuses — a Claude scratch uid, home paths and a key-shaped literal
- **readme:** the Linux paragraph points at the installer's own help, not a doc the export cannot carry

## Performance

- **core:** the incoming-row apron is stamped, so a stationary scrolled-back repaint re-reads no history
- **harness:** skip decision ledger scan for event hooks
- **harness:** reuse post-park status for quiet watch guards
- **gui:** skip lock-state lookup for native keys
- **atpkg:** overlap independent index discovery probes
- **gui:** skip cursor neighbor scans when rainbow is inactive
- **atpkg:** overlap independent vendor head checks
- **atpkg:** deliver each ready vendor hint without waiting for its peer
- **gui:** start targeted vendor pass on first ready head
- **atpkg:** skip stale vendor hints after store changes
- **gui:** retire closed tab egress writer after idle
- **harness:** skip process inspection for current Claude sessions
- **harness:** defer custody RPC until high-level act

## Refactors

- **link, ctl, primer, cli, gui, xtask, docs:** cut the file mirror; a sandbox that refuses the socket is not worked around
- **link, primer, gui, agent, cli, types, ctl, xtask:** cut the vendor hooks — nothing wakes an agent for mail; it is typed to, as a human would
- **gpu:** the Metal FFI uses aterm\_objc::Bool instead of a private copy of it

## Tests

- **cli:** the two reroute pins assert the gated hook … introduced — main is green in aterm-cli again
- **fabric:** model head subscription against stale say reader
- **atpkg:** cover every OpenPGP packet-length form and state what the refusal and MPI rules guarantee
- **atpkg:** the managed-current source test reads update\_vendor, the verb body both doors run
- **gui:** await queued key receipt visibility before asserting class
- **aterm:** the launch-isolation filter strips the internal \_\_ATERM\_ protocol markers too
- **link:** the r24 gap test establishes its premise instead of racing the bridge's pickup
- **gui:** the chained-notch anchor test runs on one timeline, not two clocks
- **atpkg:** the landing pass-through's "at once" bound clears a cold exec under a full gate
- **link:** r24's gap test waits for the push lane to watch s\_b before it stops the bridge — the 60 s hang was the test building the wrong case
- **atpkg:** the outliver case tells the kernel killing its fixture apart from the reader failing — measured, a SIGKILL on page-in of a deleted executable
- **atpkg:** tolerate fork-delayed store lock release in holder test
- **atpkg:** wait for copied process readiness before unlink
- **atpkg:** pin only rewritten synthetic probe stamps
- **gui:** drive toolbar clicks without foreground focus
- **atpkg:** wait for copied pid name before unlink
- synchronize GUI spec and selector censuses
- **gui:** allow image worker scheduling under full-suite load
- **gui:** expect abbreviated Updates label in medium rail
- **spec:** model bounded vendor hint completion lifecycle
- **atpkg:** bind pending vendor hints to lifecycle model
- **spec:** bound vendor hint mutants for non-vacuity
- **effects:** keep 32-key exact run independent of cap
- **objc:** account for restored toolbar drive selectors
- **spec:** count published spacing model in GUI report

## Docs

- re-read package listing help against signed wake path
- **fabric, changelog, xtask:** §13 records decision "B" — what was cut, with counts, and the harness as the only per-vendor layer; CHANGELOG; the design records marked superseded
- **atpkg:** vendor-direct agents and a silent, immediate update system — the design, revised after a four-lens review
- **atpkg, help:** the pkg page and §17 say the window checks both vendors about every 5 minutes
- **link, agent, fabric:** the cut hooks are no longer described in the present tense
- **help:** `aterm help pkg` says the bundle's stamp taints the helper too
- **gpu, gui, release:** say what was measured and what is only documented or expected
- **gui:** scope the displaylink probe figures to the runs they come from
- **tools:** say what an unscrubbed RUSTUP\_TOOLCHAIN actually does to the cargo-pin gate
- **gui:** pressure\_shed\_budget names YELLOW\_EXIT\_PERCENT as aterm-scrollback's crate-private constant
- **gui:** three cursor-cadence doc claims that contradicted the code beside them
- **ctl:** the `latest` alias IS handed back to a survivor, by an update-handoff rollback
- **help-surfaces:** record the reads the headless focus fix owed on manual.rs and cli.rs
- **help-surfaces:** the atpkg cli.rs row carries the repair-own-name read beside the doctor lane's — the two round-2 lanes merged
- **changelog, xtask:** the audit1 live-upgrade and shim-env fixes, and the three help surfaces they changed, re-read
- record the ux/status-reporting merge's rulings 55–72 in the binding design
- **gate:** read who nonce help surfaces

## Other

- **messages:** the aterm-messages engine crate — model, center, glass, log, wire (Phase 0)
- **gui:** the message band is born from the status bars (Phase 1)
- **gui:** Settings ▸ Messages, the deep link, and the band's Details link (Phase 2)
- **gui:** config warnings, the crash notice and the deferred lane become messages; config\_notice.rs retired (Phase 3)
- Wake package updates on new signed indexes and bound harness work
- Restore rainbow continuity through wrapped Codex composer pauses
- Wake atpkg promptly after large index skips
- Name self-socket readiness mutex for lock census
- Keep index and rainbow helpers lint-clean
- **stage(atpkg):** stage hooks, the darwin Developer ID gate, vendor sidecars and agent liveness in gc
- **stage(atpkg):** close the Developer ID gate's review findings
- **transport(atpkg):** the vendor-direct fetch lane — conditional vendor\_get, vendor\_content\_length, URL-prefix pins
- **transport(atpkg):** vendor lane review fixes — per-program pins, a pinned payload lane, verdicts apart from offline
- **vendor-direct(atpkg):** claude and codex install and update from their vendors, ahead of the index
- **vendor-direct(atpkg):** review fixes — a vendor fault fails the pass, rows say why, no index rule reaches a vendor program
- **surfaces(atpkg, gui):** vendor programs are named by version and source everywhere; rollback leaves the index out
- **surfaces(atpkg):** review fixes — no surface claims more than this build does; rollback reaches the legacy build
- **vendor-direct(atpkg|gui):** the head watch — a new Claude Code / Codex release is noticed within minutes
- **vendor-direct(atpkg|gui):** the head watch re-offers through 304s, is gated like the pass, and has its own door
- **vendor-direct(atpkg):** final-review fixes — legacy builds need no index, "latest" needs a check, vendor doors sweep only their own programs
- **vendor-direct(atpkg):** a legacy ceiling, and targeted doors refresh a stale yank latch
- atpkg,aterm: nothing prints into a shell the user did not ask to update
- atpkg,aterm: a host's unasked notices go to aterm.log; doctor says only what is true
- **aterm-gui:** silent toolchain lane — one first-run row, failures are a Settings badge
- **aterm-gui:** silent lane review fixes — first run by the ALab set, badge counts what doctor lists
- atpkg,aterm-gui: every pass cheap, correct and durable (Phase 3, pass)
- atpkg,aterm-gui: Phase 3 review fixes — rows durable, roots per build, offline is the link
- aterm-update-core,atpkg,aterm-gui,aterm: one wall-clock cadence; a published index within minutes
- aterm-update-core,atpkg,aterm-gui,aterm: Phase 3 review fixes — a failing pass retried on a pass-sized ladder, never back to back
- atpkg,aterm-gui: p3/reconciled review fixes — no duplicate launch pass, no seed on the walk's clock, a stale row is not this pass's
- atpkg,aterm-gui: Phase 4 — one package log, and Settings ▸ Packages you can review
- atpkg,aterm-gui: Phase 4 review fixes — live wording, one switch that says what runs, a log that quotes only this pass
- atpkg,aterm-update,aterm-gui,aterm: Phase 4 one path — no environment knob changes a shipped aterm
- atpkg,aterm-update,aterm-gui,aterm: Phase 4 review fixes — notes once, a presumed-read gate, a person's check never refused
- aterm-gui,aterm-cli: merge review fixes — a retired auto\_update read whole, named on Modified, never told to rename blindly
- **atpkg:** a clean helper inside a stamped .app runs from a replica, not in place
- **atpkg:** the taint walk covers every bundle the kernel charges, and the prose says only what was measured
- **gate:** fixtures are not at the mercy of the host disk, and main's census test counts upstream's new flock
- **atpkg:** delete appgate — the app's own pair is refused, always, before a byte moves
- **atpkg:** delete seam::reassert\_if\_armed, and say what the arm really does — nothing yet
- atpkg, aterm-update: delete the sealed-seed client lane and the app updater's wait on it
- atpkg, aterm-release: finish the Phase 5 deletion — the sentinel, the cause, the legacy row
- **forge:** re-pin the six native cells for aterm-messages (+1 resolved, +1 workspace)
- **gui:** the floating toast retires; its messages follow the owner's attention rule (Phase 4)
- **messages:** attention and motion — terse rows only for work, decisions and failures; animated progress with ETA (design §10)
- **messages:** the motion at 3x — a soft comet, an honest stall, load words that survive narrow windows (visual review round 1)
- **messages:** the comet enters and leaves inside its caps, the row holds still when load words latch, one progress measure (visual review round 2)
- **messages:** the final review's fixes — honest time words, noncharacters never draw band glyphs, config and first-run rows that come back when they should
- **messages:** visual review round 3 — the comet enters without an edge, the success echo says done, a fault flash in the fault hue, one ETA slot, bar ends that map to 0 and 100 %
- docs, messages: attention-and-motion rulings 55-114 become 76-135
- **atpkg:** the full-pass rule is a derived model; a pass records how it ended; a rate limit's reset holds the metered pass
- **atpkg:** the full-pass model binds the queue, the truth and progress; a queued pass stands down behind any ending
- gui, atpkg: the Software Update card carries both \[update\] switches, the head watch hears exclude live, the seed markers retire
- **gui:** the Software Update page speaks for the checker this process runs; the card's labels say what they do; a short phone keeps a switch on page 1; the package lane reads atpkg's aterm.toml
- **atpkg:** the vendor-direct cutover is one typed publish — rows kept, pins dropped, the vendor lane retiring
- **atpkg:** the cutover's review — a re-pin floor, its own word, no silent un-yank, the lane suites read the whole tee
- **gui:** the Linux update card fits its section in Linux's faces; a short phone keeps a switch on page 1; Linux facts outrank the switch's timing; \[update\] help names both updaters
- spec,gui: the typing-sound seam and the audio reopen ladder are derived models bound to the shipping code, and the binding found two defects, both fixed
- effects,gui,spec: the classic wake and an empty grid obey the one key-seam law, the tone verb's call site is bound, and the reopen ladder's reset law is refuted at tier 0
- **phase:** the formatter oracle's one standing finding is formatted
- **fmt(phase):** the prompt fixture assertion main left unformatted is reflowed
- **atpkg:** doctor says whether a newer index is published — the probe's cached answer beside the last-trusted build
- **atpkg:** doctor's channel-head line gives the remedy that can work — a refused or incomplete newer index is named, not sent back to update
- aterm-spec,aterm-gui: derive the tag record's writer/reader contract (AtpkgTagRecord)
- atpkg,aterm-gui,aterm-cli: clear the macOS provenance tag silently instead of narrating it
- **atpkg:** gate the heal stubs to the macOS tests that use them
- atpkg,xtask: review fixes to the silent heal — a refusal never replaces a standing file
- **atpkg:** say plainly why a refused lay tests the heal first
- aterm-gui,aterm-spec: the consent warm-up holds every ladder phase, Land included
- aterm-gui,aterm-spec: the apply ladder in seconds — a staged update lands within a minute
- aterm-types,xtask: `update` help says the ladder ends within a minute
- aterm-gui,xtask,docs: the switch fits inside the minute — the ladder stops waiting at 55 s
- **aterm-spec:** LogRotation — aterm.log rotation shared by several writers
- aterm-log,aterm-gui: aterm.log rotates while running, always appends, and names its writer
- **aterm-gui:** the slow-present breadcrumb is one line per episode, not one per 10 s
- **aterm-gui:** rotation tests do not race the conformance test's ty spawns
- aterm-spec,aterm-gui: LogRotation — a look that follows a rotation may also rotate
- **aterm-gui:** `metrics reset` leaves the open slow-present episode to the UI thread
- **aterm-gui:** RotatingFile::check's doc says a look can follow and rotate
- **xtask:** the control\_query.rs read note says when `metrics reset` closes an episode
- aterm-census,aterm-gui: the advisory-lock census counts all five file locks
- **aterm-gui:** status bars say what each row IS, not what its glyph looks like
- aterm-gui,aterm-update,xtask: one update is one animated row, from download to landing
- **aterm-gui:** the two package pills say what is happening, not the mechanism
- **aterm-gui:** the busy animation wakes only for a row the window actually shows
- **aterm-gui:** the flow row names unsaved editor work while it holds the install, and the health banner is short
- atpkg,aterm-gui,aterm-cli: history.log and Settings ▸ Log — the record aterm keeps, reviewed where it interrupts no one
- **aterm-gui:** the Packages Activity's link to Settings ▸ Log rides beside Open Log on desktop
- **aterm-gui:** the update record never leaves an "Installing" that was not, and dates a failure when it happened
- **aterm-gui:** Settings ▸ Log measures its rows once, folds the per-pass pair, and copies full dates
- **aterm-gui:** retire the provenance self-relaunch; measure once and log one line
- **aterm-update:** a relaunch probe for provenance tracking; rename probe runs in parallel
- **aterm-gui:** provenance log line says only what was measured; bundle scan restored
- atpkg,aterm-cli: a person's `aterm pkg` shows progress, waits instead of refusing, and speaks plainly
- aterm-spec,atpkg,aterm-gui: a waiting stub reads the pass before the shim (AtpkgPendingWait), and plainer pin and config words
- **atpkg:** the docs around the markers, the pending verb and the update summary say what the code now does
- **atpkg:** the short doctor keeps a PROBLEM's listed causes and a declined store's reason
- **atpkg:** on a terminal the shadowed: marker adds no sentence of its own
- atpkg,aterm-gui,aterm-cli,aterm-verify: WP6 review fixes — the doctor's faults follow its verdict and survive a closed pipe, the meter keeps the time left, plain default-set lines
- aterm,atpkg: `aterm <tool>` hands a pending tool its arguments, and a person's lock wait is pinned on a real terminal
- **atpkg:** on a terminal the machine-settings sentence does not repeat the Universal Control line
- **atpkg:** the pty lock-wait test keeps its terminal open until it has read it
- aterm-census,atpkg: the terminal meter's screen lock is a registered guard helper
- **aterm-gui:** apply\_launch\_posture has an arm off macOS
- **aterm-gui:** status\_bars.rs as trustfmt lays it out
- **atpkg:** unit tests fake the pass-end provenance heal; only the heal's own tests drive launchd
- **aterm-gui:** Settings ▸ Software Update says when it last checked, in plain words
- **aterm-gui:** Settings ▸ Packages says each state in plain words, one noun: ALab tools
- **aterm-update:** a healthy outcome says how often it checks, not how it reads the channel
- aterm,aterm-cli,aterm-update-core,aterm-gui,xtask: `aterm update` says one plain line; its records go to aterm.log
- **aterm-gui:** the Packages card and the automatic-updates caption say it once, plainly
- **aterm-gui:** the install switch waits while updates are off, and says so in words
- **aterm-gui:** the Packages headline says "up to date" only when it is
- **aterm-gui:** a persistent failure names the right half, and only a too-old copy is told to reinstall
- aterm,aterm-cli,aterm-update,aterm-update-core,xtask: `aterm update` says where an update installs, and says it is checking
- aterm-update,aterm-gui: expected update conditions log quietly, and a new release is found in ten minutes
- aterm-release,tools: a release cut clears com.apple.provenance itself before it refuses
- aterm-gui,xtask: per-process files no longer pile up in the control directory
- **aterm-update:** only a check that never reached its channel takes the quick retries
- **aterm-release:** a test run never heals the installed toolchain
- **aterm-gui:** toolchain ledger rows say what happened, and history.log is not told twice
- aterm-gui,aterm-types,aterm-agent,xtask: the agent harness's notes are a record, never a toolchain row
- atpkg,xtask: the window's seed pass is not told to install what it is installing
- **aterm-gui:** the update flow row is still while it waits on a person or a retry hours away
- **aterm-gui:** the landing is on record when it happens, with how long the update took
- **aterm-gui:** a routine package pass leaves its measurements at DEBUG in aterm.log
- **aterm-gui:** the landing times only a download it saw, and no pass that installed nothing reads "failed"
- **aterm-agent:** a note an older window refuses never stops the harness's act
- **xtask:** re-record the manual.rs and atpkg cli.rs help-surface rows after wave C
- **aterm-messages:** pin the busy track, the carry's busy flag, whole sentences and the harness lane
- **aterm-gui:** pin the one-row update lane, the busy animation and the harness record on the band
- aterm-gui,aterm-census: messages.log rotates while running, not only at a cold launch
- **aterm-gui:** Settings ▸ Messages speaks plain words and is where a report starts; errors kept whole
- aterm,aterm-cli,aterm-types,aterm-agent,atpkg: every pointer names Settings ▸ Messages, and the manual names messages.log
- **xtask:** re-record the help-surface rows the merge and its port moved
- **aterm-gui:** an update-health warning heals only on the proof that answers it
- aterm-types,xtask: `appstatus` help names the message band, not the two status bars
- **aterm-messages:** the invariant docs the port displaced go back on their tests
- **aterm-gui:** a toolchain failure's long reason is split at its joints, never cut
- **aterm-gui:** the staged-row test keeps upstream's guard on the handoff-off line
- **aterm-gui:** the crash row says a report was saved, not the report's path
- **aterm-messages:** split\_sentence is one pass over the sentence
- **xtask:** re-record the aterm-messages wire.rs help-surface row after …
- **aterm-messages:** trustfmt the splitter's joint table
- **aterm-gui:** trustfmt the update-health heal from …
- aterm-gui,aterm-update: the lock-order census sees main's two new lock shapes
- **aterm-gui:** the medium settings rail test expects main's short "Updates" label
- **xtask:** re-record the six help-surface rows the origin/main merge moved
- **atpkg:** a pending tool that lands runs through `atpkg run`, with its build's environment
- **xtask:** re-record the atpkg cli.rs help-surface row after the pending-tool hand-off
- **atpkg:** the orphan-label sweep test runs alone among the launchd-job tests
- **aterm-phase:** measured 2.1.280 and codex fixtures, provenance on line one — the box, trust and wall screens were hand-typed or missing
- **aterm-phase:** wall(rows) names the wall a turn ended on, from one phrase table — a 529 or a lost login read idle, a full context read as a usage limit
- **aterm-phase:** a box is its key-hint footer and its header is its own first row; Prompt v2 reads roles, focus, select and cancel — measured on 2.1.280
- **aterm-phase:** per-program readers, turn-end readers and the anchor table — Claude's grammar judged every session, Codex's gate read idle
- **aterm-phase:** a box's options are read under its question and roled only when sound; every row that may run is a command row; the question dialog's footer is a footer; a quoted box or a wrapped folder is read as what it is — review blockers on the approval parser
- **aterm-phase:** Reading says whether its phase is evidence, and pagers and editors read generic — a Codex or generic `idle` was a default a continue policy would act on
- **aterm-phase:** goal\_active needs the indicator against the right edge — a quoted `◎ /goal active` with no done row under it read as armed
- **aterm-phase:** a notice's head ends at a sentence end, not at the `.` of `4.1`; the placement doc names only the forms it recognises — `Opus 4.1 requires usage credits.` read as no wall, and a Monitor event's payload is read as the vendor's
- **aterm-phase:** the anchor drift canary fails on a missing anchor and on no binary — it printed and passed
- **aterm-link:** `hook` is a tombstone — a hook an older aterm wrote exits 0 silently instead of blocking the session
- **aterm-primer:** the hook sweep matches aterm's own commands by executable and verb, and takes the retired harness's entries and statusLine too
- **xtask:** help-surfaces re-read of aterm-link's cli.rs after the `hook` tombstone
- aterm-primer,aterm-cli,xtask: primer v9 — the rm operand shape, the short verb help, and mail read on the typed line or a connected fabric, never polled every turn
- aterm-agent,aterm-cli,aterm,xtask: `aterm harness install|uninstall` refuse and `hook|statusline` are silent no-ops — decision B, finished
- aterm-cli,xtask: the agent brief says primer v9's mail rule and verb pointer, and the harness page says what `drive watch` approves today
- aterm-primer,xtask: the hook sweep also takes an unquoted install path and the `aterm-cli` alias, the statusLine comes back from what install displaced, and the verb pointer names its offline spelling
- aterm-agent,xtask: `aterm harness hook|statusline` typed at a terminal read nothing and say they are retired, instead of waiting for ^D
- aterm,aterm-link,xtask,docs: the 2026-09-14 front-door hook shape reaches the tombstone too, and the tombstone's reason is the one the repo measured
- tools,aterm-agent: harness-capture says its live mode is suspended, and the corpus test stops naming it as a working remedy
- **aterm-phase:** a box under a blank row below a multi-row `⎿` output is a box, not a box quoted straight on
- **xtask:** help-surfaces classifies aterm-phase's wall.rs as not-help — its USAGE hit is a vendor phrase table
- aterm-core,aterm-gui: an adopted shell keeps its shell-integration nonce across a seamless update — its marks no longer go dark for life
- aterm-gui,aterm-types: the server publishes program= and the agent verdict — re-read on a changed live zone, not on the shell FSM's revision
- **xtask:** re-record the control\_session and control\_verbs help surfaces after the agent-verdict wire additions
- aterm-gui,aterm-types: write fences (key|send if-seq=/if-fp=, turn submit=guarded:\<re\>), keyed attention and the supervisor meta key — a supervisor's press cannot land on a box that replaced the one it read, and its escalations and presence are its own
- **aterm:** live-headless conformance for the write fences and the supervisor key — the swap scenario against a fake worker's key log, and a claim that clears with its connection
- **xtask:** re-record the control, control\_input, control\_session and control\_verbs help surfaces after the fence, keyed-attention and supervisor additions
- **aterm-gui:** a headless instance never runs the agent primer — test and agent-private instances no longer write the \~/.claude they inherited
- **aterm-gui:** one place for the human — the menu-bar item lists an unsupervised agent's prompt, question or limit as '\<tab\>: \<kind\> \<command\>', most severe first, and posts one native notification per transition
- aterm-gui,aterm-types: lane-C review fixes — fence presses on the screen generation, bind the guarded submit to the cursor row, stamp the verdict with the screen it read, and hand a lapsed supervisor's box to the human
- **aterm:** live-headless conformance for the review fixes — the swap worker re-enters the alternate screen at an equal seq, a shell that cats a Claude capture pushes no agent verdict, and a ttl claim's lapse is recorded
- **xtask:** re-record the control, control\_input, control\_query, control\_session and control\_verbs help surfaces after the lane-C review fixes
- **aterm-agent:** classify reads a line the way the shell does and judges program words only at heads — the measured APR-4 bypasses are refused
- **aterm-agent:** supervise::policy — one approval decider, the rm-breaker resolver, and a press guard bound to the judged row
- aterm-agent,xtask: --auto-reads judges both readings of a box's rows and refuses a noted box — the head-only program scan cannot be laundered by a space join
- **aterm-agent:** supervise::transport — the Ctl seam over one persistent control connection, and a turn's verdict read where aterm-ctl prints it
- aterm-agent,xtask: supervise engine — one approval decider in the loop, a press bound to the judged row, a wake on the footer leaving, and a watch that outlives holds and cleans up after itself
- aterm-agent,xtask: a frameless screen is re-read every 3 s, not every 20 — a watch started on the shell prompt sees the worker's first box in a second
- aterm-agent,aterm-gui,aterm-types: the merged engine fences its press on the screen generation (`key if-gen=`), taken from the read it judged
- **aterm-gui:** tippy's cloned\_ref\_to\_slice\_refs in the gesture-failure test — pre-existing on main, not from the merges
- **aterm-agent:** the retired `harness hook|statusline` drain a piped payload to its end, as aterm-link's retired\_hook does
- **aterm-agent:** supervise::config — the \[harness\] policy keys as one struct the host parses and the engine runs under
- aterm-agent,aterm-spec,aterm-cli,xtask: delete the second harness stack — `harness watch` never landed an act and duplicated the one engine
- **atpkg:** delete the harness row and path family — no publisher, no writer, and its verdict source is gone
- aterm-link,xtask: presence relays the server's agent verdict — the fabric reads no screen and links no vendor reader
- tools,aterm-agent: grep\_guard V1 — a Claude Code screen anchor spelled outside aterm-phase fails the guard
- aterm-agent,xtask: `harness disk` says "report only:" once — the refusal's own text already opens with it
- aterm-primer,aterm-cli,aterm-link,xtask: the fabric brief and `aterm help fabric` describe the presence row the bridge now writes — agents read it before they post work
- aterm-agent,docs,xtask: the disk journal is bounded again — `harness disk` appends a row on every run, and the ring it replaced had a cap
- **aterm-link:** a test proves a `meta` change after the first read reaches the bus by the `EVENT … meta` push alone — the round stopped re-reading it
- aterm-agent,xtask: `harness limits` with no session to read refuses — it classified whichever session the socket defaulted to and said nothing of whose
- **aterm-agent:** the classifier refuses arithmetic that can expand a subscript, a chosen git repository and the read tools' write forms; the rm resolver refuses relative operands and $PWD — lane B's review, now that decision 1 presses on them by default
- aterm-agent,aterm: the decider reads a box with the session's program's reader and presses only its one-shot allow; the trust dialog is answered for the session's own folder by a confirmed focus move; the loop claims the session and badges under its own key — lanes A and C adopted, lane B's review closed
- **xtask:** re-record the supervise/run.rs help surface after the policy, claim and keyed-attention prose
- aterm-spec,aterm-agent,xtask: derived models of the supervisor's claim and its focus-then-Enter choice, each bound to the real Session — a press under another's claim and an Enter on an unconfirmed focus are both caught
- aterm-agent,xtask: a box under a stale program name is judged by the server's own agent verdict, and a focus move is read only once the dialog has settled — both measured on a private headless aterm
- aterm-agent,aterm-spec: the turn-end decider — continue, walls and model switches as one pure function, its derived model bound to it state by state
- aterm-agent,aterm-cli,xtask: the loop carries out the turn-end decider — the resume probe question is gone, a usage reset gets the continuation, every act guarded and ledgered
- **aterm:** the turn-end policy live against a private headless instance — one accepted suggestion, one 529 continuation after its backoff, the decision after each escalated
- **aterm-agent:** a watch that retries nothing types no /login either, and a model switch says whether it will be switched back
- **aterm-agent:** a Bash box's command is narrowed only for the rm breaker, only on printable-ASCII rows, and only with a quarter of the box to spare — a wide-character row no longer hides a wrapped write from the read-only rule
- aterm-agent,aterm-spec: an act whose point never shows the worker busy is judged at a deadline, never awaited forever — the turn-end latch lane B2's review found
- **aterm-agent:** the worker's last words are judged as text, not screen rows — a stop phrase or a choice split by a soft wrap, a sign-off ask and an offer to do something destructive are escalated, never continued
- **aterm-agent:** a continuation is written only on the screen that was judged, and Enter only once the composer holds it as written — the paste splice and the 40-character tail guard, lane B2's review
- aterm-agent,aterm: text left typed is escalated with what happened first, and a long continuation in a narrow composer is checked live — submitted once, and a mangled composer gets no Enter
- **aterm-agent:** the typing budget is the session's — seeded from the ledger's typed rows of earlier loops within the window, so a re-spawned loop does not start a fresh six an hour
- **aterm-agent:** a model switch says it also moved the default for new sessions, and one with no way back is escalated once
- **aterm-agent:** the watch-only escalation line is one plain line again — a lost line continuation had left 18 spaces inside it
- **aterm-agent:** the Read rule judges a path where its symlinks lead, and the default claude-\* trust root is the uid's own
- aterm-agent,aterm-spec: a claim request the server did not serve leaves the loop PENDING — it types and presses nothing until one is answered, and SupervisorClaim models it
- aterm-agent,aterm-spec: the turn-end Tier-1 bind is named supervise\_conformance\_turn\_end.rs, as the lane's new tests are; the claim model says what it leaves out
- **xtask:** re-record the supervise/run.rs and lib.rs help surfaces after the fix round's prose — read against turn\_end\_loop.rs and the ledger seeding
- aterm-phase,aterm-agent,tools: the engine's screen matchers read aterm-phase's anchors — grep\_guard V1 allows no engine literal
- **aterm-agent:** harness limits.rs and rm\_policy.rs stop linking the modules lane D2 deleted
- aterm-core,aterm-spec,aterm: the observation kernel keys change on the screen generation — an await armed before an equal-seq alternate-screen re-entry now fires
- **aterm-gui:** program= names the agent an atpkg shim launches — a `/bin/sh …/agents/claude` that has not exec'd yet reads `claude`, not `sh`
- aterm-gui,aterm-types,aterm,xtask: the agent verdict comes from aterm-phase's per-program reader — walls publish as agent=wall:\<kind\>, a reading without evidence as unknown
- aterm-spec,aterm-gui: HarnessWorkerLifecycle — one supervisor per session across restarts, reloads and a held claim, derived and proved
- **aterm-gui:** the in-GUI supervisor host — every Claude session this instance owns is supervised under \[harness\], with nothing typed to start it
- aterm-gui,xtask: operator\_host's looks\_like\_approval is fail-closed over aterm-phase's parser — the folder-trust dialog is a box the actuator will not type over
- **aterm:** host\_live — a headless instance with \[harness\] headless = true supervises a fake claude with nothing started by hand
- **aterm-gui:** a stopped supervisor presses nothing — after the cut HostCtl sends only the meta read and unsets it redials for, and refuses key|send|paste|turn|feed-bin as RelayCtl does
- **aterm-gui:** the \[harness\] policy fails closed — a refused switch resolves off, an unknown key turns every approval rule off, and the launcher reads the supervisor's own bit
- **aterm-gui:** a supervised session's wall the supervisor leaves silent still raises its row — 529, api-error, auth and context reach the menu bar and the notification
- aterm-gui,aterm-types,xtask: meta unset supervisor holder=\<h\> gives back only that holder's claim, and await agent keeps limited as a deprecated alias for the limit walls
- aterm-gui,aterm-spec: the supervisor's restart budget is the session's — failures outlive a worker and a program flap, and only a changed policy forgives them
- **aterm-gui:** the harness host's suspend stops every worker before it returns, and its joins wait on done-signals instead of sleep-polling
- **aterm-gui:** the \[harness\] keys the supervisor does not apply yet say so — a notice when set, "(not yet applied)" in the config language, and no worker restart on an edit
- aterm-gui,aterm,aterm-types,xtask: program= renames only atpkg's own shim — a /bin/sh script named claude outside the managed prefix's agents/ or bin/ stays sh
- **aterm:** the live re-entry check arms its await before box B can exist — the go file waits for who's watchers= instead of a 500 ms margin
- aterm-gui,xtask: measure looks\_like\_approval — an ignored, run-by-name instrument, and the numbers
- **aterm-gui:** the stopped-worker transport test is unix-pinned — its scratch server is a Unix-domain socket
- **aterm-gui:** tippy — matches! for the cleanup-request test, and the measurement's closure need not be mut
- aterm-agent,aterm-gui: one supervisor claim per session — the loop's lease under the host's name — and a stopped loop writes nothing
- **aterm:** host\_live's read box offers a readable `No` — lane B2's decider presses a one-shot Yes only beside one
- aterm-gui,aterm-agent: every \[harness\] key the engine reads reaches it — NOT\_YET\_APPLIED is empty — and `upgrade` is a known key that restarts no worker
- aterm-gui,aterm-types: a supervised session's walls are all the supervisor's — no second menu row while its claim is live
- aterm-link,aterm-primer,aterm-cli: presence relays the server's post-reader verdict words — `unknown` and `wall:<kind>` — not the retired `limited`
- **aterm-agent:** answer\_retired keeps its own doc — the grafted upgrade section had split it from its function
- **aterm-gui:** effective() names the key it masks in its doc — the NOT\_THE\_SUPERVISORS constant was never read
- **aterm-agent:** the row guard ends on any whitespace — Claude Code 2.1.281's no-break-space composer takes the continuation, and a guard miss is escalated
- aterm-agent,aterm-types,aterm-gui: the supervisor's attention fits the server's keyed cap — 200 bytes, one constant — and a length refusal is sent again cut
- aterm-phase,aterm-agent: 2.1.281's auto-deny countdown is the box's own row, not the rm breaker's note — the rm rule reads the box again
- aterm-phase,aterm-agent: a turn a person stopped with Esc is never continued — aterm-phase reads the vendor's interrupt row
- **aterm-agent:** a destructive question is escalated in every shape — after a benign offer, with (y/n), or followed by an aside
- aterm-phase,aterm-agent: a message whose head scrolled off still has a tail — its question is judged, never read as nothing asked
- **aterm-agent:** an approval is never pressed with the generation fence dropped — a screen that keeps moving hands the box over
- aterm-agent,aterm-gui: an aterm.toml that fails to load starts the supervisor escalate-only — never the defaults' autonomy
- aterm-agent,aterm-gui: a worker the host restarts hands its badges over — the next loop adopts them, an idle point's too
- **aterm-agent:** a model switch survives a restart — its ledger row says how to undo it, and the next loop switches back at the reset
- **aterm-agent:** a point that answers the live upgrade's announcement is the sweep's — the continue policy types nothing into the wind-down
- aterm-phase,aterm-gui: one program-identity table — the host supervises a Claude Code the server identified by its screen
- **aterm-agent:** `harness limits` classes a notice by aterm-phase's wall table — one reading of the screen, not two
- **aterm-gui:** the agent push's docs say what it carries and who parks on it — the in-GUI supervisor does not yet
- aterm-agent,aterm-cli,aterm-primer: the help says what the integrated build does — the window supervises by default, --auto-reads answers the trust dialog, hooks are swept at start
- aterm-agent,aterm-gui: every hosted session keeps a journal beside its ledger, and both are bounded
- **aterm-agent:** the sweep-lock test waits out a descriptor a concurrent fork still holds
- aterm-agent,aterm-spec: a focus move the dialog dropped is made again from a fresh read — the trust dialog is answered on 2.1.281
- **aterm-agent:** a question dialog's badge names its question — not `4. Chat about this`
- **aterm-gui:** NOT\_YET\_APPLIED and the always-true wall check are gone — a supervised session's verdict rows are the supervisor's
- aterm-phase,aterm-agent,aterm-core,aterm-primer: the hook-era and pre-kernel orphans go — has\_cancel, OWN\_MARK, update\_banner, seen\_alt, seed\_seq\_in
- aterm-agent,aterm-phase,aterm-link: the docs say the server reads the screen and the bridge relays it — and ownership is the attention key
- aterm-gui,aterm-agent: a failing worker waits a growing backoff before its restart; the faulted badge says what ends it; one ATTENTION\_OWNER
- **aterm-gui:** a host thread that ended in a panic is started again by the next policy change or resume, within a budget
- aterm-gui,aterm-agent: the live upgrade is gated by the window's own parse of \[harness\] — one parser in the process
- aterm-gui,aterm-release: tippy -D warnings clean — the agent publish tuple is a named type, and two slices are borrowed, not cloned
- **aterm-gui:** the medium settings rail test expects Software Update's `Updates` label
- docs,aterm-primer,aterm-cli,aterm-agent: the texts say what ships — the window supervises every Claude Code session by default, and the resume probe is gone
- **aterm-agent:** "Should I also …?" is an offer to go on — the live probe's end of turn was escalated where the owner types "keep going"
- **aterm-gui:** the harness fault record appends through open\_append\_0600 — main split open\_0600
- aterm-types,aterm-gui,aterm-messages,docs: the `harness` note lane names no producer this branch deleted
- **aterm-agent:** trustfmt the merged harness switch test
- aterm-gui,aterm-update-core: the merge contract's two reds — the host's fault-history lock names its receiver, and the deleted prelude's variables leave the env allow-list
- **gui:** the macOS update card fits its section beside a failed check; the Linux pager keeps the check's cause; compact rungs know the check switch; the caption names the install switch; the ALab footnote drops retired lanes; one GPU-intent guard per mirror writer
- Reuse cursor neighbor captures for ribbon witness
- Avoid idle Fabric outbox polling and push parked fetches
- Keep paste ordering local to each input sink
- Enroll paste isolation in spec link registry
- Discover newer atpkg indexes behind stuck published tags
- Track atpkg apply-queue file lock in census
- Fix paste isolation boolean assertion lint
- Make index probe tests tolerate transient lock deferrals
- Keep index probe test helper within lint argument limit
- **harness:** derive wake generation from owner presence nonce
- **harness:** bind live upgrades to local PTY and notice owner
- **harness:** fail closed on incomplete session scans and moved tabs
- **harness:** recheck shell tab after relaunch scan
- Use pushed metadata changes for Fabric presence refresh
- Wake signed atpkg pass on first published near index
- Wake Claude upgrade host after atpkg activation
- audit atpkg activation notice help surface
- **aterm-gui:** the timeline record's kind list names agent-change beside main's fetch
- **atpkg:** a one-name verb refuses a second name instead of acting on the first
- **aterm-spec:** the escalation tier names the ty/ay/trust-ir it ran, where it came from, and the store copy it shadowed
- **aterm-spec:** the pending-stub check reads a candidate's head, not the whole \~160 MB tool
- **aterm-gui:** presence tests that read the process-global link take the link section
- **aterm-gui:** kitty\_log's exit tests wait for a detached writer without taking its lock
- **messages:** the band says what finished, its chips read on light themes, a fault warms without a grey frame, the comet's tail falls smoothly
- Keep single vendor head checks off the package lane
- Keep delayed same-caret typed echoes in the rainbow trail
- Avoid vi gate terminal locks for other sessions
- Reuse Claude upgrade roster for preliminary owner checks
- Checkpoint irrelevant Fabric broadcast cursors by session shard
- Recover one exact queued key after unknown insert cleanup
- Refuse blank orphan cursor echoes after unknown inserts
- Wake for higher near index after full pass starts
- Wake for newer published index after older atpkg pass
- Recheck published index promptly behind live package pass
- Reuse captured composed cursor witness rows
- Probe near published indexes every 30 seconds
- Batch Fabric session status reads on one GUI wake
- Align probe model clock with rate-limit fixture
- **gui:** await asynchronous vendor heads in park tests
- Record Fabric help review and satisfy link lint
- **gui:** accept formatted update argv in settings check test
- Make published spacing model catch every wait guard
- Record signed default-set retry in pass witness
- Use retry index provenance for full pass verdict
- Format merged Fabric status parser
- Isolate retry tests and correct help receipt
