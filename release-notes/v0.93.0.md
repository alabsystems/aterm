# aterm v0.93.0

2026-09-25 · 224 changes since v0.92.0

## Features

- **verify:** proportional push admission — a --changed PASS over a fully receipted base, a pure version bump, and one receipt store per repository
- **forge:** OB-1 attests vendor/astream as a pinned git submodule
- **verify, release:** lanes that build a fresh tree carry the astream submodule

## Fixes

- **atpkg:** the next-index probe makes no API request — its steady-state listing is gone
- **atpkg:** a pass finds the next index by HEADs from its floor before it lists
- **atpkg:** the tag walk publishes no index identities — a re-cut under its tag is fetched again
- **atpkg:** a tag walk whose newest release does not arrive whole is the listing
- **atpkg:** the tag walk looks past the 404 that ends its run — a skipped number no longer hides the newest index
- **release:** the mirror step makes zero metered calls — readability is proved on the download host
- **release:** no live bundle under the cut's rm -rf — a liveness gate and a staging dir per claim
- **release:** the website follows the cut unjournaled — a site failure never parks the journal
- **release:** the cut ships the published tree and states its ungated range
- **release:** the launcher defaults the credentials profile, and the signature line compares what it names
- **release:** the published-range refusal prints what the engine will actually do
- **release:** the ledger claim re-proves the published range on every tip it commits on
- **release:** the launcher forwards PUBLICATION\_ENGINE, and its env list is held to the cutter's source
- **atpkg:** the index probe waits out a spawn window before calling its lock held
- **publish:** the /Users/ rewrite matches only the guard's shape — PROVIDES\_NEVER ships as written
- **atpkg:** split the AKIA fixture at source — the next stage's content guard refused it
- **verify:** the merge gate runs the export and preflight suites — the install-channel stage becomes release tooling
- **verify:** the tests that measure the machine run alone, and the test run stops waiting for tippy
- **verify:** a red the machine caused is COULD NOT RUN, not a finding
- **verify:** every stage child leads its own process group, and the ceiling and an interrupt take the whole group
- **verify:** each stage's finish line, with its outcome, reaches the run log as it happens
- **release:** a test comment no longer cites a path that does not exist
- **verify:** a finding that shares a refusal's test name is still a finding
- **verify:** a narrowed run admits no push — proportional admission is retired
- **verify:** a weaker receipt never replaces a whole-tree one, and a dirty tree gets none
- **verify:** the hook's format refusal is true for a file that was never a receipt
- **update:** a skipping check announces a newer build a sibling staged — once per build
- **install:** read the channel from the download host alone — one pointer HEAD, then tag-specific GETs; no GitHub API, no gh lane
- **release:** the cut owns `latest` — one head PATCH on every channel path, appcast pair last, never over a newer head
- **install:** a checkout installs from its update channel, never from the private source repository
- **release:** the channel scan skips prereleases, as the client does
- **release:** a non-app release holding `latest` is no head to protect
- **release:** an older head serving no appcast is no head to protect
- **release:** the channel floors run before the mirror binds a release, and retire leaves an adopted release in place
- **release:** the head PATCH body is one table that the live call and the fake GitHub both send
- **release:** delete check-release-shape's --pointer-only stopgap mode; a stolen pointer's remedy resumes the cut
- **publish:** the prerelease opt-in is this tree's committed policy, beside the cutter that flips it
- **release:** the DMG is the plain UDZO image — the Finder layout that never shipped from the launcher is gone
- **release:** the behavioural self-check runs once per cut — upload, preflip and flip re-prove dist/ by digest
- **release:** the cut builds exactly the commit `pub publish` published — peer pushes neither block it nor leak into it
- **release:** the cutter's remedies name the launcher, and the DONE line promises no deadline
- **release:** a claim that fails before its push leaves the checkout on the published commit
- **release:** `ship status` names the commit the next cut builds, not this checkout's version
- **release:** the cut builds in its own cut tree — the checkout it is launched from never moves
- **release:** yank runs from any clean checkout, and its successor is handed over as a cut
- **atpkg:** the pass record is honest and modelled — a success means the index was reached, a failure is stamped
- **atpkg:** the index discovery's link verdict is the last walk's
- **atpkg:** the index walk steps over one missing number — a gap above the floor no longer strands a store
- **atpkg:** a redirect to any GitHub user-content host is a published asset — a storage move cannot blind discovery
- **release:** one version — the binary reports Cargo.toml's, and the cutter has no version knob
- **release:** every surviving remedy names the launcher or `targo --unverified ship`, never `cargo ship`
- **types:** the version test's manifest reader passes tippy -D warnings
- **atpkg-index:** a publish carries what it does not change — yanks, min\_build and unpinned program blocks
- **atpkg-index:** publish straight to the public channel at baseline+1; packs mirrored by download-host HEADs
- **atpkg-auto-alab:** build only what was signed — SHA256SUMS.sig verified, dev commit bound through the ledger
- **gui, atpkg:** three checks main added meet the merged tree
- **gui:** the toolbar drive makes its window key before auditing tab clicks
- **fabric:** reject a misframed bridge roster
- **atpkg:** a dev-link-only pass says what it kept, and the merged tests speak the union record
- **aterm-update:** the checker clock is the macOS lane's, so Linux builds no dead code
- **aterm-release:** the liveness refusal's test reads the kernel process table's words
- **aterm-release:** the cut journal is format 10, so the journal 0.92 left gets its one-sentence refusal
- **install:** the Linux target's head-only pre-flight is a function the channel suite tests, and the catalog tests go with the catalog
- **publish:** test-after-cut's releases are complete apps again, and the gate runs the suite
- **gui:** an echo stamp is placed at the instant it was read — an Enter no longer lands before the paste it followed
- **harness:** an upgrade restart records the model before, confirms it after, and says both
- **harness:** the upgrade's model check never retypes the carry-on, never blocks a sweep, and blames what decided the model
- **atpkg:** a link to a reroute stub on PATH never execs the stub forever
- **atpkg:** outside aterm, a laid stub makes the user's own claude a note, never SHADOWED
- **atpkg:** a copy of a reroute stub never loops, and outside aterm no stub still means a note
- **atpkg:** main's outside-aterm doctor test calls run\_with without the retired token source
- **xtask:** a citation escaped in a shell heredoc names a file that exists
- **publish:** the export check pins relocate.rs's bare /Users/ alone, because PROVIDES\_NEVER went with the pkg protocol
- **verify:** the gate runs test-atpkg-pack-toolchain.sh, whose B7-B11 pin the version a bundle pack signs
- **core, render, gpu, gui, messages:** the audit of the scroll and full-width-meter work, every finding fixed at its root
- **core, render, gpu, gui, verify, agent, paint:** the second audit's findings, and one gate per machine
- **gui, verify, tests:** the load reds, each at its cause — no timeout raised to hide one
- **verify, gui, tests:** the third audit of my own work — a gate that would have hung on itself, and six more
- **verify, gui:** the fourth audit round, and the gate's own red — a lock test racing a forked child
- **verify, census, tools:** main's two gate reds, and the machine lock unlocks instead of closing
- **update:** the engine and codec stop turning screen content into update refusals
- **update:** the successor degrades one refused screen instead of refusing the handoff
- **update:** the producer carries every screen at a rung instead of refusing the update
- **update:** a capture refusal is its own typed lane, never a park miss
- **update:** no latch fires early, nothing strands silently, a busy job no longer holds the park
- **update:** v0.91.0's real handoff bytes guard the consumer, and the landing row names repainted tabs
- **update:** the review's seven findings — no garbled tail, no held pane, priced as the consumer prices
- **update:** the merge review — marks stay signed on blank tabs, one health announcement, one verdict per fact
- **gui:** an adopted session takes the engine lock once — the lock-order census read two exclusive arms as a re-entrant `term`
- **lock:** aterm-sysprobe moves to 0.93.0 in Cargo.lock with the rest of the workspace

## Performance

- stream agent status screen fingerprint
- **fabric:** use identity-only roster for bridge refresh
- **fabric:** arm pending asks at their earliest deadline
- **atpkg:** avoid helper threads on near-only index probes
- **agent:** poll Claude composer from a terminal tail

## Refactors

- **publish:** fix fixtures at source — the export's sanitize table and glyph tripwires go
- **publish:** one version source — Cargo.toml; VERSION\_DEFAULT, version-sites and the export's normalization go
- **xtask:** delete the help-surfaces prose-hash gate
- **update:** one unmetered web lane — delete the token lane, its holds and budget, and the metered listing fallback
- **keys:** retire K1 — the paper master is the one anchor, and the cutter asks only the roster
- **install:** delete the update-token half — nothing reads a credential, so nothing writes one
- **publish:** delete post-promote's evergreen-pointer stopgap and the README's 404 caveat
- **release:** adopting a release the journal holds no intent for is a pure classifier, table-tested
- **release:** the cut stops placing a dev install at dist/aterm.app, and forgets the retired dist/cut-app
- **release:** one journal format — an older cut journal is refused in one sentence, never walked
- **atpkg:** one transport — the package client reads every byte from the release download host
- **atpkg:** the next-index probe HEADs one tag — floor+1 — and not on a store with nothing to update
- **update-core:** delete the token module — nothing resolves a credential any more
- **atpkg-index:** the stale-pin gate is gone — the ALab lane's decision table decides staleness
- **atpkg:** retire the claude/codex auto-vendor lane — the spec lists them unpinned
- **atpkg-index:** every listed program is pinned — claude and codex keep their last pins as floors, and the `-` build is gone
- **link:** the bridge reads caps, walks Last faces and connects through astream

## Tests

- **spec:** ReleaseChannelHead — the derived writer/reader contract on the channel's `latest`, Tier-1 bound to the real sequence
- **release:** the claim's writer/reader contract is a derived model, bound to the real claim and reader
- **atpkg:** an index re-pin of claude/codex never downgrades or fights — the pin is a floor only where it is installed
- **verify:** the gate runs the ALab lane's suite, and PROCESS.md names the stage as plan() does
- **spec:** the index channel's publisher and tag walk are one derived contract, bound to the real indexer and the real walk
- **atpkg:** the index-channel conformance binds the merged tag walk, not the deleted lane
- **atpkg:** the index probe's conformance judges ONE attempt; the lock-deferral retry hid the defect it waited out
- **spec:** drop sixteen tests that the tiered discharge and the registry sweeps already run
- **spec-macros:** fold eleven one-assert derive and attribute tests into three labelled tables
- **census:** stop re-walking the real tree that the freeze-safety-gate build already walks
- **forge:** hold the cells under their measured rows instead of at them, and drop the duplicate surveys
- **verify:** fold the driven-stage exit-code family and drop the prose, argv and historical-ladder pins
- **xtask:** drop the prose and hook pins, fold the non-vacuity red fixtures into one table, trim perf's compare twins
- **conformance:** gate the paint and spin files at the crate root instead of printing a not-run test
- **forge:** tie tools/forge-budget.tsv back to the measured rows — every cell's five rows must exist, sit at or under measured.rs, and bound the live graph
- **verify:** hold the selftest's skip rows at a literal 49, so an atpkg suite cannot leave the ladder with the crate green
- **lz4:** delete the never-compiled raw-pointer decoder and its 10 tests
- **parser:** drop 11 SIMD scan cases the differential already covers, table 11 CSI cases
- **selection:** delete 9 rule-pattern cases a twin test already asserts
- **search:** delete 30 inline streaming twins, fold 22 matcher cases into 2 tables, drop a print-only timer
- **scrollback:** delete 17 lz4 cases left behind when the codec moved, 2 print-only probes; take the 6 Scrollback proptests from aterm-core
- **grid:** fold bootstrap filler into exhaustive walks, delete twin suites and dead probes
- **core:** delete never-compiled and dead-code tests, move Scrollback proptests out, fold six copy-paste families
- **aterm-render:** retire never-run instruments and old-formula mirrors, fold the no-op law and the pixel-pipeline smoke into tables
- **aterm-render-api:** the Rasterizer dyn-compatibility pin becomes a const item
- **aterm-scene:** drop the never-run fill-path perf bench
- **aterm-gpu:** retire probes, rung and scanner duplicates, fold the no-op law and the cursor shapes into tables
- **aterm-effects:** retire never-run reports, benches and example tests, fold the twin families into tables
- **aterm-objc:** retire the timing printers and the count change-detectors
- **aterm-primer:** drop three prose pins on the primer block
- **aterm-gpu:** restore the scan-pairing pin and the video\_tap xref pin
- **pty:** the fifo-EOF wake is judged against its slice, not against an idle machine
- **paint:** the flying-kitty row's typist keeps its own clock, so a starved subject is still typed AT, not pecked at
- **core:** the match-scaling row compares the fastest batches, so it measures the algorithm and not the scheduler
- **handoff:** v0.92.0's producer bytes, and the nonce carry they now send

## Docs

- **manual:** the pre-push hook blocks; it is not advisory
- **update:** state the real publish-to-applied bound — about 50 minutes plus the download, not "a minute"
- **release:** the runbook's pin and signing text is this tree's — the paper master's fingerprint, checked on every cut
- **atpkg:** the transport redesign's listing step is done — the client makes zero API requests
- **atpkg:** the pass's docs name the index source it reaches, not a Releases listing
- **atpkg:** discovery's module doc says where the index is read from now
- **atpkg:** selfupdate's purity test stops guarding against a gate that no longer exists
- **atpkg-keys:** the module doc names verify-signed, and its help surface is re-read
- verify Fabric bridge roster help surfaces
- **atpkg:** the vendor-direct design no longer describes cutover tooling this tree does not carry
- **atpkg:** the legacy ceiling says what the lane does above it — read the signed manifest, then decide
- **help:** record the merged manual.rs read — the reroute residuals and upstream's messages Phase 6 lines, both true
- **gate:** reconcile astream help surfaces and citation
- **xtask:** the six help surfaces two of today's refactors moved, re-read against their handlers
- the prose names what the merge left: no authoring script, a count-based stand-down, two measuring families
- three sentences the m7 merge left describing the metered API and a listing lock

## Other

- Wake package lane when contending store writer releases
- Skip quiet Claude harness session history scans
- Recover queued rainbow echoes and trim frame work
- Fix registry count and cross-platform parity gate
- **build(astream):** vendor/astream becomes a git submodule of astream
- **aterm-agent:** a command substitution is a word of the command around it, and a name the classifier cannot read is never cleared — `$(printf 'r''m') -rf /usr` was pressed as a read
- **aterm-agent:** an awk program or a sed script a PARAMETER supplies is refused, as the module law already says — the code cleared it (defect 1 of the … review)
- **aterm-agent:** a DOUBLE-QUOTED "$(…)" is one word a flag-selecting read may keep — the flag rule refused every "$…" and escalated real reads (defect 3 of the … review)
- **aterm-agent:** a git read whose form is a COUNT or a VERB refuses an unquoted substitution — one splits into any number of words (defect 2 of the … review)
- **aterm-agent:** an expansion's value decides how its word starts — a quoted one can still BE the flag, so a flag-selecting read or git refuses any before `--`
- **messages:** the `messages` read verb and the `notice` write verb — a script's own progress on the band, everything else to the log
- **messages:** Phase 6 — the last status-bar shim goes, a script's notes keep their own log, one clock per `messages` reply, and the docs land
- **xtask:** re-record the aterm-primer help-surfaces row after the prose-pin trim
- **aterm-core:** stop re-exporting aterm-types' callback registry — nothing reads it
- **aterm-types:** 723 → 473 tests — one-case copies become labelled tables, dead code and its tests go
- **aterm-ffi-types:** 88 → 43 tests — the FFI helpers nothing calls go with their tests
- **aterm-messages:** 167 → 162 tests — five adversarial probes whose fixes carry center.rs regressions
- **aterm-grapheme:** 140 → 105 tests — the UAX #29 suite already drives the hand-picked clusters
- **aterm-alloc:** 88 → 63 tests — ArrayVec cases the upstream differential already drives
- **aterm-codec:** 73 → 62 tests — base64 cases the crates.io oracle and the in-file fuzz already reach
- **aterm-containment:** 249 → 211 tests — sanitizer twins, vacuous global-state probes, and three allowlist tables
- **aterm-policy:** 112 → 80 tests — per-profile decisions, selector matches and mirror snapshots become tables
- **aterm-provenance:** 60 → 48 tests — lattice corollaries of the pinned 36-cell join table
- **aterm-shell-integration:** 125 → 116 tests — static pins the live bash tests or a kept pin already prove
- **aterm-json:** drop the corpus walker's self-test — it tests a test helper
- **aterm-png:** drop the corpus walker's self-test — it tests a test helper
- **aterm-toml:** drop the corpus walker's self-test — it tests a test helper
- **aterm-lexicon:** 132 → 130 tests — two copies of the builtin conflict-free check
- **aterm-hash:** 11 → 6 tests — std collection behaviour and f(x) == f(x) restatements
- **aterm-log:** 25 → 23 tests — two probes that could not fail
- **aterm-uds:** 36 → 35 tests — the fd-number pin becomes a compile-time assert
- **aterm-objc:** 196 → 190 tests — timing printers and retired objc2-exit change detectors
- **aterm-vi:** 114 → 93 tests — bracket matches and word-motion landings become tables
- **aterm-digest:** 39 → 33 tests — the seven RFC 4231 vectors become one table
- **aterm-rle:** 68 → 62 tests — single-element set and set\_range become tables
- **aterm-ffi-types:** 43 → 40 tests — the four cap-boundary tests become one table
- **aterm-objc:** the prose that cited the three retired exit-condition pins says they are retired
- **aterm-lexicon:** corpus.rs names the conflict-free check that survived …
- **atpkg:** delete the OS-installer protocols and extras (design 2026-09-22 §5.3(b)/(c))
- **atpkg:** review of the §5.3(b)/(c) deletion — remove what it left dead, correct what it left false
- **atpkg:** the in-place heal is the one cure for com.apple.provenance — the untracked lanes, their records and the tag-relay are deleted
- **review(cl/provenance):** repair heals an empty store, the tracked-cutter refusal names both causes, the lane's last dead code and stale prose go
- **atpkg:** final legacy ceiling and floors, stale rustup trust repaired, seed producers and the dead dissent deleted, one daily machine-apply claim, the Rosetta pass writes its own row
- atpkg review: doctor says a stale rustup link once, one staleness rule for doctor and repair, the bundle lane derives the version it signs
- **gui:** Software Update keeps "Checked N ago." beside the automatic-checks sentence
- **gui:** Settings reads the GPU capability its host presents; the process-global mirror is gone
- **gui:** the routine update is silent — no automatic staged row, no landing burst, no rising arrow (§5.3(d))
- **gui:** review of §5.3(d) — the editor blocker reads and is said once, serious mode stills the rim, the words match the silent path
- **atpkg:** the reroute tests exec only assessed programs; the probe fixtures stop retrying
- **aterm-update-core:** a dropped FileLock is free at once — LOCK\_UN, not the close
- **atpkg:** a queued pass stands down on a moved pass COUNT, not a same-second clock
- toolchain discovery: one order everywhere, no build tree; cells rides the right cargo; certified NOT RUN; cells-foreign red at the verb
- **atpkg:** the reroute test programs are shared across runs, laid by rename
- toolchain discovery, review: the kani lane's retired probes go too; a from-source toolchain is reached SEALED
- **review:** reroute test programs off the shared /tmp; sh() runs only what the kernel would; stale lock comment
- **land:** two false sentences the five-branch landing left behind
- **gate:** the merge contract's six reds on the update-robustness branch — a reserved scope word, the fixture recordings, a placeholder citation, and two tests that timed the machine
- **messages:** explain very heavy system use — "Typing slowed by \<cause\>" only when typing measurably lags and a named cause saturates the machine
