# Modified by the aterm project in 2026; see the repository NOTICE.
# (Adds the `aterm-test-key-event` feature; see the comment on it below. Adds the macOS-gated
#  first-party `aterm-objc` dependency the ported declared classes are built on. NARROWS the
#  `objc2` and `block2` rows from `cfg(any(ios, macos))` to `cfg(macos)`; see the note on the
#  `objc2` row for why that line is a precondition of the objc2 exit condition.)
# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.70.0"
name = "winit"
version = "0.30.13"
authors = [
    "The winit contributors",
    "Pierre Krieger <pierre.krieger1708@gmail.com>",
]
build = "build.rs"
include = [
    "/build.rs",
    "/docs",
    "/examples",
    "/FEATURES.md",
    "/LICENSE",
    "/src",
    "!/src/platform_impl/web/script",
    "/src/platform_impl/web/script/**/*.min.js",
    "/tests",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Cross-platform window creation library."
documentation = "https://docs.rs/winit"
readme = "README.md"
keywords = ["windowing"]
categories = ["gui"]
license = "Apache-2.0"
repository = "https://github.com/rust-windowing/winit"

[package.metadata.docs.rs]
features = [
    "rwh_04",
    "rwh_05",
    "rwh_06",
    "serde",
    "mint",
    "android-native-activity",
]
targets = [
    "i686-pc-windows-msvc",
    "x86_64-pc-windows-msvc",
    "x86_64-apple-darwin",
    "i686-unknown-linux-gnu",
    "x86_64-unknown-linux-gnu",
    "x86_64-apple-ios",
    "aarch64-linux-android",
    "wasm32-unknown-unknown",
]
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[features]
# aterm's downstream GUI conformance tests must exercise the real `KeyEvent`
# dispatch boundary. Winit intentionally keeps the platform payload private, so
# expose its synthetic-event constructor only when aterm's dev-dependency asks
# for it; production builds leave this feature disabled.
aterm-test-key-event = []
android-game-activity = ["android-activity/game-activity"]
android-native-activity = ["android-activity/native-activity"]
default = [
    "rwh_06",
    "x11",
    "wayland",
    "wayland-dlopen",
    "wayland-csd-adwaita",
]
mint = ["dpi/mint"]
rwh_04 = [
    "dep:rwh_04",
    "ndk/rwh_04",
]
rwh_05 = [
    "dep:rwh_05",
    "ndk/rwh_05",
]
rwh_06 = [
    "dep:rwh_06",
    "ndk/rwh_06",
]
serde = [
    "dep:serde",
    "cursor-icon/serde",
    "smol_str/serde",
    "dpi/serde",
]
wayland = [
    "wayland-client",
    "wayland-backend",
    "wayland-protocols",
    "wayland-protocols-plasma",
    "sctk",
    "ahash",
    "memmap2",
]
wayland-csd-adwaita = [
    "sctk-adwaita",
    "sctk-adwaita/ab_glyph",
]
wayland-csd-adwaita-crossfont = [
    "sctk-adwaita",
    "sctk-adwaita/crossfont",
]
wayland-csd-adwaita-notitle = ["sctk-adwaita"]
wayland-dlopen = ["wayland-backend/dlopen"]
x11 = [
    "x11-dl",
    "bytemuck",
    "percent-encoding",
    "xkbcommon-dl/x11",
    "x11rb",
]

[lib]
name = "winit"
path = "src/lib.rs"

[[example]]
name = "child_window"
path = "examples/child_window.rs"

[[example]]
name = "control_flow"
path = "examples/control_flow.rs"

[[example]]
name = "pump_events"
path = "examples/pump_events.rs"

[[example]]
name = "run_on_demand"
path = "examples/run_on_demand.rs"

[[example]]
name = "window"
path = "examples/window.rs"
doc-scrape-examples = true

[[example]]
name = "x11_embed"
path = "examples/x11_embed.rs"

[[test]]
name = "send_objects"
path = "tests/send_objects.rs"

[[test]]
name = "serde_objects"
path = "tests/serde_objects.rs"

[[test]]
name = "sync_object"
path = "tests/sync_object.rs"

[dependencies.bitflags]
version = "2"

[dependencies.cursor-icon]
version = "1.1.0"

[dependencies.dpi]
version = "0.1.1"

[dependencies.rwh_04]
version = "0.4"
optional = true
package = "raw-window-handle"

[dependencies.rwh_05]
version = "0.5.2"
features = ["std"]
optional = true
package = "raw-window-handle"

[dependencies.rwh_06]
version = "0.6"
features = ["std"]
optional = true
package = "raw-window-handle"

[dependencies.serde]
version = "1"
features = ["serde_derive"]
optional = true

[dependencies.smol_str]
version = "0.2.0"

[dependencies.tracing]
version = "0.1.40"
default-features = false

[dev-dependencies.image]
version = "0.25.0"
features = ["png"]
default-features = false

[dev-dependencies.tracing]
version = "0.1.40"
features = ["log"]
default-features = false

[dev-dependencies.tracing-subscriber]
version = "0.3.18"
features = ["env-filter"]

[build-dependencies.cfg_aliases]
version = "0.2.1"

[target.'cfg(all(target_family = "wasm", target_feature = "atomics"))'.dependencies.atomic-waker]
version = "1"

[target.'cfg(all(target_family = "wasm", target_feature = "atomics"))'.dependencies.concurrent-queue]
version = "2"
default-features = false

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.ahash]
# FORK TRIM (aterm): DEFAULT FEATURES OFF, and `std` re-added explicitly.
# ahash's default set is ["std", "runtime-rng"]; the omitted arm is `runtime-rng`,
# which is the ONLY thing activating its optional `getrandom`.
# COST: -1 package / -2,920 lines on the LINUX cell only (getrandom 0.3.4). ahash
# sits under this unix-not-apple target table, so mac/win/wasm never saw it; the
# getrandom 0.2.17 that `ring` pulls is a different package-version and stays.
# WHY THIS LINE WAS ALREADY WRONG: the row asked for `no-rng` but never set
# `default-features = false`, and features are additive — so `runtime-rng` came in
# anyway and WON. In ahash-0.8.12/src/random_state.rs:47-106 the cfg_if cascade
# tests `else if #[cfg(all(feature = "runtime-rng", not(fuzzing)))]` (line 67)
# BEFORE the bare `else` fallthrough (line 101), so the declared `no-rng` intent
# was a no-op. `no-rng = []` is an empty feature; it selects nothing by itself.
# getrandom has exactly ONE call site in the crate: random_state.rs:77
# `getrandom::fill(&mut result)`, inside that arm.
# `std` MUST be re-added: it gates the `hash_map`/`hash_set` modules and the
# AHashMap / AHashSet / HashMap / HashSet aliases (ahash src/lib.rs:122-135), which
# are the only items this fork imports (src/platform_impl/linux/wayland/state.rs:7,
# wayland/seat/mod.rs:7, wayland/window/state.rs:7). `no-rng` MUST be kept: it is
# one of the three features gating `impl Default for RandomState`
# (random_state.rs:370), which would otherwise disappear.
# HONEST CAVEAT — THIS IS A BEHAVIOUR CHANGE, NOT DEAD CODE. The selected arm
# seeds the Wayland backend's internal AHashMaps with the fixed PI/PI2 constants
# (random_state.rs:33-45) instead of OS randomness. Those maps are keyed by
# compositor object ids (WlOutput / WlSeat / WindowId), never by attacker-supplied
# strings, so the HashDoS argument for random seeding does not apply here — and
# this is the posture upstream winit declared it wanted on this very line.
version = "0.8.7"
default-features = false
features = ["std", "no-rng"]
optional = true

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.bytemuck]
version = "1.13.1"
optional = true
default-features = false

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.calloop]
version = "0.13.0"

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.libc]
version = "0.2.64"

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.memmap2]
version = "0.9.0"
optional = true

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.percent-encoding]
version = "2.0"
optional = true

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.rustix]
version = "0.38.4"
features = [
    "std",
    "system",
    "thread",
    "process",
]
default-features = false

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.sctk]
version = "0.19.2"
features = ["calloop"]
optional = true
default-features = false
package = "smithay-client-toolkit"

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.sctk-adwaita]
version = "0.10.1"
optional = true
default-features = false

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.wayland-backend]
version = "0.3.10"
features = ["client_system"]
optional = true
default-features = false

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.wayland-client]
version = "0.31.10"
optional = true

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.wayland-protocols]
version = "0.32.8"
features = ["staging"]
optional = true

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.wayland-protocols-plasma]
version = "0.3.8"
features = ["client"]
optional = true

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.x11-dl]
version = "2.19.1"
optional = true

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.x11rb]
version = "0.13.0"
features = [
    "allow-unsafe-code",
    "dl-libxcb",
    "randr",
    "resource_manager",
    "xinput",
    "xkb",
]
optional = true
default-features = false

[target.'cfg(all(unix, not(any(target_os = "redox", target_family = "wasm", target_os = "android", target_os = "ios", target_os = "macos"))))'.dependencies.xkbcommon-dl]
version = "0.4.2"

# LOCAL PATCH (aterm), W9 phase 3: narrowed from `cfg(any(ios, macos))`; the
# reasoning is on the `objc2` row below and applies unchanged.
[target.'cfg(target_os = "macos")'.dependencies.block2]
version = "0.5.1"

[target.'cfg(any(target_os = "ios", target_os = "macos"))'.dependencies.core-foundation]
version = "0.9.3"

# LOCAL PATCH (aterm), W9 phase 3: `cfg(macos)`, NOT upstream's
# `cfg(any(ios, macos))` — and the difference is the whole objc2 exit condition.
#
# `aterm-objc` is replacing `objc2` file by file across `platform_impl/macos`,
# `src/platform/macos.rs` and `crates/aterm-gui/src`. Upstream's disjunction
# resolves TRUE on `aarch64-apple-darwin` — the only target aterm ships for this
# backend — so with it in place `objc2` would stay in the mac-arm dependency
# graph, and in `forge`'s package count, even on the day the last macOS use is
# gone: the `platform_impl/ios` backend would still be asking for it. That is a
# dependency nothing compiled could reach, held by a `cfg` nothing compiled
# satisfies.
#
# WHAT THIS COSTS, stated rather than discovered later: `aterm` ships no iOS
# target, and the iOS backend is 3,743 lines across 9 files (8 of them, 3,691
# lines, use `objc2` or `block2`) that no aterm build compiles. Narrowing this
# row means a HYPOTHETICAL `--target *-apple-ios` build of this fork no longer
# resolves `objc2`. Restoring it is one line for anyone who wants that target
# back; leaving it as upstream wrote it would mean the exit condition can never
# be met by porting, only by editing this file at the end anyway.
#
# `block2` above is narrowed for the same reason and by the same argument (it
# is a separate crate on the same exit condition). `core-foundation` above is
# NOT: it is not in the `objc2` family and narrowing it would buy this exit
# condition nothing.
[target.'cfg(target_os = "macos")'.dependencies.objc2]
version = "0.5.2"
features = ["relax-sign-encoding"]

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dev-dependencies.softbuffer]
version = "0.4.0"
features = [
    "x11",
    "x11-dlopen",
    "wayland",
    "wayland-dlopen",
]
default-features = false

[target.'cfg(target_family = "wasm")'.dependencies.js-sys]
version = "0.3.70"

[target.'cfg(target_family = "wasm")'.dependencies.pin-project]
version = "1"

[target.'cfg(target_family = "wasm")'.dependencies.wasm-bindgen]
version = "0.2.93"

# FORK TRIM (aterm): DEFAULT FEATURES OFF. wasm-bindgen-futures' default set is
# just ["std"], and `std` = ["wasm-bindgen/std", "js-sys/std", "web-sys/std",
# "futures-util"] — the omitted arm is that last, OPTIONAL `futures-util`.
# COST: -5 packages / -36,126 lines on the WASM cell only (futures-util 26,215 +
# futures-core 840 + futures-task 815 + slab 2,609 + pin-project-lite 5,647); the
# crate is absent from mac/win, and the futures-util on linux belongs to
# accesskit_unix and is untouched.
# WHY THE ARM IS DEAD: `futures_util` is named in exactly ONE place in the crate,
# wasm-bindgen-futures-0.4.58/src/lib.rs:50 `use futures_util::FutureExt;`, under
# `#[cfg(all(target_arch = "wasm32", feature = "std", panic = "unwind"))]`. Its
# sole consumer is the catch_unwind variant of `future_to_promise` (src/lib.rs:261);
# the equivalent ungated variant at src/lib.rs:223 is compiled instead. NOTHING in
# this graph calls `future_to_promise` — winit's web backend touches only
# `spawn_local` (src/platform_impl/web/main_thread.rs:42, web/cursor.rs:143 and
# :260, web/async/wrapper.rs:46) and `JsFuture` (web/cursor.rs:17), declared
# UNGATED at wasm-bindgen-futures src/lib.rs:92 and :113.
# The no_std arm is clean: `#![cfg_attr(not(feature = "std"), no_std)]` at
# src/lib.rs:33, and the only `std::` path in the whole src tree is src/lib.rs:263,
# inside that same cfg.
# `wasm-bindgen/std`, `js-sys/std` and `web-sys/std` are NOT lost with it: winit
# declares all three plainly with defaults on in this same wasm target table below.
[target.'cfg(target_family = "wasm")'.dependencies.wasm-bindgen-futures]
version = "0.4.43"
default-features = false

[target.'cfg(target_family = "wasm")'.dependencies.web-time]
version = "1"

[target.'cfg(target_family = "wasm")'.dependencies.web_sys]
version = "0.3.70"
features = [
    "AbortController",
    "AbortSignal",
    "Blob",
    "BlobPropertyBag",
    "console",
    "CssStyleDeclaration",
    "Document",
    "DomException",
    "DomRect",
    "DomRectReadOnly",
    "Element",
    "Event",
    "EventTarget",
    "FocusEvent",
    "HtmlCanvasElement",
    "HtmlElement",
    "HtmlImageElement",
    "ImageBitmap",
    "ImageBitmapOptions",
    "ImageBitmapRenderingContext",
    "ImageData",
    "IntersectionObserver",
    "IntersectionObserverEntry",
    "KeyboardEvent",
    "MediaQueryList",
    "MessageChannel",
    "MessagePort",
    "Navigator",
    "Node",
    "OrientationLockType",
    "OrientationType",
    "PageTransitionEvent",
    "Permissions",
    "PermissionState",
    "PermissionStatus",
    "PointerEvent",
    "PremultiplyAlpha",
    "ResizeObserver",
    "ResizeObserverBoxOptions",
    "ResizeObserverEntry",
    "ResizeObserverOptions",
    "ResizeObserverSize",
    "Screen",
    "ScreenOrientation",
    "Url",
    "VisibilityState",
    "WheelEvent",
    "Window",
    "Worker",
]
package = "web-sys"

[target.'cfg(target_family = "wasm")'.dev-dependencies.console_error_panic_hook]
version = "0.1"

[target.'cfg(target_family = "wasm")'.dev-dependencies.tracing-web]
version = "0.1"

[target.'cfg(target_os = "android")'.dependencies.android-activity]
version = "0.6.0"

[target.'cfg(target_os = "android")'.dependencies.ndk]
version = "0.9.0"
default-features = false

[target.'cfg(target_os = "ios")'.dependencies.objc2-foundation]
version = "0.2.2"
features = [
    "block2",
    "dispatch",
    "NSArray",
    "NSEnumerator",
    "NSGeometry",
    "NSObjCRuntime",
    "NSOperation",
    "NSString",
    "NSProcessInfo",
    "NSThread",
    "NSSet",
]

[target.'cfg(target_os = "ios")'.dependencies.objc2-ui-kit]
version = "0.2.2"
features = [
    "UIApplication",
    "UIDevice",
    "UIEvent",
    "UIGeometry",
    "UIGestureRecognizer",
    "UITextInput",
    "UITextInputTraits",
    "UIOrientation",
    "UIPanGestureRecognizer",
    "UIPinchGestureRecognizer",
    "UIResponder",
    "UIRotationGestureRecognizer",
    "UIScreen",
    "UIScreenMode",
    "UITapGestureRecognizer",
    "UITouch",
    "UITraitCollection",
    "UIView",
    "UIViewController",
    "UIWindow",
]

[target.'cfg(target_os = "macos")'.dependencies.core-graphics]
version = "0.23.1"

# LOCAL PATCH (aterm): aterm's own Objective-C runtime layer — `objc_allocateClassPair`,
# `class_addMethod`/`class_addIvar`/`class_addProtocol`, typed sends, the block ABI, and the
# main-thread witness that gates instantiation. This backend's DECLARED CLASSES are moving off
# `objc2`'s `declare_class!` onto it, one file at a time; `window_delegate.rs` is the first.
# FIRST-PARTY and path-only: it is a workspace member of the repository that owns this fork, has
# ZERO third-party dependencies by construction, and is macOS-gated here exactly as the AppKit
# bindings beside it are. It removes no package from the graph on its own and does not claim to —
# `objc2` leaves only when every file in this backend has stopped using it.
[target.'cfg(target_os = "macos")'.dependencies.aterm-objc]
path = "../../crates/aterm-objc"


[target.'cfg(target_os = "macos")'.dependencies.objc2-app-kit]
version = "0.2.2"
features = [
    "NSAppearance",
    "NSApplication",
    "NSBitmapImageRep",
    "NSButton",
    "NSColor",
    "NSControl",
    "NSCursor",
    "NSDragging",
    "NSEvent",
    "NSGraphics",
    "NSGraphicsContext",
    "NSImage",
    "NSImageRep",
    "NSMenu",
    "NSMenuItem",
    "NSOpenGLView",
    "NSPasteboard",
    "NSResponder",
    "NSRunningApplication",
    "NSScreen",
    "NSTextInputClient",
    "NSTextInputContext",
    "NSView",
    "NSWindow",
    "NSWindowScripting",
    "NSWindowTabGroup",
]

[target.'cfg(target_os = "macos")'.dependencies.objc2-foundation]
version = "0.2.2"
features = [
    "block2",
    "dispatch",
    "NSArray",
    "NSAttributedString",
    "NSData",
    "NSDictionary",
    "NSDistributedNotificationCenter",
    "NSEnumerator",
    "NSKeyValueObserving",
    "NSNotification",
    "NSObjCRuntime",
    "NSPathUtilities",
    "NSProcessInfo",
    "NSRunLoop",
    "NSString",
    "NSThread",
    "NSValue",
]

[target.'cfg(target_os = "redox")'.dependencies.orbclient]
version = "0.3.47"
default-features = false

[target.'cfg(target_os = "redox")'.dependencies.redox_syscall]
version = "0.4.1"

[target.'cfg(target_os = "windows")'.dependencies.unicode-segmentation]
version = "1.7.1"

[target.'cfg(target_os = "windows")'.dependencies.windows-sys]
version = "0.52.0"
features = [
    "Win32_Devices_HumanInterfaceDevice",
    "Win32_Foundation",
    "Win32_Globalization",
    "Win32_Graphics_Dwm",
    "Win32_Graphics_Gdi",
    "Win32_Media",
    "Win32_System_Com_StructuredStorage",
    "Win32_System_Com",
    "Win32_System_LibraryLoader",
    "Win32_System_Ole",
    "Win32_Security",
    "Win32_System_SystemInformation",
    "Win32_System_SystemServices",
    "Win32_System_Threading",
    "Win32_System_WindowsProgramming",
    "Win32_UI_Accessibility",
    "Win32_UI_Controls",
    "Win32_UI_HiDpi",
    "Win32_UI_Input_Ime",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_Input_Pointer",
    "Win32_UI_Input_Touch",
    "Win32_UI_Shell",
    "Win32_UI_TextServices",
    "Win32_UI_WindowsAndMessaging",
]

# An empty table so this fork can be driven STANDALONE — `targo trust check -p winit`
# and any cargo invocation with cwd inside vendor/winit/. Without it cargo exits 101
# ("current package believes it's in a workspace when it's not"). The other five
# forks carry the same stub.
[workspace]
